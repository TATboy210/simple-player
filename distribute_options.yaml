output: dist/
releases:
  - name: dev
    jobs:
      - name: release-windows-msix
        package:
          platform: windows
          target: msix
          build_args:
            - --dart-define=USE_MOCK_ENGINE=false
