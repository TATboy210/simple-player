/// 媒体错误类型枚举 — UI 层根据类型选择操作按钮，不依赖字符串匹配
enum MediaErrorType {
  /// 文件相关错误（路径为空、文件不存在、路径无效）
  file,

  /// 编解码/格式错误（不支持的格式、解码失败）
  codec,

  /// 播放错误（播放失败、跳转失败）
  playback,

  /// 网络错误（URL 超时、连接失败、流中断）
  network,

  /// 未知/其他错误
  unknown,
}
