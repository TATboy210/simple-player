import 'dart:ui';

/// Shared desktop window geometry used by initialization and resize handling.
const defaultWindowSize = Size(1280, 752);

/// Smallest supported outer window geometry.
const minimumWindowSize = Size(854, 513);
