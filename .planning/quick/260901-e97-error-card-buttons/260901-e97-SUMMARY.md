---
phase: quick-260901-e97
plan: 01
status: complete
subsystem: ui
tags: [error-card, diagnostics, l10n, explorer-exe, process-start, osd, tdd]

# Dependency graph
requires:
  - phase: Phase 3 (错误卡片)
    provides: ErrorCard 折叠/展开纯呈现 widget、error-card-copy 复制按钮（formatDiagnosticPack 单一来源）、CARD-04 失败隔离形态
  - phase: Phase 3 (宿主接线)
    provides: ErrorCardHost 呈现接线人、OsdService OSD 单例、KernelLogger isInitialized 探针守卫（WR-02）
  - phase: Phase 2 (文件证据)
    provides: ErrorReporterImpl.diagnosticLogPath（DiagnosticLogStatus → 当前生效 error.log 路径单一读取路径）
provides:
  - 错误卡片折叠态「打开日志」按钮（error-card-open-log，条件渲染，无需展开即可定位日志）
  - 宿主 _openLog 全链路：error-card-open-log → _onOpenLog → _openLog → logExplorer 缝 → Process.start('explorer.exe', ['/select,', path])
  - logExplorer 仅测试注入缝（生产挂载点 const ErrorCardHost() 零参不变）
  - 3 个新 l10n key（errorCardOpenLogTooltip / errorCardLogOpened / errorCardOpenLogFailed，en+zh）
  - 6 个新 widget 测试用例（卡片 3 + 宿主 3），TDD RED→GREEN 双门各两组
affects: [错误反馈闭环, error-card 后续动作扩展, UAT-错误卡片]

# Actuals (#2632)
actuals:
  tokens: 13000 # chars/4 over the realized diff (52147 chars, 9 files)
  tasks: 3
  commits: 5

# Tech tracking
tech-stack:
  added: [] # 零新增依赖（dart:io Process 标准库）
  patterns:
    - "进程启动测试缝：Future<void> Function(String)? 构造参数注入，生产默认顶层函数，widget 测试记录型假缝（绝不拉起真实进程）"
    - "explorer.exe /select, 列表参数形态启动（argList，无 shell 无字符串拼接）"

key-files:
  created: []
  modified:
    - lib/ui/player/error_card.dart
    - lib/ui/player/error_card_host.dart
    - lib/l10n/app_en.arb
    - lib/l10n/app_zh.arb
    - lib/l10n/app_localizations.dart
    - lib/l10n/app_localizations_en.dart
    - lib/l10n/app_localizations_zh.dart
    - test/widget/player/error_card_test.dart
    - test/widget/player/error_card_host_test.dart

key-decisions:
  - "不新增复制实现：折叠态复制需求由既有 error-card-copy 按钮（formatDiagnosticPack 单一来源）完整满足，本任务零复制路径新增（LOG-05 单一来源保持）"
  - "打开日志用 dart:io Process.start 列表参数形态（T-E97-01 缓解）：无 shell、无字符串拼接，路径不参与命令行拼装；零新增依赖"
  - "logExplorer 为仅测试注入缝：生产挂载点 app.dart 保持 const ErrorCardHost() 零参形态；三个宿主新用例全部注入假缝，严禁真实缝进测试"
  - "路径不可用降级复用既有 errorCardLogUnavailable key，不新增第四个 l10n key（计划钦定）"
  - "按钮不用 GlassButton（FocusableActionDetector 请求焦点破坏 CARD-01 零焦点抢占），与 copy/close 同一 GestureDetector + Semantics 纪律"

patterns-established:
  - "Process-start test seam: optional constructor param + production default top-level launcher; widget tests inject recording fakes"
  - "explorer.exe /select, argList form: Windows select-in-folder semantics without shell concat (T-E97-01 mitigation)"

requirements-completed: [] # quick task (260901-e97) — no ROADMAP requirement IDs

coverage:
  - id: D1
    description: 折叠态错误卡片暴露「打开日志」按钮，点击以 explorer.exe 定位当前生效 error.log
    verification:
      - kind: unit
        ref: "test/widget/player/error_card_test.dart#open-log 打开日志按钮（E97）"
        status: pass
      - kind: unit
        ref: "test/widget/player/error_card_host_test.dart#open-log 宿主接线与失败隔离（E97）"
        status: pass
  - id: D2
    description: 打开日志取值与展开区日志路径段同源（diagnosticLogPath.value 动作时刻读取），成功/失败/不可用三态 OSD 反馈
    verification:
      - kind: unit
        ref: "test/widget/player/error_card_host_test.dart#open-log passes diagnosticLogPath value to the seam and shows opened OSD"
        status: pass
      - kind: unit
        ref: "test/widget/player/error_card_host_test.dart#null diagnosticLogPath degrades to unavailable OSD with zero seam calls"
        status: pass
      - kind: unit
        ref: "test/widget/player/error_card_host_test.dart#ProcessException from the seam shows failed OSD without escaping"
        status: pass
  - id: D3
    description: 复制详情单一实现路径保持（formatDiagnosticPack 仅被 _copyDiagnosticPack 调用），卡片零复制路径新增
    verification:
      - kind: other
        ref: "grep error_card.dart formatDiagnosticPack → 1 doc mention + 1 call site"
        status: pass
---

# Phase Quick 260901-e97 Plan 01: 错误卡片「打开日志」按钮 + 「复制详情」单一实现锁定 Summary

**One-liner:** 错误卡片折叠态新增「打开日志」按钮（explorer.exe /select, 列表参数启动 + logExplorer 测试缝 + ProcessException 失败隔离），并锁定「复制详情」为既有 error-card-copy 单一实现路径。

## Accomplishments

- **Task 1（l10n）**：app_en.arb / app_zh.arb 新增 3 个 errorCard open-log key（tooltip/成功 OSD/失败 OSD），`flutter gen-l10n` 再生成三类文件，幂等重跑零 diff。
- **Task 2（TDD，RED 512f483a → GREEN 7cd24114）**：ErrorCard 新增 `onOpenLog` 可选参数与折叠态 `error-card-open-log` 按钮（Semantics + GestureDetector + Icons.folder_open，全 Token 来源）；3 个新用例锁死「折叠态存在 + 恰好一次回调 + CARD-04 命中隔离 + null 条件渲染」。
- **Task 3（TDD，RED f4a2e79f → GREEN 92d04f8f）**：宿主新增 `logExplorer` 测试缝、`_launchExplorerSelect` 生产启动器（`Process.start('explorer.exe', ['/select,', path])`）、`_openLog`（l10n 提前解析 / diagnosticLogPath 动作时刻取值 / null 路径降级 / typed ProcessException catch + 探针守卫的 kernel warn，日志 context 只记 executable+message）；3 个新用例锁死「取值同源逐字符一致 + 零缝调用降级 + ProcessException 零外溢」。

## Verification Evidence（acceptance_criteria HARD GATE）

| # | 准则 | 证明命令 | 结果 |
|---|------|----------|------|
| 1 | flutter gen-l10n 幂等（重跑无 diff） | `flutter gen-l10n && git status --short lib/l10n/`（末次为空） | PASS |
| 2 | flutter analyze 0 error（全项目） | `flutter analyze \| grep -cE "^\s*error -"` → `0`（60 个 info 级为预存，非本次触碰） | PASS |
| 3 | 两测试文件全绿（新用例 + 零回归） | `flutter test test/widget/player/error_card_test.dart test/widget/player/error_card_host_test.dart` → `+41: All tests passed!`（20 卡片 + 21 宿主，含 6 个 E97 新用例） | PASS |
| 4 | 卡内只有一条复制实现路径 | `grep -n formatDiagnosticPack lib/ui/player/error_card.dart` → 仅 1 处 doc 提及 + 1 处 `_copyDiagnosticPack` 调用点 | PASS |
| 5 | 可见树零完整路径渲染（T-03-05 不回归） | `full media path never rendered in visible tree` 用例在全量卡片套件中通过；新按钮只渲染 Icon 无任何路径文本 | PASS |

**TDD Gate Compliance:** RED→GREEN 双门齐备 —— Task 2: `test(260901-e97)` 512f483a（RED，编译失败 `No named parameter onOpenLog`）在 `feat(260901-e97)` 7cd24114（GREEN）之前；Task 3: `test(260901-e97)` f4a2e79f（RED，`const ErrorCardHost({super.key})` 无 logExplorer）在 `feat(260901-e97)` 92d04f8f（GREEN）之前。git log 顺序已核验。

## Deviations from Plan

**None** — plan executed exactly as written. 执行中的两次 GREEN 内迭代（首版 GREEN 漏把 `onOpenLog` 加进构造参数列表导致编译失败，补齐后全绿；宿主测试 fake 缺 `flutter/foundation.dart` 导入致 `ValueListenable` 不可见，补导入后全绿）均为任务内阻塞性修复，在各自提交前完成，无破坏性提交落地，不构成计划偏差。

**Total deviations:** 0
**Impact:** 无 —— 计划钦定的 API 形态（argList 启动、缝注入、l10n key 数量、条件渲染纪律）全部原样落地。

## Known Stubs

None — 无未接线的占位数据、无 TODO/FIXME 新增、无空值渲染链。

## Threat Flags

None — 本次引入的安全面（explorer.exe 参数传递、剪贴板完整路径）均在计划 `<threat_model>` 登记内（T-E97-01 已按 mitigate 落地为 argList 形态；T-E97-02 accept 维持；T-E97-03 已按 mitigate 落地为 typed catch + OSD 反馈）。

## Self-Check: PASSED

- 9 个触碰文件 + SUMMARY.md 全部存在（逐项 `[ -f ]` 核验通过）
- 5 个提交 hash 全部存在于 git log（24c47a64 / 512f483a / 7cd24114 / f4a2e79f / 92d04f8f）
- 禁入文件（.mcp.json / pubspec.yaml / pubspec.lock / .planning/*）零 staged，保持任务前原样
