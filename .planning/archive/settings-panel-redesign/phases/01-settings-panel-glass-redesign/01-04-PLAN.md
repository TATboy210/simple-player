---
phase: 01-settings-panel-glass-redesign
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - lib/ui/shared/settings_card.dart
  - lib/ui/shared/settings_expander_card.dart
  - lib/ui/shared/settings_action_card.dart
autonomous: false
requirements: [COMP-03, TRIG-01, TRIG-02, INTX-01, INTX-02, INTX-04]

must_haves:
  truths:
    - SettingsCard class is removed from codebase
    - SettingsExpanderCard class is removed from codebase
    - SettingsActionCard class is removed from codebase
    - All 7 tabs compile without importing SettingsCard/SettingsExpanderCard/SettingsActionCard
    - flutter analyze passes with no errors or new warnings
    - Settings panel opens and renders correctly (visual verification)
  artifacts:
    - lib/ui/shared/settings_card.dart (modified — only SettingRow, SettingSwitchRow, exports remain)
    - lib/ui/shared/settings_expander_card.dart (deleted)
    - lib/ui/shared/settings_action_card.dart (deleted)
  key_links:
    - settings_card.dart still exports SettingRow, SettingSwitchRow, SettingActionRow, SettingSliderRow
    - No tab file imports SettingsCard, SettingsExpanderCard, or SettingsActionCard
    - flutter analyze confirms zero references to removed classes

---

<objective>
Cleanup: remove dead SettingsCard, SettingsExpanderCard, and SettingsActionCard classes, verify full build.

Purpose: Complete the component layer simplification by removing the intermediate card classes that are no longer used by any tab. This fulfills COMP-03 and ensures the codebase is clean.
Output: Dead components removed, settings_card.dart reduced to row component exports, flutter analyze clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-settings-panel-glass-redesign/01-RESEARCH.md
@.planning/phases/01-settings-panel-glass-redesign/01-02-SUMMARY.md
@.planning/phases/01-settings-panel-glass-redesign/01-03-SUMMARY.md
@lib/ui/shared/settings_card.dart
@lib/ui/shared/settings_expander_card.dart
@lib/ui/shared/settings_action_card.dart
@lib/ui/theme/tokens.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove SettingsCard, SettingsExpanderCard, SettingsActionCard classes</name>
  <files>lib/ui/shared/settings_card.dart, lib/ui/shared/settings_expander_card.dart, lib/ui/shared/settings_action_card.dart</files>

  <read_first>
    lib/ui/shared/settings_card.dart
    lib/ui/shared/settings_expander_card.dart
    lib/ui/shared/settings_action_card.dart
    lib/ui/shared/setting_action_row.dart
    lib/ui/shared/setting_slider_row.dart
  </read_first>

  <action>
**Step 1: Verify no remaining imports of removed classes**

Before deleting, grep to confirm no tab file still imports `SettingsCard`, `SettingsExpanderCard`, or `SettingsActionCard`:
```bash
grep -r "SettingsCard\|SettingsExpanderCard\|SettingsActionCard" lib/ui/dialogs/
```
If any matches found, those tabs were not migrated in Plans 02/03 — fix them first.

**Step 2: Delete SettingsExpanderCard**

Delete `lib/ui/shared/settings_expander_card.dart` entirely. Per RESEARCH Pitfall 1: this class is dead code (no tab imports it).

**Step 3: Delete SettingsActionCard**

Delete `lib/ui/shared/settings_action_card.dart` entirely. Per RESEARCH Pattern 3: AboutTab now uses GlassContainer + InkWell + SettingRow instead.

**Step 4: Modify settings_card.dart**

Remove the `SettingsCard` class (lines 23-83) and its `cardDecoration` static field.

Keep in `settings_card.dart`:
- The file itself (don't delete it — it still hosts SettingRow, SettingSwitchRow)
- `SettingRow` class (lines 91-188)
- `SettingSwitchRow` class (lines 199-231)
- The `SectionHeader` import (if it was re-exported here, remove that — tabs import from `section_header.dart` directly)

Update the exports at the top of the file:
- Remove `export 'settings_action_card.dart';`
- Remove `export 'settings_expander_card.dart';`
- Keep `export 'setting_action_row.dart';`
- Keep `export 'setting_slider_row.dart';`

The final `settings_card.dart` should contain only:
1. Import for `tokens.dart`
2. Exports for `setting_action_row.dart` and `setting_slider_row.dart`
3. `SettingRow` class
4. `SettingSwitchRow` class

Per COMP-03 (remove SettingsCard/SettingsExpanderCard/SettingsActionCard intermediate layers).
  </action>

  <verify>
    <automated>grep -r "class SettingsCard " lib/ && echo "FAIL: SettingsCard still exists" || echo "PASS: SettingsCard removed"; grep -r "class SettingsExpanderCard " lib/ && echo "FAIL" || echo "PASS: SettingsExpanderCard removed"; grep -r "class SettingsActionCard " lib/ && echo "FAIL" || echo "PASS: SettingsActionCard removed"; flutter analyze lib/ui/shared/</automated>
  </verify>

  <acceptance_criteria>
    - `lib/ui/shared/settings_expander_card.dart` file does not exist
    - `lib/ui/shared/settings_action_card.dart` file does not exist
    - `lib/ui/shared/settings_card.dart` does NOT contain `class SettingsCard`
    - `lib/ui/shared/settings_card.dart` DOES contain `class SettingRow` and `class SettingSwitchRow`
    - `lib/ui/shared/settings_card.dart` exports `setting_action_row.dart` and `setting_slider_row.dart`
    - `lib/ui/shared/settings_card.dart` does NOT export `settings_action_card.dart` or `settings_expander_card.dart`
    - `grep -r "SettingsCard" lib/ui/dialogs/` returns no matches (no tab references the removed class)
    - `flutter analyze` passes with no errors on `lib/ui/shared/`
  </acceptance_criteria>

  <done>
    SettingsCard, SettingsExpanderCard, SettingsActionCard removed. settings_card.dart reduced to row component exports only.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — settings panel glass rendering</name>
  <files>none — visual verification only</files>

  <action>
Perform visual verification of the settings panel glassmorphism rendering:

1. Run `flutter run -d windows` to launch the app
2. Open the settings panel (left-click the settings button in the control bar)
3. Verify the panel shell has a glassmorphism background (blur effect visible behind the panel)
4. Verify the title bar has a glass background (not solid color)
5. Click through all 7 tabs (General, Equalizer, Audio, Video, Shortcuts, About, Performance)
6. Verify each tab section has a glass background with visible blur
7. Verify sidebar nav items have hover feedback (bgHover color on mouse enter)
8. Verify OK/Cancel/Apply buttons work (OK closes, Cancel closes, Apply stays open)
9. Verify panel can be dragged by the title bar
10. Right-click the settings button in the control bar — verify quick language/theme menu still appears
11. Run `flutter analyze` — verify no new warnings
  </action>

  <what-built>
Phase 1 complete: settings panel migrated from solid-color SettingsCard backgrounds to GlassContainer glassmorphism. Panel shell, sidebar nav, and all 7 tabs now use glass styling matching the control bar.
  </what-built>

  <how-to-verify>
1. Panel shell shows blur effect (BackdropFilter working)
2. All 7 tab sections show glass background
3. Sidebar nav hover feedback matches control bar
4. OK/Cancel/Apply buttons functional
5. Panel drag functional
6. Right-click quick menu functional
7. flutter analyze clean
  </how-to-verify>

  <verify>
Manual verification — no automated command for visual rendering.
  </verify>

  <done>
Human confirms settings panel glass rendering matches control bar style, all interactions work, flutter analyze clean.
  </done>

  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure UI visual refactor, no security-sensitive code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No external packages installed — all components are project-local |
</threat_model>

<verification>
1. `flutter analyze` passes with no errors or new warnings across entire project
2. `grep -r "SettingsCard\|SettingsExpanderCard\|SettingsActionCard" lib/` returns no class definitions
3. Settings panel opens and renders with glassmorphism background
4. All 7 tabs display with glass section containers
5. OK/Cancel/Apply delayed apply mode works
6. Left-click opens panel, right-click opens quick menu
7. Panel drag works
8. ESC key closes panel / cancels shortcut recording
</verification>

<success_criteria>
- SettingsCard, SettingsExpanderCard, SettingsActionCard classes fully removed
- settings_card.dart contains only SettingRow, SettingSwitchRow, and row component exports
- All 7 tabs compile and render correctly with GlassContainer
- flutter analyze clean across entire project
- Human visual verification of glass rendering confirms glassmorphism effect
</success_criteria>

<output>
Create `.planning/phases/01-settings-panel-glass-redesign/01-04-SUMMARY.md` when done
</output>
