---
context: startup-optimization-v2
task: 5
total_tasks: 6
status: paused
last_updated: 2026-05-25T18:30:00+08:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: FFI 对象不能跨 Isolate — mdk.Player()/RustLib.init() 绑定主 Isolate，不要用 Isolate.run/compute 包装
- [ ] CONSTRAINT: Flutter CLI 路径 — 不在 PATH，必须用 `/d/flutter/bin/flutter`

<current_state>
Phase 1 (Opt 2+5) 完成: StartupState/Coordinator/ProgressSplashScreen + 全链路上报。
Phase 2 (Opt 1/3/4) 待做: Task #5 lazy FvpEngine (最大收益 50-150ms), Task #6 prewarm tiers + isolate。
7 个文件未提交 (3 new + 4 modified)。dart analyze 零错误。
</current_state>

<completed_work>
- Task 1: Create StartupState + StartupCoordinator — Done
- Task 2: Create ProgressSplashScreen widget — Done
- Task 3: Wire StartupCoordinator into main.dart + app.dart — Done
- Task 4: Report phases from deferred_player_feature + player_feature — Done
</completed_work>

<remaining_work>
- Task 5: Lazy FvpEngine player creation — fvp_engine.dart:29 `final mdk.Player _player = mdk.Player()` → lazy `_playerInstance ??= _createPlayer()`. 20+ references. P1, 50-150ms impact.
- Task 6: EnginePrewarm tier flags + PlaylistStore.loadInBackground() with Isolate.run(). P2, 5-20ms impact.
</remaining_work>

<decisions_made>
- FFI 对象不能跨 Isolate — 只有 PlaylistStore.load() 和 FolderScanner.scan() 可用 Isolate.run()
- Splash 由 App 层统一驱动 — 移除 DeferredPlayerFeature 重复 SplashScreen
- StartupCoordinator 用 ValueNotifier — 符合项目无 Provider/Riverpod 约定
- AnimatedSwitcher 300ms — splash → MaterialApp 平滑 fade
</decisions_made>

<blockers>
None
</blockers>

## Required Reading
1. `lib/kernel/engine/fvp_engine.dart` — Task #5 目标，需确认所有 `_player` 引用
2. `lib/kernel/engine/engine_prewarm.dart` — Task #6 目标
3. `C:\Users\35490\.claude\plans\graceful-snacking-comet.md` — 完整优化计划

## Infrastructure State
- Flutter: D:\flutter\bin\flutter
- dart analyze: 零错误
- 新文件: lib/kernel/startup/startup_state.dart, startup_coordinator.dart, lib/ui/shared/progress_splash_screen.dart

<context>
启动优化 V2。Phase 1 完成 (状态机 + 进度 Splash + 全链路上报)。
Phase 2 最大收益是 Task #5: 将 FvpEngine 的 mdk.Player() 从字段初始化器改为 open() 时懒创建。
这是侵入性重构 — 20+ 引用需通过 lazy getter 访问，需要 _ensurePlayer() guard。
</context>

<next_action>
Read `lib/kernel/engine/fvp_engine.dart` 全文，识别所有 `_player` 引用，
改为 lazy pattern:
```dart
mdk.Player? _playerInstance;
mdk.Player get _player => _playerInstance ??= _createPlayer();
```
</next_action>
