---
context: phase
phase: architecture-simplification
task: 1
total_tasks: 3
status: completed
last_updated: 2026-05-25T05:00:00+08:00
---

<current_state>
Phase 1 (VideoProcessingState) 完成。Phase 2 (PlaybackRuntime) 和 Phase 3 (Window 清理) 未开始。
</current_state>

<completed_work>
- Task 1: VideoProcessingState — 7 notifiers → 1 immutable state ✅
  - Created `lib/features/player/models/video_processing_state.dart` (~65 lines)
  - Rewrote `lib/features/player/services/video_processing_service.dart` (132→105 lines)
  - Rewrote `lib/ui/dialogs/settings/video_tab.dart` — single `ValueListenableBuilder<VideoProcessingState>`, custom `_VideoSlider`, `_BoolNotifier` bridge
  - `flutter analyze` 零错误
</completed_work>

<remaining_work>
- Task 2: PlaybackRuntime — mixin → composition
  - Create `playback_runtime.dart` (~20 lines, module holder)
  - Rewrite `playback_navigator.dart`, `file_operations.dart`, `state_monitor.dart` as standalone classes (constructor-injected `_rt`)
  - Rewrite `playback_controller.dart` as thin façade (~30 lines)
  - Delete `playback_contract.dart`
  - UI 层零修改（PlaybackController 公开 API 不变）

- Task 3: Window Layer cleanup
  - Remove 3 stubs from `window_service.dart` (_configureLinux, _configureMacOS, hideCursor)
  - Move `aspect_ratio_service.dart` from `kernel/window/` to `window/`, add `dispose()`
  - Eliminate dual-write state pattern (rely solely on _WindowListener)
</remaining_work>

<decisions_made>
- VideoProcessingState: immutable `@immutable` class with `copyWith`, `==`, `hashCode`
- Single `ValueNotifier<VideoProcessingState>` replaces 7 notifiers
- VideoTab uses custom `_VideoSlider` widget instead of generic `SettingSliderRow` (avoids adapter overhead)
- `_BoolNotifier` bridge for `SettingSwitchRow` compatibility
- PlaybackRuntime: composition over mixin, each capability gets `_rt` reference via constructor
- PlaybackController remains as thin façade (UI doesn't know about Runtime)
</decisions_made>

<next_action>
Start Phase 2: Read current mixin files, create `PlaybackRuntime`, rewrite 3 capabilities as standalone classes, rewrite `PlaybackController` as façade, delete `playback_contract.dart`.
</next_action>
