---
context: v1.6-rendering-perf
phase: widget-layer-perf
task: 0
total_tasks: 5
status: planned
last_updated: "2026-06-26T00:00:00.000Z"
---

# v1.6 Phase 16: Widget 层优化 — 暂停点

## 当前状态

PLAN.md 已创建，5 个任务未开始。R3-2（AuroraBackground Ticker）确认已完成，跳过。

## 已完成

- [x] v1.6 里程碑初始化（PROJECT/REQUIREMENTS/ROADMAP/STATE）
- [x] codebase 全量刷新（7 个文件，4 并行 agent）
- [x] Phase 16 研究 + PLAN.md 编写
- [x] 确认 R3-2 已完成（`_syncTicker()` 在非 idle 时暂停）

## 待执行（Task 1-5）

### Task 1: PlayerScreen RepaintBoundary 隔离
- 文件: `lib/ui/player/player_screen.dart`
- Stack 的 videoContent 和 PlaylistPanel 分别包裹 RepaintBoundary

### Task 2: Progress Bar Opacity 修复
- 文件: `lib/ui/player/progress_bar.dart:313`
- `Opacity(` → `AnimatedOpacity` 或 `Visibility`

### Task 3: VLB 嵌套扁平化
- 文件: `lib/ui/player/player_screen.dart`
- 合并 playlistVisible + playlistMounted 为单一 VLB

### Task 4: 硬编码颜色迁移（最大工作量）
- edge_glow.dart (15+) → Tokens.glowStops
- control_bar.dart (10) → Tokens.controlBar*
- app.dart (7) → Tokens.accentBlue / textSecondary
- aurora_background.dart (5) → Tokens.auroraBlobColors
- 其他 4 个文件各 1-2 处

### Task 5: 测试验证
- 现有测试通过 + 新增 RepaintBoundary 测试 + 120fps 验证

## 关键发现

- AuroraBackground Ticker 优化已存在（`_syncTicker()` 检查 `engineState != idle`）
- Opacity 仅 3 处使用，2 处已是 AnimatedOpacity
- 硬编码颜色 ~40 处违规，edge_glow.dart 最严重
- ValueListenableBuilder 33 处，PlayerScreen 4 层嵌套

## 恢复指令

```
/gsd-resume-work
```

然后 `/gsd-run-phase` 执行 Task 1-5。
