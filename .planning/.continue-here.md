---
context: research
phase: 03-performance-optimization
task: 0
total_tasks: 0
status: research_complete
last_updated: 2026-05-29T05:00:00+08:00
---

# Continue Here

**Paused:** 2026-05-29 05:00 GMT+8
**Reason:** Window management research complete, ready to implement nativeapi migration

## What Was Done This Session

1. **Resumed from previous session** — Phase 3 gap closure complete, fix/window-startup branch
2. **Security audit** — Window management code reviewed: 0 CRITICAL/HIGH, 1 MEDIUM (NaN/Inf UB), 5 positive findings
3. **Window frameless anti-patterns documented** — 3 failed C++ approaches saved to memory as cautionary reference
4. **nativeapi research** — v0.1.3 (pub 2026-05-26), FFI to libnativeapi C library, Window class with titleBarStyle=hidden, setSize, center, maximize, minimize, VisualEffect (blur/acrylic/mica)
5. **Compared 3 approaches** — nativeapi (97⭐ WIP) vs window_manager (836⭐ mature) vs custom C++ (flawed)
6. **User decision: use nativeapi** — FFI bypasses Flutter HandleTopLevelWindowProc, solves root cause

## Root Cause Analysis (Why Custom C++ Failed)

| Problem | Root Cause |
|---------|-----------|
| WM_NCHITTEST resize not working | Flutter's HandleTopLevelWindowProc intercepts before custom handler |
| DWM animation lost | Removing WS_CAPTION breaks DWM animation pipeline |
| Top edge cursor lost | WM_NCCALCSIZE rgrc[0].top+=1 insufficient with Flutter interception |

## Current State

- **Branch:** fix/window-startup
- **Phase:** 3 complete, window management migration pending
- **Progress:** Phase 1+2+3 done, window research done
- **Uncommitted:** player_services.dart (setFrameless(true))

## Next Step

**Primary:** Implement nativeapi migration:
1. `flutter pub add nativeapi` (v0.1.3)
2. Rewrite `WindowService` to use nativeapi FFI (replace MethodChannel)
3. Remove `windows/runner/window_channel.cpp` + `.h`
4. Remove MethodChannel registration from `flutter_window.cpp`
5. Update `player_services.dart` startup sequence
6. Test: frameless + resize + DWM animation + rounded corners

## Key Files to Modify

| File | Action |
|------|--------|
| `pubspec.yaml` | Add nativeapi dependency |
| `lib/kernel/bridge/window_service.dart` | Rewrite to use nativeapi Window API |
| `windows/runner/window_channel.cpp` | DELETE |
| `windows/runner/window_channel.h` | DELETE |
| `windows/runner/flutter_window.cpp` | Remove WindowChannel registration |
| `windows/runner/flutter_window.h` | Remove WindowChannel member |
| `lib/features/player/player_services.dart` | Update startup sequence |
| `windows/runner/CMakeLists.txt` | Remove window_channel source files |

## Infrastructure State

- Flutter: D:\flutter\bin\flutter (not in PATH)
- Platform: Windows 11
- Git branch: fix/window-startup
- Test suite: 312 tests, all passing
- No running background processes
