---
context: default
phase: player-feature-refactor
task: 3
total_tasks: 7
status: paused
last_updated: 2026-05-25T04:15:00+08:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Plan file is at `C:\Users\35490\.claude\plans\transient-juggling-brook.md` — read it for full 7-phase plan details
- [ ] CONSTRAINT: Flutter CLI not in PATH — use `D:\flutter\bin\flutter` or ask user to run `flutter analyze` manually
- [ ] CONSTRAINT: No Riverpod/BLoC — ValueNotifier + ValueListenableBuilder only

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| God Object migration | Moving all responsibilities to PlayerFeature defeats the purpose | blocking | PlayerServices (lifecycle) + PlayerFeature (UI state) must be separate files |
| Re-export left behind | Forgetting to delete re-export shims after import updates | advisory | Phase 7 explicitly handles cleanup |

<current_state>
Phase 3 of 7 complete. 7 files in `lib/features/player/services/` (5 playback + video_processing + subtitle_service). 7 re-export shims in `lib/kernel/services/`. SubtitleService unified with `detectAndLoadSync()` wired into PlaybackNavigator via abstract `subtitleService` getter. `flutter analyze` passes (0 errors).
</current_state>

<completed_work>

Completed Tasks:
- Task 35: Phase 1: Extract AppBootstrap — Done (uncommitted)
- Task 36: Phase 2: Move PlaybackController + mixins to features/player/ — Done (uncommitted)
- Task 37: Phase 3: Move VideoProcessing + unify SubtitleService — Done (uncommitted)

</completed_work>

<remaining_work>

- Task 38: Phase 4: Create PlayerServices (~60 lines) + PlayerFeature (~100 lines)
- Task 39: Phase 5: Slim app.dart from 340→~110 lines (remove player creation/callbacks)
- Task 40: Phase 6: Remove 3 derivable props from PlayerScreen (isVideo, onPrevious, onNext)
- Task 41: Phase 7: Delete re-export shims, update imports, update CLAUDE.md

</remaining_work>

<decisions_made>

- PlayerServices + PlayerFeature split: service creation/lifecycle separate from UI composition
- Settings stays in app.dart: SettingsPanel/QuickMenu need MaterialApp-level BuildContext
- Re-export strategy: old kernel/services/ files become one-line re-exports during migration
- SubtitleService sync method: detectAndLoadSync() for playIndex hot path, Directory.listSync() OK for small dirs
- SubtitleService wired via abstract getter: PlaybackNavigator has `SubtitleService? get subtitleService`, PlaybackController provides it as optional constructor param

</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state
2. `C:\Users\35490\.claude\plans\transient-juggling-brook.md` — full 7-phase plan with code examples

## Infrastructure State
- Flutter: `D:\flutter\bin\flutter`
- Git branch: `feature/performance-optimization`

## Files in features/player/services/ (new)
```
playback_controller.dart    # Orchestrator (3 mixins) + optional subtitleService
playback_contract.dart      # Abstract contract
playback_navigator.dart     # playIndex/Next/Prev, uses subtitleService
state_monitor.dart          # Lifecycle, auto-advance, persistence
file_operations.dart        # File open/add
video_processing_service.dart # 7 ValueNotifiers, engine delegation, persistence
subtitle_service.dart       # detectAndLoad + detectAndLoadSync, _matchSubtitle helper
```

## Re-export Shims in kernel/services/ (to delete in Phase 7)
```
playback_controller.dart → re-export
playback_contract.dart → re-export
playback_navigator.dart → re-export
state_monitor.dart → re-export
file_operations.dart → re-export
video_processing_service.dart → re-export
subtitle_service.dart → re-export
```

<context>
Plan approved by user. Phases 1-3 done (file moves complete). Phase 4 is the biggest: create PlayerServices (service lifecycle) and PlayerFeature (UI state + composition) to extract responsibilities from app.dart God Object. Phase 5 then slims app.dart to ~110 lines.
</context>

<next_action>
Phase 4: Create `lib/features/player/player_services.dart` (~60 lines — FvpEngine, Playlist, PlaybackController, VideoProcessingService creation + lifecycle) and `lib/features/player/player_feature.dart` (~100 lines — UI state, file picker, drag-drop, folder scan, PlayerScreen composition). Both files are new, no existing files modified yet.
</next_action>
