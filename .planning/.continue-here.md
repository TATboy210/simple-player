---
context: startup-optimization
task: 5
total_tasks: 5
status: completed_pending_verify
last_updated: 2026-05-25T07:00:00+08:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Flutter CLI 路径 — 不在 PATH，必须用 `/d/flutter/bin/flutter`
- [ ] CONSTRAINT: test/ 有 92 个预先存在的 AspectRatioService 路径错误 — 非本次引入，忽略

<current_state>
启动优化 4 Phase 全部实现，flutter analyze lib/ 零错误。等待手动验证。
</current_state>

<completed_work>

- Task 1: 并行化 Bootstrap — Done (app_bootstrap.dart: RustLib/SharedPreferences/WindowService 三者并行 + EnginePrewarm)
- Task 2: 删除重复 prewarm — Done (app.dart 删除冗余 prewarm)
- Task 3: 合并 SettingsStore.load — Done (StateMonitor.init 接收可选 settings 参数)
- Task 4: 品牌 Splash 替换 Spinner — Done (splash_screen.dart + app.dart + player_feature.dart)
- Task 5: 验证 — flutter analyze lib/ 零错误
</completed_work>

<remaining_work>

- 手动验证: `flutter run -d windows` 确认启动体验
- 提交代码
</remaining_work>

<decisions_made>

- 并行化: 三个 async 无依赖，Future.wait 并行，总延迟从 Σ→max
- EnginePrewarm: unawaited fire-and-forget，不阻塞 runApp
- PlayerFeature spinner → SizedBox.shrink()，避免双重 splash
</decisions_made>

## Required Reading
1. `lib/bootstrap/app_bootstrap.dart` — 并行化核心
2. `lib/ui/shared/splash_screen.dart` — 新建 splash
3. `lib/features/player/player_services.dart` — settings 去重

<context>
启动优化全部完成。核心: bootstrap 并行化 + EnginePrewarm + settings 去重 + 品牌 splash。
</context>

<next_action>
运行 `D:\flutter\bin\flutter run -d windows` 手动验证启动体验，确认后提交。
</next_action>
