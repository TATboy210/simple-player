---
context: default
phase: player-feature-refactor
task: 1
total_tasks: 7
status: paused
last_updated: 2026-05-25T03:30:00+08:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Plan file is at `C:\Users\35490\.claude\plans\transient-juggling-brook.md` — read it for full 7-phase plan details
- [ ] CONSTRAINT: Flutter CLI not in PATH — use `D:\flutter\bin\flutter` or ask user to run `flutter analyze` manually
- [ ] CONSTRAINT: No Riverpod/BLoC — ValueNotifier + ValueListenableBuilder only

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| God Object migration | Moving all responsibilities to PlayerFeature defeats the purpose | blocking | PlayerServices (lifecycle) + PlayerFeature (UI state) must be separate files |
| Re-export left behind | Forgetting to delete re-export shims after import updates | advisory | Phase 7 explicitly handles cleanup |

<current_state>
Phase 1 of 7 complete. AppBootstrap extracted to `lib/bootstrap/app_bootstrap.dart`, main.dart slimmed to 6 lines. Changes uncommitted.
</current_state>

<completed_work>

Completed Tasks:
- Task 35: Phase 1: Extract AppBootstrap — Done (uncommitted)

</completed_work>

<remaining_work>

- Task 36: Phase 2: Move 5 playback files from kernel/services/ to features/player/services/, old files become re-exports
- Task 37: Phase 3: Move VideoProcessingService + unify SubtitleService (add detectAndLoadSync)
- Task 38: Phase 4: Create PlayerServices (~60 lines) + PlayerFeature (~100 lines)
- Task 39: Phase 5: Slim app.dart from 340→~110 lines (remove player creation/callbacks)
- Task 40: Phase 6: Remove 3 derivable props from PlayerScreen (isVideo, onPrevious, onNext)
- Task 41: Phase 7: Delete re-export shims, update imports, update CLAUDE.md

</remaining_work>

<decisions_made>

- PlayerServices + PlayerFeature split: service creation/lifecycle separate from UI composition
- Settings stays in app.dart: SettingsPanel/QuickMenu need MaterialApp-level BuildContext
- Re-export strategy: old kernel/services/ files become one-line re-exports during migration
- kernel/ keeps: engine, models, persistence, playlist, scanner, path_validator, thumbnail_*
- features/player/ gets: playback_controller, mixins, subtitle_service, video_processing_service

</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state
2. `C:\Users\35490\.claude\plans\transient-juggling-brook.md` — full 7-phase plan with code examples

## Infrastructure State
- Flutter: `D:\flutter\bin\flutter`
- Git branch: `feature/performance-optimization`
- 53 uncommitted files on disk (from prior work + Phase 1)

<context>
Plan approved by user. Phase 1 done (AppBootstrap extraction). The plan splits app.dart God Object into 5 focused modules: AppBootstrap (init), app.dart (MaterialApp+Settings), PlayerServices (service lifecycle), PlayerFeature (UI state), PlayerScreen (UI composition). Each phase is independently compilable.
</context>

<next_action>
Run `flutter analyze` to verify Phase 1, then start Phase 2: create `lib/features/player/services/` directory and move 5 files (playback_controller, playback_contract, playback_navigator, state_monitor, file_operations) from `lib/kernel/services/`. Old files become re-exports.
</next_action>
