---
context: phase
phase: 6
phase_name: Resize 接线修复 (CB-06)
task: 0
total_tasks: 9
status: planned
last_updated: 2026-07-08T09:20:42.833Z
---

# Phase 6: Resize 接线修复 (CB-06)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 接线断路 | 设计参数存在但中间层未透传（ProgressBar.resizing 从未被传入） | advisory | 改动后 grep 验证参数传递链完整 |
| 接口未调用 | AutoHideController.resizing setter 存在但未被调用 | advisory | 改动后验证 setter 被 _onResizeChanged 触发 |

## Current State

Phase 1-3, 5 已完成。Phase 6（resize 接线修复）已规划完毕，代码改动尚未开始。

两个 Agent 已完成深度分析，确认了 7 处代码改动 + 2 处注释的最小修复方案。

## Completed Work (本 session)

- ✅ Agent 1 分析 P0：resizing 断路 + AutoHideController 未同步
- ✅ Agent 2 分析 P1/P2：注释补全 + 断点常量确认无需改动
- ✅ PROJECT.md 更新：CB-06 加入 Active 需求
- ✅ REQUIREMENTS.md 更新：CB-06a~f 共 6 条需求
- ✅ ROADMAP.md 更新：Phase 6 含 9 项改动清单 + 4 条验收标准

## Remaining Work

全部 9 项改动待实施：

**A — ControlBar 传递 resizing（control_bar.dart）:**
- A1: 新增 `final ValueListenable<bool>? resizing;` 字段
- A2: 构造函数新增 `this.resizing`
- A3: `ProgressBar(engine: engine, resizing: resizing)`

**B — Overlay 同步 AutoHideController（controls_overlay.dart）:**
- B1: `_onResizeChanged()` 开头加 `_autoHide.resizing = resizing;`
- B2: `initState` L109 后加 `if (widget.resizing?.value == true) _onResizeChanged();`
- B3: `didUpdateWidget` L173 后加 `_onResizeChanged();`

**C — Overlay 传递 resizing 给 ControlBar:**
- C1: `ControlBar(... resizing: widget.resizing)`

**D — 注释（P1）:**
- D1: `_cachedCustomPaint` doc comment
- D2: `_handleTap` 空回调注释

## Decisions Made

- 使用最小改动方案（7 处代码 + 2 处注释），不改架构
- 不修改 AutoHideController 内部逻辑，setter 已正确实现
- P2 断点常量已确认在 tokens.dart 中，无需改动

## Required Reading (in order)

1. `.planning/ROADMAP.md` — Phase 6 完整改动清单和验收标准
2. `lib/ui/player/control_bar.dart` — 需新增 resizing 字段
3. `lib/ui/player/controls_overlay.dart` — 需同步 AutoHideController + 传参
4. `lib/ui/player/progress_bar.dart` — 已有 resizing 支持，需加注释
5. `lib/ui/player/auto_hide_controller.dart` — resizing setter 已就绪

## Next Action

执行 `/gsd-plan-phase 6` 生成 PLAN.md，然后按 A1→A2→A3→B1→B2→B3→C1→D1→D2 顺序实施改动。

或直接手动按 ROADMAP.md Phase 6 清单逐项编辑代码。
