---
context: phase
phase: rust-mpv-bridge
task: 7
total_tasks: 9
status: completed
last_updated: 2026-05-24T19:40:00+08:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Rust toolchain PATH — `rustc`/`cargo` not in default PATH, must use `export PATH="$HOME/.cargo/bin:$PATH"` before any cargo/rustup command
- [ ] CONSTRAINT: libmpv 链接路径 — cargokit 在临时目录运行 cargo，`.cargo/config.toml` 不生效，必须用 `build.rs` 的 `cargo:rustc-link-search`
- [ ] CONSTRAINT: libmpv DLL 部署 — `libmpv-2.dll` 必须复制到 `build/windows/x64/runner/Debug/`
- [ ] CONSTRAINT: FRB 无法生成 `Arc<Mutex<Mpv>>` 参数 — 用 `OnceLock` 全局 handle 绕过，事件循环和命令方法共享同一 handle
- [ ] CONSTRAINT: FRB 扫描 `rust/src/api/` — 内部模块（event_loop.rs）必须放在 api/ 外面，否则 FRB 会生成不需要的绑定

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| .cargo/config.toml for cargokit | cargokit runs cargo from temp dir, config.toml ignored | blocking | Use build.rs `cargo:rustc-link-search` |
| FRB + complex generics as params | `Arc<Mutex<Mpv>>` in function params → codegen fails | blocking | Use global OnceLock, keep FRB-facing types simple |
| FRB scanning internal modules | Internal helpers in api/ get unwanted bindings | blocking | Put internal modules outside api/ directory |

<current_state>
Phase 3 (状态同步 + 事件流) 完成。PlayerEvent enum + startEventLoop + MpvEngine 全部就绪。Flutter build windows 成功。vo=null 仍在生效。下一步是 Phase 4 视频渲染。
</current_state>

<completed_work>

Completed Tasks:
- Task 1: FRB 脚手架集成
- Task 2: Rust 工具链安装
- Task 3: 列表性能优化
- Task 4: fvp Query Fence
- Task 5: 验证 Flutter↔Rust 调用
- Task 6: Player 命令 API — MpvPlayer 13 methods
- Task 7: 状态同步 + 事件流 — PlayerEvent enum, event_loop.rs, MpvEngine

</completed_work>

<remaining_work>

- Task 8: 视频渲染 — mpv_render_context → Flutter Texture
- Task 9: HDR + 手柄 + 跨平台

</remaining_work>

<decisions_made>

- OnceLock<Arc<Mutex<Mpv>>> 全局 handle — FRB 无法生成复杂泛型参数代码
- event_loop.rs 放在 api/ 外 — 避免 FRB 扫描内部模块
- wait_event(0.1) 短超时 — 每 100ms 释放锁让命令方法执行
- stream_dart_await — Dart await 直接获取 Stream

</decisions_made>

<blockers>
- 无技术阻塞。
</blockers>

## Required Reading (in order)
1. `rust/src/api/events.rs` — PlayerEvent enum + startEventLoop FRB 入口
2. `rust/src/event_loop.rs` — 全局 Mpv handle + 事件循环实现
3. `rust/src/api/player.rs` — MpvPlayer with_mpv helper
4. `lib/kernel/engine/mpv_engine.dart` — MpvEngine implements MediaEngine
5. `lib/src/rust/api/events.dart` — FRB 生成的 Dart Stream 绑定

## Infrastructure State
- Rust: 1.95.0 stable, PATH: `export PATH="$HOME/.cargo/bin:$PATH"`
- libmpv: C:\libmpv-dev\mpv\ (libmpv-2.dll + mpv.lib + headers)
- flutter_rust_bridge_codegen: v2.12.0
- FRB deps: freezed, freezed_annotation, json_annotation, build_runner (added this session)
- 构建: `export PATH="$HOME/.cargo/bin:$PATH" && D:/flutter/bin/flutter build windows --debug`
- DLL: `cp C:/libmpv-dev/mpv/libmpv-2.dll D:/simple_player_flutter/build/windows/x64/runner/Debug/`

<context>
Phase 3 完成。核心架构: Rust event_loop.rs 用 OnceLock 共享 Mpv handle，100ms 轮询 wait_event，PropertyChange 映射到 PlayerEvent，通过 FRB StreamSink 推送到 Dart Stream。MpvEngine 用 switch (event) pattern matching 更新 ValueNotifiers。

Phase 4 视频渲染是最复杂的阶段: 需要 mpv_render_context + Flutter Texture 注册。Windows 用 D3D11 或 ANGLE(OpenGL)。vo=null 需要改为 vo=gpu 或 vo=gpu-next。
</context>

<next_action>
1. 研究 Flutter Texture widget 的 texture 注册机制 (FlutterDesktopGpuSurfaceTexture 或 TextureRegistry)
2. 在 Rust 侧用 mpv_render_context_create 创建渲染上下文
3. 将 mpv 解码帧输出到 Flutter 共享纹理
4. 移除 vo=null，启用 vo=gpu-next
</next_action>
