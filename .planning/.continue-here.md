---
context: mid-phase
phase: 5
task: technical-debt-cleanup
total_tasks: 5
status: in-progress
last_updated: 2026-05-29T22:00:00+08:00
---

# Continue Here

**Paused:** 2026-05-29 22:00 GMT+8
**Reason:** Context exhaustion — Wave 1 done, Wave 2-4 pending

## What Was Done This Session

### Wave 1: Dead Code Cleanup + Type Safety ✓

1. **删除重复 `formatMs()`** — `lib/utils/time_utils.dart` 已删除
   - 零导入引用确认（3 个消费者均用 `kernel/utils/time_utils.dart`）
   - 空目录 `lib/utils/` 已删除

2. **修复 unsafe casts** — 3 文件改动：
   - `deferred_player_feature.dart` — `Object?` → `MediaEngine`/`VideoProcessingService?`，添加 import
   - `player_feature.dart` — `FvpEngine` → `MediaEngine`，import 从 `fvp_engine.dart` 改为 `media_engine.dart`
   - `app.dart:54-68` — `_showSettingsPanel` 参数改为 typed，删除 `as MediaEngine` 和 `as VideoProcessingService?` 强制转换

## Current State

- **Phase:** 5 (技术债务清理) — Wave 1 完成，Wave 2-4 待执行
- **Branch:** fix/window-startup
- **Tests:** 545 passing
- **Analysis:** 0 errors, 5 pre-existing info/warnings
- **Uncommitted:** Wave 1 changes (3 files modified, 1 file deleted)

## Next Steps (Wave 2-4)

### Wave 2: AppSettings copyWith/==/hashCode
- **文件:** `lib/kernel/persistence/settings_store.dart` — `AppSettings` 类
- 添加 `copyWith()` (nullable 字段用 sentinel pattern)
- 添加 `operator ==` (22 字段比较)
- 添加 `hashCode` (`Object.hash`)
- **测试:** `test/kernel/persistence/settings_store_test.dart` — 6 个测试用例

### Wave 3: 单例可测试性
- `thumbnail_service.dart` — 添加 `@visibleForTesting static void reset()`
- `engine_prewarm.dart` — 添加 `@visibleForTesting static void reset()`
- `perf_monitor.dart` — 添加 `@visibleForTesting void reset()`
- 3 个新测试文件

### Wave 4: 文档更新
- `CLAUDE.md` — 更正 thumbnail_service 描述
- `CONCERNS.md` — 标记 #6/#9/#11/#17 为 RESOLVED

## Resume Command

```
/gsd:resume-work
```

## Infrastructure

- Flutter: D:\flutter\bin\flutter
- Branch: fix/window-startup
- Tests: 545 passing
- Coverage: 80.0% (1569/1961)
