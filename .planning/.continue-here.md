---
context: default
phase: 32-navigation-interaction-polish
task: 0
total_tasks: 7
status: paused-pre-research
last_updated: 2026-07-28T08:22:49.907Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Steam Input 事件签名可分性未验证 —— Phase 32 规划前 MUST 先 research(ROADMAP 硬性要求 `--research-phase 32`)。核心回答:Flutter `Focus.onKeyEvent` 能否区分手柄 LB/RB(经 Steam Input 映射)vs 原生键盘 ←/→?未回答此问题前不得写 PLAN.md。
- [ ] CONSTRAINT: NAV-04(删 `gameButtonLeft1/Right1` 绑定)与 NAV-07(单一根 `Focus(onKeyEvent:_handleKeyEvent)`)是强耦合对 —— 必须同批落地于同一个 plan,否则 ←/→ 逃逸到 `KeyboardHandler` 触发 seek ±5s 回归。

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 跳过 research 直接写 PLAN | ROADMAP 明确要求 `--research-phase 32`;Steam Input 事件签名未验证前写 plan 会导致 NAV-04 删除 gameButton 绑定时引发 double-fire 回归 | blocking | research 阶段先验证事件签名可分性,research→plan-checker 通过后才进 execute |
| NAV-04/NAV-07 拆分到不同 plan | 中间状态会让 ←/→ 逃逸触发 seek 回归 | blocking | 两者必须在同一 32-0X-PLAN.md 同批落地 |

<current_state>
Phase 31 已完成(passed-with-note,2026-07-28,提交 `55f90724`+`ae8145b4`)。Phase 32「Navigation & Interaction Polish」尚未开始 —— `.planning/phases/` 下无 `32-*` 目录,ROADMAP 标注 `Plans: TBD — needs --research-phase 32`。本会话完成 Phase 32 前置探索,上下文 82% 耗尽后暂停,等待新会话走完整 GSD research→plan→execute。
</current_state>

<completed_work>

本会话(前置探索,无代码产出):
- Explore agent #1:摸清 v4.5 里程碑结构(Phase 28–34)+ Phase 32 完整定义(NAV-01..07,ROADMAP L130-146、REQUIREMENTS L39-47、research/SUMMARY L126-131)
- Explore agent #2:确认代码库就绪(Phase 28-31 全完成、测试无 skip、工作树仅 STATE.md 未提交)
- 产出 plan file:`C:/Users/35490/.claude/plans/32-snappy-lantern.md`(完整交接文档,含 7 项需求、风险、research 必答问题、关键文件路径、6 条验收标准)
</completed_work>

<remaining_work>

Phase 32 全部 7 项需求(NAV-01..07)未开始:
- NAV-01 tab 两端圆角竖向左右箭头按钮(持久可见+可点+键盘←/→切 tab)
- NAV-02 `InputModeDetector` singleton `ValueNotifier<InputMode>{keyboard,gamepad,auto}`(heuristic+toggle,**非** Steamworks FFI)—— v4.5 唯一新基础设施
- NAV-03 `AnimatedSwitcher` 提示置换(手柄 RB/LB 渐入 / 方向键渐退;键盘反向)
- NAV-04 删除 raw `gameButtonLeft1/Right1` 绑定(强耦合 NAV-07)
- NAV-05 选项列表上下薄毛玻璃箭头(`Container(color: Tokens.bgGlass)`,非第二层 blur)
- NAV-06 键盘↑/↓时上下箭头发光反馈
- NAV-07 单一 `Focus(onKeyEvent:_handleKeyEvent)` 根捕获所有方向键(强耦合 NAV-04)
</remaining_work>

<decisions_made>

- 暂停本会话,新会话走完整 GSD 流程(用户选择,因上下文 82% + Phase 32 高风险 + research 前置)
- NAV-04/NAV-07 强耦合,必须同批落地于同一 plan
- `InputModeDetector` 用 heuristic+toggle(`PointerMoveEvent`→keyboard;箭头键+5s 无鼠标→gamepad),非 Steamworks FFI(无 Flutter 适配层)
</decisions_made>

<blockers>
- Steam Input 事件签名可分性未验证(technical,research 阶段解决)
</blockers>

## Required Reading (in order)
1. `C:/Users/35490/.claude/plans/32-snappy-lantern.md` — 本会话产出的完整交接 plan(7 项需求、research 必答问题、关键文件路径、6 条验收标准)
2. `.planning/ROADMAP.md`(L130–146)— Phase 32 权威定义
3. `.planning/REQUIREMENTS.md`(L39–47)— NAV-01..07 需求原文
4. `.planning/research/SUMMARY.md`(L126–131)— 实施项
5. `.planning/phases/31-visual-design-alignment/VERIFICATION.md` — Phase 31 完成状态(Phase 32 依赖)
6. `.planning/METHODOLOGY.md`(若存在)— 项目分析透镜,假设分析前应用

## Critical Anti-Patterns (do NOT repeat these)
- 跳过 research 直接写 PLAN → research 阶段先验证 Steam Input 事件签名可分性 + InputModeDetector heuristic 信号可靠性 + 现有 gameButton 绑定影响面,通过 plan-checker 后才进 execute

## Infrastructure State
- 分支:`feat/v1.8-stability-polish-plan-02-02`(本地领先 origin 60 提交,未 push)
- 工作树:仅 `.planning/STATE.md` 未提交(上下文耗尽元数据,非代码)
- gsd-tools:`/c/Users/35490/.claude/gsd-core/bin/gsd-tools.cjs` 可用
- 附带清理候选(非阻塞):删 `lib/kernel/player_services.dart.bak`;同步 CLAUDE.md 架构树(漏记 `lib/features/`、`kernel/adapter|diagnostics|startup`、`ui/window/`(含迁出的 `custom_title_bar.dart`)、`ui/dialogs/settings/*`;Design System 节 tokens 路径应为 `lib/ui/theme/` 非 `kernel/ui/theme/`)

## Pre-Execution Critique Required
- Design artifact:`C:/Users/35490/.claude/plans/32-snappy-lantern.md`(交接 plan,非执行 plan)
- Critique focus:research 须验证 Steam Input 事件签名可分性 + InputModeDetector heuristic 信号可靠性 + 现有 gameButton 绑定影响面 + tab_strip 挂载点 + BackdropFilter 堆叠边界
- Gate:Do NOT begin execution until research→plan 完成

<context>
用户在 plan mode 下要求"继续探索32步骤"。经两个 Explore agent 摸清:Phase 32 是 v4.5 最高风险阶段,7 项需求,核心难点是 `InputModeDetector`(v4.5 唯一新基础设施)+ Steam Input 事件签名可分性。上下文 82% 耗尽,用户选择暂停走完整 GSD。plan file 已批准并保存。
</context>

<next_action>
Start with: 运行 `/gsd-resume-work` 恢复上下文,然后 `/gsd-phase 32 --research` 启动 research 阶段(核心必答:Steam Input LB/RB 与键盘 ←/→ 事件签名是否可分)。research 完成后 `/gsd-plan-phase 32` → `/gsd-execute-phase 32` → `/gsd-validate-phase 32`。
</next_action>
