---
context: research
phase: none
plan: none
task: 3
total_tasks: 4
status: paused
last_updated: 2026-07-16T09:35:00.000Z
---

# 当前状态

里程碑 **v3.0：内核重写（兼容式替换与诊断内核）** 定义中。`/gsd-new-milestone` 进行到 3/4：版本/范围确认✅、研究（4 研究员+1 综合器）✅、需求（40 REQ-ID）✅；待：归档旧 phases + 提交、路线图（roadmapper）。

**本会话被 Bash 分类器(glm-5.2)宕机阻塞**：无法跑 `gsd-tools`/`git`。但 Write/AskUserQuestion/Agent 子代理不受影响；子代理已成功提交研究文件（commits e4816d4/8de03d4/60fe9a6/3fbf21d）。下次会话 Bash 恢复后先补归档+提交，或委托子代理跑 git。

## 已完成工作（v3.0 定义）

- **PROJECT.md** 已写：Current Milestone v3.0 + 7 Active 需求 + 4 条 v3.0 Key Decisions + Validated 节含 v2.1 已验证能力。
- **STATE.md** frontmatter 已由 SDK `state.milestone-switch` 原子重置为 v3.0/planning（机器读取源正确；body H1 残留旧标题无害，留待首次阶段推进同步，勿手改）。
- **研究** `.planning/research/`：STACK/FEATURES/ARCHITECTURE/PITFALLS/SUMMARY 已写并各自提交。
- **REQUIREMENTS.md** 已写：40 条 REQ-ID，8 类别（BASE/ADAPT/LOG/ERR/MEM/STATE/VERIFY/DOC）对齐 8 阶段构建顺序；Traceability 待 roadmapper 回填。

## 研究校正的关键事实（路线图须遵守）

- `log.dart` **已 import `package:logger`+`path_provider`**，121 调用点/30 文件 → KernelLogger 是**替换迁移**（保留 `log*.w()` 调用形状），非全新门面。内核永不导入 `package:logger`（CI grep 闸门）。
- sealed `PlayerError` **已存在**（`lib/kernel/models/player_error.dart`）→ 错误模型是**稳化/扩展**，保留 `ValueNotifier<PlayerError?>` 契约，非新建。
- `openGeneration` 在 `fvp_engine.dart:194`，与 `EngineStateMachine` 分离 → Phase 6 须用 `OpenGenerationTracker` 统一。
- `EngineStateMachine` 静默 assert-only 忽略非法转换（release 无效）→ Phase 6 须换 `Result.err`+`KernelLogger` 警告。
- **9 态(PROJECT.md) vs 6 态(engine_state_machine.dart) 矛盾** → Phase 1 须核对决定冻结基线。
- `MemoryMonitor` 仅 2 静态调用（main.dart:16, debug_exporter.dart:57）→ 单例→实例在**一个原子提交**内完成（瞬态静态桥 shim+重写2调用+删 shim）。
- 适配层须**转发 `ValueNotifier` 实例**（非重新包装），否则 `ValueListenableBuilder` 监听器脱钩→cutover 时 UI 静默冻结。
- `debugPrint` **发布不剥离** → 门面须 `kDebugMode` 门控，warn/error 走 `dart:developer.log`。
- **适配层须先于状态重写存在**，否则替换引擎成禁断 big-bang swap。故构建顺序：契约冻结(P1)→适配层(P2)→KernelLogger(P3)→错误模型(P4)→MemoryMonitor(P5)→状态重写(P6)→验证收拢(P7)→双语文档(P8)。
- **过度工程化是项目宿敌** → 尺寸预算：适配层+门面+sealed错误+tracker 合计 < 旧 `FvpEngine`；适配层除 `KernelMode`+generation 计数器外无状态。

## 剩余工作

1. **Bash 恢复后**：`gsd_run query phases.clear --confirm`（归档旧 09/10/11/12/14 phases，非删除）+ `git add .planning/milestones/ .planning/phases/` + 提交 `docs: start milestone v3.0`。
2. **提交需求工件**：`gsd_run query commit "docs: define milestone v3.0 requirements" --files .planning/REQUIREMENTS.md`（PROJECT/STATE 若未在归档提交一起则另提交）。
3. **启动 gsd-roadmapper** 生成 `ROADMAP.md`（8 阶段，Phase 15 起续编号）+ 回填 REQUIREMENTS Traceability + 呈批批准 + 提交。
4. 若 Bash 仍宕机 → 把 git 操作委托给 Agent 子代理（已验证可行）。

## 决策

- **v3.0 版本号**：标记重大规划断点（推翻 v2.1 增量式计划）。
- **兼容式替换**：保持 UI→Kernel 契约，适配器逐步替换内核，降低一次性回归风险。
- **MemoryMonitor + 统一 logger 一等化**：诊断能力为内核一等公民。
- **零依赖 KernelLogger**：`lib/kernel/diagnostics/` 门面 + `dart:developer` + 受控 `debugPrint`（语义=内核解耦 `package:logger`，保留调用形状）。
- **双语注释**：公开 API 中文意图 + 英文契约。
- **续编号 Phase 15 起**：未传 `--reset-phase-numbers`，旧 09-14 归档非删除。
- **采用代码接地 8 阶段构建顺序**（适配层前置，契约冻结优先）。

## 阻塞项

- Bash 分类器(glm-5.2)宕机 → 无法跑 gsd-tools/git；Workaround：Bash 恢复后补，或委托子代理跑 git。

## 必须保留的未提交工作

工作树含有既有未提交代码和规划文档，包括：

- `.planning/STATE.md` 及多个 Phase 10/11/12/14 文档；
- `lib/kernel/models/track_preferences.dart`；
- `lib/kernel/services/track_preference_service.dart`；
- 多个新增测试，尤其 `test/kernel/utils/memory_monitor_test.dart`。

新里程碑初始化前必须先检查这些变更的来源与归属；不可因“清除计划”而删除或覆盖它们。

## 额外检查

- `.planning/phases/10-state-machine-extraction/10-01-SUMMARY.md` 命中 `TBD/placeholder` 扫描，恢复时检查该命中是否为真实占位内容；不得将它误判为已完成摘要。
- 没有发现运行中的 async job。

## 下一步

里程碑 v3.0 定义 3/4 完成，**v2.1 收尾已提交**（`76918ab` feat / `fd789eb` test / `94bf39b` docs）。工作树仅剩 5 个 v3.0 定义文档（PROJECT/STATE/REQUIREMENTS/HANDOFF/.continue-here，均 M）。

Bash 恢复后（glm-5.2 间歇宕机）完成归档+提交：
1. `node /c/Users/35490/.claude/gsd-core/bin/gsd-tools.cjs phases list` — 确认归档范围（应 09/10/11/12/14）。
2. `node .../gsd-tools.cjs phases clear --confirm` — 归档到 `.planning/milestones/`（**移动非删除**；若命令是删除则停止报告）。
3. `git add -A .planning/ && git commit -m "docs: start milestone v3.0 (kernel rewrite: compatible replacement + diagnostic kernel)"` — 提交归档+v3.0 文档（不加 attribution，项目已禁用）。
4. 启动 `/gsd-roadmapper` 生成 8 阶段 ROADMAP.md（Phase 15 起续编号，须遵守 8 项 blocking constraints）。

**gsd-tools 语法校正**：正确调用 `gsd-tools.cjs phases clear`（子命令 list/clear），非旧记 `gsd_run query phases.clear`。若 Bash 仍宕机，归档+提交委托 Agent 子代理（已验证可行，但本次 Agent 亦被同一分类器阻塞，需待分类器恢复）。详见 `.planning/HANDOFF.json` 的 `blocking_constraints`。
