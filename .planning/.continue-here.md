---
context: v1.7-wave2-architecture-refactoring
status: in_progress
last_updated: 2026-06-26
---

## 当前状态

**Milestone:** v1.7 — Stability, Architecture & Cross-Platform Prep
**Wave:** 2 — Architecture Refactoring (修订版)
**进度:** Wave 1 ✅ 完成 + Wave 2 首个修复完成
**最新提交:** cffb0e5

## 已完成工作 (本次会话)

### Wave 1: R1-5 Magic Numbers ✅
- `e15cd6e` refactor: R1-5 extract magic numbers to named constants in SettingsStore
- SettingsStore 新增 18 个 static const 字段
- 替换 ~30 处内联魔数（窗口尺寸/字幕/视频效果/主题/语言边界）

### Wave 2: open() 状态契约修复 ✅
- `cffb0e5` fix: open() success path missing state=idle
- FvpEngine.open() 成功路径缺失 `state.value = MediaState.idle`，违反基类契约
- 3 个 Agent 并行审核发现此 MUST_FIX 问题

## 三维度审核结论 (Wave 2 修订)

### 架构审核发现
- **R2-1/R2-2/R2-3 模块已提取但未集成** — `network_configurator.dart`(84行)、`media_opener.dart`(166行)、`video_effect_controller.dart`(51行)、`open_result.dart`(22行) 已存在，但 FvpEngine 未导入使用
- **R2-5 Static→Instance 不建议执行** — SettingsStore 保持 static (13文件导入)，PlaylistStore 可选 (2文件导入，但 Isolate.run 入口限制)

### 代码审核发现
- ✅ 零 bang operator、一致 on Exception、干净 dispose 模式
- 🟡 `_guardedAction` 始终设 `_errorType = playback`（语义错误）
- 🟡 `_configureNetworkOptions` 6 个 if-block DRY 违规
- 🟡 `_createPlayer()` late 字段无错误处理
- 🟡 `open()` 162 行 6 职责混合

## 待办事项 (修订后 Wave 2)

1. **集成 NetworkConfigurator → FvpEngine**
   - 删除 `_configureNetworkOptions()` (L186-232)
   - 委托给 `NetworkConfigurator.configure(player, url)`
   - 同时合并 DRY 违规（RTMP/SRT/UDP/TCP 重复配置）

2. **集成 MediaOpener → FvpEngine**
   - 删除 `open()` 中的重复逻辑 (L249-411)
   - 委托给 `MediaOpener.open()` + `OpenResult` sealed class
   - 保留 `_isOpening` 守卫和 `_disposed` 检查

3. **集成 VideoEffectController → FvpEngine**
   - 删除视频效果方法 (L626-670)
   - 委托给 `VideoEffectController`

4. **补充提取模块单元测试**
   - NetworkConfigurator: 每个协议分支
   - MediaOpener: 所有错误路径
   - VideoEffectController: 值 clamping + 角度验证

5. **R2-4 精简版**
   - 仅提取 SettingsValidator（sanitize 逻辑）
   - 跳过 progress_bar.dart 和 control_bar.dart（<500行且内聚）

## 关键决策

- **D-W2-1:** R2-5 Static→Instance 降级 — SettingsStore 保持 static，PlaylistStore 可选
- **D-W2-2:** 提取模块已存在，任务从"提取"改为"集成"
- **D-W2-3:** R2-4 精简 — 仅 settings_store + settings_panel
- **D-W2-4:** open() 状态修复优先于集成（MUST_FIX）

## 测试状态

- **697 通过**, 全部绿色
- 提取模块 (NetworkConfigurator/MediaOpener/VideoEffectController) 无测试覆盖

## 恢复命令

```bash
/gsd-resume-work
```
