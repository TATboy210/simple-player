---
context: research
phase: rust-mpv-bridge
task: 1
total_tasks: 5
status: in_progress
last_updated: 2026-05-24T10:20:00+08:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Rust toolchain PATH — `rustc`/`cargo` not in default PATH, must use `export PATH="$HOME/.cargo/bin:$PATH"` before any cargo/rustup command in bash

**Do not proceed until all boxes are checked.**

<current_state>
Phase 1 (FRB 脚手架集成) 基本完成。flutter_rust_bridge_codegen integrate 已成功运行，Rust crate 和 rust_builder 目录已生成。下一步是验证 `flutter run` 能调用 Rust 函数，然后进入 Phase 2（Player 命令 API）。
</current_state>

<completed_work>

Completed Tasks:
- Rust 1.95.0 安装 (rustc 1.95.0, cargo 1.95.0, stable-x86_64-pc-windows-msvc)
- flutter_rust_bridge_codegen v2.12.0 安装
- `flutter_rust_bridge_codegen integrate` 成功运行
- 生成 rust/ 目录（Cargo.toml, src/api/simple.rs, src/frb_generated.rs）
- 生成 rust_builder/ 目录（cargokit 构建胶水）
- pubspec.yaml 已添加 flutter_rust_bridge:2.12.0 和 rust_lib_simple_player_flutter 依赖
- 之前的性能优化（已完成）：folder_tab O(1) indexOf, history_tab 缓存排序, per-tile RepaintBoundary, thumbnail_tile 冗余 setState 消除, fvp query fence patch

</completed_work>

<remaining_work>

- Task #9: Phase 1 剩余 — 运行 `flutter run` 验证 Rust↔Dart 调用跑通
- Task #10: Phase 2 — Player 命令 API（MpvPlayer struct: play/pause/seek/load_file）
- Task #11: Phase 3 — 状态同步 + 事件流（mpv Event → StreamSink → Dart Stream）
- Task（未创建）: Phase 4 — 视频渲染（mpv_render_context → Flutter Texture）
- Task（未创建）: Phase 5 — HDR + 手柄 + 跨平台

</remaining_work>

<decisions_made>

- 选择 libmpv 而非直接 FFmpeg：mpv 内置 A-V 同步/seek/缓冲/HDR/字幕/手柄，~300 行 wrapper vs ~5000 行手写
- 选择 flutter_rust_bridge v2 而非手写 FFI：codegen 自动生成类型安全绑定，支持 StreamSink/Opaque/异步
- 桥接层只做三件事：命令转发、状态同步、事件转发
- 不动 MediaEngine 抽象接口，仅替换 FvpEngine → MpvEngine 实现
- 先用 `--vo=null` 验证全链路，再接入视频渲染

</decisions_made>

<blockers>
- 无技术阻塞。Rust 工具链已就绪，FRB 集成已跑通。
</blockers>

## Required Reading (in order)
1. `.claude/plans/transient-juggling-brook.md` — 完整的 Rust+mpv 桥接层架构计划（5 阶段）
2. `lib/kernel/engine/media_engine.dart` — 抽象接口，MpvEngine 需要实现
3. `lib/kernel/engine/fvp_engine.dart` — 现有实现，参考模式
4. `rust/src/api/simple.rs` — FRB 生成的示例，验证 codegen 工作正常
5. `rust/Cargo.toml` — 当前 Rust 依赖配置

## Infrastructure State
- Rust: 1.95.0 stable (x86_64-pc-windows-msvc)，PATH 需手动设置
- flutter_rust_bridge_codegen: v2.12.0，位于 ~/.cargo/bin/
- Flutter: D:\flutter\bin\flutter
- FRB 集成文件已生成：rust/, rust_builder/, pubspec.yaml 已更新
- fvp query fence patch 已应用（pub cache 中），文档在 patches/fvp_query_fence.patch.md

## Pre-Execution Critique Required
- 无需 critique，Phase 1 脚手架已完成，下一步是运行验证

<context>
这是 simple_player_flutter 从 fvp/MDK 迁移到 Rust+libmpv 的第一步。FRB 脚手架已集成完毕，生成了 Rust crate 和构建胶水。下一步需要 `flutter run` 验证 hello world 调用链跑通，然后开始写 MpvPlayer 命令 API。

计划文件在 .claude/plans/transient-juggling-brook.md，包含完整的 5 阶段实施路线。

注意：之前的列表性能优化和 fvp query fence patch 已全部完成并应用。
</context>

<next_action>
1. 运行 `flutter run -d windows` 验证 FRB 集成（应能调用 `greet("world")` 返回 "Hello, world!"）
2. 验证通过后，在 `rust/src/api/player.rs` 中创建 MpvPlayer struct
3. 添加 `libmpv = "2.0"` 到 `rust/Cargo.toml`
4. 实现 play/pause/seek/load_file 命令
</next_action>
