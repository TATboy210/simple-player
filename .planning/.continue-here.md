---
context: phase
phase: window-layer-flat
task: 4
total_tasks: 4
status: completed
last_updated: 2026-05-25T01:20:00+08:00
---

# Window Layer 扁平化 — 完成

## 当前状态

Window 层重组完成。3 文件扁平结构已就位，`flutter analyze` 零错误。

## 已完成工作

1. **创建 `window_constants.dart`** (25行) — 枚举 + SharedPreferences 键 + 默认值
2. **创建 `window_service.dart`** (295行) — init/dispose + 7命令 + 持久化 + fullscreen + listener
3. **精简 `window_state.dart`** (50行) — notifiers + singleton + `late final _svc = WindowService(this)` 委托
4. **删除 `fullscreen.dart`** — 内联到 service
5. **验证** — `flutter analyze` 零错误，window 测试 8/8 通过

## 文件结构

```
lib/window/
  window_constants.dart   25L   枚举 + 键 + 默认值
  window_state.dart       50L   notifiers + singleton + 委托
  window_service.dart    295L   init/commands/persistence/listener/fullscreen
  共计                    370L
```

## 关键设计决策

- **Concrete > Abstract**: FullscreenManager 抽象层删除，内联为 `_enterFullscreen()`/`_exitFullscreen()`
- **`late final _svc = WindowService(this)`**: 解决 `this` 不能在字段初始化器中使用的问题
- **`export 'window_constants.dart'`**: 从 window_state.dart re-export，UI 文件零改动
- **UI 只依赖 window_state.dart**: service 和 constants 是内部实现

## 预存在测试失败（非本次引入）

- `custom_title_bar_test.dart`: 2 个 BackdropFilter 测试失败（性能优化移除了 BackdropFilter）
- `aspect_ratio_service_test.dart`: MethodChannel mock 问题

## 下一步

可选：提交 window 层扁平化改动，或继续其他优化工作。
