---
context: v1.6-rendering-perf
phase: rendering-pipeline
task: 0
total_tasks: 2
status: planned
last_updated: "2026-06-26T00:00:00.000Z"
---

# v1.6 Phase 2: 渲染管线优化 — 暂停点

## 当前状态

Phase 2 计划已完成并通过验证，准备执行。

## 已完成

- [x] Phase 16 Widget 层优化（6d64cf6）
- [x] v1.6 里程碑初始化（PROJECT/REQUIREMENTS/ROADMAP/STATE）
- [x] Phase 2 研究 + RESEARCH.md 编写（Open Questions 已解决）
- [x] Phase 2 PLAN.md 编写 + 验证通过
- [x] Phase 2 VALIDATION.md 创建
- [x] D-02 矛盾修复（移除 PositionPoller resize 暂停，统一为"只冻结 UI 层"）

## 待执行

- [ ] Task 1: PositionPoller 静默模式（500ms）
  - `position_poller.dart`: 新增 `startSilent()` 方法
  - `fvp_engine.dart`: `play()` 调用 `startSilent()`
  - 测试: `test/kernel/engine/position_poller_test.dart`
- [ ] Task 2: UI 层 resize 期间冻结非关键 rebuild
  - `progress_bar.dart`: 添加 `resizing` 参数，AnimatedBuilder 包裹
  - `osd_overlay.dart`: 改为 StatefulWidget，添加 `resizing` 参数
  - `controls_overlay.dart`: 传递 `resizing` 到 OsdOverlay
  - `player_screen.dart`: 确保 isResizing 传递
  - 测试: `test/widget/player/progress_bar_test.dart`, `test/widget/player/osd_overlay_test.dart`

## 架构决策

- **D-01**: PositionPoller 静默模式 500ms，不添加 resize 暂停
- **D-02**: 只冻结 UI 层，不暂停 PositionPoller（避免进度条跳变）
- **D-03**: 扩展已有 isResizing 信号链，不新建机制

## Phase Gate（阻塞完成）

1. CPU 测量 — resize 期间 CPU 下降 >30%
2. 进度条连续性 — resize 结束后无跳变
3. OSD 响应 — resize 期间按音量键，结束后显示
4. 全量测试 — `flutter test` 全部通过

## 关键发现

- R2-2 Snapshot Debounce 已验证与 resize 无关（跳过）
- R2-3 Texture 零拷贝路径已验证（DXGI_SHARED_HANDLE，跳过）
- isResizing 信号链已完整：WindowService → PlayerScreen → ControlsOverlay
- PositionPoller FFI 调用开销极低（ns 级），无需暂停

## 恢复指令

```
/gsd-resume-work
```

然后 `/gsd-execute-phase 2` 开始执行。
