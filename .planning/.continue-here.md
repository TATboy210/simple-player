---
context: default
task: PlayerEngine removal cleanup
status: in_progress
last_updated: "2026-06-29"
---

## BLOCKING CONSTRAINTS

- [ ] PowerShell UTF-8 BOM — `Set-Content -Encoding UTF8` adds BOM breaking Dart const parsing. Strip with `sed -i '1s/^\xEF\xBB\xBF//'` after any PowerShell batch edit.
- [ ] `on Exception` vs `catch` — Dart `on Exception` does NOT catch `Error` subtypes. Use `catch (e, stackTrace)` for engine init.

<current_state>
PlayerEngine 已删除，替换为 EngineState mixin。51 文件已更新。18 个 analyze 错误待修复。
</current_state>

<completed_work>
- 边框问题修复: `on Exception` → `catch` (commit 2db5580)
- 删除 player_engine_base.dart + player_engine.dart
- 创建 engine_state.dart mixin (12 ValueNotifiers + all methods)
- 更新 51 文件 (18 UI + 8 features + 3 engine + 22 test)
- FvpEngine/MockEngine 改用 `with EngineState`
- 131 → 18 errors (commit 0022403)
</completed_work>

<remaining_work>
18 个 analyze 错误 (3 类):
1. **const init in tests** (12): media_opener_test, track_manager_test, media_info_test — 缺少 import
2. **settings_tab D3D11** (2): setD3d11SyncEnabled/setHardwareDecoding 已从接口移除
3. **FakeEngine overrides** (4): 方法签名不匹配 EngineState
</remaining_work>

<decisions_made>
- mixin 替代 abstract class — 无继承链，直接混入
- D3D11 方法从接口移除 — FvpEngine 专属
- 音轨/字幕方法保持同步返回
</decisions_made>

<blockers>
- 18 个 analyze 错误阻止 flutter test 运行
</blockers>

## Required Reading
1. `lib/kernel/engine/engine_state.dart` — mixin 定义
2. `lib/kernel/engine/fvp_engine.dart` — 实际签名
3. `test/helpers/fake_engine.dart` — 需对齐签名

## Infrastructure State
- Branch: feat/v1.8-stability-polish-plan-02-02
- Latest: 0022403 (refactor: remove PlayerEngine)

<next_action>
修复 18 错误: engine_state.dart 加回 D3D11 方法 → FakeEngine 签名对齐 → 测试 import 补全 → flutter test
</next_action>
