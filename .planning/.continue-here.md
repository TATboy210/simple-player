---
context: v1.8-in-progress
phase: v1.8
task: 1
total_tasks: 5
status: in_progress
last_updated: "2026-06-27T20:30:00.000Z"
---

## 当前状态

**Milestone:** v1.8 — Bug Fixes & Control Bar Polish
**进度:** 1/5 完成 (commit c14f467)

## 已完成工作 (commit c14f467)

- ✅ 移除 TransmittedLight 底部透射光
- ✅ 恢复毛玻璃 blur (enableBlur: true)
- ✅ 毛玻璃 +10% 不透明度 (controlBarBg 0x99→0xB3)
- ✅ 进度条厚度 32→24 (progressBarHeight)
- ✅ 窗口自由缩放 (移除 setAspectRatio 锁定)
- ✅ 自动隐藏: onMouseEnter 加 scheduleHide(), hide() 移除 !_hovering
- ✅ Golden 测试更新

## 待完成工作

### R1: 控制栏按钮不可点击 [P0]
- 打开软件后鼠标无法与控制栏按钮交互
- enableBlur 已恢复，Listener 仍为 translucent
- 代码层面未找到明显阻塞原因，需实机调试
- 可能原因: BackdropFilter GPU readback 干扰 hit test

### R2: 进度条功能不正常 [P0]
- 进度条点击/拖拽 seek 不工作
- ProgressBar 有 GestureDetector + onTapDown → engine.seekTo()
- 需确认引擎 API 是否正常响应

### R3: 中心按钮功能不工作 [P0]
- 播放/暂停、快进、后退等按钮无响应
- CenterGroup 的 GlassButton.iconOnly 有 onPressed 回调
- isIdle 时为 null（正确），播放中应有回调

### R4: 按钮反馈区域远离边框 [P1]
- 控制栏上中下结构比例需调整
- 降低进度条厚度（已完成）→ 提升按钮行
- 需调整 control_bar.dart 内部 padding

### R5: 功能完整性检查 [P1]
- 用户未明确哪些功能有问题
- 需逐一排查控制栏所有按钮

## 关键文件

- `lib/ui/player/controls_overlay.dart` — 控制栏容器
- `lib/ui/player/control_bar.dart` — 控制栏布局
- `lib/ui/player/auto_hide_controller.dart` — 自动隐藏逻辑
- `lib/ui/player/progress_bar.dart` — 进度条
- `lib/ui/player/center_controls.dart` — 中心按钮组
- `lib/ui/player/player_screen.dart` — 播放器主屏幕

## 恢复命令

```bash
/gsd-resume-work
```
