---
context: phase
phase: rust-mpv-bridge
task: 6
total_tasks: 9
status: completed
last_updated: 2026-05-24T19:15:00+08:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Rust toolchain PATH — `rustc`/`cargo` not in default PATH, must use `export PATH="$HOME/.cargo/bin:$PATH"` before any cargo/rustup command in bash
- [ ] CONSTRAINT: libmpv 链接路径 — cargokit 在临时目录运行 cargo，`.cargo/config.toml` 不生效，必须用 `build.rs` 的 `cargo:rustc-link-search` 指定 `C:\libmpv-dev\mpv`
- [ ] CONSTRAINT: libmpv DLL 部署 — `libmpv-2.dll` 必须复制到 `build/windows/x64/runner/Debug/` 才能运行

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| .cargo/config.toml for cargokit | cargokit runs cargo from temp dir, .cargo/config.toml in rust/ is ignored | blocking | Use build.rs `cargo:rustc-link-search` instead |
| GitHub download via PowerShell | Invoke-WebRequest to github.com fails with connection reset | advisory | Use SourceForge mirrors or gh CLI |

<current_state>
Phase 2 (Player 命令 API) 已完成。MpvPlayer struct 在 Rust 侧实现 13 个方法，FRB 自动生成 Dart 绑定。Flutter build windows 成功链接 libmpv2。libmpv-2.dll 已复制到 build output。下一步是 Phase 3: 状态同步+事件流。
</current_state>

<completed_work>

Completed Tasks:
- Task 1: FRB 脚手架集成 — flutter_rust_bridge_codegen integrate 跑通
- Task 2: Rust 工具链安装 — rustc 1.95.0 stable
- Task 3: 列表性能优化 — folder_tab O(1) indexOf, history_tab 缓存排序, per-tile RepaintBoundary
- Task 4: fvp Query Fence — Flush() → D3D11_QUERY_EVENT
- Task 5: 验证 Flutter↔Rust 调用 — flutter build windows 成功
- Task 6: Player 命令 API — MpvPlayer struct 13 个方法，Dart 绑定生成

</completed_work>

<remaining_work>

- Task 7: 状态同步 + 事件流 — mpv Event → StreamSink → Dart Stream → ValueNotifiers
- Task 8: 视频渲染 — mpv_render_context → Flutter Texture
- Task 9: HDR + 手柄 + 跨平台 — tone mapping, input.conf, CI matrix

</remaining_work>

<decisions_made>

- 选择 libmpv2 = "6.0" 而非 libmpv = "2.0"：libmpv2 是最新版本，API 相同
- build.rs 配置链接路径：cargokit 在临时目录运行 cargo，.cargo/config.toml 不生效
- Mutex<Mpv> 线程安全包装：FRB 方法在 async 上下文调用，需要 Send+Sync
- vo=null 暂时禁用视频输出：Phase 2 仅验证命令 API，Phase 4 再接入渲染

</decisions_made>

<blockers>
- 无技术阻塞。libmpv 已成功链接，Dart 绑定已生成。
</blockers>

## Required Reading (in order)
1. `.claude/plans/transient-juggling-brook.md` — 完整的 Rust+mpv 桥接层架构计划（5 阶段）
2. `rust/src/api/player.rs` — MpvPlayer Rust 实现（当前完成的代码）
3. `lib/src/rust/api/player.dart` — FRB 生成的 Dart 绑定
4. `lib/kernel/engine/media_engine.dart` — 抽象接口，Phase 3 的 MpvEngine 需要实现
5. `rust/build.rs` — 链接器路径配置

## Infrastructure State
- Rust: 1.95.0 stable (x86_64-pc-windows-msvc)，PATH 需手动设置 `export PATH="$HOME/.cargo/bin:$PATH"`
- libmpv: C:\libmpv-dev\mpv\ (libmpv-2.dll + mpv.lib + headers)
- flutter_rust_bridge_codegen: v2.12.0
- Flutter: D:\flutter\bin\flutter
- 构建命令: `export PATH="$HOME/.cargo/bin:$PATH" && D:/flutter/bin/flutter build windows --debug`
- DLL 部署: `cp C:/libmpv-dev/mpv/libmpv-2.dll D:/simple_player_flutter/build/windows/x64/runner/Debug/`

<context>
Phase 2 完成。MpvPlayer 的 Rust 实现和 Dart 绑定已就绪。所有方法都是 async Future（FRB 自动生成）。mpv 当前以 vo=null 模式运行，仅验证命令 API 路径。

Phase 3 的核心是事件流：在 Rust 侧用 EventContext 观察 mpv 属性变化（time-pos/duration/pause），通过 FRB StreamSink 转发为 Dart Stream，最终连接到 MediaEngine 的 ValueNotifiers。

关键文件结构：
- rust/src/api/player.rs — MpvPlayer struct（Phase 2 完成）
- rust/src/api/events.rs — PlayerEvent enum + start_event_loop（Phase 3 待创建）
- lib/kernel/engine/mpv_engine.dart — MpvEngine implements MediaEngine（Phase 3 待创建）
</context>

<next_action>
1. 在 `rust/src/api/events.rs` 创建 PlayerEvent enum（position/duration/state/paused/error）
2. 在 MpvPlayer 上添加 `start_event_loop(sink: StreamSink<PlayerEvent>)` 方法
3. 用 mpv EventContext 观察 time-pos/duration/pause 属性
4. 运行 FRB codegen 生成 Dart Stream 绑定
5. 在 `lib/kernel/engine/mpv_engine.dart` 创建 MpvEngine implements MediaEngine
</next_action>
