---
context: ui-widget-layer-optimization
task: phase2
total_tasks: phase2
status: paused
last_updated: 2026-05-26T19:30:00+08:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Flutter CLI 路径 — 不在 PATH，必须用 `D:\flutter\bin\flutter`

<current_state>
UI Widget Layer Over-Rebuild Optimization — Phase 1 (8 phases) + Phase 2 (6 fixes) complete.
Phase 1: 8 phases eliminating 2054/474 rebuild counts. 10 files modified.
Phase 2: 6 targeted fixes from GPU/memory/list-diffing audit. 6 files modified.
All 16 modified files pass `flutter analyze` with 0 UI-layer issues.
</current_state>

<completed_work>
Phase 1 (widget rebuild):
- PlayerScreen VLB child params + remove Builder
- EmptyState AnimatedBuilder replaces addListener+setState
- ProgressBar drag ValueNotifier
- GlassButton hover/press ValueNotifier
- SpeedButton pre-build static segments
- PlaylistPanel tab ValueNotifier
- ThumbnailTile split _ThumbnailImage
- const constructor audit (15 fixes + analysis_options.yaml)

Phase 2 (GPU/memory/list):
- FocusNode leak fix in shortcuts_tab.dart (BUG)
- ValueKey on 3 ListView.builder instances (folder_tab, history_tab)
- RepaintBoundary on OSD _OsdBubble
- RepaintBoundary on EmptyState AnimatedBuilder subtree
- GlassButton respectResizeState default false→true
- controls_overlay TickerProviderStateMixin→SingleTickerProviderStateMixin
</completed_work>

<remaining_work>
- Manual verification: `D:\flutter\bin\flutter run -d windows` + DevTools Widget Rebuild Count
- `git commit` all 16 modified UI files
</remaining_work>

<decisions_made>
- ValueNotifier + AnimatedBuilder replaces bool+setState for all interaction state
- child parameter on VLB/AnimatedBuilder hoists static subtrees
- Pre-built widget references outside builders prevent unnecessary rebuilds
- Widget split (_ThumbnailImage) isolates async loading state
- RepaintBoundary on AnimatedBuilder subtrees caches rasterized output
- GlassButton respectResizeState defaults to true for standalone usage
</decisions_made>

<blockers>
None
</blockers>

## Files Modified (16 files)
Phase 1: player_screen.dart, empty_state.dart, progress_bar.dart, glass_container.dart, speed_button.dart, playlist_panel.dart, thumbnail_tile.dart, about_tab.dart, drop_handler.dart, osd_overlay.dart, controls_overlay.dart, control_bar.dart, analysis_options.yaml
Phase 2: shortcuts_tab.dart, folder_tab.dart, history_tab.dart

## Required Reading
1. `.claude/plans/simplified-playerscreen-to-bare-serialized-stardust.md` — Full plan with all phases

## Next Steps
- `flutter run -d windows` to verify with DevTools
- `git commit` all changes
