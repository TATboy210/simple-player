---
phase: 30-panel-layout-redesign
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - lib/ui/theme/tokens.dart
  - lib/ui/dialogs/settings/settings_overlay_shell.dart
  - lib/ui/dialogs/settings/tab_strip.dart
  - lib/ui/dialogs/settings/tab_content.dart
  - test/ui/dialogs/settings_overlay_shell_test.dart
autonomous: true
requirements:
  - LAYOUT-04
must_haves:
  truths:
    - The settings title bar, tab strip, content surface, and button bar resolve their background from Tokens.panelSectionBg.
    - Tokens.panelSectionBg retains the Tokens.bgGlass value in Phase 30, so panel sections are structurally unified without Phase 31 chrome styling.
    - The Phase 30 panel continues to retain its RepaintBoundary rendering isolation.
  artifacts:
    - path: lib/ui/theme/tokens.dart
      provides: panelSectionBg alias in the responsive settings-panel token section
    - path: lib/ui/dialogs/settings/settings_overlay_shell.dart
      provides: shared token route for title and button-bar surfaces
    - path: lib/ui/dialogs/settings/tab_strip.dart
      provides: shared token route for the tab-strip surface
    - path: lib/ui/dialogs/settings/tab_content.dart
      provides: shared token route for the content surface
    - path: test/ui/dialogs/settings_overlay_shell_test.dart
      provides: widget-level regression proof of the four-section token route
  key_links:
    - from: SettingsOverlayShell title and button sections
      to: Tokens.panelSectionBg
      via: their Container color properties
    - from: SettingsTabStrip and SettingsTabContent
      to: Tokens.panelSectionBg
      via: their rendered background widgets
    - from: Tokens.panelSectionBg
      to: Tokens.bgGlass
      via: compile-time alias retained until Phase 31
---

## Goal

Create the Phase 30 structural color route for the re-containerized settings overlay: its title bar, tab strip, content surface, and button bar must consume one `Tokens.panelSectionBg` alias whose Phase 30 value is `Tokens.bgGlass`.

Purpose: satisfy the assigned LAYOUT-04 structural-unification outcome while preserving the strict D-02 boundary: Phase 30 establishes one route; Phase 31 owns actual control-bar chrome alignment.

Output: a shared settings-panel background token, four consumers, and regression coverage proving all four sections resolve through it.

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce the Phase 30 panel-section token route in the shell</name>
  <files>lib/ui/theme/tokens.dart, lib/ui/dialogs/settings/settings_overlay_shell.dart</files>
  <behavior>
    - The responsive settings-panel token group exposes `Tokens.panelSectionBg` with the same Color value as `Tokens.bgGlass`.
    - The title-bar and bottom button-bar backgrounds resolve through `Tokens.panelSectionBg`.
    - The shell continues to wrap its panel in the existing `RepaintBoundary`.
  </behavior>
  <action>After confirming the Wave 1 shell interface from Plan 30-01, add the compile-time `Tokens.panelSectionBg = Tokens.bgGlass` alias in the responsive settings-panel token section, per D-02. Replace only the title-bar and bottom button-bar background consumers in `SettingsOverlayShell` with that alias. Retain the existing RepaintBoundary per D-06 and retain all geometry, drag, focus, opacity, and glass-tier behavior supplied by Plan 30-01. Do not edit any control-bar chrome tokens or their consumers: D-02 reserves those visual changes for Phase 31. Add or amend comments only where needed to explain that the alias is the structural single route for the four panel sections.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/dialogs/settings_overlay_shell_test.dart</automated>
  </verify>
  <done>`Tokens.panelSectionBg` aliases `Tokens.bgGlass`; title and button-bar backgrounds consume it; the shell's RepaintBoundary remains present; the shell widget suite passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route tab and content surfaces through the alias and prove all four sections</name>
  <files>lib/ui/dialogs/settings/tab_strip.dart, lib/ui/dialogs/settings/tab_content.dart, test/ui/dialogs/settings_overlay_shell_test.dart</files>
  <behavior>
    - An open settings panel renders title, tab strip, content, and button-bar backgrounds from `Tokens.panelSectionBg`.
    - The content surface no longer uses its former distinct panel-background route.
    - The test compares resolved colors to `Tokens.panelSectionBg`, preserving a Phase 31 token-value swap as a single-route change.
  </behavior>
  <action>Update `SettingsTabStrip` and `SettingsTabContent` so their background widgets read `Tokens.panelSectionBg`, completing the four-section route with the shell changes from Task 1, per D-02. Preserve the explicit seven-child IndexedStack, ValueListenable ownership, compact-mode behavior, tab order delivered by Plan 30-01, and all existing padding/animation behavior. Add stable test-only discovery keys only if the existing title-bar, button-bar, tab-strip, and content widget hierarchy cannot be asserted unambiguously; do not add state or rendering wrappers merely for testing. In `settings_overlay_shell_test.dart`, write the failing widget expectation before production routing is complete, then assert an opened shell resolves all four structural section backgrounds to `Tokens.panelSectionBg`. Keep the test color-route based rather than checking an inline ARGB value, so Phase 31 can change the alias once without duplicating consumers. Keep Phase 31's chrome, edge-glow, density, and three-state work out of this change, per D-02.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/dialogs/settings_overlay_shell_test.dart &amp;&amp; D:/flutter/bin/flutter test test/ui/dialogs/settings_tab_content_test.dart</automated>
  </verify>
  <done>All four settings-panel structural sections consume `Tokens.panelSectionBg`, the widget regression test proves that route, explicit IndexedStack structure remains intact, and the focused shell/content test suites pass.</done>
</task>

</tasks>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|---|---|
| None introduced | This expansion changes only compile-time UI color routing; it adds no input, persistence, network, auth, package, or FFI surface. |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| — | N/A | Structural panel color route | low | accept | No externally controlled data crosses this change. Widget tests verify the intended token wiring; secure behavior is N/A for this layout-only plan. |

Threat references: none. Package legitimacy gate: not applicable because this plan installs no packages.

## Verification

1. Run `D:/flutter/bin/flutter test test/ui/dialogs/settings_overlay_shell_test.dart` after Task 1 and after Task 2.
2. Run `D:/flutter/bin/flutter test test/ui/dialogs/settings_tab_content_test.dart` after Task 2 to protect the explicit content structure and tab behavior.
3. Run `D:/flutter/bin/flutter analyze` before marking the plan complete.
4. During Phase 30 Wave 3, Plan 30-04 re-baselines and executes the complete geometry, tab-order, clamp, resize, fallback, and four-section color contract. This plan's focused test is the immediate proof of its color-route artifact.
5. Human visual validation is not a gate for this low-risk structural alias. Phase 31 owns visual chrome alignment and its profile-mode visual review.

## Goal-Backward Check

### Observable truths

- The title bar, tab strip, content surface, and button bar have one structural background-token route.
- That route retains the established glass value in Phase 30.
- The shell preserves rendering isolation while its sections are unified.
- Future chrome alignment can change one alias rather than four independently maintained consumers.

### Reachability

- Opening `SettingsPanelController` renders `SettingsOverlayShell`.
- The shell composes its title bar, `SettingsTabStrip`, `SettingsTabContent`, and button bar in the same visible panel.
- Each rendered section resolves its color through `Tokens.panelSectionBg`; the widget test opens that real shell and observes those consumers.

### Source coverage audit

| Source | ID | Required outcome or constraint | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 30 SC#4 | Unified upper/middle/lower structural color route | Tasks 1-2 | COVERED |
| REQ | LAYOUT-04 | Assigned structural color-route requirement | Tasks 1-2 | COVERED |
| RESEARCH | Seam 4 | Alias route covers the content outlier and the three existing glass surfaces | Tasks 1-2 | COVERED |
| RESEARCH | Architecture responsibility | UI consumers plus Tokens only; no kernel or package work | Tasks 1-2 | COVERED |
| CONTEXT | D-02 | Alias retains bgGlass; Phase 31 chrome work stays separate | Tasks 1-2 | COVERED |
| CONTEXT | D-06 | Retain shell RepaintBoundary | Task 1 | COVERED |

All in-scope source items assigned to Plan 30-03 are covered. D-01, D-03, D-04, and D-05 are owned by Plans 30-01, 30-02, and 30-04; no deferred ideas apply.

<success_criteria>
- `Tokens.panelSectionBg` is the only Phase 30 structural background route for the title bar, tab strip, content surface, and button bar.
- The alias evaluates to `Tokens.bgGlass` in Phase 30, with no Phase 31 visual-token or control-bar-consumer edits.
- Focused widget tests and static analysis pass, while the existing RepaintBoundary and explicit seven-child IndexedStack remain in place.
</success_criteria>

<output>
Create `.planning/phases/30-panel-layout-redesign/30-03-SUMMARY.md` when execution completes.
</output>
