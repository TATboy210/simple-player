---
context: phase
phase: 30-panel-layout-redesign
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-07-26T11:23:04.329Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **research-only mode contract (`--research-phase 30`)** — `RESEARCH_ONLY=true`; do NOT spawn planner / plan-checker / verifier. After researcher returns `## RESEARCH COMPLETE`, commit RESEARCH.md, print §5.1 early-exit summary, and STOP. Violating this = running the full plan-phase pipeline the user explicitly opted out of.
- [ ] CONSTRAINT: **Locked decisions in 30-CONTEXT.md are not re-litigable** — 8 CF (CF-01..08) + D-01..06 are固化. The researcher researches HOW to implement, never WHAT. If code evidence contradicts a locked decision, flag it as `## Risk` / `## Assumption` in RESEARCH.md — do NOT silently override.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Forced spawn at 60%+ context | Spawning gsd-phase-researcher when context window is above 60% risks truncation mid-research; combined with classifier-down, spawn was blocked entirely | blocking | Check `context_window` budget before spawn; if >60%, write checkpoint + `/clear` + resume — do NOT force spawn (per `feedback_gsd_context_budget_pause`) |
| Re-litigating locked discuss-phase decisions | Researcher re-opens CF/D decisions already固化 in CONTEXT.md → wastes budget + breaks discuss→plan→execute contract | advisory | Treat CONTEXT.md CF-01..08 + D-01..06 as constraints; research implementation only |

<current_state>
Phase 30 (panel-layout-redesign) research-only mode, paused at `/gsd-plan-phase 30 --research-phase 30` §5.1 "Spawn gsd-phase-researcher" — the spawn itself did NOT happen. All prep is 100% complete: INIT JSON queried, research hook `fragment.inline` captured + filled, ROADMAP Phase 30 section + 30-CONTEXT.md read, `context_window=200000` confirmed (<500k, no cross-phase enrichment). The complete filled researcher prompt is saved verbatim in `30-RESEARCH-CHECKPOINT.md`. Two blockers prevented the spawn: (1) auto-mode safety classifier (kimi-k3) temporarily unavailable; (2) context window at 75% critical. No RESEARCH.md exists yet (`has_research=false`).
</current_state>

<completed_work>

Completed Tasks (this session, all prep for researcher spawn):
- Task 1: `query init.plan-phase 30` — got INIT JSON (researcher_model=sonnet, commit_docs=true, nyquist_validation_enabled=true, has_research=false, has_context=true, phase_req_ids=LAYOUT-01..05, context_window=200000) - Done
- Task 2: `loop render-hooks plan:pre --raw` — captured research hook `fragment.inline`, the official prompt template - Done
- Task 3: Read ROADMAP.md Phase 30 section (L62-74) + 30-CONTEXT.md (8 CF + D-01..06 locked decisions, canonical refs, code context) - Done
- Task 4: Confirmed `context_window=200000` < 500000 → no cross-phase context enrichment needed; `agent-skills gsd-phase-researcher` query empty (no skill block to inject) - Done
- Task 5: Wrote `30-RESEARCH-CHECKPOINT.md` with the complete verbatim filled researcher prompt + resume instructions - Done (uncommitted)
</completed_work>

<remaining_work>

- Task 6: Spawn `gsd-phase-researcher` (subagent_type=gsd-phase-researcher, model=sonnet, run_in_background=false, synchronous) with the verbatim prompt from `30-RESEARCH-CHECKPOINT.md` — Not started, blocked by classifier-down + context-critical
- Post-spawn (only if researcher returns `## RESEARCH COMPLETE`): verify `30-RESEARCH.md` exists; commit if `commit_docs=true`; print §5.1 research-only early-exit summary; EXIT (do NOT continue to planner/checker/verifier)
</remaining_work>

<decisions_made>

- Decided research-only mode (`--research-phase 30`) produces RESEARCH.md only and skips planner/checker/verifier, because the user invoked `/gsd-plan-phase 30 --research-phase 30` per §5.0/§5.1 early-exit contract
- Decided to write `30-RESEARCH-CHECKPOINT.md` with the filled researcher prompt instead of forcing the spawn at 68%+ context, because the `feedback_gsd_context_budget_pause` memory says 60%+ context + blocked spawn → checkpoint + pause, not forced spawn (avoids truncation mid-research)
- (Carried-forward, NOT made this session) 8 CF + D-01..06 in 30-CONTEXT.md are the discuss-phase output — researcher treats them as constraints
</decisions_made>

<blockers>
- Blocker 1: auto-mode safety classifier (kimi-k3) temporarily unavailable — Agent spawn cannot be safety-checked. Transient; retry after brief wait or `/clear` + resume from checkpoint. Read-only operations unaffected.
- Blocker 2: context window at 75% (critical, 25% remaining) — spawn + return risk overflow. Workaround: `/clear` then resume; checkpoint already has full verbatim prompt so zero state loss.
</blockers>

## Required Reading (in order)

1. `.planning/phases/30-panel-layout-redesign/30-RESEARCH-CHECKPOINT.md` — **primary resume artifact**; contains the complete verbatim filled researcher prompt + commit + early-exit instructions. Open this first and follow its "Resume action" section.
2. `.planning/phases/30-panel-layout-redesign/30-CONTEXT.md` — 8 CF + D-01..06 locked decisions, canonical refs (sizing seam, display_enumerator FFI, tab reorder seam, Tokens, test re-baseline targets), code context. The researcher reads this; the resuming orchestrator should know it too.
3. `.planning/ROADMAP.md` (Phase 30 section L62-74) — goal, 5 Success Criteria, Blocking Constraints honored; L170 notes Phase 30 as "standard patterns" but user explicitly triggered research.
4. `.planning/HANDOFF.json` — structured machine-readable companion to this file (consumed by `/gsd-resume-work`).
5. `.planning/STATE.md` — Phase 28/29 complete records, sizing seam position, always-pause verified live (CF-08).

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: forcing a subagent spawn when context >60% AND classifier is down → spawns get truncated or blocked, wasting budget. Structural mitigation: always write a checkpoint with the verbatim prompt before attempting spawn at high context; resume in a fresh window.
- ANTI-PATTERN: re-litigating discuss-phase locked decisions during research → breaks the discuss→plan→execute translation contract. Structural mitigation: researcher prompt explicitly says "do NOT re-litigate CF-01..08 / D-01..06; flag as Risk/Assumption if code contradicts."

## Infrastructure State

- Git branch: `feat/v1.8-stability-polish-plan-02-02` (do NOT create/rename/switch branches during plan-phase per §0 Git Branch Invariant)
- gsd-tools.cjs path: `C:/Users/35490/.claude/gsd-core/bin/gsd-tools.cjs` (user-global; invoke via `node "<path>" query <verb>`)
- Researcher model: `sonnet` (from INIT JSON `researcher_model`)
- `commit_docs=true` → commit RESEARCH.md after researcher writes it
- `nyquist_validation_enabled=true` → RESEARCH.md MUST include `## Validation Architecture` section
- Uncommitted files this session: `.planning/HANDOFF.json` (M), `30-RESEARCH-CHECKPOINT.md` (??), `.continue-here.md` (?? this file). Pre-existing untracked (NOT ours, exclude from any commit): `.planning/STATE.md` (M), `30-DISCUSS-CHECKPOINT.json`, `30-PLAN-CHECKPOINT.md`, `29-EXECUTE-CHECKPOINT.md`.

## Pre-Execution Critique Required

- Design artifact: `.planning/phases/30-panel-layout-redesign/30-RESEARCH-CHECKPOINT.md` (the filled researcher prompt — analogous to a spike design that has not yet been run)
- Critique focus: before re-spawning, verify the prompt's 7 research-focus items still match the actual code (e.g., re-confirm `settings_overlay_shell.dart` sizing seam line numbers, `display_enumerator.dart` interface signatures, `tab_strip.dart` tab order) — but do NOT do this verification yourself; that IS the researcher's job. Just confirm the checkpoint file is intact.
- Gate: do NOT begin execution (planner) until research is complete and `30-RESEARCH.md` exists on disk.

<context>
Mental state: executing `/gsd-plan-phase 30 --research-phase 30` (research-only). Walked §1 (init) → §2 (parse args, RESEARCH_ONLY=true) → §5.0 (three research-only branches: no --view, no --research, has_research=false → falls through to spawn) → §5.1 (Standard Research Decision SKIPPED due to RESEARCH_ONLY=true guard) → "Spawn gsd-phase-researcher" (BLOCKED). The filled fragment template is in 30-RESEARCH-CHECKPOINT.md, ready to paste into an Agent() call. Approach: checkpoint + pause per the documented `feedback_gsd_context_budget_pause` pattern — zero state loss because researcher is stateless and the prompt is fully prepared. Next session: /clear, open checkpoint, spawn, handle RESEARCH COMPLETE, commit, early-exit.
</context>

<next_action>
Start with: `/clear` (or open a fresh session), then open `.planning/phases/30-panel-layout-redesign/30-RESEARCH-CHECKPOINT.md` and follow its "Resume action" section verbatim — spawn `gsd-phase-researcher` (subagent_type=gsd-phase-researcher, model=sonnet, run_in_background=false) with the filled prompt. After `## RESEARCH COMPLETE`: verify `30-RESEARCH.md` on disk → commit via `node "C:/Users/35490/.claude/gsd-core/bin/gsd-tools.cjs" query commit "docs(30): create phase research" --files "D:/simple_player_flutter/.planning/phases/30-panel-layout-redesign/30-RESEARCH.md"` → print the §5.1 research-only early-exit summary → STOP (planner/checker/verifier skipped per RESEARCH_ONLY=true).
</next_action>
