---
phase: 30
slug: panel-layout-redesign
mode: chunked-outline
created: 2026-07-26
plan_count: 4
---

# Phase 30 — Plan Outline

> Chunked §8.5.1 outline. Per-plan `30-0N-PLAN.md` files generated by §8.5.2 stage.

## Plan Table

| Plan ID | Objective | Wave | Depends On | Requirements | Type |
|---------|-----------|------|------------|--------------|------|
| 30-01 | Deliver the production tracer: render the strict 16:9 settings overlay using the locked min/clamp formula, open on General at index 3 in the locked seven-tab sequence, and prove one real work-area-clamped drag path through the existing `DisplayEnumerator` seam. | 1 | — | LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04 | tracer |
| 30-02 | Expand and harden the multi-monitor behavior with drag-session window-position caching, per-frame work-area bounds, null/FFI fallback to the existing symmetric clamp, and resize re-clamping. | 2 | 30-01 | LAYOUT-04 | expansion |
| 30-03 | Create the Phase 30 structural color route by making the title bar, tab strip, content, and button bar consume the single `Tokens.panelSectionBg = Tokens.bgGlass` alias, without taking Phase 31 chrome work. | 2 | 30-01 | LAYOUT-05 | expansion |
| 30-04 | Re-baseline the complete widget-test contract for 16:9 geometry, General-default tab ordering, work-area/fallback/resize clamp behavior, and all-four-section token unification. | 3 | 30-02, 30-03 | LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04, LAYOUT-05 | expansion |

## Notes

- The tracer must be production-quality and test-first: it proves a visible 16:9 panel, General at index 3, and one injected work-area clamp path before expansion work. It uses the existing `display_enumerator` FFI and does not introduce packages or alternate monitor-query mechanisms.
- Plan 02 and Plan 03 can execute in Wave 2 because they have no file overlap with each other; both expand the Wave 1 shell. Plan 04 follows both so its full test baseline observes the finalized clamp and structural-token behavior.
- D-01 is a costly positional-contract change: reorder `tab_strip.dart`, the explicit seven-child `IndexedStack`, the controller's General default index, and index-sensitive tests together. Preserve the six non-General tabs' relative order and retain the explicit children; do not collapse them with `List.generate`.
- D-04 is costly: replacing the 5:4 size assertions with the strict 16:9 formula requires coordinated re-baselining across the three documented size/drag test files. Keep `Tokens.breakpointResponsive` for tab-strip compact mode; the sizing formula itself has no breakpoint branch.
- D-02 is a strict phase boundary: Phase 30 only adds the shared `panelSectionBg` alias retaining `bgGlass`; `controlBarBg`, `controlBarBorderWhite`, `glowOuterRing`, edge glow, and three-state styling remain exclusively in Phase 31.
- D-03 uses Option A: cache `windowManager.getPosition()` for each drag session, query `DisplayEnumerator.getCurrentDisplay()` synchronously per frame, inject a fake in widget tests, and log FFI failures with `debugPrint` before using the existing symmetric fallback. D-05 resize re-clamping and D-06 retaining `RepaintBoundary` are included.

## OUTLINE COMPLETE

Plan count: 4
