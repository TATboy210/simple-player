---
phase: 30-panel-layout-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/theme/tokens.dart
  - lib/ui/dialogs/settings/settings_overlay_shell.dart
  - lib/ui/dialogs/settings/tab_strip.dart
  - lib/ui/dialogs/settings/tab_content.dart
  - lib/ui/dialogs/settings/settings_panel_controller.dart
  - test/ui/dialogs/settings_overlay_shell_test.dart
  - test/ui/dialogs/settings_tab_content_test.dart
  - test/widgets/panel_size_test.dart
  - test/widgets/tab_strip_order_test.dart
  - test/widgets/multi_monitor_clamp_test.dart
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-05]
must_haves:
  truths:
    - "At each supported window size, the visible settings overlay uses width = min(0.5 × screen width, screen height × 16/9), constrained from 400 through 960, and height = width × 9/16 (D-04)."
    - "Opening the settings panel selects General at index 3 in the exact seven-tab sequence [EQ, Audio, Video, General, Shortcuts, About, Performance], while the other six tabs keep their relative order (D-01)."
    - "One visible title-bar drag path resolves an injected DisplayEnumerator work area and constrains the overlay using that work area rather than only centered window geometry (D-03)."
    - "The panel remains a centered overlay Stack sibling and retains its RepaintBoundary while its size, tab order, and tracer clamp path change (D-06)."
    - "The responsive compact-tab breakpoint remains independent of the new sizing formula; the formula itself has no breakpoint branch (D-04)."
  artifacts:
    - path: lib/ui/theme/tokens.dart
      provides: 16:9 settings-panel geometry tokens, including the 960 maximum width and aspect-ratio constant.
    - path: lib/ui/dialogs/settings/settings_overlay_shell.dart
      provides: The visible 16:9 overlay shell and one injectable work-area-clamped drag path.
    - path: lib/ui/dialogs/settings/tab_strip.dart
      provides: The locked seven-tab visual order with General fourth.
    - path: lib/ui/dialogs/settings/tab_content.dart
      provides: Matching explicit seven-child IndexedStack content order.
    - path: lib/ui/dialogs/settings/settings_panel_controller.dart
      provides: A named General default index used when opening the panel.
    - path: test/widgets/panel_size_test.dart
      provides: Focused widget evidence for the min/clamp strict-16:9 geometry path.
    - path: test/widgets/tab_strip_order_test.dart
      provides: Focused widget evidence for the General-at-index-3 tab sequence.
    - path: test/widgets/multi_monitor_clamp_test.dart
      provides: Focused fake-DisplayEnumerator evidence for one work-area drag path.
  key_links:
    - from: lib/ui/dialogs/settings/settings_overlay_shell.dart
      to: lib/ui/theme/tokens.dart
      via: _panelWidth consumes panelWidthRatio, panelAspectRatio, panelMinWidth, and panelMaxWidth before deriving height.
      pattern: "panelAspectRatio"
    - from: lib/ui/dialogs/settings/settings_panel_controller.dart
      to: lib/ui/dialogs/settings/tab_strip.dart
      via: defaultTabIndex selects the fourth tab and the reordered strip renders General at that index.
      pattern: "defaultTabIndex"
    - from: lib/ui/dialogs/settings/settings_overlay_shell.dart
      to: lib/kernel/bridge/display_enumerator.dart
      via: Optional injected DisplayEnumerator supplies getCurrentDisplay().workArea for the tracer drag clamp.
      pattern: "DisplayEnumerator"
---

## Goal

Deliver the production-quality Phase 30 tracer: a user opens a real settings overlay that renders at the locked strict-16:9 size, opens General in the middle of the seven-tab sequence, and can drag through one real injected display-work-area clamp path.

Purpose: prove the user-visible container, positional contract, and existing Win32 display-enumeration seam together before Wave 2 expands multi-monitor resiliency and the structural color route.

Output: geometry tokens and shell wiring, reordered tab presentation/content/controller state, and focused widget contracts that later plans extend rather than replace.

<objective>
Build the first end-to-end settings-overlay path for LAYOUT-01, LAYOUT-02, LAYOUT-03, and LAYOUT-05: visible strict geometry, the General-default middle tab, and one fake-injectable work-area drag clamp. This is the Wave 1 production tracer, not an isolated utility or a visual mock.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-panel-layout-redesign/30-PLAN-OUTLINE.md
@.planning/phases/30-panel-layout-redesign/30-CONTEXT.md
@.planning/phases/30-panel-layout-redesign/30-RESEARCH.md
@.planning/phases/30-panel-layout-redesign/30-UI-SPEC.md
@.planning/phases/30-panel-layout-redesign/30-VALIDATION.md
@lib/ui/theme/tokens.dart
@lib/ui/dialogs/settings/settings_overlay_shell.dart
@lib/ui/dialogs/settings/settings_panel_controller.dart
@lib/ui/dialogs/settings/tab_strip.dart
@lib/ui/dialogs/settings/tab_content.dart
@lib/kernel/bridge/display_enumerator.dart
</context>

## Tasks

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Deliver the visible strict-16:9 General-default overlay path</name>
  <reversibility rating="costly">D-01 changes the positional tab contract across the strip, explicit content stack, controller default, and index-sensitive tests; D-04 changes the shared geometry contract and requires coordinated assertion re-baselining.</reversibility>
  <files>lib/ui/theme/tokens.dart, lib/ui/dialogs/settings/settings_overlay_shell.dart, lib/ui/dialogs/settings/tab_strip.dart, lib/ui/dialogs/settings/tab_content.dart, lib/ui/dialogs/settings/settings_panel_controller.dart, test/ui/dialogs/settings_overlay_shell_test.dart, test/ui/dialogs/settings_tab_content_test.dart, test/widgets/panel_size_test.dart, test/widgets/tab_strip_order_test.dart</files>
  <behavior>
    - A 1920×1080 settings overlay measures 960×540; a 1366×768 overlay follows the proportional min result; an 800×600 overlay measures 400×225.
    - Opening the controller selects SettingsPanelController.defaultTabIndex, whose value is 3, and the visible tab order is EQ, Audio, Video, General, Shortcuts, About, Performance.
    - Selecting every tab shows its matching existing content at the index used by the explicit seven-child IndexedStack.
    - The panel remains a centered overlay and continues to be wrapped in RepaintBoundary.
  </behavior>
  <action>Start RED by creating the focused panel-size and tab-order widget contracts and updating only the affected semantic expectations in the existing shell/content suites. Then implement the end-to-end production slice. Per D-04, update the responsive settings-panel token group: retain panelMinWidth, change panelMaxWidth to 960, repurpose panelWidthRatio to 0.5, replace the single-use panelHeightRatio with panelAspectRatio = 16.0 / 9.0, and remove the unused shell ratio alias. Change the shell geometry helper to accept the complete MediaQuery Size, calculate width from the locked minimum of width ratio and height times aspect ratio, constrain it with the named token bounds, and calculate height by dividing width by panelAspectRatio. Keep Tokens.breakpointResponsive strictly for compact-tab presentation; it is not a geometry branch. Per D-01, reorder the tab-strip icon/label lists and all seven explicit IndexedStack children to EQ, Audio, Video, General, Shortcuts, About, Performance; preserve the six non-General tabs' relative order, each TweenAnimationBuilder wrapper, and the explicit children rather than collapsing them into generation logic. Add SettingsPanelController.defaultTabIndex = 3 and use it from open() so General remains the default; update affected test names and Chinese comments with the changed semantics. Preserve overlay Stack composition, ValueNotifier ownership, existing focus traversal, GlassContainer tier, and RepaintBoundary per D-06. Use Tokens for visual values and document each changed non-trivial public/helper behavior while coding.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/widgets/panel_size_test.dart test/widgets/tab_strip_order_test.dart test/ui/dialogs/settings_overlay_shell_test.dart test/ui/dialogs/settings_tab_content_test.dart</automated>
  </verify>
  <done>A user can open the real shell at strict 16:9 geometry, sees General as the fourth selected tab in the locked seven-tab sequence, can select matching content for every tab, and focused geometry/tab widget tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove one injected work-area drag path through the tracer shell</name>
  <reversibility rating="costly">D-03 establishes the overlay coordinate contract: display work area is the primary drag bound, while a later expansion completes drag-session position caching, diagnostics, fallback hardening, and resize reconciliation.</reversibility>
  <files>lib/ui/dialogs/settings/settings_overlay_shell.dart, test/widgets/multi_monitor_clamp_test.dart</files>
  <behavior>
    - A hand-written fake DisplayEnumerator supplies an asymmetric DisplayInfo.workArea to an open SettingsOverlayShell.
    - A title-bar drag that exceeds this supplied work area ends at the work-area-derived boundary, proving the shell consumed the existing display_enumerator seam.
    - The real shell still takes its normal centered-window path when no display resolver is supplied, so existing widget constructors remain compatible.
  </behavior>
  <action>Write the failing focused widget test first using a local hand-written DisplayEnumerator fake and a deterministic work area; use the existing fake playback-controller pattern, not mocks. Extend SettingsOverlayShell with a narrow optional DisplayEnumerator injection seam whose production default constructs the existing Win32DisplayAdapter. In the drag update path, resolve getCurrentDisplay() synchronously and, when a DisplayInfo is available, constrain the candidate drag offset with its workArea and the current panel dimensions instead of using only symmetric MediaQuery bounds; preserve the existing symmetric centered clamp when no resolver/display result is available. Keep this tracer path confined to the shell and existing display abstraction: do not add packages, monitor-enumeration code, WindowService state, resize reconciliation, per-session window-position caching, FFI diagnostic expansion, or Phase 31 color-token work. Those are owned by Plans 30-02 and 30-03. Add doc comments and coordinate-conversion rationale beside the new non-trivial helper, retain immutable local values and strict typing, and keep RepaintBoundary in the rendered path per D-06.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/widgets/multi_monitor_clamp_test.dart test/ui/dialogs/settings_overlay_shell_test.dart</automated>
  </verify>
  <done>An injected fake DisplayEnumerator drives one visible title-bar drag that stops at its work area, the regular no-injection shell remains constructible with symmetric bounds, and the focused clamp plus shell tests pass.</done>
</task>

</tasks>

## Threat Model

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Flutter gesture system → SettingsOverlayShell | Framework-originated drag deltas enter layout clamping and are constrained before updating dragOffset. |
| Existing Win32 display FFI → SettingsOverlayShell | Existing display work-area information enters through DisplayEnumerator and is exercised through an injected fake in widget tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-01-01 | Tampering | Overlay drag-offset calculation | low | mitigate | Clamp the candidate offset against supplied display work-area bounds in the tracer test; Wave 2 adds the production cache, error diagnostics, and resize coverage. |
| T-30-01-02 | Denial of service | Headless display lookup | low | accept | The absent-resolver path retains the established symmetric window clamp; Phase 30 has no external service or package-install surface. |
</threat_model>

Threat refs: — (0). Secure behavior: N/A. This is a low-risk layout/sizing phase with no authentication, user-data, filesystem, network, payment, or new-package surface.

## Verification

1. Execute the Task 1 focused command after its RED and GREEN stages; it must cover representative maximum, proportional, and minimum-clamp geometry plus the locked tab sequence.
2. Execute the Task 2 focused command after its RED and GREEN stages; its fake must make the work-area-derived bound distinguishable from symmetric window bounds.
3. Run `D:/flutter/bin/flutter test test/ui/dialogs/` after the plan to protect the settings dialog boundary.
4. Run `D:/flutter/bin/flutter analyze` after all edits for strict-casts, strict-inference, and strict-raw-types compatibility.
5. Treat known headless MDK DLL failures outside the touched settings modules as baseline evidence; do not suppress, skip, or remove assertions. Plans 30-02 through 30-04 complete cache/fallback/resize and comprehensive test re-baselining.

## Goal-Backward Check

### Observable truths

- The user sees a real settings overlay at the locked strict-16:9 geometry rather than the previous 5:4 geometry.
- The user opens on General, located at the center of the exact seven-tab sequence, without changing the other tabs' relative order.
- The user can perform a real title-bar drag whose primary bound comes from the existing display work-area abstraction.
- The panel remains in its existing overlay architecture and rendering-isolation boundary.

### Reachability

- SettingsPanelController.open() sets defaultTabIndex, which drives the selectedTab ValueNotifier observed by SettingsTabStrip and SettingsTabContent.
- PlayerScreen's existing shell composition renders SettingsOverlayShell as a centered Stack sibling; its panel sizing reads MediaQuery Size and tokenized geometry values.
- The title-bar GestureDetector calls the drag handler, which obtains the injected DisplayEnumerator result and writes the constrained offset to SettingsPanelController.state.dragOffset consumed by Transform.translate.
- Focused widget tests instantiate that actual shell, invoke the same controller and title-bar gesture paths, and observe rendered dimensions, selection, and drag state.

### Source coverage audit

| Source | ID | Required outcome or constraint | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 30 | 16:9 overlay, General middle, display work-area drag clamp | Tasks 1-2 | COVERED |
| REQ | LAYOUT-01 | 16:9 panel ratio | Task 1 | COVERED |
| REQ | LAYOUT-02 | Tokenized min/clamp formula [400, 960] | Task 1 | COVERED |
| REQ | LAYOUT-03 | General tab at middle/default index | Task 1 | COVERED |
| REQ | LAYOUT-05 | Phase test baseline remains reachable for later re-baselining | Task 1 scaffolds; Plan 30-04 completes full baseline | COVERED BY PLAN SET |
| RESEARCH | Seam 1 and Pitfall 4 | Size uses full MediaQuery Size; compact tab breakpoint remains presentation-only | Task 1 | COVERED |
| RESEARCH | Seam 2 and Pitfall 1 | Reorder strip/content/controller together with a named General default index | Task 1 | COVERED |
| RESEARCH | Seam 3 | Reuse the existing injectable DisplayEnumerator; no new display mechanism | Task 2 | COVERED |
| RESEARCH | Validation Architecture | Focused fake resolver widget contract exists before multi-monitor hardening | Task 2 | COVERED |
| CONTEXT | D-01 | Seven-tab order, explicit IndexedStack, General default index | Task 1 | COVERED |
| CONTEXT | D-02 | Structural color alias only and Phase 31 visual boundary | Plan 30-03 | COVERED BY PLAN SET |
| CONTEXT | D-03 | Real work-area clamp path through existing FFI seam | Task 2; Plan 30-02 completes production hardening | COVERED BY PLAN SET |
| CONTEXT | D-04 | Strict 16:9 sizing without a sizing breakpoint branch | Task 1 | COVERED |
| CONTEXT | D-05 | Resize re-clamp | Plan 30-02 | COVERED BY PLAN SET |
| CONTEXT | D-06 | Preserve RepaintBoundary | Task 1 and Task 2 | COVERED |

No deferred ideas exist. Phase 31 control-bar chrome alignment remains outside this plan, and Plans 30-02, 30-03, and 30-04 own their assigned expansion work without duplication.

<success_criteria>
- The Wave 1 tracer visibly renders the exact token-driven strict 16:9 panel formula and retains centered overlay/RepaintBoundary architecture.
- General is defaulted and rendered at index 3 across controller, strip, and explicit content stack, with matching focused widget evidence.
- A fake-injected existing DisplayEnumerator supplies a work area that constrains one real title-bar drag path in the production shell.
- Focused widget suites and Flutter static analysis pass before Wave 2 plans expand clamp and color behavior.
</success_criteria>

<output>
Create `.planning/phases/30-panel-layout-redesign/30-01-SUMMARY.md` when done.
</output>
