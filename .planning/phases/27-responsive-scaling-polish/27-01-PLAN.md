---
phase: 27-responsive-scaling-polish
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - lib/ui/theme/tokens.dart
  - lib/ui/dialogs/settings/settings_overlay_shell.dart
autonomous: true
requirements:
  - SCALE-01
  - SCALE-02
  - SCALE-03
must_haves:
  truths:
    - Panel width is clamped(windowWidth * 0.8, 400, 600) — smooth continuous scaling
    - Panel height follows 5:4 ratio from computed width
    - Tab bar switches between normal (14px/16px) and compact (12px/8px) at 800px breakpoint
    - Panel outer layer is wrapped in RepaintBoundary for 60fps isolation
    - BackdropFilter blur remains active during open/close animation
    - No animated transition when crossing 800px breakpoint — immediate switch
  artifacts:
    - lib/ui/theme/tokens.dart (new responsive constants)
    - lib/ui/dialogs/settings/settings_overlay_shell.dart (RepaintBoundary + responsive sizing)
  key_links:
    - MediaQuery.sizeOf drives both panel scaling and tab bar mode
    - Tokens responsive constants consumed by SettingsOverlayShell._buildPanel
---

<objective>
Implement responsive scaling for the settings panel: continuous width scaling (clamp 400-600), 5:4 height ratio, 800px breakpoint tab bar adaptation, and RepaintBoundary isolation for 60fps animation.

Purpose: Panel must adapt smoothly from small windows (400px) to fullscreen (600px) while tab bar compresses at <800px. RepaintBoundary prevents panel redraws from triggering PlayerScreen repaints during animation.

Output: Responsive tokens in tokens.dart, updated SettingsOverlayShell with RepaintBoundary and responsive sizing logic.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-responsive-scaling-polish/27-CONTEXT.md
@lib/ui/theme/tokens.dart
@lib/ui/dialogs/settings/settings_overlay_shell.dart
@lib/ui/shared/glass_container.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add responsive constants to tokens.dart and wire panel sizing in SettingsOverlayShell</name>
  <files>lib/ui/theme/tokens.dart, lib/ui/dialogs/settings/settings_overlay_shell.dart, test/ui/dialogs/settings_responsive_scaling_test.dart</files>
  <read_first>
    - lib/ui/theme/tokens.dart — current token structure, existing breakpoints at lines 226-228
    - lib/ui/dialogs/settings/settings_overlay_shell.dart — current _panelSize/_clampDimension at lines 213-222, _buildPanel at lines 229-271, _buildTabBar at lines 276-298
    - test/ui/dialogs/settings_overlay_shell_test.dart — existing test patterns and pumpShell helper
    - test/ui/dialogs/settings_panel_controller_test.dart — FakePlaybackController used in tests
  </read_first>
  <behavior>
    - Test: panelWidth(1920) returns 600.0 (clamped by maxWidth)
    - Test: panelWidth(800) returns 640.0 (800 * 0.8 = 640, within 400-600 range)
    - Test: panelWidth(500) returns 400.0 (clamped by minWidth)
    - Test: panelHeight(600) returns 480.0 (600 * 4/5 = 480)
    - Test: panelHeight(400) returns 320.0 (400 * 4/5 = 320)
    - Test: window >= 800px renders tab items with fontSize 14.0
    - Test: window < 800px renders tab items with fontSize 12.0
    - Test: panel is wrapped in RepaintBoundary widget
  </behavior>
  <action>
Step 1 — Add responsive constants to tokens.dart (per D-05):
  After the existing breakpoints section (line 228), add a new "Responsive" section:
  - `breakpointResponsive = 800.0` (per D-01: single breakpoint, >=800 normal, <800 compact)
  - `panelMinWidth = 400.0` (per D-04: clamp lower bound)
  - `panelMaxWidth = 600.0` (per D-04: clamp upper bound)
  - `panelWidthRatio = 0.8` (per D-04: 80% of window width)
  - `panelHeightRatio = 0.8` (per D-04: height follows width; actual ratio 5:4 computed in shell)
  - `tabBarFontNormal = 14.0` (per D-02: normal mode font)
  - `tabBarFontCompact = 12.0` (per D-03: compact mode font)
  - `tabBarSpacingNormal = 16.0` (per D-02: normal mode spacing)
  - `tabBarSpacingCompact = 8.0` (per D-03: compact mode spacing)

Step 2 — Update SettingsOverlayShell panel sizing (per D-04):
  Replace `_panelSize` and `_clampDimension` methods (lines 213-222) with:
  - Remove `panelWidthRatio` and `panelHeightRatio` static constants (lines 73-77)
  - New `_panelWidth(double windowWidth)` computes `(windowWidth * Tokens.panelWidthRatio).clamp(Tokens.panelMinWidth, Tokens.panelMaxWidth)`
  - New `_panelHeight(double width)` computes `width * 5.0 / 4.0` (5:4 ratio per D-04)
  - Update `_buildPanel` to use new sizing: `final width = _panelWidth(mediaSize.width); final height = _panelHeight(width);`

Step 3 — Wire tab bar compact mode (per D-01/D-02/D-03):
  In `_buildTabBar` (line 276), compute `isCompact = mediaSize.width < Tokens.breakpointResponsive`
  Pass isCompact to `_buildTabBar` method signature.
  In the Container, use `height: isCompact ? 48 : 64` (compact reduces height).
  In each SettingsNavItem, pass `fontSize: isCompact ? Tokens.tabBarFontNormal : Tokens.tabBarFontCompact` — wait, re-read D-02/D-03:
  - D-02 (normal): 14px font + 16px spacing
  - D-03 (compact): 12px font + 8px spacing
  So: `fontSize: isCompact ? Tokens.tabBarFontCompact : Tokens.tabBarFontNormal`
  And spacing: `padding: EdgeInsets.symmetric(horizontal: isCompact ? Tokens.tabBarSpacingCompact : Tokens.tabBarSpacingNormal)` inside each nav item.

Step 4 — Add RepaintBoundary wrapper (per D-07):
  In `_buildPanel`, wrap the `FocusTraversalGroup` child with `RepaintBoundary`:
  `RepaintBoundary(child: FocusTraversalGroup(...))`
  This isolates panel redraws from PlayerScreen during animation. BackdropFilter blur stays active per D-08.

Step 5 — Verify D-09 compliance (no transition animation on breakpoint crossing):
  The tab bar uses immediate `isCompact` boolean — no AnimatedContainer or Tween. This is correct per D-09.

Step 6 — Create test file `test/ui/dialogs/settings_responsive_scaling_test.dart`:
  Reuse the `pumpShell` pattern from `settings_overlay_shell_test.dart` (import `FakePlaybackController` from `settings_panel_controller_test.dart`).
  Tests:
  - `panelWidth scales continuously and clamps`: pump at 1920x1080, assert panel width via find.byKey(panelKey) -> SizedBox -> width
  - `panelWidth clamps to 400 at small window`: pump at 500x400, assert panel width
  - `panelHeight follows 5:4 ratio`: pump at 1000x800, assert height = width * 5/4
  - `tab bar uses normal font at >= 800px`: pump at 1000x800, assert tab text style fontSize 14.0
  - `tab bar uses compact font at < 800px`: pump at 600x400, assert tab text style fontSize 12.0
  - `panel wrapped in RepaintBoundary`: pump at 800x600, assert RepaintBoundary ancestor exists
  - `BackdropFilter present during open animation`: pump at 800x600, open panel, assert BackdropFilter present
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter test test/ui/dialogs/settings_responsive_scaling_test.dart -x mdk-dll 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tokens.dart contains: breakpointResponsive=800, panelMinWidth=400, panelMaxWidth=600, panelWidthRatio=0.8, tabBarFontNormal=14, tabBarFontCompact=12, tabBarSpacingNormal=16, tabBarSpacingCompact=8
    - settings_overlay_shell.dart has no panelWidthRatio/panelHeightRatio constants
    - _panelWidth(1920) returns 600.0, _panelWidth(500) returns 400.0, _panelWidth(800) returns 640.0
    - _panelHeight(600) returns 480.0 (600 * 5/4)
    - _buildTabBar receives isCompact parameter
    - Tab items use fontSize 12.0 when window < 800, fontSize 14.0 when >= 800
    - FocusTraversalGroup child is wrapped in RepaintBoundary
    - All new tests pass
    - `flutter analyze` reports no new errors
  </acceptance_criteria>
  <done>Panel scales continuously (400-600), tab bar adapts at 800px breakpoint, RepaintBoundary wraps panel, all tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for settings panel open/close/tab-switch/drag/keyboard paths</name>
  <files>test/ui/dialogs/settings_responsive_integration_test.dart</files>
  <read_first>
    - test/ui/dialogs/settings_overlay_shell_test.dart — existing pumpShell helper, FakePlaybackController pattern, established assertions
    - test/ui/dialogs/settings_focus_navigation_test.dart — keyboard navigation test patterns
    - lib/ui/dialogs/settings/settings_overlay_shell.dart — current build method, key events at lines 435-489
    - test/ui/dialogs/settings_panel_controller_test.dart — FakePlaybackController export
  </read_first>
  <behavior>
    - Test: open panel -> mask + panel visible + title set
    - Test: close panel -> mask + panel removed
    - Test: switch tab -> content changes (IndexedStack index)
    - Test: resize window across 800px breakpoint -> tab bar font size changes immediately
    - Test: drag title bar -> panel moves within bounds
    - Test: ESC key closes panel
    - Test: B key closes panel
    - Test: LB/RB cycles tabs
  </behavior>
  <action>
Create `test/ui/dialogs/settings_responsive_integration_test.dart` with the following test groups:

1. **Panel lifecycle** — open/close/reopen:
   - Open panel: assert shellKey visible, mask visible, title "设置" visible
   - Close panel (mask tap): assert mask removed after animation delay
   - Reopen: assert shell re-appears

2. **Tab switching**:
   - Open panel, tap tab 2 (音频): assert selectedTab changes
   - Tap tab 0 (通用): assert back to first tab

3. **Responsive breakpoint crossing** (per D-01/D-09):
   - Pump at 1000x800 (normal): assert tab text fontSize 14.0
   - Pump again at 600x400 (compact): assert tab text fontSize 12.0 — immediate, no transition animation

4. **Drag bounds** (per PANEL-07 / D-04):
   - Open panel at 1000x800: drag title bar 500px right
   - Assert dragOffset is clamped (panel stays within window bounds)

5. **Keyboard shortcuts**:
   - ESC closes panel
   - B closes panel
   - LB/RB cycles tabs (gameButton13/gameButton12)

6. **RepaintBoundary isolation** (per D-07):
   - Pump at 800x600 with panel open
   - Assert RepaintBoundary ancestor of GlassContainer
   - Assert BackdropFilter present (per D-08: blur stays during animation)

Pattern: reuse `pumpShell` from settings_overlay_shell_test.dart via import.
Use `tester.binding.setSurfaceSize(Size)` or MediaQuery override for window size changes.
</action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter test test/ui/dialogs/settings_responsive_integration_test.dart -x mdk-dll 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 8+ test cases pass
    - Tests cover: open, close, tab switch, breakpoint crossing, drag bounds, keyboard (ESC/B/LB/RB), RepaintBoundary
    - No test depends on mdk.dll (all use FakePlaybackController)
    - `flutter test test/ui/dialogs/settings_responsive_integration_test.dart` exits with code 0
  </acceptance_criteria>
  <done>Integration test suite covers panel lifecycle, responsive breakpoint, drag, keyboard, and RepaintBoundary isolation paths. All tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Window resize events | MediaQuery updates trigger panel recalculation — must not cause layout overflow |
| Tab bar mode switch | Compact/normal switch must not reset selected tab or lose focus state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | MediaQuery override in tests | low | accept | Test-only, no production impact |
| T-27-02 | Denial of Service | Rapid window resize causing layout thrash | low | accept | Flutter layout pipeline inherently debounces; no custom resize handler needed |
| T-27-SC | Tampering | npm/pip/cargo installs | N/A | N/A | No package installs in this phase |
</threat_model>

<verification>
1. `flutter analyze` — zero new errors
2. `flutter test test/ui/dialogs/settings_responsive_scaling_test.dart` — all pass
3. `flutter test test/ui/dialogs/settings_responsive_integration_test.dart` — all pass
4. Panel width at 1920px window = 600.0 (verified by test)
5. Panel width at 500px window = 400.0 (verified by test)
6. Tab bar font switches at 800px (verified by test)
7. RepaintBoundary wraps panel (verified by test)
</verification>

<success_criteria>
- Continuous scaling clamp(windowWidth * 0.8, 400, 600) works across all window sizes
- 5:4 height ratio maintained
- 800px breakpoint switches tab bar between normal/compact modes
- RepaintBoundary isolates panel from PlayerScreen redraws
- BackdropFilter blur persists during animation
- All existing tests still pass
- Zero flutter analyze errors
</success_criteria>

<output>
Create `.planning/phases/27-responsive-scaling-polish/27-01-SUMMARY.md` when done
</output>
