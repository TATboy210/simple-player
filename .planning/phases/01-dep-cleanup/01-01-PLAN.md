---
phase: 01-dep-cleanup
plan: 01
type: execute
wave: 2
depends_on: []
files_modified:
  - lib/kernel/engine/fvp_callback_handler.dart
  - lib/kernel/engine/fvp_engine.dart
  - lib/kernel/engine/media_opener.dart
  - lib/kernel/engine/mock_engine.dart
  - lib/kernel/engine/open_result.dart
  - lib/kernel/engine/track_manager.dart
  - lib/kernel/engine/video_effect_controller.dart
  - lib/app.dart
  - lib/ui/player/auto_hide_controller.dart
  - lib/ui/player/control_bar.dart
  - lib/ui/player/controls_overlay.dart
  - lib/ui/player/center_controls.dart
  - lib/ui/player/error_banner.dart
  - lib/ui/player/player_screen.dart
  - lib/ui/player/progress_bar.dart
  - lib/ui/player/speed_button.dart
  - lib/ui/player/time_range_display.dart
  - lib/ui/player/video_surface.dart
  - lib/ui/player/volume_controls.dart
  - lib/ui/shared/empty_state.dart
  - lib/ui/shared/aurora_background.dart
  - lib/ui/dialogs/media_info_dialog.dart
  - lib/ui/dialogs/settings_panel.dart
  - lib/ui/dialogs/settings/audio_tab.dart
  - lib/ui/dialogs/settings/equalizer_tab.dart
  - lib/ui/dialogs/settings/settings_tab_performance.dart
  - lib/features/player/player_feature.dart
  - lib/features/player/deferred_player_feature.dart
  - lib/features/player/player_services.dart
  - lib/features/player/services/playback_controller.dart
  - lib/features/player/services/playback_navigator.dart
  - lib/features/player/services/video_processing_service.dart
  - lib/features/player/services/state_monitor.dart
  - lib/features/player/services/subtitle_service.dart
  - test/helpers/fake_engine.dart
  - test/golden/control_layouts_golden_test.dart
  - test/debug/button_hit_test.dart
  - test/perf/control_bar_perf_test.dart
  - test/integration/playback_flow_test.dart
  - test/features/player/services/subtitle_service_test.dart
  - test/kernel/models/media_info_test.dart
  - test/widget/shared/aurora_background_test.dart
  - test/widget/player/auto_hide_controller_test.dart
  - test/widget/player/control_bar_startup_test.dart
  - test/widget/player/control_bar_test.dart
  - test/widget/player/controls_overlay_test.dart
  - test/widget/player/volume_controls_test.dart
  - test/kernel/engine/fvp_callback_handler_test.dart
  - test/kernel/engine/track_manager_test.dart
  - test/kernel/engine/media_opener_test.dart
  - test/kernel/engine/video_effect_controller_test.dart
  - test/kernel/services/playback_controller_test.dart
  - test/kernel/services/state_monitor_test.dart
  - test/kernel/services/video_processing_service_test.dart
  - test/widget/player/video_surface_test.dart
  - test/widget/player/error_banner_test.dart
autonomous: true
requirements:
  - DEP-02
  - DEP-03
  - DEP-04
must_haves:
  truths:
    - All 56 source files import engine types via local relative paths, not package:player_engine
    - Engine internal 7 files use direct file imports (not barrel) for types they need
    - barrel export file (player_engine.dart) still exports all 8 symbols unchanged
    - No file imports from a non-existent path (all relative paths resolve)
  artifacts:
    - 56 .dart files with rewritten import statements
  key_links:
    - Engine internal direct imports to leaf types (enums, models, abstract class)
    - External consumers to barrel (player_engine.dart) via relative path
    - Test files to barrel via relative path crossing lib/ boundary
---

<objective>
Migrate all 56 source files (34 lib/ + 22 test/) from `package:player_engine/player_engine.dart` imports to local relative path imports.

Purpose: Eliminate the fragile external path dependency by making all imports resolve to the local `lib/kernel/engine/` directory.
Output: 56 files with corrected import paths; barrel file unchanged; engine internal files using direct imports per D-09.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dep-cleanup/01-CONTEXT.md
@.planning/phases/01-dep-cleanup/01-RESEARCH.md
@lib/kernel/engine/player_engine.dart
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate 49 external consumer imports to relative barrel paths</name>
  <files>
lib/app.dart, lib/ui/player/*.dart, lib/ui/shared/*.dart, lib/ui/dialogs/*.dart, lib/ui/dialogs/settings/*.dart, lib/features/player/*.dart, lib/features/player/services/*.dart, test/helpers/fake_engine.dart, test/golden/*.dart, test/debug/*.dart, test/perf/*.dart, test/integration/*.dart, test/features/**/*.dart, test/kernel/**/*.dart, test/widget/**/*.dart
  </files>
  <read_first>
lib/kernel/engine/player_engine.dart
.planning/phases/01-dep-cleanup/01-RESEARCH.md (Relative Path Calculation table)
pubspec.yaml
  </read_first>
  <action>
Replace `import 'package:player_engine/player_engine.dart';` with the correct relative path in all 49 non-engine source files. Do NOT touch the 7 engine internal files yet (Task 2 handles those).

Use PowerShell to find all affected files and compute relative paths:

Step 1 — Get the list of non-engine files to migrate:
```
(Get-ChildItem -Path lib, test -Recurse -Filter *.dart) |
  Select-String -Pattern "package:player_engine" |
  Where-Object { $_.Path -notmatch "lib[/\\]kernel[/\\]engine[/\\]" } |
  Select-Object -ExpandProperty Path -Unique
```

Step 2 — For each file, compute the relative path from its directory to `lib/kernel/engine/player_engine.dart`:

| File Location Pattern | Relative Path To Use |
|----------------------|---------------------|
| `lib/app.dart` | `kernel/engine/player_engine.dart` |
| `lib/ui/player/*.dart` | `../../kernel/engine/player_engine.dart` |
| `lib/ui/shared/*.dart` | `../../kernel/engine/player_engine.dart` |
| `lib/ui/dialogs/*.dart` | `../../kernel/engine/player_engine.dart` |
| `lib/ui/dialogs/settings/*.dart` | `../../../kernel/engine/player_engine.dart` |
| `lib/features/player/*.dart` | `../../kernel/engine/player_engine.dart` |
| `lib/features/player/services/*.dart` | `../../../kernel/engine/player_engine.dart` |
| `test/helpers/*.dart` | `../../lib/kernel/engine/player_engine.dart` |
| `test/golden/*.dart` | `../../lib/kernel/engine/player_engine.dart` |
| `test/debug/*.dart` | `../../lib/kernel/engine/player_engine.dart` |
| `test/perf/*.dart` | `../../lib/kernel/engine/player_engine.dart` |
| `test/integration/*.dart` | `../../lib/kernel/engine/player_engine.dart` |
| `test/features/**/*.dart` | varies (compute from file depth) |
| `test/kernel/**/*.dart` | varies (compute from file depth) |
| `test/widget/**/*.dart` | varies (compute from file depth) |

Step 3 — Apply the replacement. Use PowerShell per-file replacement:
```powershell
$file = "path/to/file.dart"
$old = "import 'package:player_engine/player_engine.dart';"
$new = "import 'THE_COMPUTED_RELATIVE_PATH';"
(Get-Content $file -Raw) -replace [regex]::Escape($old), $new | Set-Content $file -NoNewline
```

Per D-01, use real relative paths (not `package:simple_player_flutter/...`).

IMPORTANT: Preserve the existing import ordering convention (dart: -> package: -> relative). The `package:player_engine` import is typically the third import group; the new relative import should stay in the same position within the import block.
  </action>
  <verify>
    <automated>grep -r "package:player_engine" lib/ test/ --include="*.dart" | grep -v "lib/kernel/engine/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
- grep -r "package:player_engine" lib/ test/ --include="*.dart" returns ONLY the 7 engine internal files (fvp_callback_handler, fvp_engine, media_opener, mock_engine, open_result, track_manager, video_effect_controller) — zero non-engine hits
- Each file's relative path correctly resolves to lib/kernel/engine/player_engine.dart from its location
- No `package:simple_player_flutter/` imports were introduced (per D-01)
- Import ordering convention preserved (relative imports in correct group position)
- The 7 engine internal files are UNCHANGED (still have `package:player_engine` — Task 2 handles them)
  </acceptance_criteria>
  <done>
49 non-engine source files have relative path imports to player_engine.dart barrel. 7 engine internal files remain unchanged. grep confirms zero non-engine `package:player_engine` hits.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate 7 engine internal files to direct file imports</name>
  <files>
lib/kernel/engine/fvp_callback_handler.dart, lib/kernel/engine/fvp_engine.dart, lib/kernel/engine/media_opener.dart, lib/kernel/engine/mock_engine.dart, lib/kernel/engine/open_result.dart, lib/kernel/engine/track_manager.dart, lib/kernel/engine/video_effect_controller.dart
  </files>
  <read_first>
lib/kernel/engine/player_engine.dart (barrel — to verify 8 exported symbols)
lib/kernel/engine/fvp_callback_handler.dart
lib/kernel/engine/fvp_engine.dart
lib/kernel/engine/media_opener.dart
lib/kernel/engine/mock_engine.dart
lib/kernel/engine/open_result.dart
lib/kernel/engine/track_manager.dart
lib/kernel/engine/video_effect_controller.dart
  </read_first>
  <action>
Replace `import 'package:player_engine/player_engine.dart';` in each of the 7 engine files with the specific direct file imports for the types they actually use (per D-03, D-09, D-10). All imports use same-directory relative paths since all files are in `lib/kernel/engine/`.

1. `fvp_callback_handler.dart` — uses `MediaState` only:
   Replace barrel import with: `import 'media_state.dart';`

2. `fvp_engine.dart` — uses `MediaState`, `MediaErrorType`, `PlayerEngine` (extends), `MediaInfo`, `AudioTrackInfo`, `SubtitleTrackInfo`, `VideoEffectType`:
   Replace barrel import with these direct imports:
   `import 'media_state.dart';`
   `import 'media_error_type.dart';`
   `import 'player_engine_base.dart';`
   `import 'models/media_info.dart';`
   `import 'models/audio_track_info.dart';`
   `import 'models/subtitle_track_info.dart';`
   `import 'video_effect_type.dart';`

3. `media_opener.dart` — uses `MediaErrorType` only:
   Replace barrel import with: `import 'media_error_type.dart';`

4. `mock_engine.dart` — uses `MediaState`, `MediaErrorType`, `MediaInfo`, `PlayerEngine` (implements), `AudioTrackInfo`, `SubtitleTrackInfo`, `VideoEffectType`:
   Replace barrel import with these direct imports:
   `import 'media_state.dart';`
   `import 'media_error_type.dart';`
   `import 'player_engine_base.dart';`
   `import 'models/media_info.dart';`
   `import 'models/audio_track_info.dart';`
   `import 'models/subtitle_track_info.dart';`
   `import 'video_effect_type.dart';`

5. `open_result.dart` — uses `MediaInfo`, `MediaErrorType`:
   Replace barrel import with:
   `import 'models/media_info.dart';`
   `import 'media_error_type.dart';`

6. `track_manager.dart` — uses `MediaInfo`, `AudioTrackInfo`, `SubtitleTrackInfo`:
   Replace barrel import with:
   `import 'models/media_info.dart';`
   `import 'models/audio_track_info.dart';`
   `import 'models/subtitle_track_info.dart';`

7. `video_effect_controller.dart` — uses `VideoEffectType` only:
   Replace barrel import with: `import 'video_effect_type.dart';`
  </action>
  <verify>
    <automated>grep -r "package:player_engine" lib/kernel/engine/ --include="*.dart" | wc -l</automated>
  </verify>
  <acceptance_criteria>
- grep -r "package:player_engine" lib/kernel/engine/ returns zero results
- fvp_engine.dart imports exactly: media_state.dart, media_error_type.dart, player_engine_base.dart, models/media_info.dart, models/audio_track_info.dart, models/subtitle_track_info.dart, video_effect_type.dart
- mock_engine.dart imports exactly: media_state.dart, media_error_type.dart, player_engine_base.dart, models/media_info.dart, models/audio_track_info.dart, models/subtitle_track_info.dart, video_effect_type.dart
- open_result.dart imports exactly: models/media_info.dart, media_error_type.dart
- track_manager.dart imports exactly: models/media_info.dart, models/audio_track_info.dart, models/subtitle_track_info.dart
- fvp_callback_handler.dart imports exactly: media_state.dart
- media_opener.dart imports exactly: media_error_type.dart
- video_effect_controller.dart imports exactly: video_effect_type.dart
- No circular imports introduced (each file imports only leaf types, not other engine logic files)
  </acceptance_criteria>
  <done>
All 7 engine internal files use direct file imports for the specific types they need. Zero `package:player_engine` references remain in the engine directory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Import resolution | Dart compiler resolves relative paths at build time — wrong paths cause compile errors (fail-fast) |
| Engine internal imports | Direct file imports between engine files must not create circular dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Tampering | Import paths | low | accept | Dart compiler catches broken imports at build time (fail-fast) |
| T-1-02 | Repudiation | Import paths | low | accept | Grep verification catches any residual package:player_engine references |
| T-1-03 | Denial of Service | Build system | medium | mitigate | Verify flutter analyze + flutter test pass after migration |
| T-1-04 | Info Disclosure | N/A | none | N/A | No data handling changes in this phase |
| T-1-05 | Elevation of Privilege | N/A | none | N/A | No permission changes |
| T-1-SC | Tampering | npm/pip/cargo installs | none | N/A | No packages installed this phase (only removal) |
</threat_model>

<verification>
## Post-Migration Verification

1. `grep -r "package:player_engine" lib/ test/ --include="*.dart"` — must return zero results (all 56 files migrated)
2. `flutter analyze` — zero errors, zero warnings (all relative paths resolve correctly)
3. `flutter test` — all existing tests pass (no behavioral changes)
4. Manual check: `lib/kernel/engine/player_engine.dart` still has exactly 8 export lines (unchanged)
5. Spot-check 3-5 files from different directories to confirm relative paths resolve correctly
</verification>

<success_criteria>
- grep -r "package:player_engine" lib/ test/ returns zero results (DEP-02 satisfied)
- flutter analyze zero errors zero warnings
- flutter test all pass
- lib/kernel/engine/player_engine.dart barrel export contains all 8 symbols unchanged (DEP-03 satisfied)
- Engine internal 7 files use direct file imports (DEP-04 satisfied)
- 49 external consumer files use relative path to barrel
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

**Modified files (56):**
- 49 non-engine .dart files — import rewritten from `package:player_engine/player_engine.dart` to relative path
- 7 engine .dart files — import rewritten from barrel to direct file imports

**No new files created.** This phase only modifies existing files.

**Symbols affected:** None. All 8 exported symbols (MediaErrorType, MediaState, AudioTrackInfo, MediaInfo, SubtitleTrackInfo, VideoCodecInfo, PlayerEngine, VideoEffectType) continue to be accessible via the barrel. Import paths change, but the symbols and their definitions are untouched.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-dep-cleanup/01-01-SUMMARY.md` when done
</output>
