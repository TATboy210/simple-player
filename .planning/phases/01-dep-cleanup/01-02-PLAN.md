---
phase: 01-dep-cleanup
plan: 02
type: execute
wave: 3
depends_on:
  - 01-01
files_modified:
  - pubspec.yaml
  - .planning/REQUIREMENTS.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STACK.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DEP-01
  - DEP-02
must_haves:
  truths:
    - pubspec.yaml has no player_engine entry
    - Zero files in lib/, test/, or pubspec.yaml reference package:player_engine
    - flutter analyze passes with zero errors
    - flutter test all pass
    - Planning docs reflect correct file count (56)
  artifacts:
    - pubspec.yaml without player_engine dependency
    - Updated REQUIREMENTS.md with corrected count
    - Updated CONVENTIONS.md with relative path import example
    - Updated ARCHITECTURE.md, STACK.md, ROADMAP.md, PROJECT.md with corrected player_engine references
  key_links:
    - pubspec.yaml dependency removal enables self-contained project
    - flutter pub get succeeds without the removed path dependency
---

<objective>
Remove the `player_engine` path dependency from pubspec.yaml, update planning docs with correct counts, and run full verification.

Purpose: Complete the dependency cleanup by removing the external path reference after all source imports have been migrated (Plan 01).
Output: Self-contained pubspec.yaml, verified build, updated documentation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dep-cleanup/01-CONTEXT.md
@.planning/phases/01-dep-cleanup/01-RESEARCH.md
@.planning/phases/01-dep-cleanup/01-01-SUMMARY.md
@pubspec.yaml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove player_engine dependency from pubspec.yaml</name>
  <files>pubspec.yaml</files>
  <read_first>
pubspec.yaml
  </read_first>
  <action>
In `pubspec.yaml`, remove the `player_engine` path dependency entry (per D-02). The entry occupies lines 14-15:

```yaml
  player_engine:
    path: ../widget_tree_flutter/player_engine
```

Delete both lines entirely. The resulting `dependencies:` section should go directly from `flutter_localizations:` to `fvp:`:

```yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  fvp: ^0.37.2
```

Verify that the YAML indentation remains consistent (2-space indent for dependency names, 4-space indent for nested values like `sdk:`).

After removal, run `flutter pub get` to confirm the dependency graph resolves without the removed entry.
  </action>
  <verify>
    <automated>(Select-String -Path pubspec.yaml -Pattern "player_engine").Count</automated>
  </verify>
  <acceptance_criteria>
- pubspec.yaml contains zero lines with `player_engine`
- `flutter pub get` succeeds (exit code 0) — confirms no other dependency references the removed package
- YAML indentation is consistent and valid
  </acceptance_criteria>
  <done>
pubspec.yaml has no player_engine dependency. flutter pub get succeeds.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update planning docs and run full verification</name>
  <files>.planning/REQUIREMENTS.md, .planning/codebase/CONVENTIONS.md</files>
  <read_first>
.planning/REQUIREMENTS.md
.planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
**Part A — Update REQUIREMENTS.md (per D-06):**

In `.planning/REQUIREMENTS.md`, update DEP-02 description. Change the file count from "37 个文件" to "56 个文件":

Before: `- [ ] **DEP-02**: 将 37 个文件的 import 从 package:player_engine/player_engine.dart 改为本地相对路径`
After: `- [ ] **DEP-02**: 将 56 个文件的 import 从 package:player_engine/player_engine.dart 改为本地相对路径`

**Part B — Update CONVENTIONS.md:**

In `.planning/codebase/CONVENTIONS.md`, update the import example section (around line 24-28). Replace the `package:player_engine` example with a relative path example:

Before:
```
import 'package:player_engine/player_engine.dart';
import '../../kernel/models/playlist_item.dart';
```

After:
```
import '../../kernel/engine/player_engine.dart';
import '../../kernel/models/playlist_item.dart';
```

**Part C — Run full verification (per D-12):**

Execute these checks in order:

1. `(Get-ChildItem -Path lib, test -Recurse -Filter *.dart | Select-String -Pattern "package:player_engine").Count` and `(Select-String -Path pubspec.yaml -Pattern "player_engine").Count` — both must return zero
2. `flutter analyze` — must return zero errors and zero warnings
3. `flutter test` — all tests must pass
4. `(Select-String -Path lib/kernel/engine/player_engine.dart -Pattern "^export").Count` — must return 8 (barrel still exports all symbols)
  </action>
  <verify>
    <automated>$remaining = (Get-ChildItem -Path lib, test -Recurse -Filter *.dart | Select-String -Pattern "package:player_engine").Count; $yamlRemaining = (Select-String -Path pubspec.yaml -Pattern "player_engine").Count; Write-Output "Dart files remaining: $remaining"; Write-Output "YAML files remaining: $yamlRemaining"; flutter analyze 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
- PowerShell `(Get-ChildItem -Path lib, test -Recurse -Filter *.dart | Select-String -Pattern "package:player_engine").Count` returns zero; `(Select-String -Path pubspec.yaml -Pattern "player_engine").Count` returns zero
- flutter analyze returns zero errors and zero warnings
- flutter test passes all tests
- lib/kernel/engine/player_engine.dart has exactly 8 export lines
- REQUIREMENTS.md DEP-02 says "56 个文件"
- CONVENTIONS.md import example uses relative path (not package:player_engine)
  </acceptance_criteria>
  <done>
All verification checks pass. pubspec.yaml is self-contained. Documentation reflects actual file counts. The project builds and tests cleanly with no external player_engine dependency.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update remaining .planning/ docs with corrected player_engine references (per D-05)</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/STACK.md, .planning/ROADMAP.md, .planning/PROJECT.md</files>
  <read_first>
.planning/codebase/ARCHITECTURE.md
.planning/codebase/STACK.md
.planning/ROADMAP.md
.planning/PROJECT.md
  </read_first>
  <action>
Update the remaining 4 .planning/ files that reference `player_engine` to reflect the completed migration (per D-05). Each file needs targeted edits:

**Part A — ARCHITECTURE.md:**
Find the table entry referencing `player_engine` as "Abstract `PlayerEngine` interface (local package)" and update the description to note the path dependency has been removed. For example, change "local package" to "removed — imports now use local relative paths".

**Part B — STACK.md:**
Find the dependency table entry for `player_engine` (marked as "local path" dependency) and update to note removal. Change "local path" to "removed" and add a note that the dependency was eliminated in Phase 1.

**Part C — ROADMAP.md:**
Update Phase 1 success criteria references. The success criteria mention `grep -r "package:player_engine" lib/` — replace with PowerShell equivalent: `(Get-ChildItem -Path lib -Recurse -Filter *.dart | Select-String -Pattern "package:player_engine").Count`. Also update any stale file counts if present.

**Part D — PROJECT.md:**
Update the "外部 player_engine 包结构" section and the todo list to reflect that the external dependency has been removed. Change the status from "Pending" to "Done" for the removal item. Update the file count references from stale numbers to 56 if present.

NOTE: `.planning/research/` files (ARCHITECTURE.md, PITFALLS.md, SUMMARY.md, STACK.md in research/) are historical reference documents — do NOT modify them. They describe the pre-migration state and remain valuable as context.
  </action>
  <verify>
    <automated>$refCount = (Get-ChildItem -Path .planning/codebase, .planning/ROADMAP.md, .planning/PROJECT.md -Recurse -Filter *.md | Select-String -Pattern "package:player_engine").Count; Write-Output "Remaining 'package:player_engine' references in updated docs: $refCount"</automated>
  </verify>
  <acceptance_criteria>
- ARCHITECTURE.md no longer describes player_engine as an active dependency
- STACK.md no longer lists player_engine as an active path dependency
- ROADMAP.md Phase 1 success criteria use PowerShell-compatible verification commands
- PROJECT.md marks player_engine removal as Done
- Zero `package:player_engine` import syntax references remain in the 4 updated files (prose references to the historical dependency are acceptable)
  </acceptance_criteria>
  <done>
All 4 planning docs reflect the completed migration. D-05 scope is fully covered. Research docs are untouched (historical reference).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build verification | flutter analyze + flutter test confirm no regressions from import changes |
| pubspec dependency graph | flutter pub get confirms no dangling references to removed package |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | pubspec.yaml | low | accept | Single dependency removal verified by pub get + analyze |
| T-2-02 | Denial of Service | Build system | medium | mitigate | Run flutter analyze + flutter test to catch any broken imports |
| T-2-03 | Info Disclosure | N/A | none | N/A | No data handling changes |
| T-2-SC | Tampering | npm/pip/cargo installs | none | N/A | No packages installed this phase |
</threat_model>

<verification>
## Full Phase Verification

1. `(Get-ChildItem -Path lib, test -Recurse -Filter *.dart | Select-String -Pattern "package:player_engine").Count` — must return zero; `(Select-String -Path pubspec.yaml -Pattern "player_engine").Count` — must return zero
2. `flutter pub get` — succeeds without player_engine dependency
3. `flutter analyze` — zero errors, zero warnings
4. `flutter test` — all tests pass
5. `(Select-String -Path lib/kernel/engine/player_engine.dart -Pattern "^export").Count` — returns 8
6. REQUIREMENTS.md DEP-02 states "56 个文件"
7. CONVENTIONS.md import example shows relative path
</verification>

<success_criteria>
- pubspec.yaml contains no player_engine dependency (DEP-01 satisfied)
- PowerShell `(Get-ChildItem -Path lib, test -Recurse -Filter *.dart | Select-String -Pattern "package:player_engine").Count` returns zero (DEP-02 satisfied)
- flutter analyze zero errors zero warnings
- flutter test all pass
- `lib/kernel/engine/player_engine.dart` barrel export contains all 8 symbols (verified via `(Select-String -Path lib/kernel/engine/player_engine.dart -Pattern "^export").Count` = 8, DEP-03 satisfied)
- Engine internal imports verified by successful build (DEP-04 satisfied)
- Planning docs updated with correct counts
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

**Modified files (7):**
- pubspec.yaml — `player_engine` path dependency removed
- .planning/REQUIREMENTS.md — DEP-02 file count corrected (37 -> 56)
- .planning/codebase/CONVENTIONS.md — import example updated to relative path
- .planning/codebase/ARCHITECTURE.md — player_engine dependency status updated
- .planning/codebase/STACK.md — player_engine dependency status updated
- .planning/ROADMAP.md — success criteria verification commands updated
- .planning/PROJECT.md — dependency removal status updated to Done

**No new files created.**

**Verification artifacts (ephemeral):**
- flutter analyze output confirming zero errors/warnings
- flutter test output confirming all tests pass
- PowerShell Select-String output confirming zero package:player_engine references
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-dep-cleanup/01-02-SUMMARY.md` when done
</output>
