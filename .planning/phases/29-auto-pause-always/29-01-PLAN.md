---
phase: 29-auto-pause-always
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/dialogs/settings/settings_panel_controller.dart
  - test/ui/dialogs/settings_panel_controller_test.dart
autonomous: true
requirements:
  - PAUSE-01
  - PAUSE-02
  - PAUSE-03
  - PAUSE-04
must_haves:
  truths:
    - "PAUSE-01: Opening a closed settings panel always invokes the existing SettingsPanelPlayback.pause() command exactly once, whether playback was active or not."
    - "PAUSE-02: SettingsPanelController records a MediaState _preOpenState at open-time instead of retaining a bool _wasPlaying field."
    - "PAUSE-03: Closing a panel resumes only when its open-time snapshot is MediaState.playing; loading/opening, buffering, completed/EOF, and manually paused scenarios do not issue play()."
    - "PAUSE-04: The controller regression suite proves loading, EOF, manually paused, and playing open-close paths through the existing SettingsPanelPlayback fake."
    - "The SettingsPanelPlayback interface remains unchanged and SettingsPanelController gains no direct MediaEngine dependency."
  artifacts:
    - "lib/ui/dialogs/settings/settings_panel_controller.dart preserves the narrow SettingsPanelPlayback constructor seam while storing MediaState _preOpenState."
    - "test/ui/dialogs/settings_panel_controller_test.dart contains four named open-close race regression tests with pause/play call-count assertions."
  key_links:
    - "SettingsPanelController.open() snapshots pre-open semantics before invoking SettingsPanelPlayback.pause()."
    - "SettingsPanelController.close() reads _preOpenState to decide whether to call the existing SettingsPanelPlayback.play()."
---

<objective>
Make the settings-panel auto-pause policy deterministic: opening always pauses through the existing playback seam, while closing resumes only if the pre-open snapshot proves playback was active.

Purpose: Prevent later settings-panel phases from racing playback during media opening, completion, and manual pause states without expanding the kernel boundary.
Output: An in-place controller policy refactor and focused regression coverage for four state races.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-settings-shell-split-legacy-deletion/28-01-SUMMARY.md
@lib/ui/dialogs/settings/settings_panel_controller.dart
@test/ui/dialogs/settings_panel_controller_test.dart
@lib/kernel/services/playback_controller.dart
@lib/kernel/engine/media_state.dart
</context>

<dependencies>
Phase 28 is complete and provides the split settings-panel edit surface at `lib/ui/dialogs/settings/settings_panel_controller.dart`. This is the only plan in the phase, so all four ordered tasks execute sequentially in Wave 1 and deliberately share the controller and its focused test file.
</dependencies>

<requirements>
- PAUSE-01 — `open()` always invokes the existing pause command.
- PAUSE-02 — replace the boolean with a `MediaState` pre-open snapshot.
- PAUSE-03 — only a `MediaState.playing` snapshot may resume on close.
- PAUSE-04 — prove loading, EOF, manual-pause, and playing open-close scenarios by call counts.
</requirements>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Snapshot pre-open playback semantics as MediaState</name>
  <reversibility rating="reversible">This replaces one private controller field and its local assignment; reverting is a one-file git change with no public contract change.</reversibility>
  <files>lib/ui/dialogs/settings/settings_panel_controller.dart</files>
  <read_first>Read `lib/ui/dialogs/settings/settings_panel_controller.dart`, `lib/kernel/services/playback_controller.dart`, and `lib/kernel/engine/media_state.dart` to retain the existing narrow interface and use the repository’s actual enum names (`opening`, `completed`).</read_first>
  <behavior>
    - The controller has a `MediaState _preOpenState` field with a safe non-playing initial value.
    - On the first open of a closed panel, the controller records `MediaState.playing` when the existing seam reports active playback and a non-playing MediaState otherwise, before its pause side effect.
    - Repeated open while already open remains a no-op and does not replace the original snapshot.
  </behavior>
  <action>Per PAUSE-02, import the existing MediaState enum and replace the private boolean snapshot with `MediaState _preOpenState`. Preserve constructor injection and the SettingsPanelPlayback members exactly as they are: the seam supplies only `isPlaying`, `pause()`, and `play()`, so express the snapshot through that existing signal as playing versus a safe non-playing state rather than adding a state getter or importing MediaEngine. Update the controller documentation and inline ordering comment while changing the field so they describe why the snapshot precedes the pause side effect. Keep `open()` idempotence, tab reset, pending registration, and ValueNotifier ownership unchanged.</action>
  <verify>
    <automated>D:/flutter/bin/flutter analyze lib/ui/dialogs/settings/settings_panel_controller.dart</automated>
  </verify>
  <done>PAUSE-02 is represented by a MediaState `_preOpenState` captured before the pause side effect, with no SettingsPanelPlayback or kernel-interface change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the open path pause through the existing seam every time</name>
  <reversibility rating="reversible">The open-path command ordering is local to one private controller implementation and can be restored through git without caller migration.</reversibility>
  <files>lib/ui/dialogs/settings/settings_panel_controller.dart</files>
  <read_first>Read the completed Task 1 controller state and the existing `open()` documentation before editing so the snapshot remains before the command side effect.</read_first>
  <behavior>
    - Opening from active playback records playing and invokes pause once.
    - Opening from a non-playing state still invokes pause once.
    - A second open while the panel is open does not invoke pause again.
  </behavior>
  <action>Per PAUSE-01, make the first-open path call the existing `_playback.pause()` unconditionally after recording `_preOpenState`; remove the conditional branch that previously made the command dependent on the old boolean. Revise the open-method documentation to state the invariant precisely: snapshot first because pause changes playback, then always request pause to establish the panel-open safety policy. Do not alter the deferred-apply registrations, `isOpen` ordering, ValueNotifier model, or the SettingsPanelPlayback interface.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/dialogs/settings_panel_controller_test.dart</automated>
  </verify>
  <done>PAUSE-01 holds: every transition from closed to open issues exactly one pause command through the unchanged seam, including an already non-playing source state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resume exclusively from a playing pre-open snapshot</name>
  <reversibility rating="reversible">The close-path predicate is a private conditional in one controller method and has no persistence, public API, or migration cost.</reversibility>
  <files>lib/ui/dialogs/settings/settings_panel_controller.dart</files>
  <read_first>Read `close()` and the MediaState enum after Tasks 1–2; confirm the repository uses `opening` and `completed` rather than the requirement prose’s loading and EOF labels.</read_first>
  <behavior>
    - Closing after a playing snapshot calls play once.
    - Closing after any non-playing snapshot does not call play.
    - Closing still resets dragOffset and disposes deferred pending state in every case.
  </behavior>
  <action>Per PAUSE-03, replace the close-path resume predicate with an equality check against `MediaState.playing`. Retain the existing close idempotence, state closure before resume decision, drag-offset reset, and pending-state disposal. Update the method documentation to name the safe-resume rule and explain that non-playing snapshots cover opening/loading, buffering represented outside the primary enum, completed/EOF, and manual pause without any resume command. Keep the deferred-apply seam and all ValueNotifier behavior intact.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/dialogs/settings_panel_controller_test.dart</automated>
  </verify>
  <done>PAUSE-03 holds: close invokes play only for `_preOpenState == MediaState.playing`, while all non-playing snapshots retain their state and cleanup behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add four controller race regression tests using the hand-written playback fake</name>
  <reversibility rating="reversible">The focused test cases and fake-state configuration are isolated to one test file and can be removed without production contract changes.</reversibility>
  <files>test/ui/dialogs/settings_panel_controller_test.dart</files>
  <read_first>Read the final controller and the existing `FakePlaybackController` call-count assertions. Keep the fake implementing only the unchanged SettingsPanelPlayback interface and avoid fvp/MDK test dependencies.</read_first>
  <behavior>
    - Loading/opening → open → close produces one pause command and zero play commands.
    - Completed/EOF → open → close produces one pause command and zero play commands.
    - Manually paused → open → close produces one pause command and zero play commands.
    - Playing → open → close produces one pause command and one play command.
  </behavior>
  <action>Per PAUSE-04, extend the existing focused controller test file with four descriptive AAA regression cases for loading/opening, completed/EOF, manually paused, and playing. Import `MediaState` in the test and evolve only the hand-written fake’s private setup state so `isPlaying` derives from a configured enum value while `pause()` and `play()` continue recording calls; this makes each scenario explicit without adding members to SettingsPanelPlayback or using a real MediaEngine. Each case must construct a fresh controller, call open then close, assert pause and play call counts, and dispose the controller. Update the prior non-playing open expectation so it verifies the new always-pause policy, and preserve the existing idempotence and drag-offset coverage.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/dialogs/settings_panel_controller_test.dart &amp;&amp; D:/flutter/bin/flutter analyze lib/ui/dialogs/settings/settings_panel_controller.dart test/ui/dialogs/settings_panel_controller_test.dart</automated>
  </verify>
  <done>PAUSE-04 is covered by four passing focused regression tests: opening/loading, completed/EOF, and manual-pause paths issue no resume; the playing path resumes once; every path receives the panel-open pause command.</done>
</task>

</tasks>

<reversibility_gates>
TRACER_MODE=true: Task 1 establishes the production controller’s snapshot path before Tasks 2–4 expand the policy and edge coverage. REVERSIBILITY_GATES=true: all four changes are rated reversible because they are private, in-place controller/test edits with no public seam, stored data, external service, or kernel contract migration; no decision checkpoint is required.
</reversibility_gates>

<risks>
- Zero kernel interface expansion: SettingsPanelPlayback remains exactly `isPlaying`, `pause()`, and `play()`; the controller must not depend directly on MediaEngine.
- Semantic projection: the narrow seam exposes active/not-active rather than every engine state, so the MediaState snapshot uses the existing signal solely to distinguish resumable `playing` from non-resumable states. The four call-count tests enforce the required safety behavior without widening the seam.
- ValueNotifier remains unchanged: retain `SettingsPanelState` ownership and current open/close notifier ordering.
- Deferred-apply seam remains preserved: retain `PendingSettingsState` registration and disposal behavior unchanged.
- No security surface (in-process state machine, no auth/input/PII).
</risks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Panel open always pauses; resume is proven only from an active pre-open snapshot | 29-01 | COVERED | Tasks 1–3 implement the policy and Task 4 proves it.
REQ | PAUSE-01 | Unconditional pause on open | 29-01 | COVERED | Task 2.
REQ | PAUSE-02 | MediaState pre-open snapshot replacing boolean | 29-01 | COVERED | Task 1.
REQ | PAUSE-03 | Resume only for `MediaState.playing`; other states do not resume | 29-01 | COVERED | Task 3.
REQ | PAUSE-04 | Four race regression scenarios | 29-01 | COVERED | Task 4.
RESEARCH | — | No RESEARCH.md supplied; research intentionally skipped | 29-01 | COVERED | No research-derived scope exists.
CONTEXT | — | No CONTEXT.md supplied; user locked continuation without context | 29-01 | COVERED | No D-XX decisions exist for this phase.
</source_audit>

<verification>
Run the focused controller suite with `D:/flutter/bin/flutter test test/ui/dialogs/settings_panel_controller_test.dart`, then analyze the changed controller and test files. The resulting tests must establish the command-count matrix for opening/loading, completed/EOF, manual-pause, and playing scenarios while preserving the unchanged narrow playback seam.
</verification>

<success_criteria>
1. PAUSE-01: `open()` invokes pause unconditionally for the closed-to-open transition.
2. PAUSE-02: The boolean snapshot is replaced by `MediaState _preOpenState`, captured before pause.
3. PAUSE-03: `close()` resumes only when `_preOpenState == MediaState.playing`; opening/loading, buffering, completed/EOF, and manual-pause do not resume.
4. PAUSE-04: Four focused regression tests pass for the required sub-races.
5. SettingsPanelPlayback remains unchanged, no direct MediaEngine dependency is added, and deferred-apply plus ValueNotifier behavior remains intact.
</success_criteria>

<output>
Create `.planning/phases/29-auto-pause-always/29-01-SUMMARY.md` when done.
</output>
