---
phase: 21-verify-migration-adapter-convergence
plan: 04
type: execute
wave: 3
depends_on:
  - 21-03
files_modified:
  - tool/audit/phase21_gates.sh
  - tool/audit/phase21_release_gate.sh
  - tool/audit/rollback.sh
  - docs/ROLLBACK.md
  - test/adapter/kernel_adapter_contract_test.dart
  - test/adapter/kernel_adapter_identity_test.dart
autonomous: true
requirements:
  - VERIFY-03
  - VERIFY-04
  - VERIFY-06

must_haves:
  truths:
    - "适配层删除闸门 4 项硬性检查全部可执行（VERIFY-04, D9）"
    - "release 构建冒烟脚本可执行（VERIFY-06, D15）"
    - "回退脚本可执行，翻回 DelegationPolicy.all(KernelMode.legacy)（D16, D19）"
    - "adapter 测试已删除，契约测试保留（D8）"
  artifacts:
    - "tool/audit/phase21_gates.sh — 4 项适配层闸门检查脚本（D12）"
    - "tool/audit/phase21_release_gate.sh — release 构建冒烟脚本（D15）"
    - "tool/audit/rollback.sh — 紧急回退脚本（D19）"
    - "docs/ROLLBACK.md — 回退文档（D19）"
  key_links:
    - "phase21_gates.sh → DelegationPolicy migratedMethods 检查"
    - "phase21_gates.sh → dual_track_regression_test 全绿检查"
    - "phase21_gates.sh → OpenGenerationTracker 在 FvpEngine 检查"
    - "phase21_gates.sh → rollback.sh + ROLLBACK.md 存在检查"
    - "rollback.sh → PlayerServices DelegationPolicy 翻回"
---

<objective>
创建适配层删除闸门脚本、release 冒烟脚本、回退脚本与文档，删除 adapter 测试保留契约测试。

Purpose: 以显式闸门清单守护适配层收拢（VERIFY-04），提供回退路径（D16-D19），清理不再需要的 adapter 测试（D8）。
Output: 4 个脚本/文档文件 + adapter 测试删除。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-verify-migration-adapter-convergence/21-CONTEXT.md
@.planning/phases/21-verify-migration-adapter-convergence/21-RESEARCH.md
@tool/audit/phase16_gates.sh (gate 脚本模式参考)
@lib/kernel/adapter/kernel_adapter.dart
@lib/kernel/player_services.dart
@test/adapter/kernel_adapter_contract_test.dart
@test/adapter/kernel_adapter_identity_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 phase21_gates.sh 适配层闸门脚本（4 项硬性检查）</name>
  <files>tool/audit/phase21_gates.sh</files>
  <read_first>
    - tool/audit/phase16_gates.sh (gate 脚本模式: set -euo pipefail, SCRIPT_DIR/REPO_ROOT, gate 函数)
    - lib/kernel/player_services.dart (DelegationPolicy 构造位置)
    - lib/kernel/adapter/kernel_adapter.dart (DelegationPolicy + KernelMode)
    - lib/kernel/engine/fvp_engine.dart (OpenGenerationTracker 引用)
    - tool/audit/rollback.sh (Task 2 产出，存在性检查目标)
    - docs/ROLLBACK.md (Task 2 产出，存在性检查目标)
  </read_first>
  <action>
创建 `tool/audit/phase21_gates.sh`，沿用 `phase16_gates.sh` 模式（set -euo pipefail, SCRIPT_DIR/REPO_ROOT, gate 函数 + main()）。

4 个闸门（per D9）：

**GATE 1: 100% 调用方已迁移（DelegationPolicy 全部 migratedMethods）**
- grep PlayerServices 中 DelegationPolicy 构造，检查 migratedMethods 是否包含所有 MediaEngine 方法
- 或检查是否使用 `DelegationPolicy.all(KernelMode.migrated)` — 但注意 `all()` 会清空 migratedMethods（Pitfall 3）
- 实际检查：确认 PlayerServices 中 DelegationPolicy 的 7 个 per-capability 字段均为 `KernelMode.migrated`

**GATE 2: 双轨回归全绿**
- 运行 `flutter test test/regression/dual_track_regression_test.dart`
- exit 0 即 PASS

**GATE 3: 守卫已移入新引擎**
- grep `OpenGenerationTracker` 在 `lib/kernel/engine/fvp_engine.dart` 中存在
- 确认 `lib/kernel/adapter/kernel_adapter.dart` 中无 `_openGeneration` 字段（Phase 16 D22 闸门复用）

**GATE 4: 回退路径已审计**
- 检查 `tool/audit/rollback.sh` 存在且可执行
- 检查 `docs/ROLLBACK.md` 存在

脚本输出格式与 phase16_gates.sh 一致：每闸门打印 PASS/FAIL + 证据行，main() 汇总 exit code。
  </action>
  <verify>
    <automated>bash tool/audit/phase21_gates.sh 2>&1 | tail -10</automated>
  </verify>
  <done>phase21_gates.sh 可执行，4 项闸门检查逻辑正确（GATE 2 可能 FAIL 因为双轨回归尚未运行，其余应 PASS）</done>
</task>

<task type="auto">
  <name>Task 2: 创建回退脚本 + 文档 + release 冒烟脚本 + 删除 adapter 测试</name>
  <files>tool/audit/rollback.sh, docs/ROLLBACK.md, tool/audit/phase21_release_gate.sh</files>
  <read_first>
    - lib/kernel/player_services.dart (DelegationPolicy 构造 — 翻回目标)
    - lib/kernel/adapter/kernel_adapter.dart (DelegationPolicy.all 构造函数)
    - tool/audit/phase16_gates.sh (脚本模式参考)
    - test/adapter/kernel_adapter_contract_test.dart (待删除)
    - test/adapter/kernel_adapter_identity_test.dart (待删除)
  </read_first>
  <action>
创建 3 个文件 + 删除 2 个 adapter 测试：

1. **tool/audit/rollback.sh** (per D19):
   - `#!/usr/bin/env bash` + `set -euo pipefail`
   - 功能：自动翻回 DelegationPolicy 到 all-legacy
   - 实现：sed 替换 `player_services.dart` 中 DelegationPolicy 构造为 `const DelegationPolicy.all(KernelMode.legacy)`
   - 同时翻回 DiagnosticsBundle 为 noop（per D18 回退范围 = 引擎+诊断组件）
   - 打印翻回前后 diff，确认变更
   - 提示用户运行 `flutter test` 验证

2. **docs/ROLLBACK.md** (per D19):
   - 标题：紧急回退指南
   - 触发条件（per D17）：无法播放、崩溃、音画不同步等用户可感知的播放故障
   - 回退步骤：
     1. `bash tool/audit/rollback.sh`
     2. `flutter test` 验证全绿
     3. 提交变更
   - 回退范围（per D18）：引擎+诊断组件，不涉及 UI/设置/播放列表
   - 恢复步骤：重新创建 DelegationPolicy 为 per-method migrated

3. **tool/audit/phase21_release_gate.sh** (per D15):
   - `#!/usr/bin/env bash` + `set -euo pipefail`
   - 功能：`flutter build windows --release` 后 grep 产物中的 debugPrint/debug/info 字符串
   - 实现：
     - 运行 `flutter build windows --release`
     - 在 `build/windows/x64/runner/Release/` 目录 grep `debugPrint`、`debug(`、`.info(` 字符串
     - 排除已知合法的二进制文件（.dll, .exe 等二进制格式不 grep）
     - 检查 `.dart_tool/` 下的 AOT snapshot 或 kernel binary
   - 如果发现 debugPrint 泄漏，打印文件名+行号，exit 1

4. **删除 adapter 测试** (per D8):
   - 删除 `test/adapter/kernel_adapter_contract_test.dart`
   - 删除 `test/adapter/kernel_adapter_identity_test.dart`
   - 保留 `test/contracts/` 下所有接口级契约测试（它们验证接口，不验证适配层）
   - git commit: `test(21): delete adapter tests, keep contract tests (D8)`
  </action>
  <verify>
    <automated>bash tool/audit/rollback.sh --dry-run 2>&1 | head -5; echo "---"; test -f docs/ROLLBACK.md && echo "ROLLBACK.md exists"; test -f tool/audit/phase21_release_gate.sh && echo "release_gate.sh exists"; test ! -f test/adapter/kernel_adapter_contract_test.dart && echo "adapter tests deleted"</automated>
  </verify>
  <done>rollback.sh 可执行，ROLLBACK.md 存在，phase21_release_gate.sh 存在，adapter 测试已删除，契约测试保留</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rollback.sh → player_services.dart | 回退脚本修改 DelegationPolicy，需 git 权限 |
| release gate → build artifacts | 只读检查构建产物，无写入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04 | Denial of Service | rollback.sh | medium | mitigate | 脚本需 git 权限，文档说明前置条件；--dry-run 模式可用 |
| T-21-05 | Elevation of Privilege | gate scripts | low | accept | CI 强制执行 gate 脚本，非零退出阻断构建 |
</threat_model>

<verification>
- `bash tool/audit/phase21_gates.sh` 可执行
- `bash tool/audit/rollback.sh --dry-run` 输出预期
- `test -f docs/ROLLBACK.md` 存在
- `bash tool/audit/phase21_release_gate.sh` 可执行
- `test ! -f test/adapter/kernel_adapter_contract_test.dart` 已删除
- `test -f test/contracts/contract_test_runner.dart` 保留
</verification>

<success_criteria>
- 4 个新文件存在且可执行/可读（phase21_gates.sh, phase21_release_gate.sh, rollback.sh, ROLLBACK.md）
- adapter 测试删除，契约测试保留（D8）
- 回退脚本可一行翻回 DelegationPolicy（D16）
</success_criteria>

<artifacts>
本 phase 产出物：
- tool/audit/phase21_gates.sh — 适配层删除闸门脚本
- tool/audit/phase21_release_gate.sh — release 构建冒烟脚本
- tool/audit/rollback.sh — 紧急回退脚本
- docs/ROLLBACK.md — 回退文档
</artifacts>

<output>
Create `.planning/phases/21-verify-migration-adapter-convergence/21-04-SUMMARY.md` when done
</output>
