---
phase: 21-verify-migration-adapter-convergence
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/migration-order.md
autonomous: true
requirements:
  - VERIFY-03

must_haves:
  truths:
    - "迁移顺序由依赖图推导：叶子 → 编排器 → 状态管理器 → UI 绑定（VERIFY-03）"
  artifacts:
    - "docs/migration-order.md — 迁移顺序文档"
  key_links:
    - "codegraph callers/impact → 依赖图 → 迁移顺序"
---

<objective>
使用 codegraph 分析推导 MediaEngine 迁移顺序，产出迁移顺序文档。

Purpose: VERIFY-03 要求迁移顺序由依赖图推导而非主观判断。
Output: docs/migration-order.md 记录叶子→编排器→状态管理器→UI 绑定的迁移顺序。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-verify-migration-adapter-convergence/21-CONTEXT.md
@lib/kernel/engine/media_engine.dart
@lib/kernel/engine/fvp_engine.dart
@lib/kernel/adapter/kernel_adapter.dart
@lib/kernel/services/playback_controller.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: codegraph 分析 MediaEngine 依赖图 + 产出迁移顺序文档</name>
  <files>docs/migration-order.md</files>
  <read_first>
    - lib/kernel/engine/media_engine.dart (MediaEngine 接口定义)
    - lib/kernel/engine/fvp_engine.dart (FvpEngine 实现 — 被迁移对象)
    - lib/kernel/adapter/kernel_adapter.dart (KernelAdapter — 路由层)
    - lib/kernel/services/playback_controller.dart (PlaybackController — 编排器)
    - lib/kernel/player_services.dart (PlayerServices — 组合根)
  </read_first>
  <action>
使用 codegraph MCP 工具分析 MediaEngine 的依赖图，推导迁移顺序：

1. **codegraph explore "MediaEngine"** — 获取 MediaEngine 接口的所有实现者和调用者
2. **codegraph callers "FvpEngine.open"** — 找出 open() 的所有调用者（叶子方法）
3. **codegraph callers "FvpEngine.play"** — 找出 play() 的调用者
4. **codegraph impact "FvpEngine"** — 分析 FvpEngine 变更的影响范围
5. **codegraph callers "PlaybackController"** — 找出编排器的调用者

基于分析结果，推导迁移顺序（per VERIFY-03）：
- **叶子层**（无下游依赖）：VideoEffectControl, RendererControl, VolumeControl, TrackControl, SubtitleConfig
- **编排器层**（依赖叶子）：PlaybackControl (open/play/pause/seek)
- **状态管理层**（依赖编排器）：EngineStateView (state/position/duration notifiers)
- **UI 绑定层**（依赖状态管理）：PlayerScreen, ControlsOverlay, ProgressBar 等 UI 组件

产出 `docs/migration-order.md`：
- 依赖图摘要（ASCII 图或表格）
- 每层的组件列表
- 推荐迁移顺序（叶子优先，UI 最后）
- Phase 20 实际翻转顺序对比（per D11: open→play→pause→seek→volume→mute→...→其他）
- 风险评估（哪些组件变更影响最大）
  </action>
  <verify>
    <automated>test -f docs/migration-order.md && echo "migration-order.md exists" && wc -l docs/migration-order.md</automated>
  </verify>
  <done>docs/migration-order.md 存在，包含依赖图摘要、4 层迁移顺序、Phase 20 实际翻转对比</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codegraph → docs | 只读分析，无安全影响 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | Tampering | npm/pip/cargo installs | low | accept | Phase 21 不安装新外部包 |
</threat_model>

<verification>
- `test -f docs/migration-order.md` 存在
- 文档包含 4 层迁移顺序（叶子→编排器→状态管理→UI 绑定）
</verification>

<success_criteria>
- 迁移顺序由 codegraph 依赖图推导（非主观判断）
- 文档记录叶子→编排器→状态管理器→UI 绑定顺序
- 与 Phase 20 实际翻转顺序对比
</success_criteria>

<artifacts>
本 phase 产出物：
- docs/migration-order.md — 迁移顺序文档
</artifacts>

<output>
Create `.planning/phases/21-verify-migration-adapter-convergence/21-05-SUMMARY.md` when done
</output>
