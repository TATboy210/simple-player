---
phase: 21-verify-migration-adapter-convergence
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - lib/kernel/engine/fvp_engine.dart
  - lib/kernel/engine/engine_state_machine.dart
  - lib/kernel/diagnostics/kernel_logger.dart
  - lib/kernel/scanner/folder_scanner.dart
  - lib/kernel/services/global_hotkey_service.dart
  - lib/kernel/utils/log.dart
  - analysis_options.yaml
autonomous: true
requirements:
  - VERIFY-05
  - VERIFY-06

must_haves:
  truths:
    - "flutter analyze 零 errors（VERIFY-05）"
    - "lib/kernel/ 零 debugPrint 调用（VERIFY-06 前置）"
    - "analysis_options.yaml 含 lib/kernel/** 禁止 debugPrint 的 lint rule（D14）"
  artifacts:
    - "analysis_options.yaml — 新增 kernel/ 目录 debugPrint 禁止规则"
    - "lib/kernel/**/*.dart — 所有 debugPrint 替换为 KernelLogger 或移除"
  key_links:
    - "KernelLoggerImpl.I → 替换 debugPrint 调用点"
    - "analysis_options.yaml lint rule → 防止新增 debugPrint"
---

<objective>
修复 pre-existing analyze errors 并清理 lib/kernel/ 中所有 debugPrint 调用，添加 lint rule 防止新增。

Purpose: VERIFY-05 要求 flutter analyze 严格干净；VERIFY-06 要求 release 构建零 debugPrint 泄漏。
Output: flutter analyze 零 errors，lib/kernel/ 零 debugPrint，lint rule 生效。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-verify-migration-adapter-convergence/21-CONTEXT.md
@.planning/phases/21-verify-migration-adapter-convergence/21-RESEARCH.md
@lib/kernel/diagnostics/kernel_logger.dart
@lib/kernel/engine/fvp_engine.dart
@analysis_options.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 pre-existing analyze errors（KernelLogger.I + ambiguous import）</name>
  <files>lib/kernel/engine/fvp_engine.dart, lib/kernel/engine/engine_state_machine.dart, lib/kernel/diagnostics/kernel_logger.dart</files>
  <read_first>
    - lib/kernel/diagnostics/kernel_logger.dart (KernelLoggerImpl.I 静态访问器)
    - lib/kernel/engine/fvp_engine.dart (KernelLogger.I 引用位置)
    - lib/kernel/engine/engine_state_machine.dart (KernelLogger.I 引用位置)
    - analysis_options.yaml (当前 lint 配置)
  </read_first>
  <action>
修复约 35 个 pre-existing analyze errors：

1. **KernelLogger.I undefined** — 搜索所有 `KernelLogger.I` 引用，替换为 `KernelLoggerImpl.I`。Phase 17 迁移时 `KernelLogger` 是抽象类，`KernelLoggerImpl` 是具体实现，静态访问器 `I` 在 `KernelLoggerImpl` 上。grep 确认 `KernelLogger.I` 零命中。

2. **log ambiguous import** — 搜索同时 import `package:simple_player_flutter/kernel/utils/log.dart` 和 `diagnostics/kernel_logger.dart` 的文件，消除歧义：
   - 如果文件已使用 `KernelLoggerImpl.I`，移除旧 `log.dart` import
   - 如果文件仍使用旧 `log.dart` API，迁移到 `KernelLoggerImpl.I`（与 Phase 17 一致）

3. 运行 `flutter analyze` 确认零 errors。如有其他 errors，逐个修复。
  </action>
  <verify>
    <automated>flutter analyze 2>&1 | grep -c 'error •' || echo "0 errors"</automated>
  </verify>
  <done>flutter analyze 输出零 errors（不含 info/warning）</done>
</task>

<task type="auto">
  <name>Task 2: 清理 lib/kernel/ 中所有 debugPrint 调用 + 添加 lint rule</name>
  <files>lib/kernel/engine/fvp_engine.dart, lib/kernel/scanner/folder_scanner.dart, lib/kernel/services/global_hotkey_service.dart, lib/kernel/utils/log.dart, analysis_options.yaml</files>
  <read_first>
    - lib/kernel/engine/fvp_engine.dart (4 处 debugPrint: 行 309, 397, 418, 709)
    - lib/kernel/scanner/folder_scanner.dart (1 处 debugPrint: 行 76)
    - lib/kernel/services/global_hotkey_service.dart (5 处 debugPrint: 行 27, 68, 72, 84, 90)
    - lib/kernel/utils/log.dart (1 处 debugPrint: 行 211)
    - lib/kernel/diagnostics/kernel_logger.dart (DebugPrintSink 内的 debugPrint 保留 — 这是 sink 实现)
    - analysis_options.yaml (添加 lint rule)
  </read_first>
  <action>
清理 lib/kernel/ 中 11 处 debugPrint 调用（不包括 kernel_logger.dart DebugPrintSink 内的实现）：

1. **fvp_engine.dart** (4 处):
   - 行 309: `debugPrint('🔍 open() result: ...')` → 替换为 `bundle.logger.d('open result: ${result.runtimeType}', context: {'file': PathUtils.basename(trimmed)})`
   - 行 397: `debugPrint(...)` → 替换为 `bundle.logger.e(...)` 或 `bundle.logger.w(...)`
   - 行 418: `debugPrint('❌ play() failed: $e')` → 替换为 `bundle.logger.e('play failed', error: e)`
   - 行 709: `debugPrint('⚠️ FvpEngine.dispose: ...')` → 替换为 `bundle.logger.w('dispose: ${entry.key} still has listeners')`

2. **folder_scanner.dart** (1 处):
   - 行 76: `debugPrint('[FolderScanner] Failed to scan...')` → 替换为 `KernelLoggerImpl.I.e('FolderScanner: failed to scan', error: e)`
   - 添加 import `package:simple_player_flutter/kernel/diagnostics/kernel_logger.dart`

3. **global_hotkey_service.dart** (5 处):
   - 行 27/68/72/84/90: 全部替换为 `KernelLoggerImpl.I.w/e/i(...)` 对应级别
   - 添加 import `package:simple_player_flutter/kernel/diagnostics/kernel_logger.dart`

4. **log.dart** (1 处):
   - 行 211: `debugPrint('[Log] file logging init failed: $e')` → 替换为 `KernelLoggerImpl.I.e('file logging init failed', error: e)`

5. **analysis_options.yaml** — 添加 lib/kernel/** 禁止 debugPrint 的 lint rule（per D14）：
   ```yaml
   analyzer:
     errors:
       # Phase 21 D14: lib/kernel/ 禁止 debugPrint — 内核必须用 KernelLogger
       # 使用 avoid_print rule + exclude 模式实现目录级禁止
   ```
   具体实现方式：在 `linter: rules:` 下添加 `avoid_print: true`（已存在），然后在 `analyzer:` 下为 `lib/kernel/**` 添加自定义 analysis error 映射。如果 Flutter analyzer 不支持目录级 rule override，改用 CI grep 闸门（`tool/audit/phase21_release_gate.sh`）作为兜底。

6. 运行 `flutter analyze` 确认 lint rule 生效 + 零新增 errors。
  </action>
  <verify>
    <automated>grep -rn 'debugPrint(' lib/kernel/ | grep -v '///' | grep -v 'kernel_logger.dart' | grep -v '//' | wc -l</automated>
  </verify>
  <done>lib/kernel/ 零 debugPrint 调用（不包括 kernel_logger.dart DebugPrintSink 实现），lint rule 防新增。注：D13 双重保障的 grep 构建产物检查由 Plan 04 phase21_release_gate.sh 交付</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib/kernel/ → KernelLogger | debugPrint 替换为结构化日志，不引入新攻击面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Tampering | npm/pip/cargo installs | low | accept | Phase 21 不安装新外部包 |
</threat_model>

<verification>
- `flutter analyze` 零 errors
- `grep -rn 'debugPrint(' lib/kernel/ | grep -v kernel_logger.dart | grep -v '//' | wc -l` 输出 0
- `flutter test test/engine/fvp_engine_contract_test.dart` exit 0（确认修改未破坏契约）
</verification>

<success_criteria>
- flutter analyze 严格干净（零 errors）
- lib/kernel/ 零 debugPrint 调用（VERIFY-06 前置条件）
- lint rule 或 CI grep 闸门防止新增 debugPrint（D14）
</success_criteria>

<artifacts>
本 phase 产出物：
- analysis_options.yaml — 更新 lint 配置
- lib/kernel/**/*.dart — debugPrint 清理后的代码
</artifacts>

<output>
Create `.planning/phases/21-verify-migration-adapter-convergence/21-03-SUMMARY.md` when done
</output>
