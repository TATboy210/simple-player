---
phase: 21-verify-migration-adapter-convergence
plan: 06
type: execute
wave: 3
depends_on:
  - 21-03
  - 21-04
files_modified: []
autonomous: true
requirements:
  - VERIFY-05
  - VERIFY-06

must_haves:
  truths:
    - "release 构建冒烟测试产出零 debugPrint/debug/info 行（VERIFY-06）"
    - "kernel/ 覆盖率 ≥ 80%（VERIFY-05）"
    - "契约测试对当前 FvpEngine 通过（VERIFY-01 最终确认）"
    - "双轨回归全绿（VERIFY-02 最终确认）"
  artifacts:
    - "release 构建产物 — 零 debugPrint 泄漏"
    - "coverage 报告 — kernel/ ≥ 80%"
  key_links:
    - "phase21_release_gate.sh → flutter build windows --release → grep 零 debugPrint"
    - "flutter test --coverage → kernel/ 覆盖率 ≥ 80%"
---

<objective>
运行最终验证：release 冒烟、契约测试、双轨回归、覆盖率检查。

Purpose: Phase 21 的最终验收门禁，确认所有 VERIFY 需求满足。
Output: 全部验证通过，Phase 21 可标记完成。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-verify-migration-adapter-convergence/21-CONTEXT.md
@.planning/phases/21-verify-migration-adapter-convergence/21-RESEARCH.md
@tool/audit/phase21_release_gate.sh (Plan 04 产出)
@tool/audit/phase21_gates.sh (Plan 04 产出)
@test/engine/fvp_engine_contract_test.dart
@test/regression/dual_track_regression_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行 release 冒烟闸门 + 全套验证</name>
  <files></files>
  <read_first>
    - tool/audit/phase21_release_gate.sh (release 冒烟脚本)
    - tool/audit/phase21_gates.sh (适配层闸门脚本)
    - test/engine/fvp_engine_contract_test.dart (契约测试)
    - test/regression/dual_track_regression_test.dart (双轨回归)
  </read_first>
  <action>
运行 Phase 21 全套验证：

1. **release 冒烟闸门** (VERIFY-06):
   - `bash tool/audit/phase21_release_gate.sh`
   - 预期：flutter build windows --release 成功 + grep 零 debugPrint 泄漏
   - 如果失败：检查 lib/kernel/ 是否还有遗漏的 debugPrint（Plan 03 应已清理）

2. **契约测试** (VERIFY-01 最终确认):
   - `flutter test test/engine/fvp_engine_contract_test.dart`
   - 预期：7 组 run*ContractTests 全部 PASS

3. **双轨回归** (VERIFY-02 最终确认):
   - `flutter test test/regression/dual_track_regression_test.dart`
   - 预期：all-legacy 和 all-migrated 两组全部 PASS，DiffReport 零差异

4. **覆盖率检查** (VERIFY-05):
   - `flutter test --coverage`
   - 检查 `coverage/lcov.info` 中 `lib/kernel/` 的行覆盖率 ≥ 80%
   - 计算方式：`lcov --summary coverage/lcov.info --filter 'lib/kernel/'` 或手动解析

5. **适配层闸门** (VERIFY-04):
   - `bash tool/audit/phase21_gates.sh`
   - 预期：4 项闸门全部 PASS

6. **flutter analyze** (VERIFY-05):
   - `flutter analyze`
   - 预期：零 errors

如果任何验证失败，记录失败原因并报告（不在本 plan 中修复 — 修复在对应 plan 中）。
  </action>
  <verify>
    <automated>flutter test test/engine/fvp_engine_contract_test.dart && flutter test test/regression/dual_track_regression_test.dart && flutter analyze 2>&1 | grep -c 'error •' || echo "0 errors"</automated>
  </verify>
  <done>
    - release 冒烟零 debugPrint 泄漏
    - 契约测试 7 组全 PASS
    - 双轨回归零差异
    - flutter analyze 零 errors
    - kernel/ 覆盖率 ≥ 80%
    - 适配层闸门 4 项全 PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release build → binary artifacts | 只读检查，无安全影响 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-07 | Tampering | npm/pip/cargo installs | low | accept | Phase 21 不安装新外部包 |
</threat_model>

<verification>
- `bash tool/audit/phase21_release_gate.sh` exit 0
- `flutter test test/engine/fvp_engine_contract_test.dart` exit 0
- `flutter test test/regression/dual_track_regression_test.dart` exit 0
- `flutter analyze` 零 errors
- `flutter test --coverage` + kernel/ 覆盖率 ≥ 80%
- `bash tool/audit/phase21_gates.sh` exit 0
</verification>

<success_criteria>
- Phase 21 全部 6 项 VERIFY 需求满足
- release 构建零 debugPrint 泄漏（VERIFY-06）
- kernel/ 覆盖率 ≥ 80%（VERIFY-05）
- 适配层闸门 4 项全 PASS（VERIFY-04）
</success_criteria>

<artifacts>
本 phase 产出物：
- 验证报告（console output）— 全部 VERIFY 需求通过
</artifacts>

<output>
Create `.planning/phases/21-verify-migration-adapter-convergence/21-06-SUMMARY.md` when done
</output>
