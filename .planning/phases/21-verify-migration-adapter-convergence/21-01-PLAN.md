---
phase: 21-verify-migration-adapter-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/engine/fvp_engine_contract_test.dart
autonomous: true
requirements:
  - VERIFY-01

must_haves:
  truths:
    - "Phase 15 契约测试全部 7 组对当前 FvpEngine 通过（VERIFY-01）"
    - "契约测试挂载点使用真实 FvpEngine 工厂，非 FakeEngine"
  artifacts:
    - "test/engine/fvp_engine_contract_test.dart — 挂载 7 组 run*ContractTests"
  key_links:
    - "contract_test_runner.dart → 7 组参数化契约测试 → FvpEngine 工厂"
---

<objective>
验证 Phase 15 契约测试对当前 FvpEngine（Phase 20 重构后）全部通过。

Purpose: 确认 Phase 20 的引擎重构没有破坏冻结的接口契约（BASE-04）。
Output: 7 组契约测试全部 green，作为后续双轨回归的基线。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-verify-migration-adapter-convergence/21-CONTEXT.md
@.planning/phases/21-verify-migration-adapter-convergence/21-RESEARCH.md
@test/contracts/contract_test_runner.dart
@test/engine/fvp_engine_contract_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行契约测试挂载点验证 FvpEngine 契约</name>
  <files>test/engine/fvp_engine_contract_test.dart</files>
  <read_first>
    - test/engine/fvp_engine_contract_test.dart
    - test/contracts/contract_test_runner.dart
    - test/contracts/playback_control_contract.dart
    - lib/kernel/engine/fvp_engine.dart (前 50 行确认构造函数签名)
  </read_first>
  <action>
运行现有契约测试挂载点 `test/engine/fvp_engine_contract_test.dart`，验证 Phase 20 重构后的 FvpEngine 仍然通过全部 7 组接口契约测试。

当前挂载点已经调用 `run*ContractTests(() => FvpEngine())`（7 组），无需修改代码。如果测试失败：
1. 检查失败的契约组（EngineStateView / PlaybackControl / TrackControl / SubtitleConfig / VideoEffectControl / RendererControl / VolumeControl）
2. 分析失败原因 — 如果是 Phase 20 引入的回归（如 TransitionResult 替换 assert、OpenGenerationTracker 统一等），修复 FvpEngine 而非修改契约测试
3. 如果是 headless 环境 texture mock 问题（MissingPluginException），确认 `_installFvpTextureChannelMock()` 已正确注册

验证通过标准：全部 7 组 `run*ContractTests` 调用 exit 0。
  </action>
  <verify>
    <automated>flutter test test/engine/fvp_engine_contract_test.dart</automated>
  </verify>
  <done>7 组契约测试（EngineStateView/PlaybackControl/TrackControl/SubtitleConfig/VideoEffectControl/RendererControl/VolumeControl）全部通过，exit 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → FvpEngine | 契约测试验证引擎行为，不涉及用户输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | npm/pip/cargo installs | low | accept | Phase 21 不安装新外部包 |
</threat_model>

<verification>
- `flutter test test/engine/fvp_engine_contract_test.dart` exit 0
- 7 组 run*ContractTests 全部 PASS
</verification>

<success_criteria>
- Phase 15 契约测试对 Phase 20 重构后的 FvpEngine 全部通过
- 无代码修改需要（测试即验证）
</success_criteria>

<artifacts>
本 phase 产出物：
- test/engine/fvp_engine_contract_test.dart — 契约测试挂载点（已存在，验证通过）
</artifacts>

<output>
Create `.planning/phases/21-verify-migration-adapter-convergence/21-01-SUMMARY.md` when done
</output>
