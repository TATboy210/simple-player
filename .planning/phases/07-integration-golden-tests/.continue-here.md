---
context: phase
phase: 07-window-stability
task: 2
total_tasks: 5
status: in_progress
last_updated: 2026-05-29T23:50:00+08:00
---

# Continue Here

**Paused:** 2026-05-29 23:50 GMT+8
**Reason:** Fix 1-2 已编码，Fix 3-5 待实现，上下文即将耗尽

## Current State

Phase 7 窗口稳定性修复。本次 session 完成 Fix 1 (DWM 阴影) 和 Fix 2 (保留 WS_THICKFRAME)。

- **阶段:** Phase 7 — Window Stability Fixes
- **状态:** 2/5 修复已完成，3/5 待实现
- **分支:** `fix/window-startup`
- **未提交:** 1 个文件

## 本次 Session 完成

| # | 修复 | 文件 | 状态 |
|---|------|------|------|
| 1 | DWM 阴影恢复 (margins 0,0,1,0) | `window_service.dart` | ✅ 已完成 |
| 2 | 保留 WS_THICKFRAME 原生缩放 | `window_service.dart` | ✅ 已完成 |
| 3 | 消除启动闪烁 (main.dart 同步 FFI) | `main.dart` | ❌ 待实现 |
| 4 | 全屏退出恢复 _baseStyle | `window_service.dart` | ❌ 字段已加，待接入 |
| 5 | 窗口尺寸持久化 (去抖保存) | `settings_store.dart` | ❌ 待实现 |

## 已编码改动

```dart
// window_service.dart — _removeBorder() 改为：
// 只移除 WS_CAPTION，保留 WS_THICKFRAME
final newStyle = style & ~_wsCaption;

// DWM 阴影保留：顶部 1px frame
final margins = calloc<_Margins>()
  ..ref.left = 0
  ..ref.right = 0
  ..ref.top = 1
  ..ref.bottom = 0;

// 新增字段：
int? _baseStyle;  // _removeBorder() 完成后设置
```

## Remaining Work

### Fix 3: 消除启动闪烁
- 将 `_removeBorder()` 提取为静态方法 `removeBorderImmediate()`
- 在 `main.dart` 的 `waitUntilReadyToShow` 回调中调用，先于 `windowManager.show()`

### Fix 4: 全屏退出恢复 _baseStyle
- `_enterFullscreen()` 中用 `_baseStyle` 替代 `_getWindowLongPtr(hwnd, _gwlStyle)` 保存
- `_baseStyle` 已声明并赋值，只需修改 `_enterFullscreen` 读取逻辑

### Fix 5: 窗口尺寸持久化
- `onWindowResize` 中添加 500ms debounce
- 保存到 `SettingsStore` (SharedPreferences)

## 未提交文件

```
lib/kernel/bridge/window_service.dart
```

## Resume Command

```
/gsd:resume-work
```

## Infrastructure

- Flutter: D:\flutter\bin\flutter
- Branch: fix/window-startup
- Tests: 564 passing
- Coverage: 80.0%
