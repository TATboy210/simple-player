---
phase: 01-fullscreen-simplification
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/kernel/bridge/win32/win32_fullscreen_ffi.dart
  - test/platform/windows_fullscreen_driver_test.dart
  - test/platform/linux_fullscreen_driver_test.dart
  - test/platform/macos_fullscreen_driver_test.dart
  - test/regression/smoke_suite_test.dart
  - test/regression/high_risk_suite_test.dart
  - test/unit/kernel/bridge/window_service_test.dart
autonomous: true
requirements:
  - ARCH-REM-03
  - ARCH-REM-04

must_haves:
  truths:
    - "win32_fullscreen_ffi.dart no longer exists in the codebase"
    - "test/platform/ directory contains no driver test files"
    - "Regression tests compile without FullscreenDriver references"
    - "window_service_test.dart compiles without FullscreenDriver references"
  artifacts:
    - test/regression/smoke_suite_test.dart (updated, FullscreenDriver refs removed)
    - test/regression/high_risk_suite_test.dart (updated, FullscreenDriver refs removed)
    - test/unit/kernel/bridge/window_service_test.dart (updated, FullscreenDriver refs removed)
  key_links:
    - "win32/ directory retains win32_display_enumerator.dart"
    - "Regression tests remain compilable (full rewrite deferred to Phase 4)"
---

<objective>
Delete the Win32 FFI fullscreen bindings and all old fullscreen driver test files. Update regression and unit tests that reference the deleted FullscreenDriver to remove those references, keeping the tests compilable even if some test logic is temporarily incomplete.

Purpose: Completes the old architecture removal. After this plan, zero files in the codebase reference the deleted FullscreenDriver abstraction. Test updates here are minimal (import removal, mock class removal) — full test rewrites for the new architecture happen in Phase 4.

Output: 1 FFI file deleted, 3 driver test files deleted, 3 test files updated to remove deleted type references.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fullscreen-simplification/01-RESEARCH.md
@.planning/phases/01-fullscreen-simplification/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Win32 FFI file and driver test files</name>
  <files>lib/kernel/bridge/win32/win32_fullscreen_ffi.dart, test/platform/windows_fullscreen_driver_test.dart, test/platform/linux_fullscreen_driver_test.dart, test/platform/macos_fullscreen_driver_test.dart</files>
  <read_first>lib/kernel/bridge/win32/win32_fullscreen_ffi.dart, test/platform/windows_fullscreen_driver_test.dart, test/platform/linux_fullscreen_driver_test.dart, test/platform/macos_fullscreen_driver_test.dart</read_first>
  <action>Delete these 4 files. Critical: do NOT delete the win32/ directory itself — it still contains win32_display_enumerator.dart which is unrelated to fullscreen.

Files to delete:
- lib/kernel/bridge/win32/win32_fullscreen_ffi.dart (Win32 FFI bindings for fullscreen)
- test/platform/windows_fullscreen_driver_test.dart
- test/platform/linux_fullscreen_driver_test.dart
- test/platform/macos_fullscreen_driver_test.dart

After deletion, verify win32/ directory still contains win32_display_enumerator.dart. Verify test/platform/ directory is empty or removed.</action>
  <verify>
    <automated>test -f "D:/simple_player_flutter/lib/kernel/bridge/win32/win32_fullscreen_ffi.dart" && echo "FAIL" || echo "OK: ffi deleted"; test -f "D:/simple_player_flutter/lib/kernel/bridge/win32/win32_display_enumerator.dart" && echo "OK: display_enum kept" || echo "FAIL: display_enum lost"; test -f "D:/simple_player_flutter/test/platform/windows_fullscreen_driver_test.dart" && echo "FAIL" || echo "OK: win test deleted"; test -f "D:/simple_player_flutter/test/platform/linux_fullscreen_driver_test.dart" && echo "FAIL" || echo "OK: linux test deleted"; test -f "D:/simple_player_flutter/test/platform/macos_fullscreen_driver_test.dart" && echo "FAIL" || echo "OK: macos test deleted"</automated>
  </verify>
  <acceptance_criteria>
    - win32_fullscreen_ffi.dart does not exist
    - win32_display_enumerator.dart still exists in win32/
    - All 3 platform driver test files deleted
    - test/platform/ directory is empty or removed
  </acceptance_criteria>
  <done>Win32 FFI file and all 3 driver test files deleted. win32/ directory retains display_enumerator.dart.</done>
</task>

<task type="auto">
  <name>Task 2: Update regression and unit tests to remove FullscreenDriver references</name>
  <files>test/regression/smoke_suite_test.dart, test/regression/high_risk_suite_test.dart, test/unit/kernel/bridge/window_service_test.dart</files>
  <read_first>test/regression/smoke_suite_test.dart, test/regression/high_risk_suite_test.dart, test/unit/kernel/bridge/window_service_test.dart</read_first>
  <action>Update these 3 test files to remove all references to the deleted FullscreenDriver class and FullscreenCapability. The goal is compilability — full test rewrites happen in Phase 4.

For test/regression/smoke_suite_test.dart:
1. REMOVE import: `import 'package:simple_player_flutter/kernel/bridge/fullscreen_driver.dart';`
2. REMOVE import: `import 'package:simple_player_flutter/kernel/models/fullscreen_capability.dart';`
3. DELETE the entire `_MockFullscreenDriver extends FullscreenDriver` class
4. In tests that create `_MockFullscreenDriver()` instances — comment out or remove those test bodies with a TODO: `// TODO: Phase 4 — rewrite without FullscreenDriver mock`
5. Keep test structure intact (test groups, other non-fullscreen tests)

For test/regression/high_risk_suite_test.dart:
1. REMOVE import: `import 'package:simple_player_flutter/kernel/bridge/fullscreen_driver.dart';`
2. REMOVE import: `import 'package:simple_player_flutter/kernel/models/fullscreen_capability.dart';`
3. DELETE the entire `_MockFullscreenDriver extends FullscreenDriver` class
4. In tests that create `_MockFullscreenDriver()` instances — comment out or remove those test bodies with a TODO: `// TODO: Phase 4 — rewrite without FullscreenDriver mock`
5. Keep test structure intact

For test/unit/kernel/bridge/window_service_test.dart:
1. REMOVE import: `import 'package:simple_player_flutter/kernel/bridge/fullscreen_driver.dart';`
2. DELETE the entire `_FakeFullscreenDriver extends FullscreenDriver` class
3. In tests that create `_FakeFullscreenDriver()` — update to use the new WindowService constructor (no driver parameter). If tests depend on fullscreen behavior that no longer exists, comment out with TODO: `// TODO: Phase 4 — rewrite fullscreen tests`
4. Keep non-fullscreen WindowService tests intact

After all changes, run `flutter analyze test/` to verify no references to deleted types remain. The tests may not all pass (Phase 4 rewrites them), but they must compile.</action>
  <verify>
    <automated>cd "D:/simple_player_flutter" && flutter analyze test/regression/smoke_suite_test.dart test/regression/high_risk_suite_test.dart test/unit/kernel/bridge/window_service_test.dart 2>&1 | grep -i "fullscreen_driver\|FullscreenDriver\|FullscreenCapability\|FullscreenResult" | grep -c "error" || echo "0 fullscreen-related errors"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test files have zero imports of deleted files
    - No class extends or references FullscreenDriver
    - No references to FullscreenCapability remain
    - All 3 files pass flutter analyze (compilable)
    - Non-fullscreen tests remain functional
    - TODO comments mark areas needing Phase 4 rewrites
  </acceptance_criteria>
  <done>All test files compile without references to deleted types. Fullscreen-related test logic marked with Phase 4 TODOs. Non-fullscreen tests unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase only deletes code and updates test files — no runtime trust boundaries affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Regression tests | low | accept | Tests temporarily incomplete — Phase 4 rewrites them |
| T-02-02 | Denial of Service | flutter test suite | low | accept | Some tests may fail until Phase 4 — known and expected |
</threat_model>

<verification>
1. `test -f lib/kernel/bridge/win32/win32_fullscreen_ffi.dart` returns false
2. `test -f lib/kernel/bridge/win32/win32_display_enumerator.dart` returns true
3. `ls test/platform/` returns empty or "no such directory"
4. `grep -r "FullscreenDriver" test/` returns zero matches
5. `grep -r "FullscreenCapability" test/` returns zero matches
6. `flutter analyze test/regression/ test/unit/kernel/bridge/window_service_test.dart` — no errors from deleted types
</verification>

<success_criteria>
- win32_fullscreen_ffi.dart deleted, win32_display_enumerator.dart preserved
- 3 platform driver test files deleted
- 3 test files updated: no references to FullscreenDriver, FullscreenCapability, or FullscreenResult
- All updated test files compile (flutter analyze passes)
- Zero references to deleted types across entire lib/ and test/ directories
</success_criteria>

<output>
Create `.planning/phases/01-fullscreen-simplification/01-02-SUMMARY.md` when done
</output>
