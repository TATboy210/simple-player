---
phase: 01-fullscreen-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/bridge/fullscreen_driver.dart
  - lib/kernel/models/fullscreen_capability.dart
  - lib/kernel/bridge/platform/windows_fullscreen_driver.dart
  - lib/kernel/bridge/platform/linux_fullscreen_driver.dart
  - lib/kernel/bridge/platform/macos_fullscreen_driver.dart
  - lib/kernel/bridge/window_service.dart
autonomous: true
requirements:
  - ARCH-REM-01
  - ARCH-REM-02

must_haves:
  truths:
    - "fullscreen_driver.dart no longer exists in the codebase"
    - "fullscreen_capability.dart no longer exists in the codebase"
    - "platform/ directory contains no driver files"
    - "window_service.dart compiles without referencing FullscreenDriver"
  artifacts:
    - lib/kernel/bridge/window_service.dart (updated, no driver imports)
  key_links:
    - "WindowService constructor no longer accepts FullscreenDriver parameter"
    - "setMode method no longer calls _handleEnter/_handleLeave"
---

<objective>
Delete the FullscreenDriver abstract interface, FullscreenCapability model, and all 3 platform-specific driver implementations (Windows/Linux/macOS). Update WindowService to remove all references to the deleted types so it compiles cleanly.

Purpose: This is the first half of removing the old multi-layer fullscreen architecture. After this plan, only WindowService remains — the driver abstraction layer and all platform drivers are gone. Phase 2 will wire WindowService directly to the fullscreen_window package.

Output: 5 source files deleted, window_service.dart updated with driver references removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fullscreen-simplification/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete driver source files</name>
  <files>lib/kernel/bridge/fullscreen_driver.dart, lib/kernel/models/fullscreen_capability.dart, lib/kernel/bridge/platform/windows_fullscreen_driver.dart, lib/kernel/bridge/platform/linux_fullscreen_driver.dart, lib/kernel/bridge/platform/macos_fullscreen_driver.dart</files>
  <read_first>lib/kernel/bridge/fullscreen_driver.dart, lib/kernel/models/fullscreen_capability.dart, lib/kernel/bridge/platform/windows_fullscreen_driver.dart, lib/kernel/bridge/platform/linux_fullscreen_driver.dart, lib/kernel/bridge/platform/macos_fullscreen_driver.dart</read_first>
  <action>Delete these 5 files. Do NOT delete the platform/ directory itself — only the 3 driver files inside it. Do NOT delete win32/ directory or any files inside it. Order: delete platform drivers first (they import fullscreen_driver.dart), then fullscreen_driver.dart, then fullscreen_capability.dart.

Files to delete:
- lib/kernel/bridge/fullscreen_driver.dart (abstract class + FullscreenResult sealed class)
- lib/kernel/models/fullscreen_capability.dart (FullscreenCapability model)
- lib/kernel/bridge/platform/windows_fullscreen_driver.dart
- lib/kernel/bridge/platform/linux_fullscreen_driver.dart
- lib/kernel/bridge/platform/macos_fullscreen_driver.dart

Verify each file exists before deleting. After deletion, confirm the platform/ directory is either empty or gone.</action>
  <verify>
    <automated>test -f "D:/simple_player_flutter/lib/kernel/bridge/fullscreen_driver.dart" && echo "FAIL: still exists" || echo "OK: deleted"; test -f "D:/simple_player_flutter/lib/kernel/models/fullscreen_capability.dart" && echo "FAIL: still exists" || echo "OK: deleted"; test -f "D:/simple_player_flutter/lib/kernel/bridge/platform/windows_fullscreen_driver.dart" && echo "FAIL: still exists" || echo "OK: deleted"; test -f "D:/simple_player_flutter/lib/kernel/bridge/platform/linux_fullscreen_driver.dart" && echo "FAIL: still exists" || echo "OK: deleted"; test -f "D:/simple_player_flutter/lib/kernel/bridge/platform/macos_fullscreen_driver.dart" && echo "FAIL: still exists" || echo "OK: deleted"</automated>
  </verify>
  <acceptance_criteria>
    - None of the 5 files exist after this task
    - platform/ directory is empty or removed
    - lib/kernel/bridge/win32/ directory and its contents are untouched
  </acceptance_criteria>
  <done>All 5 driver-related source files deleted. platform/ directory empty or removed. win32/ directory intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update WindowService to remove driver dependencies</name>
  <files>lib/kernel/bridge/window_service.dart</files>
  <read_first>lib/kernel/bridge/window_service.dart</read_first>
  <action>Update window_service.dart to remove all references to the deleted FullscreenDriver types. Make these specific changes:

1. REMOVE these 4 import lines:
   - import 'fullscreen_driver.dart';
   - import 'platform/linux_fullscreen_driver.dart';
   - import 'platform/macos_fullscreen_driver.dart';
   - import 'platform/windows_fullscreen_driver.dart';

2. UPDATE the constructor — remove the FullscreenDriver parameter and related initialization:
   - Remove `FullscreenDriver? driver,` parameter
   - Remove `_fullscreenDriver = driver ?? _createDriver()` from initializer list
   - Remove the body block that sets `_fullscreenDriver?.onNativeStateChanged = _onNativeFullScreenChanged`
   - New constructor: `WindowService({DisplayEnumerator? displayEnumerator}) : _displayEnumerator = displayEnumerator ?? Win32DisplayAdapter();`

3. DELETE the entire static `_createDriver()` method (lines ~44-67 approximately)

4. DELETE these fields:
   - `final FullscreenDriver? _fullscreenDriver;`
   - `final Map<int, _RestoreSnapshot> _restoreSnapshots = {};`
   - `Completer<bool>? _confirmationCompleter;`

5. DELETE these methods entirely:
   - `_onNativeFullScreenChanged(int windowId, bool isFullscreen)`
   - `_handleEnter(int windowId)` (returns Future<FullscreenResult>)
   - `_handleLeave(int windowId)` (returns Future<FullscreenResult>)
   - `_applyDesync()`
   - `_captureRestoreSnapshot(int windowId)`
   - `_restoreFromSnapshot(int windowId)`
   - `_waitForConfirmation(bool expectedFullscreen)`

6. UPDATE the `setMode` method — remove fullscreen case branches:
   - In `case WindowMode.windowed:` — remove the entire `if (_state.mode.value == WindowMode.fullscreen && _fullscreenDriver != null)` block and the `FullscreenResult` check. Keep the `else if` for maximized.
   - In `case WindowMode.fullscreen:` — remove the entire `if (_fullscreenDriver != null)` block. Leave the case empty or with a TODO comment: `// TODO: Phase 2 — wire to fullscreen_window package`

7. UPDATE the `dispose` method — remove lines referencing:
   - `_fullscreenDriver?.onNativeStateChanged = null;`
   - `_fullscreenDriver?.dispose();`
   - `_restoreSnapshots.clear();`
   - The `_confirmationCompleter` null-check and complete block

8. DELETE the `_RestoreSnapshot` class at the end of the file (lines ~437-450 approximately)

9. UPDATE doc comments — remove references to FullscreenDriver, _createDriver, platform drivers from the class-level and constructor doc comments.

After all changes, run `flutter analyze lib/kernel/bridge/window_service.dart` to verify no errors related to deleted types remain. There may be other unrelated warnings — those are acceptable.</action>
  <verify>
    <automated>cd "D:/simple_player_flutter" && flutter analyze lib/kernel/bridge/window_service.dart 2>&1 | grep -i "fullscreen_driver\|fullscreen_capability\|FullscreenDriver\|FullscreenCapability\|FullscreenResult\|_RestoreSnapshot" | grep -c "error" || echo "0 fullscreen-related errors"</automated>
  </verify>
  <acceptance_criteria>
    - window_service.dart has zero imports of deleted files
    - No references to FullscreenDriver, FullscreenCapability, FullscreenResult, or _RestoreSnapshot remain in the file
    - Constructor accepts only DisplayEnumerator? parameter
    - setMode fullscreen case is a stub/TODO (no driver calls)
    - dispose method has no driver cleanup code
    - flutter analyze shows no errors caused by this task
  </acceptance_criteria>
  <done>window_service.dart compiles without any references to deleted driver types. Fullscreen cases in setMode are stubbed for Phase 2 rewiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase only deletes code — no runtime trust boundaries are affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | WindowService setMode | low | accept | Phase 2 rewires fullscreen — stubbed cases are intentional |
| T-01-02 | Information Disclosure | Deleted test mocks | low | accept | Test mocks contained no real secrets — deletion is safe |
</threat_model>

<verification>
1. `flutter analyze lib/kernel/bridge/window_service.dart` — no errors from deleted types
2. `ls lib/kernel/bridge/fullscreen_driver.dart` — file does not exist
3. `ls lib/kernel/models/fullscreen_capability.dart` — file does not exist
4. `ls lib/kernel/bridge/platform/` — directory empty or does not exist
5. `grep -r "FullscreenDriver" lib/` — zero matches in lib/
</verification>

<success_criteria>
- 5 source files deleted (fullscreen_driver, fullscreen_capability, 3 platform drivers)
- window_service.dart updated: no imports, fields, or methods referencing deleted types
- No compilation errors in lib/ caused by this plan
</success_criteria>

<output>
Create `.planning/phases/01-fullscreen-simplification/01-01-SUMMARY.md` when done
</output>
