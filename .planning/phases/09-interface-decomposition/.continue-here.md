# Continue Here — Phase 9: 接口分解 + 状态模型统一

**Paused:** 2026-07-14
**Status:** Plan 02 执行中 (Task 2/7 完成)
**Next:** Task 3 — 重写 FvpEngine (`with` → `implements 6 接口`)

## 本次会话完成的工作

### ✅ Task 1: FvpCallbackHandler 更新
**文件:** `lib/kernel/engine/fvp_callback_handler.dart`
- MediaState.loading → opening
- MediaState.buffering 引用移除 — 只设置 isBuffering flag
- MediaState.seeking/buffering 在 completed 检查中移除
- MediaState.stopped → idle (mapMdkState)
- 新增 import player_error.dart

### ✅ Task 2: MediaOpener 更新
**文件:** `lib/kernel/engine/media_opener.dart`
- import media_error_type.dart → player_error.dart
- 所有 MediaErrorType 引用替换为 PlayerError 子类型:
  - file → FileError(FileErrorCode.pathEmpty/fileNotFound)
  - codec → CodecError(CodecErrorCode.decodeFailed)
  - network → NetworkError(NetworkErrorCode.timeout)
  - texture → PlaybackError(PlaybackErrorCode.textureFailed)

## 剩余工作 (Task 3-7)

### Task 3: 重写 FvpEngine — 实现 6 接口 (待执行)
**文件:** `lib/kernel/engine/fvp_engine.dart` (641行)
**核心变更:**
- with EngineState, TrackControl, VideoEffects, RendererConfig → implements EngineStateView, PlaybackControl, TrackControl, SubtitleConfig, VideoEffectControl, RendererControl
- errorMessage → lastError (ValueNotifier<PlayerError?>)
- 删除 errorType getter + _errorType 字段
- 添加 isSeeking (ValueNotifier<bool>)
- MediaState.loading → opening, stopped → idle, seeking → isSeeking
- 所有 _errorType = MediaErrorType.xxx → lastError.value = PlayerError子类型
- 更新 dispose() ValueNotifier 列表

### Task 4: 迁移服务文件到 kernel/ + 删除 PlaybackContract
**迁移:** features/player/services/ (7个文件) → kernel/services/
**DI:** features/player/player_services.dart → kernel/player_services.dart
**删除:** playback_contract.dart

### Task 5: 更新 UI 消费者 (25+ 文件)
- EngineState → EngineStateView
- ErrorBanner 重写: MediaErrorType → PlayerError sealed class
- Import 路径: features/player/services/ → kernel/services/

### Task 6: 更新测试文件
### Task 7: flutter analyze + flutter test 验证

## 恢复指令
1. 读取此文件恢复上下文
2. 继续 Task 3: 重写 FvpEngine
3. 按 Task 4→5→6→7 顺序执行

## 相关文件
- Plan: .planning/phases/09-interface-decomposition/09-02-PLAN.md
- Context: .planning/phases/09-interface-decomposition/09-CONTEXT.md
- State: .planning/STATE.md

---
*Paused at 2026-07-14 — Plan 02 Task 2/7 complete*
