---
context: phase
phase: 09-interface-decomposition
task: 1
total_tasks: 6
status: paused
last_updated: 2026-07-14
---

# BLOCKING CONSTRAINTS

- [ ] `media_error_type.dart` 已删除 — FvpEngine/MediaOpener 中 `MediaErrorType` 必须替换为 `PlayerError` sealed class
- [ ] barrel export 不再导出 `EngineState` mixin — 32个文件中 `EngineState` 必须改为 `EngineStateView`
- [ ] `MediaState` 枚举值已变 — `loading`→`opening`, `stopped` 移除, `seeking`/`buffering` 移至独立 ValueNotifier

<current_state>
Phase 9 Plan 02 执行中。Task1 完成（canTransitionTo 扩展 + barrel export 更新，未提交）。代码库当前编译失败 — 旧消费者引用已删除类型。需继续 Task 2-6。
</current_state>

<completed_work>
- Task 1: canTransitionTo extension + barrel export — Done (uncommitted)
- Plan 01 (previous session): 6 ISP interfaces + PlayerError sealed class + 6-value MediaState + FakeEngine + barrel export
</completed_work>

<remaining_work>
- Task 2: FvpEngine rewrite — `with`→`implements` 6 interfaces, lastError/isSeeking, new MediaState values, PlayerError subtypes
- Task 3: MediaOpener (PlayerError) + FvpCallbackHandler (new MediaState)
- Task 4: Move7 services + PlayerServices to kernel/, update PlaybackController, delete PlaybackContract
- Task 5: UI consumers — ErrorBanner (PlayerError), AutoHideController (new MediaState), EngineState→EngineStateView (25+ files)
- Task 6: Tests — mixin_capability_test, error_banner_test, player_error_test
</remaining_work>

## Key Type Mappings

| Old | New |
|-----|-----|
| `EngineState` mixin | `EngineStateView` + `PlaybackControl` |
| `MediaErrorType` enum | `PlayerError` sealed class (5 subtypes) |
| `errorMessage` (String?) | `lastError` (PlayerError?) |
| `MediaState.loading` | `MediaState.opening` |
| `MediaState.stopped` | `MediaState.idle` |
| `MediaState.seeking` | `isSeeking.value` |
| `MediaState.buffering` | `isBuffering.value` |
| `VideoEffects` mixin | `VideoEffectControl` abstract class |
| `RendererConfig` mixin | `RendererControl` abstract class |
| `OpenError(MediaErrorType, String)` | `OpenError(PlayerError)` |

## Uncommitted Files
- `lib/kernel/engine/media_state.dart` — added MediaStateTransition extension
- `lib/kernel/engine/engine_state.dart` — added player_error.dart + open_result.dart exports

## Required Reading
1. `.planning/phases/09-interface-decomposition/09-02-PLAN.md` — 完整迁移计划
2. `.planning/phases/09-interface-decomposition/09-01-SUMMARY.md` — Plan 01 产出物

<next_action>
Task 2: 重写 FvpEngine — `class FvpEngine implements EngineStateView, PlaybackControl, TrackControl, SubtitleConfig, VideoEffectControl, RendererControl`。替换 errorMessage/errorType 为 lastError/isSeeking。更新 MediaState 引用和错误处理。
</next_action>
