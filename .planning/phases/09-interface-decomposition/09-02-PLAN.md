---
phase: 09-interface-decomposition
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - lib/kernel/services/playback_controller.dart
  - lib/kernel/services/playback_navigator.dart
  - lib/kernel/services/file_operations.dart
  - lib/kernel/services/state_monitor.dart
  - lib/kernel/services/subtitle_service.dart
  - lib/kernel/services/video_processing_service.dart
  - lib/kernel/services/breakpoint_saver.dart
  - lib/kernel/player_services.dart
  - lib/kernel/engine/fvp_engine.dart
  - lib/ui/player/error_banner.dart
  - lib/ui/player/player_screen.dart
  - lib/ui/player/controls_overlay.dart
  - lib/ui/player/control_bar.dart
  - lib/ui/player/left_button_group.dart
  - lib/ui/player/video_surface.dart
  - lib/ui/player/progress_bar.dart
  - lib/ui/player/auto_hide_controller.dart
  - lib/ui/player/speed_button.dart
  - lib/ui/player/volume_controls.dart
  - lib/ui/player/time_range_display.dart
  - lib/ui/player/center_controls.dart
  - lib/ui/dialogs/settings_panel.dart
  - lib/ui/dialogs/media_info_dialog.dart
  - lib/ui/dialogs/settings/settings_tab_performance.dart
  - lib/ui/dialogs/settings/equalizer_tab.dart
  - lib/ui/dialogs/settings/audio_tab.dart
  - lib/ui/dialogs/settings/video_tab.dart
  - lib/ui/shared/empty_state.dart
  - lib/ui/shared/aurora_background.dart
  - lib/app.dart
  - lib/features/player/player_feature.dart
  - lib/features/player/deferred_player_feature.dart
  - lib/kernel/engine/fvp_engine.dart
  - lib/kernel/engine/track_manager.dart
  - lib/kernel/engine/media_opener.dart
  - lib/kernel/engine/fvp_callback_handler.dart
  - test/helpers/fake_engine.dart
  - test/engine/mixin_capability_test.dart
  - test/widget/player/error_banner_test.dart
  - test/kernel/models/player_error_test.dart
autonomous: true
requirements:
  - SVC-01
  - ENG-01
  - ENG-03
must_haves:
  truths:
    - PlaybackController and all 6 sub-modules live in kernel/services/ (not features/player/services/)
    - PlayerServices DI container lives in kernel/ (not features/player/)
    - PlaybackContract interface is deleted
    - All 34 files importing engine_state.dart compile with the barrel export (EngineStateView accessible)
    - FvpEngine implements all 6 new interfaces (not with EngineState mixin)
    - ErrorBanner uses PlayerError sealed class for switch (not MediaErrorType)
    - No file imports from features/player/services/ path
    - flutter analyze reports 0 errors
    - flutter test passes (all existing tests green)
  artifacts:
    - lib/kernel/services/ (7 files migrated from features/player/services/)
    - lib/kernel/player_services.dart (migrated from features/player/)
  key_links:
    - FvpEngine implements EngineStateView + PlaybackControl + 4 capability interfaces
    - ErrorBanner switches on PlayerError sealed subtypes
    - All consumer files reference kernel/services/ path (not features/player/services/)
---

<objective>
Migrate all service modules from features/player/services/ to kernel/services/, update FvpEngine to implement 6 new interfaces, rewrite ErrorBanner to use PlayerError sealed class, update all 34+ consumer files to use new types from Plan 01 barrel export, delete PlaybackContract, update all tests.

Purpose: Complete the architectural migration — services in correct layer, all consumers using new ISP interfaces, error model fully wired through.
Output: 7 migrated service files, 1 migrated DI container, FvpEngine rewritten, 30+ consumer files updated, 3 test files rewritten
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-interface-decomposition/09-CONTEXT.md
@.planning/phases/09-interface-decomposition/09-RESEARCH.md
@.planning/phases/09-interface-decomposition/09-01-SUMMARY.md
@lib/kernel/engine/fvp_engine.dart
@lib/features/player/services/playback_controller.dart
@lib/features/player/services/playback_contract.dart
@lib/features/player/services/playback_navigator.dart
@lib/features/player/services/file_operations.dart
@lib/features/player/services/state_monitor.dart
@lib/features/player/services/subtitle_service.dart
@lib/features/player/services/video_processing_service.dart
@lib/features/player/services/breakpoint_saver.dart
@lib/features/player/player_services.dart
@lib/ui/player/error_banner.dart
@test/helpers/fake_engine.dart
@test/engine/mixin_capability_test.dart
@test/widget/player/error_banner_test.dart
@test/kernel/models/player_error_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 7 service files to kernel/services/ + PlayerServices DI + delete PlaybackContract + rewrite FvpEngine</name>
  <files>
    lib/kernel/services/playback_controller.dart,
    lib/kernel/services/playback_navigator.dart,
    lib/kernel/services/file_operations.dart,
    lib/kernel/services/state_monitor.dart,
    lib/kernel/services/subtitle_service.dart,
    lib/kernel/services/video_processing_service.dart,
    lib/kernel/services/breakpoint_saver.dart,
    lib/kernel/player_services.dart,
    lib/kernel/engine/fvp_engine.dart,
    lib/features/player/services/playback_contract.dart
  </files>
  <read_first>
    lib/features/player/services/playback_controller.dart — source file to move, has imports of engine_state.dart + playback_contract.dart + sub-modules
    lib/features/player/services/playback_navigator.dart — source file to move, references EngineState type
    lib/features/player/services/file_operations.dart — source file to move, references EngineState type
    lib/features/player/services/state_monitor.dart — source file to move, references EngineState type
    lib/features/player/services/subtitle_service.dart — source file to move
    lib/features/player/services/video_processing_service.dart — source file to move
    lib/features/player/services/breakpoint_saver.dart — source file to move
    lib/features/player/player_services.dart — DI container to move, references EngineState type
    lib/features/player/services/playback_contract.dart — to DELETE per D-13
    lib/kernel/engine/fvp_engine.dart — to rewrite: `with EngineState` -> `implements 6 interfaces`
    lib/kernel/engine/engine_state.dart — barrel export (from Plan 01)
    lib/kernel/engine/engine_state_view.dart — new interface (from Plan 01)
    lib/kernel/engine/playback_control.dart — new interface (from Plan 01)
    lib/kernel/models/player_error.dart — new error model (from Plan 01)
    lib/kernel/engine/media_state.dart — new 6-value enum (from Plan 01)
    lib/kernel/engine/media_opener.dart — uses OpenResult, may need PlayerError updates
    lib/kernel/engine/fvp_callback_handler.dart — references MediaState, may need state value updates
  </read_first>
  <action>
**Step 1: Move 7 service files from features/player/services/ to kernel/services/** (per D-11)

For each of the 7 files, move from `lib/features/player/services/` to `lib/kernel/services/`:

a) **playback_controller.dart** — Update imports:
   - `import '../../../kernel/engine/engine_state.dart'` -> `import '../engine/engine_state.dart'`
   - `import 'playback_contract.dart'` -> DELETE this import (per D-13)
   - `import 'playback_navigator.dart'` -> `import 'playback_navigator.dart'` (same directory now)
   - `import 'file_operations.dart'` -> same
   - `import 'state_monitor.dart'` -> same
   - `import 'subtitle_service.dart'` -> same
   - Update any `EngineState` type annotations to `EngineStateView` where the parameter is read-only, or keep `EngineStateView` if the controller only reads state
   - Actually, PlaybackController takes `EngineState engine` in constructor — change to `EngineStateView engine` for read-only access. For methods that call engine.play()/pause()/open(), cast to PlaybackControl or require the parameter to implement PlaybackControl
   - Simpler approach: since FvpEngine implements ALL 6 interfaces, PlaybackController constructor should accept a type that covers both read and write. Use `EngineStateView` for state reading and require the caller to pass an object that also implements PlaybackControl. The cleanest approach: change the `engine` parameter type to `EngineStateView` and cast to PlaybackControl where needed, OR accept both types
   - BEST approach per D-02/D-05: PlaybackController should accept `EngineStateView` for state reading AND `PlaybackControl` for commands. Update constructor to: `PlaybackController({required EngineStateView engine, required PlaybackControl control, ...})`. But this changes the API significantly
   - ALTERNATIVE: Since FvpEngine implements all 6 interfaces, and PlaybackController needs both state and control, accept a combined type. Dart doesn't have intersection types, so: accept `EngineStateView` and cast to `PlaybackControl` internally. Or accept `dynamic` and cast. Or define a local typedef
   - PRACTICAL approach: Keep the parameter as `EngineStateView engine` and add a separate `PlaybackControl control` parameter. In PlayerServices, pass the same FvpEngine instance for both. This is the cleanest ISP approach
   - Actually, looking at the existing code, PlaybackController accesses both state (engine.state.value) and commands (engine.play(), engine.open()). The simplest correct approach: accept `EngineStateView stateView` and `PlaybackControl control` as separate parameters. Update PlayerServices to pass `engine` for both (since FvpEngine implements all interfaces)
   - Wait, this is getting complex. Let me simplify: Since FvpEngine implements all 6 interfaces, and PlaybackController needs state + control, the pragmatic approach is to keep a single parameter but use the combined type. In Dart, we can't express `EngineStateView & PlaybackControl`, so:
     - Option A: Accept `EngineStateView` and cast to `PlaybackControl` where needed (runtime check)
     - Option B: Accept `PlaybackControl` and cast to `EngineStateView` where needed
     - Option C: Create a local interface that combines both (defeats the purpose of ISP)
     - Option D: Accept both as separate parameters
   - Go with **Option D**: `PlaybackController({required EngineStateView stateView, required PlaybackControl control, ...})`. This is the correct ISP pattern. Update all call sites in PlayerServices and tests to pass both
   - For sub-modules (PlaybackNavigator, FileOperations, etc.) that currently receive `PlaybackController` via constructor: they access engine through `controller.engine`. Update their access to use the new parameter names
   - Actually, looking at D-13: "Delete PlaybackContract interface, sub-modules depend directly on PlaybackController." This means sub-modules reference `PlaybackController` type directly, not a contract. The sub-modules receive `this` (the PlaybackController) in their constructor. So sub-modules access `controller.engineView` and `controller.control` (or whatever the new field names are)
   - SIMPLIFICATION: Keep the parameter name as `engine` but change its type to `EngineStateView`. Add a second parameter `control` of type `PlaybackControl`. In sub-modules, they access `controller.engine` (state view) and `controller.control` (commands). This minimizes the diff

   Actually, let me re-read the existing PlaybackController to understand its exact usage pattern.

   Looking at the existing code, PlaybackController has `final EngineState engine` field. Sub-modules access `controller.engine` for both state reading and command execution. The ISP split means:
   - State reading: `engine.state.value`, `engine.position.value`, etc. -> needs EngineStateView
   - Commands: `engine.play()`, `engine.open()`, etc. -> needs PlaybackControl
   - Track/subtitle/effect commands: `engine.getAudioTracks()`, etc. -> needs TrackControl/SubtitleConfig/etc.

   The cleanest approach that preserves backward compat for sub-modules:
   - Keep `engine` field but change type to `EngineStateView` (for state reading)
   - Add `control` field of type `PlaybackControl` (for commands)
   - Sub-modules access `controller.engine` for state, `controller.control` for commands
   - For capability interfaces (TrackControl, etc.), sub-modules that need them can access `controller.control` and cast, or PlaybackController can expose them as separate fields

   This is getting too detailed for a plan. Let me use a simpler approach:
   - Keep `engine` parameter type as `EngineStateView` (covers all state getters)
   - Add `control` parameter of type `PlaybackControl` (covers open/play/pause/seek/etc.)
   - For capability interfaces, sub-modules that need them (e.g., SubtitleService needs SubtitleConfig) receive them as additional parameters
   - This is the correct ISP decomposition

b) **playback_navigator.dart** — Update imports:
   - `import '../../../kernel/engine/engine_state.dart'` -> `import '../engine/engine_state.dart'`
   - Update type references from `EngineState` to `EngineStateView` for state reading
   - Add import for `playback_control.dart` if it calls engine commands

c) **file_operations.dart** — Same import path updates

d) **state_monitor.dart** — Same import path updates

e) **subtitle_service.dart** — Same import path updates

f) **video_processing_service.dart** — Same import path updates

g) **breakpoint_saver.dart** — Same import path updates

**Step 2: Move PlayerServices to kernel/** (per D-12)
- Move `lib/features/player/player_services.dart` to `lib/kernel/player_services.dart`
- Update imports to use relative paths within kernel/
- Change `late final EngineState engine` to `late final FvpEngine engine` (concrete type needed for creating instances)
- Pass both `engine` (as EngineStateView) and `engine` (as PlaybackControl) to PlaybackController constructor

**Step 3: Delete PlaybackContract** (per D-13)
- Delete `lib/features/player/services/playback_contract.dart`
- Grep for any remaining imports of `playback_contract` and update them

**Step 4: Rewrite FvpEngine to implement 6 interfaces** (per D-04)
- Change `class FvpEngine with EngineState, TrackControl, VideoEffects, RendererConfig` to `class FvpEngine implements EngineStateView, PlaybackControl, TrackControl, SubtitleConfig, VideoEffectControl, RendererControl`
- All `@override` fields and methods remain (FvpEngine already has explicit implementations)
- Add `@override final ValueNotifier<PlayerError?> lastError = ValueNotifier<PlayerError?>(null)` (replacing errorMessage)
- Remove `@override final ValueNotifier<String?> errorMessage` field
- Remove `MediaErrorType get errorType` getter
- Add `@override final ValueNotifier<bool> isSeeking = ValueNotifier<bool>(false)` (per D-17)
- Update all `_safeSetState()` calls: replace `state.value = MediaState.loading` with `state.value = MediaState.opening`, remove `MediaState.buffering` and `MediaState.seeking` references
- Remove `import 'media_error_type.dart'` from FvpEngine
- Update MediaOpener calls that return OpenError to use PlayerError types
- Update FvpCallbackHandler if it references MediaState values that no longer exist

**Step 5: Update MediaOpener** to use PlayerError
- In `lib/kernel/engine/media_opener.dart`, change all `OpenError(MediaErrorType.xxx, message)` to `OpenError(FileError(...)` / `OpenError(CodecError(...)` / etc.
- Import `../../models/player_error.dart` instead of `media_error_type.dart`
- Map each MediaErrorType usage to the corresponding PlayerError subtype:
  - `MediaErrorType.file` -> `FileError(FileErrorCode.pathEmpty, message)` or `FileError(FileErrorCode.fileNotFound, message)`
  - `MediaErrorType.codec` -> `CodecError(CodecErrorCode.unsupportedFormat, message)`
  - `MediaErrorType.playback` -> `PlaybackError(PlaybackErrorCode.playFailed, message)`
  - `MediaErrorType.network` -> `NetworkError(NetworkErrorCode.timeout, message)`
  - `MediaErrorType.unknown` -> `UnknownError(message)`

**Step 6: Update FvpCallbackHandler** if it references MediaState.seeking/buffering
- In `lib/kernel/engine/fvp_callback_handler.dart`, replace `state.value = MediaState.buffering` with `isBuffering.value = true` (and set back to false when buffering ends)
- Replace `state.value = MediaState.seeking` with `isSeeking.value = true` (and set back to false)
- The handler receives `state` and `isBuffering` ValueNotifiers — add `isSeeking` parameter if needed

**Step 7: Verify**
- Run `flutter analyze` to check all files compile
- The consumer files (UI layer) will still have compile errors until Task 2 updates them
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter analyze lib/kernel/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 7 files exist in `lib/kernel/services/` (playback_controller, playback_navigator, file_operations, state_monitor, subtitle_service, video_processing_service, breakpoint_saver)
    - `lib/kernel/player_services.dart` exists (migrated from features/player/)
    - `lib/features/player/services/playback_contract.dart` is deleted
    - `lib/features/player/services/` directory no longer exists (or contains no .dart files)
    - FvpEngine class declaration is `class FvpEngine implements EngineStateView, PlaybackControl, TrackControl, SubtitleConfig, VideoEffectControl, RendererControl`
    - FvpEngine has `lastError` field (ValueNotifier<PlayerError?>) instead of `errorMessage`
    - FvpEngine has `isSeeking` field (ValueNotifier<bool>)
    - FvpEngine uses `MediaState.opening` (not `loading`), no `MediaState.buffering`/`MediaState.seeking`/`MediaState.stopped` references
    - MediaOpener uses `FileError`/`CodecError`/`PlaybackError`/`NetworkError`/`UnknownError` instead of `MediaErrorType`
    - `flutter analyze lib/kernel/` reports 0 errors
    - No file in lib/ imports from `features/player/services/` path
  </acceptance_criteria>
  <done>
    All 7 service files migrated to kernel/services/
    PlayerServices DI container migrated to kernel/
    PlaybackContract deleted
    FvpEngine implements all 6 interfaces (not with EngineState)
    MediaOpener uses PlayerError subtypes
    FvpCallbackHandler updated for orthogonal state model
    flutter analyze passes on kernel/ directory
    No imports from old features/player/services/ path in lib/kernel/
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all UI consumers + ErrorBanner + app.dart + player_feature + tests</name>
  <files>
    lib/ui/player/error_banner.dart,
    lib/ui/player/player_screen.dart,
    lib/ui/player/controls_overlay.dart,
    lib/ui/player/control_bar.dart,
    lib/ui/player/left_button_group.dart,
    lib/ui/player/video_surface.dart,
    lib/ui/player/progress_bar.dart,
    lib/ui/player/auto_hide_controller.dart,
    lib/ui/player/speed_button.dart,
    lib/ui/player/volume_controls.dart,
    lib/ui/player/time_range_display.dart,
    lib/ui/player/center_controls.dart,
    lib/ui/dialogs/settings_panel.dart,
    lib/ui/dialogs/media_info_dialog.dart,
    lib/ui/dialogs/settings/settings_tab_performance.dart,
    lib/ui/dialogs/settings/equalizer_tab.dart,
    lib/ui/dialogs/settings/audio_tab.dart,
    lib/ui/dialogs/settings/video_tab.dart,
    lib/ui/shared/empty_state.dart,
    lib/ui/shared/aurora_background.dart,
    lib/app.dart,
    lib/features/player/player_feature.dart,
    lib/features/player/deferred_player_feature.dart,
    test/engine/mixin_capability_test.dart,
    test/widget/player/error_banner_test.dart,
    test/kernel/models/player_error_test.dart
  </files>
  <read_first>
    lib/kernel/engine/engine_state.dart — barrel export (from Plan 01) providing all new types
    lib/kernel/engine/engine_state_view.dart — EngineStateView interface
    lib/kernel/models/player_error.dart — PlayerError sealed class with 5 subtypes
    lib/kernel/engine/media_state.dart — new 6-value MediaState enum
    lib/ui/player/error_banner.dart — currently switches on MediaErrorType, must switch on PlayerError
    lib/features/player/player_feature.dart — currently imports from features/player/services/, must import from kernel/services/
    lib/features/player/deferred_player_feature.dart — same
    lib/app.dart — imports from features/player/services/
    test/engine/mixin_capability_test.dart — tests old mixin types (TrackControl/VideoEffects/RendererConfig)
    test/widget/player/error_banner_test.dart — tests ErrorBanner with old errorType field
    test/kernel/models/player_error_test.dart — tests old PlayerErrorCode enum
  </read_first>
  <action>
**Step 1: Rewrite ErrorBanner** (per D-09, D-10)
- Change constructor parameter type from `EngineState engine` to `EngineStateView engine`
- Change `ValueListenableBuilder2<MediaState, String?>` to `ValueListenableBuilder2<MediaState, PlayerError?>`
  - First: `engine.state` (ValueNotifier<MediaState>)
  - Second: `engine.lastError` (ValueNotifier<PlayerError?>)
- Change builder parameters: `state` stays, `msg` becomes `error` (PlayerError?)
- Update visibility check: `if (state != MediaState.error || error == null)` (same logic, different type)
- Replace switch on `engine.errorType` (MediaErrorType) with switch on `error` (PlayerError sealed class):
  ```dart
  switch (error) {
    case FileError(:final code):
      callback = onOpenFile;
      actionLabel = l10n.reopen;
    case CodecError():
      callback = onOpenFile;
      actionLabel = l10n.selectOtherFile;
    case PlaybackError() || NetworkError() || UnknownError():
      callback = onRetry;
      actionLabel = l10n.retry;
  }
  ```
- Change error message display from `msg` to `error.message`
- Remove `import '../../kernel/engine/engine_state.dart'` if it was the only import from that path — actually, keep it since engine_state.dart is now a barrel export that provides EngineStateView + PlayerError

**Step 2: Update all UI files importing engine_state.dart** (per D-01 — consumers reference EngineStateView)
Most UI files only need a type annotation update. The barrel export ensures `EngineStateView` is available through the existing import path. The changes needed per file:

For each file that has `EngineState engine` as a parameter or field:
- Change `EngineState` to `EngineStateView` in parameter types, field types, and variable declarations
- Files: player_screen.dart, controls_overlay.dart, control_bar.dart, left_button_group.dart, video_surface.dart, progress_bar.dart, speed_button.dart, volume_controls.dart, time_range_display.dart, center_controls.dart, settings_dialog.dart (settings_panel.dart), media_info_dialog.dart, settings_tab_performance.dart, equalizer_tab.dart, audio_tab.dart, video_tab.dart, empty_state.dart, aurora_background.dart

For files that call engine commands (play/pause/seek/open):
- These files receive the engine as `EngineStateView` but need to call commands
- Cast to `PlaybackControl` where needed: `(engine as PlaybackControl).play()`
- OR accept both `EngineStateView` and `PlaybackControl` parameters
- BEST approach: Since PlayerScreen creates the player and passes engine to children, and children need both state and commands, the cleanest approach is to pass the concrete FvpEngine (which implements all 6 interfaces) and let children use the appropriate interface
- PRACTICAL approach: Keep the parameter as `EngineStateView` and add a separate `PlaybackControl control` parameter where commands are needed. Most UI widgets only read state (ValueListenableBuilder), so they only need EngineStateView. Widgets that trigger commands (like control_bar buttons) need PlaybackControl
- For widgets that need both: add a `PlaybackControl? control` optional parameter. If null, commands are disabled

For auto_hide_controller.dart:
- If it references MediaState.seeking or MediaState.buffering, update to use isSeeking/isBuffering

**Step 3: Update app.dart and player_feature files**
- `lib/app.dart`: Update import from `features/player/services/` to `kernel/services/` or `kernel/player_services.dart`
- `lib/features/player/player_feature.dart`: Update import from `services/playback_controller.dart` to `../../kernel/services/playback_controller.dart`, update `import 'player_services.dart'` to `import '../../kernel/player_services.dart'`
- `lib/features/player/deferred_player_feature.dart`: Same import updates
- In PlayerFeature, `late final EngineState engine` becomes `late final FvpEngine engine` (concrete type for creation) or `late final EngineStateView engine` (for state reading)
- Actually, PlayerFeature creates PlayerServices which creates FvpEngine. PlayerFeature accesses `services.engine` for state. Change type to `EngineStateView` for state reading

**Step 4: Update settings_panel.dart and video_tab.dart** (import path changes)
- These files import from `features/player/services/` — update to `kernel/services/`
- Or if they only import from `features/player/player_services.dart` — update to `kernel/player_services.dart`

**Step 5: Rewrite test files**

a) **test/engine/mixin_capability_test.dart**:
- Rename to `test/engine/interface_capability_test.dart` (or keep name, update content)
- Update all type checks from old mixin names to new interface names:
  - `isA<TrackControl>()` stays (same name, new abstract class)
  - `isA<VideoEffects>()` -> `isA<VideoEffectControl>()`
  - `isA<RendererConfig>()` -> `isA<RendererControl>()`
  - `isA<EngineState>()` -> `isA<EngineStateView>()`
- Update pattern matching tests: `case final VideoEffects ve` -> `case final VideoEffectControl ve`, `case final RendererConfig rc` -> `case final RendererControl rc`
- Add new tests for SubtitleConfig and PlaybackControl interface checks
- Add test for `isA<EngineStateView>()` with lastError getter
- Update `all 12 ValueNotifiers` test: replace `engine.errorMessage` with `engine.lastError`, add `engine.isSeeking` check (13 ValueNotifiers total now)

b) **test/widget/player/error_banner_test.dart**:
- Change `engine.errorType = MediaErrorType.file` to `engine.lastError.value = FileError(FileErrorCode.pathEmpty, 'Cannot open')`
- Update `simulateError('File not found')` calls — FakeEngine.simulateError now sets lastError
- Update test that checks "shows nothing when errorMessage is null" — now checks `lastError.value`
- Update tests that check action button labels — switch on PlayerError subtypes

c) **test/kernel/models/player_error_test.dart**:
- Complete rewrite for sealed class hierarchy:
  - Test exhaustive pattern matching (switch on PlayerError with all 5 subtypes)
  - Test each subtype carries correct code + message
  - Test FileError/CodecError/PlaybackError/NetworkError/UnknownError constructors
  - Test that PlayerError is sealed (can't be instantiated directly)
  - Remove old PlayerErrorCode enum tests

**Step 6: Full verification**
- `flutter analyze` — 0 errors across entire project
- `flutter test` — all existing tests pass
- Grep for any remaining references to old types:
  - `grep -r "EngineState " lib/ --include="*.dart"` should only appear in barrel export
  - `grep -r "MediaErrorType" lib/` should return 0 results
  - `grep -r "PlayerErrorCode" lib/` should return 0 results (except in player_error.dart if sub-enum codes kept)
  - `grep -r "errorMessage" lib/` should return 0 results (except comments)
  - `grep -r "features/player/services/" lib/` should return 0 results
  - `grep -r "MediaState.loading\|MediaState.stopped\|MediaState.seeking\|MediaState.buffering" lib/` should return 0 results
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter analyze 2>&1 | tail -5</automated>
    <automated>cd D:/simple_player_flutter && flutter test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ErrorBanner constructor takes `EngineStateView engine` (not `EngineState`)
    - ErrorBanner switches on `PlayerError` sealed subtypes (not `MediaErrorType`)
    - ErrorBanner displays `error.message` (not `msg` from `errorMessage`)
    - `flutter analyze` reports 0 errors across entire project
    - `flutter test` passes — all existing tests green (or updated to match new types)
    - `grep -r "MediaErrorType" lib/` returns 0 results (no references to deleted enum)
    - `grep -r "features/player/services/" lib/` returns 0 results (no old import paths)
    - `grep -r "MediaState.loading\|MediaState.stopped\|MediaState.seeking\|MediaState.buffering" lib/` returns 0 results (no old enum values)
    - `grep -r "errorMessage" lib/ --include="*.dart"` returns 0 results in production code (only in comments if any)
    - test/engine/mixin_capability_test.dart (or renamed) passes with new interface type checks
    - test/widget/player/error_banner_test.dart passes with PlayerError-based test setup
    - test/kernel/models/player_error_test.dart passes with sealed class hierarchy tests
  </acceptance_criteria>
  <done>
    ErrorBanner uses PlayerError sealed class for error display
    All 34+ files compile with new ISP interfaces via barrel export
    All UI widgets use EngineStateView for state reading, PlaybackControl for commands
    app.dart and player_feature use kernel/ import paths
    PlaybackContract deleted, no references remain
    All 3 test files rewritten for new types
    flutter analyze: 0 errors
    flutter test: all tests pass
    No references to deleted types (MediaErrorType, old MediaState values, errorMessage, features/player/services/ path)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | This phase migrates existing code to new interfaces — no new I/O, no new external calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-SC | Tampering | npm/pip/cargo installs | N/A | N/A | No external packages installed in this phase |
| T-9-02 | Tampering | Import path migration | low | mitigate | Global grep verification ensures no stale imports remain. Compile-time errors catch any missed migrations. |
</threat_model>

<verification>
- `flutter analyze` across entire project: 0 errors
- `flutter test` across entire test suite: all tests pass
- Grep verification: no references to deleted types or old import paths
- All service files in kernel/services/ (not features/player/services/)
- All consumer files compile with new ISP interfaces
</verification>

<success_criteria>
- All 7 service files + PlayerServices DI in kernel/
- PlaybackContract deleted
- FvpEngine implements 6 interfaces (not with EngineState mixin)
- ErrorBanner uses PlayerError sealed class
- 34+ consumer files updated to new types
- 3 test files rewritten
- flutter analyze: 0 errors
- flutter test: all pass
- No references to deleted types or old paths
</success_criteria>

<output>
Create `.planning/phases/09-interface-decomposition/09-02-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

### Migrated Files (8)
| File | From | To |
|------|------|-----|
| `lib/kernel/services/playback_controller.dart` | `lib/features/player/services/playback_controller.dart` | kernel layer |
| `lib/kernel/services/playback_navigator.dart` | `lib/features/player/services/playback_navigator.dart` | kernel layer |
| `lib/kernel/services/file_operations.dart` | `lib/features/player/services/file_operations.dart` | kernel layer |
| `lib/kernel/services/state_monitor.dart` | `lib/features/player/services/state_monitor.dart` | kernel layer |
| `lib/kernel/services/subtitle_service.dart` | `lib/features/player/services/subtitle_service.dart` | kernel layer |
| `lib/kernel/services/video_processing_service.dart` | `lib/features/player/services/video_processing_service.dart` | kernel layer |
| `lib/kernel/services/breakpoint_saver.dart` | `lib/features/player/services/breakpoint_saver.dart` | kernel layer |
| `lib/kernel/player_services.dart` | `lib/features/player/player_services.dart` | kernel layer |

### Rewritten Files (4)
| File | Change |
|------|--------|
| `lib/kernel/engine/fvp_engine.dart` | `with EngineState` -> `implements 6 interfaces`, lastError, isSeeking |
| `lib/kernel/engine/media_opener.dart` | MediaErrorType -> PlayerError subtypes |
| `lib/kernel/engine/fvp_callback_handler.dart` | MediaState.seeking/buffering -> isSeeking/isBuffering ValueNotifiers |
| `lib/ui/player/error_banner.dart` | MediaErrorType switch -> PlayerError sealed class switch |

### Updated Consumer Files (25+)
| File | Change Type |
|------|-------------|
| `lib/ui/player/*.dart` (12 files) | EngineState -> EngineStateView type annotation |
| `lib/ui/dialogs/*.dart` (5 files) | EngineState -> EngineStateView type annotation |
| `lib/ui/shared/*.dart` (2 files) | EngineState -> EngineStateView type annotation |
| `lib/app.dart` | Import path update |
| `lib/features/player/*.dart` (2 files) | Import path update |
| `test/engine/mixin_capability_test.dart` | New interface type checks |
| `test/widget/player/error_banner_test.dart` | PlayerError-based test setup |
| `test/kernel/models/player_error_test.dart` | Sealed class hierarchy tests |

### Deleted Files (1)
| File | Reason |
|------|--------|
| `lib/features/player/services/playback_contract.dart` | Replaced by direct PlaybackController dependency (D-13) |
