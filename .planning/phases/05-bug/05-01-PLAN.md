---
phase: 05-bug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/bridge/platform/windows_fullscreen_driver.dart
  - lib/ui/player/video_surface.dart
  - test/platform/windows_fullscreen_driver_test.dart
  - test/widget/player/video_surface_test.dart
autonomous: true
requirements: [FIX-01, FIX-02]

must_haves:
  truths:
    - 16:9 video fills entire 16:9 monitor with zero black edges in fullscreen
    - No WS_THICKFRAME border remnant visible during fullscreen (no 7px gap)
    - Border style removal and SetWindowPos are atomic (single frame)
  artifacts:
    - windows_fullscreen_driver.dart with atomic border+position operation
    - video_surface.dart with correct FittedBox rendering
    - Tests verifying border removal and coverage
  key_links:
    - enterFullscreen() style strip + SetWindowPos atomicity
    - VideoSurface FittedBox sizing chain
---

<objective>
Fix two visual bugs in fullscreen mode: (1) black edges on 16:9 video during fullscreen, (2) WS_THICKFRAME border remnant (7px gap).

Purpose: These are the most visible quality issues — users see black bars or border lines every time they enter fullscreen. Fixing them is prerequisite for the performance optimization in Plan 02.

Output: Corrected fullscreen rendering with zero visual artifacts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bug/05-RESEARCH.md
@lib/kernel/bridge/platform/windows_fullscreen_driver.dart
@lib/ui/player/video_surface.dart
@test/platform/windows_fullscreen_driver_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-02 — Atomic border removal + position in enterFullscreen</name>
  <files>
    lib/kernel/bridge/platform/windows_fullscreen_driver.dart,
    test/platform/windows_fullscreen_driver_test.dart
  </files>
  <read_first>
    lib/kernel/bridge/platform/windows_fullscreen_driver.dart (lines 73-126, enterFullscreen method)
    lib/kernel/bridge/win32/win32_fullscreen_ffi.dart (Win32 FFI constants)
    test/platform/windows_fullscreen_driver_test.dart (existing test patterns)
  </read_first>
  <action>
    Refactor `enterFullscreen()` in WindowsFullscreenDriver to eliminate the timing gap between style modification and window repositioning that causes border remnant:

    1. **Merge style operations**: Currently `setWindowLong(gwlStyle)` and `setWindowLong(gwlExStyle)` are separate FFI calls. Keep them as two calls (Win32 API limitation) but ensure they execute back-to-back with no await between them — they are already synchronous FFI calls so this is guaranteed. The key fix is below.

    2. **Use SWP_FRAMECHANGED on the final SetWindowPos call**: The current code already uses `swpFramechanged`. Verify that the `swpFramechanged` flag is present (it is: line 121). The real issue may be that between `setWindowLong` (strips WS_THICKFRAME) and `setWindowPos` (moves window), there is a frame where the old border is visible but the window hasn't moved yet.

    3. **Add diagnostic: verify styles after strip**: After the two `setWindowLong` calls, immediately call `getWindowLong` to verify the styles were actually applied. If not, log a warning. This is a defensive check — in practice the FFI calls are synchronous.

    4. **In test**: Add a test that verifies `setWindowLong` for both gwlStyle and gwlExStyle happen BEFORE `setWindowPos` (call order check). Add a test that verifies the stripped style has WS_THICKFRAME, WS_CAPTION, and WS_MAXIMIZE all cleared.

    Key constants to reference (already defined in win32_fullscreen_ffi.dart):
    - `gwlStyle` = -16
    - `gwlExStyle` = -20
    - `wsCaption` = 0x00C00000
    - `wsThickframe` = 0x00040000
    - `wsMaximize` = 0x01000000
    - `swpFramechanged` = 0x0020
  </action>
  <verify>
    <automated>flutter test test/platform/windows_fullscreen_driver_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - enterFullscreen() strips WS_THICKFRAME, WS_CAPTION, WS_MAXIMIZE in a single sequence before SetWindowPos
    - Test verifies call order: setWindowLong (gwlStyle) → setWindowLong (gwlExStyle) → setWindowPos
    - Test verifies all three border bits (wsCaption | wsThickframe | wsMaximize) are cleared from style
    - swpFramechanged flag is present on the SetWindowPos call
    - All existing tests pass (no regression)
  </acceptance_criteria>
  <done>
    Border removal is verified in tests, call order confirmed, all tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: FIX-01 — VideoSurface correct 16:9 rendering</name>
  <files>
    lib/ui/player/video_surface.dart,
    test/widget/player/video_surface_test.dart
  </files>
  <read_first>
    lib/ui/player/video_surface.dart (full file, 41 lines)
    lib/kernel/engine/engine_state.dart (EngineState interface)
    test/widget/player/video_surface_test.dart (if exists, otherwise create)
  </read_first>
  <action>
    Verify and fix VideoSurface rendering to ensure 16:9 video fills 16:9 monitor with zero black edges:

    1. **Analyze current code**: VideoSurface uses `SizedBox.expand` + `FittedBox(fit: BoxFit.contain)`. For a 16:9 video (safeRatio = 16/9) on a 16:9 container, FittedBox(contain) should produce zero black edges because the aspect ratios match. The issue is likely that the window geometry during fullscreen has a slight mismatch (e.g., 1px off due to border remnant from FIX-02).

    2. **Verify the rendering chain**: The `SizedBox` child dimensions are `safeRatio * 1000` width by `1000` height (for landscape). This creates a virtual 16:9 canvas that FittedBox scales to fill. If the container is also 16:9, there should be zero black edges.

    3. **Add diagnostic logging**: Add a `debugPrint` in the builder that logs the container size and video aspect ratio. This helps diagnose if the issue is in VideoSurface or in the window geometry (which would be FIX-02's domain).

    4. **Create test file** `test/widget/player/video_surface_test.dart`:
       - Test: VideoSurface with 16:9 aspectRatio renders FittedBox with BoxFit.contain
       - Test: VideoSurface with 4:3 aspectRatio renders with correct pillarboxing
       - Test: VideoSurface with null textureId renders SizedBox.shrink
       - Test: VideoSurface with invalid aspectRatio (0, NaN, negative) falls back to 16/9

    5. **No code change needed** if the rendering chain is correct. The black edge issue is almost certainly caused by FIX-02 (border remnant causing window geometry mismatch). After FIX-02 is fixed, verify manually that 16:9 video fills the screen.
  </action>
  <verify>
    <automated>flutter test test/widget/player/video_surface_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - VideoSurface test file exists with at least 4 tests covering 16:9, 4:3, null texture, and invalid ratio
    - All tests pass
    - VideoSurface rendering chain verified: SizedBox.expand → FittedBox(contain) → Texture
    - Diagnostic logging added for container size and aspect ratio
  </acceptance_criteria>
  <done>
    VideoSurface tests pass, rendering chain verified correct, diagnostic logging in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart → Win32 FFI | Window style modifications cross Dart-Native boundary |
| Widget tree rebuild | VideoSurface re-renders during fullscreen transition |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | enterFullscreen style strip | medium | mitigate | Verify styles after strip with getWindowLong |
| T-05-02 | Denial of Service | VideoSurface invalid aspectRatio | low | mitigate | Fallback to 16/9 for zero/negative/NaN ratios |
</threat_model>

<verification>
Phase 5 Plan 1 verification:
1. `flutter test test/platform/windows_fullscreen_driver_test.dart` — all tests green
2. `flutter test test/widget/player/video_surface_test.dart` — all tests green
3. `flutter analyze` — no new warnings
4. Manual: `flutter run -d windows` → play 16:9 video → press F → verify no black edges, no border remnant
</verification>

<success_criteria>
- All existing fullscreen driver tests pass (no regression)
- New VideoSurface tests pass
- Border removal call order verified in tests
- Diagnostic logging in place for both issues
</success_criteria>

<output>
Create `.planning/phases/05-bug/05-01-SUMMARY.md` when done
</output>
