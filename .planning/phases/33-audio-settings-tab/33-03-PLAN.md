---
phase: 33-audio-settings-tab
plan: 03
type: tdd
wave: 3
depends_on: [33-02]
files_modified:
  - lib/ui/dialogs/settings/audio_filter_compositor.dart
  - lib/ui/dialogs/settings/tabs/equalizer_tab.dart
  - test/kernel/audio/audio_filter_compositor_test.dart
  - test/ui/dialogs/settings/audio_tab_test.dart
  - test/kernel/persistence/settings_store_audio_test.dart
  - test/ui/dialogs/settings_tab_content_test.dart
  - test/ui/dialogs/settings_overlay_shell_test.dart
autonomous: true
requirements: [AUDIO-04, AUDIO-05, AUDIO-06, AUDIO-07]
must_haves:
  truths:
    - "A user can stage volume normalization as a toggle, and only Apply/OK sends it in the one combined af chain and persists it."
    - "All four audio controls can be combined and committed in order as EQ, balance, sync, then normalization; Cancel discards all staged values."
    - "The first settings tab is the sole Phase 33 implementation surface while the other placeholder tabs remain intact."
  artifacts:
    - path: "lib/ui/dialogs/settings/tabs/equalizer_tab.dart"
      provides: "Combined audio settings controls for EQ, balance, sync, and normalization"
    - path: "test/ui/dialogs/settings/audio_tab_test.dart"
      provides: "Commit/cancel integration regression coverage for all audio controls"
    - path: "test/kernel/audio/audio_filter_compositor_test.dart"
      provides: "Complete chain-order and normalization composition tests"
  key_links:
    - from: "lib/ui/dialogs/settings/tabs/equalizer_tab.dart"
      to: "lib/ui/dialogs/settings/settings_panel_controller.dart"
      via: "all four controls write registered audio pending keys before commit"
      pattern: "pending\.update\('(eqPresetIndex|balance|syncMs|normalization)'"
    - from: "lib/ui/dialogs/settings/settings_panel_controller.dart"
      to: "lib/features/player/player_feature.dart"
      via: "one typed audio commit callback applies and persists the complete AudioSettings snapshot"
      pattern: "AudioSettingsCommit"
---

<objective>
Finish the combined audio tab with normalization and lock the complete deferred-apply contract through focused integration regression tests and the target-runtime filter smoke check.

Purpose: Make the four audio controls a cohesive, persistent user capability without leaking live engine mutation into UI interactions.
Output: Normalization toggle, complete-chain test coverage, placeholder-tab scope regression checks, and Windows runtime evidence for the fvp audio filters.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-audio-settings-tab/33-RESEARCH.md
@.planning/phases/33-audio-settings-tab/33-01-SUMMARY.md
@.planning/phases/33-audio-settings-tab/33-02-SUMMARY.md
</context>

## Artifacts this phase produces

- Deferred `Volume Normalization` Switch backed by the `normalization` pending key.
- Complete audio-chain coverage for `dynaudnorm=f=500:g=15:p=0.95` as the final optional segment.
- Integration assertions for Apply/OK once, Cancel zero calls, all raw values persisted, and second/non-audio placeholder tabs preserved.

<tasks>

<task type="executor" agent_type="gsd-executor" tdd="true">
  <name>Task 1: Add normalization to the deferred combined audio tab and prove full-chain commit/cancel behavior</name>
  <files>lib/ui/dialogs/settings/audio_filter_compositor.dart, lib/ui/dialogs/settings/tabs/equalizer_tab.dart, test/kernel/audio/audio_filter_compositor_test.dart, test/ui/dialogs/settings/audio_tab_test.dart, test/kernel/persistence/settings_store_audio_test.dart, test/ui/dialogs/settings_tab_content_test.dart, test/ui/dialogs/settings_overlay_shell_test.dart</files>
  <read_first>
    - lib/ui/dialogs/settings/audio_filter_compositor.dart
    - lib/ui/dialogs/settings/tabs/equalizer_tab.dart
    - lib/ui/dialogs/settings/tabs/audio_tab.dart
    - lib/ui/dialogs/settings/settings_panel_controller.dart
    - lib/ui/dialogs/settings/settings_overlay_shell.dart
    - lib/ui/dialogs/settings/tab_content.dart
    - lib/kernel/persistence/settings_store.dart
    - test/kernel/audio/audio_filter_compositor_test.dart
    - test/ui/dialogs/settings/audio_tab_test.dart
    - test/kernel/persistence/settings_store_audio_test.dart
    - test/ui/dialogs/settings_tab_content_test.dart
    - test/ui/dialogs/settings_overlay_shell_test.dart
  </read_first>
  <behavior>
    - Test 1: normalization false omits its segment; true appends exactly `dynaudnorm=f=500:g=15:p=0.95` after EQ, pan, and adelay.
    - Test 2: all non-default values produce the canonical complete chain `bass=g=8,treble=g=6,pan=stereo|c0=0.70*c0|c1=1.00*c1,adelay=200|200,dynaudnorm=f=500:g=15:p=0.95` for rock, balance `0.3`, sync `200`, normalization enabled.
    - Test 3: toggle, preset, and both sliders alter only pending values; one Apply or OK invokes exactly one fake equalizer call and the stored raw values survive a fresh SettingsStore load.
    - Test 4: Cancel after changing all four controls invokes zero fake equalizer calls and retains the previous persisted values.
    - Test 5: tab-content regressions retain seven explicit IndexedStack children and the second AudioTab, VideoTab, subtitle-related, playback-related placeholders remain their prior UI surfaces.
  </behavior>
  <action>Start RED by adding the normalization and full-chain cases to the focused tests. Extend the existing AudioSettings composer so `normalization == true` appends exactly `dynaudnorm=f=500:g=15:p=0.95` after every eligible EQ, pan, and adelay segment; false must omit the segment. Add a `Volume Normalization` SettingRow to the combined first tab with a Switch keyed for tests, defaulting to false from `pending.current('normalization')`, using Tokens.accent for the active thumb, and writing only `pending.update('normalization', value)` followed by local redraw. Do not invoke engine code from the switch callback. Expand widget integration tests around the existing button bar to exercise all four pending keys, Apply, OK, and Cancel using the Plan 01 hand-written commit fake; assert an Apply does not close the shell, OK closes it, both commit paths produce one callback invocation, and Cancel produces none. Extend persistence tests to verify all four raw values round-trip after commit. Update only stale test assertions that referenced the replaced skeleton; retain the seven-child explicit IndexedStack contract and verify `tabs/audio_tab.dart`, video, subtitle, and playback placeholder surfaces are not transformed into Phase 33 controls. Add doc comments or inline rationale where the normalization filter order and deferred side effect boundary are non-obvious.</action>
  <acceptance_criteria>
    - Normalization true yields the exact `dynaudnorm=f=500:g=15:p=0.95` final segment; false yields no normalization segment.
    - The complete representative chain exactly follows EQ → pan → adelay → dynaudnorm and matches the stated rock/balance/sync sample.
    - Focused widget tests cover all four pending keys, Apply, OK, and Cancel; no UI interaction itself reaches an engine fake.
    - Focused persistence tests confirm `audioEqPreset`, `audioBalance`, `audioSyncMs`, and `audioNormalization` survive a new SettingsStore load.
    - Regression tests continue to assert seven explicit tab children and demonstrate that the second AudioTab plus non-audio placeholders were not repurposed by Phase 33.
  </acceptance_criteria>
  <verify>
    <automated>D:/flutter/bin/flutter test test/kernel/audio/audio_filter_compositor_test.dart test/kernel/persistence/settings_store_audio_test.dart test/ui/dialogs/settings/audio_tab_test.dart test/ui/dialogs/settings_tab_content_test.dart test/ui/dialogs/settings_overlay_shell_test.dart --concurrency=1 &amp;&amp; D:/flutter/bin/flutter analyze</automated>
    <human-check>On Windows with playable local media, select Rock, set balance +0.3, sync +200ms, enable Volume Normalization, press Apply, then reopen Settings and confirm all raw control values persisted. Repeat with Cancel after staging different values and confirm audible output and saved values remain unchanged.</human-check>
  </verify>
  <done>The first tab provides all four requested audio controls through one deferred commit callback; the compositor has exact complete-chain coverage, persistence round-trips raw values, Cancel has no engine-side impact, and other tabs remain outside Phase 33 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pending UI controls → one audio commit | Four local user settings become one FFmpeg filter chain only at Apply/OK. |
| Fvp bundled FFmpeg → audible output | Runtime availability of pan, adelay, and dynaudnorm is determined by the shipped Windows bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-07 | Tampering | Normalization state and filter order | low | mitigate | Use an immutable AudioSettings snapshot, fixed filter order, exact-string unit tests, and persisted-value validation. |
| T-33-08 | Denial of service | FFmpeg filter graph | medium | mitigate | Commit once per Apply/OK and run the bounded Windows media smoke test for every individual runtime filter path. |
| T-33-09 | Integrity | Scope boundary | low | mitigate | Assert seven tab children and retain the second audio and non-audio placeholder surfaces in focused regression tests. |
| T-33-SC | Tampering | package installs | high | accept | No package-manager operation occurs in this phase; pubspec remains unchanged. |
</threat_model>

<verification>
Run the complete Phase 33 focused suite and `D:/flutter/bin/flutter analyze`. The human Windows check is required to establish actual bundled-FFmpeg behavior for pan, adelay, and dynaudnorm, which cannot be proven by headless widget tests alone.
</verification>

<success_criteria>
- AUDIO-04 supplies deferred normalization with the exact required dynaudnorm parameters.
- AUDIO-05 sends the complete ordered chain through one existing equalizer entry.
- AUDIO-06 is validated for sliders, preset selection, toggle, Apply/OK, and Cancel.
- AUDIO-07 verifies raw persisted values across a new store read.
</success_criteria>

<output>
Create `.planning/phases/33-audio-settings-tab/33-03-SUMMARY.md` when done.
</output>
