---
phase: 03-platform-adaptation
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - lib/kernel/bridge/desktop_fullscreen_driver_factory.dart
  - lib/kernel/bridge/fullscreen_driver.dart
  - lib/kernel/bridge/desktop_fullscreen_adapter.dart
  - lib/main.dart
  - test/platform/fullscreen_driver_factory_test.dart
autonomous: true
requirements: [PLAT-04]

must_haves:
  truths:
    - "工厂根据 Platform.isXXX 选择正确的平台驱动 (D-P02)"
    - "编译时 flag USE_WINDOWS_NATIVE_FULLSCREEN 控制 Windows 驱动选择 (D-P03)"
    - "capabilities() 返回每平台真实能力 (PLAT-04)"
    - "Adapter 将原生回调转发给 Driver 的确认信号 (D-P11)"
  artifacts:
    - "lib/kernel/bridge/desktop_fullscreen_driver_factory.dart — 工厂实现"
    - "lib/kernel/bridge/fullscreen_driver.dart — 添加可选原生回调 setter"
    - "lib/kernel/bridge/desktop_fullscreen_adapter.dart — 回调转发修改"
    - "lib/main.dart — 使用工厂替换硬编码 driver"
    - "test/platform/fullscreen_driver_factory_test.dart — 工厂测试"
  key_links:
    - "DesktopFullscreenDriverFactory ↔ 三个平台 Driver — 创建关系"
    - "DesktopFullscreenAdapter ↔ FullscreenDriver.onNativeStateChanged — 回调转发"
    - "main.dart ↔ DesktopFullscreenDriverFactory — 初始化注入"
---

<objective>
实现平台驱动工厂 + 集成接入 — 统一创建入口、原生回调转发和 main.dart 接线。

Purpose: 将三端平台驱动统一接入 FullscreenAdapter 体系，通过工厂模式和编译时 flag 控制驱动选择。
Output: DesktopFullscreenDriverFactory + FullscreenDriver 接口扩展 + Adapter 回调转发 + main.dart 接线 + 测试。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-adaptation/03-CONTEXT.md
@.planning/phases/03-platform-adaptation/03-RESEARCH.md

@lib/kernel/bridge/fullscreen_driver.dart
@lib/kernel/bridge/desktop_fullscreen_adapter.dart
@lib/kernel/bridge/desktop_fullscreen_driver.dart
@lib/kernel/bridge/platform/windows_fullscreen_driver.dart
@lib/kernel/bridge/platform/macos_fullscreen_driver.dart
@lib/kernel/bridge/platform/linux_fullscreen_driver.dart
@lib/main.dart
@lib/kernel/models/fullscreen_capability.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: FullscreenDriver 接口扩展 + DesktopFullscreenDriverFactory</name>
  <files>
    lib/kernel/bridge/fullscreen_driver.dart
    lib/kernel/bridge/desktop_fullscreen_driver_factory.dart
    test/platform/fullscreen_driver_factory_test.dart
  </files>
  <read_first>
    - lib/kernel/bridge/fullscreen_driver.dart (当前接口: 11 个方法)
    - lib/kernel/bridge/desktop_fullscreen_driver.dart (现有 fallback 驱动)
    - lib/kernel/bridge/platform/windows_fullscreen_driver.dart (Plan 01 产出)
    - lib/kernel/bridge/platform/macos_fullscreen_driver.dart (Plan 02 产出)
    - lib/kernel/bridge/platform/linux_fullscreen_driver.dart (Plan 03 产出)
    - lib/kernel/models/fullscreen_capability.dart (FullscreenCapability 模型)
    - lib/main.dart (当前初始化逻辑: 第 42-49 行)
  </read_first>
  <action>
    **Step 1: 扩展 FullscreenDriver 接口 (D-P04 + D-P11)**

    修改 `lib/kernel/bridge/fullscreen_driver.dart`:

    添加可选的原生回调 setter (D-P11 统一确认链):
    ```dart
    /// 设置原生全屏状态变化回调 — 用于三级确认链的 Level 1。
    ///
    /// macOS: NSWindow delegate 回调
    /// Linux: GdkWindow window-state-event 信号
    /// Windows: 无需此机制 (FFI 同步操作)
    ///
    /// [callback] 参数: (windowId, isFullscreen)
    /// 默认空实现，支持原生回调的 Driver 子类覆盖。
    set onNativeStateChanged(void Function(int windowId, bool isFullscreen)? callback) {
      // 默认空实现 — Windows FFI 驱动不需要回调
    }

    /// 获取平台能力 — 每平台 Driver 返回真实值 (PLAT-04)。
    ///
    /// Phase B DesktopFullscreenDriver 返回默认值，
    /// Phase C 平台驱动返回真实能力矩阵。
    Future<FullscreenCapability> capabilities() async {
      return const FullscreenCapability();
    }
    ```

    注意: 使用默认实现而非 abstract，避免破坏现有 DesktopFullscreenDriver。

    **Step 2: 创建 DesktopFullscreenDriverFactory (D-P02 + D-P03)**

    创建 `lib/kernel/bridge/desktop_fullscreen_driver_factory.dart`:

    ```dart
    import 'dart:io';
    import 'fullscreen_driver.dart';
    import 'desktop_fullscreen_driver.dart';

    /// 平台驱动工厂 — 根据 Platform.isXXX 选择具体 Driver (D-P02)。
    ///
    /// 编译时 flag 控制驱动选择 (D-P03):
    /// - USE_NEW_FULLSCREEN=true: 使用 FullscreenAdapter 体系
    /// - USE_WINDOWS_NATIVE_FULLSCREEN=true: Windows 使用 FFI 驱动
    class DesktopFullscreenDriverFactory {
      DesktopFullscreenDriverFactory._();

      /// 编译时 flag: Windows 原生 FFI 驱动 (D-P03)。
      static const _useWindowsNative = bool.fromEnvironment(
        'USE_WINDOWS_NATIVE_FULLSCREEN',
        defaultValue: false,
      );

      /// 创建当前平台的 FullscreenDriver。
      ///
      /// Windows: WindowsFullscreenDriver (FFI) 或 DesktopFullscreenDriver (window_manager fallback)
      /// macOS: MacosFullscreenDriver (fullscreen_window 插件 + delegate 回调)
      /// Linux: LinuxFullscreenDriver (fullscreen_window 插件 + state-changed 信号)
      /// 其他: DesktopFullscreenDriver (window_manager fallback)
      static FullscreenDriver create() {
        if (Platform.isWindows) {
          if (_useWindowsNative) {
            // D-P03: USE_WINDOWS_NATIVE_FULLSCREEN=true 时使用 FFI 驱动
            return WindowsFullscreenDriver();
          }
          return DesktopFullscreenDriver();
        }
        if (Platform.isMacOS) {
          return MacosFullscreenDriver();
        }
        if (Platform.isLinux) {
          return LinuxFullscreenDriver();
        }
        // 未知平台: 降级到 window_manager
        return DesktopFullscreenDriver();
      }
    }
    ```

    **Step 3: 工厂测试**

    创建 `test/platform/fullscreen_driver_factory_test.dart`:

    由于 `Platform.isXXX` 在测试环境中固定 (当前为 Windows)，测试验证:
    - `DesktopFullscreenDriverFactory.create()` 返回正确的驱动类型
    - 在 Windows 环境下返回 `DesktopFullscreenDriver` (默认 flag) 或 `WindowsFullscreenDriver` (flag 启用时)
    - 工厂方法不抛出异常
    - capabilities() 返回非默认值的 FullscreenCapability (对已实现的平台驱动)
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/bridge/fullscreen_driver.dart lib/kernel/bridge/desktop_fullscreen_driver_factory.dart && flutter test test/platform/fullscreen_driver_factory_test.dart</automated>
  </verify>
  <done>FullscreenDriver 接口添加 onNativeStateChanged setter + capabilities() 方法，DesktopFullscreenDriverFactory 根据平台和编译时 flag 创建正确驱动，测试通过</done>
</task>

<task type="auto">
  <name>Task 2: Adapter 回调转发 + main.dart 接线</name>
  <files>
    lib/kernel/bridge/desktop_fullscreen_adapter.dart
    lib/main.dart
  </files>
  <read_first>
    - lib/kernel/bridge/desktop_fullscreen_adapter.dart (当前实现: onNativeFullScreenChanged 方法)
    - lib/kernel/bridge/fullscreen_driver.dart (Task 1 修改后的接口)
    - lib/kernel/bridge/desktop_fullscreen_driver_factory.dart (Task 1 产出)
    - lib/main.dart (当前初始化逻辑: 第 18-49 行)
    - lib/kernel/bridge/window_service.dart (FullscreenAdapter 注入点)
  </read_first>
  <action>
    **Step 1: 修改 DesktopFullscreenAdapter — 回调转发 (D-P11)**

    修改 `lib/kernel/bridge/desktop_fullscreen_adapter.dart`:

    在构造函数中，设置 driver 的原生回调:
    ```dart
    DesktopFullscreenAdapter(this._driver) {
      // D-P11: 将 driver 的原生回调转发到 Adapter 的确认信号
      _driver.onNativeStateChanged = onNativeFullScreenChanged;
    }
    ```

    这样 macOS/Linux driver 收到原生回调时，会自动调用 `onNativeFullScreenChanged(windowId, isFullscreen)`，
    完成 `_confirmByWindowId[windowId].complete(true)` (P0-1 确认信号)。

    **Step 2: 修改 main.dart — 使用工厂 (D-P02 + D-P03)**

    修改 `lib/main.dart`:

    替换当前初始化逻辑:
    ```dart
    // 旧:
    // final driver = DesktopFullscreenDriver();
    // fullscreenAdapter = DesktopFullscreenAdapter(driver);

    // 新:
    final driver = DesktopFullscreenDriverFactory.create();
    fullscreenAdapter = DesktopFullscreenAdapter(driver);
    ```

    添加 import:
    ```dart
    import 'kernel/bridge/desktop_fullscreen_driver_factory.dart';
    ```

    添加第二个编译时 flag (D-P03):
    ```dart
    /// 编译期开关：--dart-define=USE_WINDOWS_NATIVE_FULLSCREEN=true 启用 Windows FFI 驱动 (D-P03)。
    ///
    /// 仅在 USE_NEW_FULLSCREEN=true 时生效。
    /// 默认 false，Windows 使用 window_manager 包装。
    const bool _useWindowsNativeFullscreen = bool.fromEnvironment(
      'USE_WINDOWS_NATIVE_FULLSCREEN',
      defaultValue: false,
    );
    ```

    注意: `_useWindowsNativeFullscreen` 在 main.dart 中声明但实际由 factory 内部读取。
    保留 main.dart 中的声明是为了文档化目的和未来可能的条件逻辑。

    更新初始化注释:
    ```dart
    // P0-4 初始化顺序 (Phase C 更新):
    //   1. DesktopFullscreenDriverFactory.create() ← 每平台最优驱动
    //   2. DesktopFullscreenAdapter(driver)        ← 持有 driver + 回调转发
    //   3. WindowService(fullscreenAdapter)        ← 转发全屏操作
    ```

    **Step 3: 验证集成**

    确保:
    - `flutter analyze` 通过 (无未使用的 import，无类型错误)
    - 现有 `flutter test` 全套件不回归
    - `DesktopFullscreenDriver` (fallback) 不受接口扩展影响 (默认空实现)
  </action>
  <verify>
    <automated>flutter analyze lib/main.dart lib/kernel/bridge/desktop_fullscreen_adapter.dart && flutter test</automated>
  </verify>
  <done>DesktopFullscreenAdapter 通过 onNativeStateChanged 接收 driver 原生回调并转发确认信号，main.dart 使用工厂创建平台驱动，编译时 flag 控制驱动选择</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main.dart → Factory → Driver | 编译时 flag 控制驱动选择 — 运行时不可篡改 |
| Adapter ↔ Driver 回调 | 原生回调信号在 Adapter/Driver 间传递 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | 编译时 flag | low | accept | bool.fromEnvironment 在编译时确定，运行时不可修改。恶意修改需重新编译 |
| T-03-09 | Elevation of Privilege | 工厂创建 | low | accept | 工厂仅创建 FullscreenDriver 实例，不涉及权限提升。所有 driver 实现相同接口 |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | Phase C 不安装新外部包，无供应链风险 |
</threat_model>

<verification>
- `flutter analyze lib/kernel/bridge/fullscreen_driver.dart` — 零 warning
- `flutter analyze lib/kernel/bridge/desktop_fullscreen_driver_factory.dart` — 零 warning
- `flutter analyze lib/kernel/bridge/desktop_fullscreen_adapter.dart` — 零 warning
- `flutter analyze lib/main.dart` — 零 warning
- `flutter test test/platform/fullscreen_driver_factory_test.dart` — 全通过
- `flutter test` — 全套件不回归 (无现有测试被破坏)
</verification>

<success_criteria>
1. FullscreenDriver 接口添加 onNativeStateChanged setter (默认空实现) 和 capabilities() 方法
2. DesktopFullscreenDriverFactory.create() 根据 Platform.isXXX 返回正确驱动
3. USE_WINDOWS_NATIVE_FULLSCREEN 编译时 flag 控制 Windows 驱动选择
4. DesktopFullscreenAdapter 构造时设置 driver.onNativeStateChanged = onNativeFullScreenChanged
5. main.dart 使用 DesktopFullscreenDriverFactory.create() 替代硬编码 DesktopFullscreenDriver()
6. 现有 DesktopFullscreenDriver (fallback) 不受接口扩展影响
7. flutter test 全套件不回归
</success_criteria>

<output>
创建 `.planning/phases/03-platform-adaptation/03-04-SUMMARY.md` 当计划完成时
</output>
