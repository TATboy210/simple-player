---
phase: 01-window-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/window/window_manager_service.dart
  - lib/kernel/services/platform_service.dart
  - lib/kernel/platform/windows_platform_service.dart
  - lib/kernel/ui/theme/tokens.dart
  - lib/main.dart
autonomous: true
requirements: [WIN-01, WIN-08, WIN-09, WIN-10, WIN-11, WIN-12, WIN-13, WIN-14, WIN-15]

must_haves:
  truths:
    - "App launches frameless with no white flash (setAsFrameless before show, forceRedraw via MethodChannel)"
    - "F11 toggles fullscreen reliably with reentry guard preventing ABA state corruption"
    - "Window state (size/position/maximized/fullscreen/pin) persists with 500ms debounce"
    - "Restored window position is clamped to visible screen area on multi-monitor setups"
    - "Minimum window size is 640x360 (360p 16:9)"
    - "Always-on-top toggle works via window_manager.setAlwaysOnTop"
  artifacts:
    - path: "lib/kernel/window/window_manager_service.dart"
      provides: "WindowManagerService singleton with all window management"
      contains: "class WindowManagerService"
      min_lines: 300
    - path: "lib/kernel/services/platform_service.dart"
      provides: "Extended PlatformService with window control methods"
      contains: "ValueNotifier<WindowMode> get mode"
    - path: "lib/kernel/platform/windows_platform_service.dart"
      provides: "WindowsPlatformService delegating to WindowManagerService"
      contains: "WindowManagerService.I"
    - path: "lib/kernel/ui/theme/tokens.dart"
      provides: "Title bar design tokens"
      contains: "titleBarHeight"
  key_links:
    - from: "lib/kernel/platform/windows_platform_service.dart"
      to: "lib/kernel/window/window_manager_service.dart"
      via: "delegation pattern"
      pattern: "WindowManagerService\\.I\\."
    - from: "lib/main.dart"
      to: "lib/kernel/window/window_manager_service.dart"
      via: "PlatformService.init(WindowsPlatformService())"
      pattern: "PlatformService\\.init"
---

<objective>
Create the WindowManagerService singleton and extend PlatformService with window control methods.

Purpose: This is the foundation layer -- all window operations (frameless, fullscreen, persistence, bounds check, always-on-top) go through this service. The CustomTitleBar (Plan 02) and app wiring (Plan 03) depend on it.

Output: WindowManagerService, extended PlatformService interface, updated WindowsPlatformService, title bar tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-window-shell/01-CONTEXT.md
@.planning/phases/01-window-shell/01-RESEARCH.md

<interfaces>
<!-- Reference implementations the executor MUST port from -->

From D:\player_flutter\lib\kernel\window\window_manager_service.dart (516 lines):
- Singleton pattern: WindowManagerService._() + static final I
- Reactive state: mode (ValueNotifier<WindowMode>), isAlwaysOnTop, isMaximized, isResizing
- Guards: _togglingFullscreen, _closing, _initCompleter, _closeCompleter, _persistInFlight
- Lifecycle: init() -> ensureInitialized -> waitUntilReadyToShow -> _prepareFramelessFirstFrame -> show -> focus
- WindowControls: minimize(), toggleMaximize(), close(), startDragging()
- Fullscreen: toggleFullscreen() with reentry guard, manual setSize+setPosition (NOT setFullScreen)
- Persistence: _schedulePersist() with 500ms debounce, _persistWindowState() with concurrent guard
- Bounds: _clampToVisibleBounds() using PlatformDispatcher

From D:\player_flutter\lib\kernel\services\platform_service.dart:
- enum WindowMode { windowed, fullscreen }
- Extended abstract class with: minimize, toggleMaximize, close, startDragging, toggleFullscreen, exitFullscreen, toggleAlwaysOnTop
- Reactive getters: mode, isAlwaysOnTop, isMaximized, isResizing

From D:\player_flutter\lib\kernel\platform\windows_platform_service.dart:
- Delegates all methods to WindowManagerService.I
- initService() calls _wm.init()
- dispose() calls _wm.dispose()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WindowManagerService + extend PlatformService + add tokens</name>
  <files>
    lib/kernel/window/window_manager_service.dart,
    lib/kernel/services/platform_service.dart,
    lib/kernel/platform/windows_platform_service.dart,
    lib/kernel/ui/theme/tokens.dart,
    lib/main.dart
  </files>
  <read_first>
    D:\player_flutter\lib\kernel\window\window_manager_service.dart,
    D:\player_flutter\lib\kernel\services\platform_service.dart,
    D:\player_flutter\lib\kernel\platform\windows_platform_service.dart,
    D:\simple_player_flutter\lib\kernel\services\platform_service.dart,
    D:\simple_player_flutter\lib\kernel\platform\windows_platform_service.dart,
    D:\simple_player_flutter\lib\kernel\ui\theme\tokens.dart,
    D:\simple_player_flutter\lib\main.dart,
    D:\simple_player_flutter\lib\kernel\persistence\settings_store.dart
  </read_first>
  <behavior>
    - WindowManagerService singleton: init() restores persisted state, sets frameless, shows window
    - WindowManagerService: toggleFullscreen uses manual setSize+setPosition (NOT setFullScreen)
    - WindowManagerService: _togglingFullscreen guard prevents rapid F11 ABA corruption
    - WindowManagerService: 500ms debounce on persist, _persistInFlight prevents concurrent writes
    - WindowManagerService: _clampToVisibleBounds centers off-screen windows
    - WindowManagerService: minSize = Size(640, 360)
    - WindowManagerService: toggleAlwaysOnTop persists via SettingsStore
    - WindowManagerService: _prepareFramelessFirstFrame calls setAsFrameless + setHasShadow + forceRedraw
    - PlatformService: adds WindowMode enum, window control methods, reactive state getters
    - WindowsPlatformService: delegates all new methods to WindowManagerService.I
    - tokens.dart: adds titleBarHeight=36.0, titleBarButtonWidth=36.0, titleBarBg, titleBarBorder, titleBarHover, closeHoverBg
    - main.dart: PlatformService.init(WindowsPlatformService()) unchanged (WindowsPlatformService now delegates to WindowManagerService)
  </behavior>
  <action>
    1. Create `lib/kernel/window/window_manager_service.dart`:
       - Port from `D:\player_flutter\lib\kernel\window\window_manager_service.dart` (516 lines)
       - Singleton: `WindowManagerService._()` + `static final WindowManagerService I = WindowManagerService._();`
       - Reactive state: `final mode = ValueNotifier<WindowMode>(WindowMode.windowed);`, `isAlwaysOnTop`, `isMaximized`, `isResizing`
       - Constants: `minSize = Size(640, 360)`, `_persistDebounceMs = 500`, `_resizeDebounceMs = 500`
       - MethodChannel: `_redrawChannel = MethodChannel('com.simple_player/redraw')`
       - Guards: `_togglingFullscreen`, `_closing`, `_initCompleter`, `_closeCompleter`, `_persistInFlight`
       - `init()`: ensureInitialized -> load settings -> WindowOptions(size, minSize, center, backgroundColor: Colors.black, titleBarStyle: TitleBarStyle.hidden, windowButtonVisibility: false) -> waitUntilReadyToShow -> setPosition -> _clampToVisibleBounds -> maximize if saved -> setAlwaysOnTop if saved -> setPreventClose(true) -> _prepareFramelessFirstFrame -> show -> focus -> restore fullscreen if saved -> addListener -> isMaximized sync
       - `_prepareFramelessFirstFrame()`: setAsFrameless -> setHasShadow(true) -> noop setSize to force WM_SIZE -> forceRedraw via MethodChannel
       - `dispose()`: _disposed=true -> cancel timers -> wait init completer (5s timeout) -> removeListener -> _persistWindowState -> wait close completer (5s timeout)
       - `minimize()`, `toggleMaximize()`, `close()`, `startDragging()`: try-catch wrappers around windowManager calls
       - `toggleFullscreen()`: if _togglingFullscreen or _disposed return; _togglingFullscreen=true; if fullscreen -> _exitFullscreenInternal else cache windowed size/position -> mode.value=fullscreen -> setHasShadow(false) -> setPosition(Offset.zero) -> setSize(screenW, screenH) -> saveIsFullscreen(true); rollback on error; finally _togglingFullscreen=false
       - `_exitFullscreenInternal()`: mode.value=windowed -> setSize(_windowedSize) -> setPosition(_windowedPosition) -> setHasShadow(true) -> saveIsFullscreen(false); rollback on error
       - `toggleAlwaysOnTop()`: next=!isAlwaysOnTop.value -> setAlwaysOnTop(next) -> isAlwaysOnTop.value=next -> saveIsAlwaysOnTop(next)
       - WindowListener callbacks: onWindowMaximize/Unmaximize -> update isMaximized + persist; onWindowEnterFullScreen/LeaveFullScreen -> update mode; onWindowResize -> isResizing=true; onWindowResized -> _schedulePersist + 500ms debounce isResizing=false; onWindowMoved -> _schedulePersist; onWindowClose -> _closing guard -> _persistWindowState -> destroy
       - `_schedulePersist()`: cancel timer -> new Timer(500ms, _persistWindowState)
       - `_persistWindowState()`: _persistInFlight guard -> Future.wait[getSize, getPosition, isMaximized, isFullScreen] -> if not fullscreen update cache + saveWindowGeometry else use cache; complete completer in finally
       - `_clampToVisibleBounds()`: PlatformDispatcher views.first -> screenW/H -> check if off-screen (minVisible=100) -> center() if off-screen

    2. Extend `lib/kernel/services/platform_service.dart`:
       - Add `enum WindowMode { windowed, fullscreen }` at top of file
       - Add abstract methods: `minimize()`, `toggleMaximize()`, `close()`, `startDragging()`, `toggleFullscreen()`, `exitFullscreen()`, `toggleAlwaysOnTop()`
       - Add reactive state getters: `ValueNotifier<WindowMode> get mode`, `ValueNotifier<bool> get isAlwaysOnTop`, `ValueNotifier<bool> get isMaximized`, `ValueNotifier<bool> get isResizing`

    3. Update `lib/kernel/platform/windows_platform_service.dart`:
       - Import `window_manager_service.dart`
       - Add `final WindowManagerService _wm = WindowManagerService.I;`
       - Delegate all new methods to `_wm.*`
       - `initService()` -> `_wm.init()`
       - `dispose()` -> `_wm.dispose()`
       - Reactive getters: `mode` -> `_wm.mode`, `isAlwaysOnTop` -> `_wm.isAlwaysOnTop`, etc.

    4. Add title bar tokens to `lib/kernel/ui/theme/tokens.dart`:
       - `static const titleBarHeight = 36.0;`
       - `static const titleBarButtonWidth = 36.0;` (per D-04: 36x36px)
       - `static const titleBarBg = Color(0xE61A1A24);` (90% opacity bgPanel)
       - `static const titleBarBorder = Color(0x33FFFFFF);` (reuse borderHighlight)
       - `static const titleBarHover = Color(0x1AFFFFFF);` (10% white overlay)
       - `static const closeHoverBg = Color(0xFFC42B1C);` (Win11 close red -- D-01 says follow system, but fallback color needed)

    5. Update `lib/main.dart`:
       - No changes needed -- WindowsPlatformService() constructor now internally accesses WindowManagerService.I
       - Verify the initialization order: SharedPreferences -> SettingsStore.prewarm -> PlatformService.init(WindowsPlatformService()) -> runApp
  </action>
  <verify>
    <automated>cd "D:\simple_player_flutter" && dart analyze lib/kernel/window/window_manager_service.dart lib/kernel/services/platform_service.dart lib/kernel/platform/windows_platform_service.dart lib/kernel/ui/theme/tokens.dart lib/main.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/kernel/window/window_manager_service.dart exists and contains `class WindowManagerService`
    - lib/kernel/window/window_manager_service.dart contains `static final WindowManagerService I = WindowManagerService._()`
    - lib/kernel/window/window_manager_service.dart contains `ValueNotifier<WindowMode> mode`
    - lib/kernel/window/window_manager_service.dart contains `_togglingFullscreen` (reentry guard)
    - lib/kernel/window/window_manager_service.dart contains `_persistDebounceMs` set to 500
    - lib/kernel/window/window_manager_service.dart contains `_prepareFramelessFirstFrame`
    - lib/kernel/window/window_manager_service.dart contains `forceRedraw` MethodChannel call
    - lib/kernel/window/window_manager_service.dart contains `_clampToVisibleBounds`
    - lib/kernel/window/window_manager_service.dart contains `minSize` set to `Size(640, 360)`
    - lib/kernel/window/window_manager_service.dart does NOT contain `setFullScreen` (uses manual setSize+setPosition)
    - lib/kernel/services/platform_service.dart contains `enum WindowMode { windowed, fullscreen }`
    - lib/kernel/services/platform_service.dart contains `ValueNotifier<WindowMode> get mode`
    - lib/kernel/services/platform_service.dart contains `Future<void> minimize()`
    - lib/kernel/services/platform_service.dart contains `Future<void> toggleFullscreen()`
    - lib/kernel/platform/windows_platform_service.dart contains `WindowManagerService.I`
    - lib/kernel/ui/theme/tokens.dart contains `titleBarHeight = 36.0`
    - lib/kernel/ui/theme/tokens.dart contains `titleBarButtonWidth = 36.0`
    - lib/kernel/ui/theme/tokens.dart contains `titleBarBg`
    - lib/kernel/ui/theme/tokens.dart contains `closeHoverBg`
    - dart analyze exits 0 on all modified files
  </acceptance_criteria>
  <done>
    WindowManagerService singleton exists with all window management (frameless, fullscreen with reentry guard, 500ms debounce persistence, bounds check, always-on-top, min size 640x360). PlatformService extended with WindowMode enum and window control methods. WindowsPlatformService delegates to WindowManagerService. Title bar tokens added. dart analyze passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart -> Native (MethodChannel) | forceRedraw and aspect_ratio channels cross to C++ runner |
| Window events -> Dart | WindowListener callbacks from window_manager package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-01-01 | Tampering | SettingsStore persistence | mitigate | _sanitizeDimension/_sanitizeCoordinate already validate inputs in SettingsStore |
| T-01-02 | Spoofing | WindowListener callbacks | accept | window_manager package is trusted, callbacks come from OS |
| T-01-03 | Denial of Service | Rapid F11 toggling | mitigate | _togglingFullscreen reentry guard prevents ABA state corruption |
</threat_model>

<verification>
- `dart analyze lib/kernel/window/ lib/kernel/services/platform_service.dart lib/kernel/platform/windows_platform_service.dart lib/kernel/ui/theme/tokens.dart lib/main.dart` exits 0
- WindowManagerService singleton pattern matches reference project
- PlatformService interface has all window control methods
- WindowsPlatformService delegates to WindowManagerService
- tokens.dart has title bar tokens
</verification>

<success_criteria>
- WindowManagerService exists with frameless init, fullscreen toggle (manual, not setFullScreen), reentry guard, 500ms debounce persistence, bounds check, always-on-top, min size
- PlatformService extended with WindowMode enum and 7 window control methods + 4 reactive state getters
- WindowsPlatformService delegates all new methods to WindowManagerService
- tokens.dart has 6 new title bar tokens
- dart analyze passes on all files
</success_criteria>

<output>
After completion, create `.planning/phases/01-window-shell/01-01-SUMMARY.md`
</output>
