---
phase: 23-ui-layer-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/dialogs/settings/equalizer_tab.dart
  - lib/ui/dialogs/settings/audio_tab.dart
  - lib/ui/dialogs/settings/video_tab.dart
  - lib/ui/dialogs/settings/settings_tab_performance.dart
  - lib/ui/dialogs/media_info_dialog.dart
  - lib/ui/dialogs/settings_panel.dart
autonomous: true
requirements:
  - DOC-33
  - DOC-34
  - DOC-35
  - DOC-36
  - DOC-37
must_haves:
  truths:
    - "Every Dialogs layer tab file has a module-level overview comment"
    - "equalizer_tab.dart has complete FFmpeg filter syntax explanations (filter chain, dB units, multi-filter combination)"
    - "All settings tabs explain each parameter's technical details (range, default, meaning)"
    - "media_info_dialog.dart explains every media info field (codec, resolution, bitrate, etc.)"
    - "settings_panel.dart has navigation pattern and tab function overview comments"
    - "flutter analyze produces no new warnings or errors"
  artifacts:
    - lib/ui/dialogs/settings/equalizer_tab.dart
    - lib/ui/dialogs/settings/audio_tab.dart
    - lib/ui/dialogs/settings/video_tab.dart
    - lib/ui/dialogs/settings/settings_tab_performance.dart
    - lib/ui/dialogs/media_info_dialog.dart
    - lib/ui/dialogs/settings_panel.dart
  key_links:
    - "equalizer_tab.dart uses EngineState.setProperty('af', ...) — comments must explain MDK audio filter property"
    - "video_tab.dart uses VideoProcessingService — comments must explain color correction API"
    - "settings_tab_performance.dart D3D11 sync toggle — comments must explain sync.cpu meaning"
    - "settings_panel.dart is the main entry point — comments must explain sidebar navigation pattern"
---

<objective>
Add or improve documentation comments on 5 Dialogs layer files + 1 settings_panel.dart (DOC-33~DOC-37 + D-42).

Purpose: Enable new developers to understand each settings tab's parameters, FFmpeg filter syntax, and media info fields without running the code. Equalizer tab needs detailed FFmpeg domain knowledge per D-09~D-21.

Output: 6 .dart files with complete class-level doc comments, parameter explanations, and FFmpeg filter syntax documentation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ui-layer-documentation/23-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document equalizer_tab.dart (A-class, FFmpeg filters)</name>
  <files>
    lib/ui/dialogs/settings/equalizer_tab.dart
  </files>
  <read_first>
    lib/ui/dialogs/settings/equalizer_tab.dart,
    .planning/phases/23-ui-layer-documentation/23-CONTEXT.md
  </read_first>
  <action>
    This is the highest-priority file. Add comprehensive FFmpeg filter documentation per D-09~D-21.

    **Module-level overview (D-21):**
    - Add file header comment explaining this is the equalizer settings tab
    - Explain the audio filter chain system: settings are applied via `EngineState.setProperty('af', filterString)` which sets MDK's `af` (audio filter) property

    **FFmpeg filter chain explanation (D-13, D-14):**
    - Before the filter construction code, add a block comment explaining the filter chain format:
      - `filter_name=param1=value1,param2=value2` — FFmpeg equalizer syntax
      - Multiple filters separated by commas: `equalizer=f=100:t=q:w=1:g=5,equalizer=f=1000:t=q:w=1:g=-3`
      - Empty string `''` disables equalizer (original audio passes through) per D-17

    **Preset documentation (D-10):**
    - Each preset string gets an inline comment explaining the listening goal:
      - 'Flat': 无增益，原始音频直通
      - 'Bass Boost': 增强低频（60-250Hz），适合流行/嘻哈音乐
      - 'Treble Boost': 增强高频（2kHz-16kHz），适合古典/人声突出
      - 'Rock': 低频增强+中频微调，摇滚乐典型频响曲线
      - 'Pop': 中频突出，人声清晰度优先
      - 'Jazz': 低频柔和+高频延伸，爵士乐温暖感
      - 'Classical': 平坦中频+高频细节，交响乐动态范围保留

    **Parameter explanations (D-15):**
    - Add inline comments explaining dB units: // 增益单位 dB（分贝），正数增强、负数衰减
    - Add gain range warning per D-20: // 增益范围建议 -20dB ~ +20dB，过高可能导致削波失真

    **Real-time switching (D-19):**
    - Add comment explaining filter chain hot-swap: // 滤镜链热切换，不需要重新打开文件，MDK 实时重新初始化音频滤镜图

    **Modification guide (D-18):**
    - Add comment block explaining how to add new presets: copy an existing preset string, adjust equalizer parameters (f=frequency, t=type, w=width, g=gain)

    **Language rules (D-01~D-03):**
    - `///` doc comments: English
    - `//` inline why-explanations: Chinese
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -c '//' lib/ui/dialogs/settings/equalizer_tab.dart</automated>
  </verify>
  <acceptance_criteria>
    - Module-level overview comment exists at file top
    - Filter chain format explanation block comment exists (mentions `filter_name=param1=value1`)
    - Every preset has inline Chinese comment explaining listening goal
    - dB units explanation exists (mentions 分贝 or dB)
    - Gain range warning exists (mentions -20dB or 削波)
    - Real-time switching explanation exists (mentions 热切换)
    - Modification guide comment block exists
    - `///` doc comments are in English (D-01)
    - `//` why-explanations are in Chinese (D-03)
    - `flutter analyze` shows no new warnings
  </acceptance_criteria>
  <done>equalizer_tab.dart has complete FFmpeg filter syntax documentation, preset explanations, dB unit explanations, and modification guide</done>
</task>

<task type="auto">
  <name>Task 2: Document audio_tab + video_tab + performance_tab + media_info_dialog (5 files)</name>
  <files>
    lib/ui/dialogs/settings/audio_tab.dart,
    lib/ui/dialogs/settings/video_tab.dart,
    lib/ui/dialogs/settings/settings_tab_performance.dart,
    lib/ui/dialogs/media_info_dialog.dart,
    lib/ui/dialogs/settings_panel.dart
  </files>
  <read_first>
    lib/ui/dialogs/settings/audio_tab.dart,
    lib/ui/dialogs/settings/video_tab.dart,
    lib/ui/dialogs/settings/settings_tab_performance.dart,
    lib/ui/dialogs/media_info_dialog.dart,
    lib/ui/dialogs/settings_panel.dart,
    .planning/phases/23-ui-layer-documentation/23-CONTEXT.md
  </read_first>
  <action>
    Audit each file's existing comments, then supplement per decisions D-22~D-44.

    **audio_tab.dart (DOC-34):**
    - Add module-level overview (D-28): explain this tab manages audio track selection and audio delay
    - Add `///` doc comment to the widget class explaining purpose
    - Explain track selection logic (D-26): how available audio tracks are listed via EngineState, switching via trackManager.switchAudioTrack(index)
    - Explain audio delay parameter: positive = delay audio relative to video, negative = advance audio (lip-sync correction)
    - Add magic number explanations if any numeric constants exist

    **video_tab.dart (DOC-35):**
    - Add module-level overview (D-28): explain this tab manages video effects (brightness, contrast, saturation, hue, rotation, aspect ratio, deinterlace)
    - Add `///` doc comment to the widget class explaining purpose
    - Explain each color correction parameter (D-23):
      - brightness: 亮度调整，范围 -1.0 到 1.0，0.0 = 无变化
      - contrast: 对比度调整，范围 -1.0 到 1.0，0.0 = 无变化
      - saturation: 饱和度调整，范围 -1.0 到 1.0，0.0 = 无变化
      - hue: 色调调整，范围 -180 到 180 度，0.0 = 无变化
    - Explain rotation: only 0/90/180/270 degrees (hardware rotation steps)
    - Explain aspect ratio modes via AspectRatioMode enum
    - Explain deinterlace toggle and yadif filter

    **settings_tab_performance.dart (DOC-36):**
    - Add module-level overview (D-28): explain this tab manages D3D11 sync and hardware decoding
    - Add `///` doc comment to the widget class explaining purpose
    - Explain D3D11 sync toggle (D-24): // sync.cpu 强制 CPU 同步，避免 D3D11 异步拷贝导致撕裂，性能换稳定性
    - Explain hardware decoding toggle (D-25): // 硬件解码（D3D11/NVDEC）降低 CPU 使用率，但可能有兼容性/驱动问题

    **media_info_dialog.dart (DOC-37):**
    - Add module-level overview: explain this dialog displays technical metadata of the current media file
    - Add `///` doc comment to the widget class explaining purpose
    - Explain each media info field (D-27):
      - codec: 编解码器名称（如 H.264, HEVC, VP9）
      - resolution: 视频分辨率（如 1920x1080）
      - bitrate: 码率（单位 kbps）
      - frameRate: 帧率（单位 fps）
      - pixelFormat: 像素格式（如 yuv420p, nv12）
      - colorSpace: 色彩空间（如 bt709, bt2020）
    - Explain how MediaInfo is obtained from EngineState

    **settings_panel.dart (D-42, not in DOC range):**
    - Add module-level overview: explain sidebar navigation pattern
    - Explain tab switching logic: sidebar items map to tab content via index
    - Add brief description of each tab's function (General, Audio, Video, Equalizer, Shortcuts, About, Performance)

    **Language rules (D-01~D-03):**
    - `///` doc comments: English
    - `//` inline why-explanations: Chinese
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -c '///' lib/ui/dialogs/settings/audio_tab.dart lib/ui/dialogs/settings/video_tab.dart lib/ui/dialogs/settings/settings_tab_performance.dart lib/ui/dialogs/media_info_dialog.dart lib/ui/dialogs/settings_panel.dart | grep -v ':0$'</automated>
  </verify>
  <acceptance_criteria>
    - audio_tab.dart: module-level overview exists; track selection logic explained; audio delay parameter explained
    - video_tab.dart: module-level overview exists; each color correction parameter (brightness/contrast/saturation/hue) has Chinese inline comment with range; rotation limit (0/90/180/270) explained
    - settings_tab_performance.dart: module-level overview exists; D3D11 sync.cpu explanation exists (mentions CPU 同步 or 撕裂); hardware decoding pros/cons explained
    - media_info_dialog.dart: module-level overview exists; codec/resolution/bitrate/frameRate/pixelFormat/colorSpace fields have Chinese inline comments
    - settings_panel.dart: module-level overview exists; sidebar navigation pattern explained; each tab has brief function description
    - All `///` doc comments are in English (D-01)
    - All `//` why-explanations are in Chinese (D-03)
    - `flutter analyze` shows no new warnings
  </acceptance_criteria>
  <done>5 Dialogs files have complete parameter explanations, FFmpeg references, and media info field documentation; settings_panel.dart has navigation pattern explanation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Documentation-only changes — no new inputs, outputs, or control flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | N/A | All files | none | accept | Documentation-only changes introduce no new attack surface. No code behavior modification. |
</threat_model>

<verification>
1. `flutter analyze` — no new warnings or errors introduced
2. Grep check: equalizer_tab.dart has inline comment mentioning "ffmpeg" or "滤镜" or "equalizer"
3. Grep check: video_tab.dart has inline comment mentioning "brightness" or "亮度" or "-1.0"
4. Grep check: settings_tab_performance.dart has inline comment mentioning "sync" or "D3D11"
5. Grep check: media_info_dialog.dart has inline comment mentioning "codec" or "编解码" or "分辨率"
6. Spot check: settings_panel.dart has module-level overview explaining sidebar navigation
</verification>

<success_criteria>
- equalizer_tab.dart: complete FFmpeg filter syntax docs, preset explanations, dB units, gain range warnings
- audio_tab.dart: track selection logic and audio delay parameter explained
- video_tab.dart: all color correction parameters documented with ranges
- settings_tab_performance.dart: D3D11 sync and hardware decoding explained
- media_info_dialog.dart: all media info fields documented
- settings_panel.dart: sidebar navigation pattern and tab functions explained
- All `///` doc comments are in English (D-01)
- All `//` why-explanations are in Chinese (D-03)
- `flutter analyze` produces no new warnings or errors
</success_criteria>

<output>
Create `.planning/phases/23-ui-layer-documentation/23-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Description |
|--------|------|-------------|
| (documentation comments only — no new symbols) | All 6 files | Module-level overviews, class/method doc comments, inline FFmpeg/parameter explanations |
