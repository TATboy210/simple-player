---
phase: 23-ui-layer-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/player/drop_handler.dart
  - lib/ui/player/player_actions.dart
  - lib/ui/player/error_banner.dart
  - lib/ui/player/time_range_display.dart
  - lib/ui/shared/app_dialog.dart
  - lib/ui/shared/context_menu_row.dart
  - lib/ui/shared/merged_listenable.dart
  - lib/ui/shared/splash_screen.dart
autonomous: true
requirements:
  - DOC-38
  - DOC-39
  - DOC-40
  - DOC-41
  - DOC-42
  - DOC-43
  - DOC-44
  - DOC-45
must_haves:
  truths:
    - "Every Player layer file has a module-level overview comment explaining interaction logic"
    - "drop_handler.dart explains desktop_drop platform channel mechanism and file type filtering"
    - "player_actions.dart explains callback bundle design intent"
    - "merged_listenable.dart explains ValueNotifier merge principle and generic reusability"
    - "Every Shared component has usage scenario and pattern explanation"
    - "flutter analyze produces no new warnings or errors"
  artifacts:
    - lib/ui/player/drop_handler.dart
    - lib/ui/player/player_actions.dart
    - lib/ui/player/error_banner.dart
    - lib/ui/player/time_range_display.dart
    - lib/ui/shared/app_dialog.dart
    - lib/ui/shared/context_menu_row.dart
    - lib/ui/shared/merged_listenable.dart
    - lib/ui/shared/splash_screen.dart
  key_links:
    - "drop_handler.dart uses desktop_drop package — comments must explain why not Flutter native DragTarget"
    - "player_actions.dart is the callback bundle used by all player widgets — comments must explain design intent"
    - "merged_listenable.dart is generic — comments must emphasize reusability beyond time display"
    - "app_dialog.dart uses LayoutBuilder for responsive sizing — comments must explain breakpoint logic"
---

<objective>
Add or improve documentation comments on 4 Player layer files + 4 Shared layer files (DOC-38~DOC-45).

Purpose: Enable new developers to understand drag-and-drop mechanics, callback bundle pattern, error state machine, ValueNotifier merging, and dialog responsive design without running the code.

Output: 8 .dart files with complete class-level doc comments, interaction logic explanations, and design pattern documentation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ui-layer-documentation/23-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Player layer files (4 files)</name>
  <files>
    lib/ui/player/drop_handler.dart,
    lib/ui/player/player_actions.dart,
    lib/ui/player/error_banner.dart,
    lib/ui/player/time_range_display.dart
  </files>
  <read_first>
    lib/ui/player/drop_handler.dart,
    lib/ui/player/player_actions.dart,
    lib/ui/player/error_banner.dart,
    lib/ui/player/time_range_display.dart,
    .planning/phases/23-ui-layer-documentation/23-CONTEXT.md
  </read_first>
  <action>
    Audit each file's existing comments, then supplement per decisions D-29~D-35.

    **drop_handler.dart (DOC-38):**
    - Add module-level overview (D-35): explain this widget handles OS-level file drag-and-drop on desktop
    - Explain desktop_drop technology choice (D-29, D-34): // desktop_drop 包通过平台通道监听 OS 级 Drop 事件 — Flutter 原生 DragTarget 只支持应用内拖放，不支持从文件管理器拖入
    - Explain file type filtering (D-33): // PathValidator 过滤逻辑：路径长度检查、空字节检查、合法字符验证，防止恶意路径注入
    - Add `///` doc comment to the widget class and any public methods
    - Explain the callback chain: drop event → file path extraction → PathValidator validation → onFileDropped callback

    **player_actions.dart (DOC-39):**
    - Add module-level overview (D-35): explain this is a callback bundle class that aggregates all UI-to-controller actions
    - Explain design intent (D-30): // 替代散落的回调参数，简化 PlayerScreen 等上层组件的构造函数 — 将所有动作集中到一个对象中传递
    - Add `///` doc comment to each callback field explaining when it's triggered:
      - onPlayPause: 空格键或控制栏播放/暂停按钮
      - onPrevious/onNext: N/P 键或控制栏上/下一曲按钮
      - onSeek: 进度条拖拽或左右箭头键
      - onVolumeChange: 上下箭头键或音量滑块
      - onToggleFullscreen: F 键或双击
      - onToggleMute: M 键或音量图标点击
      - onOpenFile: O 键或菜单
      - onTogglePlaylist: 播放列表按钮
    - Note that all callbacks are optional (VoidCallback?) — widgets check null before calling

    **error_banner.dart (DOC-40):**
    - Add module-level overview (D-35): explain this widget displays error state overlay when playback fails
    - Explain display conditions (D-31): // 显示条件：MediaState.error 且 errorMessage != null — 只在有明确错误信息时显示
    - Explain interaction buttons: retry (重试打开文件) and open file (打开新文件)
    - Add `///` doc comment to the widget class

    **time_range_display.dart (DOC-41):**
    - Add module-level overview (D-35): explain this widget shows current position / total duration
    - Explain MergedListenable usage (D-32): // 使用 MergedListenable 合并 position 和 duration 两个 ValueNotifier — 避免分别监听导致多次 rebuild
    - Add `///` doc comment to the widget class
    - Note the TimePair data structure (merged position + duration)

    **Language rules (D-01~D-03):**
    - `///` doc comments: English
    - `//` inline why-explanations: Chinese
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -c '///' lib/ui/player/drop_handler.dart lib/ui/player/player_actions.dart lib/ui/player/error_banner.dart lib/ui/player/time_range_display.dart | grep -v ':0$'</automated>
  </verify>
  <acceptance_criteria>
    - drop_handler.dart: module-level overview exists; desktop_drop explanation exists (mentions 平台通道 or OS 级 or DragTarget); file type filtering logic explained
    - player_actions.dart: module-level overview exists; design intent explanation exists (mentions 替代散落回调 or 简化构造函数); each callback field has doc comment
    - error_banner.dart: module-level overview exists; display condition explanation exists (mentions MediaState.error)
    - time_range_display.dart: module-level overview exists; MergedListenable usage explanation exists (mentions 避免多次 rebuild)
    - All `///` doc comments are in English (D-01)
    - All `//` why-explanations are in Chinese (D-03)
    - `flutter analyze` shows no new warnings
  </acceptance_criteria>
  <done>4 Player layer files have complete interaction logic documentation, desktop_drop explanation, callback bundle design intent, and error state machine docs</done>
</task>

<task type="auto">
  <name>Task 2: Document Shared layer files (4 files)</name>
  <files>
    lib/ui/shared/app_dialog.dart,
    lib/ui/shared/context_menu_row.dart,
    lib/ui/shared/merged_listenable.dart,
    lib/ui/shared/splash_screen.dart
  </files>
  <read_first>
    lib/ui/shared/app_dialog.dart,
    lib/ui/shared/context_menu_row.dart,
    lib/ui/shared/merged_listenable.dart,
    lib/ui/shared/splash_screen.dart,
    .planning/phases/23-ui-layer-documentation/23-CONTEXT.md
  </read_first>
  <action>
    Audit each file's existing comments, then supplement per decisions D-36~D-44.

    **app_dialog.dart (DOC-42):**
    - Add module-level overview (D-41): explain this is the base dialog widget used by all settings/info dialogs
    - Explain responsive design (D-38, D-39): // LayoutBuilder 根据屏幕宽度调整尺寸 — 宽屏使用指定尺寸，窄屏自适应宽度
    - Explain visual spec (D-38): // 使用 bgElevated 背景色 + radiusLarge 圆角，与全局设计系统一致
    - Add `///` doc comment to the widget class explaining constructor parameters
    - Explain the LayoutBuilder breakpoint logic: above threshold uses fixed width, below uses screen fraction

    **context_menu_row.dart (DOC-43):**
    - Add module-level overview (D-41): explain this is a shared right-click menu row component
    - Explain extraction source (D-40): // 从 folder_tab 和 thumbnail_tile 提取的共享组件 — 统一右键菜单样式和交互
    - Add `///` doc comment to the widget class
    - Explain the icon + label + action pattern

    **merged_listenable.dart (DOC-44):**
    - Add module-level overview (D-41): explain this merges two ValueNotifiers into a single listenable
    - Explain merge principle (D-36): // 监听两个 ValueNotifier，任一变化时触发 rebuild — 合并为一个 Listenable 避免嵌套 ValueListenableBuilder
    - Explain generic reusability (D-37): // 通用设计：可复用于任何两个 ValueNotifier<int>，不限于 position/duration 时间显示
    - Add `///` doc comment to the class explaining type parameters and usage
    - Note the TimePair typedef if present, or explain the generic pair mechanism

    **splash_screen.dart (DOC-45):**
    - Add module-level overview (D-41): explain this is the startup splash/loading screen
    - Add `///` doc comment to the widget class
    - Explain animation logic if present (fade-in, progress indicator)
    - Explain when it's shown (app startup, before player ready)

    **Phase 20 cleanup notes (D-44):**
    - If any file has references to Phase 20 changes (EdgeGlow gradient-only, breakpoint reduction), add inline comment explaining the design intent

    **Language rules (D-01~D-03):**
    - `///` doc comments: English
    - `//` inline why-explanations: Chinese
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -c '///' lib/ui/shared/app_dialog.dart lib/ui/shared/context_menu_row.dart lib/ui/shared/merged_listenable.dart lib/ui/shared/splash_screen.dart | grep -v ':0$'</automated>
  </verify>
  <acceptance_criteria>
    - app_dialog.dart: module-level overview exists; LayoutBuilder responsive explanation exists (mentions 宽屏 or 窄屏 or 自适应); visual spec explanation exists (mentions bgElevated or radiusLarge)
    - context_menu_row.dart: module-level overview exists; extraction source explanation exists (mentions folder_tab or thumbnail_tile or 共享组件)
    - merged_listenable.dart: module-level overview exists; merge principle explanation exists (mentions 避免嵌套 or 合并); generic reusability note exists (mentions 可复用 or 通用)
    - splash_screen.dart: module-level overview exists; widget class has doc comment
    - All `///` doc comments are in English (D-01)
    - All `//` why-explanations are in Chinese (D-03)
    - `flutter analyze` shows no new warnings
  </acceptance_criteria>
  <done>4 Shared layer files have complete usage scenario documentation, responsive design explanation, ValueNotifier merge principle, and component extraction notes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Documentation-only changes — no new inputs, outputs, or control flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-02 | N/A | All files | none | accept | Documentation-only changes introduce no new attack surface. No code behavior modification. |
</threat_model>

<verification>
1. `flutter analyze` — no new warnings or errors introduced
2. Grep check: drop_handler.dart has inline comment mentioning "desktop_drop" or "拖放" or "DragTarget"
3. Grep check: player_actions.dart has inline comment mentioning "回调" or "callback"
4. Grep check: merged_listenable.dart has inline comment mentioning "合并" or "merge" or "ValueNotifier"
5. Grep check: app_dialog.dart has inline comment mentioning "LayoutBuilder" or "响应式" or "自适应"
6. Spot check: context_menu_row.dart has inline comment mentioning "提取" or "共享"
</verification>

<success_criteria>
- drop_handler.dart: desktop_drop mechanism, file type filtering, and platform channel explanation
- player_actions.dart: callback bundle design intent and each field documented
- error_banner.dart: display conditions and interaction buttons documented
- time_range_display.dart: MergedListenable usage rationale documented
- app_dialog.dart: responsive LayoutBuilder design and visual spec documented
- context_menu_row.dart: component extraction source and usage scenarios documented
- merged_listenable.dart: merge principle and generic reusability documented
- splash_screen.dart: startup splash purpose and animation logic documented
- All `///` doc comments are in English (D-01)
- All `//` why-explanations are in Chinese (D-03)
- `flutter analyze` produces no new warnings or errors
</success_criteria>

<output>
Create `.planning/phases/23-ui-layer-documentation/23-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Description |
|--------|------|-------------|
| (documentation comments only — no new symbols) | All 8 files | Module-level overviews, class/method doc comments, inline interaction logic and design pattern explanations |
