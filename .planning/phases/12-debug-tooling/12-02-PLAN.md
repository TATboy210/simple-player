---
phase: 12-debug-tooling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/engine/fvp_engine.dart
  - lib/kernel/bridge/window_service.dart
autonomous: true
requirements:
  - DBG-01

must_haves:
  truths:
    - "FvpEngine.open() emits Timeline.startSync/finishSync events visible in DevTools"
    - "FvpEngine.seekTo() emits Timeline.startSync/finishSync events visible in DevTools"
    - "WindowService._enterFullscreen() emits Timeline.startSync/finishSync events"
    - "WindowService._exitFullscreen() emits Timeline.startSync/finishSync events"
    - "Timeline.finishSync() is always called even when exceptions occur (try/finally)"
  artifacts:
    - path: "lib/kernel/engine/fvp_engine.dart"
      provides: "Timeline tracing on open() and seekTo()"
      contains: "Timeline.startSync"
    - path: "lib/kernel/bridge/window_service.dart"
      provides: "Timeline tracing on _enterFullscreen() and _exitFullscreen()"
      contains: "Timeline.startSync"
  key_links:
    - from: "lib/kernel/engine/fvp_engine.dart"
      to: "dart:developer"
      via: "import dart:developer for Timeline"
      pattern: "import 'dart:developer'"
    - from: "lib/kernel/bridge/window_service.dart"
      to: "dart:developer"
      via: "import dart:developer for Timeline"
      pattern: "import 'dart:developer'"
---

<objective>
Wrap 4 performance-sensitive async methods with dart:developer Timeline events for DevTools profiling.

Purpose: Enable developers to see open/seek/fullscreen latency in DevTools Performance panel without adding log noise.

Output: Timeline.startSync/finishSync calls in FvpEngine.open(), FvpEngine.seekTo(), WindowService._enterFullscreen(), WindowService._exitFullscreen().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-debug-tooling/12-CONTEXT.md
@.planning/phases/12-debug-tooling/12-RESEARCH.md
@lib/kernel/engine/fvp_engine.dart
@lib/kernel/bridge/window_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Timeline tracing to FvpEngine.open() and seekTo()</name>
  <files>lib/kernel/engine/fvp_engine.dart</files>
  <read_first>
    - lib/kernel/engine/fvp_engine.dart (open() at line 230, seekTo() at line 422 — read method bodies)
    - .planning/phases/12-debug-tooling/12-CONTEXT.md (D-08: track open, D-09: track seekTo, D-11: Timeline.startSync/finishSync)
  </read_first>
  <action>
    In `lib/kernel/engine/fvp_engine.dart`:

    1. Add import: `import 'dart:developer';` at top of file (alongside existing imports)

    2. Wrap `open(String path)` method body (line 230) with Timeline:
       - At method entry: `Timeline.startSync('fvp.open');`
       - Wrap entire existing method body in `try { ... } finally { Timeline.finishSync(); }`
       - Do NOT change any existing logic inside open()

    3. Wrap `seekTo(int milliseconds)` method body (line 422) with Timeline:
       - At method entry: `Timeline.startSync('fvp.seek');`
       - Wrap entire existing method body in `try { ... } finally { Timeline.finishSync(); }`
       - Do NOT change any existing logic inside seekTo()

    Category naming convention: `'fvp.open'`, `'fvp.seek'` — short, dot-separated, prefixed by module
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/engine/fvp_engine.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source: `import 'dart:developer';` exists in fvp_engine.dart
    - Source: `open(` method body starts with `Timeline.startSync('fvp.open');` inside a try block
    - Source: `seekTo(` method body starts with `Timeline.startSync('fvp.seek');` inside a try block
    - Source: both methods have `Timeline.finishSync();` in a `finally` block
    - Behavior: flutter analyze passes with no errors on fvp_engine.dart
  </acceptance_criteria>
  <done>
    FvpEngine.open() and seekTo() wrapped with Timeline.startSync/finishSync in try/finally. flutter analyze passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Timeline tracing to WindowService._enterFullscreen() and _exitFullscreen()</name>
  <files>lib/kernel/bridge/window_service.dart</files>
  <read_first>
    - lib/kernel/bridge/window_service.dart (_enterFullscreen() at line 197, _exitFullscreen() at line 256 — read method bodies)
    - .planning/phases/12-debug-tooling/12-CONTEXT.md (D-10: track fullscreen toggle, D-11: Timeline.startSync/finishSync)
  </read_first>
  <action>
    In `lib/kernel/bridge/window_service.dart`:

    1. Add import: `import 'dart:developer';` at top of file (alongside existing imports)

    2. Wrap `_enterFullscreen()` method body (line 197) with Timeline:
       - At method entry: `Timeline.startSync('window.enterFullscreen');`
       - Wrap entire existing method body in `try { ... } finally { Timeline.finishSync(); }`
       - Do NOT change any existing logic inside _enterFullscreen()

    3. Wrap `_exitFullscreen()` method body (line 256) with Timeline:
       - At method entry: `Timeline.startSync('window.exitFullscreen');`
       - Wrap entire existing method body in `try { ... } finally { Timeline.finishSync(); }`
       - Do NOT change any existing logic inside _exitFullscreen()

    Category naming convention: `'window.enterFullscreen'`, `'window.exitFullscreen'` — descriptive, dot-separated, prefixed by module
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/bridge/window_service.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source: `import 'dart:developer';` exists in window_service.dart
    - Source: `_enterFullscreen(` method body starts with `Timeline.startSync('window.enterFullscreen');` inside a try block
    - Source: `_exitFullscreen(` method body starts with `Timeline.startSync('window.exitFullscreen');` inside a try block
    - Source: both methods have `Timeline.finishSync();` in a `finally` block
    - Behavior: flutter analyze passes with no errors on window_service.dart
  </acceptance_criteria>
  <done>
    WindowService._enterFullscreen() and _exitFullscreen() wrapped with Timeline.startSync/finishSync in try/finally. flutter analyze passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → VM timeline buffer | Timeline events written to Dart VM — no external exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Information Disclosure | Timeline events | accept | Timeline data only accessible via DevTools connected to debug VM; no production exposure |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — dart:developer is SDK built-in |
</threat_model>

<verification>
- `flutter analyze` — no errors on both modified files
- Source: `grep -c "Timeline.startSync" lib/kernel/engine/fvp_engine.dart` returns 2
- Source: `grep -c "Timeline.startSync" lib/kernel/bridge/window_service.dart` returns 2
- Source: `grep -c "Timeline.finishSync" lib/kernel/engine/fvp_engine.dart` returns 2
- Source: `grep -c "Timeline.finishSync" lib/kernel/bridge/window_service.dart` returns 2
- Source: `grep -c "finally" lib/kernel/engine/fvp_engine.dart` >= 2 (existing + 2 new)
- Source: `grep -c "finally" lib/kernel/bridge/window_service.dart` >= 2 (existing + 2 new)
</verification>

<success_criteria>
- 4 methods wrapped with Timeline.startSync/finishSync
- All finishSync calls in finally blocks (no exception leaks)
- flutter analyze passes on both files
- Category names: fvp.open, fvp.seek, window.enterFullscreen, window.exitFullscreen
</success_criteria>

<output>
Create `.planning/phases/12-debug-tooling/12-02-SUMMARY.md` when done
</output>
