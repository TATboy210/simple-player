---
phase: 32-navigation-interaction-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
files_modified:
  - lib/ui/dialogs/settings/option_list_navigation_overlay.dart
  - lib/ui/dialogs/settings/tabs/general_tab.dart
  - test/ui/dialogs/option_list_navigation_overlay_test.dart
autonomous: false
requirements:
  - NAV-05
  - NAV-06
must_haves:
  truths:
    - "The option-list navigation overlay uses Container(color: Tokens.bgGlass) with zero BackdropFilter in its subtree — the structural test traverses the overlay widget subtree and asserts BackdropFilter count equals 0 (NAV-05, D-04)."
    - "When InputModeDetector.arrowGlow is ArrowDirection.up, the top overlay arrow is in the glow state; when ArrowDirection.down, the bottom arrow glows; when null, neither glows — the null state is reachable because Plan 01 setArrowGlow auto-resets to null after the glow duration (NAV-06)."
    - "The full-suite regression gate consumes a pre-committed immutable baseline artifact (32-baseline-failures.txt) generated by 32-01 Task 0 on a clean Phase 31 tree at the execute startpoint (before any Phase 32 lib/ code is introduced); Plan 03 only CONSUMES this artifact — it does not generate it. The gate: (1) asserts the baseline artifact exists and is non-empty, failing closed (exit 1) if missing/empty with a message to run 32-01 Task 0 first; (2) runs flutter test, captures exit status — exit 0 means GATE PASS; (3) parses current [E] failing test identities into a temp set; (4) Phase-32-file exclusion — any Phase 32 test file in the current failures is exit 1 (defense-in-depth 1); (5) comm -23 per-failure membership — any failing test NOT in the baseline artifact is exit 1 (PRIMARY classifier — the gate never exits 0 merely because a filename grep did not match or the mdk.dll signature is present); (6) total-count ceiling >70 is exit 1 (defense-in-depth 2). All passed means GATE PASS. No git stash/worktree is used anywhere."
    - "flutter analyze reports zero warnings on the enumerated set of all 10 Phase 32 production files across Plans 01–03: lib/kernel/services/input_mode_detector.dart, lib/ui/dialogs/settings/panel_key_bindings.dart, lib/ui/theme/tokens.dart, lib/ui/dialogs/settings/tab_arrow_button.dart, lib/ui/dialogs/settings/input_mode_hint.dart, lib/ui/dialogs/settings/tab_strip.dart, lib/ui/dialogs/settings/settings_overlay_shell.dart, lib/ui/dialogs/settings/shortcuts_tab.dart, lib/ui/dialogs/settings/option_list_navigation_overlay.dart, lib/ui/dialogs/settings/tabs/general_tab.dart."
    - "A Windows human checkpoint confirms native keyboard vs Steam Input dual-mode tab navigation (tab-index exactly ±1 per press, hint mode correct, toggle fallback effective) and profile-mode raster A/B (no new blur layer, raster avg increase ≤ 1ms, no jank on drag/scroll)."
  artifacts:
    - path: lib/ui/dialogs/settings/option_list_navigation_overlay.dart
      provides: Translucent top/bottom arrow overlay with glow subscription to InputModeDetector.arrowGlow
    - path: lib/ui/dialogs/settings/tabs/general_tab.dart
      provides: GeneralTab (index 3) SingleChildScrollView wrapped with OptionListNavigationOverlay — the planner-selected scrollable settings list
    - path: test/ui/dialogs/option_list_navigation_overlay_test.dart
      provides: No-second-blur structural assertion and up/down glow state coverage
  key_links:
    - from: lib/ui/dialogs/settings/option_list_navigation_overlay.dart
      to: lib/kernel/services/input_mode_detector.dart
      via: arrowGlow ValueNotifier subscription — up/down arrows glow on key press
      pattern: arrowGlow
    - from: lib/ui/dialogs/settings/tabs/general_tab.dart
      to: lib/ui/dialogs/settings/option_list_navigation_overlay.dart
      via: SingleChildScrollView wrapped in a Stack with the overlay as a sibling
      pattern: OptionListNavigationOverlay
---

<objective>
Mount a thin-glass option-list navigation overlay on the GeneralTab (index 3 — the default tab with a SingleChildScrollView containing discrete language and appearance setting rows), wire its up/down glow to the Plan 01 arrowGlow notifier, run the full regression gate, and obtain the blocking Windows human checkpoint that validates Steam Input dual-mode navigation and profile-mode raster performance.

Purpose: Complete NAV-05 (thin-glass overlay without a second blur) and NAV-06 (up/down glow feedback), and validate the entire phase with automated regression evidence plus the manual Windows checks that headless tests cannot truthfully assert.
Output: A tested option-list overlay, a modified GeneralTab with the overlay mounted, a structural no-second-blur test, a full-suite regression comparison against the pre-committed baseline artifact (generated by 32-01 Task 0), and a human-confirmed Windows dual-mode and raster checkpoint.
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-navigation-interaction-polish/32-RESEARCH.md
@.planning/phases/32-navigation-interaction-polish/32-PLAN-OUTLINE.md
@.planning/phases/32-navigation-interaction-polish/32-01-PLAN.md
@.planning/phases/32-navigation-interaction-polish/32-02-PLAN.md
@.planning/phases/31-visual-design-alignment/VERIFICATION.md
@lib/ui/dialogs/settings/tabs/general_tab.dart
@lib/ui/dialogs/settings/tab_content.dart
@lib/kernel/services/input_mode_detector.dart
@lib/ui/shared/glass_container.dart
</context>

## Planner-selected scrollable settings list (OQ3 resolution)

**Selected:** GeneralTab (index 3 in the IndexedStack).

**Rationale:** GeneralTab is the default tab (defaultTabIndex = 3, D-01 middle position), most commonly viewed. It contains a SingleChildScrollView wrapping an AnimatedSectionList with discrete GlassContainer sections — language SpinControl and appearance switches — that form a natural option list for up/down navigation. It has clear top and bottom boundaries for arrow overlays and is representative of the general settings experience. This choice does not speculatively overlay all seven IndexedStack children (per the approved approach and 32-RESEARCH OQ3 — scope to one shipped list).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OptionListNavigationOverlay + GeneralTab mount + regression gate</name>
  <read_first>
    - .planning/phases/32-navigation-interaction-polish/32-RESEARCH.md (Pattern 4 single blur owner; Pitfall 5 nested blur/readback; Validation Architecture L353+)
    - .planning/phases/31-visual-design-alignment/VERIFICATION.md (L28-34 pre-existing baseline: ~57 mdk.dll + 4 dialog + focus_navigation compile error now repaired)
    - lib/ui/dialogs/settings/tabs/general_tab.dart (L42-79 SingleChildScrollView + AnimatedSectionList structure)
    - lib/kernel/services/input_mode_detector.dart (from Plan 01 — arrowGlow ValueNotifier<ArrowDirection?>)
    - lib/ui/shared/glass_container.dart (L144-168 single BackdropFilter boundary — NAV-05 constraint)
  </read_first>
  <files>lib/ui/dialogs/settings/option_list_navigation_overlay.dart, lib/ui/dialogs/settings/tabs/general_tab.dart, test/ui/dialogs/option_list_navigation_overlay_test.dart</files>
  <behavior>
    - Test 1 (structural): The OptionListNavigationOverlay subtree contains zero BackdropFilter widgets (NAV-05 — traverse all descendants and assert count == 0).
    - Test 2 (glow up): When arrowGlow value is ArrowDirection.up, the top arrow indicator is in the glow visual state (e.g., a glow color or elevated opacity).
    - Test 3 (glow down): When arrowGlow value is ArrowDirection.down, the bottom arrow indicator is in the glow visual state.
    - Test 4 (no glow): When arrowGlow value is null, neither arrow indicator is in the glow state.
    - Test 5 (container): The overlay arrow indicators use Container with color Tokens.bgGlass (not a BackdropFilter or GlassContainer).
  </behavior>
  <action>
    Create lib/ui/dialogs/settings/option_list_navigation_overlay.dart as a StatelessWidget accepting a child widget (the scrollable content to overlay). Build a Stack with the child as the base layer and two Positioned arrow indicators — one at the top edge and one at the bottom edge. Each indicator is a Container(color: Tokens.bgGlass) with an Icon (Icons.arrow_drop_up for top, Icons.arrow_drop_down for bottom) sized at Tokens.iconMd with Tokens.textSecondary color. Do NOT use BackdropFilter, GlassContainer, or any blur widget in the overlay subtree (NAV-05, D-04 — the panel GlassContainer already owns the single blur boundary).

    Subscribe to InputModeDetector.instance.arrowGlow via a ValueListenableBuilder<ArrowDirection?>. When the value is ArrowDirection.up, apply a glow visual to the top indicator (e.g., switch the icon color to Tokens.accentBlue or Tokens.glowCore, or apply an AnimatedOpacity to a glow layer); when ArrowDirection.down, glow the bottom indicator; when null, both indicators are in the default non-glow state. Use ValueListenableBuilder to rebuild only the glow state, not the entire overlay. Do NOT add glow-reset logic here — the null-reset lifecycle (cancellable tokenized timer) is owned by InputModeDetector.setArrowGlow in Plan 01; this overlay only projects the notifier's current value (including null, which is reachable because Plan 01 auto-resets it after the glow duration).

    Modify lib/ui/dialogs/settings/tabs/general_tab.dart: wrap the existing SingleChildScrollView in a Stack with the OptionListNavigationOverlay as a sibling layer over the scrollable content. The overlay sits inside the existing panel GlassContainer blur boundary (D-04) — it must NOT create its own blur. Keep the existing AnimatedSectionList, GlassContainer sections, SettingSpinRow, and switch logic unchanged. Do NOT add a FocusTraversalGroup (D-08).

    Write test/ui/dialogs/option_list_navigation_overlay_test.dart: pump the overlay with a simple scrollable child (e.g., a Column of SizedBoxes). Traverse the widget subtree using tester.widgetList(find.byType(BackdropFilter)) and assert the count is zero within the overlay subtree. Set arrowGlow to ArrowDirection.up and assert the top indicator is in glow state; set to ArrowDirection.down and assert the bottom; set to null and assert neither. Assert the indicator containers use Tokens.bgGlass color.

    Run the full regression gate: D:/flutter/bin/flutter test (full suite) and compare failures to the pre-committed baseline artifact (32-baseline-failures.txt, generated by 32-01 Task 0 on a clean Phase 31 tree at the execute startpoint — Plan 03 only consumes it, does not generate it). The settings_focus_navigation_test compile error is now repaired (Plan 01 Task 2). Remaining pre-existing failures must match: ~57 mdk.dll FFI headless failures + 4 Phase 25 settings-dialog headless failures. No new failure may be attributed to Phase 32 files. If a Phase 32 test introduces a new failure, fix the test or implementation — do NOT remove or weaken any assertion (CLAUDE.md testing rule). Run D:/flutter/bin/flutter analyze on ALL Phase 32 files across all three plans and assert zero warnings.
  </action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/dialogs/option_list_navigation_overlay_test.dart && D:/flutter/bin/flutter analyze lib/kernel/services/input_mode_detector.dart lib/ui/dialogs/settings/panel_key_bindings.dart lib/ui/theme/tokens.dart lib/ui/dialogs/settings/tab_arrow_button.dart lib/ui/dialogs/settings/input_mode_hint.dart lib/ui/dialogs/settings/tab_strip.dart lib/ui/dialogs/settings/settings_overlay_shell.dart lib/ui/dialogs/settings/shortcuts_tab.dart lib/ui/dialogs/settings/option_list_navigation_overlay.dart lib/ui/dialogs/settings/tabs/general_tab.dart && (
      BASELINE=".planning/phases/32-navigation-interaction-polish/32-baseline-failures.txt"
      # The baseline artifact is generated by 32-01 Task 0 on a clean Phase 31 tree
      # (before any Phase 32 lib/ code is introduced) and committed as immutable.
      # This task only CONSUMES it — it does not generate it.
      # (1) Fail closed if the baseline artifact is missing or empty.
      if [ ! -s "$BASELINE" ]; then
        echo "GATE FAIL: baseline artifact $BASELINE is missing or empty — run 32-01 Task 0 first (on a clean Phase 31 tree, before Phase 32 lib/ code is introduced) to generate it"
        exit 1
      fi
      # (2) Run full suite, capture exit status.
      tmp=$(mktemp); tmp_fails=$(mktemp)
      D:/flutter/bin/flutter test >"$tmp" 2>&1; flutter_test_status=$?
      tail -60 "$tmp"
      echo "--- Regression gate: flutter test exit status = $flutter_test_status ---"
      if [ "$flutter_test_status" -eq 0 ]; then
        echo "GATE PASS: flutter test exited 0 (clean suite, zero failures)"
        rm -f "$tmp" "$tmp_fails"; exit 0
      fi
      # (3) Parse current [E] failing test identities (path: test name) into tmp_fails.
      grep -E '\[E\]' "$tmp" | sed -E 's/^[0-9]{2}:[0-9]{2} [+-][0-9]+( -[0-9]+)?: //; s/ \[E\].*$//' | sort -u > "$tmp_fails"
      # (4) Defense-in-depth (1): Phase 32 file exclusion — any Phase 32 test file failing = automatic FAIL regardless of baseline membership.
      phase32_tests='input_mode_detector_test|settings_tab_strip_test|input_mode_hint_test|option_list_navigation_overlay_test|settings_overlay_shell_test|settings_focus_navigation_test'
      phase32_fails=$(grep -E "(${phase32_tests})\.dart" "$tmp_fails" | sort -u)
      if [ -n "$phase32_fails" ]; then
        echo "GATE FAIL: Phase 32 test file(s) appear in the failure list (NEW regression — Phase 32 code is pure Dart/Flutter widget logic, never part of the baseline):"
        echo "$phase32_fails"
        rm -f "$tmp" "$tmp_fails"; exit 1
      fi
      # (5) PRIMARY classifier: per-failure membership — ANY failing test NOT in the baseline artifact = NEW regression (exit 1). The gate never exits 0 merely because a filename grep did not match or the mdk.dll signature is present.
      new_fails=$(comm -23 "$tmp_fails" "$BASELINE")
      if [ -n "$new_fails" ]; then
        echo "GATE FAIL: failing test(s) NOT in the baseline artifact (NEW regression outside documented baseline):"
        echo "$new_fails"
        echo "--- baseline: $(wc -l < "$BASELINE" | tr -d ' ') entries; current failures: $(wc -l < "$tmp_fails" | tr -d ' ') entries ---"
        rm -f "$tmp" "$tmp_fails"; exit 1
      fi
      # (6) Defense-in-depth (2): total-count ceiling (>70) — supplementary safeguard, NOT a substitute for per-failure membership.
      fail_count=$(wc -l < "$tmp_fails" | tr -d ' ')
      if [ "$fail_count" -gt 70 ]; then
        echo "GATE FAIL: failure count $fail_count exceeds supplementary ceiling 70 — suspected mass regression beyond documented baseline (per-failure membership already passed; this is defense-in-depth)"
        rm -f "$tmp" "$tmp_fails"; exit 1
      fi
      echo "GATE PASS: every failing test is a member of the baseline artifact ($BASELINE, $fail_count entries), no Phase 32 test file failed, count within supplementary ceiling — these are pre-existing environment failures (mdk.dll FFI / Phase 25 headless), NOT Phase 32 regressions"
      rm -f "$tmp" "$tmp_fails"; exit 0
    )</automated>
  </verify>
  <residual_note>The per-failure membership classifier compares test identities (path: test name), not failure reasons. If a Phase 32 change induces a NEW failure mode in a pre-existing test that was already failing at baseline time for a DIFFERENT reason (e.g. a pre-existing mdk.dll FFI loading failure), the test identity would match the baseline set and the Phase 32-induced failure would be masked. This risk is low in practice: Phase 32 changes are pure Dart/Flutter widget logic (no FFI, no native code per D-06), so failure modes are structurally disjoint from the mdk.dll FFI loading failures that dominate the baseline. The Phase-32-file exclusion (defense-in-depth 1) catches Phase 32 test files directly; this residual applies only to pre-existing test files that happen to overlap with the baseline failure set — it cannot be closed without per-failure root-cause attribution, which is not automatable in a shell-only gate.</residual_note>
  <acceptance_criteria>
    - OptionListNavigationOverlay subtree contains zero BackdropFilter widgets (structural test).
    - Arrow indicators use Container(color: Tokens.bgGlass), not GlassContainer or BackdropFilter.
    - Up arrow glows when arrowGlow is ArrowDirection.up; down arrow glows when ArrowDirection.down; neither glows when null.
    - GeneralTab SingleChildScrollView is wrapped with the overlay; existing sections, rows, and switches are unchanged.
    - The full-suite regression gate consumes a pre-committed immutable baseline artifact (32-baseline-failures.txt) generated by 32-01 Task 0 on a clean Phase 31 tree at the execute startpoint (before any Phase 32 lib/ code is introduced); Plan 03 only CONSUMES this artifact — it does not generate it. The gate: (1) asserts the baseline artifact exists and is non-empty, failing closed (exit 1) if missing/empty with a message to run 32-01 Task 0 first; (2) runs flutter test, captures exit status — exit 0 means GATE PASS; (3) parses current [E] failing test identities into a temp set; (4) Phase-32-file exclusion (input_mode_detector_test, settings_tab_strip_test, input_mode_hint_test, option_list_navigation_overlay_test, settings_overlay_shell_test, settings_focus_navigation_test) — any Phase 32 test file in the current failures is exit 1 (defense-in-depth 1); (5) comm -23 per-failure membership — any failing test NOT in the baseline artifact is exit 1 (PRIMARY classifier — the gate never exits 0 merely because a filename grep did not match or the mdk.dll signature is present); (6) total-count ceiling >70 is exit 1 (defense-in-depth 2). All passed means GATE PASS. No git stash/worktree is used anywhere.
    - flutter analyze reports zero warnings on the enumerated set of all 10 Phase 32 production files across Plans 01–03: lib/kernel/services/input_mode_detector.dart, lib/ui/dialogs/settings/panel_key_bindings.dart, lib/ui/theme/tokens.dart, lib/ui/dialogs/settings/tab_arrow_button.dart, lib/ui/dialogs/settings/input_mode_hint.dart, lib/ui/dialogs/settings/tab_strip.dart, lib/ui/dialogs/settings/settings_overlay_shell.dart, lib/ui/dialogs/settings/shortcuts_tab.dart, lib/ui/dialogs/settings/option_list_navigation_overlay.dart, lib/ui/dialogs/settings/tabs/general_tab.dart.
  </acceptance_criteria>
  <done>The option-list overlay is mounted on GeneralTab with no second blur, up/down glow is wired to the arrowGlow notifier, the regression gate matches the documented baseline, and flutter analyze is clean on all Phase 32 files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Windows Steam Input dual-mode + profile raster A/B checkpoint</name>
  <read_first>
    - .planning/phases/32-navigation-interaction-polish/32-RESEARCH.md (Open Question 1 Steam Input event signature; Environment Availability Windows + Steam Input controller required; Manual-Only Validation table)
    - .planning/phases/31-visual-design-alignment/31-03-PLAN.md (Task 2 checkpoint structure to mirror)
    - .planning/phases/31-visual-design-alignment/VERIFICATION.md (L28-34 pre-existing baseline)
    - lib/ui/dialogs/settings/panel_key_bindings.dart (arrow routing — all four arrows handled)
    - lib/ui/dialogs/settings/settings_overlay_shell.dart (Listener + InputModeHint + root Focus)
  </read_first>
  <files>lib/ui/dialogs/settings/option_list_navigation_overlay.dart, lib/ui/dialogs/settings/tabs/general_tab.dart</files>
  <what-built>End-cap tab arrows, InputModeDetector singleton with mouse-idle heuristic and toggle fallback, root Focus handling all four directional arrows with gameButtonLeft1/Right1 deleted, AnimatedSwitcher hint substitution, mouse Listener wired to the detector, pointer-only toggle in the shortcuts tab, and a thin-glass option-list overlay on GeneralTab with up/down glow — no second BackdropFilter anywhere.</what-built>
  <action>Run the Windows profile-mode checks below. This task changes no source files unless the developer reports a specific failure; return the affected section, measured raster evidence, or observed input issue to the executor for a corrective plan.</action>
  <how-to-verify>
    **Part A — Steam Input dual-mode navigation (NAV-02 / NAV-04 / NAV-07 manual-only):**

    1. Run `D:/flutter/bin/flutter run --profile -d windows` and open the Settings overlay.
    2. Press native keyboard Left and Right arrows. Record the logicalKey, physicalKey, and deviceType for each (add a temporary debugPrint in panel_key_bindings.handle or use the existing F12 debug export). Assert: tab index changes by exactly ±1 per press, the InputModeHint shows keyboard mode (← / →), and no seek or volume change occurs in the player behind the panel.
    3. If a Steam Input controller is available: map LB/RB to keyboard Left/Right in the Steam profile, then press LB and RB. Record the same key fields. Assert: tab index changes by exactly ±1 per press (no double-fire — NAV-04 deletion prevents the old gameButtonLeft1/Right1 route from firing alongside the arrow route), the hint shows gamepad mode (LB / RB) after 5s idle, and the manual toggle in the shortcuts tab can override the mode.
    4. If a direct gamepad is available: press gameButton12 and gameButton13. Assert: tab navigation works (D-05 — direct gamepad routing preserved) and does not double-fire with the arrow route.
    5. If no Steam controller is available: mark this sub-check as deferred and note it in the summary — the heuristic and toggle are designed to be source-agnostic (D-01) and the automated tests cover the logic.

    **Part B — Profile-mode raster A/B (NAV-05 / Pitfall 5 manual-only):**

    1. Enable PerfMonitor.instance.enable() and export a baseline raster statistic (exportStats) BEFORE opening the settings panel — or use the Phase 31 pre-change baseline if available.
    2. Open the Settings panel, navigate to the General tab (default), press Up/Down arrows to trigger glow and confirm it auto-resets to null (no persistent glow after the glow duration elapses — the reset lifecycle is owned by Plan 01 InputModeDetector.setArrowGlow), scroll the option list, and drag the panel across the window.
    3. Export PerfMonitor statistics and compare with the baseline. Pass if: raster average increases by no more than 1ms, there are no sustained raster-dominant slow frames, and scrolling/dragging produces no visible jank.
    4. Inspect the top and bottom overlay arrows on the General tab — they must be translucent (thin glass) with no visible double-blur or seam. The option-list overlay must not create a second blur layer (NAV-05 — structural test already asserts 0 BackdropFilter, this confirms the visual).
  </how-to-verify>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/dialogs/option_list_navigation_overlay_test.dart test/kernel/services/input_mode_detector_test.dart test/ui/dialogs/settings_tab_strip_test.dart test/ui/dialogs/input_mode_hint_test.dart test/ui/dialogs/settings_overlay_shell_test.dart test/ui/dialogs/settings_focus_navigation_test.dart</automated>
    <human-check>Complete both Part A (Steam Input dual-mode) and Part B (profile raster A/B) checks described in how-to-verify.</human-check>
  </verify>
  <acceptance_criteria>
    - Native keyboard Left/Right changes tab index by exactly ±1 per press with no seek/volume leak (NAV-07).
    - Steam Input LB/RB (if available) changes tab index by exactly ±1 per press with no double-fire (NAV-04).
    - gameButton12/gameButton13 direct gamepad routing works (D-05 — if a controller is available).
    - InputModeHint shows the correct mode and the toggle fallback overrides the heuristic.
    - Profile raster average increase is at most 1ms from baseline with no sustained raster-dominant slow frames.
    - The General tab overlay arrows are translucent with no visible double-blur or seam.
    - Up/Down arrow glow auto-resets to null after the glow duration (no persistent glow — reset lifecycle owned by Plan 01).
  </acceptance_criteria>
  <resume-signal>Type `approved` after all checks pass, or describe the input or visual/performance issue with the affected component or measured raster evidence.</resume-signal>
  <done>Windows profile and manual evidence confirm dual-mode navigation correctness (no double-fire, hint mode correct, toggle effective) and no raster regression from the thin-glass overlay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Option-list overlay → existing panel blur | The overlay must not introduce a second BackdropFilter under the panel GlassContainer blur boundary. |
| Glow notifier → overlay visual state | The arrowGlow ValueNotifier drives the overlay glow; a stale or null value must not leave a persistent glow. |
| Steam Input → root key router | External mapping injects keyboard arrows; the manual checkpoint validates that no double-fire occurs with the deleted gameButtonLeft1/Right1 bindings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-10 | Denial of service | Second BackdropFilter from option-list overlay (nested-blur) | high | mitigate | Overlay uses Container(color: Tokens.bgGlass) only; structural test asserts 0 BackdropFilter in overlay subtree (NAV-05, D-04). Windows checkpoint confirms no visible double-blur. |
| T-32-11 | Tampering | gameButtonLeft1/Right1 double-fire with Steam LB/RB | high | mitigate | NAV-04 deletion (Plan 01 grep gate) + Windows checkpoint confirms no double-fire with Steam Input mapped arrows. |
| T-32-12 | Denial of service | Persistent glow from stale arrowGlow value | low | mitigate | Reset lifecycle owned by 32-01 Task 1 (cancellable glow-reset timer → null, replacement-on-new-press, dispose cancels); 32-03 overlay only projects the notifier value (including null) — no reset logic here. |
| T-32-13 | Spoofing | Steam Input event signature mismatch | medium | accept | Heuristic is source-agnostic (D-01); the exact Steam signature is validated only in the manual checkpoint and does not alter production routing. |
| T-32-03X | Tampering | Arrow key leak to KeyboardHandler seek/volume (cross-plan, arrow-leak-to-seek) | high | mitigate | Owned by 32-01 Task 1 (single-root Focus handles all four arrows) + Task 2 (outer KeyboardHandler spy zero-call test); 32-03's overlay only projects glow, does not route keys. |
| T-32-03Y | Denial of service | Timer callback after InputModeDetector disposal (cross-plan, timer-after-dispose) | medium | mitigate | Owned by 32-01 Task 1 (dispose cancels gamepad-detection + glow-reset timers, fakeAsync test verifies); 32-03 only subscribes to arrowGlow, owns no timers. |
| T-32-SC | Tampering | Package installs | high | mitigate | No package-manager install occurs; zero new dependencies (D-06). |
</threat_model>

<verification>
Plan 03 completes the phase with automated structural and glow-state coverage, a full-suite regression comparison against the pre-committed baseline artifact (generated by 32-01 Task 0 on a clean Phase 31 tree, consumed by Plan 03 — not generated in-place), and a blocking Windows human checkpoint that validates the two manual-only dimensions: Steam Input dual-mode navigation (no double-fire, hint mode, toggle) and profile-mode raster performance (no new blur layer, raster avg ≤ +1ms). The checkpoint mirrors the Phase 31-03 Task 2 structure.
</verification>

<success_criteria>
- OptionListNavigationOverlay is mounted on GeneralTab with zero BackdropFilter in its subtree (NAV-05).
- Up/down glow feedback works via the arrowGlow notifier (NAV-06).
- The full-suite regression gate consumes a pre-committed immutable baseline artifact (32-baseline-failures.txt) generated by 32-01 Task 0 on a clean Phase 31 tree at the execute startpoint (before any Phase 32 lib/ code is introduced); Plan 03 only CONSUMES this artifact — it does not generate it. The gate fails closed (exit 1) if the artifact is missing/empty, then runs flutter test (exit 0 = PASS), parses [E] failures, excludes Phase 32 test files (defense-in-depth 1), checks each failure against the baseline via comm -23 (PRIMARY classifier — exit 1 for any non-member), and enforces a count ceiling >70 (defense-in-depth 2). No git stash/worktree is used anywhere; per-failure membership is the primary classifier.
- flutter analyze reports zero warnings on the enumerated set of all 10 Phase 32 production files across Plans 01–03 (input_mode_detector.dart, panel_key_bindings.dart, tokens.dart, tab_arrow_button.dart, input_mode_hint.dart, tab_strip.dart, settings_overlay_shell.dart, shortcuts_tab.dart, option_list_navigation_overlay.dart, general_tab.dart).
- The blocking Windows human checkpoint confirms dual-mode navigation and raster performance.
</success_criteria>

<output>
Create .planning/phases/32-navigation-interaction-polish/32-03-SUMMARY.md when done.
</output>
