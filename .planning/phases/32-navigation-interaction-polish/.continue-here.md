---
context: phase
phase: 32-navigation-interaction-polish
task: 2
total_tasks: 2
status: paused-task2-resolved-with-deferral
last_updated: 2026-07-29T17:20:13.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through failure. Acknowledge each before proceeding. Full detail in `.planning/HANDOFF.json` `blocking_constraints_for_resume`.

- [ ] **windows-gamepad-buttons-do-not-reach-focus** (blocking) — Profile diagnostic 2026-07-29 PROVED Xbox LB/RB/A/B/X/Y do NOT arrive at `Focus.onKeyEvent` on Windows desktop; only D-pad (remapped to keyboard arrows, HID page 0x07 usages 0x4F/0x50/0x51/0x52) arrives. `gameButtonLeft1/Right1/12/13` are Android-derived (Android keycodes 102/103/199/200) and are DEAD routing on Windows desktop. **DO NOT re-add gameButtonLeft1/Right1 expecting to fix LB/RB — it will not.** Any gamepad fix MUST go through XInput (`window_bridge` MethodChannel + `GamepadService`), not keyboard Focus. Deferred to 32-04.
- [ ] **baseline-extraction-has-two-bugs** (blocking) — The 32-03 plan `<verify>` block ships a broken extraction (`grep -E` non-binary-safe + sed missing `~[0-9]+` group). If the regression gate is re-run, MUST use: `grep -aE '\[E\]' "$tmp" | sed -E 's/^[0-9]{2}:[0-9]{2} [+-][0-9]+( ~[0-9]+)?( -[0-9]+)?: //; s/ \[E\].*$//' | sort -u` (same as `3716104d` baseline). Task 1 already applied this override.
- [ ] **verify-gate-trust-root-must-be-pre-phase32** (blocking) — Task 0 baseline (`3716104d`, 69 entries) is the immutable trust root; 32-03 CONSUMES it only, do NOT regenerate.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Asymmetric extraction | baseline + 32-03 gate using different `grep`/`sed` → false regression diff | blocking | Both MUST use the fixed `grep -aE + ( ~[0-9]+)?` extraction (Task 1 applied; carried into 32-03 gate) |
| Premature gamepad key-swap fix | restoring `gameButtonLeft1/Right1` looks plausible but events never reach Focus on Windows desktop | blocking | Diagnostic NOTE in `panel_key_bindings.dart` (commit `cac31475`) records dead-routing; gamepad fix routed to 32-04 XInput bridge |
| From-post-32-tree baseline | regenerating the regression baseline after Phase 32 lib code exists | blocking | Task 0 done on clean Phase-31 tree (`3716104d`); 32-03 consumes only |

<current_state>
Phase 32 (navigation-interaction-polish), Plan 03 (32-03), Task 2 checkpoint RESOLVED with gamepad deferred to 32-04. Task 1 done (`52764c33`); cleanup `cac31475` (debugPrint removed + diagnostic NOTE). 32-02 done (`9b9054e0`+`a41b0c4a`+`68d78dd9`). All 3 Phase-32 plans' CODE is done; only 32-03-SUMMARY.md + STATE update + phase verification remain.
</current_state>

<completed_work>
- 32-03 Task 1: OptionListNavigationOverlay + GeneralTab mount + regression gate — DONE `52764c33` (6/6 tests, gate PASS, 10 files analyze clean). BP3 extraction override applied.
- 32-03 Task 2 checkpoint: RESOLVED — user `approved` 2026-07-29; keyboard Part A verified via diagnostic; gamepad LB/RB DEFERRED to 32-04 (architectural); Part B perf structural mitigations tested (0-BackdropFilter), empirical A/B deferred.
- `cac31475`: removed temp diagnostic debugPrint + added dead-routing NOTE to `panel_key_bindings.dart`.
- Diagnostic finding: Xbox LB/RB don't reach Focus.on Windows desktop (only D-pad via keyboard remap); 32-01 `gameButton12/13` premise falsified.
</completed_work>

<remaining_work>
- Write `32-03-SUMMARY.md` (lead with Diagnostic finding; Task 1; Task 2 resolution; Rule 1 deviations; BP3 override).
- Update `STATE.md` session continuity + mark 32-03 complete.
- All 3 Phase-32 plans complete → run `gsd-verifier` for Phase 32 (will flag gamepad gap → 32-04 gap-closure plan: XInput bridge).
</remaining_work>

<decisions_made>
- Defer gamepad to 32-04 XInput bridge (not restore gameButtonLeft1/Right1) — diagnostic proved events don't reach Focus on Windows desktop.
- Did NOT commit a fake key-swap fix — would have been dishonest; recorded finding + deferred instead.
- Applied BP3 fixed extraction override (plan `<verify>` text is broken, do not copy verbatim).
</decisions_made>

<blockers>
- None technical. Gamepad is a deferred architectural item (32-04), not a blocker.
- Cost-critical: this session paused at ~$82 / context 91%.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable full state + 3 blocking_constraints_for_resume (primary resume artifact for `/gsd-resume-work`).
2. `.planning/phases/32-navigation-interaction-polish/32-03-PLAN.md` — Task 2 `<how-to-verify>` + Step 5 deferral mechanism.
3. `lib/ui/dialogs/settings/panel_key_bindings.dart` — the diagnostic NOTE (L96 area) records the dead-routing finding.
4. `3716104d` baseline artifact `32-baseline-failures.txt` — immutable trust root (consumed, not regenerated).

## Infrastructure State
- Flutter: `D:/flutter/bin/flutter` (NOT in PATH — use full path).
- Pre-existing test failures (not regressions): ~57 `fvp_engine_contract_test` (mdk.dll FFI headless) + ~4 Phase-25 settings-dialog headless + 12 dialog/widget headless — judged by module boundary, not Phase 32.
- Branch: `feat/v1.8-stability-polish-plan-02-02`. HEAD: `cac31475`.

<context>
The session's most important output is the DIAGNOSTIC that falsified a premise shared across 32-01/02/03 (gameButton12/13 route shoulders on Windows desktop — they do not fire there). The 1-line temp debugPrint (plan Part A step 2 sanctioned) earned its cost by preventing a wrong key-name-swap fix. Threat T-32-11 (gameButtonLeft1/Right1 double-fire) is moot on Windows desktop (those keys never fire).
</context>

<next_action>
Start with: write `.planning/phases/32-navigation-interaction-polish/32-03-SUMMARY.md` — lead with the "Diagnostic finding" section (LB/RB do not reach Focus on Windows desktop; 32-01 gameButton12/13 premise falsified; deferred to 32-04 XInput bridge), then Task 1 (`52764c33`, 6/6 tests, gate PASS), Task 2 checkpoint resolution (keyboard verified, gamepad deferred per Step 5), Rule 1 deviations, BP3 extraction override. Then update `STATE.md` + mark 32-03 complete → run `gsd-verifier` for Phase 32.
</next_action>
