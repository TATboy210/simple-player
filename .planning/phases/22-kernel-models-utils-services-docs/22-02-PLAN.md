---
phase: 22-kernel-models-utils-services-docs
plan: 02
type: execute
wave: 1
depends_on: [22-01]
files_modified:
  - lib/kernel/services/locale_service.dart
  - lib/kernel/startup/startup_coordinator.dart
  - lib/kernel/startup/startup_state.dart
  - lib/kernel/scanner/folder_scanner.dart
autonomous: true
requirements:
  - DOC-27
  - DOC-29
  - DOC-30
  - DOC-31
must_haves:
  truths:
    - "All B-class Services/Startup/Scanner files have field-level /// doc comments"
    - "All magic numbers have inline why-explanations (Chinese) per D-04/D-05"
    - "flutter analyze produces no new warnings or errors"
  artifacts:
    - lib/kernel/services/locale_service.dart
    - lib/kernel/startup/startup_coordinator.dart
    - lib/kernel/startup/startup_state.dart
    - lib/kernel/scanner/folder_scanner.dart
  key_links:
    - "C-class files (DOC-26, DOC-28, DOC-32) are skipped — already well-documented"
    - "startup_state.dart StartupPhase enum is already documented; only StartupState class needs supplement"
    - "folder_scanner.dart VideoFile fields need docs explaining relationship to PlaylistItem"
---

<objective>
Add or improve documentation comments on 4 B-class Services/Startup/Scanner files (DOC-27, DOC-29~DOC-31).

Purpose: Enable new developers to understand each file's responsibility without running the code. C-class files (DOC-26 global_hotkey_service, DOC-28 thumbnail_service, DOC-32 settings_validator) are skipped — already well-documented.

Output: 4 .dart files with complete field-level doc comments and magic number explanations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-kernel-models-utils-services-docs/22-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document locale_service + startup files (3 files)</name>
  <files>
    lib/kernel/services/locale_service.dart,
    lib/kernel/startup/startup_coordinator.dart,
    lib/kernel/startup/startup_state.dart
  </files>
  <read_first>
    lib/kernel/services/locale_service.dart,
    lib/kernel/startup/startup_coordinator.dart,
    lib/kernel/startup/startup_state.dart,
    .planning/phases/22-kernel-models-utils-services-docs/22-CONTEXT.md
  </read_first>
  <action>
    Supplement field-level docs and magic number explanations.

    **locale_service.dart (DOC-27):**
    - B-class: needs 3 member docs + magic number
    - Add `/// Singleton instance accessor.` to `I` field
    - Add `/// Current locale. Notifies listeners on change.` to `locale` field
    - Add `/// Releases resources. Call when service is no longer needed.` to `dispose()`
    - Add inline why to `Locale('zh')`: // 默认中文 — 与 SettingsValidator.defaultLocale 保持一致

    **startup_coordinator.dart (DOC-29):**
    - B-class: needs 1 method doc
    - Add `/// Releases all resources. Call during app shutdown.` to `dispose()`
    - Add inline why to `duration.inMicroseconds / 1000`: // μs → ms 转换

    **startup_state.dart (DOC-30):**
    - B-class: needs member docs on StartupState class
    - Add `/// Default initial state at app startup.` to `initial` static field
    - Add `/// Current startup phase.` to `phase` field
    - Add `/// Whether startup is complete (phase == StartupPhase.ready).` to `isReady` getter
    - Skip `copyWith`, `operator ==`, `hashCode` per D-09 (boilerplate methods)

    **Language rules (D-01~D-03):**
    - `///` doc comments: English
    - `//` inline why-explanations: Chinese
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -c '///' lib/kernel/services/locale_service.dart lib/kernel/startup/startup_coordinator.dart lib/kernel/startup/startup_state.dart | grep -v ':0$'</automated>
  </verify>
  <acceptance_criteria>
    - locale_service.dart: `I`, `locale`, `dispose()` have English doc comments; `Locale('zh')` has Chinese inline comment
    - startup_coordinator.dart: `dispose()` has English doc comment; `/ 1000` has Chinese inline comment
    - startup_state.dart: `initial`, `phase`, `isReady` have English doc comments
    - `flutter analyze` shows no new warnings
  </acceptance_criteria>
  <done>3 files have complete field-level doc comments and magic number explanations</done>
</task>

<task type="auto">
  <name>Task 2: Document folder_scanner.dart (B-class)</name>
  <files>
    lib/kernel/scanner/folder_scanner.dart
  </files>
  <read_first>
    lib/kernel/scanner/folder_scanner.dart,
    .planning/phases/22-kernel-models-utils-services-docs/22-CONTEXT.md
  </read_first>
  <action>
    Supplement field-level docs for VideoFile class.

    **folder_scanner.dart (DOC-31):**
    - B-class: needs 3 field docs on VideoFile
    - Add `/// Absolute path to the video file.` to `path` field
    - Add `/// File name with extension (e.g. "movie.mp4").` to `name` field
    - Add `/// Parent directory path.` to `folderPath` field
    - Add inline why to `_extensions` set: // 14 种常见视频格式，覆盖主流容器和编码

    **Language rules (D-01~D-03):**
    - `///` doc comments: English
    - `//` inline why-explanations: Chinese
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -c '///' lib/kernel/scanner/folder_scanner.dart</automated>
  </verify>
  <acceptance_criteria>
    - VideoFile: `path`, `name`, `folderPath` have English doc comments
    - `_extensions` set has Chinese inline comment
    - `flutter analyze` shows no new warnings
  </acceptance_criteria>
  <done>folder_scanner.dart has complete field-level doc comments</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Documentation-only changes — no new inputs, outputs, or control flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-02 | N/A | All files | none | accept | Documentation-only changes introduce no new attack surface. |
</threat_model>

<verification>
1. `flutter analyze` — no new warnings or errors introduced
2. Grep check: locale_service.dart has `///` on `I`, `locale`, `dispose`
3. Grep check: startup_state.dart has `///` on `initial`, `phase`, `isReady`
4. Grep check: folder_scanner.dart has `///` on `path`, `name`, `folderPath`
</verification>

<success_criteria>
- locale_service.dart: `I`, `locale`, `dispose()` documented; `Locale('zh')` explained
- startup_coordinator.dart: `dispose()` documented; μs→ms conversion explained
- startup_state.dart: `initial`, `phase`, `isReady` documented
- folder_scanner.dart: `path`, `name`, `folderPath` documented
- All `///` doc comments are in English (D-01)
- All `//` why-explanations are in Chinese (D-03)
- `flutter analyze` produces no new warnings or errors
</success_criteria>

<output>
Create `.planning/phases/22-kernel-models-utils-services-docs/22-02-SUMMARY.md` when done
</output>
