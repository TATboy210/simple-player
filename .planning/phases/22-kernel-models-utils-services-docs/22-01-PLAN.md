---
phase: 22-kernel-models-utils-services-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/models/aspect_ratio_mode.dart
  - lib/kernel/models/validation_error.dart
  - lib/kernel/models/app_settings.dart
  - lib/kernel/models/player_error.dart
  - lib/kernel/utils/perf_monitor.dart
autonomous: true
requirements:
  - DOC-17
  - DOC-18
  - DOC-19
  - DOC-20
  - DOC-21
must_haves:
  truths:
    - "Every Models layer enum/class has field-level /// doc comments"
    - "app_settings.dart has all 25+ fields documented with purpose, range, and units"
    - "All magic numbers have inline why-explanations (Chinese) per D-04/D-05"
    - "flutter analyze produces no new warnings or errors"
  artifacts:
    - lib/kernel/models/aspect_ratio_mode.dart
    - lib/kernel/models/validation_error.dart
    - lib/kernel/models/app_settings.dart
    - lib/kernel/models/player_error.dart
    - lib/kernel/utils/perf_monitor.dart
  key_links:
    - "Models are exported via barrel files — doc comments appear in IDE tooltips"
    - "app_settings.dart is the configuration source for all services — field docs benefit the whole codebase"
    - "perf_monitor.dart uses Flutter SchedulerBinding for frame timing — comments must explain the callback mechanism"
---

<objective>
Add or improve documentation comments on 5 Models + Utils layer files (DOC-17~DOC-21).

Purpose: Enable new developers to understand each file's responsibility and key logic without running the code. Focus on field-level docs for app_settings.dart and magic number explanations.

Output: 5 .dart files with complete field-level doc comments and magic number explanations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-kernel-models-utils-services-docs/22-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document app_settings.dart (A-class, 25+ fields)</name>
  <files>
    lib/kernel/models/app_settings.dart
  </files>
  <read_first>
    lib/kernel/models/app_settings.dart,
    .planning/phases/22-kernel-models-utils-services-docs/22-CONTEXT.md,
    lib/kernel/persistence/settings_store.dart
  </read_first>
  <action>
    This is the highest-priority file. Add `///` doc comments to ALL 25+ fields.

    **Class-level doc comment (expand existing):**
    - Add English doc comment explaining this is the immutable data container for all app settings
    - Note that it uses `_sentinel` pattern for nullable `copyWith` fields
    - Note the `playbackSpeed` implicit field anomaly (declared only in constructor, not in field list)

    **Field-level docs — every field gets a `///` line with:**
    - Purpose (what it controls)
    - Valid range / units where applicable
    - Default value explanation where non-obvious

    Specific field documentation:
    - `volume`: `/// Audio volume level. Range: 0.0 (mute) to 100.0 (max).`
    - `lastFile`: `/// Path of the last opened media file. Empty string if none.`
    - `windowWidth`/`windowHeight`: `/// Window dimensions in logical pixels.`
    - `windowX`/`windowY`: `/// Window position in logical pixels. Null = centered by system.`
    - `isMaximized`: `/// Whether the window is maximized.`
    - `playMode`: `/// Playlist loop mode index. Maps to PlayMode enum (0=LoopAll, 1=LoopSingle, 2=Shuffle).`
    - `isMuted`: `/// Whether audio output is muted.`
    - `isAlwaysOnTop`: `/// Whether the window stays above other windows.`
    - `isFullscreen`: `/// Whether the window is in fullscreen mode.`
    - `subtitleFontSize`: `/// Subtitle font size in logical pixels. Default 17.0 — standard readable size for 1080p.`
    - `subtitleColorIndex`: `/// Subtitle color preset index. Maps to a predefined color palette.`
    - `subtitleBottomOffset`: `/// Distance from bottom edge to subtitle text in logical pixels. Default 80.0 — clears the control bar.`
    - `videoBrightness`: `/// Video brightness adjustment. Range: -1.0 to 1.0. 0.0 = no change.`
    - `videoContrast`: `/// Video contrast adjustment. Range: -1.0 to 1.0. 0.0 = no change.`
    - `videoSaturation`: `/// Video saturation adjustment. Range: -1.0 to 1.0. 0.0 = no change.`
    - `videoHue`: `/// Video hue adjustment in degrees. Range: -180.0 to 180.0. 0.0 = no change.`
    - `videoRotation`: `/// Video rotation in degrees. Valid: 0, 90, 180, 270.`
    - `videoAspectRatioIndex`: `/// Video aspect ratio mode index. Maps to AspectRatioMode enum.`
    - `videoDeinterlace`: `/// Whether to enable deinterlacing for interlaced content.`
    - `playbackSpeed`: `/// Playback speed multiplier. Range: 0.25 to 4.0. Default 1.0 = normal speed.`
    - `d3d11Sync`: `/// Whether to enable D3D11 CPU sync. Prevents tearing at the cost of performance.`
    - `hardwareDecoding`: `/// Whether to use hardware-accelerated decoding (D3D11/NVDEC).`

    **Known anomaly (document, don't fix):**
    - Add a `// NOTE:` comment near the constructor explaining that `playbackSpeed` is an implicit field
    - Add a `// BUG:` comment in `operator ==` noting that `playbackSpeed` is missing from the comparison (violates ==/hashCode contract)
    - Do NOT fix the bug — this phase is documentation-only

    **Magic number explanations:**
    - `17.0`: // 标准可读字号，1080p 下等效约 17px，参考系统默认字体大小
    - `80.0`: // 底部偏移量，刚好避开 64px 高的控制栏 + 16px 间距
    - `1.0`: // 正常播放速度，MDK 以倍率表示
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -c '///' lib/kernel/models/app_settings.dart</automated>
  </verify>
  <acceptance_criteria>
    - Every field (volume, lastFile, windowWidth, windowHeight, windowX, windowY, isMaximized, playMode, isMuted, isAlwaysOnTop, isFullscreen, subtitleFontSize, subtitleColorIndex, subtitleBottomOffset, videoBrightness, videoContrast, videoSaturation, videoHue, videoRotation, videoAspectRatioIndex, videoDeinterlace, playbackSpeed, d3d11Sync, hardwareDecoding) has a `///` doc comment
    - Magic numbers 17.0, 80.0, 1.0 have inline why-explanations
    - playbackSpeed anomaly is documented with NOTE/BUG comments
    - All `///` doc comments are in English (D-01)
    - All `//` why-explanations are in Chinese (D-03)
    - `flutter analyze` shows no new warnings
  </acceptance_criteria>
  <done>app_settings.dart has complete field-level English doc comments, magic number explanations, and anomaly documentation</done>
</task>

<task type="auto">
  <name>Task 2: Document Models B-class files (3 files)</name>
  <files>
    lib/kernel/models/aspect_ratio_mode.dart,
    lib/kernel/models/validation_error.dart,
    lib/kernel/models/player_error.dart
  </files>
  <read_first>
    lib/kernel/models/aspect_ratio_mode.dart,
    lib/kernel/models/validation_error.dart,
    lib/kernel/models/player_error.dart,
    .planning/phases/22-kernel-models-utils-services-docs/22-CONTEXT.md
  </read_first>
  <action>
    Supplement field-level docs and magic number explanations for 3 B-class Models files.

    **aspect_ratio_mode.dart (DOC-17):**
    - B-class: has class doc, needs magic number explanations
    - Add inline why-comment to `1.1920928955078125e-7`: // mdk 特殊常量 — 表示"保持原始宽高比"，非零极小值触发 mdk 的 preserve-aspect-ratio 逻辑（0.0 表示拉伸，负值表示裁剪填充）
    - Add inline why-comment to `-1.1920928955078125e-7`: // mdk 特殊常量的负值 — 表示"裁剪填充"，负号触发 mdk 的 crop-to-fill 模式
    - Add field-level docs: `/// Display label for UI.` for `label`, `/// mdk Player.setAspectRatio() float value.` for `mdkValue`

    **validation_error.dart (DOC-18):**
    - B-class: has class doc, needs 2 field-level docs
    - Add `/// The category of validation failure.` to `type` field
    - Add `/// Human-readable description of the error for UI display.` to `message` field
    - Enum values already have Chinese doc comments — leave as-is (they're clear)

    **player_error.dart (DOC-20):**
    - B-class: has class doc, needs 3 field-level docs
    - Add `/// Error category for programmatic handling.` to `code` field
    - Add `/// Human-readable error message for UI display.` to `message` field
    - Add `/// Optional original exception. Excluded from equality comparison.` to `cause` field
    - Enum values already have Chinese doc comments — leave as-is

    **Language rules (D-01~D-03):**
    - `///` doc comments: English
    - `//` inline why-explanations: Chinese
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -c '///' lib/kernel/models/aspect_ratio_mode.dart lib/kernel/models/validation_error.dart lib/kernel/models/player_error.dart | grep -v ':0$'</automated>
  </verify>
  <acceptance_criteria>
    - aspect_ratio_mode.dart: `1.1920928955078125e-7` has Chinese inline comment explaining mdk preserve-aspect-ratio behavior; `label` and `mdkValue` fields have English doc comments
    - validation_error.dart: `type` and `message` fields have English doc comments
    - player_error.dart: `code`, `message`, and `cause` fields have English doc comments; `cause` doc mentions nullable and equality exclusion
    - All `///` doc comments are in English (D-01)
    - All `//` why-explanations are in Chinese (D-03)
    - `flutter analyze` shows no new warnings
  </acceptance_criteria>
  <done>3 Models B-class files have complete field-level doc comments and magic number explanations</done>
</task>

<task type="auto">
  <name>Task 3: Document perf_monitor.dart (B-class, magic numbers)</name>
  <files>
    lib/kernel/utils/perf_monitor.dart
  </files>
  <read_first>
    lib/kernel/utils/perf_monitor.dart,
    .planning/phases/22-kernel-models-utils-services-docs/22-CONTEXT.md
  </read_first>
  <action>
    Add magic number explanations to perf_monitor.dart.

    **perf_monitor.dart (DOC-21):**
    - B-class: has good class/method docs, needs magic number explanations
    - Add inline why to `16` (line 57): // 16ms — 60fps 下一帧的预算时间（1000/60 ≈ 16.67ms），超过即视为慢帧
    - Add inline why to `100` (line 67): // 每 100 帧输出一次统计，平衡日志频率和信息量
    - `1000` (microseconds-to-milliseconds): appears 8 times — this is a standard conversion factor, add one comment at first usage: // μs → ms 转换因子
    - Note: `_maxFrames = 300` is already documented in class doc comment — no change needed

    **Language rules (D-01~D-03):**
    - `///` doc comments: English
    - `//` inline why-explanations: Chinese
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -c '///' lib/kernel/utils/perf_monitor.dart</automated>
  </verify>
  <acceptance_criteria>
    - `16` has Chinese inline comment explaining 60fps frame budget
    - `100` has Chinese inline comment explaining stats interval rationale
    - First `1000` usage has Chinese inline comment explaining μs→ms conversion
    - `flutter analyze` shows no new warnings
  </acceptance_criteria>
  <done>perf_monitor.dart has all magic numbers explained with Chinese inline why-comments</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Documentation-only changes — no new inputs, outputs, or control flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | N/A | All files | none | accept | Documentation-only changes introduce no new attack surface. No code behavior modification. |
</threat_model>

<verification>
1. `flutter analyze` — no new warnings or errors introduced
2. Grep check: app_settings.dart has 25+ `///` doc comment lines (one per field)
3. Grep check: aspect_ratio_mode.dart has inline comment mentioning "mdk" or "宽高比"
4. Grep check: perf_monitor.dart has inline comment mentioning "60fps" or "16ms"
5. Spot check: player_error.dart `cause` field doc mentions "nullable" or "Optional"
</verification>

<success_criteria>
- app_settings.dart: all 25+ fields have `///` doc comments with purpose, range, and units
- aspect_ratio_mode.dart: mdk magic constants have Chinese inline explanations
- validation_error.dart: `type` and `message` fields have English doc comments
- player_error.dart: `code`, `message`, `cause` fields have English doc comments
- perf_monitor.dart: `16` and `100` magic numbers have Chinese inline explanations
- All `///` doc comments are in English (D-01)
- All `//` why-explanations are in Chinese (D-03)
- `flutter analyze` produces no new warnings or errors
</success_criteria>

<output>
Create `.planning/phases/22-kernel-models-utils-services-docs/22-01-SUMMARY.md` when done
</output>
