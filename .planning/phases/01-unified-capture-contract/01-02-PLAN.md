---
phase: 01-unified-capture-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/kernel/diagnostics/global_error_hooks.dart
  - lib/main.dart
  - lib/kernel/player_services.dart
  - test/diagnostics/global_error_hooks_test.dart
autonomous: true
requirements:
  - CAP-01
  - CAP-02
  - CAP-03
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "FlutterError.onError first retains framework development presentation and then safely forwards the same FlutterErrorDetails to ErrorReporterImpl; if development presentation itself fails, a non-recursive fallback is attempted before reporter intake continues."
    - "PlatformDispatcher.onError safely forwards its exact error and stack to ErrorReporterImpl and always returns true, so root-isolate asynchronous errors are treated as handled."
    - "The first executable statement of main starts one runZonedGuarded closure containing both binding branches, KernelLogger/ErrorReporter initialization, global-hook assignment, MediaKit and window initialization, and runApp."
    - "A handled WindowService initialization failure preserves the existing App windowInitError state and reaches the reporter exactly once rather than being rethrown or reported by two paths."
    - "ErrorReporterImpl is initialized from main before application services access it, while PlayerServices no longer initializes KernelLoggerImpl and retains its existing MemoryMonitor lifecycle ownership."
  artifacts:
    - "lib/kernel/diagnostics/global_error_hooks.dart"
    - "lib/main.dart"
    - "lib/kernel/player_services.dart"
    - "test/diagnostics/global_error_hooks_test.dart"
  key_links:
    - "GlobalErrorHooks.install → FlutterError.onError / PlatformDispatcher.instance.onError → ErrorReporterImpl safe intake"
    - "main runZonedGuarded onError → ErrorReporterImpl.reportBootstrapSafely"
    - "main window init catch → existing windowInitError plus one ErrorReporterImpl report"
  prohibitions:
    - statement: "MUST NOT replace framework error presentation, swallow dispatcher errors by returning false, or register global callbacks outside the guarded bootstrap zone."
      status: flagged-unverified
      flagged: true
    - statement: "MUST NOT turn diagnostic capture into a second startup failure: hook callbacks stay synchronous, avoid I/O, and return normally when reporter work fails."
      status: flagged-unverified
      flagged: true
    - statement: "MUST NOT add a MediaKitEngine.lastError listener, file writing, card UI, route, or modal interaction in this integration plan."
      status: flagged-unverified
      flagged: true
---

<objective>
**As a** 开发者/用户，**I want to** 任何错误发生时被安全捕获为统一 ErrorReport，**so that** 出错可定位且报告链自身不会造成新故障。

Purpose: Connect the tested kernel reporter to the actual Flutter/Dart capture boundaries and startup lifecycle without duplicating capture or perturbing player service ownership.
Output: Thin global hook adapter, same-zone guarded bootstrap, single diagnostic initialization owner, and hook behavior tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-unified-capture-contract/01-CONTEXT.md
@.planning/phases/01-unified-capture-contract/01-RESEARCH.md
@.planning/phases/01-unified-capture-contract/01-PATTERNS.md
@.planning/phases/01-unified-capture-contract/01-01-SUMMARY.md
@lib/main.dart
@lib/kernel/player_services.dart
@lib/kernel/diagnostics/error_reporter.dart
@lib/kernel/diagnostics/kernel_logger.dart
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove framework and dispatcher hooks reach the production reporter without escaping their callback boundary</name>
  <reversibility rating="reversible">The hook adapter is a small injected wrapper around process-global callback assignments, so callback plumbing can be changed without migrating stored data or public output.</reversibility>
  <files>lib/kernel/diagnostics/global_error_hooks.dart, test/diagnostics/global_error_hooks_test.dart</files>
  <read_first>
    - lib/kernel/diagnostics/global_error_hooks.dart (new-file target; confirm it does not yet exist before creating it)
    - test/diagnostics/global_error_hooks_test.dart (new test target; confirm it does not yet exist before creating it)
    - lib/kernel/diagnostics/error_reporter.dart and lib/kernel/diagnostics/error_reporting_dependencies.dart from Plan 01
    - .planning/phases/01-unified-capture-contract/01-PATTERNS.md sections for global_error_hooks.dart, global hook restoration, and global containment
    - lib/main.dart and test/ui/player/responsive_layout_test.dart
    - .planning/phases/01-unified-capture-contract/01-CONTEXT.md (D-01) and 01-RESEARCH.md (Pattern 5 and Validation Architecture)
  </read_first>
  <behavior>
    - RED: installed Flutter callback invokes the injected framework presentation once and forwards one FlutterErrorDetails input to the reporter even if presentation throws.
    - RED: installed dispatcher callback forwards the exact error and StackTrace to the reporter and returns true.
    - RED: a reporter that throws does not escape either callback, and a throwing framework presentation invokes the last-resort seam without suppressing reporter forwarding.
    - GREEN: production install assigns FlutterError.onError and PlatformDispatcher.instance.onError through setter-injected seams and never awaits or performs I/O inside callbacks.
  </behavior>
  <action>Create `lib/kernel/diagnostics/global_error_hooks.dart` as a thin, documented `GlobalErrorHooks` adapter with production `install(ErrorReporter reporter)` and an injectable construction/install seam for tests that controls framework callback assignment, dispatcher callback assignment, framework presentation, and `LastResortOutput`. The framework callback must invoke `FlutterError.presentError(details)` before `reporter.reportFlutterSafely(details)` to meet CAP-02; isolate presentation failure independently and then still attempt reporter intake. The dispatcher callback must call `reporter.reportPlatformSafely(error, stackTrace)` and return literal `true` after containment. Use containment only at this error composition boundary and route failures to the injected non-recursive fallback, never back through `KernelLogger` or reporter. Write RED tests first using hand-written recording/throwing reporter and callback variables; avoid constructing `MediaKitEngine`, and use immediate teardown restoration if a real Flutter global is touched.</action>
  <verify>
    <automated>flutter test test/diagnostics/global_error_hooks_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/kernel/diagnostics/global_error_hooks.dart` contains `GlobalErrorHooks.install`, assigns both production Flutter error boundaries, and has a test-injectable callback setter seam.
    - The dispatcher test asserts exact error and stack identity/value forwarding and a `true` result.
    - The framework test asserts presentation precedes forwarding, and a presentation failure still reaches reporter intake.
    - Throwing reporter/presentation test doubles leave both installed callbacks returning normally.
    - `flutter test test/diagnostics/global_error_hooks_test.dart` exits 0.
  </acceptance_criteria>
  <done>The two live Flutter/Dart callback shapes have a real, independently tested path into the safe kernel reporter.</done>
</task>

<task type="expansion" tdd="true">
  <name>Task 2: Put all startup work in one guarded zone and establish main as the sole diagnostics initializer</name>
  <reversibility rating="costly">D-02 changes the application-wide initialization ownership of KernelLoggerImpl and introduces ErrorReporterImpl.I as a shared cross-layer lifecycle contract.</reversibility>
  <files>lib/main.dart, lib/kernel/player_services.dart, test/diagnostics/global_error_hooks_test.dart</files>
  <read_first>
    - lib/main.dart and lib/kernel/player_services.dart
    - lib/kernel/diagnostics/global_error_hooks.dart, lib/kernel/diagnostics/error_reporter.dart, and lib/kernel/diagnostics/kernel_logger.dart from Plan 01/Task 1
    - test/diagnostics/global_error_hooks_test.dart
    - .planning/phases/01-unified-capture-contract/01-PATTERNS.md sections for main.dart, player_services.dart, singleton lifecycle, and global containment
    - .planning/phases/01-unified-capture-contract/01-CONTEXT.md (D-01 and D-02) and 01-RESEARCH.md (exact bootstrap ordering, Pitfalls 1, 2, and 6)
  </read_first>
  <behavior>
    - RED: a source-level/bootstrap-oriented test or testable helper proves the global hook installer is called only after ErrorReporterImpl initialization from the same guarded bootstrap path.
    - RED: a captured bootstrap error delegates to reportBootstrapSafely and returns normally.
    - RED: a handled window initialization exception retains the App-facing error string and produces exactly one reporter intake, with no rethrow.
    - GREEN: main’s first executable statement establishes runZonedGuarded; every binding, hook, initialization, await, and runApp operation remains inside that closure.
  </behavior>
  <action>Refactor `Future<void> main()` so its first executable statement calls `runZonedGuarded<Future<void>>`; put both the `MarionetteBinding.ensureInitialized()` debug branch and `WidgetsFlutterBinding.ensureInitialized()` branch, `MediaKit.ensureInitialized()`, `KernelLoggerImpl.init()`, `ErrorReporterImpl.init(...)`, `windowManager.ensureInitialized()`, `GlobalErrorHooks.install(ErrorReporterImpl.I)`, existing `StartupTimeline` and `WindowService` work, and `runApp` inside that same async closure exactly as D-01 requires. Install hooks inside the guarded closure because dispatcher registration binds to the current zone. Keep existing window initialization recovery: preserve `windowInitError`, retain `KernelLogger.I.e`, add exactly one `ErrorReporterImpl.I.reportBootstrapSafely(error, stackTrace)` forwarding call in its local `on Object` handler, and do not rethrow it. Make the zone `onError` delegate directly to the reporter safe bootstrap intake and return normally. Per D-02, remove only `KernelLoggerImpl.init()` from `PlayerServices._initOnce`, update its initialization-order documentation to say main owns logger initialization, and preserve `MemoryMonitor` construction/disposal plus all service gates. Extend the hook/bootstrap tests with a testable helper or narrow source assertion that covers the initialized reporter registration order and local catch forwarding. Do not add a `MediaKitEngine.lastError` subscription, file effect, or card host; this plan connects only the three global hooks and existing startup path.</action>
  <verify>
    <automated>flutter test test/diagnostics/error_report_test.dart test/diagnostics/error_reporter_test.dart test/diagnostics/global_error_hooks_test.dart &amp;&amp; flutter analyze</automated>
    <human_check>Run `flutter run -d windows` in debug mode; induce or observe startup and framework diagnostics, then confirm the application reaches its normal player/error state rather than terminating because the feedback chain handled an error.</human_check>
  </verify>
  <acceptance_criteria>
    - `lib/main.dart` has `runZonedGuarded` as its first executable statement and contains both binding branches, global hook installation, all existing bootstrap awaits, and `runApp` inside the guarded closure.
    - `main` initializes `KernelLoggerImpl` and `ErrorReporterImpl` before `GlobalErrorHooks.install` and before WindowService initialization.
    - The local WindowService catch preserves `windowInitError`, keeps logging, forwards exactly once via `reportBootstrapSafely`, and does not rethrow.
    - `lib/kernel/player_services.dart` no longer invokes `KernelLoggerImpl.init()` while still injects `KernelLoggerImpl.I` into `MemoryMonitor`.
    - Focused diagnostics tests and `flutter analyze` exit 0; the Windows debug smoke check has an explicit observed outcome recorded in the task summary.
  </acceptance_criteria>
  <done>The actual application starts and registers all three global error boundaries in one guarded zone, preserving framework diagnostics and forwarding failures without duplicating startup evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | Symbols produced or modified | Purpose |
|---|---|---|
| `lib/kernel/diagnostics/global_error_hooks.dart` | `GlobalErrorHooks`, `GlobalErrorHooks.install`, injectable hook setter/presentation seams | Thin adapters from Flutter/Dart globals to `ErrorReporter`. |
| `lib/main.dart` | Guarded `main`, same-zone bootstrap closure, reporter initialization, bootstrap error forwarding | Production capture composition root. |
| `lib/kernel/player_services.dart` | Updated `PlayerServices._initOnce` initialization-order contract | Removes duplicate logger initialization while retaining service-owned monitor lifecycle. |
| `test/diagnostics/global_error_hooks_test.dart` | Framework/dispatcher forwarding and containment test groups | Verifies global callback contracts. |

<source_audit>
SOURCE | ID | Feature/requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Four sources safely reach ordered reports without feedback-chain crashes | 01-02 | COVERED | Integrates three global sources into Plan 01 reporter.
REQ | CAP-01 | Framework, async, startup, player sources share contract | 01-02 | COVERED | Framework/dispatcher/bootstrap wiring; explicit player API remains Plan 01.
REQ | CAP-02 | Three hooks in one guarded zone; framework output retained; dispatcher returns true | 01-02 | COVERED | Tasks 1–2 implement and test exact hook/startup behavior.
REQ | CAP-03 | Intake does not create new application fault | 01-02 | COVERED | Callback containment tests and safe reporter delegation.
RESEARCH | — | Same-zone binding/dispatcher registration, presentation-before-reporting, local window catch retained | 01-02 | COVERED | Task 2 specifies ordered bootstrap and one-time local forwarding.
RESEARCH | — | Hook adapter stays synchronous and setter-injectable | 01-02 | COVERED | Task 1’s adapter and tests.
CONTEXT | D-01 | Guarded zone wraps entire main including bindings/runApp | 01-02 | COVERED | Task 2 exact structural action.
CONTEXT | D-02 | Main initializes ErrorReporter early; duplicate logger init converges | 01-02 | COVERED | Task 2 exact ownership transfer, recorded costly.
CONTEXT | D-03 | Pre-runApp errors are retained for later flush | 01-02 | COVERED | Startup errors enter Plan 01 queue before eventual Phase 3 host flush.
CONTEXT | D-04 | Queue/dedupe policy | 01-01 | COVERED | Implemented and tested in predecessor plan.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Flutter framework/runtime global callbacks → hook adapter | Framework details and root-isolate exceptions cross into application diagnostics. |
| Guarded bootstrap zone → reporter singleton | Startup failures occur before UI mount and must not fail open into a second crash. |
| PlayerServices → composition-root singleton | Service initialization depends on already-initialized diagnostics global state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-05 | Denial of Service | `FlutterError.onError` and dispatcher callbacks | high | mitigate | Task 1 makes callbacks synchronous and contained; reporter errors route to a non-recursive fallback and dispatcher always acknowledges handling. |
| T-01-06 | Repudiation | Bootstrap/window error capture | medium | mitigate | Task 2 forwards the handled local window exception once while retaining its original error/stack and App error state. |
| T-01-07 | Tampering | Global hook registration zone and callback installation | medium | mitigate | Task 2 installs exactly once from the same guarded startup closure after reporter initialization; task tests use injected setters rather than uncontrolled global mutation. |
| T-01-08 | Information Disclosure | Error payload forwarded to presentation | medium | mitigate | Task 1 forwards opaque report inputs only; source interpretation and disk output remain outside this plan and are guarded in later phases. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installation occurs in this plan. |
</threat_model>

<verification>
1. Run `flutter test test/diagnostics/error_report_test.dart test/diagnostics/error_reporter_test.dart test/diagnostics/global_error_hooks_test.dart`.
2. Run `flutter analyze` with zero errors.
3. Run `flutter test` at the Phase 1 completion gate and distinguish known native media DLL environment failures from any changed diagnostics failures.
4. Run the stated Windows debug startup smoke check because same-zone binding/startup behavior crosses process-global Flutter runtime boundaries.
</verification>

<success_criteria>
- Production Flutter framework, root-isolate asynchronous, and guarded bootstrap entry points call the single reporter safely.
- Framework debug diagnostics remain presented; dispatcher errors are marked handled with a literal `true` result.
- The entire bootstrap runs in one guarded zone and the local window failure stays visible to the App while entering the reporter once.
- Diagnostic initialization has one normal owner in main; player services retain their existing owned resource lifecycle without duplicate logger creation.
</success_criteria>

<output>
Create `.planning/phases/01-unified-capture-contract/01-02-SUMMARY.md` when done.
</output>
