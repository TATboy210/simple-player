---
phase: 04-quality-migration
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - lib/kernel/bridge/window_service.dart
  - lib/main.dart
  - pubspec.yaml
  - test/integration/fullscreen_e2e_test.dart
autonomous: true
requirements:
  - ARCH-03
  - RST-01
  - RST-02
  - RST-03
  - RST-04
  - PLAT-01
  - PLAT-02
  - PLAT-03
must_haves:
  truths:
    - "旧 fullscreen_window 直连路径标记为 @Deprecated"
    - "pubspec.yaml 版本号更新为 RC 版本"
    - "USE_NEW_FULLSCREEN=true 为默认值"
    - "E2E 测试脚本存在且可执行"
  artifacts:
    - lib/kernel/bridge/window_service.dart (deprecated markers)
    - lib/main.dart (default flag change)
    - pubspec.yaml (version bump)
    - test/integration/fullscreen_e2e_test.dart
  key_links:
    - "window_service.dart legacy path -> @Deprecated annotation"
    - "main.dart USE_NEW_FULLSCREEN default -> true"
    - "pubspec.yaml version -> RC version number"
---

<objective>
旧实现标记废弃 + RC 版本号 + E2E 测试脚手架。

Purpose: 按 D-46/D-47 决策标记旧 fullscreen_window 路径为 deprecated，更新版本号为 RC，创建 E2E 测试脚本供手动验证。
Output: 4 个文件修改/创建 — deprecated 标记、版本号、默认 flag、E2E 测试。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-quality-migration/04-CONTEXT.md
@.planning/phases/04-quality-migration/04-RESEARCH.md
@lib/kernel/bridge/window_service.dart
@lib/main.dart
@pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 旧实现废弃标记 + 默认 flag 切换</name>
  <files>lib/kernel/bridge/window_service.dart, lib/main.dart</files>
  <read_first>
    - lib/kernel/bridge/window_service.dart (第 295~310 行，legacy fullscreen 路径)
    - lib/main.dart (USE_NEW_FULLSCREEN flag 定义和使用)
    - .planning/phases/04-quality-migration/04-CONTEXT.md (D-46, D-47: 渐进式删除，先 deprecated 再删除)
    - .planning/phases/02-command-queue-recovery/02-CONTEXT.md (D-27: feature flag 决策)
  </read_first>
  <action>
修改 `lib/kernel/bridge/window_service.dart`：

1. 在文件顶部 import 区域，为 `fullscreen_window` 添加废弃注释：
   `// @deprecated 2026-07-10 — 旧 fullscreen_window 直连路径，将在 v1.2 移除。请使用 FullscreenAdapter。`

2. 在 `setMode` 方法中 legacy fullscreen 路径（`_fullscreenAdapter == null` 分支内的 `fullScreenWindow.setFullScreen(true)` 调用）上方添加 `@Deprecated('Use FullscreenAdapter instead. Will be removed in v1.2')` 注释。由于这是方法内代码块而非独立方法，使用行内注释标记：
   ```dart
   // @deprecated(v1.2) — 旧 fullscreen_window 直连，保留为 fallback。新实现通过 FullscreenAdapter。
   await fullScreenWindow.setFullScreen(true);
   ```

3. 在 `_fullscreenAdapter == null` 的 fallback 分支整体添加 TODO 注释：
   ```dart
   // TODO(ARCH-03): v1.2 移除此分支 — D-46: RC 后 2 版本内无 blocker 则删除
   ```

修改 `lib/main.dart`：

1. 将 `USE_NEW_FULLSCREEN` 的默认值从 `false` 改为 `true`（per D-46: RC 版本默认使用新实现）。找到 `bool.fromEnvironment('USE_NEW_FULLSCREEN', defaultValue: false)` 改为 `defaultValue: true`

2. 更新相关注释说明默认值已切换：
   ```dart
   /// 默认 true（RC 版本）。设为 false 回退到旧 fullscreen_window 实现。
   ```

3. 保留 `USE_WINDOWS_NATIVE_FULLSCREEN` flag 不变（Windows 原生 FFI 驱动控制）
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/bridge/window_service.dart lib/main.dart --fatal-infos 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - window_service.dart 中 legacy fullscreen 路径包含 `@deprecated` 或 `@Deprecated` 注释
    - window_service.dart 包含 `TODO(ARCH-03): v1.2 移除` 注释
    - main.dart 中 `defaultValue: true`（USE_NEW_FULLSCREEN 默认开启）
    - `flutter analyze` 零警告通过
    - 现有测试不受影响（`flutter test` 通过）
  </acceptance_criteria>
  <done>旧实现标记废弃，USE_NEW_FULLSCREEN 默认 true，flutter analyze 通过</done>
</task>

<task type="auto">
  <name>Task 2: RC 版本号 + E2E 测试脚手架</name>
  <files>pubspec.yaml, test/integration/fullscreen_e2e_test.dart</files>
  <read_first>
    - pubspec.yaml (当前版本 0.0.1)
    - .planning/phases/04-quality-migration/04-CONTEXT.md (D-48: 语义化版本 v1.0.0-rc.1)
    - .planning/phases/04-quality-migration/04-RESEARCH.md (Pitfall 4: MSIX 版本格式 x.x.x.x)
    - test/integration/controls_flow_test.dart (现有集成测试结构)
    - test/regression/smoke_suite_test.dart (冒烟测试 case ID)
  </read_first>
  <action>
修改 `pubspec.yaml`：

1. 将 `version: 0.0.1` 改为 `version: 1.0.0-rc.1`（D-48: 语义化版本）

2. 更新 `msix_config.msix_version` 为 `1.0.0.1`（MSIX 不支持预发布标签，映射 rc.1 -> .1，per Research Pitfall 4）

创建 `test/integration/fullscreen_e2e_test.dart`：

这是真实窗口 E2E 测试脚手架，用于手动验证（CI 中无法测试真实窗口管理器）。结构：

```dart
// 全屏 E2E 测试 — 需要真实窗口管理器，不可在 headless CI 运行。
// 使用: flutter test integration_test/fullscreen_e2e_test.dart -d windows
//
// 覆盖场景（对应 regression_matrix.md）:
//   FS-WIN-001: Enter/exit fullscreen
//   FS-WIN-002: Rapid F key 10x
//   FS-WIN-004: Maximized -> FS -> Exit
//   FS-WIN-005: Playing + fullscreen
//   FS-WIN-008: ESC semantic
```

测试内容：
1. 集成 `WidgetsFlutterBinding.ensureInitialized()`
2. 创建真实 `DesktopFullscreenDriverFactory.create()` 驱动
3. 创建 `DesktopFullscreenAdapter` 使用真实驱动
4. 测试 enter fullscreen -> 验证 snapshot.phase == FullscreenPhase.stable
5. 测试 exit fullscreen -> 验证 snapshot.isFullscreen == false
6. 测试 rapid toggle 10x -> 验证最终 stable 无错误
7. 使用 `addTearDown` 清理 adapter

注意：
- 此文件位于 `test/integration/` 目录（与现有 controls_flow_test.dart 同级）
- 标注 `@Tags(['e2e'])` 以便 `flutter test` 默认跳过（需要 `-t e2e` 显式运行）
- 文件头注释说明运行方式和前置条件（需要真实桌面环境）
  </action>
  <verify>
    <automated>grep "version:" pubspec.yaml | head -1</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml version 为 `1.0.0-rc.1`
    - pubspec.yaml msix_config.msix_version 为 `1.0.0.1`
    - test/integration/fullscreen_e2e_test.dart 存在且非空
    - E2E 测试文件包含 `@Tags(['e2e'])` 注解
    - E2E 测试文件头注释说明运行方式（flutter test -t e2e）
    - E2E 测试覆盖至少 enter/exit/rapid toggle 3 个场景
    - `flutter analyze test/integration/fullscreen_e2e_test.dart` 通过
  </acceptance_criteria>
  <done>RC 版本号就绪，E2E 测试脚手架创建完成，可手动验证全屏场景</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deprecated code -> production | 旧路径标记 @Deprecated 但保留为 fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed in this plan |

</threat_model>

<verification>
- `flutter analyze` 零警告
- `flutter test` 全部通过（USE_NEW_FULLSCREEN=true 默认配置）
- pubspec.yaml 版本号为 RC 格式
- E2E 测试文件存在且可手动运行
</verification>

<success_criteria>
1. window_service.dart 旧路径标记 @Deprecated + TODO(v1.2 移除)
2. main.dart USE_NEW_FULLSCREEN 默认 true
3. pubspec.yaml 版本 1.0.0-rc.1，msix_version 1.0.0.1
4. E2E 测试脚手架创建，覆盖 3+ 场景
5. flutter analyze + flutter test 通过
</success_criteria>

<artifacts>
## Artifacts This Phase Produces

| Symbol | File | Type | Description |
|--------|------|------|-------------|
| DeprecatedMarkers | lib/kernel/bridge/window_service.dart | code | @Deprecated + TODO(v1.2) 标记旧路径 |
| DefaultFlagSwitch | lib/main.dart | code | USE_NEW_FULLSCREEN default=true |
| RCVersion | pubspec.yaml | config | version=1.0.0-rc.1 + msix_version=1.0.0.1 |
| E2ETest | test/integration/fullscreen_e2e_test.dart | test | E2E 测试脚手架（手动验证） |
</artifacts>

<output>
Create `.planning/phases/04-quality-migration/04-03-SUMMARY.md` when done
</output>
