---
phase: 04-quality-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/regression/high_risk_suite_test.dart
  - test/regression/smoke_suite_test.dart
  - test/regression/regression_matrix.md
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - EVT-01
  - EVT-02
  - EVT-03
  - ERR-01
  - ERR-02
  - ERR-03
  - CMD-01
  - CMD-02
  - CMD-03
  - RST-01
  - RST-02
  - RST-03
  - RST-04
  - ARCH-01
  - ARCH-02
  - ARCH-03
must_haves:
  truths:
    - "快速连按 F 10 次不出现状态错位"
    - "maximized -> fullscreen -> exit 恢复到 maximized"
    - "StateDesync 后手动重试可恢复"
    - "P0 用例 100% 通过"
  artifacts:
    - test/regression/high_risk_suite_test.dart
    - test/regression/smoke_suite_test.dart
    - test/regression/regression_matrix.md
  key_links:
    - "FakeFullscreenAdapter -> high_risk_suite_test (test double for rapid toggle)"
    - "DesktopFullscreenAdapter -> smoke_suite_test (integration-level scenarios)"
    - "regression_matrix.md -> all test IDs (traceability)"
---

<objective>
创建回归测试矩阵：高风险套件 + 冒烟套件 + 矩阵文档。

Purpose: Phase D 验证收尾的核心交付物。覆盖 8 项必测场景 + 高风险场景（快速连按、maximized恢复、StateDesync恢复），确保所有 v1 需求可验收。
Output: 3 个文件 — 高风险测试、冒烟测试、回归矩阵文档。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-migration/04-CONTEXT.md
@.planning/phases/04-quality-migration/04-RESEARCH.md
@test/kernel/bridge/fullscreen_adapter_test.dart
@test/kernel/bridge/fullscreen_command_queue_test.dart
@test/kernel/bridge/desktop_fullscreen_adapter_test.dart
@test/integration/controls_flow_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: 高风险测试套件 + 冒烟测试套件</name>
  <files>test/regression/high_risk_suite_test.dart, test/regression/smoke_suite_test.dart</files>
  <read_first>
    - test/kernel/bridge/fullscreen_adapter_test.dart (FakeFullscreenAdapter 用法)
    - test/kernel/bridge/fullscreen_command_queue_test.dart (命令队列测试模式)
    - test/kernel/bridge/desktop_fullscreen_adapter_test.dart (adapter 集成测试模式)
    - test/integration/controls_flow_test.dart (集成测试结构)
    - lib/kernel/bridge/fullscreen_adapter.dart (接口签名)
    - lib/kernel/models/fullscreen_snapshot.dart (FullscreenPhase 枚举)
    - lib/kernel/models/fullscreen_error.dart (FullscreenError 类型)
  </read_first>
  <action>
创建 `test/regression/high_risk_suite_test.dart`，覆盖 D-35 高风险场景：

1. **快速连按 F 10 次** — 使用 FakeFullscreenAdapter 连续调用 setFullscreen(true) 10 次，验证最终 phase == stable，无 BusyTransition 残留错误
2. **快速连按 F 50 次** — 同上但 50 次，验证命令队列幂等合并（CMD-02）不崩溃
3. **maximized -> fullscreen -> exit** — 先设 snapshot 为 maximized 状态，进全屏，退出，验证 restoreMode 正确（RST-02）
4. **StateDesync 后手动重试** — 模拟 StateDesync 错误，调用 setFullscreen 重试，验证 error 自动清理 + 恢复到 stable（ERR-02, CMD-03）
5. **回调缺失超时** — 验证 driver 不回调时 adapter 超时进入 error phase（RST-04 变体）
6. **per-window 隔离** — 两个 windowId 并发操作，验证状态互不污染（STATE-03）

每个测试使用 FakeFullscreenAdapter 作为测试替身（不需要真实窗口管理器）。导入路径：
- `import 'package:simple_player_flutter/kernel/bridge/fullscreen_adapter.dart';`
- `import 'package:simple_player_flutter/kernel/models/fullscreen_snapshot.dart';`
- `import 'package:simple_player_flutter/kernel/models/fullscreen_error.dart';`
- `import 'package:simple_player_flutter/kernel/models/fullscreen_request.dart';`
- 复用 `test/kernel/bridge/fullscreen_adapter_test.dart` 中的 FakeFullscreenAdapter（如果可导出），或在本文件内创建简化版

创建 `test/regression/smoke_suite_test.dart`，覆盖 8 项必测场景（D-32）：

1. **播放中 + 全屏** — 模拟播放状态，进全屏，验证 phase 进入 entering 后到 stable
2. **暂停中 + 全屏** — 模拟暂停状态，进全屏，验证状态正确
3. **F 键与按钮一致性** — toggle(true) 和 setFullscreen(true) 结果相同
4. **ESC 语义** — fullscreen 状态下 setFullscreen(false) 退出全屏
5. **连续切换稳定性** — 10 次 toggle，最终状态正确
6. **windowed -> fullscreen -> windowed** — 恢复到原始窗口几何（RST-01）
7. **副屏位置恢复** — 设置 secondary displayId，退出后验证位置信息（RST-03）
8. **错误事件通知** — 触发 Unsupported 错误，验证 events 流包含 error 事件（ERR-02）

每个测试标注 case ID（如 FS-REG-001 ~ FS-REG-008），与 regression_matrix.md 对应。
  </action>
  <verify>
    <automated>flutter test test/regression/ --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test/regression/high_risk_suite_test.dart 包含 6 个 test()：快速10x、快速50x、maximized恢复、StateDesync重试、回调超时、per-window隔离
    - test/regression/smoke_suite_test.dart 包含 8 个 test()，每个标注 case ID（FS-REG-001 ~ FS-REG-008）
    - `flutter test test/regression/ --no-pub` 全部通过
    - 所有测试使用 FakeFullscreenAdapter，不依赖真实窗口管理器
    - 高风险测试验证 CMD-02 幂等合并（快速连按不崩溃）
    - 高风险测试验证 ERR-02 错误事件通知（StateDesync 后重试恢复）
  </acceptance_criteria>
  <done>高风险套件 6 测试 + 冒烟套件 8 测试全部通过，覆盖所有 P0 场景</done>
</task>

<task type="auto">
  <name>Task 2: 回归矩阵文档</name>
  <files>test/regression/regression_matrix.md</files>
  <read_first>
    - test/regression/high_risk_suite_test.dart (Task 1 产出)
    - test/regression/smoke_suite_test.dart (Task 1 产出)
    - .planning/phases/04-quality-migration/04-CONTEXT.md (D-32~D-41 决策)
    - .planning/REQUIREMENTS.md (22 个 v1 需求 ID)
  </read_first>
  <action>
创建 `test/regression/regression_matrix.md`，按 D-38 文档格式：

文档结构：
1. **元数据头** — Branch / Build / Flags / Date / Tester 字段（D-38, D-41）
2. **结果表** — 统一 case ID（FS-{PLATFORM}-{NNN}），字段：Platform / Scenario / Type(Auto/Manual) / Priority(P0/P1) / Result(pass/fail/blocked/flaky/skipped) / Evidence
3. **用例列表**（至少 24 条，按 D-32 标准版深度 24-40 用例）：

Windows 深测（12 条 P0）：
- FS-WIN-001 ~ 008: 8 项必测场景（播放中/暂停中/连续切换/F与按钮一致/ESC语义/maximized恢复/副屏恢复/多窗口隔离）
- FS-WIN-009: 快速连按 F 10 次（高风险）
- FS-WIN-010: 快速连按 F 50 次（高风险）
- FS-WIN-011: StateDesync 后重试恢复（高风险）
- FS-WIN-012: minimized -> fullscreen 先恢复窗口（RST-04）

macOS 冒烟（6 条 P1）：
- FS-MAC-001 ~ 006: enter/exit、播放中、暂停中、maximized恢复、连续切换、ESC语义

Linux 冒烟（6 条 P1）：
- FS-LIN-001 ~ 006: 同 macOS 冒烟场景，注明 GNOME 为主

4. **覆盖映射表** — 每个 case ID 映射到 v1 requirement ID（如 FS-WIN-001 -> RST-01）
5. **阻断规则** — P0 失败阻断发布、StateDesync 无恢复阻断发布、任何平台"状态错乱/无法恢复窗口/卡死"阻断发布（D-36, D-37）
6. **构建配置记录** — 每次测试必须标注 USE_NEW_FULLSCREEN / USE_WINDOWS_NATIVE_FULLSCREEN 值（D-41）
7. **执行策略** — 渐进式验证（每个 Plan 完成后跑对应子集）+ 每周跨平台冒烟 + RC 前全量回归（D-39）
8. **失败分级** — blocker/high/medium/low 定义 + 是否阻断发布（D-40）
  </action>
  <verify>
    <automated>test -f test/regression/regression_matrix.md && echo "MATRIX_DOC_EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - test/regression/regression_matrix.md 存在且非空
    - 包含至少 24 条用例（Windows 12 + macOS 6 + Linux 6）
    - 每条用例有 Platform / Scenario / Type / Priority / Result 字段
    - 包含"覆盖映射表"关联 case ID 到 v1 requirement ID
    - 包含"阻断规则"章节定义 P0 失败 = 阻断发布
    - 包含"构建配置记录"章节要求标注 USE_NEW_FULLSCREEN 值
    - 包含"失败分级"章节定义 blocker/high/medium/low
  </acceptance_criteria>
  <done>回归矩阵文档完成，24+ 用例覆盖所有 v1 需求，阻断规则和构建配置要求明确</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code -> production code | Tests validate production behavior through public interfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed in this plan |

</threat_model>

<verification>
- `flutter test test/regression/ --no-pub` 全部通过
- `test/regression/regression_matrix.md` 包含 24+ 用例
- 每个用例有 case ID、Platform、Scenario、Type、Priority 字段
</verification>

<success_criteria>
1. 高风险套件 6 测试全部通过（快速连按、maximized恢复、StateDesync恢复）
2. 冒烟套件 8 测试全部通过（8 项必测场景）
3. 回归矩阵文档 24+ 用例，覆盖所有 22 个 v1 需求
4. 阻断规则明确：P0 失败 = 阻断发布
</success_criteria>

<artifacts>
## Artifacts This Phase Produces

| Symbol | File | Type | Description |
|--------|------|------|-------------|
| HighRiskSuite | test/regression/high_risk_suite_test.dart | test | 6 个高风险测试（快速连按、maximized恢复、StateDesync） |
| SmokeSuite | test/regression/smoke_suite_test.dart | test | 8 个冒烟测试（必测场景） |
| RegressionMatrix | test/regression/regression_matrix.md | doc | 24+ 用例回归矩阵文档 |
</artifacts>

<output>
Create `.planning/phases/04-quality-migration/04-01-SUMMARY.md` when done
</output>
