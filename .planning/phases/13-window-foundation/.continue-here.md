# Continue Here — Phase 13: Window Foundation

**Paused**: 2026-05-31T20:17:49Z
**Branch**: fix/window-startup
**Reason**: Context at 69% (31% remaining), natural pause point after Wave 2

## Current Position

- **Phase**: Phase 13 — Window Foundation
- **Approach**: Pure Dart (user chose over C++ WndProc)
- **Wave 2**: COMPLETE
- **Wave 3**: PENDING

## What Was Done (Wave 2)

### Files Modified

1. **`lib/kernel/bridge/win32_bindings.dart`**
   - Added `swpNomove = 0x0002` and `swpNosize = 0x0001` constants

2. **`lib/kernel/bridge/window_service.dart`** — 6 changes:
   - Removed `_baseStyle` field (line 28)
   - Removed `_removeBorder()` instance method (lines 49-51)
   - Removed `_baseStyle` reference from `_enterFullscreen` (line 195)
   - H-1 fix: `onWindowClose` uses `whenComplete` to await geometry save before `windowManager.destroy()`
   - H-2 fix: `_enterFullscreen` frees old `_savedFrame` before allocating new
   - M-1 fix: `removeBorderImmediate()` uses `swpNomove | swpNosize` instead of `0x0001 | 0x0002`
   - `_saveGeometryImmediate` returns `Future<void>` (was fire-and-forget IIFE)
   - Updated docstring (removed `_baseStyle` reference)

### Verification

- `flutter analyze` passes — only pre-existing test file warnings (8 issues, all in test/)

## What Remains (Wave 3)

### Goal
WindowService ~408 lines → ~350 lines (-15%)

### Tasks
- Remove dead code paths left from old border removal approach
- Simplify fullscreen restore logic (no `_baseStyle` dependency)
- Add Timeline instrumentation to maximize/restore
- Verify: resize, maximize/restore, fullscreen, DragToResizeArea, Win11 rounded corners

### Files to Modify
- `lib/kernel/bridge/window_service.dart` — main cleanup target

## Decisions Made

| Decision | Rationale |
|----------|-----------|
| Pure Dart over C++ WndProc | Simpler, `waitUntilReadyToShow` → `removeBorderImmediate()` → `show()` already handles timing |
| Keep `removeBorderImmediate()` static | Still needed for initial setup in `main.dart` |
| `whenComplete` over `await` for H-1 | `onWindowClose` override returns void, can't await directly |

## Resume Steps

1. Run `flutter run -d windows` — verify Wave 2 changes work (no flicker, no regressions)
2. Check current WindowService line count: `wc -l lib/kernel/bridge/window_service.dart`
3. Execute Wave 3: remove dead code, simplify fullscreen restore, add Timeline
4. Run `flutter analyze` + `flutter test`
5. Manual verification: resize, maximize/restore, fullscreen, DragToResizeArea

## Context for Next Session

- Flutter at `D:\flutter\bin\flutter` (not in PATH)
- `main.dart:30-45` has the startup flow: `waitUntilReadyToShow` → `removeBorderImmediate` → restore geometry → show → focus
- `removeBorderImmediate()` is static, called from both `main.dart` and previously from `_removeBorder()` (now removed)
- `_savedStyle` in `_enterFullscreen` now uses `win32.getWindowLongPtr(hwnd, gwlStyle)` directly (no `_baseStyle`)
