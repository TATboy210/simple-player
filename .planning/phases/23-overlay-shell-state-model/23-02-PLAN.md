---
phase: 23-overlay-shell-state-model
plan: 02
type: execute
wave: 1
depends_on:
  - 23-01
files_modified:
  - lib/ui/dialogs/settings/settings_overlay_shell.dart
  - lib/ui/player/player_screen.dart
  - lib/features/player/player_feature.dart
  - lib/features/player/deferred_player_feature.dart
  - lib/app.dart
  - test/ui/dialogs/settings_overlay_shell_test.dart
autonomous: false
requirements:
  - PANEL-01
  - PANEL-03
  - PANEL-04
  - PANEL-05
  - PANEL-06
  - PANEL-07
user_setup: []
must_haves:
  truths:
    - "PANEL-03: opening the in-tree overlay renders a centered GlassContainer using GlassTier.normal, with its BackdropFilter, Tokens.bgGlass, and Tokens.borderHighlight."
    - "PANEL-04: the title bar contains the text 设置, has a GlassButton.iconOnly close control, and only its title-bar gesture updates dragOffset."
    - "PANEL-05: a full-player mask tap closes the panel; after close, the shell is conditionally removed and cannot receive pointer events."
    - "PANEL-05 animation: open and close use AnimatedOpacity plus AnimatedScale with a 200ms duration; the open endpoint has opacity 1.0 and scale 1.0 after a 200ms test pump."
    - "PANEL-06: ESC and B close the open shell and are consumed with KeyEventResult.handled, leaving the injected fullscreen-exit observer uncalled."
    - "PANEL-07: panel dimensions equal min(500, windowWidth * 0.8) by min(400, windowHeight * 0.8), preserving a 5:4 base ratio for a 500×400 or constrained size."
    - "PANEL-07 boundary: at exactly 625×500 the panel is 500×400; below either threshold its affected dimension follows 80% of the MediaQuery dimension rather than overflowing."
    - "PANEL-07 precision: computed dimensions remain double values from MediaQuery multiplication with no ceil, floor, or truncation; widget tests assert exact representative values."
    - "FLAGGED ASSUMPTION (PANEL-03 unclassified): GlassContainer's existing cached blur and resize degradation behavior is the authoritative shell rendering implementation."
    - "FLAGGED ASSUMPTION (PANEL-04 empty/encoding): the fixed Chinese title 设置 is nonempty display text; empty dynamic titles and grapheme-truncation policy are outside this shell-only phase."
  artifacts:
    - "lib/ui/dialogs/settings/settings_overlay_shell.dart defines SettingsOverlayShell."
    - "lib/ui/player/player_screen.dart receives a SettingsPanelController and mounts SettingsOverlayShell as the topmost child of the content-region Stack below CustomTitleBar."
    - "lib/features/player/player_feature.dart constructs SettingsPanelController from its existing PlaybackController and disposes it with PlayerFeature."
    - "test/ui/dialogs/settings_overlay_shell_test.dart covers visual structure, drag, mask close, key consumption, animation endpoint, and responsive bounds."
  key_links:
    - "PlayerFeature is the composition root that constructs SettingsPanelController(_services.controller) and passes it to PlayerScreen by constructor; App and DeferredPlayerFeature remove the obsolete dialog callback path, per D-02 and D-06 cutover."
    - "SettingsOverlayShell observes controller.state.isOpen and invokes controller.close from mask tap, close button, and handled ESC/B key events."
    - "The topmost PlayerScreen content Stack places shell hit testing above both wide/narrow playlist presentations while CustomTitleBar remains outside it."
  prohibitions:
    - statement: "FLAGGED-UNVERIFIED: the old dialog implementation must not be deleted or bundled into this feature implementation commit."
      status: flagged-unverified
    - statement: "FLAGGED-UNVERIFIED: visual values in the new overlay must not be introduced as raw colors, font sizes, spacing, radii, or durations when a Tokens value supplies that visual role."
      status: flagged-unverified
---

<objective>
Deliver the production-quality in-tree settings shell through the actual PlayerFeature → PlayerScreen composition path, with glass, lifecycle input handling, drag constraints, and responsive sizing.

Purpose: Users can open and close a real settings overlay without leaving the player route or triggering fullscreen behavior.
Output: SettingsOverlayShell, its composition-root wiring, and end-to-end widget tests for PANEL-03 through PANEL-07.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-overlay-shell-state-model/23-CONTEXT.md
@.planning/phases/23-overlay-shell-state-model/23-RESEARCH.md
@.planning/phases/23-overlay-shell-state-model/23-PATTERNS.md
@.planning/phases/23-overlay-shell-state-model/23-VALIDATION.md
@.planning/phases/23-overlay-shell-state-model/23-01-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- Classes: SettingsPanelState, SettingsPanelController, SettingsOverlayShell, FakePlaybackController.
- Service contract: SettingsPanelPlayback.
- Methods: SettingsPanelController.open(), close(), toggle(); PlaybackController.pause(), play(); PlaybackController.isPlaying getter.
- Notifier fields: SettingsPanelState.isOpen, selectedTab, dragOffset.
- Source paths: lib/ui/shared/apple_curves.dart; lib/kernel/services/playback_controller.dart; lib/ui/dialogs/settings/settings_panel_state.dart; lib/ui/dialogs/settings/settings_panel_controller.dart; lib/ui/dialogs/settings/settings_overlay_shell.dart.
- Test paths: test/ui/dialogs/settings_panel_state_test.dart; test/ui/dialogs/settings_panel_controller_test.dart; test/ui/dialogs/settings_overlay_shell_test.dart.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="advisory">
  <name>Task 1: Record the already-locked in-tree Stack mounting decision</name>
  <read_first>
    - lib/ui/player/player_screen.dart
    - lib/ui/playlist/playlist_panel.dart
    - lib/features/player/player_feature.dart
    - .planning/phases/23-overlay-shell-state-model/23-CONTEXT.md
    - .planning/phases/23-overlay-shell-state-model/23-PATTERNS.md
    - .planning/phases/23-overlay-shell-state-model/23-VALIDATION.md
    - CLAUDE.md
  </read_first>
  <reversibility rating="costly">D-05 commits the shell to PlayerScreen's content Stack; changing to route-based mounting later would rebuild composition, focus, drag, animation, and trigger wiring.</reversibility>
  <decision>Confirm the D-05 in-tree Stack mounting model before integrating the shell.</decision>
  <context>The user already selected Stack in-tree. This advisory record fixes the integration point as the topmost PlayerScreen content layer below CustomTitleBar; it does not revisit showDialog or block execution.</context>
  <options>
    <option id="locked-d05">
      <name>Keep the user-locked Stack in-tree mounting</name>
      <pros>Matches PlaylistPanel compositing, preserves player-level focus and enables mask/drag animation without Navigator routing.</pros>
      <cons>Future route-based mounting would require coordinated rewiring.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The implementation record identifies the PlayerScreen content Stack as the shell mount below CustomTitleBar.
    - The checkpoint remains advisory and does not request a new decision.
  </acceptance_criteria>
  <verify><automated>flutter test test/ui/dialogs/settings_panel_controller_test.dart -x</automated></verify>
  <resume-signal>Continue with locked-d05.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end open, mask-close, and playback-resume shell path</name>
  <read_first>
    - lib/ui/dialogs/settings/settings_panel_state.dart
    - lib/ui/dialogs/settings/settings_panel_controller.dart
    - lib/ui/shared/apple_curves.dart
    - lib/ui/shared/glass_container.dart
    - lib/ui/playlist/playlist_panel.dart
    - lib/ui/player/player_screen.dart
    - lib/features/player/player_feature.dart
    - lib/features/player/deferred_player_feature.dart
    - lib/app.dart
    - .planning/phases/23-overlay-shell-state-model/23-CONTEXT.md
    - .planning/phases/23-overlay-shell-state-model/23-PATTERNS.md
    - .planning/phases/23-overlay-shell-state-model/23-VALIDATION.md
    - CLAUDE.md
  </read_first>
  <reversibility rating="costly">D-05 uses a PlayerScreen in-tree Stack mount so overlay focus, drag, mask, and animation share the actual player composition path.</reversibility>
  <files>lib/ui/dialogs/settings/settings_overlay_shell.dart, lib/ui/player/player_screen.dart, lib/features/player/player_feature.dart, lib/features/player/deferred_player_feature.dart, lib/app.dart, test/ui/dialogs/settings_overlay_shell_test.dart</files>
  <behavior>
    - Test 1: calling the controller's open path makes SettingsOverlayShell visible above player content and displays 设置 plus a close button.
    - Test 2: tapping the full-player mask closes the overlay, returns the tree to no SettingsOverlayShell hit target, and the controller resumes a previously playing fake.
    - Test 3: the visible shell contains GlassContainer and a BackdropFilter through GlassTier.normal, and its settled enter endpoint is scale 1.0 with opacity 1.0 after 200ms.
  </behavior>
  <action>Start by writing the end-to-end widget tests with a MaterialApp/MediaQuery harness and the hand-written fake from Plan 23-01; tests must run without mdk.dll. Create `SettingsOverlayShell` as a focused stateful widget that accepts a required `SettingsPanelController`, listens to `state.isOpen`, and owns only the ephemeral exit-animation mount flag required to keep a 200ms close animation visible; the persistent phase state remains exactly the controller's three D-04 notifiers. Per D-05/D-07/D-08, compose a full-size mask sibling and centered panel sibling in a Stack; use AnimatedOpacity plus AnimatedScale for exactly `Duration(milliseconds: 200)`, choosing AppleCurves.fullscreenEnter when open and AppleCurves.fullscreenExit when closed. Keep interaction safe with `IgnorePointer(ignoring: !isOpen)` throughout the close interval, then conditionally remove the shell child after that interval only if isOpen remains false; do not make opacity-zero widgets interactive. Build the panel with `GlassContainer(tier: GlassTier.normal, borderRadius: BorderRadius.circular(Tokens.radiusLg), resizing: windowService.isResizing)` so the existing component owns BackdropFilter, bgGlass, borderHighlight, caching, and resize behavior. Add only shell-frame content: the title and close control; do not add sidebar or tab content. Add a required `SettingsPanelController settingsPanelController` constructor parameter to PlayerScreen, mount one SettingsOverlayShell as the topmost content-region Stack child below CustomTitleBar, and have PlayerFeature construct it from `_services.controller`, pass it to PlayerScreen, and dispose it. Per D-06 cutover, make PlayerFeature's gear action call `settingsPanelController.open()` while preserving its right-click quick-menu callback; remove the obsolete `onSettings` callback parameter and forwarding from PlayerFeature, DeferredPlayerFeature, and App, and remove App's settings-panel import and _showSettingsPanel method. Do not delete lib/ui/dialogs/settings_panel.dart in this task: D-06 reserves that deletion for its own subsequent commit after the full cutover is verified.</action>
  <acceptance_criteria>
    - `SettingsOverlayShell` uses `AnimatedOpacity`, `AnimatedScale`, `Duration(milliseconds: 200)`, AppleCurves.fullscreenEnter, and AppleCurves.fullscreenExit.
    - `SettingsOverlayShell` contains a `GlassContainer` configured with `GlassTier.normal` and a `GlassButton.iconOnly` close control alongside the literal 设置 title.
    - PlayerFeature creates SettingsPanelController from `_services.controller`, passes it via PlayerScreen's required constructor parameter, and disposes it.
    - App, DeferredPlayerFeature, and PlayerFeature have no obsolete onSettings route callback path; the gear action calls `settingsPanelController.open()`.
    - PlayerScreen mounts the open shell above its responsive video/playlist layout and below CustomTitleBar.
    - `flutter test test/ui/dialogs/settings_overlay_shell_test.dart -x` exits 0.
  </acceptance_criteria>
  <verify><automated>flutter test test/ui/dialogs/settings_overlay_shell_test.dart -x</automated></verify>
  <done>The real settings trigger opens an in-tree glass shell, a mask click closes it, and the tested end-to-end lifecycle resumes only previously playing playback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand shell interaction safety, drag bounds, keyboard handling, and sizing tests</name>
  <read_first>
    - lib/ui/dialogs/settings/settings_overlay_shell.dart
    - lib/ui/dialogs/settings/settings_panel_controller.dart
    - lib/ui/dialogs/settings/settings_panel_state.dart
    - lib/ui/shared/apple_curves.dart
    - lib/ui/shared/glass_container.dart
    - lib/ui/player/keyboard_handler.dart
    - lib/ui/playlist/playlist_panel.dart
    - test/ui/dialogs/settings_overlay_shell_test.dart
    - .planning/phases/23-overlay-shell-state-model/23-CONTEXT.md
    - .planning/phases/23-overlay-shell-state-model/23-PATTERNS.md
    - .planning/phases/23-overlay-shell-state-model/23-VALIDATION.md
    - CLAUDE.md
  </read_first>
  <files>lib/ui/dialogs/settings/settings_overlay_shell.dart, test/ui/dialogs/settings_overlay_shell_test.dart</files>
  <behavior>
    - Test 1: title-bar-only drag changes dragOffset and clamps it inside MediaQuery bounds, including a window smaller than 500×400.
    - Test 2: ESC and B each close an open panel and leave a supplied fullscreen-exit observer at zero invocations.
    - Test 3: 625×500 produces 500×400; widths/heights one logical pixel below their 80%-threshold constrain only the affected dimension without integer rounding.
    - Test 4: after mask/close-button/key closure and the 200ms exit interval, the overlay is absent from the hit-test tree.
  </behavior>
  <action>Extend SettingsOverlayShell and its existing widget test file, keeping all modifications within the shell feature. Per D-09, place the only drag `GestureDetector.onPanUpdate` on the title bar and update `state.dragOffset` through `Offset` arithmetic clamped from the current MediaQuery size and the same computed panel size used by layout; do not call window-manager or WindowBridge drag APIs. Calculate panel width as `min(500.0, mediaSize.width * 0.8)` and height as `min(400.0, mediaSize.height * 0.8)` with double arithmetic, and use those exact values for both SizedBox layout and drag limits. Per D-10, wrap the panel subtree with FocusTraversalGroup and autofocus Focus; for KeyDownEvent escape or keyB call controller.close and return KeyEventResult.handled, otherwise return ignored. Expose a test-only callback parameter only if needed to prove no fullscreen-exit invocation; do not alter KeyboardHandler's global ownership. Use widget keys for shell, mask, title-bar drag region, and close button so all interaction tests target stable elements. Keep visual values expressed through Tokens and document non-obvious boundary calculations beside the implementation.</action>
  <acceptance_criteria>
    - The only `onPanUpdate` in SettingsOverlayShell is attached to its title-bar region and clamps dragOffset using MediaQuery size plus computed panel dimensions.
    - The panel is sized by `min(500.0, size.width * 0.8)` and `min(400.0, size.height * 0.8)` without conversion to integer values.
    - A FocusTraversalGroup contains an autofocus Focus whose key handler returns KeyEventResult.handled for both LogicalKeyboardKey.escape and LogicalKeyboardKey.keyB.
    - The widget test asserts ESC does not call the fullscreen-exit observer, asserts B close, tests exact 625×500 sizing plus below-threshold sizing, and verifies no closed-overlay hit target after the exit duration.
    - `flutter test test/ui/dialogs/settings_overlay_shell_test.dart -x` and `flutter analyze` both exit 0.
  </acceptance_criteria>
  <verify><automated>flutter test test/ui/dialogs/settings_overlay_shell_test.dart -x && flutter analyze</automated></verify>
  <done>All remaining PANEL-03 through PANEL-07 behaviors are covered: secure mask teardown, constrained title-bar drag, consumed ESC/B close, and exact responsive panel bounds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player content to overlay | Pointer and keyboard events move from untrusted user input into an in-tree modal shell. |
| overlay to playback service | Close and open requests alter playback only through SettingsPanelController and its injected facade. |
| window dimensions to layout | MediaQuery size controls visible geometry and drag-bound calculation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering | SettingsOverlayShell mask and teardown | low | mitigate | IgnorePointer gates closed state and conditional shell removal removes the overlay hit target; widget test proves mask close and closed hit safety for PANEL-05. |
| T-23-02 | Tampering | SettingsOverlayShell Focus.onKeyEvent | low | mitigate | KeyDown ESC/B calls controller.close and returns KeyEventResult.handled; widget test verifies fullscreen observer stays uncalled for PANEL-06. |
| T-23-03 | Tampering | SettingsOverlayShell title drag | low | mitigate | Offset calculation clamps to MediaQuery and current calculated panel dimensions, including undersized windows; widget tests exercise limits for PANEL-04/PANEL-07. |
| T-23-SC | Tampering | package installation | low | accept | No package-manager installation occurs; only Flutter SDK and already-tracked in-tree utilities are used. |
</threat_model>

<verification>
Run `flutter test test/ui/dialogs/settings_panel_state_test.dart test/ui/dialogs/settings_panel_controller_test.dart test/ui/dialogs/settings_overlay_shell_test.dart -x`, then `flutter analyze`. For visual blur only, run `flutter run -d windows`, open the settings shell, and confirm the GlassContainer blur is visible; automated tests assert widget structure rather than pixels.
</verification>

<success_criteria>
- The gear trigger opens the in-tree settings shell and the shell remains inside PlayerScreen's player-content region.
- A centered 500×400-or-80%-bounded glass panel provides title, close, mask, animated scale/fade, and no tab content.
- Drag, ESC, and B use the explicitly tested safe behaviors without click-through or fullscreen escape bubbling.
</success_criteria>

<output>
Create `.planning/phases/23-overlay-shell-state-model/23-02-SUMMARY.md` when done.
</output>
