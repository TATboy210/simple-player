---
phase: 28-settings-shell-split-legacy-deletion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/dialogs/settings/settings_overlay_shell.dart
  - lib/ui/dialogs/settings/tab_strip.dart
  - lib/ui/dialogs/settings/tab_content.dart
  - lib/ui/dialogs/settings/panel_key_bindings.dart
  - lib/ui/dialogs/settings_panel.dart
  - lib/ui/shared/settings_button.dart
  - lib/ui/dialogs/settings/general_tab.dart
  - lib/ui/dialogs/settings/shortcuts_tab.dart
autonomous: true
requirements:
  - REFAC-01
  - REFAC-02
must_haves:
  truths:
    - "The settings overlay preserves all seven tab render paths, pending deferred-apply actions, focus isolation, drag behavior, and SettingsPanelPlayback controller seam after the shell split."
    - "The maintainable shell is decomposed into tab_strip.dart, tab_content.dart, and panel_key_bindings.dart, with each extracted file under 300 lines and no visual or interaction drift."
    - "The legacy sidebar settings panel is absent from production and tests; the settings framework test suite and complete Flutter suite pass without modifying pubspec dependencies."
  artifacts:
    - path: lib/ui/dialogs/settings/tab_strip.dart
      provides: SettingsTabStrip rendering the seven existing settings navigation items.
    - path: lib/ui/dialogs/settings/tab_content.dart
      provides: SettingsTabContent retaining the seven-child IndexedStack and tab fade behavior.
    - path: lib/ui/dialogs/settings/panel_key_bindings.dart
      provides: SettingsPanelKeyBindings keyboard and gamepad event routing helpers.
    - path: lib/ui/dialogs/settings/settings_overlay_shell.dart
      provides: Reduced overlay composition, animation, sizing, focus, and drag shell.
  key_links:
    - from: lib/ui/dialogs/settings/settings_overlay_shell.dart
      to: lib/ui/dialogs/settings/tab_strip.dart
      via: SettingsTabStrip receives the selectedTab ValueNotifier and writes selected indices through the existing controller state.
    - from: lib/ui/dialogs/settings/settings_overlay_shell.dart
      to: lib/ui/dialogs/settings/tab_content.dart
      via: SettingsTabContent receives PendingSettingsState and selectedTab notifier to preserve IndexedStack tab state.
    - from: lib/ui/dialogs/settings/settings_overlay_shell.dart
      to: lib/ui/dialogs/settings/panel_key_bindings.dart
      via: root Focus delegates KeyDownEvent handling while retaining controller nextTab, prevTab, and close calls.
---

<objective>
Consolidate the active settings UI onto a focused overlay shell and remove the obsolete sidebar implementation while retaining every tested interaction.

Purpose: Make the v4.5 settings surface safe to extend without exceeding the project file-size cap or retaining two competing panel implementations.
Output: Three extracted shell collaborators, a smaller SettingsOverlayShell, and deletion of the legacy settings panel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@lib/ui/dialogs/settings/settings_overlay_shell.dart
@lib/ui/dialogs/settings/settings_panel_controller.dart
@lib/ui/dialogs/settings/pending_settings.dart
@test/ui/dialogs/settings_overlay_shell_test.dart
@test/ui/dialogs/settings_tab_content_test.dart
</context>

## Artifacts this phase produces

- `lib/ui/dialogs/settings/tab_strip.dart` — `SettingsTabStrip`, the existing seven-item horizontal tab renderer.
- `lib/ui/dialogs/settings/tab_content.dart` — `SettingsTabContent`, the seven-tab `IndexedStack` and per-tab opacity transition renderer.
- `lib/ui/dialogs/settings/panel_key_bindings.dart` — `SettingsPanelKeyBindings`, root panel keyboard/gamepad routing helpers.
- `lib/ui/dialogs/settings/settings_panel.dart` — deleted legacy sidebar panel.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extract the horizontal tab-strip path and prove tab selection end-to-end</name>
  <files>lib/ui/dialogs/settings/settings_overlay_shell.dart, lib/ui/dialogs/settings/tab_strip.dart, test/ui/dialogs/settings_overlay_shell_test.dart</files>
  <read_first>
    - lib/ui/dialogs/settings/settings_overlay_shell.dart — extract only its `_tabIcons`, `_tabLabels`, and `_buildTabBar` responsibilities after preserving the exact seven-item order and compact sizing behavior.
    - lib/ui/dialogs/settings/_settings_nav_item.dart — preserve the existing `SettingsNavItem` constructor and selected/tap behavior.
    - lib/ui/dialogs/settings/settings_panel_controller.dart — retain `state.selectedTab` as the state owner; do not introduce another notifier.
    - test/ui/dialogs/settings_overlay_shell_test.dart — retain the existing `SettingsOverlayShell` test harness and selection assertions.
  </read_first>
  <behavior>
    - A newly opened overlay renders exactly seven `SettingsNavItem` instances in the existing General, Equalizer, Audio, Video, Shortcuts, About, Performance order.
    - Tapping the item at index 3 updates `SettingsPanelController.state.selectedTab` to 3 through the extracted strip.
    - Widths below `Tokens.breakpointResponsive` use `Tokens.tabBarFontCompact` and `Tokens.tabBarSpacingCompact`; widths at or above it use their normal counterparts.
  </behavior>
  <action>Create `tab_strip.dart` with a documented public `SettingsTabStrip` widget owning the current icon/label lists and the `ValueListenableBuilder<int>`/horizontal `Row` rendering path. Its constructor must accept the existing selected-tab notifier plus an index-selection callback so `SettingsPanelController` remains the sole state owner. Move the current 56/64px container, compact font/spacing selection, seven `SettingsNavItem` entries, and tap-to-index behavior there without changing labels, icons, `Tokens.*` values, or item order. In `settings_overlay_shell.dart`, import and compose `SettingsTabStrip` from `_buildPanel`; remove the moved private lists and builder. Update only assertions in the existing shell test if extraction changes widget ownership, retaining the same open → tap tab → selected index end-to-end test path. This is the production-quality tracer for REFAC-01, not a new navigation design.</action>
  <verify>
    <automated>flutter test test/ui/dialogs/settings_overlay_shell_test.dart test/ui/dialogs/settings_responsive_scaling_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l lib/ui/dialogs/settings/tab_strip.dart` returns a value below 300.
    - `flutter test test/ui/dialogs/settings_overlay_shell_test.dart test/ui/dialogs/settings_responsive_scaling_test.dart` exits 0.
    - The active shell still renders seven `SettingsNavItem` widgets and tab index 3 is selectable through a tap.
  </acceptance_criteria>
  <done>The tab-strip extraction is wired through the live overlay and its existing responsive, selection, and lifecycle checks are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract tab-content and root-key routing collaborators without changing shell behavior</name>
  <files>lib/ui/dialogs/settings/settings_overlay_shell.dart, lib/ui/dialogs/settings/tab_content.dart, lib/ui/dialogs/settings/panel_key_bindings.dart, test/ui/dialogs/settings_tab_content_test.dart, test/ui/dialogs/settings_focus_navigation_test.dart</files>
  <read_first>
    - lib/ui/dialogs/settings/settings_overlay_shell.dart — extract the existing `_buildContent`, `_handleKeyEvent`, shoulder-key helpers, and their direct dependencies only; leave lifecycle, animation, sizing, focus ownership, title drag, and button bar in the shell.
    - lib/ui/dialogs/settings/tab_strip.dart — preserve the tracer's selected-tab interface and avoid a second tab state path.
    - lib/ui/dialogs/settings/settings_panel_controller.dart — preserve `PendingSettingsState`, `nextTab`, `prevTab`, and `close` calls exactly.
    - lib/ui/dialogs/settings/pending_settings.dart — retain pure deferred-apply ownership and `IndexedStack` tab persistence.
    - test/ui/dialogs/settings_tab_content_test.dart and test/ui/dialogs/settings_focus_navigation_test.dart — retain all seven content, IndexedStack, ESC/B, arrow, and shoulder-button assertions.
  </read_first>
  <behavior>
    - `SettingsTabContent` preserves all seven current tab widgets in a seven-child `IndexedStack`; selecting a tab changes the stack index without disposing other tab widgets.
    - The existing 200ms per-tab opacity transition and `Tokens.spMd` content padding remain in the active overlay.
    - The root `Focus` consumes existing ESC, B, left/right arrow, gameButton12, and gameButton13 KeyDown events through the unchanged controller methods; KeyUp remains ignored.
  </behavior>
  <action>Create `tab_content.dart` with a documented public `SettingsTabContent` widget that accepts the selected-tab `ValueListenable<int>` and the existing `PendingSettingsState`; move the current colored content backing, padding, seven explicit tab children, and `TweenAnimationBuilder<double>` opacity wrappers unchanged. Create `panel_key_bindings.dart` with a documented public `SettingsPanelKeyBindings` helper that receives a `SettingsPanelController` and returns the current `KeyEventResult`; retain ESC/B close, arrow previous/next, and gameButton13/gameButton12 routing. Keep the root `Focus` in `SettingsOverlayShell`, delegate its handler to that helper, and do not change focus traversal, controller APIs, `ValueNotifier` ownership, drag bounds, panel sizing, animation duration, or deferred-apply button actions. Adapt existing test assertions only where extracted public widget boundaries require it; all observed behavior stays the same. This completes REFAC-01 without changing visual tokens or adding packages.</action>
  <verify>
    <automated>flutter test test/ui/dialogs/settings_tab_content_test.dart test/ui/dialogs/settings_focus_navigation_test.dart test/ui/dialogs/settings_responsive_integration_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l lib/ui/dialogs/settings/tab_content.dart` and `wc -l lib/ui/dialogs/settings/panel_key_bindings.dart` each return values below 300.
    - `wc -l lib/ui/dialogs/settings/settings_overlay_shell.dart` returns a value below 500.
    - `flutter test test/ui/dialogs/settings_tab_content_test.dart test/ui/dialogs/settings_focus_navigation_test.dart test/ui/dialogs/settings_responsive_integration_test.dart` exits 0.
    - The active tree retains one seven-child `IndexedStack`, three `SettingsButton` actions, a `FocusTraversalGroup`, and a root `Focus` whose recognized KeyDown events are handled.
  </acceptance_criteria>
  <done>The smaller shell delegates content and key routing to focused files while all existing tabs, pending settings behavior, focus isolation, and controller seam remain intact.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the retired sidebar panel and certify framework-only coverage</name>
  <reversibility rating="costly">The retired panel can be restored from git, but reversal would reintroduce a competing UI path and coordinated stale documentation/tests.</reversibility>
  <files>lib/ui/dialogs/settings_panel.dart, lib/ui/shared/settings_button.dart, lib/ui/dialogs/settings/general_tab.dart, lib/ui/dialogs/settings/shortcuts_tab.dart</files>
  <read_first>
    - lib/ui/dialogs/settings_panel.dart — compare its responsibilities with the active `settings/` framework before deletion and confirm it has no unique caller-required behavior.
    - lib/ui/dialogs/settings/settings_overlay_shell.dart — confirm the active shell owns the maintained overlay path.
    - lib/ui/shared/settings_button.dart — remove or revise only comments that identify the retired file as its source while preserving the reusable component API.
    - lib/ui/dialogs/settings/general_tab.dart and lib/ui/dialogs/settings/shortcuts_tab.dart — revise stale documentation references to describe the active controller/pending-settings framework.
    - test/ui/dialogs/settings_overlay_shell_test.dart and test/ui/dialogs/settings_tab_content_test.dart — confirm the maintained framework test path covers panel lifecycle and seven-tab rendering.
  </read_first>
  <action>Delete `lib/ui/dialogs/settings_panel.dart` after confirming the live application uses `SettingsOverlayShell` through `PlayerScreen` and no import or constructor call targets the retired widget. Keep all active settings tests on the `test/ui/dialogs/settings_*_test.dart` framework path; no legacy-only test file should remain because the repository search identifies no caller outside the retired declaration. Update stale source comments in `settings_button.dart`, `general_tab.dart`, and `shortcuts_tab.dart` so they refer to the current settings framework rather than the removed widget, without altering their runtime behavior. Do not edit `pubspec.yaml`, add dependencies, alter `SettingsPanelController`, or change tokens/visual layout. This implements REFAC-02 and preserves the control-bar alignment baseline for later phases.</action>
  <verify>
    <automated>grep -r "SettingsPanel(" lib/ test/; test $? -eq 1 &amp;&amp; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e lib/ui/dialogs/settings_panel.dart` succeeds.
    - `grep -r "SettingsPanel(" lib/ test/` returns zero production callers.
    - `flutter test test/ui/dialogs/` exits 0 and `flutter test` exits 0.
    - `test -z "$(git diff -- pubspec.yaml)"` succeeds, demonstrating no dependency additions or other pubspec changes in this phase.
  </acceptance_criteria>
  <done>The only settings panel implementation is the extracted overlay framework; legacy source and stale references are removed, and settings plus full-suite checks are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PlayerScreen to SettingsOverlayShell | Existing in-process controller state enters the retained settings UI; this refactor must preserve focus and pending-state routing. |
| Settings UI to SettingsPanelPlayback | Existing narrow playback seam remains the sole UI-to-kernel boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | `settings_overlay_shell.dart` extraction | low | mitigate | Preserve controller-owned state and cover tab selection, pending actions, and key handling with existing widget tests. |
| T-28-02 | Denial of service | Root focus/key routing | low | mitigate | Retain root `Focus` event consumption and execute focus-navigation widget tests to prevent keys reaching player controls. |
| T-28-03 | Elevation of privilege | SettingsPanelPlayback seam | low | accept | No new kernel API, input boundary, permission, external API, or file-system access is introduced; the existing narrow seam is retained. |
| T-28-SC | Tampering | package-manager operations | low | accept | This phase installs no package and must leave `pubspec.yaml` unchanged. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Split the shell and remove the legacy panel before v4.5 features | 28-01 | COVERED | Tasks 1–3 deliver the complete prerequisite refactor. |
| REQ | REFAC-01 | Extract the three named subfiles with unchanged behavior and green suite | 28-01 | COVERED | Tasks 1–2 plus per-file and widget-test gates. |
| REQ | REFAC-02 | Delete legacy panel and retain only framework tests/callers | 28-01 | COVERED | Task 3 deletion, caller search, and complete-suite gates. |
| RESEARCH | Cross-Cutting Theme 4 | Split before adding v4.5 features; avoid shell bloat | 28-01 | COVERED | Three focused collaborators are extracted before later work. |
| RESEARCH | Pitfall 3 / Cross-Cutting Risk 2 | Preserve single-glass and control-bar alignment baseline | 28-01 | COVERED | Tasks explicitly retain token/layout behavior without visual changes. |
| CONTEXT | — | No phase CONTEXT.md decisions supplied | 28-01 | COVERED | No locked decisions apply. |

<verification>
1. Run focused settings widget tests after each extraction task.
2. Run `flutter test test/ui/dialogs/` and then the complete `flutter test` suite after legacy removal.
3. Confirm every extracted file is below 300 lines, the coordinating shell below 500 lines, and the retired source file no longer exists.
4. Confirm the caller search returns no results and `pubspec.yaml` has no phase diff.
</verification>

<success_criteria>
- `tab_strip.dart`, `tab_content.dart`, and `panel_key_bindings.dart` exist, each below 300 lines, and the active overlay remains behaviorally unchanged under its existing settings tests.
- `settings_panel.dart` no longer exists and no `SettingsPanel(` caller remains under `lib/` or `test/`.
- `flutter test` succeeds with no new dependency changes.
</success_criteria>

<output>
Create `.planning/phases/28-settings-shell-split-legacy-deletion/28-01-SUMMARY.md` when done.
</output>
