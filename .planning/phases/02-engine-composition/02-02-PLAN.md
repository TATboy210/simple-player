---
phase: 02-engine-composition
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/kernel/engine/fvp_engine.dart
autonomous: true
requirements:
  - COMP-04
  - COMP-05
must_haves:
  truths:
    - FvpEngine.setVolume() delegates to _volumeController.setVolume()
    - FvpEngine.setMute() delegates to _volumeController.setMute()
    - FvpEngine.setExternalSubtitle() delegates to _subtitleConfigurator.setExternalSubtitle()
    - FvpEngine.setSubtitleDelay() delegates to _subtitleConfigurator.setSubtitleDelay()
    - FvpEngine.subtitleDelay getter delegates to _subtitleConfigurator.getSubtitleDelay()
    - FvpEngine.setEqualizer() delegates to _subtitleConfigurator.setEqualizer()
    - FvpEngine._applyD3d11Defaults() replaced by _d3d11Configurator.applyDefaults()
    - FvpEngine.setD3d11SyncEnabled() delegates to _d3d11Configurator.setSyncEnabled()
    - FvpEngine.setHardwareDecoding() delegates to _d3d11Configurator.setHardwareDecoding()
    - ValueNotifier fields remain as FvpEngine final fields (not moved to helpers)
    - _guardedAction pattern remains in FvpEngine (not moved to helpers)
    - D3D11 properties set in _createPlayer() before return (PLAT-01)
  artifacts:
    - lib/kernel/engine/fvp_engine.dart (delegated, ~480 lines)
  key_links:
    - _volumeController constructed in _createPlayer() with volume and isMuted references
    - _subtitleConfigurator constructed in _createPlayer() with player reference only
    - _d3d11Configurator constructed in _createPlayer() with player reference only
    - _applyD3d11Defaults() private method deleted and replaced by _d3d11Configurator.applyDefaults()
    - Constants _defaultVideoDecoders, _ffmpegDecoderThreads, _maxBufferFrames removed from FvpEngine
---

<objective>
Wire delegation in FvpEngine: replace inline volume/subtitle/D3D11 logic with helper calls, remove dead code, verify all tests pass.

Purpose: Complete the engine composition refactoring. FvpEngine becomes a delegation hub that owns ValueNotifiers and lifecycle guards, while helpers own domain logic. This reduces FvpEngine from ~553 to ~480 lines.

Output: Refactored FvpEngine with delegation wired, all constants moved to D3D11Configurator, _applyD3d11Defaults removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-engine-composition/02-CONTEXT.md
@.planning/phases/02-engine-composition/02-RESEARCH.md
@.planning/phases/02-engine-composition/02-01-SUMMARY.md
@lib/kernel/engine/fvp_engine.dart
@lib/kernel/engine/volume_controller.dart
@lib/kernel/engine/subtitle_configurator.dart
@lib/kernel/engine/d3d11_configurator.dart
@lib/kernel/bridge/display_config.dart
@lib/kernel/engine/player_engine_base.dart
@lib/kernel/engine/mock_engine.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire delegation and remove dead code from FvpEngine</name>
  <files>lib/kernel/engine/fvp_engine.dart</files>
  <read_first>
    - lib/kernel/engine/fvp_engine.dart (full 553-line file — read once, extract all patterns)
    - lib/kernel/engine/volume_controller.dart (constructor signature: _player, volume, isMuted)
    - lib/kernel/engine/subtitle_configurator.dart (constructor signature: _player)
    - lib/kernel/engine/d3d11_configurator.dart (expanded version from Plan 02-01)
    - lib/kernel/bridge/display_config.dart (DisplayConfig.d3d11SyncMode() static call)
  </read_first>
  <action>
Refactor FvpEngine to delegate to the 3 helper classes. This is the core wiring task for Phase 2.

**Step 1: Add imports (top of file)**
Add after existing imports:
- `import 'volume_controller.dart';`
- `import 'subtitle_configurator.dart';`
- `import 'd3d11_configurator.dart';`

Remove import `../bridge/display_config.dart` (no longer needed — D3D11Configurator handles it internally).

**Step 2: Add helper late fields**
After the existing `late VideoEffectController _videoEffectController;` line, add:
- `late VolumeController _volumeController;`
- `late SubtitleConfigurator _subtitleConfigurator;`
- `late D3D11Configurator _d3d11Configurator;`

**Step 3: Remove constants that moved to D3D11Configurator**
Remove these 3 constants entirely (they now live in D3D11Configurator):
- `static const _defaultVideoDecoders = 'D3D11:shader_resource=1,NVDEC,FFmpeg';`
- `static const _ffmpegDecoderThreads = '2';`
- `static const _maxBufferFrames = '3';`

Keep `_minPlaybackRate` and `_maxPlaybackRate` — those are FvpEngine's own playback speed constraints.

**Step 4: Wire helpers in _createPlayer()**
After `_mediaOpener = MediaOpener(p, _trackManager);` (line 131), add:
- `_volumeController = VolumeController(p, volume: volume, isMuted: isMuted);`
- `_subtitleConfigurator = SubtitleConfigurator(p);`
- `_d3d11Configurator = D3D11Configurator(p);`

**Step 5: Replace _applyD3d11Defaults call**
In _createPlayer(), replace `// D3D11 性能参数 — 在 init 后、open 前设置` and `_applyD3d11Defaults(p);` (lines 136-138) with:
`_d3d11Configurator.applyDefaults();`

Keep the comment above it (or update to `// D3D11 性能参数 via D3D11Configurator — 在 init 后、open 前设置`).

**Step 6: Delete _applyD3d11Defaults method**
Remove the entire `_applyD3d11Defaults(mdk.Player p)` method (lines 146-178, ~33 lines). This method is fully replaced by D3D11Configurator.applyDefaults().

**Step 7: Delegate setVolume**
Replace the body of `setVolume(double value)` (lines 329-342) with:
```dart
@override
void setVolume(double value) {
  _guardedAction('setVolume', () {
    _volumeController.setVolume(value);
  });
}
```

**Step 8: Delegate setMute**
Replace the body of `setMute(bool mute)` (lines 345-350) with:
```dart
@override
void setMute(bool mute) {
  _guardedAction('setMute', () {
    _volumeController.setMute(mute);
  });
}
```

**Step 9: Delegate setExternalSubtitle**
Replace the body of `setExternalSubtitle(String path)` (lines 435-439) with:
```dart
@override
void setExternalSubtitle(String path) {
  _guardedAction('setExternalSubtitle', () {
    _subtitleConfigurator.setExternalSubtitle(path);
  });
}
```

**Step 10: Delegate setSubtitleDelay**
Replace the body of `setSubtitleDelay(int milliseconds)` (lines 444-448) with:
```dart
@override
void setSubtitleDelay(int milliseconds) {
  _guardedAction('setSubtitleDelay', () {
    _subtitleConfigurator.setSubtitleDelay(milliseconds);
  });
}
```

**Step 11: Delegate subtitleDelay getter**
Replace the body of `int get subtitleDelay` (lines 451-459) with:
```dart
@override
int get subtitleDelay {
  if (_disposed) return 0;
  return _subtitleConfigurator.getSubtitleDelay();
}
```

**Step 12: Delegate setEqualizer**
Replace the body of `setEqualizer(String afFilter)` (lines 464-468) with:
```dart
@override
void setEqualizer(String afFilter) {
  _guardedAction('setEqualizer', () {
    _subtitleConfigurator.setEqualizer(afFilter);
  });
}
```

**Step 13: Delegate setD3d11SyncEnabled**
Replace the body of `setD3d11SyncEnabled(bool enabled)` (lines 503-509) with:
```dart
@override
void setD3d11SyncEnabled(bool enabled) {
  _guardedAction('setD3d11SyncEnabled', () {
    _d3d11Configurator.setSyncEnabled(enabled);
  });
}
```

**Step 14: Delegate setHardwareDecoding**
Replace the body of `setHardwareDecoding(bool enabled)` (lines 512-525) with:
```dart
@override
void setHardwareDecoding(bool enabled) {
  _guardedAction('setHardwareDecoding', () {
    _d3d11Configurator.setHardwareDecoding(enabled);
  });
}
```

**Step 15: Update class doc comment**
Update the class doc comment (lines 25-33) to reflect the new helper composition. Add volume_controller, subtitle_configurator, d3d11_configurator to the list of helpers.

**Step 16: Verify dispose() is unchanged**
The dispose() method (lines 530-552) does NOT need changes. Helpers don't own ValueNotifiers, so there's nothing extra to dispose. Confirm this is the case — do not add helper disposal.

CRITICAL CONSTRAINTS per CONTEXT.md decisions:
- ValueNotifier fields MUST remain as FvpEngine `final` fields (COMP-05)
- `_guardedAction` MUST remain in FvpEngine (Decision 2)
- Helpers receive ValueNotifier references via constructor, not ownership
- Do NOT change PlayerEngine interface (no new methods, no signature changes)
- Do NOT change MockEngine (438 lines unchanged)
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter analyze lib/kernel/engine/fvp_engine.dart 2>&1 | grep -c "error" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/kernel/engine/fvp_engine.dart` reports 0 errors
    - FvpEngine imports volume_controller.dart, subtitle_configurator.dart, d3d11_configurator.dart
    - FvpEngine does NOT import display_config.dart (moved to D3D11Configurator)
    - FvpEngine has `_volumeController`, `_subtitleConfigurator`, `_d3d11Configurator` late fields
    - FvpEngine does NOT have `_defaultVideoDecoders`, `_ffmpegDecoderThreads`, `_maxBufferFrames` constants
    - FvpEngine does NOT have `_applyD3d11Defaults()` private method
    - `_createPlayer()` calls `_d3d11Configurator.applyDefaults()` (not `_applyD3d11Defaults(p)`)
    - `_createPlayer()` constructs all 3 helpers before calling applyDefaults()
    - `setVolume()` body is `_guardedAction('setVolume', () { _volumeController.setVolume(value); });`
    - `setMute()` body is `_guardedAction('setMute', () { _volumeController.setMute(mute); });`
    - `setExternalSubtitle()` delegates to `_subtitleConfigurator.setExternalSubtitle(path)`
    - `setSubtitleDelay()` delegates to `_subtitleConfigurator.setSubtitleDelay(milliseconds)`
    - `subtitleDelay` getter delegates to `_subtitleConfigurator.getSubtitleDelay()`
    - `setEqualizer()` delegates to `_subtitleConfigurator.setEqualizer(afFilter)`
    - `setD3d11SyncEnabled()` delegates to `_d3d11Configurator.setSyncEnabled(enabled)`
    - `setHardwareDecoding()` delegates to `_d3d11Configurator.setHardwareDecoding(enabled)`
    - ValueNotifier fields remain as FvpEngine `final` fields (not moved)
    - `_guardedAction` remains in FvpEngine (not in helpers)
    - PlayerEngine interface unchanged (no new abstract methods)
    - MockEngine unchanged (438 lines)
    - FvpEngine line count is between 450-500 (reduced from 553)
  </acceptance_criteria>
  <done>FvpEngine delegates all volume/subtitle/D3D11 logic to helpers. Inline logic removed, constants moved to D3D11Configurator, _applyD3d11Defaults deleted. ValueNotifier ownership unchanged. _guardedAction unchanged. PlayerEngine interface unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite passes with delegation wiring</name>
  <files>
    lib/kernel/engine/fvp_engine.dart (verify only, no changes)
  </files>
  <read_first>
    - lib/kernel/engine/fvp_engine.dart (verify delegation is correct)
    - test/helpers/fake_engine.dart (FakeEngine — should be unchanged)
    - lib/kernel/engine/mock_engine.dart (MockEngine — should be unchanged)
  </read_first>
  <action>
Run the complete test suite to verify no regressions after delegation wiring.

This is a verification-only task. No code changes expected. If tests fail, the executor must diagnose and fix the issue in fvp_engine.dart (not in test files or helpers).

Key verification points:
1. `flutter test` — all existing tests pass (FakeEngine implements PlayerEngine, not FvpEngine)
2. `flutter analyze` — 0 errors, 0 warnings across entire project
3. Verify FvpEngine line count: should be between 450-500 lines (down from 553)
4. Verify MockEngine is unchanged (no new imports, no signature changes)
5. Verify PlayerEngine interface is unchanged (no new abstract methods)

If any test fails:
- Check that delegation methods match the original inline behavior exactly
- Check that D3D11Configurator.applyDefaults() is called in _createPlayer() before return
- Check that ValueNotifier ownership is preserved (final fields, not getters)
- Do NOT modify test files — the issue is in the production code

Run commands in order:
1. `flutter analyze` — full project analysis
2. `flutter test` — full test suite
3. `wc -l lib/kernel/engine/fvp_engine.dart` or equivalent line count check
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test` passes with 0 failures
    - `flutter analyze` reports 0 errors, 0 warnings
    - FvpEngine line count between 450-500 (reduced from 553, target ~480)
    - MockEngine unchanged (no modifications detected by git diff)
    - PlayerEngine interface unchanged (no new abstract methods)
    - ValueNotifier fields remain as FvpEngine `final` fields
    - D3D11 properties set in _createPlayer() before return (PLAT-01 timing preserved)
  </acceptance_criteria>
  <done>All tests pass. FvpEngine refactored to ~480 lines. No regressions. ValueNotifier ownership preserved. MockEngine unchanged. PlayerEngine interface unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FvpEngine._guardedAction -> helper.method() | Lifecycle guard must wrap all delegated calls — disposed check + error handling |
| Helper._player.setProperty -> mdk engine | Property values flow from helper to MDK — validated by existing D3D11 defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Elevation of Privilege | _guardedAction bypass | medium | mitigate | Every delegated method must go through _guardedAction — never call helper directly |
| T-02-04 | Denial of Service | Helper construction before _player ready | low | mitigate | Helpers constructed in _createPlayer() after mdk.Player() creation, before applyDefaults() |
| T-02-05 | Tampering | ValueNotifier ownership moved to helpers | critical | mitigate | COMP-05 constraint: ValueNotifier fields remain as FvpEngine final fields — grep verification |
</threat_model>

<verification>
1. `flutter analyze` — 0 errors, 0 warnings
2. `flutter test` — full suite passes
3. FvpEngine line count: 450-500 (reduced from 553)
4. `grep -c "ValueNotifier" lib/kernel/engine/fvp_engine.dart` — confirms ValueNotifier ownership
5. `grep -c "_guardedAction" lib/kernel/engine/fvp_engine.dart` — confirms guard pattern retained
6. `grep -c "_volumeController" lib/kernel/engine/fvp_engine.dart` — confirms delegation wiring
7. `grep -c "_subtitleConfigurator" lib/kernel/engine/fvp_engine.dart` — confirms delegation wiring
8. `grep -c "_d3d11Configurator" lib/kernel/engine/fvp_engine.dart` — confirms delegation wiring
9. `grep "_applyD3d11Defaults" lib/kernel/engine/fvp_engine.dart` — confirms method removed (0 matches)
10. `grep "_defaultVideoDecoders" lib/kernel/engine/fvp_engine.dart` — confirms constant removed (0 matches)
</verification>

<success_criteria>
- FvpEngine delegates all volume/subtitle/D3D11 logic to helpers (9 delegation points)
- FvpEngine reduced from ~553 to ~480 lines
- ValueNotifier fields remain as FvpEngine final fields (COMP-05)
- _guardedAction remains in FvpEngine (Decision 2)
- D3D11 properties set before open() via _d3d11Configurator.applyDefaults() (PLAT-01)
- PlayerEngine interface unchanged
- MockEngine unchanged
- All tests pass with 0 failures
- flutter analyze 0 errors, 0 warnings
</success_criteria>

<output>
Create `.planning/phases/02-engine-composition/02-02-SUMMARY.md` when done
</output>
