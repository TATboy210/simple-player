---
context: phase
phase: 25-performance-quick-wins
task: 0
total_tasks: 2
status: in_progress
last_updated: 2026-07-06
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Pitfall 2: resize 期间 decoration 竞争 | resize 动画进行时 engine state 变化触发 decoration 动画，导致 controller 竞争 | blocking | _onEngineStateChanged 中检查 _isResizing 标志，resize 期间忽略 state 变化 |
| Pitfall 5: SingleTickerProviderStateMixin 限制 | AutoHideController 已占用唯一 ticker，新增 controller 会抛异常 | blocking | ControlsOverlay 必须切换到 TickerProviderStateMixin |

<current_state>
Phase 25 规划完成。1 plan (25-01-PLAN.md)，2 tasks，尚未执行任何任务。
研究(HIGH)→规划→验证 全部通过。VALIDATION.md 已创建。准备好执行。
</current_state>

<completed_work>

Phase 25 规划阶段全部完成：
- ✅ 25-CONTEXT.md — 18 项锁定决策 (D-01~D-18)
- ✅ 25-RESEARCH.md — 技术研究 (ticker 约束、BoxShadow 对齐、blur 缓存、色值近似)
- ✅ 25-VALIDATION.md — 验证策略 (测试映射、采样频率、手动验证项)
- ✅ 25-01-PLAN.md — 执行计划 (2 tasks, 5 files)

</completed_work>

<remaining_work>

- Task 1: Tokens 常量 + GlassTier blur 缓存 (tokens.dart, glass_container.dart)
  - 新增 Tokens.tapJitterThreshold = 18.0
  - 修改 controlBarBorderWhite → 0x1A6496FF, controlBarBorderIdle → 0x0D6496FF
  - GlassTier 新增 blurFilter getter + static final _thinBlur/_normalBlur
  - GlassContainer._buildBlurContent 改用 tier.blurFilter
- Task 2: ControlsOverlay 动画 + ControlBar/PlaylistPanel 缓存清理
  - ControlsOverlay: SingleTickerProviderStateMixin → TickerProviderStateMixin
  - 新增 _animController (150ms) + _resizeOpacity (easeOut)
  - _onResizeChanged: resizing true→reverse(), false→forward()
  - ControlBar: getter → static final decoration + DecorationTween
  - ControlBar/PlaylistPanel: 删除 _blurFilter，改用 GlassTier.normal.blurFilter

</remaining_work>

<decisions_made>

- SingleTickerProviderStateMixin → TickerProviderStateMixin (AutoHideController 已占用唯一 ticker)
- 共享 AnimationController 驱动 resize + decoration (resize 优先级更高)
- controlBarBorderWhite 从 0x1AFFFFFF → 0x1A6496FF (淡蓝辉光)
- controlBarBorderIdle 从 0x0DFFFFFF → 0x0D6496FF (5% 淡蓝)
- thick 与 normal 共享 blurFilter 实例 (sigma 都是 10)

</decisions_made>

<blockers>
None — 所有 checker issues 已修复
</blockers>

## Required Reading (in order)
1. `.planning/phases/25-performance-quick-wins/25-CONTEXT.md` — 18 项锁定决策
2. `.planning/phases/25-performance-quick-wins/25-RESEARCH.md` — 技术研究 + 5 个 pitfall
3. `.planning/phases/25-performance-quick-wins/25-01-PLAN.md` — 执行计划 (2 tasks)
4. `./CLAUDE.md` — 项目约定 (Tokens 设计系统、注释规范、ValueNotifier 模式)

## Infrastructure State
- Flutter SDK: 已安装
- dart:ui ImageFilter: SDK 内置，不可变对象
- Tokens.durationNormal: 已存在 (value 150)
- Tokens.glassBlur/glassBlurThin: 已存在

<context>
Phase 25 是 v1.6 的第一个阶段。规划流程：discuss-phase (25-CONTEXT.md) → research (25-RESEARCH.md) → plan (25-01-PLAN.md) → verify (plan-checker: 1 blocker + 2 warnings, 全部修复) → VALIDATION.md 创建。
关键发现：ControlsOverlay 的 SingleTickerProviderStateMixin 必须改为 TickerProviderStateMixin，否则新增 AnimationController 会抛 ticker 异常。
建议在新会话中执行，当前 context 使用率约 78%。
</context>

<next_action>
运行 /gsd-execute-phase 25 开始执行 Task 1 (Tokens + GlassTier blur 缓存)
</next_action>
