---
phase: 25-performance-quick-wins
plan: 02
type: gap_closure
wave: 1
depends_on: [25-01]
files_modified:
  - lib/ui/player/control_bar.dart
  - lib/ui/player/controls_overlay.dart
  - lib/ui/theme/tokens.dart
autonomous: true
gap_closure: true
requirements:
  - PERF-01
  - PERF-04

must_haves:
  truths:
    - "窗口 resize 开始时毛玻璃以 150ms easeOut 渐变淡出（非二元跳变）"
    - "窗口 resize 结束时毛玻璃以 150ms easeOut 渐变淡入"
    - "engine 状态变化（idle↔playing）驱动 _animController 切换 decoration"
    - "resize 期间 engine 状态变化被 _isResizing 标志忽略"
    - "controlBarBorderIdle 常量存在且与 controlBarBorderWhite 不同"
    - "_decorationIdle 使用 controlBarBorderIdle 而非 controlBarBorderWhite"
    - "tapJitterThreshold 在 controls_overlay.dart 中被消费或标记为 future-use"
  artifacts:
    - lib/ui/player/control_bar.dart — 移除 resizing AnimatedBuilder 的二元跳过，改用 opacity 渐变
    - lib/ui/player/controls_overlay.dart — _onEngineStateChanged 驱动 _animController
    - lib/ui/theme/tokens.dart — 新增 controlBarBorderIdle 常量

key-decisions:
  - "Fade-out 方案：移除 ControlBar 中 resizing AnimatedBuilder 的二元跳过（line 183-192），统一由 _animController 的 opacity 渐变控制。resize 开始时 _animController.reverse() 渐变到 0，opacity < 0.01 时 _buildBlur 自动跳过 BackdropFilter"
  - "Decoration 状态切换：_onEngineStateChanged 中检测 idle↔playing 变化，idle→playing 触发 _animController.forward()，playing→idle 触发 _animController.reverse()。但需注意 resize 优先级：_isResizing=true 时跳过"
  - "controlBarBorderIdle 色值：0x056496FF（2% 淡蓝，比 playing 的 0x0A 更淡），per D-18 playing 稍亮 idle 稍淡"
  - "tapJitterThreshold 消费：由于 controls_overlay.dart 使用 GestureDetector.onTap（无 _onPointerUp），该常量标记为 future-use 并在 TODO 注释中说明"
---

<objective>
修复 Phase 25 VERIFICATION.md 中发现的 4 个 gap：
1. Resize fade-out 二元跳变 → 统一由 opacity 渐变控制
2. Decoration 状态切换未连接 → _onEngineStateChanged 驱动 _animController
3. controlBarBorderIdle 常量缺失 → 新增常量并更新 _decorationIdle
4. tapJitterThreshold 未消费 → 标记为 future-use（worktree 结构不支持直接消费）

Purpose: 让 Phase 25 的 4 个性能优化全部达到 ROADMAP 成功标准。
Output: 3 个文件修改完成，flutter analyze 无新增 warning，所有现有测试通过。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-performance-quick-wins/25-VERIFICATION.md
@.planning/phases/25-performance-quick-wins/25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 resize fade-out 二元跳变 + 新增 controlBarBorderIdle</name>
  <files>lib/ui/player/control_bar.dart, lib/ui/theme/tokens.dart</files>
  <read_first>
    - lib/ui/player/control_bar.dart (完整读取，理解 resizing AnimatedBuilder 逻辑和 _buildBlur)
    - lib/ui/theme/tokens.dart (完整读取，定位 controlBarBorderWhite 常量位置)
    - .planning/phases/25-performance-quick-wins/25-VERIFICATION.md (Gap 1 和 Gap 3 详情)
  </read_first>
  <action>
    **1a. tokens.dart — 新增 controlBarBorderIdle 常量**

    在 `controlBarBorderWhite` (line 48) 之后新增：
    - `static const controlBarBorderIdle = Color(0x056496FF);`
    - 注释：`/// 2% 淡蓝描边（idle 状态，比 playing 的 10% 更淡，per D-18）`

    **1b. control_bar.dart — 移除 resizing 二元跳过，统一由 opacity 渐变控制**

    当前问题：`build()` 方法 line 183-192 的 `AnimatedBuilder` 监听 `resizing`，当 `resizing.value==true` 时立即返回 `RepaintBoundary`（无 BackdropFilter），在 `_animController.reverse()` 还没产生可见帧之前就短路了。

    修复方案：移除这段 resizing AnimatedBuilder，让 `_buildBlur` 中已有的 opacity 渐变逻辑统一控制 fade-out。

    具体修改：
    - 删除 line 182-192 的 resizing AnimatedBuilder 块
    - 替换为直接调用 `_buildBlur(content)`（与无 resizing 参数时相同）
    - 更新注释：`// opacity 渐变由 _animController 驱动（ControlsOverlay 传入），_buildBlur 中 opacity < 0.01 时自动跳过 BackdropFilter`
    - `resizing` 参数保留（仍传递给 OsdOverlay 等其他组件），但 ControlBar 不再用它做二元跳过

    **1c. control_bar.dart — _decorationIdle 使用 controlBarBorderIdle**

    修改 `_decorationIdle` (line 38-49)：
    - `border: Border.all(color: Tokens.controlBarBorderWhite, width: 1)` 改为 `border: Border.all(color: Tokens.controlBarBorderIdle, width: 1)`
    - 第一个 BoxShadow 的 color 从 `Tokens.controlBarBorderWhite` 改为 `Tokens.controlBarBorderIdle`
    - 注释更新：`/// 空状态装饰 — 2% 淡蓝描边（idle，比 playing 淡，per D-18）`
  </action>
  <verify>
    <automated>cd /d D:\simple_player_flutter && flutter analyze lib/ui/player/control_bar.dart lib/ui/theme/tokens.dart</automated>
  </verify>
  <acceptance_criteria>
    - tokens.dart 包含 `static const controlBarBorderIdle = Color(0x056496FF);`
    - control_bar.dart 的 `_decorationIdle` 使用 `Tokens.controlBarBorderIdle`（非 controlBarBorderWhite）
    - control_bar.dart 的 `build()` 方法不再有 resizing AnimatedBuilder 二元跳过逻辑
    - control_bar.dart 的 `_buildBlur` 仍正常处理 opacity 渐变（opacity < 0.01 时跳过 BackdropFilter）
    - `flutter analyze` 无新增 warning/error
  </acceptance_criteria>
  <done>
    tokens.dart 新增 controlBarBorderIdle 常量，control_bar.dart 移除 resize 二元跳过改由 opacity 渐变统一控制，_decorationIdle 使用淡蓝描边区分 idle/playing 状态。
  </done>
</task>

<task type="auto">
  <name>Task 2: 连接 decoration 状态切换 + tapJitterThreshold 标记</name>
  <files>lib/ui/player/controls_overlay.dart</files>
  <read_first>
    - lib/ui/player/controls_overlay.dart (完整读取，理解 _onEngineStateChanged 和 _animController)
    - .planning/phases/25-performance-quick-wins/25-VERIFICATION.md (Gap 2 和 Gap 4 详情)
  </read_first>
  <action>
    **2a. _onEngineStateChanged — 驱动 _animController 进行 decoration 状态切换**

    当前 `_onEngineStateChanged` (line 143-147) 只调用 `_autoHide.onEngineStateChanged()`，不驱动 `_animController`。

    修改方案：在 `_onEngineStateChanged` 中检测 idle↔playing 变化，驱动 _animController。

    具体修改：
    - 在 `_onEngineStateChanged` 方法中，`_autoHide.onEngineStateChanged()` 之后新增 decoration 状态切换逻辑
    - idle 时 `_animController.reverse()`（渐变到 idle 装饰）
    - playing 时 `_animController.forward()`（渐变到 playing 装饰）
    - `_isResizing` 守卫已在方法开头（line 145），resize 期间会跳过这些逻辑

    **时序修复**：在 `_onResizeChanged` 的 `forward()` 分支中检查 engine 状态：
    - resize 结束时如果 engine 是 idle → `_animController.reverse()`（恢复到 idle 装饰）
    - resize 结束时如果 engine 是 playing → `_animController.forward()`（恢复到 playing 装饰）

    **2b. tapJitterThreshold — 标记为 future-use**

    由于 controls_overlay.dart 使用 `GestureDetector.onTap`（无 `_onPointerUp` 阈值逻辑），`Tokens.tapJitterThreshold` 无法在当前结构中直接消费。

    在 `_handleTap` 方法上方添加 TODO 注释说明未来用法。
  </action>
  <verify>
    <automated>cd /d D:\simple_player_flutter && flutter analyze lib/ui/player/controls_overlay.dart</automated>
  </verify>
  <acceptance_criteria>
    - controls_overlay.dart 的 `_onEngineStateChanged` 方法包含 `_animController` 调用
    - controls_overlay.dart 的 `_onResizeChanged` 在 resize 结束时检查 engine 状态决定 forward/reverse
    - controls_overlay.dart 的 `_handleTap` 方法附近有 TODO 注释说明 tapJitterThreshold 未来用法
    - `_isResizing` 守卫仍然保护 resize 期间不触发动画
    - `flutter analyze` 无新增 warning/error
  </acceptance_criteria>
  <done>
    controls_overlay.dart 的 _onEngineStateChanged 现在驱动 _animController 进行 idle↔playing decoration 切换，resize 结束后根据 engine 状态恢复正确装饰，tapJitterThreshold 标记为 future-use。
  </done>
</task>

</tasks>

<verification>
Phase 完成验证：
1. `flutter analyze` — 无新增 warning/error
2. `flutter test` — 所有现有测试通过
3. `grep "controlBarBorderIdle" lib/ui/theme/tokens.dart` — 有结果
4. `grep "controlBarBorderIdle" lib/ui/player/control_bar.dart` — 有结果（_decorationIdle 使用）
5. `grep "resizing!.value.*RepaintBoundary" lib/ui/player/control_bar.dart` — 无结果（二元跳过已移除）
6. `grep "_animController\.\(forward\|reverse\)" lib/ui/player/controls_overlay.dart` — 有 4 处结果（resize + engine state）
</verification>

<success_criteria>
- resize 开始时毛玻璃以 150ms easeOut 渐变淡出（非二元跳变）
- resize 结束时毛玻璃以 150ms easeOut 渐变淡入
- engine 状态变化驱动 decoration 切换（idle↔playing）
- resize 期间 engine 状态变化被忽略（_isResizing 守卫）
- controlBarBorderIdle 与 controlBarBorderWhite 不同（idle 更淡）
- flutter analyze 无新增 warning/error
- 所有现有测试通过
</success_criteria>

<output>
创建 `.planning/phases/25-performance-quick-wins/25-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Description |
|--------|------|-------------|
| `Tokens.controlBarBorderIdle` | `lib/ui/theme/tokens.dart` | 2% 淡蓝描边常量 (idle 状态) |
| `ControlBar.build()` (modified) | `lib/ui/player/control_bar.dart` | 移除 resizing 二元跳过，统一由 opacity 渐变控制 |
| `ControlBar._decorationIdle` (modified) | `lib/ui/player/control_bar.dart` | 使用 controlBarBorderIdle 替代 controlBarBorderWhite |
| `_ControlsOverlayState._onEngineStateChanged` (modified) | `lib/ui/player/controls_overlay.dart` | 新增 _animController 驱动 decoration 状态切换 |
| `_ControlsOverlayState._onResizeChanged` (modified) | `lib/ui/player/controls_overlay.dart` | resize 结束后检查 engine 状态恢复正确装饰 |
