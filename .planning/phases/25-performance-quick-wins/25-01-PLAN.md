---
phase: 25-performance-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/theme/tokens.dart
  - lib/ui/shared/glass_container.dart
  - lib/ui/player/controls_overlay.dart
  - lib/ui/player/control_bar.dart
  - lib/ui/playlist/playlist_panel.dart
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04

must_haves:
  truths:
    - 窗口 resize 时毛玻璃以 150ms easeOut 渐变淡出/淡入，无二元跳变
    - _decorationPlaying/_decorationIdle 为 static final，不在 build() 中重复创建
    - GlassContainer 的 ImageFilter.blur 按 GlassTier static final 缓存
    - 18px 魔法数字提取为 Tokens.tapJitterThreshold
    - 控制栏颜色从白色微光改为淡蓝辉光（playing 稍亮，idle 稍淡）
  artifacts:
    - lib/ui/theme/tokens.dart — 新增 tapJitterThreshold + 淡蓝辉光色值常量
    - lib/ui/shared/glass_container.dart — GlassTier enum 新增 blurFilter getter + static final 缓存
    - lib/ui/player/controls_overlay.dart — AnimationController + TickerProviderStateMixin + tapJitterThreshold
    - lib/ui/player/control_bar.dart — static final decoration + 删除 _blurFilter + 接收 animation 参数
    - lib/ui/playlist/playlist_panel.dart — 删除 _blurFilter，改用 GlassTier.blurFilter
  key_links:
    - ControlsOverlay → GlassContainer (opacity 驱动 resize 渐变)
    - ControlsOverlay → ControlBar (decoration + opacity 参数传递)
    - GlassTier.blurFilter → ControlBar/PlaylistPanel (blur 缓存消费)
---

<objective>
消除 Phase 25 的 4 项 P0 性能/代码质量问题：resize 毛玻璃渐变过渡（PERF-01）、decoration 缓存（PERF-02）、blur 缓存（PERF-03）、魔法数字提取（PERF-04）。附带控制栏颜色从白色微光改为淡蓝辉光。

Purpose: 消除控制栏渲染路径中的重复对象创建和视觉跳变，提升 resize 期间的用户体验。
Output: 5 个文件修改完成，flutter analyze 无新增 warning，所有现有测试通过。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-performance-quick-wins/25-CONTEXT.md
@.planning/phases/25-performance-quick-wins/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokens 常量 + GlassTier blur 缓存</name>
  <files>lib/ui/theme/tokens.dart, lib/ui/shared/glass_container.dart</files>
  <read_first>
    - lib/ui/theme/tokens.dart (完整读取，定位控制栏装饰常量区域)
    - lib/ui/shared/glass_container.dart (完整读取，理解 GlassTier enum 和 _buildBlurContent)
    - .planning/phases/25-performance-quick-wins/25-CONTEXT.md (D-10~D-18 锁定决策)
    - .planning/phases/25-performance-quick-wins/25-RESEARCH.md (PERF-03 代码示例 + 淡蓝辉光色值)
  </read_first>
  <action>
    **1a. tokens.dart — 新增常量 + 修改控制栏色值**

    在 Tokens 类中新增 `tapJitterThreshold` 常量（per D-13, D-14）：
    - 位置：在 `iconXl` (28.0) 之后新增一行
    - `static const double tapJitterThreshold = 18.0;`
    - 注释：`/// 手指/鼠标点击的抖动容差，小于此距离视为点击而非拖拽`

    修改控制栏色值为淡蓝辉光（per D-15, D-16, D-17, D-18）：
    - `controlBarBorderWhite` 从 `Color(0x1AFFFFFF)` 改为 `Color(0x1A6496FF)` — 淡蓝辉光描边（playing 状态）
    - `controlBarBorderIdle` 从 `Color(0x0DFFFFFF)` 改为 `Color(0x0D6496FF)` — 5% 淡蓝描边（idle 状态）
    - `controlBarShadowBlack` 保持 `Color(0x0F5078FF)` 不变（已是蓝色调）
    - `controlBarOuterShadow` 保持 `Color(0x26000000)` 不变
    - 注释更新：`controlBarBorderWhite` 注释改为 `/// 10% 淡蓝辉光描边（playing 状态）`，`controlBarBorderIdle` 注释改为 `/// 5% 淡蓝描边（idle 状态，比 playing 淡）`
    - 常量名 `controlBarBorderWhite` 保持不变（per D-16：修改现有常量，不重命名），仅修改值和注释

    **1b. GlassTier enum — 新增 blurFilter getter + static final 缓存（per D-10, D-12）**

    在 `GlassTier` enum 的 `const GlassTier(this.sigma);` 之后新增：

    - `blurFilter` getter：使用 switch expression 返回对应的 static final ImageFilter
      - `GlassTier.thin` → `_thinBlur`
      - `GlassTier.normal || GlassTier.thick` → `_normalBlur`（D-12：sigma 相同，共享实例）
    - 两个 static final 字段：
      - `static final ImageFilter _thinBlur = ImageFilter.blur(sigmaX: Tokens.glassBlurThin, sigmaY: Tokens.glassBlurThin);`
      - `static final ImageFilter _normalBlur = ImageFilter.blur(sigmaX: Tokens.glassBlur, sigmaY: Tokens.glassBlur);`
    - doc comment：`/// 获取缓存的 ImageFilter 实例 — 不可变对象，生命周期与应用一致（D-10）`

    在 `glass_container.dart` 的 `_buildBlurContent` 方法中：
    - 删除 `final blurFilter = ui.ImageFilter.blur(sigmaX: tier.sigma, sigmaY: tier.sigma);`
    - 改为 `final blurFilter = tier.blurFilter;`（引用缓存实例）
    - 删除 `import 'dart:ui' as ui;`（不再需要直接创建 ImageFilter）

    **注释规范**：每个新增/修改的公开成员添加 `///` doc comment，非显而易见的逻辑添加行内注释解释 *why*。
  </action>
  <verify>
    <automated>cd /d D:\simple_player_flutter && flutter analyze lib/ui/theme/tokens.dart lib/ui/shared/glass_container.dart</automated>
  </verify>
  <acceptance_criteria>
    - tokens.dart 包含 `static const double tapJitterThreshold = 18.0;`
    - tokens.dart 中 `controlBarBorderWhite` 值为 `Color(0x1A6496FF)`
    - tokens.dart 中 `controlBarBorderIdle` 值为 `Color(0x0D6496FF)`
    - glass_container.dart 中 GlassTier enum 包含 `ImageFilter get blurFilter` getter
    - glass_container.dart 中 GlassTier 包含 `static final ImageFilter _thinBlur` 和 `static final ImageFilter _normalBlur`
    - glass_container.dart 中 `_buildBlurContent` 使用 `tier.blurFilter` 而非 `ui.ImageFilter.blur(...)`
    - glass_container.dart 不再 import `dart:ui as ui`
    - `flutter analyze` 无新增 warning/error
  </acceptance_criteria>
  <done>
    Tokens 新增 tapJitterThreshold 常量和淡蓝辉光色值，GlassTier 新增 blurFilter getter 和 static final 缓存，GlassContainer._buildBlurContent 使用缓存实例。
  </done>
</task>

<task type="auto">
  <name>Task 2: ControlsOverlay 动画 + ControlBar/PlaylistPanel 缓存清理</name>
  <files>lib/ui/player/controls_overlay.dart, lib/ui/player/control_bar.dart, lib/ui/playlist/playlist_panel.dart</files>
  <read_first>
    - lib/ui/player/controls_overlay.dart (完整读取，理解 AutoHideController 和 SingleTickerProviderStateMixin)
    - lib/ui/player/control_bar.dart (完整读取，理解 decoration getter 和 _blurFilter)
    - lib/ui/playlist/playlist_panel.dart (完整读取，理解 _blurFilter 使用位置)
    - lib/ui/shared/glass_container.dart (读取修改后的版本，确认 GlassTier.blurFilter API)
    - lib/ui/theme/tokens.dart (读取修改后的版本，确认新常量名)
    - .planning/phases/25-performance-quick-wins/25-CONTEXT.md (D-01~D-09 锁定决策)
    - .planning/phases/25-performance-quick-wins/25-RESEARCH.md (Pattern 1/2 代码示例 + Pitfall 2/5)
  </read_first>
  <action>
    **2a. ControlsOverlay — AnimationController + TickerProviderStateMixin + tapJitterThreshold**

    修改 mixin（per Pitfall 5 / RESEARCH.md）：
    - `with SingleTickerProviderStateMixin` 改为 `with TickerProviderStateMixin`
    - 注释：`/// TickerProviderStateMixin: AutoHideController + _animController 各需一个 ticker`

    新增字段：
    - `late final AnimationController _animController;` — 共享 controller，150ms，初始 value=1.0（per D-09）
    - `late final Animation<double> _resizeOpacity;` — CurvedAnimation，easeOut 曲线（per D-05, D-07）
    - `bool _isResizing = false;` — 标记 resize 状态，防止 resize 期间 decoration 动画打断（per Pitfall 2）

    initState 中新增：
    - 创建 `_animController = AnimationController(vsync: this, duration: Duration(milliseconds: Tokens.durationNormal), value: 1.0);`
    - 创建 `_resizeOpacity = CurvedAnimation(parent: _animController, curve: Curves.easeOut);`
    - 监听 resizing 变化：`widget.resizing?.addListener(_onResizeChanged);`

    新增 `_onResizeChanged` 方法（per D-07）：
    - 获取 `widget.resizing?.value`
    - 如果 resizing == true：`_isResizing = true; _animController.reverse();`（1.0→0.0）
    - 如果 resizing == false：`_isResizing = false; _animController.forward();`（0.0→1.0）

    新增 `_onEngineStateChanged` 中的 resize 期间延迟逻辑（per Pitfall 2）：
    - 在现有 `_onEngineStateChanged` 方法中，如果 `_isResizing` 则 return（不触发动画）
    - 注释：`// resize 期间忽略 engine 状态变化，避免 controller 竞争（Pitfall 2）`

    didUpdateWidget 中新增 resizing 监听迁移：
    - 如果 `oldWidget.resizing != widget.resizing`，移除旧监听，添加新监听

    dispose 中新增：
    - `widget.resizing?.removeListener(_onResizeChanged);`
    - `_animController.dispose();`

    修改 build 方法中 ControlBar 的调用：
    - 新增参数 `opacity: _resizeOpacity`（per D-06）
    - 保留 `resizing: widget.resizing` 不变

    修改 `_onPointerUp` 方法中的 18px（per D-13）：
    - `if (dx > 18 || dy > 18) return;` 改为 `if (dx > Tokens.tapJitterThreshold || dy > Tokens.tapJitterThreshold) return;`

    **2b. ControlBar — static final decoration + 删除 _blurFilter**

    删除 getter `_decorationPlaying` 和 `_decorationIdle`（per D-01）：
    - 替换为两个 `static final` 字段，与当前 getter 结构相同但为编译时常量引用
    - 注释更新：`/// 播放状态装饰 — 淡蓝辉光 + 4 个 BoxShadow（D-01: static final 缓存）`
    - 注释更新：`/// 空状态装饰 — 淡化蓝辉光 + 补齐 4 个 BoxShadow（D-01/D-04）`

    idle decoration 对齐 BoxShadow（per D-04）：
    - idle 当前只有 2 个 BoxShadow，补齐到 4 个
    - 第 3、4 个用 `BoxShadow(color: Colors.transparent, blurRadius: 0, spreadRadius: 0)` 补齐
    - 注释：`/// 补齐 4 个 BoxShadow，让 DecorationTween 插值更平滑（D-04）`

    删除 `static final _blurFilter`（per D-11）：
    - 删除 `static final _blurFilter = ui.ImageFilter.blur(...);`
    - 删除 `import 'dart:ui' as ui;`（不再需要）

    修改 `_buildBlur` 方法：
    - `BackdropFilter(filter: _blurFilter, ...)` 改为 `BackdropFilter(filter: GlassTier.normal.blurFilter, ...)`
    - 需要 import glass_container.dart（或通过 glass_widgets.dart barrel import）
    - 现有 import `import '../shared/edge_glow.dart';` 附近添加 `import '../shared/glass_widgets.dart';`

    修改 `build` 方法中的 decoration 使用：
    - `decoration: isIdle ? _decorationIdle : _decorationPlaying` 保持不变（static final 字段同名）

    **AnimatedContainer 替换为 AnimatedBuilder + DecorationTween（per D-01, D-02）**：
    - 将 `AnimatedContainer(duration: ..., curve: ..., height: ..., decoration: isIdle ? _decorationIdle : _decorationPlaying)` 替换为：
    - `AnimatedBuilder(animation: _decorationAnim, builder: (_, __) => Container(height: Tokens.controlBarHeight, decoration: _decorationTween.evaluate(_decorationAnim)))`
    - 但 ControlBar 是 StatelessWidget，没有 AnimationController
    - **正确方案**：ControlBar 接收 `Animation<double>? decoration` 参数，用 AnimatedBuilder 监听
    - 新增参数 `final Animation<double>? decoration;`
    - 在 build 中，如果 `decoration` 非空，用 AnimatedBuilder + DecorationTween 替代 AnimatedContainer
    - 如果 `decoration` 为 null，回退到当前 AnimatedContainer 行为（向后兼容）
    - DecorationTween 定义为 ControlBar 的 static final：`static final _decorationTween = DecorationTween(begin: _decorationIdle, end: _decorationPlaying);`

    **ControlsOverlay 传递 decoration 动画**：
    - ControlsOverlay 的 ControlBar 调用中新增 `decoration: _animController` 参数

    **2c. PlaylistPanel — 删除 _blurFilter，改用 GlassTier（per D-11）**

    删除 `static final _blurFilter`：
    - 删除 `static final _blurFilter = ui.ImageFilter.blur(sigmaX: Tokens.glassBlur, sigmaY: Tokens.glassBlur);`
    - 删除 `import 'dart:ui' as ui;`

    修改 `_buildBackdrop` 方法：
    - `filter: _blurFilter` 改为 `filter: GlassTier.normal.blurFilter`
    - 需要 import `import '../shared/glass_widgets.dart';`（或直接 import glass_container.dart）

    **注释规范**：每个新增/修改的公开成员添加 `///` doc comment，非显而易见的逻辑添加行内注释解释 *why*。
  </action>
  <verify>
    <automated>cd /d D:\simple_player_flutter && flutter analyze lib/ui/player/controls_overlay.dart lib/ui/player/control_bar.dart lib/ui/playlist/playlist_panel.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - controls_overlay.dart 中 mixin 为 `TickerProviderStateMixin`（非 SingleTickerProviderStateMixin）
    - controls_overlay.dart 包含 `_animController` 和 `_resizeOpacity` 字段
    - controls_overlay.dart 中 `_onPointerUp` 使用 `Tokens.tapJitterThreshold` 而非硬编码 `18`
    - control_bar.dart 中 `_decorationPlaying` 和 `_decorationIdle` 为 `static final`（非 getter）
    - control_bar.dart 中 idle decoration 有 4 个 BoxShadow（补齐透明 shadow）
    - control_bar.dart 不再包含 `static final _blurFilter` 和 `import 'dart:ui'`
    - control_bar.dart 中 BackdropFilter 使用 `GlassTier.normal.blurFilter`
    - control_bar.dart 包含 `Animation<double>? decoration` 参数和 DecorationTween
    - playlist_panel.dart 不再包含 `static final _blurFilter` 和 `import 'dart:ui'`
    - playlist_panel.dart 中 BackdropFilter 使用 `GlassTier.normal.blurFilter`
    - `flutter analyze` 无新增 warning/error
    - `flutter test` 全部通过
  </acceptance_criteria>
  <done>
    ControlsOverlay 新增共享 AnimationController 驱动 resize 渐变和 decoration 切换，ControlBar decoration 改为 static final + DecorationTween，ControlBar 和 PlaylistPanel 删除重复 _blurFilter 改用 GlassTier 缓存。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI→Engine | ControlsOverlay 监听 engine.state，但仅读取不写入 |
| Tokens→All UI | 常量修改影响所有引用处，需全局一致性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | Tokens 常量修改 | low | accept | 色值为近似值，用户运行后微调（D-17），无安全风险 |
| T-25-02 | Denial of Service | static final ImageFilter | low | accept | dart:ui ImageFilter 不可变，static final 无泄漏风险 |

本阶段无外部依赖、无用户输入处理、无认证逻辑。安全风险极低。
</threat_model>

<verification>
Phase 完成验证：
1. `flutter analyze` — 无新增 warning/error
2. `flutter test` — 所有现有测试通过
3. `grep -r "ImageFilter.blur" lib/` — 仅 glass_container.dart 的 GlassTier 中存在（ControlBar/PlaylistPanel 已删除）
4. `grep "SingleTickerProviderStateMixin" lib/ui/player/controls_overlay.dart` — 无结果（已改为 TickerProviderStateMixin）
5. `grep "tapJitterThreshold" lib/ui/player/controls_overlay.dart` — 有结果（18px 已提取）
6. `grep "controlBarBorderWhite.*0x1A6496FF" lib/ui/theme/tokens.dart` — 有结果（淡蓝辉光已应用）
</verification>

<success_criteria>
- 窗口 resize 时控制栏毛玻璃以 150ms easeOut 渐变淡出/淡入
- _decorationPlaying/_decorationIdle 为 static final，不在 build() 中重复创建
- GlassTier.blurFilter 按 tier 缓存 ImageFilter，thick/normal 共享实例
- 18px 魔法数字提取为 Tokens.tapJitterThreshold
- 控制栏颜色从白色微光改为淡蓝辉光
- flutter analyze 无新增 warning/error
- 所有现有测试通过
</success_criteria>

<output>
创建 `.planning/phases/25-performance-quick-wins/25-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Description |
|--------|------|-------------|
| `Tokens.tapJitterThreshold` | `lib/ui/theme/tokens.dart` | 点击抖动容差常量 (18.0) |
| `Tokens.controlBarBorderWhite` (modified) | `lib/ui/theme/tokens.dart` | 淡蓝辉光描边色值 `0x1A6496FF` |
| `Tokens.controlBarBorderIdle` (modified) | `lib/ui/theme/tokens.dart` | 淡蓝描边色值 `0x0D6496FF` |
| `GlassTier.blurFilter` | `lib/ui/shared/glass_container.dart` | getter 返回缓存的 ImageFilter 实例 |
| `GlassTier._thinBlur` | `lib/ui/shared/glass_container.dart` | thin tier 缓存 ImageFilter |
| `GlassTier._normalBlur` | `lib/ui/shared/glass_container.dart` | normal/thick tier 共享缓存 ImageFilter |
| `ControlBar._decorationPlaying` (static final) | `lib/ui/player/control_bar.dart` | 缓存的 playing 状态 BoxDecoration |
| `ControlBar._decorationIdle` (static final) | `lib/ui/player/control_bar.dart` | 缓存的 idle 状态 BoxDecoration（4 BoxShadow） |
| `ControlBar._decorationTween` | `lib/ui/player/control_bar.dart` | DecorationTween 实例 |
| `ControlBar.decoration` (param) | `lib/ui/player/control_bar.dart` | 外部传入的 decoration 动画 |
| `_ControlsOverlayState._animController` | `lib/ui/player/controls_overlay.dart` | 共享 AnimationController (150ms) |
| `_ControlsOverlayState._resizeOpacity` | `lib/ui/player/controls_overlay.dart` | CurvedAnimation easeOut |
