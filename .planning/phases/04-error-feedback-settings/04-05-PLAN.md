---
phase: 04-error-feedback-settings
plan: 05
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03", "04-04"]
gap_closure: true
gap_ids: [G-04-1]
files_modified:
  - lib/kernel/diagnostics/error_log_location.dart
  - lib/ui/dialogs/settings/error_feedback_settings.dart
  - lib/ui/dialogs/settings/diagnostic_log_target.dart
  - lib/ui/dialogs/settings/general_settings_content.dart
  - lib/main.dart
  - lib/app.dart
  - lib/l10n/app_en.arb
  - lib/l10n/app_zh.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_en.dart
  - lib/l10n/app_localizations_zh.dart
  - test/diagnostics/error_log_location_test.dart
  - test/diagnostics/error_feedback_settings_store_test.dart
  - test/diagnostics/diagnostic_log_target_test.dart
  - test/widget/dialogs/general_settings_content_test.dart
autonomous: true
requirements: [SET-02]
estimate:
  tokens: 9000
  raw_tokens: 6000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "设置「通用」tab 仅渲染错误卡片开关行——路径输入、浏览按钮、防抖校验状态、有效路径行全部不存在（G-04-1 UI 面，UAT Test 3/5 对应自动化证据）"
    - "settings.json 仅含 version 与卡片开关两个键；旧文件残留的第三个键被 load 静默忽略（向后兼容）"
    - "诊断日志固定经双层链落点：exe 根 logs/error.log 优先，exe 层不可写时静默回退 Application Support logs/；无任何用户可配置入口"
    - "启动激活仍是协调器唯一激活实现；运行时重定向 API 与 D-04 回退通知桥不复存在"
    - "设置 store 的 WR-06 双层回退（exe→AS）、WR-05 串行持久化链与唯一 tmp、原子写四级降级行为不变"
    - "reporter/单写者/delegate 语义零改动；kernel 改动仅限 error_log_location.dart；零新增依赖"
  artifacts:
    - lib/kernel/diagnostics/error_log_location.dart（双层 resolve + 保留的内部可写探测）
    - lib/ui/dialogs/settings/error_feedback_settings.dart（单偏好 store）
    - lib/ui/dialogs/settings/diagnostic_log_target.dart（仅启动激活的最小协调器）
    - lib/ui/dialogs/settings/general_settings_content.dart（仅开关行）
    - lib/main.dart（load→resolve→activateResolved 双层接线）
    - lib/app.dart（无通知桥挂载）
    - lib/l10n/app_en.arb + app_zh.arb + 三个 gen-l10n 产物（设置域仅剩开关标签键）
    - test/diagnostics/error_log_location_test.dart（双层断言 + 探测语义组）
    - test/diagnostics/error_feedback_settings_store_test.dart（两键 round-trip + 双层 tracer）
    - test/diagnostics/diagnostic_log_target_test.dart（仅启动激活用例）
    - test/widget/dialogs/general_settings_content_test.dart（开关行 + absence 用例）
  key_links:
    - "main.dart _activateDiagnosticLog → store load → 双层 resolve → activateResolved（启动激活链保持贯通，hooks-first 源码顺序不动）"
    - "store _isDirectoryWritable → kernel validateConfiguredDirectory（WR-06 探测复用链——该 kernel API 保留的唯一生产消费者）"
    - "ErrorCardHost 呈现门控 → store state notifier（SET-01 语义不受本计划影响）"
---

<objective>
关闭 G-04-1：按 D-07（UAT 后修订决策，2026-09-01 用户拍板）将日志路径配置功能整体移除——通用 tab 只留卡片开关行，日志固定写软件根目录 logs/（exe 根优先、Application Support 静默回退的双层链），D-04 双通道回退告知随之作废。

Purpose: 用户实测判定路径配置鸡肋；SET-02 已按修订语义在 REQUIREMENTS.md 关账（[x] + 删除线修订注），本计划交付修订后的代码与测试事实，使 UAT Test 3/5 作废项与 gap 正式关账。
Output: 收窄后的 kernel/store/协调器/通用 tab + 同步收窄的四个测试文件 + l10n 键清理 + `04-05-SUMMARY.md`。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-error-feedback-settings/04-CONTEXT.md（D-07 = 本计划的权威移除规格，逐条实现；D-01/D-05 保留语义不得破坏）
@.planning/phases/04-error-feedback-settings/04-UAT.md（G-04-1 gap 卡：truth/reason/artifacts/missing）
@.planning/phases/04-error-feedback-settings/04-04-SUMMARY.md（widget 测试真实 I/O 协议：pump+runAsync 交替、teardown 禁 await plain async fn——改写测试时必须遵守）
@.planning/REQUIREMENTS.md（SET-02 修订语义）

# 被移除面的生产源（Task 2 的编辑对象；符号清单以文件实况为准）
@lib/kernel/diagnostics/error_log_location.dart
@lib/ui/dialogs/settings/error_feedback_settings.dart
@lib/ui/dialogs/settings/diagnostic_log_target.dart
@lib/ui/dialogs/settings/general_settings_content.dart
@lib/main.dart（_activateDiagnosticLog，约 128-174 行）
@lib/app.dart（buildErrorCardMount，约 120-150 行）

# 被改写的测试与 l10n
@test/diagnostics/error_log_location_test.dart
@test/diagnostics/error_feedback_settings_store_test.dart
@test/diagnostics/diagnostic_log_target_test.dart
@test/widget/dialogs/general_settings_content_test.dart
@lib/l10n/app_en.arb（设置域键区约 471-512 行）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 测试面收窄到目标形态（RED）——absence 断言先行</name>
  <files>test/widget/dialogs/general_settings_content_test.dart, test/diagnostics/error_log_location_test.dart, test/diagnostics/error_feedback_settings_store_test.dart, test/diagnostics/diagnostic_log_target_test.dart</files>
  <precondition>工作区基线：04-04 已收口——`flutter analyze` 0 error、全量 `flutter test` 全绿（1377 例）。不满足则停下上报，不得在脏基线上开移除。</precondition>
  <action>
对四个测试文件做「删配置面、留保留面」的收窄（只动测试，零生产改动；所有新用例须可在旧生产代码下编译——不引用任何将被删除的 API）：

1. **通用内容 widget 套件**：删除路径行全部九个用例（防抖校验保存换位、非法输入三不、清空回默认链、浏览取消、浏览回填、有效路径与回退原因行内呈现、输入框以 store 值播种、picker 自身失败分型、回退判断大小写不敏感）。保留开关行持久化用例与整套 harness（store 单例隔离 resetForTesting + 临时目录 seam + addTearDown 清理 + delegate fire-and-forget dispose 纪律，逐字继承）。新增一条 absence 用例：通用内容只渲染开关行——按类型找不到文本输入框、按 ValueKey `settings-log-path-browse` 找不到浏览按钮、找不到任何校验状态文案；开关行翻转仍然置 store 并可等待持久化（沿用 pendingPersist 等待点惯例）。

2. **位置解析套件**：删除配置层相关两组——「三层回退链」组内四个配置层用例（配置层胜出、空配置跳层、文件占据跳层、胜出保持回退原因空）与「启动配置层校验契约」组整组（空白值跳层、相对路径携带原因、正斜杠 UNC 携带原因、trim 收纳）。保留并改写双层用例：exe 层不可写回退 AS 层、全败降级 Unavailable 不抛出、注入 probe 全败降级——断言只锁「注入 provider 的 exe 根 logs/error.log 胜出 / AS 回退 / Unavailable」，不再引用被移除的回退原因字段。保留校验矩阵组全部 11 用例（探测语义不变），组描述由「SET-02 采用面」改写为「内部可写探测契约（store 双层回退复用）」。04-01 的契约翻转用例（kernel 不直接读进程位置、exe 层只经注入到达）逐字保留，组/用例措辞中的「三层」表述同步改「双层」。

3. **store 套件**：tracer 端到端用例改写——真实临时 settings.json 只写入 version 与开关两个键，断言解码后 JSON 恰为这两个键、不含第三键（旧实现写三键 → 本断言 RED）；装载驱动链断言改为注入 exe/AS provider 的双层落点（exe 根 logs/error.log 胜出）+ sink 激活后诊断包可读（保持 store→链→sink→磁盘纵切证明）。损坏矩阵五用例保留（第三键错型用例改写为「未知键被静默忽略、开关字段正常装载」）。生产加固组四个用例（round-trip、原子写无 tmp 残留、快速串行、保存失败吞没）改用开关字段驱动断言。WR-06 两层回退组两个用例逐字保留。debug 默认 provider 用例保留。

4. **协调器套件**：删除重定向协议组全部七个用例（有效换位保序、间隙补发、重叠 apply 串行、非法三不、空串回链、解析失败不 dispose、同目录幂等）与「通知桥」组整组（一次性通知、挂起不覆盖、通知桥 widget 两用例）。保留并改写「启动激活」用例：attach → 单参数 activateResolved 后 delegate 已激活、落点与注入文件一致、有效读数不再存在故无相关断言、无任何通知副作用。harness（gate 化 writer、临时文件纪律）按保留用例的实际需要裁剪。
  </action>
  <verify>
    <automated>flutter test test/widget/dialogs/general_settings_content_test.dart test/diagnostics/error_log_location_test.dart test/diagnostics/error_feedback_settings_store_test.dart test/diagnostics/diagnostic_log_target_test.dart --reporter compact</automated>
    <fails_when>本任务为 RED 取证，flutter test 预期非 0 退出；不合格信号为：退出码 0（两条新 absence 断言未拦住旧代码，RED 未成立）、摘要行 "Some tests failed" 的失败计数不是恰好 2、或输出为编译错误（如 "isn't defined"）而非断言失败</fails_when>
  </verify>
  <done>
RED 形态成立：四套件中恰好两条新 absence 断言失败——通用内容 absence 用例（路径行仍被旧代码渲染）与 store 两键 round-trip（旧实现写三键）——其余改写后用例全部通过；零生产文件改动（`git status` 仅四个测试文件）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 生产面移除（GREEN）——编译原子的收窄单元</name>
  <files>lib/kernel/diagnostics/error_log_location.dart, lib/ui/dialogs/settings/error_feedback_settings.dart, lib/ui/dialogs/settings/diagnostic_log_target.dart, lib/ui/dialogs/settings/general_settings_content.dart, lib/main.dart, lib/app.dart</files>
  <behavior>
    - 通用内容仅渲染开关行：文本输入框/浏览键/校验状态/有效路径文案均 findsNothing；开关翻转仍置 store 并持久化（Task 1 absence 用例转绿）
    - store round-trip：解码 JSON 恰含 version 与 errorCardEnabled 两键（Task 1 两键断言转绿）
    - 双层链：注入 exe/AS provider → exe 根 logs/error.log 胜出；exe 层不可写 → AS logs/ 回退；全败 → Unavailable 不抛出
    - 启动激活：attach → activateResolved(file) 后 delegate.logPath == 注入文件路径（Task 1 改写用例转绿）
    - 保留面回归：store WR-06 两层回退/串行链/原子写、host 门控四用例、settings_dialog 七用例全绿
  </behavior>
  <action>
六个文件是一个编译原子（互相引用被移除符号），必须同任务内完成；各文件内部按依赖顺序编辑（先内容/组合根，后 store/协调器），任一中途态不得提交。注释纪律（CLAUDE.md）：随每处移除同步改写 doc comment，不攒批。

1. **kernel（唯一允许的 kernel 编辑，仍仅此文件）**：resolve 链收窄为双层——删除首层配置目录分支与对应入参，链序变为 exe 根 logs/ → Application Support logs/；成功结果类移除回退原因字段并同步删除其在默认链帮助函数中的透传管道与形参。**逐字保留**：validateConfiguredDirectory 及其封闭原因枚举、sealed 结果类、长度上界常量、控制字符扫描、_prepareTier、默认探测函数、两个 provider typedef、WritableDirectoryProbe seam、logsDirectoryName/logFileName 常量（封闭原因集整体保留——它是保留 API 的返回词汇表，且 store 探测复用中不可删，per D-07 第 3 条）。库头与类 doc comment 的三层表述改双层、移除对配置层与回退原因的描述。

2. **store**：移除持久化目录字段四件套——数据类字段（含 ==/hashCode 中的项）、load 的对应分支、目录写入器方法、编码键。**逐字保留**：开关字段及其 setter、WR-05 串行持久化链与唯一 tmp 后缀、WR-06 双层回退与 _isDirectoryWritable 对 kernel 保留 API 的复用、原子写四级降级、值相等契约（按剩余字段重算）、defaultSettingsFile、两个构造形态、resetForTesting。库 doc comment「两项偏好」改「单项偏好」，两层回退描述保留。

3. **协调器**：收窄为仅启动激活的最小形态——保留 attach（签名不变）、唯一激活实现 activateResolved（签名收窄为仅 file 形参：激活换入 sink 并写 delegate 读数——不再有 UI 权威读数 notifier 与通知置值）、resetForTesting（按剩余字段裁剪）。删除：运行时重定向协议全体（校验转发方法、提交入口、单次协议体、默认链回退体、唯一换位通道、串行队列字段、同目录幂等分支、落点组合帮助函数）与一次性通知族（通知 notifier、消费器、激活入参、整个通知桥 widget 类与其 State）。两个 UI 读数 notifier 一并删除（生产消费者已随路径行移除，per D-07「倾向删除未用 API」）。import 面随符号面收窄：位置 typedef 仍需 kernel 位置文件导入，UI/l10n/设置 store 导入随用随删；库 doc comment 改写为「仅启动激活，无运行时重定向」。

4. **通用内容**：收窄为开关行——删除路径行区块整段（标签行与浏览按钮、文本输入框与其控制器、防抖计时与提交链、浏览网关与生产 picker 调用、行内状态族 sealed 类与状态行组件、有效路径行组件、回退判断帮助函数、浏览按钮组件类）；保留 _SettingsRow 行语法组件与开关行（Switch 主题 activeThumbColor 等实现细节逐字不动）；构造函数的两个注入参数（目录网关、防抖时长）随面移除；import 面随符号面收窄（异步计时器、dart:io、目录选择插件、kernel 位置文件、协调器导入全删）；库 doc comment 改写为仅开关行语义。

5. **main.dart**：启动激活链保持 load → resolve → activateResolved 贯通——resolve 调用移除配置目录实参，switch 解构移除回退原因字段；设置加载保留（开关持久化需要，WR-06 的 AS provider 实参保留）；相关注释（配置层语义、回退告知）同步改写；hooks-first 源码顺序（attach 位于 hooks 安装后、激活前）与 unawaited 不阻塞启动的纪律逐字不动。

6. **app.dart**：移除通知桥的 Positioned 挂载子树、对应 import 与「防塌缩须 Positioned」注释块；buildErrorCardMount 的 Stack 恢复为卡片挂载原貌（Positioned.fill + 卡片两种 mode 分支逐字不动）。
  </action>
  <verify>
    <automated>flutter analyze && flutter test test/diagnostics/ test/widget/dialogs/ --reporter compact</automated>
    <fails_when>链上任一命令非 0 退出：analyze 报告任何 error（触碰文件出现任何条目），或 flutter test 摘要行出现 "Some tests failed"；`<done>` 的残留符号计数门不满足（应为 0 的计数非 0、应为 ≥1/≥3 的保留符号计数不达标）同样判不合格</fails_when>
  </verify>
  <done>
GREEN 成立：Task 1 全部目标测试转绿且宿主/dialog 既有套件零回归；`flutter analyze` 0 error（触碰文件 0 条目）。lib/ 全树被移除符号零残留——`grep -rn "logDirectory\|setLogDirectory\|configuredDirectory\|configuredFailure\|DiagnosticFallbackNotice\|pendingFallbackNotice\|consumeFallbackNotice\|effectiveLogPath" lib/ | wc -l` 等于 0；保留符号在位——`grep -c "validateConfiguredDirectory" lib/kernel/diagnostics/error_log_location.dart` 与 `grep -c "validateConfiguredDirectory" lib/ui/dialogs/settings/error_feedback_settings.dart` 均 ≥ 1，`grep -c "errorCardEnabled" lib/ui/dialogs/settings/error_feedback_settings.dart` ≥ 3，`grep -c "activateResolved" lib/main.dart` ≥ 1。
  </done>
</task>

<task type="auto">
  <name>Task 3: l10n 键清理 + 全门禁收口</name>
  <files>lib/l10n/app_en.arb, lib/l10n/app_zh.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_en.dart, lib/l10n/app_localizations_zh.dart</files>
  <action>
1. **l10n 清理**：从 en/zh 两份 ARB 移除以下设置域 10 个已无消费者的键——正向完整清单，逐键抄录自 app_en.arb 设置域键区（约 471-518 行；zh 对应约 225-236 行，键集一致），逐键删除，`@key` 描述项随键同删：
   - 通知桥文案（D-04 一次性 OSD 通知，1 个）：`logFallbackNotice`
   - 路径行文案（9 个）：`logPathLabel`（行标签）、`logPathHint`（空输入提示）、`logPathBrowse`（浏览按钮）、`logPathValidatingStatus`（校验中状态）、`logPathValidStatus`（校验通过状态）、`logPathInvalidStatus`（校验失败状态）、`logPathPickerFailureStatus`（picker 自身失败状态）、`logFallbackReasonPrefix`（行内回退原因前缀）、`logEffectivePathLabel`（有效路径行，含 `{path}` placeholder）
   删除后设置域仅剩 `errorCardToggleLabel`（开关行唯一文案）；`<done>` 的双语计数门只作删除后的机器复核，不再反推清单。运行 `flutter gen-l10n` 再生成三个产物文件并入库（禁止手编生成文件）。

2. **全门禁**（04-04 同口径）：`flutter analyze`（0 error，触碰文件 0 条目，61 条既有 info 基线同值）；全量 `flutter test`（零失败；总例数较 1377 基线按删除用例数自然下降，属预期，0 delta 门的口径是「无新增失败」非「数不变」）；`bash tool/audit/kernel_logger_gate.sh`（GATE 1/2 PASS）。

3. **红线自检**：`git diff` 审计——error_reporter/error_capture_snapshot/error_reporting_dependencies/error_log_file_sink/formatter 五文件零 diff；pubspec.yaml/pubspec.lock 零 diff（目录选择插件依赖保留给既有功能，零新增依赖）；lib/kernel/ 内 diff 仅 error_log_location.dart。若任何门失败，修复后重跑全门禁，不得带红收口。
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter analyze && flutter test --reporter compact && bash tool/audit/kernel_logger_gate.sh</automated>
    <fails_when>链上任一命令非 0 退出：gen-l10n 再生成后产物与 ARB 不一致（git status 出现未入库 diff）、analyze 报告任何 error、flutter test 摘要行出现 "Some tests failed"、或 kernel_logger_gate.sh 输出任何 GATE FAIL（非 "GATE 1 PASS" 且 "GATE 2 PASS"）；`<done>` 的 l10n 计数门不满足同样判不合格</fails_when>
  </verify>
  <done>
l10n 双语 10 键移除且三产物再生成入库——`grep -c "logPath\|logFallback\|logEffectivePath" lib/l10n/app_en.arb lib/l10n/app_zh.arb` 每文件等于 0，`grep -c "errorCardToggleLabel" lib/l10n/app_en.arb` 等于 1；全量 `flutter test` 零失败、`flutter analyze` 0 error、kernel gate GATE 1/2 PASS；红线 diff 审计为空。G-04-1 的四条 missing 项全部有对应落地证据。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.json → store load | 手编/残缺文件内容跨解码边界进入内存态（保留面，本计划不动其防御） |
| 目录 provider → kernel 探测 | 平台目录路径经 provider seam 进入探测（保留面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure | 被移除面（路径 UI/通知桥/有效路径读数） | low | accept | 纯移除缩小呈现面，无新增边界；不引入新信息流 |
| T-04-05-02 | Tampering | settings.json 手编残留第三键 | low | accept | load 的 is! Map 守卫 + 逐字段类型校验天然忽略未知键，向后兼容由 Task 1 损坏矩阵用例锁定 |
| T-04-05-03 | Tampering | npm/pip/cargo 包安装 | high | mitigate | 本计划零包管理器安装任务、零新依赖；红线审计要求 pubspec 零 diff（Task 3 门禁） |
| T-01-13/19 | Information Disclosure | 诊断写路径（保留面） | low | accept | 写路径仍只产出诊断包纯文本；本计划纯移除、不新增 retained 串 sink——Phase 1 SECURITY 的 re-verified 口径维持，随 Phase 收尾 04-SECURITY.md 收账 |
</threat_model>

<verification>
1. `flutter analyze` 0 error，触碰文件 0 条目
2. 全量 `flutter test` 零失败（例数因删除用例自然下降，无新增失败）
3. `bash tool/audit/kernel_logger_gate.sh` GATE 1/2 PASS
4. lib/ 全树被移除符号零残留（Task 2/3 的 grep 门全过）
5. 保留面正门：kernel 内部探测 API 在位且被 store 复用；WR-05/WR-06 store 行为用例全绿；开关行 absence+行为用例全绿
6. 红线：reporter/单写者/delegate 五文件零 diff；kernel diff 仅 error_log_location.dart；pubspec 零 diff
</verification>

<success_criteria>
- 通用 tab 仅剩错误卡片开关行，路径配置入口在任何层（UI/store/kernel 链/协调器/l10n）都不复存在
- 日志固定落 exe 根 logs/error.log，exe 不可写时静默回退 Application Support logs/（双层链自动化锁定）
- settings.json 两键形态 + 旧文件第三键静默忽略（向后兼容）
- D-04 通知桥与运行时重定向协议整体移除，启动激活经协调器唯一实现保持贯通
- WR-05 串行链/唯一 tmp、WR-06 设置双层回退、原子写、开关默认开语义逐字保留
- 三质量门全绿；G-04-1 具备关账证据（UAT Test 3/5 作废项的自动化对应面）
</success_criteria>

<output>
Create `.planning/phases/04-error-feedback-settings/04-05-SUMMARY.md` when done
</output>
