---
phase: 03-iface-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/engine/engine_state.dart
  - lib/kernel/engine/track_control.dart
  - lib/kernel/engine/video_effects.dart
  - lib/kernel/engine/renderer_config.dart
  - lib/kernel/engine/fvp_engine.dart
  - lib/kernel/engine/mock_engine.dart
  - test/helpers/fake_engine.dart
  - test/engine/mixin_capability_test.dart
autonomous: true
requirements:
  - IFACE-01
  - IFACE-02
  - IFACE-03
  - IFACE-04
  - IFACE-05
must_haves:
  truths:
    - FvpEngine/MockEngine/FakeEngine 通过 with TrackControl, VideoEffects, RendererConfig 组合实现全部能力
    - engine is TrackControl / engine is VideoEffects / engine is RendererConfig 在运行时为 true
    - 57 个 UI 文件的 EngineState import 和类型声明零修改
    - 840+ 测试全部通过（3 个 golden 失败为预存问题）
  artifacts:
    - lib/kernel/engine/track_control.dart
    - lib/kernel/engine/video_effects.dart
    - lib/kernel/engine/renderer_config.dart
    - test/engine/mixin_capability_test.dart
  key_links:
    - "engine_state.dart -> track_control.dart (mixin TrackControl on EngineState)"
    - "fvp_engine.dart -> with TrackControl, VideoEffects, RendererConfig"
    - "mock_engine.dart -> with TrackControl, VideoEffects, RendererConfig"
    - "fake_engine.dart -> with TrackControl, VideoEffects, RendererConfig"
---

<objective>
Split EngineState's ~30 members into capability-group sub-mixins (TrackControl, VideoEffects, RendererConfig), enabling UI-layer capability checks via Dart 3 pattern matching while maintaining zero-change backward compatibility for all 57 UI consumer files.

Purpose: Reduce interface surface area per consumer, enable runtime capability discovery, prepare for future engine implementations that may not support all capabilities.
Output: 3 new mixin files, updated engine implementations, mixin capability test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/kernel/engine/engine_state.dart
@lib/kernel/engine/fvp_engine.dart
@lib/kernel/engine/mock_engine.dart
@test/helpers/fake_engine.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 sub-mixin files + trim EngineState base</name>
  <files>
    lib/kernel/engine/track_control.dart,
    lib/kernel/engine/video_effects.dart,
    lib/kernel/engine/renderer_config.dart,
    lib/kernel/engine/engine_state.dart
  </files>
  <action>
Create three new mixin files, each with `mixin X on EngineState` syntax. These mixins contain ONLY abstract method signatures (pure interfaces, no default implementation) per D-02.

**track_control.dart** — 9 methods (per D-04):
- `List<AudioTrackInfo> getAudioTracks()`
- `void switchAudioTrack(int trackId)`
- `List<int> get activeAudioTracks`
- `List<SubtitleTrackInfo> getSubtitleTracks()`
- `void switchSubtitleTrack(int trackId)`
- `void toggleSubtitle()`
- `void setExternalSubtitle(String path)`
- `void setSubtitleDelay(int delay)`
- `void setEqualizer(String preset)`
Import only: `engine_state.dart`, `models/audio_track_info.dart`, `models/subtitle_track_info.dart`

**video_effects.dart** — 4 methods (per D-05):
- `void setVideoEffect(VideoEffectType effectType, double value)`
- `void rotate(int degrees)`
- `void setAspectRatio(double ratio)`
- `void setDeinterlace(bool enable)`
Import only: `engine_state.dart`, `video_effect_type.dart`

**renderer_config.dart** — 2 methods (per D-06):
- `void setD3d11SyncEnabled(bool enabled)`
- `void setHardwareDecoding(bool enabled)`
Import only: `engine_state.dart`

**engine_state.dart** — Remove the 15 methods that moved to sub-mixins. Keep:
- 12 ValueNotifiers (per D-08 — all stay in base)
- 3 abstract getters: errorType, mediaInfo, subtitleDelay
- Core playback: open, play, pause, stop, togglePlayPause, seekTo, setVolume, setMute, setPlaybackRate, setRange, skipForward, skipBack
- dispose
- All existing exports (media_error_type, media_state, media_info, video_codec_info, video_effect_type, audio_track_info, subtitle_track_info)

Add re-exports for the 3 new mixin files at the top of engine_state.dart so consumers importing engine_state.dart get TrackControl/VideoEffects/RendererConfig automatically:
```dart
export 'track_control.dart';
export 'video_effects.dart';
export 'renderer_config.dart';
```

This ensures the 57 UI files that `import 'engine_state.dart'` automatically get the new mixin types without changing their import statements.
  </action>
  <verify>
    <automated>cd /d D:\simple_player_flutter && dart analyze lib/kernel/engine/track_control.dart lib/kernel/engine/video_effects.dart lib/kernel/engine/renderer_config.dart lib/kernel/engine/engine_state.dart 2>&1 | findstr /C:"error" /C:"warning"</automated>
  </verify>
  <done>
    - track_control.dart exists with `mixin TrackControl on EngineState` and 9 abstract methods
    - video_effects.dart exists with `mixin VideoEffects on EngineState` and 4 abstract methods
    - renderer_config.dart exists with `mixin RendererConfig on EngineState` and 2 abstract methods
    - engine_state.dart no longer contains the 15 moved method signatures
    - engine_state.dart re-exports all 3 new mixin files
    - `dart analyze` on all 4 files passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire mixin composition into FvpEngine, MockEngine, FakeEngine</name>
  <files>
    lib/kernel/engine/fvp_engine.dart,
    lib/kernel/engine/mock_engine.dart,
    test/helpers/fake_engine.dart
  </files>
  <action>
Update all 3 engine classes to mix in the 3 new sub-mixins. The concrete implementations already exist — this is purely a declaration change.

**fvp_engine.dart** — Change line 41:
- FROM: `class FvpEngine with EngineState {`
- TO: `class FvpEngine with EngineState, TrackControl, VideoEffects, RendererConfig {`
No other changes needed — FvpEngine already implements all 15 methods via _guardedAction delegation to helpers (TrackManager, VideoEffectController, D3D11Configurator).

**mock_engine.dart** — Change line 57:
- FROM: `class MockEngine with EngineState {`
- TO: `class MockEngine with EngineState, TrackControl, VideoEffects, RendererConfig {`
No other changes needed — MockEngine already has stub implementations for all 15 methods.

**fake_engine.dart** — Change line 9:
- FROM: `class FakeEngine with EngineState {`
- TO: `class FakeEngine with EngineState, TrackControl, VideoEffects, RendererConfig {`
No other changes needed — FakeEngine already has all 15 method implementations with call tracking.

Note: The `with` clause order matters — EngineState must come first since TrackControl/VideoEffects/RendereConfig have `on EngineState` constraint.
  </action>
  <verify>
    <automated>cd /d D:\simple_player_flutter && flutter analyze lib/kernel/engine/fvp_engine.dart lib/kernel/engine/mock_engine.dart 2>&1 | findstr /C:"error" /C:"No issues"</automated>
  </verify>
  <done>
    - FvpEngine declaration includes `with EngineState, TrackControl, VideoEffects, RendererConfig`
    - MockEngine declaration includes `with EngineState, TrackControl, VideoEffects, RendererConfig`
    - FakeEngine declaration includes `with EngineState, TrackControl, VideoEffects, RendererConfig`
    - `flutter analyze` passes with zero errors on all 3 files
    - All existing method implementations remain unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Add mixin capability test + full test verification</name>
  <files>
    test/engine/mixin_capability_test.dart
  </files>
  <action>
Create a new test file that verifies the mixin composition works correctly at runtime. This is the primary verification for IFACE-05 (capability checks).

**test/engine/mixin_capability_test.dart**:
- Test group "Mixin capability checks":
  - `FakeEngine is TrackControl` → true
  - `FakeEngine is VideoEffects` → true
  - `FakeEngine is RendererConfig` → true
  - `FakeEngine is EngineState` → true (base still works)
  - Dart 3 pattern matching: `if (engine case TrackControl tc)` succeeds and tc methods are callable
  - `engine case TrackControl tc` → tc.getAudioTracks() returns empty list (FakeEngine default)
  - `engine case VideoEffects ve` → ve.setVideoEffect() is callable without error
  - `engine case RendererConfig rc` → rc.setD3d11SyncEnabled() is callable without error
- Test group "EngineState base still works":
  - FakeEngine.open() succeeds
  - FakeEngine.play() sets state to playing
  - FakeEngine.dispose() works
  - All 12 ValueNotifiers are accessible

Use FakeEngine as the test subject — it's hand-written, no FFI, runs purely in Dart.

After creating the test, run the FULL test suite to verify zero regressions:
```
flutter test --no-pub
```
Expect: 840+ pass, 3 golden failures (pre-existing, unrelated).
  </action>
  <verify>
    <automated>cd /d D:\simple_player_flutter && flutter test test/engine/mixin_capability_test.dart --no-pub 2>&1 | findstr /C:"+"" /C:"All tests passed"</automated>
  </verify>
  <done>
    - test/engine/mixin_capability_test.dart exists with capability check tests
    - `engine is TrackControl` / `is VideoEffects` / `is RendererConfig` all return true for FakeEngine
    - Dart 3 pattern matching `engine case TrackControl tc` works correctly
    - Full test suite: 840+ pass, 3 golden failures (pre-existing)
    - Zero new test failures introduced
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mixin → concrete class | Abstract mixin methods must all be implemented by concrete classes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced — pure Dart refactoring |
| T-03-02 | Elevation | mixin composition | low | accept | `mixin X on EngineState` enforces compile-time constraint; no privilege escalation possible |

No new external dependencies. No user input handling changes. No network calls. This is a pure structural refactoring.
</threat_model>

<verification>
## Phase Verification

1. `dart analyze lib/` — zero errors, zero warnings
2. `flutter test --no-pub` — 840+ pass, 3 golden failures (pre-existing)
3. `grep -r "class FvpEngine" lib/` shows `with EngineState, TrackControl, VideoEffects, RendererConfig`
4. `grep -r "class MockEngine" lib/` shows `with EngineState, TrackControl, VideoEffects, RendererConfig`
5. `grep -r "class FakeEngine" test/` shows `with EngineState, TrackControl, VideoEffects, RendererConfig`
6. All 57 UI files importing engine_state.dart compile without changes
7. `engine is TrackControl` returns true at runtime (verified by mixin_capability_test.dart)
</verification>

<success_criteria>
- TrackControl/VideoEffects/RendererConfig mixin interfaces defined with correct method signatures
- FvpEngine/MockEngine/FakeEngine compose all 3 sub-mixins via `with` clause
- 57 UI files zero-change backward compatibility (EngineState import still works)
- `engine is TrackControl` / `is VideoEffects` / `is RendererConfig` capability checks work at runtime
- `flutter analyze` zero errors
- `flutter test` 840+ pass (3 golden failures pre-existing)
</success_criteria>

<output>
Create `.planning/phases/03-iface-optimization/03-01-SUMMARY.md` when done
</output>
