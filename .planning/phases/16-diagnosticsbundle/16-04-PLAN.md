---
phase: 16-diagnosticsbundle
plan: 04
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - test/adapter/kernel_adapter_contract_test.dart
  - test/adapter/kernel_adapter_identity_test.dart
  - test/diagnostics/diagnostics_bundle_test.dart
  - test/diagnostics/kernel_logger_test.dart
autonomous: true
requirements: [ADAPT-01, ADAPT-02, ADAPT-03, ADAPT-04]
must_haves:
  truths:
    - "The 7 Phase-15 contract test groups pass mounted against KernelAdapter via factory swap (D24 layer 2)"
    - "All 13 EngineStateView ValueNotifiers are same() as the wrapped legacy engine's notifiers (D24 layer 3 / D25)"
    - "ADAPT-04 ('single arbiter + no dual source') is satisfied in P16 by KernelMode arbiter (16-01) + D22 grep gate at 0 hits (16-05); counter migration is a P20 placeholder in the D21 class-level checklist — NOT by a P16 adapter-layer openGeneration test (D20: P16 adapter is transparent, guards live in old engine)"
    - "DiagnosticsBundle.noop() constructs + cascading dispose is safe; KernelLogger accepts all 3 live call shapes (ADAPT-02/D6)"
    - "The existing full suite stays green after the composition-root swap (D24 layer 1 regression)"
  artifacts:
    - "test/adapter/kernel_adapter_contract_test.dart"
    - "test/adapter/kernel_adapter_identity_test.dart"
    - "test/diagnostics/diagnostics_bundle_test.dart"
    - "test/diagnostics/kernel_logger_test.dart"
  key_links:
    - "Contract mount reuses Phase 15 run*ContractTests via factory swap only (contract_test_runner.dart)"
    - "Identity test is the same() gate catching Blocking Constraint #6 (rewrapped notifier)"
---

<objective>
Build the three-layer test composition (D24): (1) the existing full suite stays green
[regression], (2) the 7 Phase-15 contract test groups re-mounted against KernelAdapter via
factory swap [显式 forwarding fidelity], (3) the `same()` notifier-identity widget test
[专项, D25]. Plus the ADAPT-02 diagnostics unit tests (bundle construction/dispose +
KernelLogger signature acceptance).

Purpose: Prove zero behavior change (ADAPT-01), notifier identity forwarding (ADAPT-03, #6),
and the diagnostics carrier skeleton (ADAPT-02). ADAPT-04 ('single arbiter + no dual source')
is NOT tested at the adapter layer in P16 — D20 rules the P16 adapter transparent (no counter,
guards live in the old engine fvp_engine.dart:259/267/311/320), so an adapter-layer
openGeneration test would be testing nothing (#8 KISS forbids). ADAPT-04 is instead satisfied
by: the KernelMode arbiter in 16-01, the D22 grep gate at 0 hits in 16-05, and the P20
forward-looking placeholder in the D21 class-level checklist. D24 picks three layers because it
is the most complete coverage (implicit regression + explicit contract + targeted identity) and
reuses Phase 15's parameterized `accepts MediaEngine` design.

Output: 4 test files under test/adapter/ and test/diagnostics/. All green + existing suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-diagnosticsbundle/16-CONTEXT.md
@.planning/phases/16-diagnosticsbundle/16-PATTERNS.md
@.planning/phases/16-diagnosticsbundle/16-VALIDATION.md

# Contract-test reuse mechanism + texture-channel mock to copy verbatim:
@test/engine/fvp_engine_contract_test.dart
@test/contracts/contract_test_runner.dart

# EngineStateView notifier field list (source of truth for the 13 same() assertions):
@lib/kernel/engine/engine_state_view.dart

# Units under test (produced by Plans 16-01 and 16-02):
@lib/kernel/adapter/kernel_adapter.dart
@lib/kernel/diagnostics/kernel_logger.dart
@lib/kernel/diagnostics/diagnostics_bundle.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract-test mount (D24 layer 2) — 7 ISP groups via factory swap</name>
  <files>test/adapter/kernel_adapter_contract_test.dart</files>
  <action>
Create `test/adapter/kernel_adapter_contract_test.dart` by copying the mount-point structure
from `test/engine/fvp_engine_contract_test.dart` (PATTERNS.md lines 364-407), changing ONLY the
factory. Copy the ENTIRE `_installFvpTextureChannelMock()` function plus the `_fvpChannel`
constant and `_nextFakeTextureId` counter (lines 36-56 of the analog) verbatim — KernelAdapter
wraps a real FvpEngine that hits the same headless native-texture gap (MissingPluginException
on CreateRT/ReleaseRT without the mock).

Define a local factory:
  KernelAdapter makeAdapter() {
    final fvp = FvpEngine();
    return KernelAdapter(
      legacy: fvp,
      migrated: fvp,
      policy: const DelegationPolicy.all(KernelMode.legacy),
    );
  }
Then call all 7 run*ContractTests(makeAdapter): runEngineStateViewContractTests,
runPlaybackControlContractTests, runTrackControlContractTests, runSubtitleConfigContractTests,
runVideoEffectControlContractTests, runRendererControlContractTests,
runVolumeControlContractTests. Import `../contracts/contract_test_runner.dart`, the adapter
file, and the FvpEngine file. This is D24 layer 2 — proves all 7 ISP contracts forward
faithfully through the adapter.

Do NOT create a separate adapter-layer openGeneration test file (D20: P16 adapter is
transparent — no counter, no guard, open() 100% forwards; the openGeneration guard lives in
the old engine at fvp_engine.dart:259/267/311/320; testing adapter-layer generation behavior in
P16 would be testing nothing, which #8 KISS forbids). The PlaybackControl contract test group
already covers open() forwarding fidelity. ADAPT-04 is satisfied by 16-01 (KernelMode arbiter +
D21 placeholder) + 16-05 (D22 grep gate at 0 hits), not by an adapter-layer test here.
  </action>
  <verify>
    <automated>flutter test test/adapter/kernel_adapter_contract_test.dart</automated>
  </verify>
  <done>Contract test mounts all 7 run*ContractTests against KernelAdapter (factory swap only) and passes; the texture-channel mock is copied verbatim. No separate adapter-layer openGeneration test is created (D20/#8 KISS). Green.</done>
</task>

<task type="auto">
  <name>Task 2: Notifier identity test — same() for all 13 EngineStateView notifiers (D24 layer 3 / D25 / ADAPT-03)</name>
  <files>test/adapter/kernel_adapter_identity_test.dart</files>
  <action>
Create `test/adapter/kernel_adapter_identity_test.dart` implementing D25. Copy the test-body
pattern from PATTERNS.md lines 435-456. Construct a legacy FvpEngine, wrap it in a KernelAdapter
(legacy: legacy, migrated: legacy, policy: all-legacy), then assert `expect(adapter.X,
same(legacy.X))` for EACH of the 13 EngineStateView ValueNotifier fields (from
engine_state_view.dart, PATTERNS.md lines 416-432):
  textureId, state, position, duration, volume, isMuted, isBuffering, isSeeking, subtitleText,
  buffered, aspectRatio, lastError, playbackSpeed.
Exclude `mediaInfo` (plain getter, not a ValueNotifier — no same() needed) and `dispose()`
(method). `same()` is reference-identity (ships in package:matcher via flutter_test), NOT
`equals()` — this is the literal "实例相等性" from Success Criterion 3.

This is THE test that catches Blocking Constraint #6 violations (a rewrapped
`ValueNotifier(x.value)` getter would fail same() while passing value-equality) — it is
critical, not optional. Note that all 13 must be present; a missing assertion is a coverage
gap that lets a silently-detached notifier slip through.

Use the texture-channel mock only if constructing FvpEngine here requires it (identity checks
don't call open(), so the mock may be unnecessary — add it only if analyze/runtime demands).
  </action>
  <verify>
    <automated>flutter test test/adapter/kernel_adapter_identity_test.dart</automated>
  </verify>
  <done>Identity test asserts same() for all 13 EngineStateView ValueNotifier fields (textureId..playbackSpeed), excluding mediaInfo and dispose(). Uses same() (reference identity), not equals(). Green — proving the adapter forwards the wrapped instance, never rewraps (ADAPT-03, #6).</done>
</task>

<task type="auto">
  <name>Task 3: Diagnostics unit tests — bundle construction/dispose + KernelLogger 3-shape acceptance (ADAPT-02/D6)</name>
  <files>test/diagnostics/diagnostics_bundle_test.dart, test/diagnostics/kernel_logger_test.dart</files>
  <action>
Create `test/diagnostics/diagnostics_bundle_test.dart` (ADAPT-02): assert
`const DiagnosticsBundle.noop()` constructs without error; all 4 slots are non-null and callable
as no-ops (e.g. `bundle.logger.info('x')`, `bundle.metrics.recordOpen(success: true)`,
`bundle.eventLog.add('open')`, `bundle.memoryMonitor.snapshot()` return safely); and
`bundle.dispose()` runs without throwing (cascading dispose over the noop slots, D10). Follow
standard flutter_test group/test/expect conventions (AAA).

Create `test/diagnostics/kernel_logger_test.dart` (D6 census): assert a NullKernelLogger accepts
all 3 live call shapes without error — (a) `error('msg', error: e, stackTrace: st)` [both named],
(b) `error('msg', stackTrace: st)` [stackTrace-only], (c) `error('msg')` [neither], and the
same 3 shapes for `fatal(...)`; plus `trace/debug/info/warn` each accept a single positional
String. This is a compile+execute signature-acceptance test (the 84-call-site census found 3
`.e()` sites using named args in 2 shapes — the interface must express all of them). Standard
flutter_test conventions.
  </action>
  <verify>
    <automated>flutter test test/diagnostics/diagnostics_bundle_test.dart test/diagnostics/kernel_logger_test.dart</automated>
  </verify>
  <done>diagnostics_bundle_test asserts noop construction, 4 callable noop slots, and safe cascading dispose. kernel_logger_test asserts error()/fatal() accept all 3 named-arg shapes (both, stackTrace-only, neither) plus single-positional trace/debug/info/warn. Both green.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite regression gate (D24 layer 1) — existing suite green after composition-root swap</name>
  <files>test/adapter/kernel_adapter_contract_test.dart</files>
  <action>
Run the ENTIRE existing test suite (`flutter test`) to confirm D24 layer 1 (regression): after
Plan 16-03's composition-root swap (FvpEngine -> KernelAdapter), every existing test —
including the pre-existing `test/engine/fvp_engine_contract_test.dart` mount against raw
FvpEngine (which stays valid since fvp_engine.dart is untouched per D20) and all
PlaybackController/VideoProcessingService/widget tests — must stay green. This task adds no new
file; it is the verification gate that the seam introduced zero observable behavior change
(ADAPT-01, Success Criterion 1). If any existing test fails, the failure is a real regression
in the adapter or the wiring — fix the adapter (Plan 16-01) or wiring (Plan 16-03), never the
existing tests (they are the frozen baseline).

If a headless-native-texture failure surfaces in a pre-existing test that newly routes through
the adapter, confirm the texture-channel mock coverage matches the Phase 15 pattern; do not
weaken any existing assertion.
  </action>
  <verify>
    <automated>flutter test</automated>
  </verify>
  <done>`flutter test` (full suite) is green: existing tests + the new adapter/diagnostics tests all pass. No existing assertion weakened or removed. Confirms zero behavior change through the seam (ADAPT-01 regression, D24 layer 1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan. No production trust boundary introduced. Tests exercise the in-process delegation seam; no new input/network/persistence surface. Actors unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Tampering | Test suite integrity (frozen baseline) | low | mitigate | D24 layer 1 forbids weakening/removing existing assertions to make the suite pass — a regression must be fixed in the adapter/wiring, not the tests. The identity test (D25) and contract mount (D24 L2) are structural gates that cannot be satisfied by a broken adapter. |
| T-16-01 | Tampering | notifier identity (silent detach) | low | mitigate | The same() identity test (D25) is the explicit structural control that catches a rewrapped notifier (Blocking Constraint #6) — the one correctness/regression risk this phase carries, addressed by a test rather than a security control per RESEARCH Security Domain. |

**threat_level: low** — no high-severity threats. Test-only plan verifying a pure delegation
seam. No new auth/input/crypto/file-system/external-call surface. Gate emitted unconditionally
per Step 5.55.
</threat_model>

<verification>
Wave 3 checks:
- `flutter test test/adapter/ test/diagnostics/` green (scoped fast run — 4 new files: contract mount, identity, diagnostics_bundle, kernel_logger).
- `flutter test` (full suite) green — D24 layer 1 regression + layer 2 contract mount + layer 3 identity all pass.
- Identity test asserts exactly 13 same() (one per EngineStateView notifier) — no field omitted (D25).
- kernel_logger_test covers all 3 live call shapes (D6 census).
- No adapter-layer openGeneration test exists (D20/#8 KISS — P16 adapter is transparent); ADAPT-04 covered by 16-01 KernelMode arbiter + D21 placeholder + 16-05 D22 grep gate.
</verification>

<success_criteria>
- D24 layer 1: existing full suite stays green after the swap (ADAPT-01 regression).
- D24 layer 2: 7 Phase-15 contract groups pass mounted against KernelAdapter via factory swap (ADAPT-01).
- D24 layer 3 / D25: same() passes for all 13 EngineStateView notifiers (ADAPT-03, #6).
- ADAPT-04 ('single arbiter + no dual source') satisfied by 16-01 KernelMode arbiter + 16-05 D22 grep gate (0 hits) + D21 class-level P20 placeholder — NOT by an adapter-layer openGeneration test (D20: P16 adapter transparent, #8 KISS).
- diagnostics tests green — noop construction/cascade + KernelLogger 3-shape acceptance (ADAPT-02, D6).
</success_criteria>

<output>
Create `.planning/phases/16-diagnosticsbundle/16-04-SUMMARY.md` when done.
</output>
