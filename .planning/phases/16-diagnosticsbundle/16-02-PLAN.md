---
phase: 16-diagnosticsbundle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/diagnostics/kernel_logger.dart
  - lib/kernel/diagnostics/memory_monitor_slot.dart
  - lib/kernel/diagnostics/metrics_slot.dart
  - lib/kernel/diagnostics/event_log_slot.dart
  - lib/kernel/diagnostics/diagnostics_bundle.dart
autonomous: true
requirements: [ADAPT-02, ADAPT-05]
must_haves:
  truths:
    - "DiagnosticsBundle.noop() constructs as a const value with 4 noop slots (D1/D4)"
    - "KernelLogger has exactly trace/debug/info/warn/error/fatal; error/fatal carry {Object? error, StackTrace? stackTrace} (D5/D6)"
    - "3 remaining slots (MemoryMonitorSlot/MetricsSlot/EventLogSlot) are minimal abstract interfaces with instance methods, no static singleton, no concrete-type coupling (D9/D10)"
    - "DiagnosticsBundle.dispose() cascades to its slots (D10 mirror of PlayerServices)"
  artifacts:
    - "lib/kernel/diagnostics/kernel_logger.dart (KernelLogger + NullKernelLogger)"
    - "lib/kernel/diagnostics/memory_monitor_slot.dart (MemoryMonitorSlot + NullMemoryMonitorSlot)"
    - "lib/kernel/diagnostics/metrics_slot.dart (MetricsSlot + NullMetricsSlot)"
    - "lib/kernel/diagnostics/event_log_slot.dart (EventLogSlot + NullEventLogSlot)"
    - "lib/kernel/diagnostics/diagnostics_bundle.dart (DiagnosticsBundle + .noop() factory)"
  key_links:
    - "DiagnosticsBundle.noop() is the const default injected into KernelAdapter (D10/D12)"
    - "Slot names avoid collision with existing MemoryMonitor/EngineMetrics/EngineEventLog (D9)"
---

<objective>
Build the DiagnosticsBundle carrier skeleton: 5 files in a new `lib/kernel/diagnostics/`
subpackage (D11). Four minimal abstract slot interfaces (KernelLogger, MemoryMonitorSlot,
MetricsSlot, EventLogSlot), each with a `Null*` const noop implementation, plus the
`DiagnosticsBundle` final-class carrier with a `const DiagnosticsBundle.noop()` factory and
cascading dispose.

Purpose: Give Phases 17-20 a construction-injected home to attach real diagnostics
capabilities. Phase 16 is deliberate dead code (D2/D3) — all slots noop, no consumer until
Phase 20's NewFvpEngine. The bundle contracts are traced from the existing concrete classes'
public APIs but decoupled from their implementation details (D9/D10).

Output: 5 files under `lib/kernel/diagnostics/`. The bundle's `.noop()` const default is what
KernelAdapter's constructor (Plan 16-01) uses as its `bundle` parameter default.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-diagnosticsbundle/16-CONTEXT.md
@.planning/phases/16-diagnosticsbundle/16-PATTERNS.md

# Concrete-class public APIs to trace minimal slot method sets FROM (do NOT copy impl details — D9/D10):
@lib/kernel/utils/memory_monitor.dart
@lib/kernel/engine/engine_metrics.dart
@lib/kernel/engine/engine_event_log.dart

# DI-container dispose-cascade pattern to mirror:
@lib/kernel/player_services.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KernelLogger interface + NullKernelLogger (D5/D6/D7 — signature only)</name>
  <files>lib/kernel/diagnostics/kernel_logger.dart</files>
  <behavior>
    - NullKernelLogger() is const-constructible.
    - error()/fatal() accept all 3 live call shapes (both named, stackTrace-only, neither) — verified by Plan 16-04 kernel_logger_test.
    - trace/debug/info/warn accept a single positional String message.
  </behavior>
  <action>
Create `lib/kernel/diagnostics/` and `kernel_logger.dart`. Copy the abstract class +
NullKernelLogger VERBATIM from PATTERNS.md lines 158-186:

`abstract class KernelLogger` with EXACTLY 6 methods:
  void trace(String message);
  void debug(String message);
  void info(String message);
  void warn(String message);
  void error(String message, {Object? error, StackTrace? stackTrace});
  void fatal(String message, {Object? error, StackTrace? stackTrace});

D6 rationale (from the 84-call-site census): both `error` and `fatal` carry BOTH optional
named params because 3 of 84 live `.e()` sites pass named args in 2 shapes (error:+stackTrace:
and stackTrace:-only). trace/debug/info/warn take only `String message`. `fatal` mirrors
`error` by symmetry (0 live `.f()` sites today — flagged [ASSUMED] in RESEARCH A1, acceptable
per D6).

`final class NullKernelLogger implements KernelLogger` with `const NullKernelLogger()` and
empty method bodies for all 6.

Do NOT add a LogLevel enum, sink interface, redaction API, or formatting (D7 — those are
Phase 17's job hidden behind this interface). Do NOT port log.dart's PrefixPrinter /
JsonPrinter / _RotatingFileOutput / initLog() machinery.

Document the D8 level-mapping (log*.t/d/i/w/e/f -> trace/debug/info/warn/error/fatal) in a
class-level `///` doc comment so Phase 17's migration is a table lookup (D8). Add bilingual
DOC-01 comments (Chinese intent + English contract).
  </action>
  <verify>
    <automated>test -f lib/kernel/diagnostics/kernel_logger.dart && grep -q 'void error(String message, {Object? error, StackTrace? stackTrace});' lib/kernel/diagnostics/kernel_logger.dart && grep -q 'void fatal(String message, {Object? error, StackTrace? stackTrace});' lib/kernel/diagnostics/kernel_logger.dart && grep -q 'const NullKernelLogger()' lib/kernel/diagnostics/kernel_logger.dart</automated>
  </verify>
  <done>kernel_logger.dart has KernelLogger with exactly 6 methods (error/fatal with both optional named params, others single positional), NullKernelLogger const noop impl, no LogLevel/sink/redaction/formatting additions (D7). `flutter analyze lib/kernel/diagnostics/kernel_logger.dart` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3 slot interfaces + Null impls — MemoryMonitorSlot, MetricsSlot, EventLogSlot (D9/D10)</name>
  <files>lib/kernel/diagnostics/memory_monitor_slot.dart, lib/kernel/diagnostics/metrics_slot.dart, lib/kernel/diagnostics/event_log_slot.dart</files>
  <behavior>
    - NullMemoryMonitorSlot() / NullMetricsSlot() / NullEventLogSlot() are all const-constructible.
    - Each Null impl no-ops every method and returns empty/default/null for readers.
    - Each slot exposes dispose() so DiagnosticsBundle.dispose() can cascade (Plan 16-04 verifies cascade).
  </behavior>
  <action>
Create 3 files, each holding one minimal abstract slot interface + its `Null*` const noop
impl. Derive method sets from the concrete classes' PUBLIC APIs but do NOT copy implementation
details (D9/D10): no `static` singleton pattern, no concrete field names, and use loose return
types (`Object?` / `Map<String, Object?>`) so Phases 18/19 can change concrete shapes without
breaking the interface.

`memory_monitor_slot.dart` — `abstract class MemoryMonitorSlot` traced from MemoryMonitor's
public surface (start/stop/snapshot). Copy the instance-method contract from PATTERNS.md
lines 205-224:
  void start({Duration? interval});
  void stop();
  Object? snapshot();  // loose return — do NOT couple to concrete MemorySnapshot (D10)
  void dispose();
Plus `final class NullMemoryMonitorSlot implements MemoryMonitorSlot` (const, noops,
snapshot()=>null). The `Slot` suffix avoids the D9 name collision with existing
`MemoryMonitor`. Do NOT import or reference MemorySnapshot/MetricSample.

`metrics_slot.dart` — `abstract class MetricsSlot` traced from EngineMetrics record* verbs
(engine_metrics.dart:44-79). Expose the record verbs (the actual capability P18 needs) WITHOUT
the internal counter fields (framesDropped/_totalSeekTime stay impl-private in the real class,
D10):
  void recordOpen({required bool success});
  void recordSeek(Duration elapsed);
  void recordFrameDrop([int count = 1]);
  void recordDecodeError();
  void recordBufferUnderrun();
  void reset();
  Map<String, Object?> toJson();  // loose value type, not the concrete Map<String, Object>
  void dispose();
Plus `final class NullMetricsSlot implements MetricsSlot` (const, noops all record*/reset,
toJson()=>const <String, Object?>{}).

`event_log_slot.dart` — `abstract class EventLogSlot` traced from EngineEventLog
(engine_event_log.dart:66-102). Loosen `entries`/`toJson` off the concrete EngineEvent type
(D10):
  void add(String type, [Map<String, Object?>? data]);
  List<Map<String, Object?>> get entries;  // loosened from List<EngineEvent> (D10)
  void clear();
  List<Map<String, Object?>> toJson();
  void dispose();
Plus `final class NullEventLogSlot implements EventLogSlot` (const, add/clear noop,
entries/toJson return const empty lists).

Keep each file ~40-60 lines (D11). Bilingual DOC-01 comments. Do NOT add methods beyond these
minimal sets (D11/RESEARCH Pitfall 3 — every extra method is size-budget risk + speculative
design Phase 17-19 may contradict).
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/diagnostics/memory_monitor_slot.dart lib/kernel/diagnostics/metrics_slot.dart lib/kernel/diagnostics/event_log_slot.dart</automated>
  </verify>
  <done>3 slot files exist, each with a minimal instance-method abstract interface (no static, no concrete-type coupling per D9/D10) + a const Null* noop impl exposing dispose(). Loose return types (Object?/Map<String,Object?>/List<Map<String,Object?>>) used to avoid coupling to MemorySnapshot/EngineEvent. `flutter analyze` clean on all 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DiagnosticsBundle carrier + const .noop() factory + cascading dispose (D1/D4/D10)</name>
  <files>lib/kernel/diagnostics/diagnostics_bundle.dart</files>
  <behavior>
    - const DiagnosticsBundle.noop() constructs a value with all 4 noop slots.
    - DiagnosticsBundle.dispose() calls dispose() on each disposable slot (cascade, D10).
    - The primary const constructor accepts all 4 slots as required named params.
  </behavior>
  <action>
Create `diagnostics_bundle.dart`. Import the 4 slot files. Copy the carrier VERBATIM from
PATTERNS.md lines 284-310:

`final class DiagnosticsBundle` (final class supports dispose, blocks accidental inheritance,
D1) with 4 final slot fields: `KernelLogger logger`, `MemoryMonitorSlot memoryMonitor`,
`MetricsSlot metrics`, `EventLogSlot eventLog`.

Primary `const DiagnosticsBundle({required this.logger, required this.memoryMonitor,
required this.metrics, required this.eventLog})`.

`const DiagnosticsBundle.noop()` factory initializing logger = const NullKernelLogger(),
memoryMonitor = const NullMemoryMonitorSlot(), metrics = const NullMetricsSlot(),
eventLog = const NullEventLogSlot() (D4 — the sole const factory, Phase 16 default).

`void dispose()` cascading to memoryMonitor.dispose(), metrics.dispose(), eventLog.dispose()
(D10 — mirrors PlayerServices dispose cascade at player_services.dart:99-109; KernelLogger has
no dispose in its D7-capped contract, so it is not in the cascade). Add a `///` note that noop
slots no-op but future real slots may hold timers/streams needing cleanup.

Bilingual DOC-01 comments. This is deliberate dead code in Phase 16 (D3) — note in the
class-level doc that no consumer reads the bundle until Phase 20's NewFvpEngine.
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/diagnostics/diagnostics_bundle.dart && grep -q 'const DiagnosticsBundle.noop()' lib/kernel/diagnostics/diagnostics_bundle.dart</automated>
  </verify>
  <done>diagnostics_bundle.dart has final-class DiagnosticsBundle with 4 final slot fields, a required-named primary const constructor, a const .noop() factory wiring all 4 Null* slots, and a dispose() that cascades to the 3 disposable slots. `flutter analyze lib/kernel/diagnostics/` clean across all 5 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 16 diagnostics slots are all no-ops (D2/D3). No new trust boundary: nothing is emitted, stored, transmitted, or persisted. Actors = same as existing (in-process, single-user desktop app). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02 | Information Disclosure | Noop diagnostics slots (logger/memory/metrics/eventLog) | low | accept | All 4 slots are no-op bodies — they emit/store nothing in Phase 16. Path redaction and log sink controls are Phase 17's responsibility behind the KernelLogger interface (D7). No data can leak from empty method bodies. Defaults suffice. |
| T-16-03 | Denial of Service | DiagnosticsBundle.dispose() cascade | low | accept | Noop slot dispose() bodies are empty; no resource exhaustion possible. Cascade shape mirrors the already-audited PlayerServices.dispose(). |

**threat_level: low** — no high-severity threats, no blocking mitigations. Pure noop carrier
skeleton with no auth/input/crypto/file-system/external-call surface. Gate emitted
unconditionally per Step 5.55.
</threat_model>

<verification>
Wave 1 checks (after this plan, before Plan 16-01 consumes the bundle type):
- `flutter analyze lib/kernel/diagnostics/` clean across all 5 files.
- `const DiagnosticsBundle.noop()` compiles as a const value (enables the const default in KernelAdapter's constructor, Plan 16-01).
- No `static` member on any slot interface (D10); no import of MemorySnapshot/MetricSample/EngineEvent concrete types in slot contracts (D10).
- Combined size stays within the D27 6-file budget (5 diagnostics files ~230 lines; full gate in Plan 16-05).
</verification>

<success_criteria>
- 5 files under lib/kernel/diagnostics/ (D11): kernel_logger, memory_monitor_slot, metrics_slot, event_log_slot, diagnostics_bundle (ADAPT-02).
- KernelLogger has exactly 6 methods with D6-resolved error/fatal signatures (D5/D6); no LogLevel/sink/redaction (D7).
- 3 remaining slots are minimal instance-method abstract interfaces, no static, no concrete-type coupling (D9/D10).
- DiagnosticsBundle final class + const .noop() default (D1/D4) + cascading dispose (D10).
- Deliberate dead code — no Phase 16 consumer (D2/D3).
- Diagnostics files ~230 lines total (ADAPT-05 budget headroom, D27).
</success_criteria>

<output>
Create `.planning/phases/16-diagnosticsbundle/16-02-SUMMARY.md` when done.
</output>
