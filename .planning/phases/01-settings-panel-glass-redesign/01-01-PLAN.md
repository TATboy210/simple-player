---
phase: 01-settings-panel-glass-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/shared/section_header.dart
  - lib/ui/shared/settings_card.dart
  - lib/ui/dialogs/settings_panel.dart
  - lib/ui/dialogs/settings/_settings_nav_item.dart
autonomous: true
requirements: [COMP-04, STYLE-01, STYLE-02, STYLE-03, STYLE-04]

must_haves:
  truths:
    - Settings panel outer shell uses GlassContainer with BackdropFilter blur
    - Settings panel title bar matches control bar glass style
    - Sidebar nav items have glass hover feedback matching control bar buttons
    - _SectionHeader is accessible from a shared import location
  artifacts:
    - lib/ui/shared/section_header.dart
    - lib/ui/dialogs/settings_panel.dart (modified)
    - lib/ui/dialogs/settings/_settings_nav_item.dart (modified)
  key_links:
    - _SectionHeader import path used by all 7 tabs
    - GlassContainer in panel shell works with overlay BackdropFilter
    - _SettingsNavItem hover matches GlassButton.iconOnly pattern

---

<objective>
Foundation: extract _SectionHeader to shared file, apply glassmorphism to panel shell and sidebar nav.

Purpose: Establish the shared _SectionHeader component and glass panel shell that all 7 tabs will use in subsequent plans. Without this, tab migration cannot proceed.
Output: section_header.dart, modified settings_panel.dart with GlassContainer shell, modified _settings_nav_item.dart with glass hover.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-settings-panel-glass-redesign/01-RESEARCH.md
@lib/ui/shared/settings_card.dart
@lib/ui/shared/glass_container.dart
@lib/ui/dialogs/settings_panel.dart
@lib/ui/dialogs/settings/_settings_nav_item.dart
@lib/ui/theme/tokens.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract _SectionHeader to shared file</name>
  <files>lib/ui/shared/section_header.dart, lib/ui/shared/settings_card.dart</files>

  <read_first>
    lib/ui/shared/settings_card.dart
    lib/ui/shared/glass_container.dart
    lib/ui/theme/tokens.dart
  </read_first>

  <behavior>
    - Test: SectionHeader renders title text correctly
    - Test: SectionHeader renders icon when provided
    - Test: SectionHeader renders description when provided
    - Test: SectionHeader uses Tokens.textSecondary for title color
    - Test: SectionHeader uses Tokens.fontCaption for title font size
  </behavior>

  <action>
Create `lib/ui/shared/section_header.dart` with a public `SectionHeader` widget (rename from `_SectionHeader` to `SectionHeader` since it's now public). Copy the exact implementation from `settings_card.dart` lines 234-273. The widget takes `title` (required String), `description` (optional String), and `icon` (optional IconData).

In `settings_card.dart`:
1. Add import for `section_header.dart`
2. Replace all `_SectionHeader(` references with `SectionHeader(`
3. Remove the `_SectionHeader` class definition (lines 234-273)
4. Keep the `SettingsCard`, `SettingRow`, `SettingSwitchRow` classes intact for now
5. Keep the exports for `setting_action_row.dart`, `setting_slider_row.dart`, `settings_action_card.dart`, `settings_expander_card.dart`

Per D-03 (精简组件层级): This extraction is the first step toward removing SettingsCard while preserving the section header pattern.
  </action>

  <verify>
    <automated>flutter test test/widget/shared/section_header_test.dart 2>/dev/null; flutter analyze lib/ui/shared/section_header.dart lib/ui/shared/settings_card.dart</automated>
  </verify>

  <acceptance_criteria>
    - `lib/ui/shared/section_header.dart` exists and exports `SectionHeader` class
    - `SectionHeader` has `title`, `description`, `icon` constructor parameters
    - `settings_card.dart` imports `section_header.dart` and uses `SectionHeader` (no `_SectionHeader` class defined)
    - `flutter analyze` passes with no new errors on both files
    - Existing `SettingsCard`, `SettingRow`, `SettingSwitchRow` classes unchanged
  </acceptance_criteria>

  <done>
    SectionHeader extracted to shared file, settings_card.dart compiles without _SectionHeader, flutter analyze clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply GlassContainer to settings panel shell</name>
  <files>lib/ui/dialogs/settings_panel.dart</files>

  <read_first>
    lib/ui/dialogs/settings_panel.dart
    lib/ui/shared/glass_container.dart
    lib/ui/theme/tokens.dart
    lib/ui/player/control_bar.dart (lines 1-50 for glass styling reference)
  </read_first>

  <action>
Modify `settings_panel.dart` to replace the `Material(elevation: 8, color: Tokens.bgElevated)` panel shell with `GlassContainer`.

Specific changes in `_SettingsPanelState.build()`:

1. Replace the `Material` widget (line 146-149) wrapping `SizedBox` with `GlassContainer`:
   - Remove `Material(elevation: 8, color: Tokens.bgElevated, borderRadius: ..., clipBehavior: ...)`
   - Wrap the `SizedBox(width: 600, height: 480, ...)` in `GlassContainer(borderRadius: BorderRadius.circular(Tokens.radiusLarge), child: ...)`
   - GlassContainer provides its own ClipRRect, so remove `clipBehavior: Clip.antiAlias` from the old Material

2. Replace the title bar `Container` (lines 161-200) background:
   - Change `color: Tokens.bgElevated` to `color: Tokens.bgGlass` for glass consistency
   - The title bar sits inside GlassContainer's ClipRRect, so BackdropFilter works

3. Replace the bottom bar `Container` (lines 275-292) background:
   - Change `color: Tokens.bgElevated` to `color: Tokens.bgGlass` for glass consistency

4. Keep all existing functionality unchanged:
   - `_offset` ValueNotifier for drag (INTX-04)
   - `_commitChanges()` for delayed apply (INTX-01, INTX-02)
   - `_cancel()` with restore logic (INTX-03)
   - `_ok()`, `_apply()` methods
   - `GestureDetector` for drag on title bar
   - `AnimatedSwitcher` for tab transitions

Per STYLE-01 (bgGlass + BackdropFilter), STYLE-02 (radiusLarge), STYLE-03 (borderHighlight) — GlassContainer uses all three tokens automatically.
  </action>

  <verify>
    <automated>flutter analyze lib/ui/dialogs/settings_panel.dart</automated>
  </verify>

  <acceptance_criteria>
    - `settings_panel.dart` compiles without errors
    - Panel outer shell uses `GlassContainer` instead of `Material(elevation: 8)`
    - Title bar background uses `Tokens.bgGlass` instead of `Tokens.bgElevated`
    - Bottom bar background uses `Tokens.bgGlass` instead of `Tokens.bgElevated`
    - Drag functionality preserved (GestureDetector onPanUpdate still calls `_offset.value += d.delta`)
    - OK/Cancel/Apply buttons still call `_ok()`, `_cancel()`, `_apply()`
    - `flutter analyze` passes with no new warnings
  </acceptance_criteria>

  <done>
    Settings panel shell uses GlassContainer with glass background, all interaction logic preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update sidebar nav item with glass hover styling</name>
  <files>lib/ui/dialogs/settings/_settings_nav_item.dart</files>

  <read_first>
    lib/ui/dialogs/settings/_settings_nav_item.dart
    lib/ui/shared/glass_container.dart (GlassButton.iconOnly pattern, lines 267-313)
    lib/ui/theme/tokens.dart
  </read_first>

  <action>
Modify `_settings_nav_item.dart` to match the control bar's `GlassButton.iconOnly` hover/press pattern.

Current state: `SettingsNavItem` uses `InkWell` with `Tokens.bgHover` for selected state, and has a left border accent for selected.

Changes:
1. Keep the existing `InkWell` wrapping (it already provides tap feedback)
2. Ensure hover feedback uses `Tokens.bgHover` (already correct)
3. Verify the icon size uses `Tokens.iconLg` (already correct at line 36)
4. Verify the text styling matches control bar button text:
   - Selected: `color: Tokens.accent`, `fontWeight: Tokens.weightMedium`
   - Unselected: `color: Tokens.textSecondary`, `fontWeight: Tokens.weightRegular`
5. Keep the left border accent (`Border(left: BorderSide(...))`) for selected state — this is a sidebar-specific affordance not present in control bar buttons, and is acceptable

Per STYLE-04 (sidebar nav hover matches control bar): The current implementation already uses the same `Tokens.bgHover` pattern as `GlassButton.iconOnly`. The key visual consistency is the hover color and icon/text token usage. No scale animation (per user preference: no button animations).

Per TRIG-03 (settings button hover feedback): The settings button in the control bar already has hover feedback via GlassButton. This task ensures the sidebar nav items inside the panel match that pattern.
  </action>

  <verify>
    <automated>flutter analyze lib/ui/dialogs/settings/_settings_nav_item.dart</automated>
  </verify>

  <acceptance_criteria>
    - `_settings_nav_item.dart` compiles without errors
    - Hover state uses `Tokens.bgHover` color
    - Selected state uses `Tokens.accent` for icon and text color
    - Icon size is `Tokens.iconLg`
    - Text uses `Tokens.fontOverline` (10px) — unchanged from current
    - Left border accent preserved for selected state
    - No scale animation added (per user preference)
    - `flutter analyze` passes with no new warnings
  </acceptance_criteria>

  <done>
    Sidebar nav items have glass hover feedback matching control bar button pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure UI visual refactor, no security-sensitive code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No external packages installed — all components are project-local |
</threat_model>

<verification>
1. `flutter analyze` passes with no new warnings
2. `SettingsPanel` widget renders without errors
3. `_SectionHeader` is importable from `section_header.dart`
4. GlassContainer visible in panel shell (visual verification)
5. Sidebar nav hover feedback matches control bar pattern
</verification>

<success_criteria>
- Settings panel outer shell uses GlassContainer with BackdropFilter blur
- _SectionHeader extracted to shared file, importable by all tabs
- Sidebar nav items match control bar hover pattern
- All existing interaction logic (drag, apply, cancel) preserved
- flutter analyze clean
</success_criteria>

<output>
Create `.planning/phases/01-settings-panel-glass-redesign/01-01-SUMMARY.md` when done
</output>
