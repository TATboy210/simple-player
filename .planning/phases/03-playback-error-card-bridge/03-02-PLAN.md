---
phase: 03-playback-error-card-bridge
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/ui/player/error_card.dart
  - lib/ui/player/error_card_host.dart
  - lib/ui/theme/tokens.dart
  - test/widget/player/error_card_test.dart
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-03
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CARD-03：折叠态显示摘要/严重级/媒体路径 basename；展开态显示文件:行号、源码行 ±2、完整调用栈、日志路径与重复信息（D-04 段序）。"
    - "D-03/D-04：视觉复用 GlassContainer + Tokens，严重级语义色分层；整卡点击折叠/展开 + chevron；零新视觉体系。"
    - "CARD-01：卡片常驻手动关，无自动消失、无 route/barrier/autofocus；点击卡片任意交互元素后键盘焦点不变（Space 仍归 KeyboardHandler）。"
    - "CARD-02：卡片矩形之外的点击全部穿透到下层控件；D-10 挂载层下卡片不吞 Navigator 内容命中。"
    - "D-07 Phase 2 脱敏边界：可见卡片树内不渲染完整媒体路径字段（仅折叠 basename 与复制诊断包允许完整路径）。"
  artifacts:
    - path: lib/ui/player/error_card.dart
      provides: "完整 ErrorCard：折叠/展开、l10nKey 消息解析、严重级语义色、手动关闭按钮。"
    - path: lib/ui/theme/tokens.dart
      provides: "Tokens.warning（amber）+ fatal 深红语义色 token（D-03）。"
    - path: test/widget/player/error_card_test.dart
      provides: "CARD-01/02/03 widget 测试（expand/hit-test/focus 断言组）。"
  key_links:
    - from: ErrorCard 整卡 GestureDetector
      to: 折叠/展开状态切换
      via: "chevron 图标指示 + D-04 整卡点击"
      pattern: "GestureDetector(onTap"
    - from: error_banner.dart:124-141 l10nKey switch
      to: ErrorCard 消息解析
      via: "删除前完整复制 13 key 枚举 + fallback raw message"
      pattern: "error.l10nKey"
    - from: ErrorCardHost 关闭按钮
      to: ErrorReporterImpl.dismissCurrent()
      via: "手动关闭推进 FIFO，关闭后展示下一项（CAP-04）"
      pattern: "dismissCurrent()"
---

<objective>
**用户故事：作为播放器用户，我希望能从折叠卡片一眼读出摘要、严重级和媒体路径，并展开查看文件:行号、源码行、完整调用栈与日志路径，以便不接调试器也能定位问题——而卡片绝不抢走我的键盘焦点或挡住任何控件。**

在 03-01 纵切上扩展两个垂直切片：其一，CARD-03/D-03/D-04 的完整折叠/展开详情与语义色；其二，CARD-01/CARD-02 的常驻手动关、零焦点抢占与严格 hit-test 边界（含 D-10 挂载层的穿透验证）。

Purpose: 卡片从「能看到」升级为「能定位」且「不妨碍操作」，同时把 MIG-01 删除前必须继承的 l10nKey 解析逻辑先行落位。
Output: 完整 ErrorCard、Tokens 语义色、CARD-01/02/03 测试组。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playback-error-card-bridge/03-CONTEXT.md
@.planning/phases/03-playback-error-card-bridge/03-RESEARCH.md
@.planning/phases/03-playback-error-card-bridge/03-PATTERNS.md
@.planning/phases/03-playback-error-card-bridge/03-VALIDATION.md
@.planning/phases/02-trusted-location-file-evidence/02-CONTEXT.md
@lib/ui/player/error_card.dart
@lib/ui/player/error_card_host.dart
@lib/ui/player/error_banner.dart
@lib/ui/player/keyboard_handler.dart
@lib/ui/shared/glass_container.dart
@lib/ui/theme/tokens.dart
@lib/kernel/diagnostics/error_location.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 折叠/展开详情与严重级语义色（CARD-03/D-03/D-04）</name>
  <files>lib/ui/player/error_card.dart, lib/ui/theme/tokens.dart, test/widget/player/error_card_test.dart</files>
  <read_first>
    - lib/ui/player/error_card.dart（03-01 折叠视图基线，本任务向展开区扩展）
    - lib/ui/player/error_banner.dart:120-141（l10nKey → AppLocalizations 的 13 key 完整 switch——MIG-01 删除前必须整体迁移到这里；:44-64 的动作按钮 switch 按 D-09 明确**不**迁移）
    - lib/ui/theme/tokens.dart:18（Tokens.danger 现值；warning/fatal token 的新增位置与命名惯例）
    - lib/kernel/diagnostics/error_location.dart（ErrorLocation.file/line/primaryFrame/sourceLines 契约，sourceLines 为 lineNumber:text 对）
    - lib/kernel/diagnostics/error_report.dart（rawStackTrace 16384 上界、occurrenceCount、firstOccurredAt/lastOccurredAt、mediaPath basename 安全语义）
    - lib/kernel/diagnostics/error_reporter.dart:133-135（diagnosticLogPath ValueListenable<String?>——展开区日志路径来源）
    - lib/ui/shared/glass_container.dart:73-106（GlassContainer 构造参数、resizing 透传、ClipRRect 同剪裁 paint+hit-test）
    - .planning/phases/02-trusted-location-file-evidence/02-CONTEXT.md（D-01 源码行 ±2、D-04 诊断包段序、D-07 脱敏边界）
    - .planning/phases/03-playback-error-card-bridge/03-RESEARCH.md（Code Examples 折叠/展开字段映射表、语义色 discretion、A2 SingleChildScrollView 论证）
    - .planning/phases/03-playback-error-card-bridge/03-PATTERNS.md（Card content mapping、l10n 迁移模式）
  </read_first>
  <behavior>
    - RED：折叠态卡片显示 message（对携带已知 l10nKey 的 PlayerError 显示解析后的本地化文案，对未知 key 显示 raw message fallback）、严重级色点、mediaPath（basename 形态）。
    - RED：整卡点击展开后出现文件:行号（location.primaryFrame）、源码行（有 lineNumber:text 时逐行展示）、完整 rawStackTrace（逐字符含换行，SelectableText）、日志路径（diagnosticLogPath.value，null 时显示 errorCardLogUnavailable）、occurrenceCount/首次末次时间；chevron 图标状态翻转；再点收起。
    - RED：location 为 null 时展开区显示 errorCardLocationUnavailable 降级文本，不抛错不缺段。
    - RED：渲染一份携带完整媒体路径的报告，展开与折叠的可见 widget 树中均找不到该完整路径文本（basename 出现，完整值只允许后续进入复制包）。
    - GREEN：实现后全部转绿，且 `flutter analyze` 0 error。
  </behavior>
  <action>先在 test/widget/player/error_card_test.dart 写失败测试（harness 复用 error_banner_test.dart 的 MaterialApp + localizationsDelegates + buildSubject 形态，数据用 ErrorReporterImpl.forTesting 产出的真实 ErrorReport，不构造 MediaKitEngine）。实现侧：第一，在 tokens.dart 的 `Tokens.danger` 旁新增 `Tokens.warning`（amber 系）与 fatal 用深红（可新增 `Tokens.dangerFatal` 或复用 danger 加深——discretion，取其一并在行尾注释说明），全部走 static const 惯例；`_severityColor` 用 Dart 3 switch 表达式映射三值。第二，从 error_banner.dart:124-141 把 l10nKey switch **完整复制**进 error_card.dart（这是 MIG-01 删除的前置条件；按 D-09 不复制动作按钮块）。第三，展开区按 D-04 Phase 2 诊断包段序排布各段（定位→源码→调用栈→日志路径→重复信息），长文本段用 SingleChildScrollView + SelectableText（A2：栈硬上界 16384 字符，无需虚拟化）；GlassContainer 传 severity 对应色 border（D-03），若 windowService 的 resizing ValueListenable 从挂载点可达则透传（不可达则在 SUMMARY 记录省略理由）。整卡 GestureDetector 切换折叠态（StatefulWidget 内部状态即可，无新状态库）；交互元素不使用 GlassButton（FocusableActionDetector 会抢焦点，CARD-01 归 03-02 Task 2 验证，但本任务就避免引入）。展开态内容一律纯文本渲染。文案全部走 03-01 的 l10n key——**所有 l10n key 已于 03-01 Task 1 预置（见 03-VALIDATION.md Wave 0 第 4 项），执行时直接引用 `AppLocalizations` 既有 key，禁止新增重复 key。**</action>
  <verify>
    <automated>flutter test test/widget/player/error_card_test.dart --plain-name expand</automated>
    <fails_when>命令非零退出，或折叠三要素、展开五段（定位/源码/调用栈/日志路径/重复）、chevron 切换、location 为 null 降级、完整路径不外漏任一断言未通过。</fails_when>
  </verify>
  <acceptance_criteria>
    - l10nKey switch 的 13 个 key 枚举与 error_banner.dart 逐项一致且有 unknown fallback；动作按钮逻辑未迁移（D-09）。
    - 展开区五段齐备且顺序遵循 D-04 Phase 2 段序；rawStackTrace 逐字符原样展示（terminal 语义不二次处理）。
    - 可见 widget 树测试断言完整媒体路径文本 findsNothing，basename 照常显示。
    - 严重级三值均有 token 来源（无硬编码 Color 字面量）；新 token 有双语注释。
    - focused 命令退出 0；flutter analyze 对本任务文件 0 error。
  </acceptance_criteria>
  <done>折叠/展开双态完整可用：摘要一眼可读，展开即得文件:行号、源码行、完整调用栈与日志路径，视觉零新体系，为 03-04 的等效覆盖断言备齐消息解析逻辑。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 常驻手动关、零焦点抢占与严格 hit-test（CARD-01/CARD-02）</name>
  <files>lib/ui/player/error_card.dart, lib/ui/player/error_card_host.dart, test/widget/player/error_card_test.dart</files>
  <read_first>
    - lib/ui/player/error_card.dart 与 error_card_host.dart（03-01/02-Task1 建立的宿主与卡片）
    - lib/ui/player/error_banner.dart（旧横幅无关闭按钮的对照；其挂载参数即旧命中形态）
    - lib/ui/player/keyboard_handler.dart:74（根 Focus(autofocus: true) 单焦点源——焦点断言的基准）
    - lib/kernel/diagnostics/error_reporter.dart:231-245（dismissCurrent 推进 FIFO 的语义与空队列行为）
    - .planning/phases/03-playback-error-card-bridge/03-RESEARCH.md（Pattern 3 焦点方案、Pattern 2 hit-test 框架行为、Pitfall 3/4、CARD-01/02 test map 行）
    - .planning/phases/03-playback-error-card-bridge/03-PATTERNS.md（挂载反陷阱清单）
    - .planning/phases/03-playback-error-card-bridge/03-VALIDATION.md（Manual-Only Verifications：Windows 实机部分本任务只做 widget 侧）
  </read_first>
  <behavior>
    - RED：卡片可见后连续 tester.pump 多帧/定时器触发，卡片不消失（无自动隐藏计时器）；点击关闭按钮后卡片消失且 presentation 推进到队首下一项（多报告时），单报告时归零。
    - RED：tap 卡片折叠区、展开区、关闭按钮后，`FocusManager.instance.primaryFocus` 与点击前一致（KeyboardHandler 根焦点未被夺走）。
    - RED：卡片子树内不存在可请求焦点的节点（ExcludeFocus 包裹生效）；卡片内无 GlassButton/FocusableActionDetector 实例。
    - RED：Stack 内卡片矩形下方放置一个带 onTap 的控件，tap 该控件位置其回调触发（穿透）；同一测试断言 tap 卡片矩形内回调不触发到下层（边界严格双向）。
    - RED：push 不透明 route 后 tap route 内容命中 route 控件、卡片仍可见（D-10 挂载层不吞 Navigator 命中）。
  </behavior>
  <action>先写失败测试再实现。第一，折叠区尾部加关闭按钮：GestureDetector（或 InkWell 带 canRequestFocus: false），图标 + errorCardClose 语义（l10n key 已于 03-01 预置，直接引用 `AppLocalizations` 既有 key，禁止新增重复 key）；回调走宿主传入的 onClose → `ErrorReporterImpl.I.dismissCurrent()`（只在手动关闭调用，03-03 的徽标轮览不得复用此路径）；确认 dismissCurrent 的现有 containment 已保证关闭异常不外溢。第二，宿主 build 子树外层包 `ExcludeFocus`；全卡交互元素核查一遍均为 GestureDetector/canRequestFocus:false 形态，移除任何 FocusableActionDetector 依赖。第三，确认宿主不存在任何自动隐藏 Timer（对照 OsdOverlay 的 auto-hide——卡片有意不用）；无 showModalBottomSheet/showDialog/barrier/autofocus。第四，hit-test 测试用真实 Stack 层级（宿主挂载形态复刻 03-01 的 builder Stack + Positioned.fill navigator），断言双向命中边界；不加任何新的命中拦截包装。若发现 03-01 实现里存在全尺寸可命中包装（违反 CARD-02），此任务负责修正而非绕过。Windows 实机 hit-test 与全屏冒烟属 VER-04/Manual-Only，不在本任务自动化范围，但在 doc comment 与 SUMMARY 注明待办。</action>
  <verify>
    <automated>flutter test test/widget/player/error_card_test.dart --plain-name hit-test</automated>
    <fails_when>命令非零退出，或穿透/边界双向命中、常驻不消失、手动关推进队列、焦点不变、无 route/barrier 任一断言未通过。</fails_when>
  </verify>
  <acceptance_criteria>
    - 卡片无自动消失路径（源码无隐藏 Timer/自动 dismiss）；唯一消失途径为手动关闭与 reporter 队列自然推进。
    - 关闭按钮 → dismissCurrent 接线唯一；多报告场景关闭后下一项可见（CAP-04 语义在 UI 落地）。
    - ExcludeFocus 包裹卡片子树；点击卡片前后 primaryFocus 不变；卡片子树内无 focusable 节点。
    - hit-test 双向边界测试通过：卡外穿透、卡内不穿透；无 Positioned.fill/全尺寸容器包宿主。
    - flutter analyze 0 error；focused 命令退出 0。
  </acceptance_criteria>
  <done>CARD-01/CARD-02 的全部可自动化断言落地：卡片常驻、手动关、零焦点抢占、命中严格限卡片边界；Windows 实机冒烟项已显式登记为 Manual-Only 待办。</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | Symbols produced or modified | Purpose |
|---|---|---|
| `lib/ui/player/error_card.dart` | 完整 `ErrorCard`（折叠/展开、`_resolveMessage` l10nKey switch、`_severityColor`、关闭按钮） | CARD-03/CARD-01 的呈现与交互本体。 |
| `lib/ui/theme/tokens.dart` | `Tokens.warning`、fatal 深红 token | D-03 语义色分层，零硬编码。 |
| `lib/ui/player/error_card_host.dart` | `onClose` → `dismissCurrent` 接线、ExcludeFocus 包裹 | 手动关闭与焦点隔离。 |
| `test/widget/player/error_card_test.dart` | expand / hit-test / focus 断言组 | CARD-01/02/03 自动化验证 + MIG-01 前的 l10n 迁移基线。 |

## Multi-Source Coverage Audit

| Source | Item | Disposition |
|---|---|---|
| GOAL | 可展开、不阻碍控制的统一卡片 | COVERED：Task 1 详情 + Task 2 命中/焦点边界。 |
| REQ | CARD-01 | COVERED：Task 2 常驻/手动关/无焦点抢占。 |
| REQ | CARD-02 | COVERED：Task 2 双向 hit-test（Windows 实机部分为 Manual-Only，登记 VER-04）。 |
| REQ | CARD-03 | COVERED：Task 1 折叠/展开五段详情。 |
| CONTEXT | D-03 | COVERED：Task 1 Tokens 语义色。 |
| CONTEXT | D-04 | COVERED：Task 1 整卡点击/chevron/段序。 |
| CONTEXT | D-07 Phase 2 脱敏边界 | COVERED：Task 1 完整路径不进可见树（测试锁死）。 |
| CONTEXT | D-09 | COVERED：Task 1 不迁移动作按钮 switch（03-04 等效断言遵循同一边界）。 |
| CONTEXT | D-10 | COVERED：Task 2 route 之上命中验证。 |
| CONTEXT | D-01/D-02/D-06/D-11 | COVERED BY 03-03：徽标轮览、warning 路由、复制反馈。 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| 展开区 → 源码行/调用栈 | sourceLines 来自受信源码根读取（Phase 2 校验），但 UI 仍只做纯文本渲染。 |
| 卡片命中 ↔ 播放控制 | 挂载层高于 Navigator，任何过大的命中矩形都会吞掉全应用交互。 |
| 卡片交互 ↔ 键盘焦点体系 | 卡片内可聚焦节点会破坏 KeyboardHandler 单焦点源假设。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-05 | Information Disclosure | 展开区渲染 | high | mitigate | 可见树不渲染完整媒体路径字段；源码行/栈为 Phase 2 已校验内容且纯文本渲染（widget 测试锁死）。 |
| T-03-06 | Denial of Service | 命中边界 | high | mitigate | Positioned(left,top) 内在尺寸 + 双向 hit-test 测试；Windows 实机冒烟在 VER-04 复核。 |
| T-03-07 | Denial of Service | 焦点抢占 | medium | mitigate | ExcludeFocus + 无 focusable 子节点 + primaryFocus 不变断言。 |
| T-03-SC | Tampering | 依赖供应链 | low | accept | 零新增包。 |
</threat_model>

<verification>
  <automated>flutter test test/widget/player/error_card_test.dart test/widget/player/error_card_host_test.dart &amp;&amp; flutter analyze</automated>
  <fails_when>测试或 analyze 非零退出，或 expand/hit-test/focus 断言组任一回归，或出现新 analyzer error。</fails_when>
</verification>

<success_criteria>
- 折叠/展开双态信息完整（CARD-03 五段齐备），D-03/D-04 交互与视觉决策全部落地。
- CARD-01/CARD-02 可自动化断言全绿；Windows 实机冒烟项显式登记 Manual-Only。
- l10nKey 解析逻辑已就位，03-04 删除 ErrorBanner 后无解析能力缺口。
- flutter analyze 0 error；headless 预存失败按基线鉴别。
</success_criteria>

<output>
Create `.planning/phases/03-playback-error-card-bridge/03-02-SUMMARY.md` when done.
</output>
