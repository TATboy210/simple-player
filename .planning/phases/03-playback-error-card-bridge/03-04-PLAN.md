---
phase: 03-playback-error-card-bridge
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - lib/ui/player/player_video_controls.dart
  - lib/kernel/models/player_error.dart
  - test/widget/player/error_banner_equivalence_test.dart
files_deleted:
  - lib/ui/player/error_banner.dart
  - test/widget/player/error_banner_test.dart
autonomous: false
requirements:
  - MIG-01
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "MIG-01：同一引擎错误（FakeEngine 经真实 PlayerErrorReportBridge → ErrorReporter）在新卡片的可见反馈与旧 ErrorBanner 等效（消息 + 严重级可见性），该等效在删除前对双路径同时验证。"
    - "D-07：error_banner.dart 与其挂载、其测试在同一 phase 内删除，仓库无任何残留引用；不留双路径。"
    - "D-09：等效断言只含消息 + 严重级可见性，不含 reopen/retry 动作按钮行为。"
    - "收尾后全项目质量门全绿：flutter analyze 0 error、flutter test 全绿（headless 预存失败按基线鉴别）、kernel_logger_gate 通过。"
  artifacts:
    - path: test/widget/player/error_banner_equivalence_test.dart
      provides: "MIG-01 等效覆盖测试：删前双路径断言，删后保留为卡片路径集成证据。"
    - path: (deleted) lib/ui/player/error_banner.dart
      provides: "删除目标（142 行旧横幅）。"
    - path: lib/ui/player/player_video_controls.dart
      provides: "移除 ErrorBanner 挂载子树与 import。"
  key_links:
    - from: FakeEngine.lastError
      to: PlayerErrorReportBridge → ErrorReporterImpl
      via: "真实桥接路径（player_services.dart:148 同构）"
      pattern: "PlayerErrorReportBridge"
    - from: 等效断言集（消息+严重级）
      to: ErrorCard 可见呈现
      via: "l10nKey 解析 + severity 色点，与旧横幅同源"
      pattern: "error_banner_equivalence_test.dart"
    - from: 删除收尾
      to: grep 门 + 全量质量门
      via: "仓库零残留引用 + analyze/test 绿线"
      pattern: "grep -rq ErrorBanner"
---

<objective>
**用户故事：作为维护者，我希望旧 ErrorBanner 在新卡片被证明对同一引擎错误给出等效可见反馈之后被干净移除，以便错误展示只有一条路径、不再有第二套 UI 需要维护。**

MIG-01 收官：先写双路径等效覆盖测试（D-07 判定 + D-09 断言边界），经人工确认后执行删除与全量质量门。

Purpose: 消除第二条错误展示路径——里程碑「不再形成第二条展示路径」目标的最后一步；删除不可逆，故置于等效证明与人工确认之后。
Output: 等效测试、ErrorBanner 全量删除、残留引用清零、质量门收尾。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playback-error-card-bridge/03-CONTEXT.md
@.planning/phases/03-playback-error-card-bridge/03-RESEARCH.md
@.planning/phases/03-playback-error-card-bridge/03-PATTERNS.md
@.planning/phases/03-playback-error-card-bridge/03-VALIDATION.md
@lib/ui/player/error_banner.dart
@lib/ui/player/player_video_controls.dart
@lib/kernel/diagnostics/player_error_report_bridge.dart
@lib/kernel/models/player_error.dart
@test/diagnostics/player_error_report_bridge_test.dart
@test/widget/player/error_banner_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MIG-01 等效覆盖测试——删前双路径消息+严重级断言</name>
  <files>test/widget/player/error_banner_equivalence_test.dart</files>
  <read_first>
    - test/diagnostics/player_error_report_bridge_test.dart:12-191（_BridgeFixture 模板：FakeEngine + 真实 bridge + ErrorReporterImpl.forTesting(FakeClock/eventIdGenerator/currentMediaPath)、依赖顺序 dispose、flushPresentation 后断言 presentation）
    - test/widget/player/error_banner_test.dart:20-57（旧路径 harness：MaterialApp + delegates + buildSubject；l10nKey 断言风格 `find.text('An unexpected error occurred')`）
    - lib/ui/player/error_banner.dart:31-141（旧横幅数据源：engine.state + engine.lastError 直连、MediaState.error 门控、_resolveMessage）
    - lib/kernel/diagnostics/player_error_report_bridge.dart（74 行桥：订阅 lastError → reportPlayerError）
    - lib/kernel/models/player_error.dart（PlayerError 子type → l10nKey → isFatal 映射——两路径共用的语义源）
    - test/helpers/fake_engine.dart（simulateError 注入缝；严禁构造 MediaKitEngine/media_kit Player）
    - .planning/phases/03-playback-error-card-bridge/03-CONTEXT.md（D-07 等效判定、D-09 断言边界）
    - .planning/phases/03-playback-error-card-bridge/03-RESEARCH.md（Runtime State Inventory 删除爆炸半径、MIG-01 test map 行）
    - .planning/phases/03-playback-error-card-bridge/03-PATTERNS.md（equivalence test 双 analog 段）
  </read_first>
  <behavior>
    - RED→GREEN（对双路径同时成立）：同一组 PlayerError 子type（至少覆盖 fileNotFound、decode/codec、playback、unknown 四类代表性 case + isFatal 案例）经 FakeEngine.simulateError 注入后——旧路径 harness（ErrorBanner 直连 engine）与新路径 harness（bridge→reporter→flushPresentation→ErrorCardHost）对同一错误各自显示**相同的 l10n 解析消息**与**可辨认的严重级**（fatal/error 语义一致）。
    - RED：桥接路径上同一错误重复发生时 occurrenceCount/去重语义在卡片可见（旧横幅无此能力——差异记录在测试注释，不算等效破坏项）。
    - 断言集中不含任何 reopen/select-other-file/retry 按钮查找（D-09）；不含 fullMediaPath 可见性断言。
  </behavior>
  <action>创建 test/widget/player/error_banner_equivalence_test.dart：第一，公共 fixture 复用 _BridgeFixture 形态（FakeEngine + PlayerErrorReportBridge + ErrorReporterImpl.forTesting，KernelLoggerImpl resetForTesting/init 于 setUpAll，依赖顺序 dispose）。第二，旧路径 harness 按 error_banner_test.dart buildSubject 形态直连 FakeEngine；新路径 harness 挂 ErrorCardHost 于 MaterialApp builder Stack（复刻 03-01 挂载形态）并 flushPresentation。第三，共享一个「可见反馈断言 helper」（参数：期望 l10n 文案 + 严重级），对每个代表性 PlayerError case 在两条路径各跑一遍——这就是 D-07 的等效判定集。第四，测试文件头 doc comment 记录：等效 = 消息 + 严重级可见性（D-09）；动作按钮按用户决策不迁移；occurrenceCount 为新路径增量能力。第五，本任务完成时删除尚未发生，测试必须对**双路径同时**退出 0——这是 Task 3 删除的前置门。若新路径对某 case 表现与旧路径文案不一致，修卡片实现（l10n switch 已于 03-02 迁移，理论上应一致），绝不放宽断言。</action>
  <verify>
    <automated>flutter test test/widget/player/error_banner_equivalence_test.dart</automated>
    <fails_when>命令非零退出，或任一代表性 PlayerError case 在旧横幅路径与新卡片路径的消息/严重级可见性断言不同时成立。</fails_when>
  </verify>
  <acceptance_criteria>
    - 等效断言 helper 对双路径复用；四类代表性 case + fatal case 双路径全绿。
    - 断言集零动作按钮查找（D-09）；fixture 未构造 MediaKitEngine/media_kit Player。
    - 测试在删除发生前运行且通过（旧路径文件尚在）。
    - focused 命令退出 0。
  </acceptance_criteria>
  <done>新旧两条路径对同一组引擎错误的可见反馈被同一组断言证明等效；删除的安全前提已经成立并留档。</done>
  <reversibility rating="reversible">本任务只新增测试，不删任何文件；删除决策在 Task 2 人工确认后才执行。</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: 确认执行 ErrorBanner 删除（D-07 不可逆操作门）</name>
  <files></files>
  <action>向用户呈现：Task 1 等效测试对双路径全绿的输出摘要、删除爆炸半径（error_banner.dart 142 行、player_video_controls.dart 挂载子树 + import、error_banner_test.dart、player_error.dart:48 doc comment 引用）、D-09 已确认的动作按钮不再迁移说明。用户确认后才进入 Task 3 删除；若用户否决，保留双路径并在 SUMMARY 记录阻塞原因（D-07 已拍板同 phase 删除，此门是删除执行前的最后确认而非重新讨论）。</action>
  <decision>等效覆盖已对双路径全绿，是否现在执行 ErrorBanner 的替换+删除（D-07 已拍板同 phase 内完成，此处为不可逆操作执行前确认）？</decision>
  <options>
    <option id="A">批准删除——进入 Task 3：删除两文件、清除挂载与 doc comment 引用、跑全量质量门。</option>
    <option id="B">暂缓删除——保留双路径，在 SUMMARY 记录阻塞原因并停在等效证明完成状态。</option>
  </options>
  <resume-signal>用户回复选项 A（批准）或 B（暂缓）后继续执行对应分支。</resume-signal>
  <verify>
    <automated>flutter test test/widget/player/error_banner_equivalence_test.dart test/widget/player/error_card_test.dart test/widget/player/error_card_host_test.dart</automated>
    <fails_when>命令非零退出——等效测试或卡片测试任一不绿时不得请求用户确认删除。</fails_when>
  </verify>
  <done>用户在等效证明成立的前提下明确批准删除（或明确否决并留档）。</done>
  <reversibility rating="one-way">删除 error_banner.dart 及其挂载在工作树不可逆（git 历史可恢复）；D-07 已锁定方向，此 checkpoint 是 REVERSIBILITY_GATES 要求的执行前人工确认。</reversibility>
</task>

<task type="auto">
  <name>Task 3: 删除 ErrorBanner 双路径并收尾质量门（MIG-01/D-07）</name>
  <files>lib/ui/player/error_banner.dart, test/widget/player/error_banner_test.dart, lib/ui/player/player_video_controls.dart, lib/kernel/models/player_error.dart, test/widget/player/error_banner_equivalence_test.dart</files>
  <read_first>
    - lib/ui/player/player_video_controls.dart:19 与 :879-897（import 与 Positioned 挂载子树——精确删除范围；确认 widget.actions.onOpenFile 的其余引用不受影响）
    - lib/kernel/models/player_error.dart:48（l10nKey 的 doc comment 指向 ErrorBanner——改为 ErrorCard）
    - test/widget/player/error_banner_equivalence_test.dart（Task 1 产物——本任务移除旧路径断言，保留新路径为集成证据）
    - .planning/phases/03-playback-error-card-bridge/03-RESEARCH.md（Runtime State Inventory：无存储/OS/构建产物牵连，纯 Dart 源删除）
    - .planning/phases/03-playback-error-card-bridge/03-VALIDATION.md（03-04-01 验证行：等效测试 → grep 门）
    - tool/audit/kernel_logger_gate.sh（收尾质量门之一）
  </read_first>
  <behavior>
    - 前置：Task 1+checkpoint 已绿/已批准。
    - 删除后：`grep -r ErrorBanner lib/ test/` 零匹配（含 doc comment）；`git ls-files lib/ui/player/error_banner.dart test/widget/player/error_banner_test.dart` 为空。grep 门**覆盖全部 test/，含有意保留的 error_banner_equivalence_test.dart**——该文件头注释与 fixture 命名一律用「旧横幅/legacy banner」措辞，不出现 ErrorBanner 字面量（文件名本身为小写 snake_case，不在匹配范围）。
    - 等效测试重定向后仍全绿（仅卡片路径断言）。
    - 全量门：flutter analyze 0 error；flutter test 全绿（headless 预存失败按既有基线鉴别，非本计划回归）；bash tool/audit/kernel_logger_gate.sh 通过。
  </behavior>
  <action>第一步，删除 lib/ui/player/error_banner.dart 与 test/widget/player/error_banner_test.dart 两个文件（git rm）。第二步，player_video_controls.dart 移除 :882-897 的 Positioned 挂载子树与 :19 的 import，删除其中的中文注释行；确认 widget.actions/engine 其余引用无 unused 警告。第三步，player_error.dart:48 的 l10nKey doc comment 把消费方从旧横幅改为新卡片（保留 D7 键格式说明）。第四步，等效测试移除旧 harness 与旧路径断言分支，文件保留新路径断言并在头注释记录「等效证明发生于删除前（见 03-04 Task 1 提交），此后为本路径集成测试」；头注释与 fixture 命名描述旧路径时一律用「旧横幅/legacy banner」措辞而非 ErrorBanner 类名字面量（grep 门扫描整个 test/ 含本文件，字面量残留会使 Task 3 的门自失败）；不改任何断言语义。第五步，收尾质量门全跑；预存失败按项目 memory 的 stash 鉴别法归类，不属于本次改动的不修。commit 按 conventional commits 分两笔：等效测试一笔（Task 1 已含）、删除一笔。<!-- planner-discipline-allow: ErrorBanner --></action>
  <verify>
    <automated>if grep -rq ErrorBanner lib/ test/; then echo 'stale ErrorBanner refs remain' >&2; exit 1; fi &amp;&amp; flutter test test/widget/player/ &amp;&amp; flutter analyze &amp;&amp; bash tool/audit/kernel_logger_gate.sh</automated>
    <fails_when>仓库仍残留任何指向旧横幅的引用（grep 有输出）、widget/player 目录测试非零退出、analyze 出现非 0 error、kernel_logger_gate 非零退出任一情形出现。</fails_when>
  </verify>
  <acceptance_criteria>
    - 两文件已删除且 git 状态确认；player_video_controls.dart 无挂载残留、无 unused import。
    - player_error.dart doc comment 指向新卡片；等效测试仅存卡片路径且语义未放宽。
    - grep 门零匹配——扫全部 lib/ test/ 含保留的等效测试文件（其注释/命名遵循「旧横幅/legacy banner」措辞约束）；flutter analyze 0 error；flutter test 全绿（预存失败按基线鉴别并记录）；kernel_logger_gate 通过。
    - 删除为独立 commit，diff 审阅清晰。
  </acceptance_criteria>
  <done>错误展示收敛为单一卡片路径：旧横幅及其测试、挂载、文档引用全部清除，全量质量门绿线收尾，MIG-01 完成。</done>
  <reversibility rating="one-way">工作树删除不可逆（git 历史为恢复路径）；已由前置 checkpoint 人工批准，research Runtime State Inventory 证明无存储/构建产物牵连。</reversibility>
</task>

</tasks>

## Artifacts this phase produces

| Path | Symbols produced or modified | Purpose |
|---|---|---|
| `test/widget/player/error_banner_equivalence_test.dart` | MIG-01 等效断言集（删前双路径 → 删后卡片路径） | D-07 判定的可执行证据链。 |
| `lib/ui/player/error_banner.dart`（删除） | — | 消除第二条展示路径。 |
| `test/widget/player/error_banner_test.dart`（删除） | — | 旧断言基线已由等效测试继承（D-09 边界）。 |
| `lib/ui/player/player_video_controls.dart` | 移除挂载子树 + import | 旧路径接线清除。 |
| `lib/kernel/models/player_error.dart` | l10nKey doc comment 更新 | 文档指向新消费方。 |

## Multi-Source Coverage Audit

| Source | Item | Disposition |
|---|---|---|
| GOAL | 旧错误横幅不再形成第二条展示路径 | COVERED：Task 1 等效证明 → Task 2 确认门 → Task 3 删除收尾。 |
| REQ | MIG-01 | COVERED：三任务链（等效覆盖 → 人工门 → 替换移除）。 |
| CONTEXT | D-07 | COVERED：同 phase 替换+删除、等效判定先行、irreversible 门控。 |
| CONTEXT | D-09 | COVERED：断言集仅消息+严重级，动作按钮不迁移。 |
| CONTEXT | D-01/D-02/D-03/D-04/D-05/D-06/D-08/D-10/D-11/D-12 | COVERED BY 03-01/02/03。 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| 删除操作 ↔ 仓库完整性 | 删除牵连 import/doc comment/测试三处引用，漏一处即编译或 grep 门破。 |
| 等效判定 ↔ 断言放宽诱惑 | 新路径与旧路径文案不一致时放宽断言会伪造等效。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-12 | Tampering | 等效判定 | high | mitigate | 断言 helper 双路径共用；文案不一致时修实现不放宽断言；删除前双路径全绿为硬门。 |
| T-03-13 | Denial of Service | 删除残留引用 | high | mitigate | grep 门 + widget/player 全目录测试 + analyze 三重收尾；Runtime State Inventory 证明无运行时状态牵连。 |
| T-03-14 | Repudiation | 迁移决策留痕 | low | mitigate | 等效测试头注释记录删除前证明与 D-09 边界；独立 commit 保留 diff 审阅。 |
| T-03-SC | Tampering | 依赖供应链 | low | accept | 零新增包；删除不引入任何依赖。 |
</threat_model>

<verification>
  <automated>if grep -rq ErrorBanner lib/ test/; then exit 1; fi &amp;&amp; flutter test &amp;&amp; flutter analyze &amp;&amp; bash tool/audit/kernel_logger_gate.sh</automated>
  <fails_when>残留引用存在、全量测试非零退出（headless 预存失败需按既有基线鉴别归类）、analyze 非 0 error、kernel_logger_gate 非零退出任一情形出现。</fails_when>
</verification>

<success_criteria>
- 等效覆盖在删除前对双路径成立并留档（D-07/D-09）。
- ErrorBanner、其测试、其挂载、其文档引用全部清除，仓库零残留。
- flutter analyze 0 error；flutter test 全绿（预存按基线鉴别）；kernel_logger_gate 通过。
- Windows 实机冒烟（卡片显示期间控件命中/全屏显示）已登记 VER-04 待办。
</success_criteria>

<output>
Create `.planning/phases/03-playback-error-card-bridge/03-04-SUMMARY.md` when done.
</output>
