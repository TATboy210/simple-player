---
phase: 03-playback-error-card-bridge
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - lib/ui/player/keyboard_handler.dart
  - lib/l10n/app_en.arb
  - lib/l10n/app_zh.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_en.dart
  - lib/l10n/app_localizations_zh.dart
  - test/widget/player/keyboard_handler_debug_injection_test.dart
autonomous: true
gap_closure: true
gap_ids: [G-03-1]
requirements: [G-03-1]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "debug 实机会话中按下 Ctrl+Shift+I，错误卡片立即弹出并显示「调试注入的合成错误 #N」，且数据同步进捕获徽标与 error.log（走真实链路，非旁路显示）"
    - "10 秒内连续快速按下，每次都产生新的卡片条目（计数后缀使语义身份差异化，绕过 reporter 去重合并）"
    - "每次注入伴随 OSD「已注入测试错误」pill 反馈，确认触发成功"
    - "F1 帮助（debug 构建）列出 Ctrl+Shift+I；release/MSIX 构建不含该入口（kDebugMode 编译期门控）"
  artifacts:
    - "lib/ui/player/keyboard_handler.dart — kDebugMode 注入分支 + _injectTestError + shortcutDefinitions 条目"
    - "lib/l10n/app_en.arb 与 app_zh.arb 新增 shortcutDebugInjectError + 重新生成的 app_localizations*.dart"
    - "test/widget/player/keyboard_handler_debug_injection_test.dart — 全链路 widget 测试"
  key_links:
    - "KeyboardHandler._injectTestError → ErrorReporterImpl.I.reportPlatformSafely（现有公开 intake；main.dart 初始化先于 runApp，生产路径必可达）"
    - "合成消息「#<计数>」后缀 → reporter 8 字段语义身份（error_reporter.dart:404-417）随 message 差异化 → 绕过 _dedupeWindow（error_reporter.dart:50）"
    - "_injectTestError → OsdService.I.show（OSD pill 反馈，error_card.dart:155 先例）"
    - "shortcutDefinitions 的 kDebugMode collection-if → shortcuts_help_dialog.dart:27 的 F1 单一数据源渲染"
---

<objective>
补上 G-03-1：开发用错误注入入口（调试触发）——kDebugMode 门控快捷键 Ctrl+Shift+I 构造合成错误，经 `ErrorReporterImpl.I` 现有公开 intake 走完整真实链路（FIFO → presentation → ErrorCardHost → ErrorCard + 捕获徽标 + error.log），使日常 debug 实机验证可按需让卡片弹出。

Purpose: UAT 实测发现开发者没有专门触发手段（「没有个专门启动调试的按钮，不反馈错误我也不知道」）；注入缝已生产就绪，只缺暴露入口。设计/范围缺失，非代码缺陷——故本计划零 kernel 改动、零 media_kit 接触。
Output: keyboard_handler 注入分支 + OSD 反馈 + F1 帮助条目（l10n）+ 全链路 widget 测试。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playback-error-card-bridge/03-UAT.md
@.planning/debug/g03-1-no-error-injection-entry.md

# 注入接口事实源（只读参考，不修改）
@lib/kernel/diagnostics/error_reporter.dart
# 触发先例与快捷键单一数据源（本计划唯一 UI 改动点）
@lib/ui/player/keyboard_handler.dart
# 测试 harness 惯例（setUp 单例重建 + buildMountHarness + App 组合根）
@test/widget/player/error_card_host_test.dart

# 已建卡片链路结论（14/15 must-haves，37/37 聚焦测试）——勿动 ErrorCardHost/ErrorCard 本体
@.planning/phases/03-playback-error-card-bridge/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ctrl+Shift+I 注入快捷键——合成错误走真实链路（RED→GREEN）</name>
  <files>test/widget/player/keyboard_handler_debug_injection_test.dart, lib/ui/player/keyboard_handler.dart</files>
  <behavior>
    - Test「组合键触发卡片」：harness 挂载 ErrorCardHost（builder: buildErrorCardMount）+ home 为 KeyboardHandler(child: SizedBox.expand())，依序 sendKeyDownEvent controlLeft、shiftLeft、keyI，pump 后 expect find.byType(ErrorCard) 恰一个、find.textContaining('调试注入的合成错误') 恰一个——证明按键 → intake → FIFO → presentation → Host → Card 全链路（D-12 post-frame flush 使报告立即呈现）
    - Test「双按出新卡不合并」：同一测试内快速按两次（间隔远小于 10s），expect ErrorReporterImpl.I.queuedReports 长度为 2、两条 message 互不相同（计数递增）、每条 occurrenceCount == 1——证明绕过 _dedupeWindow（error_reporter.dart:50 的 10s 合并窗）
    - Test「报告字段符合真实链路语义」：queuedReports[0].source == ErrorSource.platformDispatcher、severity == ErrorSeverity.error（D-02：error 级上常驻卡片，不走 warning OSD 分流）
    - Test「单键 I 不误触发」：只 sendKeyDownEvent keyI（无修饰键），expect queuedReports 为空
    - kDebugMode=false 分支无法在 debug 测试运行中验证（编译期 const）——release 排除由 `if (kDebugMode && ...)` 编译期折叠保证，属代码审查级证据，不写测试
  </behavior>
  <action>
    - RED：新建 test/widget/player/keyboard_handler_debug_injection_test.dart。复用 error_card_host_test.dart 的既有惯例：setUpAll 里 KernelLoggerImpl.resetForTesting() + KernelLoggerImpl.init()；每个测试 setUp 里 await ErrorReporterImpl.resetForTesting() + ErrorCaptureSnapshot.I.resetForTesting() + ErrorReporterImpl.init(effects: [ErrorCaptureSnapshot.I.record])（与生产 main.dart 的 D-11 接线一致）。harness 用 error_card_host_test.dart 的 buildMountHarness 语义副本：MaterialApp(locale: Locale('zh'), localizationsDelegates: AppLocalizations.localizationsDelegates, supportedLocales: AppLocalizations.supportedLocales, home: KeyboardHandler(...), builder: buildErrorCardMount)。组合键用 tester.sendKeyDownEvent 依序模拟 controlLeft/shiftLeft/keyI——HardwareKeyboard.instance.isControlPressed 与 isShiftPressed 由按键流驱动，与 keyboard_handler.dart:171-178 的判定方式一致。
    - GREEN：在 keyboard_handler.dart 的 Ctrl+Shift+D 调试块（171-178 行）之后仿其结构新增分支：kDebugMode 且 key == LogicalKeyboardKey.keyI 且 Ctrl、Shift 均按下 → 调私有方法 _injectTestError() 并返回 handled。KeyboardHandler 类上声明 `static int _debugInjectedErrorCount = 0;`（dev-only 计数器，文档注明仅 debug 会话存活）。_injectTestError 先自增计数，再以 StateError('调试注入的合成错误 #<计数>') 与 StackTrace.current 调用 ErrorReporterImpl.I.reportPlatformSafely——注意 .I 访问的前提是 reporter 已初始化（生产中 main.dart 组合根先于 runApp 完成，与 error_card_host.dart 直接用 .I 的先例一致，不加额外守卫）。Task 1 暂不调 OsdService（Task 2 补反馈）。
    - 约束：不新建 kernel 文件、不修改 error_reporter.dart（零 kernel 改动）；禁止在生产路径使用 forTesting 构造器与 resetForTesting（@visibleForTesting 专用）；合成 StateError 仅作 reporter 的数据载荷，绝不 throw。键名选择：Ctrl+Shift+I 与既有 Ctrl+Shift+D 平行、'I' 单键无既有映射，customBindings 路径不受影响（注入走固定组合键，不入 customBindings 体系）。
  </action>
  <verify>
    <automated>flutter test test/widget/player/keyboard_handler_debug_injection_test.dart && flutter analyze</automated>
    <fails_when>任一注入链路测试失败（组合键未弹卡 / 双按被去重合并 / 单键 I 误触发 / 报告字段不符）或 flutter analyze 报告 error，命令即非零退出。</fails_when>
  </verify>
  <done>按下 Ctrl+Shift+I 后 ErrorCard 以合成 message 弹出（widget test 证明全链路）；10s 内两次按下产生两条独立报告（去重被计数绕过）；单独按 I 无副作用；flutter analyze 0 error。</done>
</task>

<task type="auto">
  <name>Task 2: OSD 注入反馈 + F1 帮助条目（l10n 再生）</name>
  <files>lib/ui/player/keyboard_handler.dart, lib/l10n/app_en.arb, lib/l10n/app_zh.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_en.dart, lib/l10n/app_localizations_zh.dart, test/widget/player/keyboard_handler_debug_injection_test.dart</files>
  <action>
    - OSD 反馈：_injectTestError 尾部追加 OsdService.I.show('已注入测试错误', icon: Icons.bug_report)——循 error_card.dart:155（errorCardCopied）与 error_card_host.dart:168 的既有 OSD pill 反馈先例。文本用 dev-only 中文字面量（该路径仅在 kDebugMode 下可达，与 speed_button.dart '1x' 同类不进 l10n；卡片本体消息带计数，OSD 只做触发确认）。
    - l10n 键：app_en.arb 加 `"shortcutDebugInjectError": "Inject test error (debug)"`，app_zh.arb 加 `"shortcutDebugInjectError": "注入测试错误（调试）"`，各带 `@shortcutDebugInjectError` description（对齐 app_en.arb:25-26 的 shortcutPlayPause 格式）。随后运行 `flutter gen-l10n` 再生三个 committed 生成文件（pubspec.yaml generate: true）。注意：lib/l10n 生成文件当前工作区存在仅行尾（LF/CRLF）的空 diff——再生后直接提交即可，勿手工编辑生成物。
    - 帮助条目：shortcutDefinitions 列表（keyboard_handler.dart:15-26）尾部追加 collection-if 条目 `if (kDebugMode) ('Ctrl+Shift+I', l10n.shortcutDebugInjectError)`——kDebugMode 为编译期 const，release 构建的 F1 帮助自动不含该行；shortcuts_help_dialog.dart:27 渲染这份单一数据源，无需改帮助对话框本体。同步更新 shortcutDefinitions 顶部「新增快捷键必须同时更新此列表与 _handleKeyEvent」的注释所约束的两处一致性（列表 + 分支均在本文件内，已满足）。
    - 测试扩展（同 Task 1 测试文件）：用例 A——按下组合键 pump 后 expect OsdService.I.message.value?.text == '已注入测试错误' 且 .icon == Icons.bug_report（OsdMessage 字段为 text/icon/progress，osd_overlay.dart:9-16）。用例 B——在 MaterialApp delegates 上下文取 AppLocalizations 后调 shortcutDefinitions(l10n)，expect 结果含 first 元素为 'Ctrl+Shift+I' 的条目（flutter test 运行于 debug 模式，kDebugMode 为 true，条目应出现）。
  </action>
  <verify>
    <automated>flutter test test/widget/player/keyboard_handler_debug_injection_test.dart && flutter analyze</automated>
    <fails_when>任一反馈与可发现性测试失败（OSD 反馈缺失 / F1 帮助缺 Ctrl+Shift+I 条目）或 flutter analyze 报告 error，命令即非零退出。</fails_when>
  </verify>
  <done>注入有 OSD pill 确认；F1 帮助在 debug 构建列出 Ctrl+Shift+I；l10n 生成物与 ARB 同步提交；flutter analyze 0 error。</done>
</task>

<task type="auto">
  <name>Task 3: 收尾全量质量门（红绿之外的项目红线）</name>
  <files></files>
  <action>
    - 运行全项目门：flutter analyze（0 error 红线）；flutter test 全绿——headless 预存失败按既有基线鉴别（mdk.dll FFI 加载类），非本计划回归的失败须记录并排查；bash tool/audit/kernel_logger_gate.sh（新增代码全部位于 UI 层与 l10n，不触及 kernel debugPrint 红线，门仍须通过）。
    - 改动面自检：本计划触及面仅三处——UI 快捷键文件、l10n ARB 与其生成物、新测试文件。诊断内核与 media_kit 均零接触（由各任务 files 列表结构保证）。
    - 若 Task 1/2 的提交尚未做，按 conventional commits 收口：test(03-05) RED、feat(03-05) 注入链路与反馈。
  </action>
  <verify>
    <automated>flutter analyze && flutter test && bash tool/audit/kernel_logger_gate.sh</automated>
    <fails_when>任一环节非零退出——flutter analyze 报告 error、flutter test 有失败（预存失败按基线鉴别后仍属新增失败须排查记录）、或 audit 脚本 kernel_logger_gate.sh 非零退出（kernel 层检出 debugPrint 违规）。</fails_when>
  </verify>
  <done>analyze 0 error；flutter test 全绿（预存失败按基线鉴别并记录）；kernel_logger_gate 通过；无 kernel/原生文件出现在改动集。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 键盘输入 → reporter intake | debug 会话中的本地按键触发合成诊断载荷进入统一错误链（可信开发者输入，仅 kDebugMode 可达） |
| 合成报告 → error.log / 徽标 | 合成载荷经既有脱敏管线落盘与展示 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Tampering | 键盘注入分支 | low | mitigate | kDebugMode 编译期门控使 release/MSIX（distribute_options.yaml 为 release 构建）物理不含该入口；合成 message 带「调试注入」标记，与真实错误可区分 |
| T-03-05-02 | Information Disclosure | error_reporter 落盘/展示 | low | mitigate | 合成载荷走既有链路：message 与 stack 经 DiagnosticRedactor.redactDiagnosticText 脱敏后才进队列与 effects（error_reporter.dart:300-306）；内容为固定中文字面量 + StackTrace.current（仅 debug 构建含路径） |
| T-03-05-03 | DoS | reporter FIFO / error.log | low | accept | 连按受既有有界约束：FIFO 上限 5（_maxQueueLength）、文本 4KiB/栈 16KiB 截断；debug-only 造成的日志噪声由「调试注入」标记事后可 grep 鉴别 |
| T-03-05-SC | Supply Chain | 依赖安装 | high | accept | 本计划零新包安装（无 npm/pip/cargo/pub 新依赖），package-legitimacy 门不适用；pubspec 不变 |
</threat_model>

<verification>
- 组合键 → 卡片全链路：test/widget/player/keyboard_handler_debug_injection_test.dart 通过（Task 1）
- 去重绕过：双按两卡、occurrenceCount 均 1（Task 1）
- 反馈与可发现性：OSD pill 断言 + shortcutDefinitions debug 条目（Task 2）
- release 排除：kDebugMode 编译期门控（代码审查级证据，release 无法在 debug 测试运行中直接验证）
- 实机收口：G-03-1 的最终证据是实机 `flutter run -d windows` debug 会话按键观察卡片弹出——由 /gsd-verify-work 对 UAT Test 1 复测收口（widget test 无法证明实机键盘焦点与窗口交互）
</verification>

<success_criteria>
- flutter analyze 0 error；flutter test 全绿（headless 预存失败按基线鉴别）；kernel_logger_gate 通过
- debug 会话按下 Ctrl+Shift+I：卡片弹出、OSD 反馈、error.log 与捕获徽标同步更新（全真实链路）
- 快速连按每次出新卡（计数后缀绕过 10s 去重窗）
- release/MSIX 构建不含注入入口与帮助条目
- 零 kernel 改动、零 media_kit 接触、未引入新状态库（ValueNotifier 惯例未破坏）
</success_criteria>

<output>
Create `.planning/phases/03-playback-error-card-bridge/03-05-SUMMARY.md` when done
</output>
