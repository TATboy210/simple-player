---
phase: 03-playback-error-card-bridge
plan: 06
type: execute
wave: 6
depends_on: [03-05]
gap_closure: true
gap_ids: [G-03-2, G-03-3, G-03-4]
files_modified:
  - lib/ui/player/keyboard_handler.dart
  - lib/app.dart
  - lib/l10n/app_en.arb
  - lib/l10n/app_zh.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_en.dart
  - lib/l10n/app_localizations_zh.dart
  - test/widget/player/keyboard_handler_test.dart
  - test/widget/player/keyboard_handler_debug_injection_test.dart
  - test/widget/player/keyboard_handler_global_dispatch_test.dart
  - test/widget/player/error_card_mount_position_test.dart
autonomous: true
requirements: [CARD-02]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 窗口化时错误卡片顶缘位于标题栏下缘（视频区上缘），不再遮挡自定义标题栏；全屏时卡片仍显示于窗口左上角（D-10 保持不变）
    - 主焦点滞留在 KeyboardHandler 焦点子树之外（dead-keyboard 态，含 primaryFocus 为 null / 附着于根 scope）时，按 F1 仍弹出快捷键帮助对话框；Space/M/ESC/媒体键同样经回退路径可达
    - 回退分发不吞键：文本输入框、音量 Slider、播放列表面板、对话框（设置/帮助）内的按键行为与改造前一致；F1 不会堆叠帮助对话框
    - 源码中不再存在 G-03-1 注入入口（快捷键分支、计数器、注入方法、帮助条目）与对应 l10n key，debug 构建同样不含
  artifacts:
    - lib/ui/player/keyboard_handler.dart — StatefulWidget 注册 HardwareKeyboard 回退 handler（initState 注册 / dispose 注销）+ 路由/EditableText 双守卫 + KernelLogger 埋点
    - lib/app.dart — buildErrorCardMount 接收可选 WindowMode ValueListenable，窗口化 top 偏移 Tokens.titleBarHeight
    - lib/l10n/*.arb + 三个生成物 — 注入 l10n key 移除后 flutter gen-l10n 再生
    - test/widget/player/keyboard_handler_global_dispatch_test.dart — dead-keyboard 回归 + 不吞键守卫矩阵
    - test/widget/player/error_card_mount_position_test.dart — 窗口化/全屏双位置断言
  key_links:
    - app.dart 挂载 Positioned top ← windowService.mode（ValueListenable<WindowMode>，App 既有字段直传）
    - KeyboardHandler State 生命周期 ← HardwareKeyboard.instance.addHandler（initState 注册 / dispose 注销，测试间无泄漏）
    - 回退接管条件 ← FocusManager.instance.primaryFocus（仅当无 ModalRoute 祖先且非 handler 焦点后代时接管——这是『修死键盘』与『不吞键』同时成立的关键边界）
---

<objective>
关闭 Phase 3 UAT 的三个 open gap（G-03-2 / G-03-3 / G-03-4），全部为零 kernel 改动、零 media_kit 接触的 UI 层修复：

1. **G-03-2（major）**：错误卡片窗口化时下移到视频区域左上角，不再遮挡自定义标题栏；全屏时保持 D-10（窗口左上角）。
2. **G-03-3（major）**：修复 F1（及全部快捷键）的 dead-keyboard 失效——快捷键分发增加 HardwareKeyboard 全局回退入口，主焦点滞留时按键仍可达；附 KernelLogger 埋点留证。
3. **G-03-4（minor）**：移除 G-03-1 的调试注入入口（用后即撤）——快捷键分支、注入方法、计数器、帮助条目、l10n key 双语 ARB、相关测试用例。

Purpose: UAT Test 2/3/4 三项 issue（用户实机反馈「卡片压标题栏」「按f1没用」「测试用的入口记得移除」）；三 gap 同文件域（keyboard_handler.dart / app.dart），单 plan 顺序收口。
Output: 03-06-PLAN 执行后 UAT 三个 gap 可复测通过；analyze 0 error；全量测试全绿（含 error_card/host 316 用例、equivalence 6 用例）。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playback-error-card-bridge/03-UAT.md
@.planning/debug/g03-3-f1-help-dialog-no-op.md
@.planning/phases/03-playback-error-card-bridge/03-05-SUMMARY.md

# 现行代码事实源（改动对象）
@lib/ui/player/keyboard_handler.dart
@lib/app.dart
@lib/ui/player/player_keyboard_actions.dart
@lib/ui/player/player_video_controls.dart
@lib/ui/player/player_screen.dart

# 守卫冲突清单（Task 2 设计依据——回退分发不得吞这些键）
@lib/ui/player/volume_controls.dart
@lib/ui/playlist/playlist_panel.dart
@lib/ui/shared/spin_control.dart
@lib/kernel/window_bridge/window_bridge.dart

# 测试基建先例
@test/widget/player/keyboard_handler_test.dart
@test/widget/player/error_card_host_test.dart
@test/helpers/fake_window_service.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 移除调试注入入口（G-03-4，RED→GREEN）</name>
  <files>lib/ui/player/keyboard_handler.dart, lib/l10n/app_en.arb, lib/l10n/app_zh.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_en.dart, lib/l10n/app_localizations_zh.dart, test/widget/player/keyboard_handler_test.dart, test/widget/player/keyboard_handler_debug_injection_test.dart</files>
  <behavior>
    - Test 1: shortcutDefinitions(l10n) 返回的条目集中不存在显示文本为调试注入组合键的条目（RED：当前 collection-if 条目仍在 → 断言失败）
    - Test 2: pump KeyboardHandler（既有 _buildSubject harness + _CallbackTracker），模拟按下 Ctrl+Shift 注入组合键（control 按下 → shift 按下 → keyI 按下/抬起序列，可循被删测试文件的组合键模拟方式）→ 全部回调计数保持 0（RED：当前注入分支触发 reporter/OSD；本测试用 tracker 断言即可证明分支消失，无需 reporter 初始化）
    - Test 3（编译期门）: l10n key 移除 + gen-l10n 再生后，生成物中不再有该 getter——由 flutter analyze 兜底（若 ARB 与生成物不同步，分析器对 keyboard_handler.dart 报错）
  </behavior>
  <action>
    先写 RED：在 test/widget/player/keyboard_handler_test.dart 追加一个 group（命名体现「调试入口已移除」），含上述 Test 1/2 两个用例；运行确认失败（入口仍存在）。

    GREEN 删除清单 = 03-05-SUMMARY key-files.modified 列出的注入四件套，对照 lib/ui/player/keyboard_handler.dart 现行源码逐项移除：
    - shortcutDefinitions 末尾的 kDebugMode collection-if 帮助条目及其上方两行注释（该条目引用的 l10n getter 一并失去引用）
    - 类内的 static dev-only 注入计数器（含其 doc comment）
    - Ctrl+Shift+I 按键分支（位于既有 Ctrl+Shift+D 调试导出分支之后，含注释）——注意 Ctrl+Shift+D 调试导出（DebugExporter）是 FEAT 既有能力，不在 G-03-4 范围，必须保留
    - 注入方法本体（ErrorReporterImpl 调用 + OsdService 反馈）
    - 随方法移除而失效的 import：error_reporter.dart 与 osd_overlay.dart 两个 import 行（删后 flutter analyze 的 unused_import 会指认；逐个确认文件内确无其他使用点后再删）
    - l10n：从 lib/l10n/app_en.arb 移除注入条目键与其 @description 元数据键（app_en.arb:51-52 附近），从 lib/l10n/app_zh.arb 移除对应键（app_zh.arb:28 附近）；运行 flutter gen-l10n 再生三个生成物（03-05 先例：l10n.yaml 存在时的 "To use the command line arguments" 提示为信息性，再生正常，用 grep 验证生成物三文件同步）
    - 删除 test/widget/player/keyboard_handler_debug_injection_test.dart 整个文件（6 用例全部针对被移除入口）
    - keyboard_handler_test.dart 的 Test 2 组合键模拟：键序用 sendKeyDownEvent(controlLeft) → sendKeyDownEvent(shiftLeft) → sendKeyDownEvent(keyI)/sendKeyUpEvent，断言 tracker 全 0 后补 keyI/shift/control 的 keyUp（不留悬挂修饰态）

    Gap reason: UAT Test 4「还有测试用的入口记得移除」——G-03-1 注入入口验证使命已完成（03-05-SUMMARY），用户决策用后即撤。
  </action>
  <verify>
    <automated>flutter analyze 0 error && flutter test test/widget/player/keyboard_handler_test.dart 全绿 && bash -c 'c1=$(grep -rc "shortcutDebugInjectError" lib/ | grep -v ":0" | wc -l); c2=$(grep -rc "_injectTestError\|_debugInjectedErrorCount" lib/ test/widget/player/keyboard_handler_test.dart | grep -v ":0" | wc -l); c3=$(test -f test/widget/player/keyboard_handler_debug_injection_test.dart && echo 1 || echo 0); [ "$c1" = "0" ] && [ "$c2" = "0" ] && [ "$c3" = "0" ] && echo GATE-PASS'</automated>
    <fails_when>GATE-PASS 未输出——analyze 非 0 error、keyboard_handler_test 红、注入符号 grep 计数非 0、注入测试文件仍存在任一情形，或新增 absence 用例红，命令即非零退出。</fails_when>
  </verify>
  <done>注入四件套 + l10n key（双 ARB 与三个生成物）+ 注入测试文件全部移除；Ctrl+Shift+D 调试导出等既有能力不受影响；keyboard_handler_test.dart 新 group 证明组合键与帮助条目均已不存在；flutter analyze 0 error。</done>
  <reversibility rating="reversible">删除型改动，git 历史可整体恢复（03-05 三个 commit 为恢复点）；用户明示「用后即撤」决策。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 快捷键分发 HardwareKeyboard 全局回退（G-03-3，RED→GREEN）</name>
  <files>lib/ui/player/keyboard_handler.dart, test/widget/player/keyboard_handler_global_dispatch_test.dart</files>
  <behavior>
    守卫判定矩阵（回退 handler 只在焦点链无法送达时接管；每行一个测试用例）：
    - 主焦点滞留在 handler 焦点子树之外（在 MaterialApp 之上泵一个 Focus 包裹并使其持焦——其 context 无任何 ModalRoute 祖先）→ 按 F1 弹出帮助（RED：现行代码按键无声无息，复现 UAT 死键盘）
    - primaryFocus 为 null（unfocus 到 root disposition 后）→ 按 F1 弹出帮助（RED：现行代码死键盘）
    - primaryFocus 为音量 Slider 焦点节点（Slider 传自建 focusNode 并 requestFocus，volume_controls.dart:221 为 Material Slider 有焦点 Arrow 调节）→ ← 触发 Slider 调节、seekBackward 计数保持 0（回退不抢 Slider 方向键）
    - primaryFocus 为播放列表面板焦点节点（自有 Focus + ESC 关闭语义，playlist_panel.dart:184 先例）→ ESC 由面板消费、exitFullscreen 计数保持 0
    - 帮助对话框打开且焦点在对话框内 → 再按 F1 不堆叠第二个对话框；对话框内焦点节点收到 ↑ 时 volumeUp 计数保持 0（DialogRoute 语义不被回退劫持）
    - primaryFocus 为根 Overlay 内无 route 祖先的 TextField（OverlayEntry 注入）→ Space 进入文本框（controller.text 含空格）而非触发 playPause——直接演练 EditableText 守卫分支
    - 正常态（autofocus 回退 handler 的焦点节点持焦）→ 全部 22 个既有 keyboard_handler_test 用例语义不变（焦点分发主路径保持原样，直接由既有套件回归证明）
  </behavior>
  <action>
    方向取舍（记录）：gap missing 原文为「分发改为全局入口」；规划期核对 volume_controls.dart:221（Material Slider 焦点方向键调节）、playlist_panel.dart:184（面板 Focus ESC）、DialogRoute 内部组件（settings/spin_control.dart:114、setting_slider_row.dart:85）后确认：无差别全局替换会把回退 handler 放在焦点分发**之前**，吞掉上述三类按键。锁定为**回退式**：焦点分发保持主路径（既有 22 用例语义零变化），仅当按键根本无法沿焦点链送达 handler 子树时由全局回退接管——dead-keyboard 修复与「不吞键」约束同时成立，达成 gap truth（F1 在任何焦点态都弹出帮助）。

    实现要点（lib/ui/player/keyboard_handler.dart，单文件改造）：
    - KeyboardHandler 由 StatelessWidget 改 StatefulWidget；build 返回 Focus(autofocus: true, focusNode: 自建字段, onKeyEvent: 既有处理函数, child: child)——焦点包装**保留**（正常态主路径与既有测试语义不变），FocusNode 由内部匿名改为显式字段（守卫需引用其身份），dispose 时释放
    - initState 注册 HardwareKeyboard.instance.addHandler(回退处理函数)，dispose 注销（同 handler 引用，测试间零泄漏）；回退函数读 widget 字段取回调，与焦点路径共享同一套回调闭包（player_screen 每次 mode 变化重建 widget，State.widget 始终最新——onExitFullscreen 全屏闭包新鲜度保持）
    - 现有按键匹配主体提取为私有共享分发函数（KeyEvent → bool handled），焦点路径与回退路径共用，保证两路按键语义永不漂移
    - 回退接管守卫（顺序执行）：KeyDownEvent 之外放行 → EditableText 守卫（现行守卫原样复用）→ 焦点可达性守卫：primaryFocus 非 null 且（是 handler 焦点节点自身或其后代，或其 ModalRoute 祖先存在且不等于 handler 自身 route）时返回 false 放行给焦点分发；primaryFocus 为 null、context 已脱离、无任何 ModalRoute 祖先且非 handler 后代（滞留根 scope / 外部 Focus）时才接管。ModalRoute 比较：didChangeDependencies 捕获 handler 自身 route；后代判定沿 primaryFocus 的 parent 链上溯与 handler 焦点节点比对身份
    - KernelLogger 埋点（gap missing 第 2 项）：回退路径接管时以 debug 级记录（message 含「回退」语义 + context 附 primaryFocus 描述与按键名）；F1 到达但被守卫放行时同样留一条 debug 记录。两处埋点以 KernelLoggerImpl.isInitialized 探针守卫（WR-02 先例，kernel_logger.dart:506-511）——测试环境不 init logger 也绝不能抛 StateError；不使用静默 catch
    - ESC 语义不变（既有分支原样进入共享分发函数）：全屏滞留态经回退路径触发 onExitFullscreen 闭包恢复窗口态；窗口化 ESC 维持既有消费语义（播放列表/对话框按键由焦点分发先行处理，守卫矩阵已覆盖）

    测试（新建 test/widget/player/keyboard_handler_global_dispatch_test.dart，harness 循 keyboard_handler_test.dart 的 _CallbackTracker/_buildSubject 模式，不 init KernelLoggerImpl——顺带证明埋点探针在未初始化下不抛）：
    - 复现配方来自 .planning/debug/g03-3-f1-help-dialog-no-op.md note 字段：home 注入 KeyboardHandler（onShowHelp → showDialog），外层 Focus 持焦模拟焦点滞留
    - 按守卫判定矩阵逐行成测；Slider 用例给 Slider 传自建 focusNode；对话框用例 dialog 内容放一个消费 ↑ 的 Focus 节点
    - RED：先泵现行代码跑全部新用例 → 死键盘两用例失败 + 不吞键用例（Slider/面板/对话框）失败（现行代码无回退，不吞键用例里 Slider 用例应过、F1 用例死——以实际 RED 输出为准记录证据）

    Gap reason: UAT Test 3「按f1没用」，root cause = 主焦点滞留 handler 子树外（debug session CONFIRMED），error.log 零异常佐证「按键未到达」而非「到达后失败」。
  </action>
  <verify>
    <automated>flutter analyze 0 error && flutter test test/widget/player/keyboard_handler_global_dispatch_test.dart && flutter test test/widget/player/keyboard_handler_test.dart && flutter test</automated>
    <fails_when>死键盘两用例未转绿、守卫矩阵任一不吞键用例红、既有 22 用例回归红，或全量 flutter test 任一套件失败，命令即非零退出。</fails_when>
  </verify>
  <done>死键盘两用例（外部 Focus 滞留 / primaryFocus null）转绿：F1 弹出帮助；守卫矩阵全部通过（Slider 方向键、面板 ESC、对话框按键、Overlay TextField Space 均不被回退吞掉，F1 不堆叠对话框）；既有 22 用例零改动全绿；回退 handler initState/dispose 注册注销配对；KernelLogger 埋点在未初始化环境不抛。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 错误卡片窗口化时下移至视频区上缘（G-03-2，RED→GREEN）</name>
  <files>lib/app.dart, test/widget/player/error_card_mount_position_test.dart</files>
  <behavior>
    - Test 1: 挂载层收到窗口化 mode（ValueNotifier(windowed/maximized)）→ ErrorCardHost 顶左 dy == Tokens.titleBarHeight + Tokens.spMd（44.0，视频区上缘 + 既有呼吸距），dx == Tokens.controlBarMarginH——标题栏（Tokens.titleBarHeight = 32.0，custom_title_bar.dart:98 同源常量）不再被遮挡
    - Test 2: 挂载层收到 fullscreen mode → dy == Tokens.spMd（12.0）——D-10 全屏=窗口左上角语义保持
    - Test 3: mode 参数缺省（builder 直接 tear-off 引用 buildErrorCardMount，error_card_host_test.dart:53 既有用法）→ 按窗口化偏移定位（既有挂载测试的确定性回归锚）
  </behavior>
  <action>
    定位方案权衡（规划期锁定其一）：标题栏高度常量偏移 vs GlobalKey/布局回调取视频区实际位置。锁定**常量偏移**——Tokens.titleBarHeight 是 CustomTitleBar 实际布局高度的同一编译期常量（custom_title_bar.dart:98 SizedBox 直接消费），单源保证永不错位；GlobalKey 方案需要跨 player_screen（视频区）与 app.dart（挂载层）两层传递布局回调，引入新耦合而无精度收益（标题栏高度恒定、不随窗口尺寸/布局变化）。

    实现要点（lib/app.dart）：
    - buildErrorCardMount 增加可选命名参数 mode（ValueListenable<WindowMode>?），默认 null。可选命名参数保证既有 `builder: buildErrorCardMount` tear-off（error_card_host_test.dart:53 / error_card_test.dart:119 / error_banner_equivalence_test.dart:56）继续编译且行为确定为窗口化偏移
    - mode 非 null 时：Stack 第二个子节点包 ValueListenableBuilder<WindowMode>（订阅传入 listenable——项目 ValueNotifier 惯例，CARD-06），builder 内按 mode.isFullscreen 选 top：全屏 → Tokens.spMd（现状，D-10 保持）；非全屏 → Tokens.titleBarHeight + Tokens.spMd。Positioned 仍是 Stack 直接子节点（hit-test 穿透语义 CARD-02 不变——仅 top 值变化，不改 Positioned 结构、不加全尺寸容器）
    - mode 为 null 时：维持现结构，top 取窗口化偏移值（确定性分支，供测试与潜在非窗口宿主使用）
    - App.build 的 builder 闭包改为 (context, navigator) => buildErrorCardMount(context, navigator, mode: windowService.mode)——App 既有 windowService 字段（app.dart:20），WindowBridge.mode 即 ValueListenable<WindowMode>（window_bridge.dart:25），零新增 plumbing
    - 更新 buildErrorCardMount 与 _ErrorCardOverlayMount 的 doc comment：补记窗口化/全屏双定位语义（G-03-2 修复记录），保留既有 CARD-02 穿透与 CR-01 Overlay 说明
    - 窗口化偏移提取为一个私有常量或 getter（避免魔法算式散落），值 = Tokens.titleBarHeight + Tokens.spMd

    测试（新建 test/widget/player/error_card_mount_position_test.dart）：harness 循 error_card_host_test.dart 的单例重建惯例（ErrorReporterImpl.resetForTesting + init(effects: [ErrorCaptureSnapshot.I.record])）+ MaterialApp(home: SizedBox.shrink(), builder: 注入 mode notifier 的 buildErrorCardMount)；注入一份真实报告使卡片可见后，tester.getTopLeft(find.byType(ErrorCardHost)) 断言 dy/dx。Test 2 用同一 notifier 置 fullscreen 后 pumpAndSettle 再断言。

    Gap reason: UAT Test 2「报错窗口现在在自定义标题栏前面」——root cause = app.dart 挂载 Positioned 基于窗口原点，窗口左上角即标题栏位置（03-UAT.md gap root_cause）。
  </action>
  <verify>
    <automated>flutter analyze 0 error && flutter test test/widget/player/error_card_mount_position_test.dart && flutter test test/widget/player/error_card_host_test.dart && flutter test test/widget/player/error_card_test.dart && flutter test test/widget/player/error_banner_equivalence_test.dart</automated>
    <fails_when>窗口化 dy 断言偏离 44.0、全屏 dy 偏离 12.0、tear-off 缺省路径定位不符，或 host/equivalence 既有套件任一红，命令即非零退出。</fails_when>
  </verify>
  <done>窗口化 mode 下卡片 dy == 44.0（标题栏 32.0 之下，视频区上缘）、全屏 mode 下 dy == 12.0（D-10 保持）；tear-off 缺省路径按窗口化偏移；error_card/host 316 用例与 equivalence 6 用例全绿（位置断言为 lessThan(100) 宽松界，44.0 仍通过，无需改动既有断言）；flutter analyze 0 error。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 键盘事件 → 全局回退 handler | HardwareKeyboard.instance.addHandler 是进程级入口，先于焦点分发运行；错误守卫会劫持文本输入/控件按键 |
| 用户输入 → 注入入口（移除前） | kDebugOnly 注入快捷键曾可构造合成错误走真实链路 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | Tampering | keyboard_handler.dart 回退分发 | medium | mitigate | 双守卫（EditableText + 焦点可达性）+ 守卫判定矩阵 6 用例（Slider/面板/对话框/TextField 逐项证明不吞键）；Task 2 `<behavior>` 矩阵即回归锁 |
| T-03-06-02 | Information disclosure | keyboard_handler.dart 注入入口 | low | mitigate | Task 1 全量移除（快捷键分支/计数器/注入方法/帮助条目/l10n key）；grep 负向门证明 lib/ 零残留 |
| T-03-06-SC | Tampering | npm/pip/cargo installs | high | N/A | 本计划零包安装（无 pubspec 变更），无供应链面 |
</threat_model>

<verification>
1. `flutter analyze` 0 error（全项目）
2. `flutter test` 全项目全绿——重点回归：keyboard_handler_test.dart（22 既有 + 新增 absence group）、keyboard_handler_global_dispatch_test.dart（新，守卫矩阵）、error_card_host_test.dart / error_card_test.dart（316 用例）、error_banner_equivalence_test.dart（6 用例）、error_card_mount_position_test.dart（新，双位置）
3. grep 负向门：注入四件套与 l10n key 在 lib/ 与测试目录零残留；`grep -c "HardwareKeyboard.instance.addHandler" lib/ui/player/keyboard_handler.dart` ≥ 1（回退入口已注册）
4. 改动面结构自检：git diff 仅触碰 UI 层（lib/ui/player/keyboard_handler.dart、lib/app.dart、l10n 五文件、测试四文件）——零 kernel 文件、零 media_kit 接触、零 windows/ 原生文件
5. 实机复验（UAT re-test，非本 plan 自动化范围）：窗口化下报错卡片不压标题栏；全屏进出循环后 F1 弹帮助（5 秒判别实验的焦点链分支已被本修复覆盖）；Ctrl+Shift+I 无响应
</verification>

<success_criteria>
- G-03-2：窗口化时卡片顶缘 = 标题栏下缘（自动断言 dy == 44.0），全屏 = 窗口左上角（dy == 12.0，D-10）
- G-03-3：primaryFocus 滞留（null / 根 scope / 外部 Focus）时 F1 弹出帮助；正常态、Slider、播放列表、对话框、文本框按键行为与改造前一致（守卫矩阵全绿 + 既有 22 用例零改动全绿）
- G-03-4：注入入口四件套与 l10n key 全量移除，debug 构建同样不含；负向 grep 门通过
- 三 gap 同 plan 收口后，UAT Test 2/3/4 可复测；analyze 0 error；全项目 test 全绿
</success_criteria>

<output>
Create `.planning/phases/03-playback-error-card-bridge/03-06-SUMMARY.md` when done
</output>
