---
phase: 01-window-management
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/persistence/playlist_store.dart
  - lib/kernel/engine/fvp_engine.dart
  - lib/kernel/engine/engine_prewarm.dart
  - lib/features/player/services/subtitle_service.dart
autonomous: true
requirements:
  - PERF-02

must_haves:
  truths:
    - "No catch (_) exists anywhere in the codebase"
    - "No on Object catch exists anywhere in the codebase"
    - "All error handlers use on Exception catch (e) with logger"
    - "4 known anti-pattern locations are fixed"
  artifacts:
    - path: "lib/kernel/persistence/playlist_store.dart"
      provides: "Fixed error handling at line ~168"
      contains: "on Exception catch"
    - path: "lib/kernel/engine/fvp_engine.dart"
      provides: "Fixed error handling at line ~544"
      contains: "on Exception catch"
    - path: "lib/kernel/engine/engine_prewarm.dart"
      provides: "Fixed error handling at line ~56"
      contains: "on Exception catch"
    - path: "lib/features/player/services/subtitle_service.dart"
      provides: "Fixed error handling at lines ~37 and ~59"
      contains: "on Exception catch"
  key_links:
    - from: "lib/kernel/persistence/playlist_store.dart"
      to: "lib/kernel/utils/log.dart"
      via: "import and log.d() usage"
      pattern: "log\\.d"
---

<objective>
Replace all catch(_) and on Object catch anti-patterns with proper on Exception catch + logger.

Purpose: These anti-patterns swallow errors silently, making debugging impossible. Proper error handling with logger enables troubleshooting without crashing.
Output: 5 fixed error handling locations + global search confirms no remaining anti-patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-window-management/01-CONTEXT.md
@.planning/phases/01-window-management/01-RESEARCH.md
@.planning/phases/01-window-management/01-PATTERNS.md

<interfaces>
From lib/kernel/utils/log.dart:
```dart
import 'package:logger/logger.dart';
final Logger log = Logger(...);
// Usage: log.d('message'); log.e('message', error: e);
```

From lib/kernel/engine/fvp_engine.dart (correct pattern to follow):
```dart
} on Exception catch (e) {
  debugPrint('FvpEngine.$name error: $e');
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 4 known error handling anti-patterns</name>
  <files>lib/kernel/persistence/playlist_store.dart, lib/kernel/engine/fvp_engine.dart, lib/kernel/engine/engine_prewarm.dart, lib/features/player/services/subtitle_service.dart</files>
  <read_first>
    - lib/kernel/persistence/playlist_store.dart (lines 160-175 — catch (_) at line 168)
    - lib/kernel/engine/fvp_engine.dart (lines 535-550 — on Exception catch (_) at line 544)
    - lib/kernel/engine/engine_prewarm.dart (lines 48-62 — on Object catch at line 56)
    - lib/features/player/services/subtitle_service.dart (lines 30-62 — on Object catch at lines 37 and 59)
    - lib/kernel/utils/log.dart (logger instance)
    - .planning/phases/01-window-management/01-PATTERNS.md (Error Handling Fixes section)
  </read_first>
  <action>
Fix 4 known locations per D-32, D-33, D-34. Each fix follows the same pattern:
- Replace `catch (_)` or `on Object catch (e)` with `on Exception catch (e)`
- Add logging via log.d() or log.e() from kernel/utils/log.dart
- Keep existing behavior (return value, skip logic) unchanged

Fix 1 — playlist_store.dart line ~168:
- Current: `} catch (_) { // 跳过损坏项 }`
- Change to: `} on Exception catch (e) { log.d('PlaylistStore._migrateHistory: skipping corrupt entry: $e'); }`
- Add import for '../../utils/log.dart' if not already imported

Fix 2 — fvp_engine.dart line ~544:
- Current: `} on Exception catch (_) { return 0; }`
- Change to: `} on Exception catch (e) { log.d('FvpEngine.subtitleDelay parse error: $e'); return 0; }`
- Add import for '../utils/log.dart' if not already imported (check existing imports)

Fix 3 — engine_prewarm.dart line ~56:
- Current: `} on Object catch (e) { _prewarmed = false; log.d('EnginePrewarm failed: $e'); }`
- Change to: `} on Exception catch (e) { _prewarmed = false; log.d('EnginePrewarm failed: $e'); }`
- Only change is `on Object` -> `on Exception` (narrowing the catch)
- log import should already exist

Fix 4 — subtitle_service.dart lines ~37 and ~59:
- Line 37 current: `} on Object catch (e) { log.d('SubtitleService.detectAndLoad error: $e'); }`
- Line 37 change to: `} on Exception catch (e) { log.d('SubtitleService.detectAndLoad error: $e'); }`
- Line 59 current: `} on Object catch (e) { log.d('SubtitleService.detectAndLoadSync error: $e'); }`
- Line 59 change to: `} on Exception catch (e) { log.d('SubtitleService.detectAndLoadSync error: $e'); }`
- Only change is `on Object` -> `on Exception` at both locations
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && dart analyze --fatal-infos lib/kernel/persistence/playlist_store.dart lib/kernel/engine/fvp_engine.dart lib/kernel/engine/engine_prewarm.dart lib/features/player/services/subtitle_service.dart 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - playlist_store.dart line ~168 uses `on Exception catch (e)` (not `catch (_)`)
    - playlist_store.dart line ~168 contains log.d call
    - fvp_engine.dart line ~544 uses `on Exception catch (e)` (not `on Exception catch (_)`)
    - fvp_engine.dart line ~544 contains log.d call
    - engine_prewarm.dart line ~56 uses `on Exception catch (e)` (not `on Object catch (e)`)
    - subtitle_service.dart line ~37 uses `on Exception catch (e)` (not `on Object catch (e)`)
    - subtitle_service.dart line ~59 uses `on Exception catch (e)` (not `on Object catch (e)`)
    - dart analyze passes with no errors
  </acceptance_criteria>
  <done>All 4 known error handling anti-patterns fixed. Each uses on Exception catch (e) with logger. Existing behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Global search for remaining anti-patterns</name>
  <files></files>
  <read_first>
    - .planning/phases/01-window-management/01-CONTEXT.md (D-33: fix scope is 4 known + global search)
  </read_first>
  <action>
Run global search across the entire lib/ directory for any remaining error handling anti-patterns per D-33.

Search patterns:
1. `catch (_)` — bare catch with underscore discard
2. `on Object catch` — catches Error subtypes (too broad)

Commands to run:
- grep -rn "catch (_)" lib/ --include="*.dart" (find bare catch with underscore)
- grep -rn "on Object catch" lib/ --include="*.dart" (find Object catch)
- grep -rn "catch(e)" lib/ --include="*.dart" (find bare catch without type — should not exist)
- grep -rn "catch (e)" lib/ --include="*.dart" (find bare catch without type — same pattern)

For each match found:
- Verify it is in test files (acceptable) or production code (must fix)
- If in production code: replace with `on Exception catch (e)` + log.d/log.e
- If in test code: leave as-is (tests may need to catch broader exceptions for assertion)

Note: `catch (e)` without a type annotation is also an anti-pattern — it catches Error subtypes. But the explicit anti-pattern from D-32 is `catch (_)` and `on Object catch`. Focus on those two.
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -rn "catch (_)" lib/ --include="*.dart" | grep -v "test/" | grep -v ".dart_tool" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep for "catch (_)" in lib/ (excluding test/) returns 0 matches
    - grep for "on Object catch" in lib/ (excluding test/) returns 0 matches
    - If additional matches found, they are fixed with on Exception catch (e) + logger
    - dart analyze passes with no errors after any additional fixes
  </acceptance_criteria>
  <done>Zero catch(_) or on Object catch patterns remain in production code. Global search confirms D-33 compliance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Error handler -> logger | Exception objects logged (may contain file paths or stack traces) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Exception messages in logs | accept | Debug-only logger (PrettyPrinter), no production log shipping. Exception messages are internal. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan. |
</threat_model>

<verification>
- `dart analyze --fatal-infos` exits 0
- `flutter test` exits 0
- grep for "catch (_)" in lib/ returns 0 production matches
- grep for "on Object catch" in lib/ returns 0 production matches
</verification>

<success_criteria>
- All 4 known anti-pattern locations fixed
- Global search confirms 0 remaining catch(_) or on Object catch in production code
- All fixes use on Exception catch (e) with log.d or log.e
- Existing behavior (return values, skip logic) preserved
- dart analyze passes clean
</success_criteria>

<output>
Create `.planning/phases/01-window-management/01-04-SUMMARY.md` when done
</output>
