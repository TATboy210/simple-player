---
phase: 06-window-code-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/app.dart
  - lib/ui/player/player_screen.dart
autonomous: true
requirements:
  - OPT-01

must_haves:
  truths:
    - "DragToResizeArea disables resize edges when fullscreen is active"
    - "DragToResizeArea uses 6px resizeEdgeSize in windowed mode"
    - "ESC key exits fullscreen"
    - "CustomTitleBar hides when fullscreen (already works, verify only)"
    - "Fullscreen button visible in control bar (already works, verify only — D-01)"
    - "16:9 video matches 16:9 window with no black bars (already works, verify only — D-06)"
  artifacts:
    - path: "lib/app.dart"
      provides: "Fullscreen-aware DragToResizeArea"
      contains: "ValueListenableBuilder<bool>"
    - path: "lib/ui/player/player_screen.dart"
      provides: "ESC fullscreen exit wiring"
      contains: "onExitFullscreen"
  key_links:
    - from: "lib/app.dart"
      to: "WindowService.isFullscreen"
      via: "ValueListenableBuilder listens to isFullscreen ValueNotifier"
      pattern: "valueListenable:.*isFullscreen"
    - from: "lib/ui/player/player_screen.dart"
      to: "KeyboardHandler.onExitFullscreen"
      via: "callback passed to KeyboardHandler constructor"
      pattern: "onExitFullscreen:"
---

<objective>
Fix three window-layer bugs reported by user: DragToResizeArea not fullscreen-aware, resize edge size too small, and ESC key not wired for fullscreen exit.

Purpose: Improve fullscreen interaction -- users can exit fullscreen via ESC and drag-to-resize is properly disabled in fullscreen.
Output: Two files modified with ~15 lines of targeted changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-window-code-optimization/06-CONTEXT.md
@.planning/phases/06-window-code-optimization/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DragToResizeArea fullscreen-awareness and edge size</name>
  <files>lib/app.dart</files>
  <read_first>
    - lib/app.dart (lines 140-165 — current DragToResizeArea usage)
    - lib/kernel/bridge/window_service.dart (isFullscreen ValueNotifier)
    - .planning/phases/06-window-code-optimization/06-RESEARCH.md (Pattern 1: ValueListenableBuilder pattern, Pitfall 1 & 4)
  </read_first>
  <action>
In `lib/app.dart` at line 152-161, replace the static `DragToResizeArea` with a `ValueListenableBuilder<bool>` that listens to `windowService.isFullscreen`:

1. The `_AppState` class needs access to `windowService`. Check if `widget.coordinator` exposes a `WindowService` or if one is available via the widget tree. If not, create a `WindowService` instance in `_AppShellState.initState()` (or use the existing one from the coordinator).

2. Replace lines 152-161 (`home: DragToResizeArea(...)`) with:
   - `ValueListenableBuilder<bool>` listening to `windowService.isFullscreen`
   - Builder returns `DragToResizeArea` with:
     - `resizeEdgeSize: isFullscreen ? 0 : 6` (was 11, now 6 per D-04; 0 when fullscreen)
     - `enableResizeEdges: isFullscreen ? [] : null` (empty list disables all edges, null = all enabled)
     - `resizeEdgeColor: Colors.transparent` (preserve existing)
   - `child:` parameter passes `DeferredPlayerFeature(...)` as the cached child to avoid rebuilds

3. Key details per RESEARCH.md:
   - `enableResizeEdges: []` returns empty Container for all edges (confirmed from window_manager source)
   - Keep DragToResizeArea in tree at all times (never conditionally remove — Pitfall 4)
   - `resizeEdgeSize: 0` effectively disables resize gesture even if edges are present
  </action>
  <verify>
    <automated>flutter analyze lib/app.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/app.dart contains `ValueListenableBuilder<bool>` wrapping `DragToResizeArea`
    - lib/app.dart contains `resizeEdgeSize: isFullscreen ? 0 : 6`
    - lib/app.dart contains `enableResizeEdges: isFullscreen ? [] : null`
    - `valueListenable:` references `isFullscreen` (from WindowService or coordinator)
    - `DeferredPlayerFeature` is passed as the cached `child:` of `ValueListenableBuilder`
    - `flutter analyze` reports zero errors on lib/app.dart
  </acceptance_criteria>
  <done>DragToResizeArea rebuilds with new parameters when fullscreen state changes. resizeEdgeSize is 6 in windowed mode, 0 in fullscreen. All resize edges disabled in fullscreen.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ESC key for fullscreen exit</name>
  <files>lib/ui/player/player_screen.dart</files>
  <read_first>
    - lib/ui/player/player_screen.dart (lines 110-160 — KeyboardHandler instantiation)
    - lib/ui/player/keyboard_handler.dart (lines 39-88 — onExitFullscreen parameter definition)
    - lib/kernel/bridge/window_service.dart (setFullscreen method)
    - .planning/phases/06-window-code-optimization/06-RESEARCH.md (Pitfall 5: onExitFullscreen not passed)
  </read_first>
  <action>
In `lib/ui/player/player_screen.dart`, add `onExitFullscreen` callback to the `KeyboardHandler` constructor at line ~120.

The KeyboardHandler already defines `onExitFullscreen` as an optional `VoidCallback?` parameter (keyboard_handler.dart line 53). PlayerScreen currently does not pass `onExitFullscreen`.

Add after the existing callbacks (around line 146, before `child: Scaffold`):

`onExitFullscreen: () => widget.windowService.setFullscreen(false),`

This ensures:
- F key toggles fullscreen (existing behavior via onToggleFullscreen)
- ESC key exits fullscreen only (new behavior via onExitFullscreen)
- ESC works regardless of control bar visibility (KeyboardHandler Focus wraps entire Scaffold)

Note: `widget.windowService` is already available on PlayerScreen — it is passed as a constructor parameter and used for CustomTitleBar.
  </action>
  <verify>
    <automated>flutter analyze lib/ui/player/player_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/ui/player/player_screen.dart contains `onExitFullscreen:` in the KeyboardHandler constructor call
    - The callback calls `widget.windowService.setFullscreen(false)`
    - `flutter analyze` reports zero errors on lib/ui/player/player_screen.dart
    - `grep -n "onExitFullscreen" lib/ui/player/player_screen.dart` returns at least 1 match
  </acceptance_criteria>
  <done>ESC key exits fullscreen. KeyboardHandler receives onExitFullscreen callback that calls windowService.setFullscreen(false). Works regardless of control bar visibility.</done>
</task>

</tasks>

<verification>
Run `flutter analyze` on both modified files.
Run `flutter test test/widget/player/` to verify no regressions.
Manual check: open app, press F to enter fullscreen, verify resize edges disabled, press ESC to exit fullscreen.
Manual check (D-01): verify fullscreen button visible in bottom-right control bar (not clipped or hidden).
Manual check (D-06): open a 16:9 video, verify it fills the 960x540 window with no letterboxing or black bars.
</verification>

<success_criteria>
- DragToResizeArea uses resizeEdgeSize 6 in windowed mode
- DragToResizeArea disables all resize edges in fullscreen (enableResizeEdges: [])
- ESC key exits fullscreen
- All existing tests pass (flutter test)
- flutter analyze reports zero errors
</success_criteria>

<output>
Create `.planning/phases/06-window-code-optimization/06-01-SUMMARY.md` when done
</output>
