---
phase: 15-contract-freeze-baseline-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/engine/media_engine.dart
  - lib/kernel/engine/engine_state_view.dart
  - lib/kernel/engine/playback_control.dart
  - lib/kernel/engine/track_control.dart
  - lib/kernel/engine/subtitle_config.dart
  - lib/kernel/engine/video_effect_control.dart
  - lib/kernel/engine/renderer_control.dart
  - lib/kernel/engine/volume_control.dart
  - lib/kernel/engine/fvp_engine.dart
  - .planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md
autonomous: true
requirements: [BASE-01, BASE-03]

must_haves:
  truths:
    - "Every public member of the 7 ISP interface files (media_engine.dart's implements clause: EngineStateView, PlaybackControl, TrackControl, SubtitleConfig, VideoEffectControl, RendererControl, VolumeControl) has a frozen behavioral contract expressed via D2 tags (requires:/ensures:/states:/modifies:/throws:) or, for EngineStateView read-only getters, a class-level group contract per D3"
    - "open() success loads media and transitions state to idle (NOT playing) — the contract explicitly documents that a successful open() leaves state=idle and the caller must call play() to reach playing; this open→idle→play→playing handoff is frozen so the current 'loads video but won't play' regression becomes a documented, testable contract boundary (see debug stubs) rather than silent behavior"
    - "Each states: tag's transition set is hand-verified against engine_state_machine.dart:66-92 _canTransitionTo switch (D7 deliberate cross-check) — no invented transition sets"
    - "The 9-vs-6 state reconciliation is frozen: 6 orthogonal MediaState (idle/opening/playing/paused/completed/error) + orthogonal LifecyclePhase{alive,disposing,disposed}; the v3.0 lifecycle states to add (disposed/disposing/error-recover) are recorded as a P20 known-gap list in 15-CONTEXT.md (BASE-03 sc3)"
    - "fvp_engine.dart carries only THIN implementation-side-effect doc notes (D4) at open()/dispose()/software-decode-fallback/_guardedAction — it does NOT restate requires:/ensures: tags; contract authority lives only on the interface"
    - "media_engine.dart's class doc no longer claims '6 ISP 接口' incorrectly — it accurately describes EngineStateView + 6 control interfaces = 7 implemented interfaces (RESEARCH Pitfall 4)"
    - "flutter analyze stays strict-clean after all doc-comment edits (no !/late/as introduced; comments only)"
  artifacts:
    - lib/kernel/engine/media_engine.dart
    - lib/kernel/engine/engine_state_view.dart
    - lib/kernel/engine/playback_control.dart
    - lib/kernel/engine/track_control.dart
    - lib/kernel/engine/subtitle_config.dart
    - lib/kernel/engine/video_effect_control.dart
    - lib/kernel/engine/renderer_control.dart
    - lib/kernel/engine/volume_control.dart
  key_links:
    - "The frozen states: tags on PlaybackControl.open/play/pause/stop/seekTo become the source Plan 03 contract tests mirror (D7/D19 three-view cross-check: tag → transition table → test)"
    - "The bilingual /// structure introduced here (Chinese intent line + blank + English contract tag block) is the DOC-01 convention Phase 17-22 code will follow — no prior bilingual analog exists in the repo (PATTERNS gap), so this plan introduces it fresh"
    - "The lifecycle-gap known-gap list in 15-CONTEXT.md is consumed read-only by Plan 03 and by Phase 20 (D18 SSOT)"
  prohibitions:
    - "The frozen contract must NOT forbid the v3.0 error-recovery transition (error → {idle, opening} via recover()) that Phase 20 STATE-04 requires — the states: tags must leave recover()'s target set {idle, opening} reachable, not closed off"

  lifecycle_gap_known_list_for_p20:
    - "disposed/disposing/error-recover lifecycle states (D5 LifecyclePhase) are frozen as high-level semantics only (D6); FvpEngine baseline does not yet implement LifecyclePhase — Phase 20 NewFvpEngine must add them"

flagged_assumptions:
  - "R1/BASE-01 edge was probe-unclassified (specless fallback): acceptance authored from BASE-01 text + D1-D4. Machine-checkable via contract_completeness.sh (Plan 01 Task 2) + grep for tag presence."
  - "R3/BASE-03 edge was probe-unclassified (specless fallback): acceptance authored from BASE-03 text + D5-D12. The reconciliation verdict (6 orthogonal + LifecyclePhase) is doc-review verified + grep-checkable (LifecyclePhase named in CONTEXT known-gap list)."

assumption_delta_decision:
  scan_result: "detected:false (assumption-delta scan 15 returned no firing signals)"
  decision: "BASE-03 IS an assumption-changing decision even though the scanner did not fire: the pre-v3.0 assumption was '9-state machine ~40 edges' (PROJECT.md stale). This plan RESOLVES it by freezing '6 orthogonal MediaState + orthogonal LifecyclePhase' (D5-D12). This is a PROMOTE (replace the stale 9-state assumption), not an add-alongside — the 9-state model is retired, not kept beside the 6-state model. Recorded here per hook guidance."
---

<objective>
冻结 `MediaEngine` 及其 7 个 ISP 接口每个成员的行为契约（BASE-01），并裁决 9 态 vs 6 态差异、冻结 v3.0 生命周期态高层语义（BASE-03）。契约唯一权威落点是接口 `///` 双语注释（D1，不设独立 CONTRACT.md）。

Purpose: 让 Phase 16 适配层实现的是 frozen audited behavioral spec 而非 signature 猜测；让 Phase 20 能按 DelegationPolicy 逐能力翻转并每次跑契约测试。无此冻结，引擎替换退化为禁断 big-bang swap。同时把"loads video but won't play"回归所暴露的 open→idle→play→playing 交接语义显式冻结为可测契约边界。
Output: 7 个接口文件的 D2 标签契约 + EngineStateView 组契约（D3）+ fvp_engine.dart 薄实现注释（D4）+ 15-CONTEXT.md 的 P20 lifecycle-gap 清单（D18）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md
@.planning/phases/15-contract-freeze-baseline-audit/15-RESEARCH.md
@.planning/phases/15-contract-freeze-baseline-audit/15-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan MODIFIES existing interface files (doc-comment edits only) and APPENDS to CONTEXT.md. It INTRODUCES these NEW conventions/symbols (exclude from drift verification):
- The bilingual `///` doc-comment structure (Chinese intent line + blank + English D2 tag block) — NO prior analog in the repo (PATTERNS gap), introduced fresh here
- The D2 contract tag vocabulary in use on interface members: `requires:` / `ensures:` / `states:` / `modifies:` / `throws:`
- The class-level group-contract block above `abstract class EngineStateView` (D3) — no prior group-contract precedent in the repo
- A new "P20 lifecycle-gap 清单" subsection appended to 15-CONTEXT.md (D18)
- `LifecyclePhase { alive, disposing, disposed }` — named as a FROZEN CONTRACT (D5); the enum is NOT yet implemented in code this phase (Phase 20 implements it), only documented in contracts + CONTEXT known-gap list
</artifacts_this_phase_produces>

<do_not_touch>
In-flight debug work owns these — DO NOT modify:
- `lib/kernel/engine/media_opener.dart` (uncommitted, owned by playback-regression debug)
- `lib/main.dart` (uncommitted)
- `.planning/debug/*.md` (read-only diagnostic stubs — reference their hypotheses in rationale only)
Phase 15 FREEZES the contract; it does NOT fix the regression (fixing is Phase 20+).
</do_not_touch>

<tasks>

<task type="auto">
  <name>Task 1: EngineStateView 组契约 + media_engine 类注释修正（BASE-01 D1/D3, RESEARCH Pitfall 4）</name>
  <files>lib/kernel/engine/engine_state_view.dart, lib/kernel/engine/media_engine.dart</files>
  <read_first>
    - `lib/kernel/engine/engine_state_view.dart` (全文，14 getter + dispose；lines 15-60 现有单行中文意图风格是要保留并扩展的基线)
    - `lib/kernel/engine/media_engine.dart` (全文；line 11 "6 个 ISP 接口" 与 line 24-31 implements 7 接口不一致 — Pitfall 4)
    - `.planning/phases/15-contract-freeze-baseline-audit/15-RESEARCH.md` §"组契约模式（D3）" 目标形状 + §"D2 扩展标签集的定位" (标签是纯文本约定，dartdoc 无原生支持，勿暗示工具链解析)
    - `.planning/phases/15-contract-freeze-baseline-audit/15-PATTERNS.md` §"contract-spec, doc-comment edits" + "Shared Patterns / /// doc-comment style"（保留现有单行中文意图，标签作为新行 APPEND 于下方）
    - `.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md` D1/D2/D3/D9
  </read_first>
  <action>
    在 `abstract class EngineStateView` 声明**上方**新增一个类级组契约块（D3），保留现有类顶部中文意图行，在其下追加双语契约块：中文意图行 + 空行 + `/// requires: 无（所有 getter 幂等、无参数、永不 throw）` + `/// ensures: 返回值反映最近一次内部状态更新；disposed 后返回安全默认值（state→idle, position/duration→0, isSeeking/isBuffering→false，见 D9）` + `/// modifies: 无（本接口所有成员均为纯读取，无副作用）`。14 个 getter 各自保留现有的一行 `///` 中文意图指回组契约，**不逐个写完整契约**（D3）；`dispose()` 保留其现有一行意图（其完整契约在 D8 语境由 PlaybackControl 侧/组契约覆盖，此处只需指明"释放资源、任意态可达、终态不可逆、double-dispose 幂等 no-op"一行摘要）。
    标签顺序固定 `requires → ensures → states → modifies → throws`，无该项省略整行，不留空标签行（RESEARCH §推荐格式）。`modifies:` 用方括号 `[state]` 引用 getter 名（复用 dartdoc `[Identifier]` 语法双重收益）。
    在 `lib/kernel/engine/media_engine.dart` 用 Edit 把 line 11 的"将 6 个 ISP 接口聚合为单一类型"改为准确表述："将 EngineStateView 只读状态视图与 6 个控制类 ISP 接口（共 7 个 implements）聚合为单一类型"（RESEARCH Pitfall 4 顺手修正）。MediaEngine 本身是组合接口，无新增成员，故只修正类注释不加 D2 标签。
    这是本仓库首次引入双语 `///` 结构（PATTERNS 明确无现有 analog）——按 RESEARCH/CONTEXT 的模板新建，勿去仓库找不存在的先例。仅改注释，绝不改签名/逻辑；遵守 CLAUDE.md（Tokens 不涉及、避免 !/late/as、Chinese comments OK）。conventional-commit `docs:`。
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/engine/engine_state_view.dart lib/kernel/engine/media_engine.dart && grep -q "requires:" lib/kernel/engine/engine_state_view.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requires:" lib/kernel/engine/engine_state_view.dart` >= 1 AND `grep -c "ensures:" lib/kernel/engine/engine_state_view.dart` >= 1 AND `grep -c "modifies:" lib/kernel/engine/engine_state_view.dart` >= 1 (group contract present)
    - The group contract block appears ABOVE `abstract class EngineStateView` (grep line number of "requires:" < grep line number of "abstract class EngineStateView")
    - All 14 getters retain their original single Chinese intent line (no getter lost its `///` line)
    - `grep -cE "6 个 ISP|将 6 个" lib/kernel/engine/media_engine.dart` == 0 (stale "6 ISP" claim removed); media_engine.dart now mentions "7" or "EngineStateView ... 6 个控制"
    - `flutter analyze lib/kernel/engine/engine_state_view.dart lib/kernel/engine/media_engine.dart` reports no new errors
  </acceptance_criteria>
  <done>EngineStateView carries a class-level group contract (D3) covering the disposed-safe-default invariant (D9); each getter keeps one intent line; media_engine.dart's class doc accurately states 7 implemented interfaces (Pitfall 4 fixed).</done>
</task>

<task type="auto">
  <name>Task 2: PlaybackControl + 5 控制接口 D2 契约标签（BASE-01 D2/D4/D7, 含 open→idle→play 交接冻结）</name>
  <files>lib/kernel/engine/playback_control.dart, lib/kernel/engine/track_control.dart, lib/kernel/engine/subtitle_config.dart, lib/kernel/engine/video_effect_control.dart, lib/kernel/engine/renderer_control.dart, lib/kernel/engine/volume_control.dart</files>
  <read_first>
    - `lib/kernel/engine/playback_control.dart` (全文，13 方法) — 契约标签落点
    - `lib/kernel/engine/{track_control,subtitle_config,video_effect_control,renderer_control,volume_control}.dart` (全文) — 各接口成员
    - `lib/kernel/engine/fvp_engine.dart` open() lines 236-311（成功路径 270 行 transition 到 **idle** 非 playing）、play() 313-334、pause() 336-348、stop() 350-363、seekTo() 365-397、_guardedAction 222-232、dispose 574-618、software-decode fallback 278-289 — 契约标签的 requires/ensures/states/throws 须如实反映 FvpEngine 当前 baseline 行为（D16）
    - `lib/kernel/engine/engine_state_machine.dart` lines 66-92 `_canTransitionTo` — states: 标签的合法转换集**逐条**须与此 switch 交叉校验（D7），勿臆造
    - `lib/kernel/engine/media_state.dart` — 6 态权威
    - `lib/kernel/models/player_error.dart` — throws: 标签的错误类型（FileError/CodecError/PlaybackError/NetworkError/UnknownError）
    - `.planning/debug/video-playback-cannot-load.md` + `.planning/debug/fvp-playback-history-regression.md` — 回归假设（open 成功后 state=idle，需显式 play() 才 playing；contract 须冻结此交接语义），仅作 rationale 引用，不修改
    - `.planning/phases/15-contract-freeze-baseline-audit/15-RESEARCH.md` §推荐标签行格式 + §"Behavioral assertions over thrown-exception" (FvpEngine 错误路径设 lastError+state=error，不 throw)
  </read_first>
  <action>
    对 `playback_control.dart` 的 13 个方法各自在现有中文意图行下方追加 D2 标签块（保留中文行，APPEND 标签为新 `///` 行）。关键方法：
    - `open(String path)`: `requires: state ∈ {idle, opening, playing, paused, completed, error}`（open 任意非 disposed 态可调，如实反映 fvp_engine.dart open() 无前置态守卫）；`ensures: 成功时 state == idle 且 duration/aspectRatio 更新、lastError == null；调用者须随后 play() 才进入 playing`（**显式冻结 open→idle→play 交接**，回归边界）；`states: transitions to {opening, idle, error}`（与 _canTransitionTo 交叉校验：idle/completed/error→opening 合法，opening→idle 合法，→error 合法）；`modifies: [state], [position], [duration], [aspectRatio], [lastError], [isBuffering]`；`throws: 不抛异常 — 错误经 lastError 赋 FileError(pathEmpty)/CodecError/PlaybackError/NetworkError 并 state→error 表达（D19 行为断言，非 throwsA）`。
    - `play()`: `requires: state ∈ {idle, paused, completed}`（fvp_engine play() 只在非 playing 生效）；`ensures: state == playing`；`states: transitions to {playing, error}`；`modifies: [state]`；`throws: 经 lastError=PlaybackError + state→error 表达`。
    - `pause()`/`stop()`/`seekTo()`/`togglePlayPause()`/`setVolume()`/`setMute()`/`setPlaybackRate()`/`setRange()`/`skipForward()`/`skipBack()` 各按其 fvp_engine baseline 行为如实写标签。`stop()` states: transitions to {idle}；`seekTo()` modifies: [position],[isSeeking]，requires: state ∉ {idle} 且 duration>0。
    对 `track_control.dart`/`subtitle_config.dart`/`video_effect_control.dart`/`renderer_control.dart`/`volume_control.dart` 每个成员追加标签：多数为 `modifies:` + `ensures:`（无状态转换的控制方法 states: 省略）；getter（如 subtitleDelay/activeAudioTracks/volume/isMuted）标 `requires: 无；ensures: 返回当前值；modifies: 无`。
    标签顺序固定，省略缺项，不留空标签行。所有 `states:` 集合逐条对照 `_canTransitionTo` switch（D7 故意重复=交叉校验，Plan 03 测试会镜像断言）。
    **禁止**把 states: 写成关闭 recover() 的 error→{idle,opening} 出口（见 prohibitions；Phase 20 STATE-04 需要该出口可达）。
    绝不改签名/逻辑；仅注释。conventional-commit `docs:`。
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/engine/playback_control.dart lib/kernel/engine/track_control.dart lib/kernel/engine/subtitle_config.dart lib/kernel/engine/video_effect_control.dart lib/kernel/engine/renderer_control.dart lib/kernel/engine/volume_control.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "states:" lib/kernel/engine/playback_control.dart` >= 4 (open/play/stop/seekTo at minimum carry states: tags)
    - `grep -c "requires:" lib/kernel/engine/playback_control.dart` >= 1 AND `grep -c "ensures:" lib/kernel/engine/playback_control.dart` >= 1 AND `grep -c "modifies:" lib/kernel/engine/playback_control.dart` >= 1 AND `grep -c "throws:" lib/kernel/engine/playback_control.dart` >= 1
    - open()'s contract block contains BOTH "idle" (success target state) AND a note that the caller must call play() — verifiable: the lines between open()'s `///` and its signature contain "idle" and "play()"
    - Each of the 5 control interface files (track_control/subtitle_config/video_effect_control/renderer_control/volume_control) has at least one member carrying a `modifies:` or `ensures:` tag (`grep -lE "modifies:|ensures:"` matches all 5)
    - Every `states:` transition set in playback_control.dart is a subset of what `_canTransitionTo` allows for the corresponding source state (hand-verified against engine_state_machine.dart:66-92; documented in the SUMMARY as a checked list) — and error→{idle, opening} is NOT closed off (recover() exit preserved, per prohibitions)
    - `flutter analyze` on the 6 files reports no new errors; no method signature changed (git diff shows only `///` line additions)
  </acceptance_criteria>
  <done>All 13 PlaybackControl methods + every member of the 5 control interfaces carry D2 contract tags; open()'s contract explicitly freezes the open→idle→play→playing handoff (regression boundary); every states: set is cross-checked against the transition table and leaves recover()'s exit reachable.</done>
</task>

<task type="auto">
  <name>Task 3: fvp_engine 薄实现注释（D4）+ P20 lifecycle-gap 清单入 CONTEXT.md（BASE-03 D5-D12/D18）</name>
  <files>lib/kernel/engine/fvp_engine.dart, .planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md</files>
  <read_first>
    - `lib/kernel/engine/fvp_engine.dart` lines 190-194（`_openGeneration` doc — D4 薄实现注释的精确先例）、open() 236-311、software-decode fallback 278-289、_guardedAction 222-232、dispose 574-618
    - `.planning/phases/15-contract-freeze-baseline-audit/15-PATTERNS.md` §"fvp_engine.dart (contract-spec, impl 'thin' doc per D4)" — 只加机制注释，绝不加 requires/ensures 标签
    - `.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md` D4/D5/D6/D8/D9/D10/D11/D12/D18（本任务追加 lifecycle-gap 清单）
    - `.planning/phases/15-contract-freeze-baseline-audit/15-RESEARCH.md` §"9v6 + Lifecycle State Precedent" 3 项执行期陷阱（disposed 后全 getter 覆盖、error 态资源释放如实记录、mutating no-op 与 getter 安全默认独立）
    - `lib/kernel/models/player_error.dart` — 供 D18 清单引用 error-recover 语义
  </read_first>
  <action>
    对 `fvp_engine.dart` 的 open()/dispose()/software-decode fallback/_guardedAction 各处，仅追加**实现机制**薄注释（D4 风格，仿照 line 190-194 `_openGeneration` 注释），例如 open() 上方注明"实现特有：成功后 state→idle（非 playing），调用方须显式 play()；generation 守卫丢弃过期异步结果；codec 错误自动降级软解重试一次"；dispose() 上方注明"当前 baseline 用 `_disposed` bool 守卫，尚无 LifecyclePhase 枚举（Phase 20 补）；double-dispose 由 `_disposed` 幂等吸收"。**绝不**在 fvp_engine.dart 写 `requires:`/`ensures:`/`states:` 标签（那些只在接口，D4；冲突以接口为准）。
    在 `15-CONTEXT.md` 的 `<deferred>` 或 decisions 区新增一个明确的 `## P20 Lifecycle-Gap 清单（D18）` 子节，列出 Phase 20 NewFvpEngine 须补的生命周期态（D5-D12 冻结语义 vs 旧 FvpEngine baseline 未实现的差距）：
      1. `LifecyclePhase { alive, disposing, disposed }` 正交枚举（D5）— 旧 FvpEngine 只有 `_disposed` bool，须升级
      2. disposed 后全部 14 个 EngineStateView getter 返回安全默认（D9）— 旧 baseline 未逐个守卫（RESEARCH 陷阱 1）
      3. mutating 方法 disposed 后 no-op（D9；P20 STATE-03 升级为 Result.err + KernelLogger 警告）
      4. error 陷阱态不自动转出，仅 recover()/open() 出（D10）；recover() 仅 error 可达，目标 {idle, opening}（D12，目标态 P20 定）
      5. disposing 对调用者不可见（同步瞬态，D11）
      6. dispose() 任意态可达 + 终态不可逆 + double-dispose 幂等（D8）
    明确标注：这些是 **Phase 20 STATE-04 须实现的清单**，Phase 15 契约测试（Plan 03）只 baseline 捕获旧 FvpEngine 当前行为，不强测这些新语义（D16 边界）。
    9v6 裁决结论一句话冻结在子节顶部："冻结基线 = 6 态正交 MediaState + 正交 LifecyclePhase；9 态模型（PROJECT.md 陈旧）已退休（promote 非 add-alongside）。"
    CONTEXT.md 用 Edit APPEND（既有文件）。fvp_engine.dart 仅注释，绝不改逻辑。conventional-commit `docs:`。
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/engine/fvp_engine.dart && grep -q "LifecyclePhase" .planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md` contains a section naming "Lifecycle-Gap" (D18) AND names all of: `LifecyclePhase`, `disposing`, `disposed`, `recover()`, and "Phase 20" / "P20"
    - The section states the 9-vs-6 verdict: "6 态正交" + "LifecyclePhase" + a word meaning retired/promote (proves BASE-03 sc3 reconciliation frozen)
    - `fvp_engine.dart` contains NO `requires:` / `ensures:` / `states:` tag (`grep -cE "^\s*///\s*(requires|ensures|states):" lib/kernel/engine/fvp_engine.dart` == 0) — D4: contract authority only on interface
    - `fvp_engine.dart` open() region carries a thin note mentioning "idle" and "play()" (implementation side-effect note, not a contract tag)
    - `flutter analyze lib/kernel/engine/fvp_engine.dart` reports no new errors; no logic changed (git diff shows only `///` comment additions)
  </acceptance_criteria>
  <done>fvp_engine.dart carries thin D4 implementation notes only (no contract tags); 15-CONTEXT.md records the frozen 9→6 verdict + the P20 lifecycle-gap known-list (D18) so Phase 20 has an explicit implementation checklist and Plan 03 knows what NOT to test as baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interface contract ↔ implementation behavior | The frozen contract on the interface is the migration gate. If a contract silently diverges from the real FvpEngine behavior, adapters/NewFvpEngine implement a wrong spec and an unsafe state transition passes review. |

## STRIDE Threat Register (ASVS L1, block_on: standard)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03 | Tampering | frozen states: tags vs real transition table | high | mitigate | Contract drift between interface and implementation silently allows an unsafe state transition. Mitigated by D7 deliberate cross-check (each states: set hand-verified against engine_state_machine.dart _canTransitionTo) AND Plan 03 contract tests that mirror these tags against real FvpEngine (BASE-04) — the two-view + test three-view catches divergence (D19). |
| T-15-04 | Tampering | open→idle→play handoff contract | high | mitigate | The live "loads but won't play" regression proves the open-to-play path is fragile. Freezing open()'s ensures:/states: to document state=idle-then-explicit-play(), plus Plan 03's open()-success→play() structural regression gate, prevents this exact regression recurring silently after freeze. |
| T-15-05 | Denial of Service | contract closes recover() exit | medium | mitigate | If a states: tag wrongly forbids error→{idle,opening}, Phase 20's recover() becomes uncontractable, trapping the engine in error permanently. Mitigated by the explicit prohibition (must_haves.prohibitions) + acceptance criterion verifying error→{idle,opening} stays reachable. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs (doc-comment edits only). No legitimacy gate needed. |

Threat model is proportional: a contract-freeze plan's dominant threat is contract drift (T-15-03/04), mitigated by the D7 cross-check + Plan 03 test gate.
</threat_model>

<verification>
- `flutter analyze` on all 8 edited interface/impl files reports no new errors (comments-only edits)
- `contract_completeness.sh` (Plan 01 Task 2) reports every member of the 7 ISP interfaces now carries a contract tag or is covered by the group contract
- Every `states:` set cross-checked against `_canTransitionTo` (documented checked-list in SUMMARY); error→{idle,opening} reachable
- 15-CONTEXT.md carries the frozen 9→6 verdict + P20 lifecycle-gap known-list
- No method signature or logic changed anywhere (git diff = `///` additions + CONTEXT append only)
</verification>

<success_criteria>
BASE-01 satisfied: every MediaEngine/EngineStateView member has a frozen written behavioral contract (requires/ensures/states-transitions/throws/modified-ValueNotifier) on the interface `///` (D1), independently reviewable and mirrored by Plan 03 tests. BASE-03 satisfied: 9-vs-6 reconciled and frozen (6 orthogonal MediaState + orthogonal LifecyclePhase), v3.0 lifecycle states to add recorded as the P20 known-gap list. The open→idle→play handoff is frozen as a documented contract boundary.
</success_criteria>

<output>
Create `.planning/phases/15-contract-freeze-baseline-audit/15-02-SUMMARY.md` when done
</output>