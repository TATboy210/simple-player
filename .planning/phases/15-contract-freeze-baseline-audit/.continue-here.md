---
context: phase
phase: 15-contract-freeze-baseline-audit
task: discuss-area-3
total_tasks: 4-areas
status: in_progress
last_updated: 2026-07-16T14:18:57Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: 一次性 big-bang 替换内核 (v3.0 Out of Scope) — 兼容式重写若无冻结契约先导,适配层只能 signature 级猜测,被迫 big-bang cutover — 结构缓解: 构建顺序硬约束 P15 契约冻结→P16 适配层→P17+,Phase 15 先产出 frozen spec + 接口级契约测试(BASE-01/04),使 P16 适配层实现冻结契约而非猜测,P20 按 DelegationPolicy 逐能力翻转每次跑契约测试。Phase 15 契约测试即此闸门的具体执行机制。(已由 discuss-phase check_blocking_antipatterns 三答验证)

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 一次性 big-bang 替换内核 | 适配层 seam 不存在就重写状态机/引擎,使引擎替换退化为一次性全量 swap,回归风险不可控 | blocking | Phase 15 先冻结接口行为契约 + 产出接口级契约测试(BASE-01/04);P16 适配层实现冻结契约;P20 按 DelegationPolicy 逐能力翻转每次跑契约测试 |
| 静默失败 (EngineStateMachine assert-only 忽略非法转换) | engine_state_machine.dart:52-58 release 静默忽略非法转换,是项目记忆"静默失败"反模式 | advisory (owned by P20) | STATE-03 替换为 Result.err + KernelLogger 警告 — 归 Phase 20,Phase 15 契约 D9/D12 已为此预留 mutating no-op 语义 |
| 9 态 vs 6 态矛盾 (阻塞约束 #2) | PROJECT.md 仍写"9 态~40 边"(陈旧),engine_state_machine.dart 实际 6 态 | advisory (Phase 15 产物修正) | 已裁决冻结基线=6 态正交 + LifecyclePhase;Phase 15 派生任务修正 PROJECT.md 陈旧描述 |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
正在执行 `/gsd-discuss-phase 15` 默认模式工作流。已完成 4 个灰色区中的前 2 个(区域1契约文档落点 D1-D4、区域2 9v6裁决+生命周期态 D5-D12),共捕获 12 项决策,全部落盘到 `15-DISCUSS-CHECKPOINT.json`。因上下文耗尽至 96% 中断,区域3契约测试策略 + 区域4盘点工件+陈旧maps 未开始。
</current_state>

<completed_work>

已完成灰色区(2/4):

- **区域1: 契约文档落点 (BASE-01)** — 4 项决策
  - D1: 契约落点 = 接口 `///` 双语注释(DOC-01 结构)权威,不设独立 CONTRACT.md
  - D2: 扩展 DOC-01 标签集 +`requires:`/`ensures:`/`modifies:`;`states:`=允许转换,`throws:`=错误情形(可 grep,P22 lint 可校验 modifies: 存在)
  - D3: EngineStateView 12 只读 getter 按组共享轻量契约(接口顶部组契约 + 每 getter 一行中文意图指回)
  - D4: 仅接口契约权威;FvpEngine `///` 薄,记实现特有副作用(mdk 回调时序/generation 守卫),冲突以接口为准;审计核对 FvpEngine 当前行为符合接口契约即可

- **区域2: 9v6裁决+生命周期态 (BASE-03 + 阻塞约束#2)** — 8 项决策(初轮4 + 追问轮4)
  - D5: 独立 `LifecyclePhase { alive, disposing, disposed }` 枚举,与 6 态 MediaState 正交;error-恢复 = error 态内 recover() 路径;双重 dispose 由 disposed 守卫
  - D6: 冻结高层转换语义(态清单+形状+声明式进入/退出+recover() 显式);完整转换表留 Phase 20 实现时回写契约
  - D7: `states:` 标签每方法列入态/出态(`requires state ∈ {...}; transitions to {...}`);与转换表重复为故意交叉校验
  - D8: `dispose()` 任意态可达 + 终态不可逆;double-dispose 幂等 no-op;过渡 alive→disposing→disposed;调用者无需先 stop()
  - D9: disposed 后 getter 返回安全默认(state→idle,pos/dur→0,isSeeking/isBuffering→false)永不 throw;mutating no-op(P20 STATE-03 升级为 Result.err+logger 警告);UI 收最终 idle 后不再变
  - D10: error 是陷阱态,引擎进入 error 后不自动转出,仅显式 recover()/open() 可出
  - D11: disposing 对调用者不可见(同步瞬态);LifecyclePhase 对外实质二稳态 {alive, disposed};disposing 供内部/测试断言
  - D12: recover() 仅可从 error 调用,他态被拒(no-op,P20 升 Result.err);transitions to {idle, opening}(目标态 P20 定)

承袭 v2.1 已锁决策(不再问): 6 态正交 MediaState + isSeeking/isBuffering 独立标志(9 态~40 边已废)、ISP 接口分解(EngineStateView+PlaybackControl+4 能力接口)、PlayerError sealed+ValueNotifier<PlayerError?>、EngineStateMachine.transitionTo→bool(静默忽略即 #4 反模式 P20 修)、FvpEngine<350 行、双语注释结构。
</completed_work>

<remaining_work>

- **区域3: 契约测试策略 (BASE-04)** — 未开始,待讨论子决策:
  - 复用 FakeEngine vs 新 harness
  - 按方法 vs 按接口组织
  - 参数化供 Phase 21 对 NewFvpEngine 复用(VERIFY-01 前提 = 契约测试接受 MediaEngine 参数化)
- **区域4: 盘点工件+陈旧maps (BASE-02)** — 未开始,待讨论子决策:
  - 121 logger / MemoryMonitor.start/snapshot 2 处 / openGeneration 引用 盘点产物形态
  - codebase maps 是 v2.1 重构前(2026-07-12)陈旧快照:刷新还是标注其陈旧性
- write_context: 写 `15-CONTEXT.md` + `15-DISCUSSION-LOG.md`(读 templates/context.md + templates/discussion-log.md)
- git_commit: `gsd_run query commit "docs(15): capture phase context" --files CONTEXT.md DISCUSSION-LOG.md` 并 `rm -f 15-DISCUSS-CHECKPOINT.json`
- update_state: `gsd_run query state.record-session --stopped-at "Phase 15 context gathered" --resume-file CONTEXT.md` + commit STATE.md
- auto_advance: auto_advance 已开 → 自动跳转 `/gsd-plan-phase 15`
</remaining_work>

<decisions_made>

见 <completed_work> 的 D1-D12(12 项)。核心:
- 契约权威在接口 `///`(非独立 CONTRACT.md),扩展标签集 makes requires/ensures/modifies/states/throws 可 grep/lint
- lifecycle 与播放语义正交分离(独立 LifecyclePhase 枚举),保持 6 态纯净
- Phase 15 冻结高层转换语义,完整转换表留 Phase 20(尊重 P15/P20 边界)
- dispose 任意态可达+终态,error 陷阱态仅显式可出,disposed 后安全默认+no-op
</decisions_made>

<blockers>
- 上下文窗口耗尽(非技术阻塞): 区域2完成后 96%,剩余 4% 不足跑完区域3+4+写文档+提交。缓解: checkpoint 已保存 12 决策,下会话 Resume 零损失继续。
</blockers>

## Required Reading (in order)
1. `.planning/phases/15-contract-freeze-baseline-audit/15-DISCUSS-CHECKPOINT.json` — 本会话全部 12 决策 + discussion_log + prior_decisions + codebase_context + canonical_refs_seed(blocking 反模式三答在此)
2. `.planning/ROADMAP.md` Phase 15 detail — goal/success criteria/blocking constraint #2
3. `.planning/REQUIREMENTS.md` — BASE-01..04 定义 + Traceability 40/40
4. `.planning/.continue-here.md`(里程碑级,14723 字节,8 项 blocking constraints) — Phase 15+ 须复核;**不可覆盖**
5. `.planning/milestones/v3.0-phases/09-interface-decomposition/09-CONTEXT.md` — D-01..D-19(接口分解 + MediaState 正交拆分)
6. `.planning/milestones/v3.0-phases/10-state-machine-extraction/10-CONTEXT.md` — D-01..D-12(状态机提取, 6 态锁定)
7. `~/.claude/gsd-core/workflows/discuss-phase.md` + `workflows/discuss-phase/modes/default.md` — 工作流与默认模式(每区 4 单问题轮次→Next area 检查)
8. `.planning/codebase/TESTING.md` — FakeEngine/手写 fake 模式(契约测试参考,但注意 codebase maps 是 v2.1 前陈旧快照)

## Critical Anti-Patterns (do NOT repeat these)
- 一次性 big-bang 替换内核 → Phase 15 契约冻结 + 接口级契约测试先于适配层
- 9 态 vs 6 态分叉 → Phase 15 已裁决 6 态正交 + LifecyclePhase,必须修正 PROJECT.md 陈旧"9 态~40 边"
- 契约写在实现上 → 契约权威仅在接口 `///`(NewFvpEngine 须继承同一契约,否则迁移闸门失效)
- 过度工程化(#8) → 12 只读 getter 按组共享轻量契约;不设独立 CONTRACT.md 第二真相源

## Infrastructure State
- git HEAD = `ba6221b docs(15): discuss-phase checkpoint — git 收尾完成, 4 灰色区已选待讨论`
- 分支 `feat/v1.8-stability-polish-plan-02-02`, ahead 379 未 push
- 未提交文件: `.planning/STATE.md`(M)、`15-DISCUSS-CHECKPOINT.json`(M, 区域1+2 的 12 决策)
- 无运行中的服务/watcher/async-jobs
- GateGuard fact-forcing hook 会拦截本会话首个 Bash/Write,需先陈述事实

## Pre-Execution Critique Required
(不适用 — 当前在 discuss-phase 讨论+捕获决策阶段,非设计→执行间隙)

<context>
discuss-phase 默认模式工作流。区域2用户选了"继续追问"跑了第二轮(共8问)。承袭 v2.1 锁定决策与 codebase 侦察结果全部保留在 checkpoint。codebase maps 是 v2.1 前陈旧快照,Phase 15 基线盘点必须对 LIVE code 做。里程碑级 .continue-here.md 不可覆盖。下会话 Resume 后从 areas_pending[0]=契约测试策略 继续。
</context>

<next_action>
新会话执行 `/gsd-discuss-phase 15`。check_existing 步骤会检测 `15-DISCUSS-CHECKPOINT.json` 并提供 Resume 选项 → 选"恢复讨论" → 从 `areas_pending[0]=契约测试策略` 开始默认模式讨论(每区 4 单问题轮次 → Next area 检查) → 跑完区域3+4 → write_context 写 CONTEXT.md+DISCUSSION-LOG.md → git_commit 提交并 rm checkpoint → update_state → auto_advance 跳转 /gsd-plan-phase 15。
</next_action>
