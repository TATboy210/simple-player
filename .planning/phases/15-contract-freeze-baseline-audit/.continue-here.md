---
context: phase
phase: 15-contract-freeze-baseline-audit
task: execute-phase-entry
total_tasks: 3
status: ready_to_execute
last_updated: 2026-07-16T19:53:35Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: 一次性 big-bang 替换内核 (v3.0 Out of Scope) — 兼容式重写若无冻结契约先导,适配层只能 signature 级猜测,被迫 big-bang cutover — 结构缓解: 构建顺序硬约束 P15 契约冻结→P16 适配层→P17+,Phase 15 先产出 frozen spec + 接口级契约测试(BASE-01/04),使 P16 适配层实现冻结契约而非猜测,P20 按 DelegationPolicy 逐能力翻转每次跑契约测试。Phase 15 契约测试即此闸门的具体执行机制。(已由 discuss-phase check_blocking_antipatterns 三答验证; 契约测试计划 15-03 已落地此闸门)
- [x] CONSTRAINT: planner 越界 git commit 43e874d — planner 子代理两次自行提交,造成 git 历史错位 — 结构缓解: §13d 收尾时 `git reset --soft 43e874d~1` 保留工作树内容, 重做单次 `docs(15): create phase plan` 提交 (commit 2937238); 绝不用 `--hard`(会丢 planner 写盘内容)。**已于本会话执行完毕, 历史已修复 (HEAD=2937238)**
- [x] CONSTRAINT: §13d 提交必须显式排除 in-flight 文件 — 用户正并行调试"加载但不能播放"回归, 工作树含大量非本规划工作的未提交文件 — 结构缓解: §13d 只 `git add` 显式列出的 plan-phase 工件(3 计划 + 15-PATTERNS.md + STATE.md + ROADMAP.md), 绝不 `git add -A`/`git add .`。**已执行: staged 集验证 CLEAN, 无 in-flight 文件污染**
- [ ] CONSTRAINT (execute-phase): 3 计划的 `files_modified`/`do_not_touch` 边界须执行前复核 — 15-03 契约测试计划 files_modified 仅限 `test/contracts/*` + `test/fixtures/*` + `lib/kernel/engine/*` doc-comment edits; **不得碰** in-flight 的 `media_opener.dart`/`main.dart`/`player_screen.dart`/`video_surface.dart`/`playback_status_overlay.dart`(+test)/`l10n×5`/`.planning/debug/`。execute-phase 必须延续"显式 add, 绝不 add -A"纪律到每个 plan 的提交

**Do not proceed until all boxes are checked.** (前三项已闭合, 第四项为 execute-phase 前置复核)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 一次性 big-bang 替换内核 | 兼容式重写无冻结契约先导→适配层 signature 级猜测→被迫 big-bang cutover | blocking | 构建顺序硬约束 P15 契约冻结→P16+; Phase 15 产 frozen spec + 接口级契约测试(BASE-01/04); 15-03 契约测试已落地此闸门 |
| planner 越界 git commit | planner 子代理自行 git commit 越过编排者职责, 标错消息且漏文件, 致历史错位 | blocking | §13d `git reset --soft 43e874d~1` 保留工作树, 重做单次 docs 提交 (已完成 2937238); 严禁 `--hard` |
| §13d 误提交 in-flight 文件 | `git add -A`/`git add .` 会把用户并行调试的未提交文件一并纳入 Phase 15 提交 | blocking | §13d 只显式 `git add` plan-phase 工件清单, 绝不批量 add (已执行 CLEAN); execute-phase 每个计划提交延续此纪律 |
| execute-phase 越界改 in-flight 文件 | 执行 15-03 等计划时误碰用户并行调试的 media_opener/main/player_screen 等 | blocking | 执行前复核 3 计划 do_not_touch; 每计划提交只 add 该计划 files_modified 列出的文件 |
| glm-5.2 分类器间歇宕机阻塞 commit | 项目 STATE.md 已记录的 recurring blocker, git commit/fixture 生成/flutter test 可能需重试数次 | advisory | 提交重试; read-only 与 Write/Edit 不受影响; 若持续则留工作树未提交由下会话处理 |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 15 契约固化与基线盘点 的 **plan-phase 100% 完成** (§0-§13e 全过, §13d 已提交 2937238, §14 已呈现)。仅 §15 auto-advance 因上下文 76% **故意未触发**。当前状态 = **ready_to_execute**: STATE.md status=Ready-to-execute, plan_count=3, 等待新上下文窗口 `/clear → /gsd-execute-phase 15`。两个覆盖闸门本会话独立 grep 复验通过 (§13 requirements 4/4: BASE-01→15-02, BASE-02→15-01, BASE-03→15-02, BASE-04→15-03; §13a decisions 23/23: D1..D23 across 3 plans missing=empty)。git HEAD=2937238 (docs(15): create phase plan), 工作树保留用户并行调试 in-flight 文件未动。
</current_state>

<completed_work>

Completed Tasks (plan-phase, §0-§13e):
- Task 1: init.plan-phase 15 + 闭阶段门 + 先决条件验证 — Done
- Task 2: §7.8 gsd-pattern-mapper → 15-PATTERNS.md(7 ISP 接口校正 + state/value 非 throwsA) + §7.95 edge-probe → $COVERAGE — Done
- Task 3: §8 gsd-planner → 3 计划(15-01 BASE-02/Wave1, 15-02 BASE-01+03/Wave1, 15-03 BASE-04/Wave2依赖15-02) — Done
- Task 4: §10-§12 checker iter1 BLOCKER+WARNING → 修订(tiny_valid.mp4 第5fixture + EXECUTION 验收) → iter2 VERIFICATION PASSED — Done
- Task 5: §13 需求闸门 4/4 + §13a 决策闸门 23/23 + §13b state.planned-phase + §13c roadmap.annotate-dependencies + §13e gap-analysis clean — Done (本会话独立 grep 复验)
- Task 6: §13d 提交收尾 — **Done (commit 2937238)**: soft-reset 43e874d~1→ca67a93, precise add 6 工件 (15-01/02/03-PLAN.md + 15-PATTERNS.md + STATE.md + ROADMAP.md), single `docs(15): create phase plan`; planner 越界 commit 43e874d + wip 1efca91 已折叠; in-flight 文件 staged 集验证 CLEAN
- Task 7: §14 呈现 PHASE 15 PLANNED banner + wave table — Done
- Task 8: §15 auto-advance check — Skipped (上下文 76% 不足执行链; auto_advance=true 但留新窗口)
</completed_work>

<remaining_work>

execute-phase 3 计划 (新上下文窗口 `/gsd-execute-phase 15`):

- Plan 15-01 (Wave 1, BASE-02): 审计脚本 inventory.sh(可重跑, BYTE-IDENTICAL) + contract_completeness.sh(动态提取 7 接口成员签名) + tool/audit/README.md + 15-BASELINE-AUDIT.{json,md} 快照 + 7 陈旧 maps 水印 + PROJECT.md "9态~40边"→"6态正交+LifecyclePhase" 修正
- Plan 15-02 (Wave 1, BASE-01+03): 7 接口文件(engine_state_view 组契约 + playback_control + 5 控制接口) D2 标签(requires/ensures/states/modifies/throws) + open→idle→play 交接冻结 + fvp_engine.dart 薄实现注释(D4, 绝不加契约标签) + 15-CONTEXT.md 追加 P20 lifecycle-gap 清单(D18)
- Plan 15-03 (Wave 2, depends_on 15-02, BASE-04): test/contracts/ 7 契约测试组(含 VolumeControl 第7组) + 参数化 MediaEngine factory(D13) + test/engine/fvp_engine_contract_test.dart 真实 FvpEngine 挂载点 + test/fixtures/ 5 文件(4坏+1好 tiny_valid.mp4) + open→idle→play 结构回归闸门(EXECUTION 验收非 skippable)
- Wave 1 两计划(15-01/15-02)零文件重叠可并行; 15-03 依赖 15-02 契约冻结产出
</remaining_work>

<decisions_made>

- Phase 15 规划为 3 计划按 BASE 需求切分: 15-01(BASE-02)/15-02(BASE-01+03)/15-03(BASE-04, Wave2依赖15-02) — Wave1 两计划零文件重叠可并行
- load→play 回归断言纳入 must_haves.truths: open()成功→state==idle→play()→state==playing, BASE-04 契约测试作为正在发生的播放回归的结构闸门 — .planning/debug/ 两份诊断 stub(status:investigating)+未提交 media_opener.dart/main.dart 证明用户正经历"加载但不能播放"回归
- checker BLOCKER 修订: 加 test/fixtures/tiny_valid.mp4 第5 fixture(4坏+1好), happy-path 测试开真实 fixture 无默认 skip tag, 验收改 EXECUTION 检查(--plain-name 跑≥1测试 + 负 grep 无 skip tag), 回退用 --run-skipped 且失败而非静默跳过
- 7 个 ISP 契约测试组(非 D14 的 6 个): 补 VolumeControl 第7组 — 15-PATTERNS.md 发现 media_engine.dart implements 7 个 ISP 接口含 VolumeControl
- 契约测试断言 state/value(lastError.value + state==error)非 throwsA — FvpEngine 错误路径设 lastError.value + 状态转换而非抛异常
- §13a 决策闸门 skipped-pass(格式非内容) — 15-CONTEXT.md 用 markdown 标题/表格记 D1-D23 而非 <decisions> XML 块; 决策覆盖实质由 checker 独立 grep 23/23 验证
</decisions_made>

<blockers>
- glm-5.2 分类器间歇宕机阻塞 git commit(项目 STATE.md 已记录 recurring blocker): 提交可能需重试数次; 若持续则留工作树未提交由下会话处理
- planner 越界 commit 43e874d(标 fix 实为首次提交, 漏 15-02)致 git 历史错位: soft-reset 重做单次 docs 提交修复
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — 机读恢复状态, /gsd-resume-work 优先解析它
2. `.planning/phases/15-contract-freeze-baseline-audit/15-PLAN-PHASE-CHECKPOINT.md` — pre-planner spawn 的 resume 指南(含 load→play 回归判断的原始记录, §13a 闸门语义, 子代理配置)
3. `.planning/STATE.md` — phase 15 status=planning→Ready-to-execute, plan_count 3
4. `.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md` — 23 决策 D1-D23(硬约束, §13a)
5. `.planning/phases/15-contract-freeze-baseline-audit/15-PATTERNS.md` — LIVE code 校正(7 ISP 接口, state/value 非 throwsA, 无 analog 的 tool/audit+test/fixtures)
6. `.planning/phases/15-contract-freeze-baseline-audit/15-01-PLAN.md` / `15-02-PLAN.md` / `15-03-PLAN.md` — 3 个执行计划
7. `.planning/METHODOLOGY.md`(若存在) — 项目分析透镜
8. `C:\Users\35490\.claude\gsd-core\workflows\plan-phase.md` §13d/§13e/§14/§15 — 收尾步骤契约

## Critical Anti-Patterns (do NOT repeat these)
- planner 子代理自行 git commit: 编排者职责, 不可由子代理代劳 — 结构缓解: 编排者统一在 §13d 提交; 若发现子代理已越界提交, soft-reset 重做而非沿用
- `git add -A` / `git add .` 在含 in-flight 文件的工作树: 会污染 Phase 提交 — 结构缓解: 只显式 `git add` 本 Phase 工件清单
- `git reset --hard` 清理越界提交: 会丢 planner 写盘内容 — 结构缓解: 一律 `--soft`, 保留工作树
- 盲信子代理自报告: planner 称"更新 ROADMAP"须独立核验 diff — 结构缓解: git diff 核验后再接受

## Infrastructure State
- git: 分支 feat/v1.8-stability-polish-plan-02-02, ahead 380+ 未 push(amend/reset 安全可逆, reflog 可回退)
- HEAD: 43e874d (planner越界提交, 需 soft-reset); 其前 ca67a93 (docs phase-15 validation)
- glm-5.2 分类器: 间歇性宕机(项目 STATE.md 已记录 recurring blocker), 影响 git commit / Agent spawn
- 无后台进程 / 无 async-jobs / 无运行中 watcher
- Flutter SDK: D:\flutter\bin\flutter(不在系统 PATH, 须用全路径)

## Pre-Execution Critique Required
- 暂不适用: Phase 15 尚在 plan-phase, 未到 execute-phase; 但若 §15 auto-advance 自动链入 /gsd-execute-phase 15, 执行前应复核 3 计划的 do-not-touch 边界(契约测试计划 15-03 的 files_modified 仅限 test/contracts/* + test/fixtures/* + lib/kernel/engine/* doc-comment edits, 不得碰 in-flight 的 media_opener/main/player_screen/video_surface/playback_status_overlay)

<context>
Phase 15 规划实质完成, 仅差 §13d 提交收尾. 上下文 79% 耗尽, 故在提交前暂停. planner 两次越界提交(43e874d 标 fix 实为首次提交且漏 15-02)是主要技术债, 需 soft-reset 重做单次干净 docs 提交. 关键非显而易见判断: load→play 回归断言(用户正经历的"加载但不能播放")已正确纳入 15-03 契约测试的结构闸门, checker iter2 已验证其 EXECUTION 而非 skippable. in-flight 文件是用户并行调试工作, §13d 提交必须显式排除(绝不 git add -A). §13a 决策闸门 skipped-pass 是格式(markdown 非 <decisions> XML)非内容问题, 23 决策覆盖由 checker 独立 grep 验证.
</context>

<next_action>
Start with: §13d 提交收尾.

1. 核验 43e874d 仍是 HEAD 且未 push(`git log -1 --oneline` == 43e874d; `git status` 显示 ahead)
2. `git reset --soft 43e874d~1` — 保留工作树所有内容, 仅回退 commit(43e874d 的 15-01+15-03 改动回到暂存区)
3. `git reset HEAD` — 清空暂存区(让所有改动回到未暂存, 以便精确 add)
4. 精确 `git add` 仅这些 plan-phase 工件:
   - `.planning/phases/15-contract-freeze-baseline-audit/15-01-PLAN.md`
   - `.planning/phases/15-contract-freeze-baseline-audit/15-02-PLAN.md`
   - `.planning/phases/15-contract-freeze-baseline-audit/15-03-PLAN.md`
   - `.planning/phases/15-contract-freeze-baseline-audit/15-PATTERNS.md`
   - `.planning/STATE.md`
   - `.planning/ROADMAP.md`
   (可选含 `.planning/phases/15-contract-freeze-baseline-audit/15-PLAN-PHASE-CHECKPOINT.md` 作为 resume 历史; 或留作未提交)
5. **绝不** add 这些 in-flight 文件: media_opener.dart, main.dart, player_screen.dart, video_surface.dart, playback_status_overlay.dart, playback_status_overlay_test.dart, l10n×5, .planning/debug/, .planning/HANDOFF.json(将被 pause commit 含入)
6. `git commit -m "docs(15): create phase plan"` (glm-5.2 宕机则重试)
7. 继续 §13e gap-analysis(plan:post, 非阻塞) + §14 present + §15 auto-advance(auto_advance=true → 自动链 /gsd-execute-phase 15)

resume 命令: `/gsd-resume-work`(它会读 HANDOFF.json + 本 .continue-here.md)
</next_action>
