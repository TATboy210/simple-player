---
phase: 02-widget-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/shared/glass_container.dart
  - lib/ui/shared/glass_widgets.dart
  - lib/ui/shared/glass_icon_button.dart
  - lib/ui/player/control_bar.dart
  - lib/ui/player/center_controls.dart
  - lib/ui/player/volume_controls.dart
  - test/widget/shared/glass_button_test.dart
autonomous: true
requirements:
  - WIDGET-01

must_haves:
  truths:
    - "GlassButton.iconOnly renders as lightweight Material+InkWell (no BackdropFilter)"
    - "GlassButton label mode renders with GlassContainer+BackdropFilter wrapped in InkWell"
    - "All GlassIconButton references replaced with GlassButton.iconOnly"
    - "glass_widgets.dart exports GlassContainer, GlassButton, GlassChip, GlassTier"
    - "glass_icon_button.dart deleted, zero imports remain"
    - "Tapping icon-only button fires onPressed callback"
    - "Tapping label button fires onPressed callback"
  artifacts:
    - path: "lib/ui/shared/glass_container.dart"
      provides: "Merged GlassButton with icon and iconOnly constructors"
      contains: "class GlassButton extends StatelessWidget"
    - path: "lib/ui/shared/glass_widgets.dart"
      provides: "Barrel file for glass components"
      contains: "export 'glass_container.dart'"
    - path: "test/widget/shared/glass_button_test.dart"
      provides: "Widget tests for merged GlassButton"
      contains: "GlassButton"
  key_links:
    - from: "lib/ui/shared/glass_widgets.dart"
      to: "lib/ui/shared/glass_container.dart"
      via: "export statement"
      pattern: "export 'glass_container.dart'"
    - from: "lib/ui/player/control_bar.dart"
      to: "lib/ui/shared/glass_widgets.dart"
      via: "import"
      pattern: "import.*glass_widgets"
---

<objective>
Merge GlassIconButton into GlassButton as a dual-mode widget (icon-only lightweight + label with blur), create barrel file, migrate all 13 call sites, delete old file, and add widget tests.

Purpose: Single unified glass button API eliminates the split between GlassIconButton (Material+InkWell) and GlassButton (GlassContainer+BackdropFilter). The icon-only mode preserves lightweight rendering for 13 control bar buttons.
Output: Merged glass_container.dart, new glass_widgets.dart barrel, migrated call sites, deleted glass_icon_button.dart, widget tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-widget-unification/02-CONTEXT.md
@.planning/phases/02-widget-unification/02-RESEARCH.md
@.planning/phases/02-widget-unification/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor GlassButton + Create Barrel File</name>
  <files>lib/ui/shared/glass_container.dart, lib/ui/shared/glass_widgets.dart</files>
  <read_first>
    - lib/ui/shared/glass_container.dart (current GlassButton with StatefulWidget + scale animation)
    - lib/ui/shared/glass_icon_button.dart (source for icon-only InkWell pattern)
    - lib/ui/theme/tokens.dart (Tokens.bgHover, Tokens.radiusBtn, Tokens.iconLg, Tokens.iconButtonSizeLarge)
    - lib/ui/shared/glass_chip.dart (verify GlassChip export target)
  </read_first>
  <action>
Refactor GlassButton in glass_container.dart per D-01, D-06, D-07:

1. Convert GlassButton from StatefulWidget to StatelessWidget. Remove _GlassButtonState, _hovered, _pressed, _interaction fields, dispose() method.

2. Add new fields from GlassIconButton API:
   - `final void Function(TapUpDetails details)? onSecondaryTapUp` (nullable, default null)
   - `final double iconSize` (default Tokens.iconLg)
   - `final Color? color` (nullable, default null — falls back to isPrimary ? Tokens.textPrimary : Tokens.textSecondary)
   - `final Widget? child` (nullable, alternative to icon)
   Update both const constructors (GlassButton and GlassButton.iconOnly) to accept these new params.

3. Icon-only build path (label == null): Copy GlassIconButton's build pattern exactly — SizedBox(36x36) wrapping Material(color: Colors.transparent, borderRadius: Tokens.radiusBtn) + InkWell(onTap: onPressed, onSecondaryTapUp: onSecondaryTapUp, hoverColor: Tokens.bgHover, highlightColor: Colors.transparent, borderRadius: Tokens.radiusBtn, splashFactory: NoSplash.splashFactory). NO GlassContainer, NO BackdropFilter. Content is `child ?? Icon(icon, size: iconSize, color: effectiveColor)`. Wrap in Tooltip.

4. Label build path (label != null): Keep GlassContainer for blur, but replace GestureDetector+MouseRegion+AnimatedBuilder+scale with Material+InkWell wrapper. Remove all scale animation code. The InkWell wraps the GlassContainer. Both paths share InkWell behavior (hoverColor: Tokens.bgHover, splashFactory: NoSplash.splashFactory).

5. Create lib/ui/shared/glass_widgets.dart barrel file with:
   - `export 'glass_container.dart' show GlassContainer, GlassButton, GlassTier;`
   - `export 'glass_chip.dart' show GlassChip;`
   Per D-03, D-04.

6. DO NOT remove Tokens.hoverScale or Tokens.pressScale from tokens.dart — settings_card.dart uses them (per PATTERNS.md migration checklist).
  </action>
  <verify>
    <automated>dart analyze lib/ui/shared/glass_container.dart lib/ui/shared/glass_widgets.dart 2>&1 | grep -c "error" && echo "ANALYZE FAIL" || echo "ANALYZE OK"</automated>
  </verify>
  <acceptance_criteria>
    - GlassButton is StatelessWidget (no _GlassButtonState, no _hovered/_pressed ValueNotifiers)
    - GlassButton.iconOnly constructor exists with label=null
    - Icon-only path uses SizedBox(36x36) + Material + InkWell (grep for "BackdropFilter" in icon-only path returns 0 matches)
    - Label path uses GlassContainer wrapped in Material + InkWell (no GestureDetector, no AnimatedBuilder, no Matrix4.diagonal3Values)
    - glass_container.dart has no import of GestureDetector or MouseRegion for interaction (only for cursor if needed)
    - glass_widgets.dart exports GlassContainer, GlassButton, GlassTier, GlassChip
    - `dart analyze` passes with zero errors on both files
  </acceptance_criteria>
  <done>GlassButton is a dual-mode StatelessWidget with icon-only (lightweight Material+InkWell) and label (GlassContainer+InkWell) paths. Barrel file created. No scale animation remains.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Call Sites + Delete Old File + Widget Tests</name>
  <files>lib/ui/player/control_bar.dart, lib/ui/player/center_controls.dart, lib/ui/player/volume_controls.dart, lib/ui/shared/glass_icon_button.dart, test/widget/shared/glass_button_test.dart</files>
  <read_first>
    - lib/ui/player/control_bar.dart (6 GlassIconButton calls, import on line ~8)
    - lib/ui/player/center_controls.dart (6-7 GlassIconButton calls, import on line ~7)
    - lib/ui/player/volume_controls.dart (1 GlassIconButton call, import on line ~7)
    - lib/ui/shared/glass_icon_button.dart (to confirm deletion target)
    - test/widget/player/control_bar_test.dart (test pattern reference)
  </read_first>
  <action>
1. Migration — 3 files, 13 call sites total:
   - control_bar.dart: Change import from `glass_icon_button.dart` to `glass_widgets.dart`. Replace all 6 `GlassIconButton(` with `GlassButton.iconOnly(`. Parameters stay identical (icon, onPressed, tooltip, onSecondaryTapUp all supported by new API).
   - center_controls.dart: Change import. Replace all 6 `GlassIconButton(` with `GlassButton.iconOnly(`. Note: PlayPauseButton uses `iconSize: Tokens.iconXl` and `color: baseColor.withValues(alpha: ...)` — both now supported.
   - volume_controls.dart: Change import. Replace 1 `GlassIconButton(` with `GlassButton.iconOnly(`.

2. Delete lib/ui/shared/glass_icon_button.dart — the file itself.

3. Create test/widget/shared/glass_button_test.dart with widget tests per D-16:
   - Test: icon-only mode renders Icon widget (find.byType(Icon))
   - Test: label mode renders both Icon and Text (find.text)
   - Test: icon-only tap fires onPressed callback (use a counter variable)
   - Test: label mode tap fires onPressed callback
   - Test: disabled button (enabled: false) does not fire onPressed
   - Test: icon-only with onSecondaryTapUp receives TapUpDetails
   Use MaterialApp + Scaffold wrapper. Import glass_widgets.dart.
  </action>
  <verify>
    <automated>cd "D:/simple_player_flutter" && flutter test test/widget/shared/glass_button_test.dart && grep -r "glass_icon_button" lib/ | grep -vc "glass_icon_button.dart" ; echo "grep done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "glass_icon_button" lib/` returns 0 matches (file deleted, all imports replaced)
    - `grep -r "GlassIconButton" lib/` returns 0 matches (all call sites migrated)
    - All 3 migrated files import `glass_widgets.dart` (not `glass_icon_button.dart`)
    - control_bar.dart has 6 `GlassButton.iconOnly(` calls
    - center_controls.dart has 6 `GlassButton.iconOnly(` calls
    - volume_controls.dart has 1 `GlassButton.iconOnly(` call
    - glass_icon_button.dart file does not exist
    - test/widget/shared/glass_button_test.dart passes all test cases
    - `flutter test` full suite passes (no regressions from migration)
  </acceptance_criteria>
  <done>All 13 GlassIconButton call sites migrated to GlassButton.iconOnly, old file deleted, widget tests pass, full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget input → render | GlassButton receives IconData/String/Widget from caller — all compile-time safe types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | glass_icon_button.dart deletion | accept | File is replaced by merged GlassButton, no security boundary |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — Flutter SDK only |
</threat_model>

<verification>
- `dart analyze lib/ui/shared/` passes with zero errors
- `flutter test test/widget/shared/glass_button_test.dart` all tests pass
- `flutter test` full suite green (no regressions)
- `grep -r "GlassIconButton" lib/` returns 0 matches
- `grep -r "glass_icon_button" lib/` returns 0 matches
</verification>

<success_criteria>
- Single GlassButton widget with icon (label+blur) and iconOnly (lightweight) constructors
- glass_widgets.dart barrel file exports all 4 glass types
- Zero references to GlassIconButton or glass_icon_button.dart in lib/
- Widget tests cover both modes (render + tap + disabled)
</success_criteria>

<output>
Create `.planning/phases/02-widget-unification/02-01-SUMMARY.md` when done
</output>
