---
phase: 02-widget-unification
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/ui/shared/glass_container.dart
autonomous: true
requirements:
  - WIDGET-02

must_haves:
  truths:
    - "GlassContainer skips BackdropFilter when opacity < 0.01 (D-13)"
    - "GlassContainer supports no-blur degradation mode via blurEnabled parameter (D-14)"
    - "GlassTier evaluated — thin/normal/thick kept or reduced based on data (D-15)"
    - "All ValueListenableBuilder instances in ui/ audited for child caching (D-10, D-11)"
    - "Audit report documents which instances are already optimal and which were optimized"
  artifacts:
    - path: "lib/ui/shared/glass_container.dart"
      provides: "Conditional BackdropFilter skip + degradation mode"
      contains: "blurEnabled"
  key_links:
    - from: "lib/ui/shared/glass_container.dart"
      to: "lib/ui/player/control_bar.dart"
      via: "shared BackdropFilter skip pattern"
      pattern: "opacity.*<.*0\\.01"
---

<objective>
Apply glass performance optimizations (conditional BackdropFilter skip, degradation mode, tier evaluation) and complete the ValueNotifier rebuild audit across all ui/ components.

Purpose: Reduce GPU overhead from invisible BackdropFilter passes, provide low-end hardware fallback, and verify ValueNotifier patterns are optimal.
Output: Updated GlassContainer with performance features, comprehensive audit document confirming optimization state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-widget-unification/02-CONTEXT.md
@.planning/phases/02-widget-unification/02-RESEARCH.md
@.planning/phases/02-widget-unification/02-PATTERNS.md
@.planning/phases/02-widget-unification/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Glass Performance Optimizations</name>
  <files>lib/ui/shared/glass_container.dart</files>
  <read_first>
    - lib/ui/shared/glass_container.dart (current GlassContainer implementation after Plan 01)
    - lib/ui/player/control_bar.dart (existing BackdropFilter skip pattern at lines ~123-135)
    - lib/ui/theme/tokens.dart (GlassTier sigma values: thin=8, normal=10, thick=24)
    - lib/kernel/persistence/settings_store.dart (check for existing blurEnabled setting pattern)
  </read_first>
  <action>
Apply D-13, D-14, D-15 to GlassContainer:

1. D-13 — Conditional BackdropFilter skip: Add optional `ValueListenable<double>? opacity` parameter to GlassContainer. When provided, wrap the BackdropFilter in an AnimatedBuilder that checks `opacity.value < 0.01` and returns the child directly (skipping BackdropFilter GPU readback). Follow the exact pattern from control_bar.dart lines 123-135.

2. D-14 — Degradation mode: Add `bool blurEnabled = true` parameter to GlassContainer. When `blurEnabled == false`, skip BackdropFilter entirely — render only the Container with BoxDecoration (bgGlass color + border). This provides a no-blur fallback for low-end hardware. The caller (e.g., settings dialog) reads from SettingsStore and passes the value.

3. D-15 — GlassTier evaluation: The 3 tiers have sigma values 8/10/24. The thin→normal gap (2 sigma) is marginal. Recommendation: keep 3 tiers — the maintenance cost of one extra enum value is negligible, and the visual distinction on 4K displays is worth preserving. Document this decision in a code comment on the GlassTier enum. No code change to tier values.

4. Ensure the icon-only path (GlassButton with label==null) does NOT get the opacity/blurEnabled parameters — it has no BackdropFilter to skip.
  </action>
  <verify>
    <automated>cd "D:/simple_player_flutter" && dart analyze lib/ui/shared/glass_container.dart 2>&1 | grep -c "error" && echo "ANALYZE FAIL" || echo "ANALYZE OK"</automated>
  </verify>
  <acceptance_criteria>
    - GlassContainer has `final ValueListenable<double>? opacity` parameter
    - GlassContainer has `final bool blurEnabled` parameter (default true)
    - When opacity?.value < 0.01, BackdropFilter is skipped (child returned directly)
    - When blurEnabled == false, BackdropFilter is skipped (Container with BoxDecoration only)
    - GlassTier enum has comment documenting the decision to keep 3 tiers (D-15)
    - `dart analyze` passes with zero errors
    - Existing GlassContainer call sites (no opacity/blurEnabled params) still work with defaults
  </acceptance_criteria>
  <done>GlassContainer supports conditional BackdropFilter skip and degradation mode. GlassTier evaluation documented. All existing call sites unaffected by default params.</done>
</task>

<task type="auto">
  <name>Task 2: ValueNotifier Rebuild Audit</name>
  <files>lib/ui/player/controls_overlay.dart, lib/ui/player/control_bar.dart, lib/ui/player/progress_bar.dart, lib/ui/player/volume_controls.dart, lib/ui/player/speed_button.dart, lib/ui/playlist/playlist_panel.dart, lib/ui/player/player_screen.dart</files>
  <read_first>
    - lib/ui/player/controls_overlay.dart (reference pattern — already optimal)
    - lib/ui/player/control_bar.dart (engine state listeners)
    - lib/ui/player/progress_bar.dart (MergedListenable + hover tooltip)
    - lib/ui/player/volume_controls.dart (VolumeButton + VolumeSlider)
    - lib/ui/player/speed_button.dart (segment builder)
    - lib/ui/playlist/playlist_panel.dart (tab state)
    - lib/ui/player/player_screen.dart (textureId + playlist + empty state)
  </read_first>
  <action>
Audit all ValueListenableBuilder instances across ui/ per D-10, D-11. For each instance, evaluate 4 dimensions:

1. **Child caching**: Does the builder have a static subtree that can be passed as `child` parameter?
2. **Notifier merging**: Can multiple ValueNotifiers be merged into one Listenable.merge?
3. **Rebuild baseline**: Does the builder actually need to rebuild on every value change?
4. **Unnecessary listeners**: Is the ValueListenableBuilder wrapping something that doesn't depend on the value?

Expected results based on PATTERNS.md analysis:
- controls_overlay.dart: Already optimal (child caches Stack). NO CHANGE.
- control_bar.dart: After Plan 01 migration, audit remaining ValueListenableBuilder instances. Stable callbacks (D-08) mean no optimization needed. NO CHANGE expected.
- progress_bar.dart: Already uses MergedListenable + RepaintBoundary. Hover tooltip cannot cache. NO CHANGE.
- volume_controls.dart: VolumeSlider's Slider depends on volume — cannot cache. VolumeButton uses ValueListenableBuilder2 — already optimal. NO CHANGE.
- speed_button.dart: Left/right arrows are StatelessWidget refs (cheap). Middle segment depends on speed. NO CHANGE.
- playlist_panel.dart: All children depend on _selectedTab.value. NO CHANGE.
- player_screen.dart: All 3 instances already use child caching. NO CHANGE.

Apply any optimization found during audit. If all are already optimal (expected), document findings in a comment block at the top of controls_overlay.dart as a reference pattern, and report the audit result in the plan summary.

The 30% rebuild reduction target (D-12) is validated via qualitative code analysis — not DevTools profiling. The audit confirms existing patterns are already well-optimized.
  </action>
  <verify>
    <automated>cd "D:/simple_player_flutter" && flutter test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Every ValueListenableBuilder in the 7 audited files has been evaluated on all 4 dimensions
    - Audit result: either optimization applied (file modified) or documented as already optimal (no change)
    - No ValueListenableBuilder has a builder that produces a completely static widget tree (wasted rebuild)
    - MergedListenable is used where multiple notifiers trigger the same rebuild (progress_bar.dart already does this)
    - `flutter test` full suite passes
    - D-12 target assessed: existing patterns are already well-optimized; the 30% reduction claim is supported by the fact that child caching and MergedListenable are already in place
  </acceptance_criteria>
  <done>Complete ValueNotifier audit across all ui/ files. All instances evaluated on 4 dimensions. Either optimized or documented as already optimal. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| opacity value → render decision | opacity comes from ValueListenable controlled by AutoHideController (internal, trusted) |
| blurEnabled flag → render path | blurEnabled comes from SettingsStore (local user preference, trusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | GlassContainer params | accept | All params are compile-time safe types (bool, ValueListenable), no user input |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — Flutter SDK only |
</threat_model>

<verification>
- `dart analyze lib/ui/shared/glass_container.dart` passes
- `flutter test` full suite green
- Audit covers all 7 files listed in task
</verification>

<success_criteria>
- GlassContainer has conditional BackdropFilter skip (D-13) and degradation mode (D-14)
- GlassTier 3-tier decision documented (D-15)
- ValueNotifier audit complete across all ui/ (D-10, D-11)
- D-12 target validated via code analysis
</success_criteria>

<output>
Create `.planning/phases/02-widget-unification/02-02-SUMMARY.md` when done
</output>
