---
phase: 14-testability-data-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - test/widget/player/controls_overlay_test.dart
  - test/widget/player/volume_controls_test.dart
  - test/widget/player/speed_button_test.dart
  - test/widget/player/progress_bar_test.dart
  - test/integration/error_propagation_test.dart
autonomous: true
requirements:
  - TEST-02
  - TEST-04
  - FLOW-01
  - FLOW-02
  - FLOW-03

must_haves:
  truths:
    - "Every PlayerScreen sub-widget can be tested independently with FakeEngine"
    - "Error propagation path is verified: engine.lastError → ErrorBanner display → dismiss → clear"
    - "Widget→Kernel command flow is unidirectional (no reverse callbacks from engine to widget)"
    - "Kernel→Widget state flow is unidirectional (ValueNotifier → ValueListenableBuilder only)"
  artifacts:
    - "test/integration/error_propagation_test.dart — full error path integration test"
    - "Enhanced widget tests with interaction coverage"
  key_links:
    - "FakeEngine.lastError → ErrorBanner widget rebuild"
    - "FakeEngine state → ControlsOverlay visibility"
---

<objective>
Fill widget test coverage gaps and verify data flow unidirectionality. Write integration test for error propagation path (FvpEngine → PlaybackController → ErrorBanner).

Purpose: Phase 14 core goal — every sub-widget independently testable, error path verified, data flows documented as unidirectional.
Output: Enhanced test suite with interaction coverage + error propagation integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-testability-data-flow/RESEARCH.md

test/helpers/fake_engine.dart
test/helpers/fake_window_service.dart
test/widget/player/controls_overlay_test.dart
test/widget/player/volume_controls_test.dart
test/widget/player/speed_button_test.dart
test/widget/player/progress_bar_test.dart
test/widget/player/error_banner_test.dart
test/widget/player/player_screen_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance widget interaction tests for sub-widgets</name>
  <files>
    test/widget/player/controls_overlay_test.dart
    test/widget/player/volume_controls_test.dart
    test/widget/player/speed_button_test.dart
    test/widget/player/progress_bar_test.dart
  </files>
  <action>
Enhance existing widget tests with interaction-level coverage using FakeEngine:

**controls_overlay_test.dart** — Add:
- Auto-hide timer test: controls visible after mouse move → wait → controls hidden
- State-driven visibility: engine.state=playing → controls auto-hide; engine.state=paused → controls stay visible
- Use `tester.pump(Duration)` to advance timers

**volume_controls_test.dart** — Add:
- VolumeSlider drag interaction: drag slider → engine.setVolume called with correct value
- Mute toggle: tap mute button → engine.setMute called
- Volume display syncs with engine.volume ValueNotifier

**speed_button_test.dart** — Add:
- SpeedButton popup opens on tap
- Speed option selection → engine.setPlaybackRate called with correct rate
- Popup closes after selection

**progress_bar_test.dart** — Add:
- Seek interaction: tap on progress bar → engine.seekTo called with correct position
- Duration display syncs with engine.duration
- Position display syncs with engine.position

All tests use FakeEngine (no FFI, no platform plugins). Follow existing test patterns in player_screen_test.dart.
  </action>
  <verify>
    <automated>flutter test test/widget/player/controls_overlay_test.dart test/widget/player/volume_controls_test.dart test/widget/player/speed_button_test.dart test/widget/player/progress_bar_test.dart</automated>
  </verify>
  <done>All enhanced widget tests pass. Each sub-widget has at least one interaction-level test proving it can be independently tested with FakeEngine.</done>
</task>

<task type="auto">
  <name>Task 2: Write error propagation integration test</name>
  <files>test/integration/error_propagation_test.dart</files>
  <action>
Create a new integration test file verifying the full error propagation path:

**Test: engine error → ErrorBanner display**
1. Create FakeEngine + PlaybackController + PlayerScreen (same pattern as player_screen_test.dart)
2. Call `engine.simulateError('test error message')`
3. Pump widget
4. Assert: ErrorBanner widget is visible with error text

**Test: error dismiss → state clear**
1. Trigger error via `engine.simulateError('dismiss test')`
2. Pump widget
3. Tap dismiss button on ErrorBanner
4. Assert: `engine.lastError.value` is null, ErrorBanner is hidden

**Test: error → recovery → new playback**
1. Trigger error
2. Open new file via `engine.open('new_video.mp4')`
3. Assert: error cleared, new file playing, ErrorBanner hidden

**Test: unidirectional data flow verification**
1. Verify Widget→Kernel: tap play button → engine.play() called (no engine→widget callback)
2. Verify Kernel→Widget: engine.state changes → widget rebuilds via ValueListenableBuilder
3. Verify no bidirectional coupling: engine does not reference any widget/callback

Follow FakeEngine patterns from test/helpers/fake_engine.dart. Use PlayerScreen test shell from player_screen_test.dart.
  </action>
  <verify>
    <automated>flutter test test/integration/error_propagation_test.dart</automated>
  </verify>
  <done>Error propagation integration test passes. Full path verified: engine.error → ErrorBanner → dismiss → clear. Unidirectional flows confirmed.</done>
</task>

</tasks>

<verification>
```bash
# All new tests pass
flutter test test/widget/player/controls_overlay_test.dart test/widget/player/volume_controls_test.dart test/widget/player/speed_button_test.dart test/widget/player/progress_bar_test.dart test/integration/error_propagation_test.dart

# Full suite still passes
flutter test 2>&1 | tail -3
```
</verification>

<success_criteria>
1. Every PlayerScreen sub-widget has at least one interaction-level test with FakeEngine
2. Error propagation integration test verifies full path: engine → controller → ErrorBanner → dismiss → clear
3. Data flow unidirectionality confirmed by test assertions
4. No regressions — all existing tests still pass
5. Test count increases by ~15-20 new test cases
</success_criteria>

<output>
Create `.planning/phases/14-testability-data-flow/14-02-SUMMARY.md` when done
</output>
