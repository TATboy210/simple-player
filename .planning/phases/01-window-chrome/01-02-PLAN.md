---
phase: 01-window-chrome
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/kernel/ui/window/custom_title_bar.dart
autonomous: true
requirements:
  - WB-01
  - WB-02
  - WB-03
  - WB-04
  - WC-01
  - WC-02
  - WC-03
  - WC-04
  - WC-05
  - PQ-05
  - PQ-06
  - PQ-07

must_haves:
  truths:
    - "Title bar renders at 36px height with glass-morphism BackdropFilter blur"
    - "Title bar shows app icon (Icons.play_circle_filled), file name, and 4 window controls"
    - "Drag on title bar calls PlatformService.I.startDragging()"
    - "Double-tap on title bar calls PlatformService.I.toggleMaximize()"
    - "Pin button toggles alwaysOnTop, icon color reflects pinned state (accent when active)"
    - "Maximize button icon toggles between crop_square and filter_none based on isMaximized"
    - "Close button hover shows danger red background"
    - "During isResizing=true, BackdropFilter is skipped (plain bgGlass fallback)"
  artifacts:
    - path: "lib/kernel/ui/window/custom_title_bar.dart"
      provides: "CustomTitleBar, TitleBarControls, _TitleBarButton widgets"
      min_lines: 150
      exports: ["CustomTitleBar", "TitleBarControls"]
  key_links:
    - from: "lib/kernel/ui/window/custom_title_bar.dart"
      to: "PlatformService.I"
      via: "ValueListenableBuilder on isAlwaysOnTop, isMaximized, isResizing"
      pattern: "PlatformService\\.I\\.(isAlwaysOnTop|isMaximized|isResizing)"
    - from: "lib/kernel/ui/window/custom_title_bar.dart"
      to: "lib/kernel/ui/theme/tokens.dart"
      via: "All sizes/colors from Tokens.* constants"
      pattern: "Tokens\\.(titleBarHeight|bgGlass|accent|textSecondary|textPrimary|bgHover|danger|glassBlurThin|iconSm|iconMd|titleBarButtonWidth|spSm|spMd|fontCaption)"
    - from: "lib/kernel/ui/window/custom_title_bar.dart"
      to: "lib/l10n/app_en.arb"
      via: "AppLocalizations.of(context) for tooltip strings"
      pattern: "l10n\\.(pin|unpin|minimize|maximize|restore|close)"
---

<objective>
Create the CustomTitleBar widget with glass-morphism, window controls, drag-to-move, and double-tap maximize.

Purpose: This is the core visual component of Phase 1. It renders the 36px title bar with BackdropFilter blur, four window control buttons that reflect live state via ValueNotifier, and gesture handling for drag and double-tap.

Output: Single file `lib/kernel/ui/window/custom_title_bar.dart` containing 3 widgets (CustomTitleBar, TitleBarControls, _TitleBarButton).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/kernel/ui/theme/tokens.dart
@lib/kernel/services/platform_service.dart
@lib/l10n/app_en.arb

<interfaces>
<!-- PlatformService abstract interface — from lib/kernel/services/platform_service.dart -->
```dart
abstract class PlatformService {
  static PlatformService get I { ... }
  static void init(PlatformService impl) => _instance = impl;
  static void reset() => _instance = null;

  Future<void> minimize();
  Future<void> toggleMaximize();
  Future<void> close();
  Future<void> startDragging();
  Future<void> toggleFullscreen();
  Future<void> exitFullscreen();
  Future<void> toggleAlwaysOnTop();

  ValueNotifier<WindowMode> get mode;
  ValueNotifier<bool> get isAlwaysOnTop;
  ValueNotifier<bool> get isMaximized;
  ValueNotifier<bool> get isResizing;

  Future<void> initService();
  Future<void> dispose();
}
```

<!-- Tokens (after Plan 01) — all 49 tokens available -->
Key tokens for title bar:
- Tokens.titleBarHeight (36.0), Tokens.titleBarButtonWidth (46.0)
- Tokens.bgGlass (0x801A1A24), Tokens.bgHover (0xFF2A2A3A), Tokens.danger (0xFFFF6B6B)
- Tokens.accent (0xFF6C5CE7), Tokens.textPrimary (0xFFE8E8F0), Tokens.textSecondary (0xFF9999AA)
- Tokens.glassBlurThin (12.0)
- Tokens.iconSm (16.0), Tokens.iconMd (20.0)
- Tokens.spSm (8.0), Tokens.spMd (12.0)
- Tokens.fontCaption (12.0)

<!-- AppLocalizations keys (verified in app_en.arb and app_zh.arb) -->
- l10n.pin, l10n.unpin, l10n.minimize, l10n.maximize, l10n.restore, l10n.close

<!-- Reference implementation at D:\player_flutter\lib\kernel\ui\window\custom_title_bar.dart (189 lines) -->
The reference is proven and should be ported with minimal changes. The only adjustments:
- Import paths updated for this project's structure
- No other modifications needed (token names, l10n keys, and PlatformService API match exactly)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CustomTitleBar widget with glass-morphism, controls, and gestures</name>
  <files>lib/kernel/ui/window/custom_title_bar.dart</files>
  <behavior>
    - Widget renders Container with height 36.0 and bgGlass background
    - Row contains: 12px spacer, app icon (Icons.play_circle_filled, iconMd, accent color), 8px spacer, file name text, Spacer, TitleBarControls
    - Outer GestureDetector has onPanStart calling wm.startDragging() and onDoubleTap calling wm.toggleMaximize()
    - When isResizing is true, BackdropFilter is skipped (child rendered directly)
    - When isResizing is false, ClipRect > BackdropFilter(blur 12px) wraps the content
    - File name display: empty string or null shows "Simple Player", non-empty shows "{name} — Simple Player"
    - Pin button: Icons.push_pin, accent color when isAlwaysOnTop is true
    - Minimize button: Icons.minimize, always textSecondary
    - Maximize button: Icons.crop_square when not maximized, Icons.filter_none when maximized
    - Close button: Icons.close, isClose=true, hover shows danger red background
    - All buttons: 46x36, hover shows bgHover background (except close = danger), icon color textPrimary on hover
    - GestureDetector behavior: HitTestBehavior.translucent on outer, buttons intercept taps
  </behavior>
  <action>
    Create `lib/kernel/ui/window/custom_title_bar.dart`. Port directly from the reference implementation at `D:\player_flutter\lib\kernel\ui\window\custom_title_bar.dart` (189 lines) with these import adjustments:

    ```dart
    import 'dart:ui' as ui;
    import 'package:flutter/material.dart';
    import '../../l10n/app_localizations.dart';
    import '../../services/platform_service.dart';
    import '../theme/tokens.dart';
    ```

    The file contains 3 widgets:

    **1. CustomTitleBar (StatelessWidget)** -- lines 13-87 of reference
    - Constructor: `const CustomTitleBar({super.key, this.onOpenFile, this.fileName})`
    - Fields: `final VoidCallback? onOpenFile` and `final ValueNotifier<String>? fileName`
    - Build method:
      - Gets `PlatformService.I` as `wm`
      - Builds `content` Container (height: Tokens.titleBarHeight, color: Tokens.bgGlass)
      - Row: [SizedBox(width:12)] [Icon(Icons.play_circle_filled, size:Tokens.iconMd, color:Tokens.accent)] [SizedBox(width:8)] [fileName VLB or static "Simple Player" Text] [Spacer] [TitleBarControls]
      - File name: `ValueListenableBuilder<String>` on `fileName!`, Expanded + Text with `overflow: TextOverflow.ellipsis, maxLines: 1`, style: `TextStyle(color: Tokens.textSecondary, fontSize: Tokens.fontCaption, fontWeight: FontWeight.w500)`
      - Name display logic: `name.isEmpty ? 'Simple Player' : '$name — Simple Player'`
      - Wraps in `GestureDetector(behavior: HitTestBehavior.translucent, onPanStart: (_) => wm.startDragging(), onDoubleTap: () => wm.toggleMaximize(), child: ValueListenableBuilder<bool>(valueListenable: wm.isResizing, ...))`
      - isResizing builder: `resizing ? child! : ClipRect(child: BackdropFilter(filter: ui.ImageFilter.blur(sigmaX: Tokens.glassBlurThin, sigmaY: Tokens.glassBlurThin), child: child))`

    **2. TitleBarControls (StatelessWidget)** -- lines 90-135 of reference
    - Build: gets `AppLocalizations.of(context)` as `l10n`, `PlatformService.I` as `wm`
    - Row(mainAxisSize: MainAxisSize.min) with 4 children:
      - VLB on `wm.isAlwaysOnTop` -> `_TitleBarButton(icon: Icons.push_pin, isActive: pinned, tooltip: pinned ? l10n.unpin : l10n.pin, onPressed: wm.toggleAlwaysOnTop)`
      - `_TitleBarButton(icon: Icons.minimize, tooltip: l10n.minimize, onPressed: wm.minimize)`
      - VLB on `wm.isMaximized` -> `_TitleBarButton(icon: maximized ? Icons.filter_none : Icons.crop_square, tooltip: maximized ? l10n.restore : l10n.maximize, onPressed: wm.toggleMaximize)`
      - `_TitleBarButton(icon: Icons.close, tooltip: l10n.close, isClose: true, onPressed: wm.close)`

    **3. _TitleBarButton (StatefulWidget)** -- lines 138-188 of reference
    - Constructor params: `icon` (IconData), `tooltip` (String), `onPressed` (VoidCallback), `isActive` (bool, default false), `isClose` (bool, default false)
    - State: `bool _hovered = false`
    - Build:
      - `bgColor = _hovered ? (widget.isClose ? Tokens.danger : Tokens.bgHover) : Colors.transparent`
      - `iconColor = _hovered ? Tokens.textPrimary : (widget.isActive ? Tokens.accent : Tokens.textSecondary)`
      - Returns `Tooltip(message: widget.tooltip, waitDuration: Duration(milliseconds: 400), child: MouseRegion(cursor: SystemMouseCursors.click, onEnter/onExit toggling _hovered, child: GestureDetector(onTap: widget.onPressed, child: Container(width: Tokens.titleBarButtonWidth, height: Tokens.titleBarHeight, color: bgColor, child: Icon(widget.icon, size: Tokens.iconSm, color: iconColor)))))`

    IMPORTANT:
    - Per PQ-07: ALL sizes, colors, durations from Tokens.* constants. Zero hardcoded values.
    - Per PQ-05: No new ValueNotifiers created in this widget. Notifiers owned by PlatformService.
    - Per PQ-06: No FFI calls in this widget. All window operations delegated to PlatformService.I methods (which are already wrapped in try-catch by WindowManagerService).
    - Use `debugPrint` not `print` for any logging.
    - `on Exception catch (e)` not bare `catch (e)`.
    - Trailing commas on multi-line argument lists.
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && dart analyze lib/kernel/ui/window/custom_title_bar.dart</automated>
  </verify>
  <done>CustomTitleBar widget file exists with 3 widget classes. dart analyze passes zero warnings. All tokens from Tokens.*. All l10n from AppLocalizations. No hardcoded values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> PlatformService | Widget calls PlatformService.I methods which delegate to FFI. All FFI calls already wrapped in try-catch in WindowManagerService. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | PlatformService method calls | accept | Abstract interface with try-catch in WindowManagerService. Widget has no direct FFI access. No user input processed. |
</threat_model>

<verification>
- `dart analyze lib/kernel/ui/window/custom_title_bar.dart` zero warnings
- File contains 3 classes: CustomTitleBar, TitleBarControls, _TitleBarButton
- All 4 window control buttons present with correct icons
- Glass-morphism stack: GestureDetector > ValueListenableBuilder(isResizing) > ClipRect > BackdropFilter
- All values from Tokens.* constants (PQ-07)
</verification>

<success_criteria>
- `lib/kernel/ui/window/custom_title_bar.dart` exists with ~150-190 lines
- Exports `CustomTitleBar` and `TitleBarControls` as public classes
- dart analyze zero warnings
- Widget tree matches UI-SPEC layout contract exactly
</success_criteria>

<output>
After completion, create `.planning/phases/01-window-chrome/01-02-SUMMARY.md`
</output>
