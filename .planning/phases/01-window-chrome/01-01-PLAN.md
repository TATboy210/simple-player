---
phase: 01-window-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/ui/theme/tokens.dart
  - test/helpers/fake_platform_service.dart
autonomous: true
requirements:
  - PQ-07

must_haves:
  truths:
    - "Tokens class exposes titleBarHeight (36.0), titleBarButtonWidth (46.0), glassBlurThin (12.0)"
    - "Tokens class exposes durationFast (80), durationNormal (150), durationDebounce (500)"
    - "Tokens class exposes glassBlur (16.0), glassBlurThick (24.0), iconLg (20.0)"
    - "FakePlatformService is importable from test/helpers/ and tracks all call counts"
  artifacts:
    - path: "lib/kernel/ui/theme/tokens.dart"
      provides: "9 new design tokens for title bar"
      contains: "titleBarHeight"
    - path: "test/helpers/fake_platform_service.dart"
      provides: "Reusable FakePlatformService for widget tests"
      exports: ["FakePlatformService"]
  key_links:
    - from: "lib/kernel/ui/theme/tokens.dart"
      to: "lib/kernel/ui/window/custom_title_bar.dart"
      via: "CustomTitleBar imports Tokens for all sizing/colors"
      pattern: "Tokens\\.titleBarHeight|Tokens\\.glassBlurThin"
---

<objective>
Add 9 design tokens to the Tokens class and extract FakePlatformService to a shared test helper.

Purpose: The CustomTitleBar widget (Plan 02) needs these tokens for all dimensions, colors, and durations. FakePlatformService is currently inlined in `test/unit/platform_service_test.dart` -- extracting it to `test/helpers/` enables reuse by widget tests in Plan 03.

Output: Updated tokens.dart with 49 total tokens, new shared FakePlatformService helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/kernel/ui/theme/tokens.dart
@test/unit/platform_service_test.dart

<interfaces>
<!-- Tokens class current state (40 tokens) — from lib/kernel/ui/theme/tokens.dart -->
```dart
class Tokens {
  Tokens._();
  static const bgBase = Color(0xFF0A0A0F);
  static const bgPanel = Color(0xFF1A1A24);
  static const bgElevated = Color(0xFF242432);
  static const bgHover = Color(0xFF2A2A3A);
  static const bgGlass = Color(0x801A1A24);
  static const accent = Color(0xFF6C5CE7);
  static const accentLight = Color(0xFFA29BFE);
  static const danger = Color(0xFFFF6B6B);
  static const textPrimary = Color(0xFFE8E8F0);
  static const textSecondary = Color(0xFF9999AA);
  static const textTertiary = Color(0xFF666677);
  static const borderHighlight = Color(0x33FFFFFF);
  static const fontTitle = 18.0;
  static const fontBody = 14.0;
  static const fontCaption = 12.0;
  static const fontOverline = 10.0;
  static const iconSm = 16.0;
  static const iconMd = 20.0;
  static const spXs = 4.0;
  static const spSm = 8.0;
  static const spMd = 12.0;
  static const spLg = 16.0;
  static const spXl = 24.0;
  static const radiusSm = 6.0;
  static const radiusMd = 10.0;
  static const radiusBtn = 8.0;
}
```

<!-- FakePlatformService — from test/unit/platform_service_test.dart:6-42 -->
```dart
class FakePlatformService implements PlatformService {
  @override
  final mode = ValueNotifier<WindowMode>(WindowMode.windowed);
  @override
  final isAlwaysOnTop = ValueNotifier<bool>(false);
  @override
  final isMaximized = ValueNotifier<bool>(false);
  @override
  final isResizing = ValueNotifier<bool>(false);

  int minimizeCalls = 0;
  int toggleMaximizeCalls = 0;
  int closeCalls = 0;
  int startDraggingCalls = 0;
  int toggleFullscreenCalls = 0;
  int exitFullscreenCalls = 0;
  int toggleAlwaysOnTopCalls = 0;

  @override
  Future<void> minimize() async => minimizeCalls++;
  @override
  Future<void> toggleMaximize() async => toggleMaximizeCalls++;
  @override
  Future<void> close() async => closeCalls++;
  @override
  Future<void> startDragging() async => startDraggingCalls++;
  @override
  Future<void> toggleFullscreen() async => toggleFullscreenCalls++;
  @override
  Future<void> exitFullscreen() async => exitFullscreenCalls++;
  @override
  Future<void> toggleAlwaysOnTop() async => toggleAlwaysOnTopCalls++;
  @override
  Future<void> initService() async {}
  @override
  Future<void> dispose() async {}
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 9 design tokens to Tokens class</name>
  <files>lib/kernel/ui/theme/tokens.dart</files>
  <behavior>
    - Tokens.titleBarHeight equals 36.0
    - Tokens.titleBarButtonWidth equals 46.0
    - Tokens.glassBlurThin equals 12.0
    - Tokens.glassBlur equals 16.0
    - Tokens.glassBlurThick equals 24.0
    - Tokens.durationFast equals 80
    - Tokens.durationNormal equals 150
    - Tokens.durationDebounce equals 500
    - Tokens.iconLg equals 20.0
  </behavior>
  <action>
    Add the following 9 `static const` fields to the `Tokens` class in `lib/kernel/ui/theme/tokens.dart`. Insert them after the existing fields, grouped by category with a comment separator. All values are per UI-SPEC.md (LOCKED):

    ```dart
    // ── 标题栏 ──
    static const titleBarHeight = 36.0;
    static const titleBarButtonWidth = 46.0;

    // ── 毛玻璃 ──
    static const glassBlurThin = 12.0;
    static const glassBlur = 16.0;
    static const glassBlurThick = 24.0;

    // ── 动画 ──
    static const durationFast = 80;
    static const durationNormal = 150;
    static const durationDebounce = 500;

    // ── 图标 ──
    static const iconLg = 20.0;
    ```

    Note: `iconLg` (20.0) equals existing `iconMd` (20.0). This is intentional -- the UI-SPEC defines `iconLg` for the title bar app icon. Both tokens exist for semantic clarity (iconMd = general medium, iconLg = explicitly large). The title bar app icon should use `iconMd` per the existing codebase convention (RESEARCH.md confirms this).

    Do NOT modify any existing tokens. Only add new ones.
    Per PQ-07: all values are compile-time constants from this file, nothing hardcoded in widget code.
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && dart analyze lib/kernel/ui/theme/tokens.dart</automated>
  </verify>
  <done>Tokens class has 49 static const fields. dart analyze passes with zero warnings. All 9 new tokens are present with exact values from UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Extract FakePlatformService to shared test helper</name>
  <files>test/helpers/fake_platform_service.dart</files>
  <action>
    Create `test/helpers/fake_platform_service.dart` by extracting the `FakePlatformService` class from `test/unit/platform_service_test.dart` (lines 6-42).

    1. Create the new file with proper imports:
       ```dart
       import 'package:flutter/foundation.dart';
       import 'package:simple_player_flutter/kernel/services/platform_service.dart';
       ```

    2. Copy the entire `FakePlatformService` class (lines 6-42 of platform_service_test.dart) into the new file. This includes:
       - 4 ValueNotifiers: `mode`, `isAlwaysOnTop`, `isMaximized`, `isResizing`
       - 7 call counters: `minimizeCalls`, `toggleMaximizeCalls`, `closeCalls`, `startDraggingCalls`, `toggleFullscreenCalls`, `exitFullscreenCalls`, `toggleAlwaysOnTopCalls`
       - All method overrides

    3. Update `test/unit/platform_service_test.dart` to import from the shared helper:
       - Replace the inline `FakePlatformService` class with: `import '../helpers/fake_platform_service.dart';`
       - Keep all existing tests unchanged

    4. Run the existing platform_service_test.dart to verify no regressions.
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter test test/unit/platform_service_test.dart</automated>
  </verify>
  <done>FakePlatformService lives in test/helpers/fake_platform_service.dart and is importable. Original platform_service_test.dart tests pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Plan 01 creates compile-time constants and test helpers only. No runtime trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | Pure constants and test fakes have no attack surface |
</threat_model>

<verification>
- `dart analyze lib/kernel/ui/theme/tokens.dart` passes with zero warnings
- `flutter test test/unit/platform_service_test.dart` passes (FakePlatformService extraction is non-breaking)
- All 9 tokens present in tokens.dart with correct types (double for layout/glass/icon, int for durations)
</verification>

<success_criteria>
- Tokens class has 49 fields (40 existing + 9 new)
- FakePlatformService importable from `test/helpers/fake_platform_service.dart`
- All existing tests pass unchanged
- `flutter analyze` zero warnings on modified file
</success_criteria>

<output>
After completion, create `.planning/phases/01-window-chrome/01-01-SUMMARY.md`
</output>
