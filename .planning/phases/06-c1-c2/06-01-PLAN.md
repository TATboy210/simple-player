---
phase: 06-c1-c2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/diagnostics/dwm_capabilities_probe.dart
  - lib/kernel/diagnostics/dwm_capabilities.dart
  - lib/kernel/window_bridge/window_service_state.dart
  - test/kernel/diagnostics/dwm_capabilities_test.dart
autonomous: true
requirements: [ENAB-01]
estimate:
  tokens: 52000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "DwmCapabilities probe produces a DwmCapabilitySnapshot with buildNumber (int) and four attribute-availability booleans (supportsBorderColor, supportsCornerPreference, supportsCaptionColor, supportsTextColor)"
    - "FLAGGED ASSUMPTION (ENAB-01 boundary, specless probe unresolved): Build-number threshold pinned by named constant kWin11BuildFloor = 22000 — build 21999 = Win10 (all booleans false), build 22000 = Win11 (booleans reflect attribute probes). One step either side is the test boundary; the constant is the single source of truth for the threshold."
    - "FLAGGED ASSUMPTION (ENAB-01 precision, specless probe unresolved): Build-number comparison buildNumber >= kWin11BuildFloor is integer-exact via Uint32 (RtlGetVersion dwBuildNumber is a ULONG). No floating point, no overflow at realistic build numbers (< 2^32). The comparison contract is integer greater-than-or-equal, not half-up or ceil/floor."
    - "On non-Windows platforms the facade returns a LinuxCompositorCapabilities placeholder (Phase 11 peer slot per D-01 — Linux probe NOT implemented this phase, structure stub only with /// doc comment marking it Phase 11)"
    - "DWM attribute calls are gated by the snapshot: Win10 (build < 22000) never receives DWMWA_BORDER_COLOR / DWMWA_WINDOW_CORNER_PREFERENCE / DWMWA_CAPTION_COLOR / DWMWA_TEXT_COLOR calls from any consumer reading this snapshot"
    - "Every DwmGetWindowAttribute HRESULT != S_OK is logged via KernelLogger.I.e with '[DwmCapabilities]' prefix and context map (per-call, per D-04); first-of-kind per generic message reported via ErrorReporterImpl.I.reportPlatformSafely (D-04 10s dedup reuse — no parallel dedup)"
    - "The snapshot is exposed as ValueNotifier<DwmCapabilitySnapshot?> on WindowServiceState (identity-preserved — the getter forwards the SAME notifier instance, no wrapping)"
  artifacts:
    - lib/kernel/diagnostics/dwm_capabilities_probe.dart
    - lib/kernel/diagnostics/dwm_capabilities.dart
    - lib/kernel/window_bridge/window_service_state.dart (modified — new dwmCapabilities ValueNotifier field)
    - test/kernel/diagnostics/dwm_capabilities_test.dart
  key_links:
    - "FFI leaf (dwm_capabilities_probe.dart) → facade (dwm_capabilities.dart) → WindowServiceState.dwmCapabilities ValueNotifier (window_service_state.dart)"
    - "Facade HRESULT-failure path → KernelLogger.I.e (every call) + ErrorReporterImpl.I.reportPlatformSafely (first-of-kind, 10s dedup)"
  prohibitions:
    - "Must NOT emit Win11 22000+ DWM attribute calls on Win10 — probe gates all calls by build number (E_INVALIDARG prevention, ENAB-01)"
    - "Must NOT use DWMWA_NCRENDERING_POLICY / global DWMNCRP — withdrawn 2026-08-27, user red line '勿重提' (STACK.md)"
    - "Must NOT build a parallel dedup for D-04 — reuse ErrorReporterImpl.I.reportPlatformSafely 10s window (D-04 locked decision)"
    - "Must NOT wrap DwmCapabilitySnapshot ValueNotifier in a new notifier — identity-preservation constraint (Phase 7/8 addListener breaks on wrapping)"
    - "Must NOT use debugPrint in lib/kernel/ — KernelLogger.I exclusively (kernel_logger_gate.sh enforces)"
---

<objective>
End-to-end DwmCapabilities startup probe via Dart FFI (per D-01): RtlGetVersion build number + DwmGetWindowAttribute availability check for four Win11 22000+ DWM attributes, exposed as an immutable snapshot on a ValueNotifier consumed by the window service layer. D-04 failure reporting wires HRESULT non-S_OK through KernelLogger (every call) + ErrorReporterImpl dedup (first-of-kind).

Purpose: Gate all downstream DWM attribute calls (Phase 7/8) on a one-shot startup snapshot so Win10 never receives Win11-exclusive attributes (ENAB-01). This is the enabling facility for the entire milestone — without it, Phase 7/8 cannot safely call DWM attributes.

Output: dwm_capabilities_probe.dart (FFI leaf), dwm_capabilities.dart (facade + snapshot data class + Linux placeholder), modified window_service_state.dart (ValueNotifier peer), test file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-c1-c2/06-CONTEXT.md
@.planning/phases/06-c1-c2/06-PATTERNS.md
@.planning/research/STACK.md

# Analog files (patterns to replicate)
@lib/kernel/services/thumbnail_service.dart
@lib/kernel/window_bridge/window_manager_service.dart
@lib/kernel/window_bridge/window_service_state.dart
@lib/kernel/diagnostics/kernel_logger.dart
@lib/kernel/diagnostics/error_reporter.dart
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end DwmCapabilities probe — FFI leaf + facade + state container</name>
  <precondition>KernelLoggerImpl.init() must have been called before the facade is accessed (file-scope `final _log = KernelLogger.I;` throws StateError pre-init) — test setUpAll calls KernelLoggerImpl.resetForTesting() then init().</precondition>
  <reversibility rating="costly">D-01 FFI probe path: Phase 7/8 attribute gates will read the Dart-side snapshot ValueNotifier; reverting to C++ probe + MethodChannel would require changing multiple consumers (per CONTEXT D-01 reversibility note).</reversibility>
  <files>lib/kernel/diagnostics/dwm_capabilities_probe.dart, lib/kernel/diagnostics/dwm_capabilities.dart, lib/kernel/window_bridge/window_service_state.dart, test/kernel/diagnostics/dwm_capabilities_test.dart</files>
  <read_first>
    - .planning/phases/06-c1-c2/06-PATTERNS.md (facade+lazy _impl pattern from thumbnail_service.dart:18-43, state container from window_service_state.dart:13-39, KernelLogger+ValueNotifier from window_manager_service.dart:24-87)
    - .planning/research/STACK.md (RtlGetVersion pattern, DWM attribute build-floor matrix: DWMWA_BORDER_COLOR=34, DWMWA_WINDOW_CORNER_PREFERENCE=33, DWMWA_CAPTION_COLOR=35, DWMWA_TEXT_COLOR=36, all 22000+)
    - lib/kernel/services/thumbnail_service.dart (platform-aware facade: lazy _impl + switch(defaultTargetPlatform) + singleton)
    - lib/kernel/window_bridge/window_service_state.dart (state container: ValueNotifier fields, _disposed guard, idempotent dispose)
    - lib/kernel/window_bridge/window_manager_service.dart (file-scope `final _log = KernelLogger.I;`, identity-preserved forwarding, error handling pattern at :96-99)
    - lib/kernel/diagnostics/kernel_logger.dart (singleton I accessor, .e() signature with context/error/stackTrace)
    - windows/runner/flutter_window.cpp (read-only — confirms WS_OVERLAPPEDWINDOW fullscreen detection pattern at :63-71; the probe does NOT modify this file)
  </read_first>
  <action>
    Create dwm_capabilities_probe.dart (FFI leaf, per D-01 — Dart FFI, no MethodChannel, no new C++):
    - Use dart:ffi. DynamicLibrary.open('ntdll.dll') → lookupFunction for RtlGetVersion. RtlGetVersion fills an OSVERSIONINFOEXW struct; read dwBuildNumber as Uint32 (integer-exact, ENAB-01 precision). Copy the RtlGetVersion build-detection pattern from the read-only external media_kit_video-2.0.1/windows/utils.cc:85 (copy-not-link, per CONTEXT canonical_refs). The OSVERSIONINFOEXW struct must be declared as a Struct subclass with correct field offsets (dwOSVersionInfoSize as Uint32, dwMajorVersion, dwMinorVersion, dwBuildNumber, then platform/SP fields, then szCSDVersion as Array<Uint16>).
    - DynamicLibrary.open('user32.dll') → lookupFunction for GetShellWindow (returns the shell Progman HWND — a system top-level window always available on Windows desktop, used purely for capability detection, not the app window).
    - DynamicLibrary.open('dwmapi.dll') → lookupFunction for DwmGetWindowAttribute. Probe four attributes by calling DwmGetWindowAttribute(shellHwnd, attrId, &dummy, sizeof) and checking the HRESULT return: S_OK (0) = available, E_INVALIDARG (0x80070057) = not available (Win10). The four attribute IDs per STACK.md: DWMWA_WINDOW_CORNER_PREFERENCE=33, DWMWA_BORDER_COLOR=34, DWMWA_CAPTION_COLOR=35, DWMWA_TEXT_COLOR=36.
    - try/catch (on Exception catch error, stackTrace) around DynamicLibrary.open + lookupFunction — DLL absent or lookup failure returns a null-snapshot sentinel, logs via KernelLogger.I.e with '[DwmCapabilities]' prefix.
    - All logging via file-scope `final _log = KernelLogger.I;` (kernel convention, PATTERNS Shared Patterns). NEVER debugPrint (kernel_logger_gate.sh enforces).
    - The probe function signature: `DwmCapabilitySnapshot? probe()` — synchronous (FFI is sync), returns null on DLL-absent failure, returns a populated snapshot on success. Takes no HWND parameter (acquires shell HWND internally via GetShellWindow).

    Create dwm_capabilities.dart (facade + data classes, per D-01):
    - Immutable data class DwmCapabilitySnapshot: final int buildNumber, final bool supportsBorderColor, final bool supportsCornerPreference, final bool supportsCaptionColor, final bool supportsTextColor. const constructor. All fields final (immutability per CLAUDE.md). Add a derived getter `bool get isWin11OrLater => buildNumber >= kWin11BuildFloor;`.
    - Named constant `const int kWin11BuildFloor = 22000;` (ENAB-01 boundary — single source of truth for the threshold).
    - Placeholder class LinuxCompositorCapabilities (Phase 11 peer slot per D-01): a stub class with a /// doc comment marking it "Phase 11 — Linux compositor probe (Wayland/X11/gamescope) peer form per D-01. NOT implemented this phase." and a no-op or placeholder snapshot. This exists so the facade's switch(defaultTargetPlatform) has a Linux branch that compiles.
    - Facade class DwmCapabilities (singleton, platform-aware, following thumbnail_service.dart:18-43 pattern): private constructor DwmCapabilities._(), static final _instance, lazy _impl field. The platform select uses switch(defaultTargetPlatform): Windows branch calls dwm_capabilities_probe.dart's probe(); Linux/macOS branches return a LinuxCompositorCapabilities placeholder (D-01 peer slot); default returns placeholder. Expose `ValueNotifier<DwmCapabilitySnapshot?> get snapshot` — a nullable ValueNotifier (null until probed). The facade holds an internal `final ValueNotifier<DwmCapabilitySnapshot?> _snapshot = ValueNotifier(null);` and the getter forwards this SAME instance (identity-preservation, no wrapping). A `void probe()` method calls the FFI leaf and writes _snapshot.value = result.
    - file-scope `final _log = KernelLogger.I;` (kernel convention).
    - Bilingual /// doc comments on all public classes/methods (CLAUDE.md convention: Chinese first line, English explanation).

    Modify window_service_state.dart (per PATTERNS window_service_state.dart:13-39 pattern):
    - Add `final ValueNotifier<DwmCapabilitySnapshot?> dwmCapabilities = ValueNotifier(null);` as a peer to the existing `mode` ValueNotifier. Import dwm_capabilities.dart.
    - Add `dwmCapabilities.dispose();` to the existing dispose() method (before the _disposed=true line or alongside the existing mode.dispose() call).
    - Do NOT wrap the notifier — the field IS the notifier (identity-preserved, Phase 7/8 will addListener on it directly).

    Create test/kernel/diagnostics/dwm_capabilities_test.dart:
    - setUpAll: KernelLoggerImpl.resetForTesting() then KernelLoggerImpl.init() (per CLAUDE.md test setup convention).
    - Test group 'DwmCapabilitySnapshot': const constructor produces immutable instance; kWin11BuildFloor is 22000; isWin11OrLater getter returns true for build 22000, false for build 21999 (ENAB-01 boundary: one step either side).
    - Test group 'DwmCapabilities facade': on non-Windows (test runs on Windows headless but defaultTargetPlatform can be overridden via debugDefaultTargetPlatformOverride), the facade returns a LinuxCompositorCapabilities placeholder when platform is Linux/macOS; the snapshot ValueNotifier starts null; after probe() is called on Windows, the snapshot is populated (or null on FFI failure — both acceptable headless since dart:ffi to ntdll may or may not work in test runner).
    - Test group 'WindowServiceState integration': WindowServiceState().dwmCapabilities is a ValueNotifier<DwmCapabilitySnapshot?> starting null; disposing WindowServiceState disposes the notifier (adds a listener, disposes, listener throws after dispose).
    - The Windows-specific FFI path (real ntdll/dwmapi calls) is NOT asserted headless — covered by UAT checklist in Plan 06-02 (real-hardware Win10/Win11 boot).
  </action>
  <verify>
    <automated>flutter test test/kernel/diagnostics/dwm_capabilities_test.dart</automated>
    <fails_when>non-zero exit, or "All tests passed!" absent from output, or "Some tests failed" in output</fails_when>
    <automated>flutter analyze lib/kernel/diagnostics/dwm_capabilities.dart lib/kernel/diagnostics/dwm_capabilities_probe.dart lib/kernel/window_bridge/window_service_state.dart</automated>
    <fails_when>non-zero exit, or any line containing "error •" in output</fails_when>
    <automated>bash tool/audit/kernel_logger_gate.sh</automated>
    <fails_when>non-zero exit (gate detects debugPrint in lib/kernel/)</fails_when>
  </verify>
  <done>
    - DwmCapabilities probe produces a DwmCapabilitySnapshot with buildNumber (int) and four booleans
    - kWin11BuildFloor = 22000 named constant exists and is tested at boundary (21999/22000)
    - Facade branches on defaultTargetPlatform (Windows calls FFI, Linux/macOS return placeholder)
    - LinuxCompositorCapabilities placeholder class exists with Phase 11 doc comment (D-01 peer slot)
    - WindowServiceState has dwmCapabilities ValueNotifier<DwmCapabilitySnapshot?> (nullable, starts null)
    - ValueNotifier disposed in WindowServiceState.dispose()
    - flutter analyze: 0 errors on the three files
    - kernel_logger_gate.sh passes (no debugPrint in lib/kernel/)
    - All tests in dwm_capabilities_test.dart pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-04 failure reporting (KernelLogger + ErrorReporter dedup) + startup wiring</name>
  <precondition>Task 1 complete (dwm_capabilities_probe.dart, dwm_capabilities.dart, window_service_state.dart exist and pass analyze). ErrorReporterImpl must be initialized before D-04 path fires (the startup wiring in main.dart initializes it).</precondition>
  <files>lib/kernel/diagnostics/dwm_capabilities_probe.dart, lib/kernel/diagnostics/dwm_capabilities.dart, lib/main.dart, test/kernel/diagnostics/dwm_capabilities_test.dart</files>
  <read_first>
    - lib/kernel/diagnostics/error_reporter.dart (reportPlatformSafely signature at :25/:185, _dedupeWindow = 10s at :44, _accept dedup logic at :344-360, _identity tuple for dedup matching)
    - lib/kernel/diagnostics/kernel_logger.dart (.e() signature with context/error/stackTrace at :442-450, singleton I accessor)
    - lib/main.dart (startup sequence: MediaKit.ensureInitialized, windowManager, KernelLogger.init, StartupCoordinator — identify where to insert the probe call after window creation)
    - lib/kernel/diagnostics/dwm_capabilities_probe.dart (Task 1 output — the FFI leaf where D-04 logging is added)
    - lib/kernel/diagnostics/dwm_capabilities.dart (Task 1 output — the facade where probe() is called)
  </read_first>
  <behavior>
    - Test 1: When DwmGetWindowAttribute returns a non-S_OK HRESULT, KernelLogger.I.e is called with a '[DwmCapabilities]' prefix and a context map containing 'attribute' (the attr ID) and 'build' (the build number) — every call, no suppression.
    - Test 2: The first non-S_OK HRESULT triggers ErrorReporterImpl.I.reportPlatformSafely with a GENERIC error message (so all attribute IDs dedup to one card within the 10s window). A second non-S_OK HRESULT for a different attribute ID within 10s does NOT produce a new report (deduped — occurrenceCount increments, no new card).
    - Test 3: After startup wiring, WindowServiceState.dwmCapabilities.value is non-null on Windows (probe was called) — or null on FFI failure (both acceptable, logged either way).
  </behavior>
  <action>
    D-04 failure reporting (per D-04 locked decision — "每次失败必记" + "首次聚合成一条 ErrorReport" + "卡片不刷屏"):
    - In dwm_capabilities_probe.dart, after each DwmGetWindowAttribute call, check the HRESULT. If hr != S_OK (0):
      (a) Log every call: `_log.e('[DwmCapabilities] DwmGetWindowAttribute hr=0x${hr.toRadixString(16)}', context: {'attribute': attrId, 'build': buildNumber}, error: null);` — per-call, no suppression (D-04 "每次失败必记"). The context map carries the specific attribute ID and build number for diagnostics.
      (b) Report first-of-kind: call `ErrorReporterImpl.I.reportPlatformSafely(error, StackTrace.current)` where `error` is an Exception with a GENERIC message like 'DWM capability probe reported non-S_OK HRESULT' (NOT per-attribute — the generic message ensures all attribute failures dedup to one card within the 10s _dedupeWindow, satisfying D-04 "卡片不刷屏"). Reuse the existing _dedupeWindow + _identity tuple in error_reporter.dart:344-360 — do NOT build a parallel dedup (D-04 locked decision, also must_haves.prohibitions).
    - The attribute-availability boolean is set to false for any non-S_OK HRESULT (S_OK = true, anything else = false). This is the probe working correctly — E_INVALIDARG on Win10 correctly marks the attribute as unavailable.
    - The D-04 path fires on ALL non-S_OK HRESULTs, including expected E_INVALIDARG on Win10. The per-call KernelLogger.e log provides full diagnostics; the generic ErrorReport message + 10s dedup prevents card spam (max 1 card per 10s window regardless of how many attributes fail).

    Startup wiring (per D-01 — probe is one-shot at startup, Dart-side consumption):
    - In lib/main.dart, after KernelLoggerImpl.init() and after the window is created (after windowManager.waitUntilReadyToShow or the existing window setup sequence), call the probe and write the snapshot to the state:
      - Call `DwmCapabilities.I.probe()` (synchronous FFI call, writes to the internal ValueNotifier).
      - The WindowServiceState instance must be accessible from main.dart (it is — main.dart constructs WindowService per CLAUDE.md architecture). Wire the facade's snapshot ValueNotifier to the WindowServiceState.dwmCapabilities field, OR have WindowServiceState hold a reference to the DwmCapabilities facade and forward. The cleanest approach: WindowServiceState.dwmCapabilities is the SAME notifier as DwmCapabilities.I.snapshot (identity-preservation — assign the reference, do not copy/wrap).
      - Wrap the probe call in try/catch (on Exception catch error, stackTrace) → `_log.e('[main] DwmCapabilities probe failed: $error\n$stackTrace')` and leave the snapshot null (graceful degradation — the app continues, Phase 7/8 attribute gates will see null and skip attribute calls).
      - The probe is synchronous (FFI is sync) so no unawaited() needed. If the executor wraps it in a Future for error isolation, use unawaited() per CLAUDE.md async best practices.

    Update test/kernel/diagnostics/dwm_capabilities_test.dart:
    - Add test group 'D-04 failure reporting' with the three behavior tests above. Use a fake ErrorReporter (hand-written fake per CLAUDE.md "Fakes over mocks" convention) that records reportPlatformSafely calls. Inject the fake or verify via ErrorReporterImpl behavior (resetForTesting pattern if available, else fake). The FFI leaf's DwmGetWindowAttribute call can be tested by mocking the HRESULT return — if the FFI leaf is structured so the HRESULT check is in a separate method, test that method directly with a fake HRESULT.
  </action>
  <verify>
    <automated>flutter test test/kernel/diagnostics/dwm_capabilities_test.dart</automated>
    <fails_when>non-zero exit, or "All tests passed!" absent from output, or test count does not include the D-04 group (at least 3 new tests)</fails_when>
    <automated>flutter analyze lib/main.dart lib/kernel/diagnostics/dwm_capabilities.dart lib/kernel/diagnostics/dwm_capabilities_probe.dart</automated>
    <fails_when>non-zero exit, or any line containing "error •" in output</fails_when>
    <automated>bash tool/audit/kernel_logger_gate.sh</automated>
    <fails_when>non-zero exit (gate detects debugPrint in lib/kernel/)</fails_when>
  </verify>
  <done>
    - Every DwmGetWindowAttribute non-S_OK HRESULT is logged via KernelLogger.I.e with '[DwmCapabilities]' prefix and context map (per-call, D-04)
    - First-of-kind non-S_OK HRESULT reported via ErrorReporterImpl.I.reportPlatformSafely with generic message (deduped within 10s window, D-04)
    - No parallel dedup built — reuses ErrorReporterImpl._dedupeWindow (D-04 locked decision)
    - DwmCapabilities.I.probe() is called at startup in main.dart after KernelLogger init + window creation
    - WindowServiceState.dwmCapabilities.value is populated (or null on failure, logged)
    - Probe failure is caught gracefully (app continues, snapshot null)
    - flutter analyze: 0 errors
    - D-04 test group passes (3+ tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart → native DLLs (FFI) | dart:ffi calls to ntdll.dll (RtlGetVersion), user32.dll (GetShellWindow), dwmapi.dll (DwmGetWindowAttribute) — untrusted native function signatures |
| Kernel → ErrorReporter | D-04 failure path crosses from kernel FFI leaf to ErrorReporterImpl singleton — dedup state must not corrupt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | dwm_capabilities_probe.dart FFI signatures | medium | mitigate | Use exact Win32 SDK NativeFunctionType signatures; copy RtlGetVersion OSVERSIONINFOEXW struct layout from media_kit_video utils.cc:85 (read-only source, copy-not-link); wrong struct offsets cause memory corruption |
| T-06-02 | Denial of service | dwm_capabilities_probe.dart DynamicLibrary.open | medium | mitigate | try/catch (on Exception) around DynamicLibrary.open + lookupFunction — DLL absent or lookup failure returns null snapshot, logs via KernelLogger.I.e, app continues (graceful degradation) |
| T-06-03 | Information disclosure | dwm_capabilities.dart log output | low | accept | Build number is not sensitive (not a credential, not PII); KernelLogger context map carries build number + attribute ID for diagnostics — acceptable for a single-developer desktop app |
| T-06-04 | Tampering | dwm_capabilities.dart stale snapshot | low | accept | This phase delivers a one-shot startup probe; settle-point re-read (theme/DPI change) is Phase 7 BORD-03 (out of scope per CONTEXT specifics). Stale snapshot within a session is acceptable — DWM capabilities don't change within a boot session |
</threat_model>

<verification>
Phase 6 Plan 01 verification (ENAB-01):
- flutter test test/kernel/diagnostics/dwm_capabilities_test.dart — all tests pass (snapshot, facade, state, D-04)
- flutter analyze on all modified/new files — 0 errors
- kernel_logger_gate.sh passes — no debugPrint in lib/kernel/
- UAT (real hardware, Plan 06-02 checklist): Win10 boot produces no E_INVALIDARG crash + all booleans false; Win11 boot produces build ≥ 22000 + booleans true
</verification>

<success_criteria>
- DwmCapabilitySnapshot is produced at startup with buildNumber + 4 attribute booleans
- kWin11BuildFloor = 22000 constant is the single threshold (boundary tested)
- Win10 never receives Win11-exclusive attribute calls (gated by snapshot)
- D-04: every HRESULT non-S_OK logged; first-of-kind reported (deduped, no card spam)
- LinuxCompositorCapabilities placeholder exists (Phase 11 peer slot, D-01)
- ValueNotifier identity-preserved (no wrapping)
- kernel_logger_gate passes
</success_criteria>

<output>
Create `.planning/phases/06-c1-c2/06-01-SUMMARY.md` when done
</output>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

### New files
- `lib/kernel/diagnostics/dwm_capabilities_probe.dart` — FFI leaf class `DwmCapabilitiesProbe`
  - Method: `DwmCapabilitySnapshot? probe()` — synchronous, returns null on DLL-absent failure
  - Internal: DynamicLibrary.open for ntdll (RtlGetVersion), user32 (GetShellWindow), dwmapi (DwmGetWindowAttribute)
  - D-04 path: KernelLogger.I.e per-call + ErrorReporterImpl.I.reportPlatformSafely first-of-kind
- `lib/kernel/diagnostics/dwm_capabilities.dart` — facade + data classes
  - Class `DwmCapabilitySnapshot` (immutable, const constructor): fields `int buildNumber`, `bool supportsBorderColor`, `bool supportsCornerPreference`, `bool supportsCaptionColor`, `bool supportsTextColor`; derived getter `bool get isWin11OrLater`
  - Named constant `const int kWin11BuildFloor = 22000` (ENAB-01 boundary)
  - Class `LinuxCompositorCapabilities` (Phase 11 peer slot placeholder, D-01)
  - Class `DwmCapabilities` (singleton, platform-aware facade): `static DwmCapabilities get I`, `ValueNotifier<DwmCapabilitySnapshot?> get snapshot`, `void probe()`
- `test/kernel/diagnostics/dwm_capabilities_test.dart` — test file
  - Groups: DwmCapabilitySnapshot, DwmCapabilities facade, WindowServiceState integration, D-04 failure reporting

### Modified files
- `lib/kernel/window_bridge/window_service_state.dart` — new field `ValueNotifier<DwmCapabilitySnapshot?> dwmCapabilities` (peer to `mode`), disposed in `dispose()`
- `lib/main.dart` — startup probe call after KernelLogger init + window creation, writes snapshot to state, try/catch graceful degradation

### No new pub dependencies (dart:ffi is SDK, no package installs)
</artifacts_this_phase_produces>
