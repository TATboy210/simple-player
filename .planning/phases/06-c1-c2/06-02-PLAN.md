---
phase: 06-c1-c2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tool/audit/c1_c2_gates.sh
  - .planning/phases/06-c1-c2/06-UAT.md
autonomous: true
requirements: [ENAB-02]
estimate:
  tokens: 24000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "tool/audit/c1_c2_gates.sh exits 0 on the current canonical codebase (GATE 1 PASS + GATE 2 PASS)"
    - "GATE 1 (D-02, C1): verifies the guard comment block exists in flutter_window.cpp:51-62, verifies the WM_NCCALCSIZE branch has the WS_OVERLAPPEDWINDOW style check before return 0 (structure fingerprint), and fails if a bare unconditional return 0 is introduced at the top of the WM_NCCALCSIZE handler (no-bare-return-0 grep gate)"
    - "GATE 2 (D-03, C2): zero matches for 'VideoState.isFullscreen' read as fullscreen signal anywhere under lib/ (single negative-grep gate, '便宜，一行规则' per user原话)"
    - "The gate script follows the phase16_gates.sh + kernel_logger_gate.sh pattern: set -euo pipefail, rg/grep compat layer, per-gate evidence printing, exit 0/1 for CI"
    - "FLAGGED ASSUMPTION (ENAB-02 unclassified edge, specless probe unresolved): ENAB-02 and ROADMAP success criterion 3 name a 3-branch C++ NCCALCSZE form (fullscreen→8px inset / maximized→overshoot / default→return 0). The ACTUAL current code (flutter_window.cpp:63-71) is a SINGLE-BRANCH fullscreen handler (WS_OVERLAPPEDWINDOW stripped → return 0). The maximized and default cases are handled by plugin fallthrough (window_manager's IsMaximized adjust + 8px inset for non-maximized). This phase PINS THE CURRENT 1-BRANCH STRUCTURE as canonical — the gate fingerprints the fullscreen branch + guard comment + no-bare-return-0. The maximized→overshoot and default→return 0 branches are NOT implemented because: (a) D-02 locks the test form to gate script (no C++ test harness), (b) the current 1-branch form is the working seam fix (memory-confirmed 2026-08-23, project_fullscreen_seam_icon_fix.md), (c) the semantics of 'maximized→overshoot' and 'default→return 0' are unspecified in any source artifact (STACK.md, CONTEXT.md, PATTERNS.md — none define what overshoot value or what default→return 0 means for non-fullscreen windows). Adding unspecified C++ branches risks regressing the working seam fix. FLAGGED FOR USER REVIEW at UAT — if the user intends the 3-branch C++ form, that is a scope expansion requiring a new phase."
    - "Real-hardware UAT items (Win10/Win11 boot, no E_INVALIDARG, no 8px seam, gate catches regression) are documented in 06-UAT.md — NOT as automated verify commands (headless-invisible per CONTEXT)"
  artifacts:
    - tool/audit/c1_c2_gates.sh
    - .planning/phases/06-c1-c2/06-UAT.md
  key_links:
    - "GATE 1 reads windows/runner/flutter_window.cpp (guard comment + WM_NCCALCSIZE branch structure)"
    - "GATE 2 reads lib/ (negative-grep for VideoState.isFullscreen)"
    - "06-UAT.md references both plans' real-hardware acceptance items"
  prohibitions:
    - "Must NOT collapse WM_NCCALCSIZE handler to a bare unconditional return 0 — GATE 1 catches this (D-02, ENAB-02 regression)"
    - "Must NOT read VideoState.isFullscreen as fullscreen signal anywhere in lib/ — GATE 2 catches this (D-03, '便宜，一行规则')"
    - "Must NOT introduce a C++ test harness for C1 — D-02 locks the gate-script approach (Unix: small is beautiful, project has zero C++ test infra)"
    - "Must NOT remove or alter the guard comment block in flutter_window.cpp:51-62 — GATE 1 verifies its presence (D-02)"
    - "Must NOT re-propose withdrawn fullscreen approaches (方案A FFI bridge / 方案B DWM disable / global DWMNCRP / window_manager.setFullScreen) — reverted-approaches denylist硬约束 (STACK.md, ROADMAP Phase 10 constraints)"
---

<objective>
C1/C2 pinning gate script + real-hardware UAT checklist (per D-02, D-03). The gate script pins the current canonical C1 NCCALCSIZE structure (guard comment + fullscreen branch fingerprint + no-bare-return-0) and the C2 single-source rule (no VideoState.isFullscreen reads). The UAT checklist documents headless-invisible acceptance items for real-hardware verification.

Purpose: Prevent C1 seam regression and C2 dual-source regression in all subsequent chrome work (Phase 7-10). Without this gate, a future phase could collapse the NCCALCSIZE handler to a bare return 0 (reintroducing the 8px seam) or read VideoState.isFullscreen (reintroducing the fullscreen-signal desync) silently.

Output: tool/audit/c1_c2_gates.sh (CI-automatable gate script), .planning/phases/06-c1-c2/06-UAT.md (real-hardware checklist).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-c1-c2/06-CONTEXT.md
@.planning/phases/06-c1-c2/06-PATTERNS.md

# Analog files (gate script patterns to replicate)
@tool/audit/phase16_gates.sh
@tool/audit/kernel_logger_gate.sh

# C1/C2 anchor files (read by the gate, not modified)
@windows/runner/flutter_window.cpp
@lib/kernel/window_bridge/window_manager_service.dart
</context>

<tasks>

<task type="tracer">
  <name>Task 1: C1/C2 gate script — GATE 1 (C1 structure) + GATE 2 (C2 single-source)</name>
  <precondition>windows/runner/flutter_window.cpp and lib/kernel/window_bridge/window_manager_service.dart exist at their git-tracked paths (verified — git ls-files non-empty for both).</precondition>
  <files>tool/audit/c1_c2_gates.sh</files>
  <read_first>
    - .planning/phases/06-c1-c2/06-PATTERNS.md (gate script skeleton at :108-195, rg/grep compat layer from kernel_logger_gate.sh:23-47, GATE 2 single-rule shape at :172-186, GATE 1 structural fingerprint shape at :188-195)
    - tool/audit/phase16_gates.sh (exact structural twin: multi-gate main() dispatch at :134-148, per-gate evidence printing, exit 0/1 for CI, set -euo pipefail, SCRIPT_DIR/REPO_ROOT resolution at :24-25)
    - tool/audit/kernel_logger_gate.sh (rg/grep compat layer at :23-47, single negative-grep gate shape at :53-65)
    - windows/runner/flutter_window.cpp (the C1 anchor — guard comment block at :51-62, WM_NCCALCSIZE handler at :63-71, single-branch fullscreen→return 0 structure)
    - lib/kernel/window_bridge/window_manager_service.dart (the C2 positive baseline — `bool get isFullscreen => _state.mode.value.isFullscreen;` at :74-76, the single source GATE 2 protects)
  </read_first>
  <action>
    Create tool/audit/c1_c2_gates.sh (per D-02, D-03 — gate script + structural contract, following tool/audit/ phase16_gates.sh precedent):

    Script header (copy phase16_gates.sh:1-22 style):
    - shebang #!/usr/bin/env bash
    - Bilingual header comment: "C1/C2 钉死静态结构闸门脚本 (Phase 6, D-02/D-03)" — documents GATE 1 (D-02, C1 structure) + GATE 2 (D-03, C2 single-source), design principles (只读+stdout, rg/grep compat, CI exit 0/1)
    - set -euo pipefail
    - SCRIPT_DIR and REPO_ROOT resolution (copy phase16_gates.sh:24-25 pattern)

    rg/grep compat layer (copy kernel_logger_gate.sh:23-47 verbatim — portability across CI runners without ripgrep):
    - detect rg or grep, set SEARCH_CMD
    - helper function search_pattern(pattern, path, exclude_file) that works with both

    GATE 1 — gate1_c1_structure() (per D-02, structural fingerprint):
    - Gate 1a (guard comment presence): grep for the guard comment marker in windows/runner/flutter_window.cpp. The guard comment block at :51-62 starts with "media_kit 原生全屏期间抢先处理 WM_NCCALCSIZE". Grep for a stable substring like "抢先处理 WM_NCCALCSIZE" or "media_kit 原生全屏期间". If absent → GATE 1 FAIL (D-02a): "guard comment block missing in flutter_window.cpp — C1 regression (guard comment removed)". Print the expected line range for auditability.
    - Gate 1b (WM_NCCALCSIZE branch structure fingerprint): verify the WM_NCCALCSIZE handler has the WS_OVERLAPPEDWINDOW style check BEFORE the return 0. Grep for the pattern: a line containing "WM_NCCALCSIZE" followed by (within a few lines) a line containing "WS_OVERLAPPEDWINDOW" and a line containing "return 0". The structural fingerprint is: WM_NCCALCSIZE → style check → conditional return 0. If the style check is absent and return 0 is unconditional (bare) → GATE 1 FAIL (D-02b). Use grep -n line evidence (copy phase16_gates.sh:46-96 evidence-printing style).
    - Gate 1c (no-bare-return-0): verify there is no bare unconditional `return 0;` as the first executable statement inside the `if (message == WM_NCCALCSIZE && wparam != FALSE)` block (i.e., return 0 must be inside a nested conditional, not at the top level of the WM_NCCALCSIZE handler). The current code has `return 0;` inside `if ((style & WS_OVERLAPPEDWINDOW) == 0) { return 0; }` — that is STRUCTURED (guarded), not bare. A bare return 0 would be `return 0;` immediately after the `if (message == WM_NCCALCSIZE && wparam != FALSE) {` line with no style check. Implement this check by verifying the line count between the WM_NCCALCSIZE block open and the return 0 is > 1 (there must be at least the style check line in between). If bare return 0 detected → GATE 1 FAIL (D-02c): "bare return 0 in WM_NCCALCSIZE handler — C1 seam regression (style check removed)".
    - If all three sub-gates pass → GATE 1 PASS (D-02): print confirmation with the guard comment line range + structure fingerprint evidence.

    GATE 2 — gate2_c2_single_source() (per D-03, "便宜，一行规则"):
    - Copy the exact shape from PATTERNS.md:172-186. Single negative-grep: search for 'VideoState\.isFullscreen' in all .dart files under lib/. If any match → GATE 2 FAIL (D-03): print "'VideoState.isFullscreen' read as fullscreen signal — must use WindowBridge.isFullscreen instead:" followed by the matching lines. If zero matches → GATE 2 PASS (D-03): print "zero 'VideoState.isFullscreen' fullscreen-signal reads."
    - Use the rg/grep compat layer (not bare grep) for CI portability.

    main() dispatch (copy phase16_gates.sh:134-148 shape):
    - local exit_code=0
    - gate1_c1_structure || exit_code=1
    - echo ""
    - gate2_c2_single_source || exit_code=1
    - return exit_code
    - main "$@"

    Make the script executable (chmod +x is noted in done criteria; the verify runs via `bash` which doesn't require +x but CI may).

    The gate script does NOT modify any file — read-only + stdout, no file writes, no network, no input surface (Unix principle 8, per phase16_gates.sh design principles).
  </action>
  <verify>
    <automated>bash tool/audit/c1_c2_gates.sh</automated>
    <fails_when>non-zero exit, or "GATE 1 FAIL" in output, or "GATE 2 FAIL" in output</fails_when>
    <automated>bash -c 'tmp=$(mktemp); cp windows/runner/flutter_window.cpp "$tmp"; sed -i "s/if ((style \& WS_OVERLAPPEDWINDOW) == 0)//; s/return 0;.*$/return 0;/" windows/runner/flutter_window.cpp; bash tool/audit/c1_c2_gates.sh 2>&1 | grep -q "GATE 1 FAIL" && echo "REGRESSION_CAUGHT" || echo "REGRESSION_MISSED"; cp "$tmp" windows/runner/flutter_window.cpp; rm "$tmp"'</automated>
    <fails_when>"REGRESSION_MISSED" in output (gate must catch a bare-return-0 regression when the style check is removed)</fails_when>
  </verify>
  <done>
    - tool/audit/c1_c2_gates.sh exists and is executable
    - GATE 1 (D-02, C1): passes on current code (guard comment present, WS_OVERLAPPEDWINDOW style check present, no bare return 0)
    - GATE 1 catches regression: temporarily removing the style check (collapsing to bare return 0) makes GATE 1 FAIL
    - GATE 2 (D-03, C2): passes on current code (zero VideoState.isFullscreen matches in lib/)
    - Gate script follows phase16_gates.sh + kernel_logger_gate.sh pattern (set -euo pipefail, rg/grep compat, per-gate evidence, exit 0/1)
    - Gate script is read-only (no file writes, no network)
  </done>
</task>

<task type="auto">
  <name>Task 2: Real-hardware UAT checklist + 3-branch assumption documentation</name>
  <precondition>Task 1 complete (gate script exists and passes on current code). Plan 06-01 probe files exist (for cross-referencing UAT items).</precondition>
  <files>.planning/phases/06-c1-c2/06-UAT.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 6 success criteria — 3 items: Win10 no E_INVALIDARG, Win11 build 22000+ detected, C1 multi-branch regression test)
    - .planning/REQUIREMENTS.md (ENAB-01, ENAB-02 acceptance criteria)
    - .planning/phases/06-c1-c2/06-CONTEXT.md (domain boundary, D-02/D-03/D-04 decisions, specifics)
    - windows/runner/flutter_window.cpp (C1 current structure — for documenting the 1-branch vs 3-branch assumption)
    - tool/audit/c1_c2_gates.sh (Task 1 output — for documenting the manual regression test)
  </read_first>
  <action>
    Create .planning/phases/06-c1-c2/06-UAT.md — a real-hardware UAT checklist documenting all headless-invisible acceptance items for Phase 6 (per CONTEXT phase_specific_guidance: "Real-hardware UAT items are headless-invisible: put them in an explicit UAT checklist artifact/file, NOT as automated verify commands"):

    Section 1 — ENAB-01 DwmCapabilities probe (real-hardware, Plan 06-01):
    - UAT-01a (Win11 boot): Start the app on Win11 (build ≥ 22000, user's system is 26200). Observe: app starts without crash. Check KernelLogger output: probe logs build number ≥ 22000, all four attribute booleans (supportsBorderColor, supportsCornerPreference, supportsCaptionColor, supportsTextColor) are true. No E_INVALIDARG in logs.
    - UAT-01b (Win10 boot, if available): Start the app on Win10 (build < 22000). Observe: app starts without crash. Check logs: probe logs build number < 22000, all four booleans false. E_INVALIDARG HRESULTs logged at info/error level (D-04 per-call logging) but NO error card spam (D-04 dedup — max 1 card per 10s window).
    - UAT-01c (D-04 dedup): On Win10, confirm only ONE error card appears (not four — one per attribute). The KernelLogger logs 4 entries (per-call) but ErrorReporter produces 1 card (generic message + 10s dedup).

    Section 2 — ENAB-02 C1/C2 pinning (real-hardware + manual regression, Plan 06-02):
    - UAT-02a (Win11 fullscreen seam): Enter fullscreen on Win11 (play a video, press F). Observe: no 8px black seam on left/right/bottom edges. Exit fullscreen. Observe: window restores correctly. (This is the C1 seam fix working — the gate pins it.)
    - UAT-02b (Gate regression test — manual): Temporarily edit windows/runner/flutter_window.cpp — remove the `if ((style & WS_OVERLAPPEDWINDOW) == 0)` line (collapsing to bare return 0). Run `bash tool/audit/c1_c2_gates.sh`. Confirm GATE 1 FAIL with evidence. Revert the edit. Run the gate again. Confirm GATE 1 PASS. (This is the manual regression-catch verification — the automated verify in Task 1 does this via sed, but UAT confirms on the user's actual environment.)
    - UAT-02c (C2 single-source grep): Run `bash tool/audit/c1_c2_gates.sh`. Confirm GATE 2 PASS (zero VideoState.isFullscreen matches).

    Section 3 — FLAGGED ASSUMPTION (ENAB-02 unclassified edge, 3-branch vs 1-branch):
    - Document the flagged assumption from must_haves.truths: ENAB-02 and ROADMAP criterion 3 name a 3-branch C++ form (fullscreen→8px inset / maximized→overshoot / default→return 0). The actual current code is 1-branch (fullscreen→return 0). This phase pins the current 1-branch structure.
    - User review item: "Review and confirm: is the current 1-branch C1 handler (fullscreen→return 0, maximized/default via plugin fallthrough) the canonical form to pin? Or should the 3-branch C++ form (maximized→overshoot, default→return 0) be implemented as a scope expansion? The semantics of 'maximized→overshoot' and 'default→return 0' are unspecified in all source artifacts. The current 1-branch form is the working seam fix (memory-confirmed 2026-08-23). If you confirm 1-branch, no further action. If you want 3-branch, a new phase or plan is needed."
    - This section is NOT an automated gate — it is a user decision checkpoint documented in the UAT checklist.

    Section 4 — Cross-phase UAT items (not Phase 6 scope but documented for traceability):
    - Phase 7 UAT: Win11 border suppressed (DWMWA_BORDER_COLOR=NONE), Win10 1px accepted. (Phase 7, not this phase.)
    - Phase 10 UAT: Fullscreen transition zero flicker. (Phase 10, not this phase.)
    - These are documented here so the user knows Phase 6's probe enables them but does not deliver them.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-c1-c2/06-UAT.md && grep -c "UAT-01a" .planning/phases/06-c1-c2/06-UAT.md && grep -c "UAT-02b" .planning/phases/06-c1-c2/06-UAT.md && grep -c "FLAGGED ASSUMPTION" .planning/phases/06-c1-c2/06-UAT.md</automated>
    <fails_when>non-zero exit, or any grep count is 0 (UAT items missing), or "FLAGGED ASSUMPTION" absent (3-branch assumption not documented)</fails_when>
  </verify>
  <done>
    - .planning/phases/06-c1-c2/06-UAT.md exists
    - Section 1 (ENAB-01 probe UAT): UAT-01a (Win11 boot), UAT-01b (Win10 boot), UAT-01c (D-04 dedup) all documented
    - Section 2 (ENAB-02 C1/C2 UAT): UAT-02a (fullscreen seam), UAT-02b (gate regression test), UAT-02c (C2 grep) all documented
    - Section 3 (FLAGGED ASSUMPTION): 3-branch vs 1-branch assumption documented with user review item
    - Section 4 (cross-phase traceability): Phase 7/10 UAT items referenced for traceability
    - All items are real-hardware or manual (NOT automated verify commands — headless-invisible per CONTEXT)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate script → source files | c1_c2_gates.sh reads flutter_window.cpp and lib/ via grep — read-only, no write surface |
| CI → gate script | CI executes the gate script with repo access — script must be deterministic, no input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | flutter_window.cpp C1 structure regressed to bare return 0 | high | mitigate | GATE 1c (no-bare-return-0) catches the regression via structure fingerprint — gate runs in CI, exit 1 blocks merge |
| T-06-06 | Tampering | C2 dual-source: VideoState.isFullscreen read reintroduced in lib/ | high | mitigate | GATE 2 (D-03 single negative-grep) catches any new read — gate runs in CI, exit 1 blocks merge |
| T-06-07 | Tampering | c1_c2_gates.sh bypassed (not run in CI) | low | accept | Gate is a guardrail, not a security control; CI integration is assumed (tool/audit/ scripts are the project's established CI pattern). If CI is not configured, the gate still runs manually via /gsd-verify-phase |
| T-06-08 | Elevation of privilege | Gate script executes arbitrary code via grep pattern injection | low | accept | Gate script uses literal-string grep patterns (no user input, no eval), read-only filesystem access, set -euo pipefail — no injection surface |
</threat_model>

<verification>
Phase 6 Plan 02 verification (ENAB-02):
- bash tool/audit/c1_c2_gates.sh — exits 0 (GATE 1 PASS + GATE 2 PASS on current code)
- Regression catch: temporarily collapsing the C1 handler to bare return 0 makes GATE 1 FAIL (automated via sed in verify)
- UAT checklist exists with all required sections (probe UAT, C1/C2 UAT, flagged assumption, cross-phase traceability)
</verification>

<success_criteria>
- Gate script passes on current canonical codebase (exit 0)
- Gate script catches C1 regression (bare return 0 → GATE 1 FAIL)
- Gate script catches C2 regression (VideoState.isFullscreen read → GATE 2 FAIL)
- UAT checklist documents all headless-invisible acceptance items
- 3-branch vs 1-branch assumption explicitly flagged for user review (not silently assumed)
</success_criteria>

<output>
Create `.planning/phases/06-c1-c2/06-02-SUMMARY.md` when done
</output>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

### New files
- `tool/audit/c1_c2_gates.sh` — CI-automatable gate script
  - Function `gate1_c1_structure()` — three sub-gates: 1a (guard comment presence), 1b (WS_OVERLAPPEDWINDOW style check fingerprint), 1c (no-bare-return-0)
  - Function `gate2_c2_single_source()` — single negative-grep for `VideoState.isFullscreen` in lib/
  - Function `main()` — dispatches both gates, exit 0/1 for CI
  - rg/grep compat layer (copied from kernel_logger_gate.sh:23-47)
- `.planning/phases/06-c1-c2/06-UAT.md` — real-hardware UAT checklist
  - Section 1: ENAB-01 probe UAT (UAT-01a Win11 boot, UAT-01b Win10 boot, UAT-01c D-04 dedup)
  - Section 2: ENAB-02 C1/C2 UAT (UAT-02a fullscreen seam, UAT-02b gate regression test, UAT-02c C2 grep)
  - Section 3: FLAGGED ASSUMPTION (3-branch vs 1-branch, user review item)
  - Section 4: Cross-phase traceability (Phase 7/10 UAT references)

### No files modified (flutter_window.cpp is READ by the gate, not modified — the guard comment and C1 structure already exist and are canonical)
### No new pub dependencies
</artifacts_this_phase_produces>
