---
phase: 06-dev-workflow-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-dev-workflow-enhancement/QUALITY-PIPELINE.md
autonomous: true
requirements:
  - DEV-03
user_setup: []

must_haves:
  truths:
    - "QUALITY-PIPELINE.md exists and covers test coverage + performance benchmarks"
    - "Current monitoring capabilities (PerfMonitor, MemoryMonitor, EngineMetrics) are analyzed"
    - "Flutter ecosystem tools are compared against current capabilities"
    - "Gap analysis identifies what is missing vs what is available"
    - "Recommendations are concrete with integration steps"
    - "Document is pure evaluation — no code changes, no CI/CD configuration"
  artifacts:
    - path: ".planning/phases/06-dev-workflow-enhancement/QUALITY-PIPELINE.md"
      provides: "Quality Pipeline evaluation document"
      contains: "Current Capabilities"
  key_links:
    - from: ".planning/phases/06-dev-workflow-enhancement/QUALITY-PIPELINE.md"
      to: "lib/kernel/utils/perf_monitor.dart"
      via: "Analysis of existing frame timing and jank detection capabilities"
      pattern: "PerfMonitor"
    - from: ".planning/phases/06-dev-workflow-enhancement/QUALITY-PIPELINE.md"
      to: "lib/kernel/utils/memory_monitor.dart"
      via: "Analysis of existing memory tracking capabilities"
      pattern: "MemoryMonitor"
---

<objective>
Produce a Quality Pipeline evaluation document covering test coverage tools and performance benchmarking for the project. Analyze existing monitoring code (PerfMonitor, MemoryMonitor, EngineMetrics), compare against Flutter ecosystem tools, identify gaps, and output integration recommendations.

Purpose: Establish a quality baseline assessment that guides future quality improvements without making any code changes or CI/CD configurations.
Output: .planning/phases/06-dev-workflow-enhancement/QUALITY-PIPELINE.md evaluation document.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dev-workflow-enhancement/06-CONTEXT.md
@.planning/phases/06-dev-workflow-enhancement/06-RESEARCH.md
@lib/kernel/utils/perf_monitor.dart
@lib/kernel/utils/memory_monitor.dart
@lib/kernel/engine/engine_metrics.dart
@analysis_options.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze existing monitoring capabilities and produce evaluation document</name>
  <files>.planning/phases/06-dev-workflow-enhancement/QUALITY-PIPELINE.md</files>

  <read_first>
    - lib/kernel/utils/perf_monitor.dart — current frame timing, jank detection capabilities (162 lines)
    - lib/kernel/utils/memory_monitor.dart — current memory tracking capabilities (193 lines)
    - lib/kernel/engine/engine_metrics.dart — engine-level performance counters (91 lines)
    - analysis_options.yaml — existing static analysis configuration
    - .planning/phases/06-dev-workflow-enhancement/06-CONTEXT.md — D-09 through D-12 decisions
    - .planning/phases/06-dev-workflow-enhancement/06-RESEARCH.md — Quality Pipeline assessment structure, existing monitoring patterns
  </read_first>

  <action>
Create `.planning/phases/06-dev-workflow-enhancement/QUALITY-PIPELINE.md` with the following structure (per D-10, D-12):

1. **Title and scope**: "Quality Pipeline Assessment — Simple Player Flutter". State that this is an evaluation document (per D-10: "纯文档，不写代码"). Scope: test coverage + performance benchmarks only (per D-09: static analysis already complete).

2. **Current Capabilities Matrix** (per D-11: analyze existing code first):
   - Read perf_monitor.dart and document what it provides: frame timing (build/raster), jank detection thresholds, FPS calculation
   - Read memory_monitor.dart and document what it provides: RSS tracking, Dart heap monitoring, sampling interval
   - Read engine_metrics.dart and document what it provides: engine-level counters (decode frames, dropped frames, buffer stats)
   - Read analysis_options.yaml and document: strict-casts, strict-inference, strict-raw-types already enabled
   - Count existing test files: `find lib/ -name "*_test.dart" | wc -l` and `find test/ -name "*.dart" | wc -l`
   - Document: "flutter test --coverage" produces lcov.info but no coverage threshold is enforced

3. **Flutter Ecosystem Tool Comparison** (per D-11: compare with ecosystem tools):
   - Test coverage tools:
     - `flutter test --coverage` + lcov: Current approach, produces lcov.info
     - `coverage` package: lcov to HTML report generation
     - `test_coverage` package: continuous coverage with thresholds
     - Comparison table: tool, what it does, current status, gap
   - Performance benchmark tools:
     - `PerfMonitor` (existing): Frame timing, jank detection
     - `flutter benchmark`: Official Flutter benchmarking (if available)
     - DevTools Performance: Visual profiling, timeline
     - `integration_test` package: E2E performance metrics
     - Comparison table: tool, what it measures, current status, gap
   - Memory profiling:
     - `MemoryMonitor` (existing): RSS + Dart heap
     - DevTools Memory: Heap snapshots, allocation tracking
     - `flutter run --profile` + DevTools: Full memory timeline
     - Comparison table: tool, what it provides, current status, gap

4. **Gap Analysis**:
   - What we have vs what we need for a complete quality pipeline
   - Test coverage: "91 test files exist, coverage collected but no threshold enforced"
   - Performance: "PerfMonitor tracks frame timing but no formal benchmark suite"
   - Memory: "MemoryMonitor tracks RSS but no allocation profiling"
   - Static analysis: "Complete — strict mode enabled" (no gap)

5. **Recommendations** (per D-10: "推荐方案、集成步骤"):
   - Priority 1 (immediate): Add coverage threshold to flutter test command
   - Priority 2 (short-term): Create benchmark suite using flutter benchmark or custom harness
   - Priority 3 (long-term): DevTools integration for deeper profiling
   - Each recommendation: what tool, why, integration steps, estimated effort

6. **Integration Steps** (per D-10: "集成步骤"):
   - For each recommendation, provide concrete steps (but do NOT implement — this is evaluation only per D-12)
   - Example: "Add to CLAUDE.md: `flutter test --coverage && genhtml coverage/lcov.info -o coverage/html`"
   - Example: "Create benchmarks/ directory with startup time, seek latency, memory peak tests"

7. **Notes** (per D-12):
   - "This document is evaluation only. No CI/CD configuration is included."
   - "Current project is a local desktop app — CI integration recommended for future when applicable."
   - Reference existing code: "See lib/kernel/utils/perf_monitor.dart, memory_monitor.dart, engine_metrics.dart for current monitoring."

Use concrete values from the files you read. Do not use placeholder text like "TBD" or "TODO". If something is unknown, state "Not verified — requires manual check" rather than leaving blanks.
  </action>

  <verify>
    <automated>test -f ".planning/phases/06-dev-workflow-enhancement/QUALITY-PIPELINE.md" && grep -c "Current Capabilities" ".planning/phases/06-dev-workflow-enhancement/QUALITY-PIPELINE.md"</automated>
  </verify>

  <acceptance_criteria>
    - QUALITY-PIPELINE.md exists at .planning/phases/06-dev-workflow-enhancement/QUALITY-PIPELINE.md
    - Document contains "Current Capabilities" section with analysis of PerfMonitor, MemoryMonitor, EngineMetrics
    - Document contains "Flutter Ecosystem Tool Comparison" with comparison tables
    - Document contains "Gap Analysis" section identifying specific gaps
    - Document contains "Recommendations" with prioritized tool suggestions
    - Document contains "Integration Steps" with concrete (but unimplemented) steps
    - Document references existing source files by path
    - Document explicitly states it is evaluation-only (per D-10, D-12)
    - No code files are modified
    - All values are concrete (no TBD/TODO placeholders)
  </acceptance_criteria>

  <done>
QUALITY-PIPELINE.md is a comprehensive evaluation document covering test coverage and performance benchmarks. It analyzes existing monitoring code, compares Flutter ecosystem tools, identifies gaps, and provides prioritized recommendations with integration steps. The document is pure evaluation with no code changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation only | No code changes, no external dependencies, no security surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Information Disclosure | QUALITY-PIPELINE.md | low | accept | Document contains tool recommendations only. No secrets, no credentials, no sensitive data. |
| T-06-05 | Tampering | No code changes | none | accept | This plan produces documentation only. No production code is modified. |
</threat_model>

<verification>
1. QUALITY-PIPELINE.md exists and is readable
2. Document contains at least 5 sections (title, current capabilities, comparison, gaps, recommendations)
3. Existing monitoring files are referenced by path (perf_monitor.dart, memory_monitor.dart, engine_metrics.dart)
4. Document explicitly states evaluation-only scope
5. No production code files are modified
</verification>

<success_criteria>
QUALITY-PIPELINE.md provides a complete quality pipeline assessment covering test coverage tools, performance benchmarking, and memory profiling. It references the existing monitoring infrastructure, compares Flutter ecosystem alternatives, and delivers actionable recommendations — all as documentation with no code changes.
</success_criteria>

<output>
Create `.planning/phases/06-dev-workflow-enhancement/06-02-SUMMARY.md` when done
</output>
