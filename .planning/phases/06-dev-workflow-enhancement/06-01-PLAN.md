---
phase: 06-dev-workflow-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .mcp.json
autonomous: false
requirements:
  - DEV-01
  - DEV-02
user_setup: []

must_haves:
  truths:
    - "Context7 library ID mapping table is present in CLAUDE.md for 5 libraries"
    - "Context7 usage rules (on-demand, specific queries) are documented in CLAUDE.md"
    - "codegraph MCP server is configured in project .mcp.json"
    - "codegraph is upgraded to v1.4.1 and verified working"
    - "codegraph indexes both project code and Flutter SDK"
    - "codegraph common commands are documented in CLAUDE.md"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Context7 library ID mapping + codegraph commands + Quality Pipeline reference"
      contains: "Context7 Documentation Lookup"
    - path: ".mcp.json"
      provides: "codegraph MCP server configuration"
      contains: "codegraph"
  key_links:
    - from: "CLAUDE.md"
      to: "Context7 MCP plugin"
      via: "Library ID table enables resolve-library-id calls"
      pattern: "websites/api_flutter_dev"
    - from: ".mcp.json"
      to: "codegraph CLI"
      via: "MCP server config launches codegraph serve"
      pattern: "codegraph.*serve"
---

<objective>
Configure Context7 documentation lookup and codegraph source analysis for the project. Add Context7 library ID mapping table to CLAUDE.md, upgrade codegraph to v1.4.1, configure codegraph MCP in .mcp.json, index project and Flutter SDK code, and document all tool usage in CLAUDE.md.

Purpose: Give every future Claude session instant access to Flutter API docs (Context7) and source code intelligence (codegraph), eliminating manual grep/search for API reference and code navigation.
Output: Updated CLAUDE.md with Context7 + codegraph sections; updated .mcp.json with codegraph MCP server config.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dev-workflow-enhancement/06-CONTEXT.md
@.planning/phases/06-dev-workflow-enhancement/06-RESEARCH.md
@CLAUDE.md
@.mcp.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Context7 documentation lookup section to CLAUDE.md</name>
  <files>CLAUDE.md</files>

  <read_first>
    - CLAUDE.md — understand existing section structure and find insertion point
  </read_first>

  <action>
Add a new section to CLAUDE.md after the "## Coding Conventions" section (after line ~118) and before the "## Comment Policy" section. The section must contain:

1. Section header: `## Context7 Documentation Lookup`

2. Opening instruction: "Use Context7 MCP to query Flutter API docs. Always use `resolve-library-id` first to get the exact library ID."

3. Library ID mapping table (per D-01) with columns: Library, Context7 ID, Snippets, Use For. Rows:
   - Flutter API | `/websites/api_flutter_dev` | 30,590 | Widget API, class reference, method signatures
   - Flutter Guide | `/websites/flutter_dev` | 10,777 | How-to guides, concepts, tutorials
   - fvp | `/wang-bin/fvp` | 105 | Video player plugin API (MDK/FFmpeg)
   - shared_preferences | `/websites/pub_dev_packages_shared_preferences` | 370 | Key-value storage API
   - window_manager | `/leanflutter/window_manager` | 126 | Window control (frameless, fullscreen, etc.)

4. Usage rules subsection (per D-02, D-03):
   - "Only query when user explicitly asks or API behavior is unclear — do not query proactively"
   - "Use specific queries like 'AnimatedSlide transition usage' not broad searches like 'Flutter animation'"
   - "Chinese docs available via `/websites/flutter_cn` (4,266 snippets) for Chinese queries"

5. Brief code example showing the two-step pattern: resolve-library-id then query-docs.

Do NOT modify any existing content in CLAUDE.md. Insert the new section between existing sections.
  </action>

  <verify>
    <automated>grep -c "Context7 Documentation Lookup" CLAUDE.md</automated>
  </verify>

  <acceptance_criteria>
    - CLAUDE.md contains a "## Context7 Documentation Lookup" section
    - Table has exactly 5 library rows with correct Context7 IDs matching D-01
    - Usage rules mention "on-demand" (D-02) and "specific queries" (D-03)
    - No existing CLAUDE.md content was removed or modified
    - Section is inserted before "## Comment Policy"
  </acceptance_criteria>

  <done>
CLAUDE.md has a Context7 Documentation Lookup section with 5-row library ID table, usage rules, and code example. Existing content preserved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify codegraph upgrade from v1.1.6 to v1.4.1</name>
  <files></files>

  <read_first>
    - .planning/phases/06-dev-workflow-enhancement/06-RESEARCH.md — Pitfall 1 (index staleness after upgrade)
  </read_first>

  <action>
Run `codegraph --version` to confirm current version is v1.1.6, then `codegraph status D:/simple_player_flutter` to check current index status. This is a blocking checkpoint — the upgrade is flagged [SUS] (3 days old, no repo). The user must verify the package is safe before proceeding.
  </action>

  <what-built>
Preparation for codegraph upgrade. Before upgrading, the current version status needs verification.
  </what-built>

  <how-to-verify>
1. Run: `codegraph --version` — confirm current version is v1.1.6
2. Run: `codegraph status D:/simple_player_flutter` — check current index status
3. Note the output for the executor to compare after upgrade

After verification, the executor will:
- Upgrade: `npm install -g @colbymchenry/codegraph@1.4.1`
- Verify: `codegraph --version` shows 1.4.1
- Rebuild index: `codegraph init D:/simple_player_flutter` (full rebuild needed per Pitfall 1)
- Add Flutter SDK index: `codegraph init D:/flutter/packages/flutter/lib` (per D-07, Dart source only)
- Verify MCP tools: test that `codegraph serve` starts without errors
  </how-to-verify>

  <verify>
    <automated>codegraph --version</automated>
  </verify>

  <done>
User has verified current codegraph version and index status, and approved the upgrade to v1.4.1.
  </done>

  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Configure codegraph MCP in .mcp.json and verify tools</name>
  <files>.mcp.json</files>

  <read_first>
    - .mcp.json — existing MCP server config (code-review-graph)
    - .planning/phases/06-dev-workflow-enhancement/06-RESEARCH.md — Pattern 2 (codegraph MCP config), Pitfall 1 (index staleness)
  </read_first>

  <action>
1. Read the current .mcp.json file. Add a "codegraph" entry to the existing "mcpServers" object (per D-06). The config must be:
   - command: "npx"
   - args: ["-y", "@colbymchenry/codegraph@1.4.1", "serve"]
   - cwd: "D:\\simple_player_flutter"
   - type: "stdio"

2. Keep the existing code-review-graph entry unchanged.

3. After updating .mcp.json, run: `npx -y @colbymchenry/codegraph@1.4.1 serve` and verify it starts without errors (test for 3 seconds then kill).

4. Run `codegraph init D:/simple_player_flutter` to rebuild the project index (full rebuild required after upgrade per Pitfall 1).

5. Run `codegraph init D:/flutter/packages/flutter/lib --include "*.dart"` to index Flutter SDK Dart source (per D-07). If the --include flag is not supported, use `codegraph init D:/flutter/packages/flutter/lib` instead.

6. Run `codegraph status D:/simple_player_flutter` to verify index is healthy.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const c=JSON.parse(fs.readFileSync('.mcp.json','utf8')); console.log(c.mcpServers.codegraph ? 'OK' : 'MISSING')"</automated>
  </verify>

  <acceptance_criteria>
    - .mcp.json contains "codegraph" entry in mcpServers with correct command/args/cwd
    - Existing code-review-graph entry is unchanged
    - codegraph --version shows 1.4.1 after upgrade
    - codegraph init completes for project without errors
    - codegraph init completes for Flutter SDK (or errors are documented)
    - codegraph status shows healthy index
    - `npx -y @colbymchenry/codegraph@1.4.1 serve` starts without crash
  </acceptance_criteria>

  <done>
.mcp.json has codegraph MCP server configured alongside code-review-graph. codegraph v1.4.1 installed, project and Flutter SDK indexed, MCP server starts successfully.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add codegraph commands and Quality Pipeline reference to CLAUDE.md</name>
  <files>CLAUDE.md</files>

  <read_first>
    - CLAUDE.md — current state after Task 1 edits
  </read_first>

  <action>
Add two new sections to CLAUDE.md after the Context7 section added in Task 1:

1. Section: "## codegraph Source Analysis"
   - Brief description: "codegraph provides tree-sitter-based code intelligence via MCP. Use for call graph, impact analysis, and source navigation."
   - Common commands table (per D-13):
     - `codegraph query "SymbolName"` — Search for a symbol
     - `codegraph explore "ClassName.method"` — Source + call paths
     - `codegraph callers "Method"` — Find all callers
     - `codegraph impact "ClassName"` — Refactoring impact analysis
     - `codegraph sync` — Incremental index update after code changes
     - `codegraph status` — Check index health
   - Note: "codegraph is configured as MCP server in .mcp.json — tools available in all Claude sessions"
   - Note: "Index both project (D:/simple_player_flutter) and Flutter SDK (D:/flutter/packages/flutter/lib)"

2. Section: "## Quality Pipeline Reference"
   - Brief description: "Quality assessment covers test coverage and performance benchmarks. See .planning/QUALITY-PIPELINE.md for full evaluation."
   - Current capabilities: "Static analysis: flutter analyze with strict-casts/strict-inference/strict-raw-types (complete)"
   - Quick commands: `flutter test --coverage` for coverage, `flutter analyze` for static analysis

Do NOT modify any existing content. Insert both sections after the Context7 section.
  </action>

  <verify>
    <automated>grep -c "codegraph Source Analysis" CLAUDE.md</automated>
  </verify>

  <acceptance_criteria>
    - CLAUDE.md contains "## codegraph Source Analysis" section with command table
    - CLAUDE.md contains "## Quality Pipeline Reference" section
    - codegraph commands table includes at least 5 commands (query, explore, callers, impact, sync)
    - Quality Pipeline section references .planning/QUALITY-PIPELINE.md
    - No existing content modified
  </acceptance_criteria>

  <done>
CLAUDE.md has Context7 + codegraph + Quality Pipeline sections. All tool usage documented for future sessions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install (codegraph upgrade) | Package from npm registry, [SUS] flagged — verify package works after install |
| .mcp.json config | MCP server config in project — could affect all Claude sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | npm package @colbymchenry/codegraph | high | mitigate | Package flagged [SUS] (3 days old, no repo). Human-verify checkpoint before upgrade. Verify `codegraph --version` and `codegraph serve` work correctly after install. |
| T-06-02 | Information Disclosure | .mcp.json MCP config | low | accept | Project-level config, not committed with secrets. Only exposes CLI commands, no credentials. |
| T-06-03 | Denial of Service | Flutter SDK indexing | low | accept | Full SDK index may be slow (>5min). Mitigated by indexing only packages/flutter/lib/ (Dart source). |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [SUS] codegraph package. |
</threat_model>

<verification>
1. CLAUDE.md contains Context7 Documentation Lookup section with 5-row library ID table
2. CLAUDE.md contains codegraph Source Analysis section with command table
3. CLAUDE.md contains Quality Pipeline Reference section
4. .mcp.json contains codegraph MCP server config
5. codegraph --version returns 1.4.1
6. codegraph status shows healthy index
7. `npx -y @colbymchenry/codegraph@1.4.1 serve` starts without errors
</verification>

<success_criteria>
All three tool configurations are in place: Context7 library IDs in CLAUDE.md, codegraph MCP in .mcp.json, and usage documentation for both. Future Claude sessions can immediately use Context7 for Flutter API docs and codegraph for source navigation.
</success_criteria>

<output>
Create `.planning/phases/06-dev-workflow-enhancement/06-01-SUMMARY.md` when done
</output>
