---
phase: 03-performance-optimization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/persistence/settings_store.dart
  - lib/ui/dialogs/settings/settings_tab_performance.dart
autonomous: true
gap_closure: true
requirements:
  - PERF-01
  - PERF-03
must_haves:
  truths:
    - "D3D11 sync setting persists across app restart"
    - "Hardware decoding setting persists across app restart"
    - "D3D11 multi-hardware testing template exists for 3+ GPU configs"
    - "DevTools frame time profiling guide exists with baseline/optimized comparison"
  artifacts:
    - path: "lib/kernel/persistence/settings_store.dart"
      provides: "d3d11Sync + hardwareDecoding persistence keys and save/load methods"
      contains: "_keyD3d11Sync"
    - path: "lib/ui/dialogs/settings/settings_tab_performance.dart"
      provides: "Notifiers load initial values from SettingsStore and save on change"
      contains: "SettingsStore.loadD3d11Sync"
    - path: ".planning/phases/03-performance-optimization/D3D11_HARDWARE_TEST.md"
      provides: "Structured multi-hardware testing template"
    - path: ".planning/phases/03-performance-optimization/DEVTOOLS_PROFILING_GUIDE.md"
      provides: "Frame time profiling guide with measurement templates"
  key_links:
    - from: "settings_tab_performance"
      to: "settings_store"
      via: "SettingsStore.saveD3d11SyncEnabled + loadD3d11SyncEnabled"
      pattern: "SettingsStore\\.(save|load)D3d11Sync"
    - from: "settings_tab_performance"
      to: "settings_store"
      via: "SettingsStore.saveHardwareDecoding + loadHardwareDecoding"
      pattern: "SettingsStore\\.(save|load)HardwareDecoding"
---

<objective>
Close 3 verification gaps from Phase 3 verification report.

Purpose: Gap 3 (settings persistence) is a code fix that wires D3D11 sync and hardware decoding notifiers to SettingsStore. Gaps 1 and 2 (multi-hardware testing, DevTools frame time measurement) require structured documentation for manual testing.

Output: Settings persistence code fix + two testing/profiling guide documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-performance-optimization/03-CONTEXT.md
@.planning/phases/03-performance-optimization/03-VERIFICATION.md
@.planning/phases/03-performance-optimization/PERFORMANCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire D3D11 sync and hardware decoding to SettingsStore (Gap 3)</name>
  <files>lib/kernel/persistence/settings_store.dart, lib/ui/dialogs/settings/settings_tab_performance.dart</files>
  <read_first>
    - lib/kernel/persistence/settings_store.dart — full file (447 lines), understand AppSettings fields + _save() helper + load() constructor pattern + saveAll() batch
    - lib/ui/dialogs/settings/settings_tab_performance.dart — full file (89 lines), understand _D3d11SyncNotifier and _HardwareDecodingNotifier bridge classes
    - lib/kernel/engine/media_engine.dart — setD3d11SyncEnabled + setHardwareDecoding signatures (lines 174, 179)
  </read_first>
  <action>
Close Gap 3: "Performance settings revert to defaults on restart" — the bridge notifiers are stateless and don't persist to SettingsStore.

**Step A: Add fields to AppSettings (in settings_store.dart)**

Add two fields to the AppSettings class (around line 33, after `videoDeinterlace`):
- `final bool d3d11Sync;` with default `true` (per D-18: hardcoded defaults)
- `final bool hardwareDecoding;` with default `true`

Add to the `const AppSettings({...})` constructor with defaults:
- `this.d3d11Sync = true,`
- `this.hardwareDecoding = true,`

**Step B: Add keys to SettingsStore**

After `_keyVideoDeinterlace` (line 127), add:
- `static const _keyD3d11Sync = 'd3d11Sync';`
- `static const _keyHardwareDecoding = 'hardwareDecoding';`

**Step C: Add load values to SettingsStore.load()**

In the `AppSettings(...)` constructor inside `load()` (around line 197, after `videoDeinterlace`), add:
- `d3d11Sync: prefs.getBool(_keyD3d11Sync) ?? true,`
- `hardwareDecoding: prefs.getBool(_keyHardwareDecoding) ?? true,`

Also add to the fallback `AppSettings(...)` in the catch block (around line 219):
- `d3d11Sync: true,`
- `hardwareDecoding: true,`

**Step D: Add save methods to SettingsStore**

After `saveVideoDeinterlace` (line 387), add two methods following the exact `_save` pattern:

```dart
static Future<void> saveD3d11SyncEnabled(bool value) => _save(
  'saveD3d11SyncEnabled',
  (p) => p.setBool(_keyD3d11Sync, value),
);

static Future<void> saveHardwareDecoding(bool value) => _save(
  'saveHardwareDecoding',
  (p) => p.setBool(_keyHardwareDecoding, value),
);
```

**Step E: Add to saveAll()**

In `saveAll()` (around line 433, after `videoDeinterlace` line), add:
- `await p.setBool(_keyD3d11Sync, s.d3d11Sync);`
- `await p.setBool(_keyHardwareDecoding, s.hardwareDecoding);`

**Step F: Wire notifiers in settings_tab_performance.dart**

Modify `_D3d11SyncNotifier` to load from SettingsStore and persist on change:

1. Change constructor to load initial value asynchronously. Since `ValueNotifier` constructor is synchronous, use a factory or load pattern:
   - Keep `super(true)` as initial default
   - Add `static Future<_D3d11SyncNotifier> create(MediaEngine engine) async` factory that loads from SettingsStore, creates instance with correct initial value, and returns it
   - OR simpler: make PerformanceTab a StatefulWidget, load values in initState, and pass loaded values to notifiers

2. In the `set value` override, add `SettingsStore.saveD3d11SyncEnabled(newValue);` before the engine call.

Same pattern for `_HardwareDecodingNotifier`:
1. Load initial from `SettingsStore.loadHardwareDecoding()` (not hardcoded `true`)
2. In setter, add `SettingsStore.saveHardwareDecoding(newValue);` before the engine call.

**Recommended approach:** Change `PerformanceTab` from StatelessWidget to StatefulWidget. In `_PerformanceTabState.initState()`, call `SettingsStore.load()` to get the persisted values, then create the notifiers with those values. This avoids async factory complexity while keeping the bridge notifier pattern clean.

If PerformanceTab becomes StatefulWidget:
- Add `late _D3d11SyncNotifier _d3d11Sync;`
- Add `late _HardwareDecodingNotifier _hardwareDecoding;`
- In `initState()`: create notifiers with initial values from widget.engine + load from SettingsStore
- In `dispose()`: dispose both notifiers
- Pass `_d3d11Sync` and `_hardwareDecoding` to `SettingSwitchRow` instead of inline construction

The notifiers should accept an initial value parameter:
- `_D3d11SyncNotifier(this._engine, {required bool initialValue}) : super(initialValue);`
- `_HardwareDecodingNotifier(this._engine, {required bool initialValue}) : super(initialValue);`

For loading: Since SettingsStore.load() returns all settings, you can load once and extract both values. Or add standalone `loadD3d11SyncEnabled()` and `loadHardwareDecoding()` methods following the `loadLocale()` pattern (lines 283-291).

Add standalone load methods:
```dart
static Future<bool> loadD3d11SyncEnabled() async {
  try {
    final prefs = await _getPrefs();
    return prefs.getBool(_keyD3d11Sync) ?? true;
  } on Exception catch (e) {
    debugPrint('SettingsStore.loadD3d11SyncEnabled failed: $e');
    return true;
  }
}

static Future<bool> loadHardwareDecoding() async {
  try {
    final prefs = await _getPrefs();
    return prefs.getBool(_keyHardwareDecoding) ?? true;
  } on Exception catch (e) {
    debugPrint('SettingsStore.loadHardwareDecoding failed: $e');
    return true;
  }
}
```

**CRITICAL:** Do NOT modify MediaEngine interface or FvpEngine. Only SettingsStore and settings_tab_performance.dart change. The engine bridge (setD3d11SyncEnabled/setHardwareDecoding) already works — we're adding persistence layer on top.

**CRITICAL:** Follow the exact SettingsStore pattern. All save methods use `_save()` helper. All load methods use `_getPrefs()` with try-catch fallback. Default values are `true` for both settings (sync mode ON, hardware decoding ON).
  </action>
  <verify>
    <automated>cd /d D:\simple_player_flutter && dart analyze lib/kernel/persistence/settings_store.dart lib/ui/dialogs/settings/settings_tab_performance.dart</automated>
  </verify>
  <acceptance_criteria>
    - AppSettings has `d3d11Sync` (bool, default true) and `hardwareDecoding` (bool, default true) fields
    - SettingsStore has `_keyD3d11Sync` and `_keyHardwareDecoding` constants
    - SettingsStore has `saveD3d11SyncEnabled(bool)` and `saveHardwareDecoding(bool)` methods using `_save()` helper
    - SettingsStore has `loadD3d11SyncEnabled()` and `loadHardwareDecoding()` methods following loadLocale() pattern
    - SettingsStore.load() includes both new fields in AppSettings constructor with `?? true` defaults
    - SettingsStore.saveAll() includes both new fields
    - _D3d11SyncNotifier loads initial value from SettingsStore.loadD3d11SyncEnabled() (not hardcoded true)
    - _HardwareDecodingNotifier loads initial value from SettingsStore.loadHardwareDecoding() (not hardcoded true)
    - Both notifier setters call SettingsStore.save* before engine.set* (persistence happens on every toggle)
    - dart analyze passes with zero errors/warnings on modified files
  </acceptance_criteria>
  <done>
D3D11 sync and hardware decoding settings persist to SharedPreferences and survive app restart. Toggle off -> restart -> setting stays off. Toggle on -> restart -> setting stays on.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create D3D11 multi-hardware testing template + DevTools profiling guide (Gaps 1-2)</name>
  <files>
    .planning/phases/03-performance-optimization/D3D11_HARDWARE_TEST.md,
    .planning/phases/03-performance-optimization/DEVTOOLS_PROFILING_GUIDE.md
  </files>
  <read_first>
    - .planning/phases/03-performance-optimization/PERFORMANCE.md — understand D3D11 parameter tuning section (lines 20-60), hardware context (lines 30-35), sync mode analysis (lines 37-43), control bar profiling section (lines 64-115)
    - .planning/phases/03-performance-optimization/03-VERIFICATION.md — understand the exact gaps: lines 9-20 (gap 1 multi-hardware), lines 21-30 (gap 2 frame time)
    - .planning/phases/03-performance-optimization/03-CONTEXT.md — D-14 (low-end baseline), D-22 (DevTools frame timeline), D-23 (multi-dimensional metrics), D-24 (2-5ms target), D-25 (data aggregation), D-26 (baseline measurement)
  </read_first>
  <action>
Create two structured testing/profiling documents that enable the developer to close Gap 1 (multi-hardware D3D11 testing) and Gap 2 (DevTools frame time measurement).

**Document 1: D3D11_HARDWARE_TEST.md**

This is a structured testing template the developer fills out on each hardware config. Include:

1. **Test objective**: Verify d3d11.sync.cpu=0 (async mode) produces no visible tearing on 3+ GPU configs, document fallback behavior.

2. **Hardware config table** (fill per machine):
   - Config name (e.g., "Dedicated NVIDIA RTX 3060")
   - GPU model
   - OS version
   - Display resolution
   - Driver version

3. **Test procedure** (exact steps per config):
   - Step 1: `flutter run -d windows --profile`
   - Step 2: Open a 4K test video (document the test video path)
   - Step 3: Go to Settings -> Performance tab
   - Step 4: Toggle D3D11 Sync OFF (d3d11.sync.cpu=0)
   - Step 5: Play video for 60+ seconds, observe for tearing
   - Step 6: Toggle D3D11 Sync ON (d3d11.sync.cpu=1)
   - Step 7: Play same video for 60+ seconds, compare
   - Step 8: Document results in the per-config table

4. **Per-config results table** (to fill):
   | Test | sync.cpu=1 (default) | sync.cpu=0 (async) |
   |------|---------------------|-------------------|
   | Tearing visible? | Y/N | Y/N |
   | Tearing severity (none/minor/major) | | |
   | Latency feel (same/better/worse) | | |
   | 4K playback smooth? | Y/N | Y/N |

5. **Fallback decision matrix**:
   - If tearing on >50% of configs: keep sync default, document async as opt-in
   - If tearing on <25% of configs: consider async as default for non-4K
   - If tearing only on specific GPUs: document per-GPU recommendation

6. **Test video requirements**: 4K HEVC, 4K H.264, 1080p HEVC (at minimum)

**Document 2: DEVTOOLS_PROFILING_GUIDE.md**

This is a structured profiling guide with measurement templates. Include:

1. **Objective**: Document concrete 2-5ms/frame savings from D3D11 async mode via DevTools frame timeline.

2. **Prerequisites**: Flutter DevTools, `--profile` mode, test video (4K HEVC recommended).

3. **Baseline measurement** (d3d11.sync.cpu=1, sync mode):
   - Step 1: `flutter run -d windows --profile`
   - Step 2: Open DevTools -> Performance tab
   - Step 3: Open 4K test video, play for 10 seconds to stabilize
   - Step 4: Start recording in DevTools
   - Step 5: Play for 30 seconds (idle playback, no interaction)
   - Step 6: Stop recording
   - Step 7: Note average frame time and p99 frame time
   - Step 8: Fill baseline table

4. **Optimized measurement** (d3d11.sync.cpu=0, async mode):
   - Same steps as baseline but with D3D11 Sync toggled OFF in settings
   - Fill optimized table

5. **Comparison table** (to fill):
   | Metric | Baseline (sync) | Optimized (async) | Delta |
   |--------|----------------|-------------------|-------|
   | Average frame time (ms) | | | |
   | P99 frame time (ms) | | | |
   | Jank % (frames >16.6ms) | | | |
   | Worst frame (ms) | | | |
   | Total frames recorded | | | |

6. **Control bar interaction profiling** (separate test):
   - Record frame timeline during: hover over controls, seek via progress bar, control bar fade in/out
   - Calculate jank % for each interaction
   - Target: <5% jank per interaction type

7. **Data recording format**: Provide a CSV-friendly format for aggregating results across machines.

8. **Success criteria**:
   - Average frame time improvement >= 2ms with async mode
   - Jank % < 5% in all scenarios
   - Document any scenarios where async mode performs worse
  </action>
  <verify>
    <automated>cd /d D:\simple_player_flutter && test -f ".planning/phases/03-performance-optimization/D3D11_HARDWARE_TEST.md" -a -f ".planning/phases/03-performance-optimization/DEVTOOLS_PROFILING_GUIDE.md" && echo "Both files exist" || echo "Missing files"</automated>
  </verify>
  <acceptance_criteria>
    - D3D11_HARDWARE_TEST.md exists with: hardware config table, per-config test procedure (8 steps), per-config results table, fallback decision matrix, test video requirements
    - DEVTOOLS_PROFILING_GUIDE.md exists with: baseline measurement steps, optimized measurement steps, comparison table template, control bar interaction profiling, CSV data format, success criteria
    - Both documents reference the exact d3d11.sync.cpu parameter and settings panel toggle path
    - Both documents are structured as fill-in templates (tables with blank cells for the developer to complete)
    - D3D11_HARDWARE_TEST.md covers at least 3 GPU config slots (dedicated NVIDIA, Intel iGPU, AMD iGPU)
    - DEVTOOLS_PROFILING_GUIDE.md includes p99 frame time, average frame time, and jank % metrics
  </acceptance_criteria>
  <done>
Two structured testing documents exist that enable the developer to manually close Gap 1 (multi-hardware D3D11 validation on 3+ GPU configs) and Gap 2 (concrete DevTools frame time measurement with 2-5ms/frame savings documentation). Documents are fill-in templates, not completed reports.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user_input → SettingsStore | Boolean values persisted to SharedPreferences (low risk — no injection vector) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | SettingsStore d3d11Sync/hardwareDecoding | accept | Boolean values only, no security impact from user tampering with display settings |
| T-03-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages added in this plan |
</threat_model>

<verification>
**Gap 3 closure (automated):**
- `dart analyze lib/kernel/persistence/settings_store.dart` — zero errors
- `dart analyze lib/ui/dialogs/settings/settings_tab_performance.dart` — zero errors
- `flutter test` — existing tests still pass (no regressions)

**Gap 1-2 closure (manual):**
- D3D11_HARDWARE_TEST.md exists and contains structured testing template
- DEVTOOLS_PROFILING_GUIDE.md exists and contains profiling measurement template
- Developer runs the tests on physical hardware and fills in the templates

**End-to-end persistence test (manual):**
1. Run app -> Settings -> Performance -> toggle D3D11 Sync OFF
2. Close app
3. Run app again -> Settings -> Performance -> verify D3D11 Sync is still OFF
4. Toggle D3D11 Sync ON, toggle Hardware Decoding OFF
5. Close app, reopen, verify both settings persist
</verification>

<success_criteria>
- AppSettings includes d3d11Sync and hardwareDecoding fields with `true` defaults
- SettingsStore has save/load methods for both settings following existing _save() pattern
- PerformanceTab notifiers load initial values from SettingsStore (not hardcoded)
- PerformanceTab notifiers persist to SettingsStore on every toggle
- Two structured testing documents exist for manual Gap 1-2 closure
- dart analyze passes on modified files
</success_criteria>

<output>
Create `.planning/phases/03-performance-optimization/03-04-SUMMARY.md` when done
</output>
