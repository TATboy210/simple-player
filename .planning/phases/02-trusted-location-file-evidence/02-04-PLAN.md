---
phase: 02-trusted-location-file-evidence
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - lib/kernel/diagnostics/error_log_location.dart
  - lib/kernel/diagnostics/error_log_file_sink.dart
  - lib/kernel/diagnostics/error_reporter.dart
  - lib/kernel/diagnostics/error_reporting_dependencies.dart
  - lib/main.dart
  - test/diagnostics/error_log_location_test.dart
  - test/diagnostics/global_error_hooks_test.dart
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-03：生产默认路径只来自 getApplicationSupportDirectory()/logs/error.log；logs child 在首写前递归创建，不使用 executable directory 或 cwd fallback。"
    - "D-08：binding/KernelLogger 之后立即构造稳定的 delegating diagnostic-file effect/status，初始化唯一 ErrorReporter 并安装四源 hooks；path_provider 与目录 I/O 均发生在 hooks 已生效之后。"
    - "异步路径解析尚未完成、永久 pending 或失败时，reporter、全局 hooks 与 runApp 均已可用；稳定日志状态保持 unavailable，稍后成功只激活 writer，不替换 reporter/effect/listenable identity。"
    - "激活后的真实 resolved logPath 和 logsAvailable 通过 reporter-owned stable read-only listenables 暴露给 Phase 3，不要求 UI 持有 filesystem/service object。"
    - "全阶段 quality gates 证明没有 media_kit 修改、没有 package:logger kernel import、没有新增 diagnostics test failure。"
  artifacts:
    - path: lib/kernel/diagnostics/error_log_location.dart
      provides: "Application Support provider seam、logs directory creation 与 deterministic result。"
    - path: lib/kernel/diagnostics/error_reporting_dependencies.dart
      provides: "identity-stable delegating file effect 与 read-only availability/path listenables contract。"
    - path: lib/main.dart
      provides: "reporter/hooks-first bootstrap 与 non-blocking async path/writer activation。"
    - path: lib/kernel/diagnostics/error_reporter.dart
      provides: "production init collaborators + stable read-only diagnostic log status/path。"
    - path: test/diagnostics/global_error_hooks_test.dart
      provides: "composition ordering、pending provider 与 degraded/late-activation startup contract。"
  key_links:
    - from: main
      to: ErrorReporterImpl.init + GlobalErrorHooks.install
      via: "stable unavailable/delegating effect is injected immediately after KernelLogger init"
      pattern: "effects:"
    - from: GlobalErrorHooks.install
      to: getApplicationSupportDirectory
      via: "hooks are installed before an unawaited, contained activation starts provider/filesystem I/O"
      pattern: "getApplicationSupportDirectory"
    - from: resolved logs/error.log
      to: stable delegating effect
      via: "late activation installs the sole ErrorLogFileSink writer without replacing effect or listenables"
      pattern: "ErrorLogFileSink"
---

<objective>
把已验证的诊断文件纵切装入生产启动路径：先以 identity-stable delegating effect/status 初始化 reporter 并安装所有全局错误 hooks，再异步解析 Application Support、建立 logs/error.log 并原位激活 writer；任何 pending 或 filesystem 启动故障都只维持“日志不可用”降级。

Purpose: 实现 D-03/LOG-04 的唯一默认落点，并让 Phase 2 在真实应用中持续工作，同时消除 path_provider/目录准备之前的 capture blind spot。
Output: 路径 resolver、reporter/hooks-first composition wiring、identity-stable late activation、可观察状态和完整 quality/audit gate。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trusted-location-file-evidence/02-CONTEXT.md
@.planning/phases/02-trusted-location-file-evidence/02-RESEARCH.md
@.planning/phases/02-trusted-location-file-evidence/02-PATTERNS.md
@.planning/phases/02-trusted-location-file-evidence/02-VALIDATION.md
@.planning/phases/02-trusted-location-file-evidence/02-03-SUMMARY.md
@lib/main.dart
@lib/kernel/diagnostics/error_log_file_sink.dart
@lib/kernel/diagnostics/error_reporter.dart
@test/diagnostics/global_error_hooks_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 解析并验证唯一 Application Support 默认落点</name>
  <files>lib/kernel/diagnostics/error_log_location.dart, test/diagnostics/error_log_location_test.dart</files>
  <read_first>
    - lib/kernel/utils/debug_exporter.dart（现有 getApplicationSupportDirectory + child directory precedent）
    - lib/kernel/diagnostics/error_log_file_sink.dart（构造所需 target path 与 unavailable behavior）
    - .planning/phases/02-trusted-location-file-evidence/02-CONTEXT.md（D-03）
    - .planning/phases/02-trusted-location-file-evidence/02-RESEARCH.md（Default log path example、Pitfall 6、LOG-04 test map）
    - .planning/REQUIREMENTS.md（LOG-04；用户配置路径属于 Phase 4 SET-02，不在本任务预建）
  </read_first>
  <behavior>
    - RED：injected support directory `X` 解析为平台安全 join 的 `X/logs/error.log`，并在返回成功前创建 `X/logs` recursively；重复调用幂等。
    - RED：provider throw、Directory.create failure、support result 不可用或目标结构为 file 时返回 typed unavailable result，不向调用方抛出。
    - RED：测试检查 resolver source/returned path 不使用 Directory.current、Platform.resolvedExecutable 或 executable-parent fallback。
    - RED：Unicode/space-containing support path 保持完整，target filename 固定为 error.log。
  </behavior>
  <action>创建窄的 `ErrorLogLocation.resolve`（或等价命名）及 typed success/unavailable result；production provider 在 composition root 注入 `getApplicationSupportDirectory`，kernel resolver 本身不静态调用 plugin。使用 `Directory` 与平台 separator/path package（已有依赖）安全 join `logs/error.log`，在返回路径前 `create(recursive:true)`。只捕获明确外部 I/O/provider 异常并返回原因对象供 main 降级记录。不得尝试 exe/cwd/home 替代路径，不实现 Phase 4 的 user-configured location 或 last-known-good。常量 `logs`/`error.log` 作为单一来源并写 doc comments。</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/diagnostics/error_log_location_test.dart</automated>
    <fails_when>命令非零退出，或默认后缀不精确、目录未在返回前创建、失败向外抛、Unicode path 损坏、代码/结果出现 cwd/executable fallback。</fails_when>
  </verify>
  <acceptance_criteria>
    - success path 精确为 provider support directory 下的 `logs/error.log`，child directory 已存在且 repeated resolve 安全。
    - all provider/filesystem setup failures 返回 typed unavailable 结果，不抛出。
    - resolver 没有 cwd、executable directory、alternate location fallback。
    - Phase 4 configured path validation 未被提前实现；本 phase 只建立可替换 provider seam。
  </acceptance_criteria>
  <done>默认日志目标以可测试、失败可表达的方式唯一解析到 Application Support/logs/error.log。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 guarded startup 安装 sink effect 并完成 Phase 2 全门禁</name>
  <files>lib/main.dart, lib/kernel/diagnostics/error_log_file_sink.dart, lib/kernel/diagnostics/error_reporter.dart, lib/kernel/diagnostics/error_reporting_dependencies.dart, test/diagnostics/global_error_hooks_test.dart</files>
  <read_first>
    - lib/main.dart（binding→KernelLogger→ErrorReporter→hooks 的 Phase 1 ordering contract）
    - lib/kernel/diagnostics/error_reporter.dart（static init 当前无 collaborators；保持 singleton identity/idempotence）
    - lib/kernel/diagnostics/error_reporting_dependencies.dart（read-only status contract 的注入 seam，避免 reporter 持有 concrete sink）
    - lib/kernel/diagnostics/error_log_file_sink.dart（record、drain、logsAvailable、resolved path）
    - lib/kernel/diagnostics/error_log_location.dart（Task 1 typed result）
    - test/diagnostics/global_error_hooks_test.dart（source-order assertions 与 BootstrapErrorFallback seams）
    - tool/audit/kernel_logger_gate.sh 与 tool/audit/phase21_release_gate.sh（禁止 kernel package:logger/debug leakage）
    - .planning/phases/02-trusted-location-file-evidence/02-CONTEXT.md（D-02、D-03、D-06、D-08）
  </read_first>
  <behavior>
    - RED：main source/抽取 composition helper 的行为测试证明 binding 与 KernelLogger 初始化后，立即创建 identity-stable delegating diagnostic-file effect/status，依次完成 `ErrorReporterImpl.init(effects:[delegatingEffect.record], ...)` 与 `GlobalErrorHooks.install`；provider 尚未被调用。
    - RED：注入一个未完成的 location Future 时 initialization/runApp path returnsNormally，reporter/hooks 已可接纳错误，availability=false、logPath=null；完成 Future 后同一 effect 与同一 listenable 实例原位激活，随后报告经 drain 写入 resolved error.log。
    - RED：injected location failure 或 sink activation/constructor failure 时 reporter/hooks 持续可用，status=false、logPath=null；失败不重新报告进 ErrorReporter，也不阻断 MediaKit/window/runApp。
    - RED：重复 initialization 不替换已被 hooks/consumer 持有的 reporter、delegating effect、availability/path listenables；testing reset 等待/释放活动 writer 与静态引用。
  </behavior>
  <action>在 guarded zone 的 binding 和 KernelLogger 初始化之后，先构造一个 production-owned、identity-stable 的 delegating diagnostic-file effect/status：其 `record` 在 unavailable/pending 时安全 no-op，其 read-only availability 与 nullable log-path listenables 从创建起保持同一实例。立即将该 `record` 按 D-08 注入唯一 `ErrorReporterImpl.init`，随后安装 `GlobalErrorHooks`；这两步都不得等待 path_provider、目录 I/O 或 writer。hooks 已生效后，启动一个显式 `unawaited` 且全边界 containment 的异步 activation：await `ErrorLogLocation.resolve(getApplicationSupportDirectory)`，success 时创建唯一 `ErrorLogFileSink` 并原位接入 delegating effect，failure 时维持 unavailable/null。不得以 timeout 把慢 provider 误判失败；永久 pending 也不能阻断 runApp。activation 期间和之后都不得替换 reporter、effect callback 所属对象或任何已暴露 ValueNotifier/Listenable identity；writer 激活只影响后续接纳报告，不回放 pending 期间未落盘报告。扩展 `ErrorReporterImpl.init` 以接受稳定 effect/status collaborator，同时保留无参调用和 idempotent singleton contract；reporter 只暴露 read-only stable `ValueListenable<bool>` 与 nullable log-path listenable/value，Phase 3 不持有 sink。路径/sink setup warning 只走 KernelLogger 并 containment。保持 concrete sink 可 drain/dispose，resetForTesting 必须清理 late activation/writer，且不要改 media_kit initialization 或 playback code。完成 focused tests 后执行全 diagnostics、analyze、full suite、kernel logger audit；若 full suite 命中仓库已鉴别 headless failure，按既有 summary 的隔离方法记录证据，但任何新增 diagnostics failure必须修复。</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/diagnostics/error_log_location_test.dart test/diagnostics/global_error_hooks_test.dart</automated>
    <fails_when>命令非零退出，或 focused location/startup 测试输出 assertion error。</fails_when>
    <automated>D:/flutter/bin/flutter test test/diagnostics/ &amp;&amp; D:/flutter/bin/flutter analyze &amp;&amp; D:/flutter/bin/flutter test &amp;&amp; bash tool/audit/kernel_logger_gate.sh</automated>
    <fails_when>focused ordering/pending/late-activation 测试非零，或最终门禁任一命令非零且不能以仓库既有 headless 基线独立复现；或 diagnostics 新测试失败、analyze 有 error、kernel 导入 package:logger、identity/availability/path/effect integration 断言失败。</fails_when>
  </verify>
  <acceptance_criteria>
    - production main 在任何 path_provider/filesystem await 前完成唯一 reporter 初始化与 GlobalErrorHooks 安装；provider 永久 pending 的测试仍证明 capture 与 runApp path 不受阻。
    - reporter 从启动即持有同一个 delegating effect；路径成功后只原位激活唯一 concrete sink，不替换 reporter、effect object 或 availability/path listenables。
    - support/path/sink activation failure 不阻断 ErrorReporter、GlobalErrorHooks、MediaKit/window initialization 或 runApp；status 稳定可观察为 unavailable 且不递归报告。
    - reporter 提供 stable read-only log availability 与 nullable logPath contract，Phase 3 无需查询 filesystem；success activation 后同一监听对象发布 available/resolved path。
    - D-06/D-08 gate 成立：kernel 无 package:logger import，KernelLogger CompositeSink 未承担 ErrorReport persistence。
    - focused location+startup tests、diagnostics suite、flutter analyze、full flutter test 与 kernel audit 满足质量红线；没有 media_kit 文件变更。
  </acceptance_criteria>
  <done>真实应用启动先获得可捕获错误的 stable reporter/hooks，再异步启用 Application Support/error.log；慢、失败或永久 pending 的文件准备均不形成 bootstrap blind spot，成功时不改变任何已发布 identity。</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | Symbols produced or modified | Purpose |
|---|---|---|
| `lib/kernel/diagnostics/error_log_location.dart` | `ErrorLogLocation`, typed resolved/unavailable result, `logsDirectoryName`, `logFileName` | 唯一默认落点解析与目录准备。 |
| `lib/kernel/diagnostics/error_log_file_sink.dart` | concrete writer activation/drain lifecycle | writer 准备成功后接入稳定 delegating effect，写失败保持非递归降级。 |
| `lib/kernel/diagnostics/error_reporter.dart` | collaborator-aware `init`, stable read-only log status/path | 让 composition root 在 filesystem 前安装 effect，让 Phase 3 只消费同一监听身份。 |
| `lib/kernel/diagnostics/error_reporting_dependencies.dart` | identity-stable delegating effect 与 diagnostic-log status/provider seam | pending 时安全 no-op，late activation 时不替换 reporter/listenables。 |
| `lib/main.dart` | hooks-first bootstrap + contained async path/sink activation | guarded-zone 生产组装，不 await 文件准备后才捕获。 |
| `test/diagnostics/error_log_location_test.dart` | default path/failure tests | 锁定 D-03。 |
| `test/diagnostics/global_error_hooks_test.dart` | startup order、pending provider、late activation/degraded tests | 锁定 hooks 先于 provider I/O 且稳定身份下成功/失败均不阻断。 |

## Multi-Source Coverage Audit

| Source | Item | Disposition |
|---|---|---|
| GOAL | 报告独立写入默认本地诊断文件 | COVERED：Task 2 production wiring。 |
| REQ | LOG-01 | COVERED：Task 2 installs 02-01 direct file effect behind a startup-stable delegate, then activates its writer asynchronously。 |
| REQ | LOG-02 | COVERED：effect independent of future UI/presentation；pending file setup does not disable capture。 |
| REQ | LOG-03 | COVERED：unavailable startup + identity-stable status + late activation + drain contract。 |
| REQ | LOG-04 | COVERED：Task 1 default location；configured path validation correctly remains SET-02/Phase 4 dependency。 |
| REQ | LOG-05 | COVERED：activated production sink uses 02-01/02-03 shared formatter。 |
| RESEARCH | composition root async provider、mkdir、no static plugin init、null/MAX_PATH degradation | COVERED：Tasks 1–2；provider I/O starts only after hooks。 |
| CONTEXT | D-03 | COVERED：Task 1。 |
| CONTEXT | D-02/D-06/D-08 | COVERED：Task 2 stable effect attachment、late writer activation and audit。 |
| CONTEXT | D-01/D-04/D-05/D-07 | PRESERVED：02-01–02-03 artifacts wired without policy change。 |
| EXCLUSION | Phase 4 configurable path; Phase 3 UI; rotation/encryption/remote/native crash/media changes | EXCLUDED：明确属于后续 phase 或 Out of Scope。 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| path_provider/platform channel → app path | 平台可能返回失败、长路径或不可创建位置。 |
| startup filesystem setup → global hooks | 日志初始化不能成为 capture 或 runApp 的硬依赖。 |
| reporter status → future UI | UI 只需读 availability/path，不应获得 sink 写权限。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-14 | Spoofing | default log location | medium | mitigate | 唯一来源为 path_provider support directory；固定 logs/error.log；无 cwd/exe fallback。 |
| T-02-15 | Denial of Service | startup directory creation/platform future | high | mitigate | stable unavailable delegate → reporter/hooks install first → contained unawaited activation；provider failure 或永久 pending 均不阻断 runApp。 |
| T-02-16 | Elevation of Privilege | future configurable path | low | transfer | SET-02/Phase 4 owns writable-path validation and safe writer reactivation；本 phase 不接受 user path input。 |
| T-02-17 | Information Disclosure | reporter status | low | mitigate | 暴露 identity-stable read-only availability/path listenables，不暴露 writer/sink mutation capability。 |
| T-02-18 | Tampering | late writer activation | high | mitigate | activation only updates the stable delegate's internal target and notifier values；tests assert reporter/effect/listenable object identity never changes。 |
| T-02-SC | Tampering | dependency supply chain | low | accept | 无 package install；path_provider 已锁定并通过 Phase 2 legitimacy audit。 |
</threat_model>

<verification>
  <automated>D:/flutter/bin/flutter test test/diagnostics/ &amp;&amp; D:/flutter/bin/flutter analyze &amp;&amp; D:/flutter/bin/flutter test &amp;&amp; bash tool/audit/kernel_logger_gate.sh</automated>
  <fails_when>focused diagnostics 或 analyze/audit 非零，full suite 出现新增失败，默认路径/启动降级/production effect wiring 不成立，或任何 media_kit source 被修改。</fails_when>
</verification>

<success_criteria>
- Application Support/logs/error.log 是唯一 production default，并在首写前准备完成。
- reporter 与全局 hooks 在 provider/filesystem I/O 之前已安装稳定 delegating effect；成功 activation 后每份新 error/fatal 使用同一 formatter durable append。
- 慢、永久 pending、path/sink setup failure 均不阻断 capture 或 runApp；availability/path 通过同一 read-only listenable identity 对 Phase 3 可读。
- focused startup/location tests、diagnostics、analyze、full tests 和 kernel logger audit 达标，无新增依赖或 media_kit 改动。
</success_criteria>

<output>
Create `.planning/phases/02-trusted-location-file-evidence/02-04-SUMMARY.md` when done.
</output>
