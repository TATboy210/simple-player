---
phase: 24-features-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/player/deferred_player_feature.dart
  - lib/features/player/player_feature.dart
  - lib/features/player/player_view_model.dart
  - lib/features/player/player_services.dart
  - lib/features/player/models/video_processing_state.dart
autonomous: true
requirements: [DOC-46, DOC-51, DOC-52, DOC-53, DOC-54]

must_haves:
  truths:
    - "flutter analyze reports 0 errors and 0 warnings"
    - "flutter test passes with 0 failures"
    - "Each of the 5 features files has a module-level overview comment explaining the pattern it implements and its architectural position"
    - "Each public class has a /// doc comment explaining purpose, parameters, and behavior"
    - "Each non-trivial method has a /// doc comment with parameter descriptions"
    - "Magic numbers have inline Chinese why-explanations"
    - "Non-obvious logic has inline why-comments explaining the rationale"
  artifacts:
    - "lib/features/player/deferred_player_feature.dart (documented)"
    - "lib/features/player/player_feature.dart (documented)"
    - "lib/features/player/player_view_model.dart (documented)"
    - "lib/features/player/player_services.dart (documented)"
    - "lib/features/player/models/video_processing_state.dart (documented)"
  key_links:
    - "PlayerFeature -> PlayerServices -> PlaybackController chain documented"
    - "DeferredPlayerFeature -> PlayerFeature deferred loading pattern documented"
    - "PlayerViewModel -> PlayerServices MVVM separation documented"
    - "VideoProcessingState copyWith + VideoProcessingPatch diff pattern documented"
---

<objective>
Tool verification baseline + document 5 core MVVM layer files (DOC-46, DOC-51~DOC-54) with complete pattern documentation.

Purpose: Establish healthy baseline before documentation work, then document the architectural core of the features layer — the MVVM triad (PlayerFeature/PlayerViewModel/PlayerServices), deferred loading wrapper, and immutable state model.

Output: 5 files with class-level doc comments, method documentation, pattern explanations, and magic number annotations. flutter analyze and flutter test both clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-features-verification/24-CONTEXT.md
@.planning/phases/21-kernel-engine-bridge-documentation/21-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify tool baseline + document core MVVM files (DOC-51~DOC-53)</name>
  <files>
    lib/features/player/deferred_player_feature.dart,
    lib/features/player/player_feature.dart,
    lib/features/player/player_view_model.dart,
    lib/features/player/player_services.dart
  </files>
  <read_first>
    lib/features/player/deferred_player_feature.dart,
    lib/features/player/player_feature.dart,
    lib/features/player/player_view_model.dart,
    lib/features/player/player_services.dart,
    lib/features/player/services/playback_controller.dart,
    lib/kernel/engine/engine_state.dart,
    .planning/phases/21-kernel-engine-bridge-documentation/21-CONTEXT.md,
    .planning/phases/24-features-verification/24-CONTEXT.md
  </read_first>
  <action>
**Step 1: Tool verification baseline (D-04/D-08/D-09)**
Run `flutter analyze` and confirm 0 errors, 0 warnings. If any exist, fix them before proceeding.
Run `flutter test` and confirm all tests pass. If any fail, diagnose and fix before proceeding.

**Step 2: Document `deferred_player_feature.dart` (DOC-46)**
This file implements the Dart `deferred as` loading pattern. Add:
- Module-level overview comment at top of file explaining: what deferred loading is, why it's used (avoid eager import of FvpEngine etc.), how it works with StartupCoordinator
- Class-level `///` doc comment on `DeferredPlayerFeature` explaining: lazy loading wrapper pattern, the deferred import mechanism, error handling strategy
- Method-level doc on `_loadLibrary()` explaining the async loading flow and StartupCoordinator reporting
- Inline why-comment on `player_feature.loadLibrary()` explaining deferred import resolution
- The file already has a class-level doc — enhance it with pattern explanation (what + why + architectural position)

**Step 3: Document `player_feature.dart` (DOC-51)**
This is the MVVM View layer. Add:
- Module-level overview comment explaining: MVVM pattern in this project, PlayerFeature as the View that owns PlayerServices lifecycle, how it differs from PlayerViewModel
- Class-level `///` doc comment on `PlayerFeature` explaining: holds PlayerServices, manages UI state (ready/error/drag-hover), provides callbacks, composes PlayerScreen
- Method-level doc on `_init()` explaining the async initialization sequence and StartupCoordinator integration
- Method-level doc on `_openFile()` explaining FilePicker usage and allowed extensions
- Method-level doc on `_buildPlayerScreen()` explaining how services are wired to PlayerScreen
- Inline why-comment on `debugDumpApp()` explaining it's a debug call (line 174)
- Inline why-comment on the `allowedExtensions` list explaining it covers video + audio formats

**Step 4: Document `player_view_model.dart` (DOC-52)**
This is the MVVM ViewModel layer. Add:
- Module-level overview explaining: extracted business logic from PlayerFeature, ChangeNotifier pattern, separation from BuildContext/Widget
- Class-level `///` doc on `PlayerViewModel` explaining: holds PlayerServices + lifecycle, manages ready/error/drag-hover state, provides business logic methods
- Method-level doc on `init()` explaining async initialization, StartupCoordinator reporting, error handling
- Method-level doc on `openFile()` explaining FilePicker integration
- Method-level doc on `onFilesDropped()` explaining drag-and-drop file handling
- Method-level doc on `onTogglePlayMode()` explaining mode cycling and OSD feedback
- Inline why-comment on `allowedExtensions` static const explaining why it's a static const (shared with PlayerFeature)

**Step 5: Document `player_services.dart` (DOC-53)**
This is the DI container. Add:
- Module-level overview explaining: service container pattern, dependency injection, lifecycle management (create/init/dispose)
- Class-level `///` doc on `PlayerServices` explaining: creates and owns all player services, provides init/dispose lifecycle, no UI state or BuildContext
- Method-level doc on `create()` static factory explaining: async creation pattern, why static factory instead of constructor
- Method-level doc on `init()` explaining: engine creation (with override support), playlist creation, controller creation, settings loading, video processing service creation
- Method-level doc on `dispose()` explaining: disposal order (reverse of init), what each service disposes
- Inline why-comment on `engineOverride` explaining MockEngine debug mode support
- Inline why-comment on `playlistGeneration` ValueNotifier explaining UI rebuild trigger mechanism
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter analyze --no-fatal-infos 2>&1 | tail -5 && flutter test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze reports 0 errors, 0 warnings
    - flutter test passes with 0 failures
    - `grep -c '///' lib/features/player/deferred_player_feature.dart` >= 5
    - `grep -c '///' lib/features/player/player_feature.dart` >= 10
    - `grep -c '///' lib/features/player/player_view_model.dart` >= 10
    - `grep -c '///' lib/features/player/player_services.dart` >= 8
    - Each file has a module-level overview comment (first comment block above imports or class)
    - No new warnings introduced by documentation changes
  </acceptance_criteria>
  <done>
    Tool baseline verified (0 errors, 0 warnings, all tests pass). DeferredPlayerFeature, PlayerFeature, PlayerViewModel, and PlayerServices all have class-level doc comments, method documentation, pattern explanations, and magic number annotations. No regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document video_processing_state.dart (DOC-54)</name>
  <files>lib/features/player/models/video_processing_state.dart</files>
  <read_first>
    lib/features/player/models/video_processing_state.dart,
    lib/features/player/services/video_processing_service.dart,
    lib/kernel/models/aspect_ratio_mode.dart,
    .planning/phases/24-features-verification/24-CONTEXT.md
  </read_first>
  <action>
**Document `video_processing_state.dart` (DOC-54)**
This file defines the immutable state model and diff patch for video processing. Add:
- Module-level overview explaining: immutable value object pattern, why a single ValueNotifier replaces 7 independent ones, copyWith pattern for atomic updates
- Class-level `///` doc on `VideoProcessingState` explaining: immutable data class, all fields with default values, copyWith for updates, equality for diff detection
- Field-level doc comments on each field explaining: what it controls, valid range (brightness/contrast/saturation/hue are -1.0 to 1.0, rotation is degrees, deinterlaceEnabled is bool)
- Inline why-comment on `defaults` static const explaining: zero-state represents no adjustments applied
- Inline why-comment on `copyWith` explaining: creates new instance for immutability, null means "keep current value"
- Class-level `///` doc on `VideoProcessingPatch` explaining: diff tracking between two states, used by VideoProcessingService._syncEngine() to only push changed properties to engine
- Method-level doc on `hasAny` explaining: quick check if any field changed
- Method-level doc on `isColorAdjustment` explaining: groups brightness/contrast/saturation/hue for batch processing
- Inline why-comment on the `Object.hash` call explaining: all 7 fields participate in hash for proper equality
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter analyze --no-fatal-infos 2>&1 | tail -5 && grep -c '///' lib/features/player/models/video_processing_state.dart</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze reports 0 errors, 0 warnings
    - `grep -c '///' lib/features/player/models/video_processing_state.dart` >= 10
    - VideoProcessingState has field-level doc comments on all 7 fields
    - VideoProcessingPatch has class-level doc and method docs
    - Module-level overview explains the immutable value object pattern and why it replaces multiple ValueNotifiers
  </acceptance_criteria>
  <done>
    VideoProcessingState and VideoProcessingPatch both have complete documentation including module-level overview, class-level docs, field-level docs, and pattern explanations. No regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only changes, no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
1. `flutter analyze` — 0 errors, 0 warnings
2. `flutter test` — all tests pass, 0 failures
3. grep -c '///' counts for each file meet minimum thresholds
4. Each file has module-level overview, class-level doc, method docs, and inline why-comments
</verification>

<success_criteria>
- Tool baseline verified (analyze clean, tests passing)
- 5 core MVVM files fully documented with pattern explanations
- No regressions introduced
</success_criteria>

<output>
Create `.planning/phases/24-features-verification/24-01-SUMMARY.md` when done
</output>
