---
phase: 09-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/bridge/window_service.dart
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "短生命周期 calloc 指针在异常路径下也能正确释放"
    - "长生命周期指针 (_savedFrame, _savedMaximizeFrame) 在所有退出路径下都能释放"
    - "dispose() 释放 _savedFrame 和 _savedMaximizeFrame 两个指针"
    - "fullscreen 转换不会因 Win32 API 死锁而永久锁定"
  artifacts:
    - path: "lib/kernel/bridge/window_service.dart"
      provides: "FFI 内存安全加固"
      contains: "using("
  key_links:
    - from: "lib/kernel/bridge/window_service.dart"
      to: "dart:ffi Arena"
      via: "using() 包裹短生命周期分配"
      pattern: "using\\(\\(arena\\)"
---

<objective>
FFI 指针生命周期安全加固

Purpose: 消除 WindowService 中所有 calloc 分配的内存泄漏和 use-after-free 风险，确保短生命周期指针用 Arena 管理、长生命周期指针在所有异常路径释放、fullscreen 转换有超时保护。

Output: 修改后的 window_service.dart，通过 `flutter test` 和 `dart analyze` 验证。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-security-hardening/09-CONTEXT.md
@.planning/phases/09-security-hardening/09-RESEARCH.md
@lib/kernel/bridge/window_service.dart
@lib/kernel/bridge/win32_bindings.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 短生命周期指针 Arena 包裹 + 长生命周期指针 try/finally 异常保护</name>
  <files>lib/kernel/bridge/window_service.dart</files>
  <read_first>
    - lib/kernel/bridge/window_service.dart (当前 328 行，所有 calloc 分配位置)
    - lib/kernel/bridge/win32_bindings.dart (FFI struct 定义: Margins, Rect, MonitorInfo)
  </read_first>
  <behavior>
    - removeBorderImmediate() 的 calloc&lt;Margins&gt;() 用 using((arena) {...}) 包裹（per D-01）
    - _enterFullscreen() 的 calloc&lt;Rect&gt; frame 用 using() 包裹，calloc&lt;Rect&gt; saved(_savedFrame) 保持手动分配但赋值后立即 free frame
    - _enterFullscreen() 的 calloc&lt;Margins&gt; 和 calloc&lt;MonitorInfo&gt; 用 using() 包裹（per D-01）
    - maximize() 的 calloc&lt;Rect&gt; frame 用 using() 包裹，calloc&lt;MonitorInfo&gt; mi 用 using() 包裹（per D-01）
    - _exitFullscreen() 中 _savedFrame 的 free 移到 finally 块，_savedStyle 用 null 检查替代 ! 操作符（per D-02）
    - restore() 中 _savedMaximizeFrame 的 free 移到 finally 块（per D-02）
  </behavior>
  <action>
    1. 添加 `import 'dart:ffi' show using;` 到文件顶部（如果 using 未在现有 import 中）。

    2. removeBorderImmediate() (L54-81): 将 L62-68 的 calloc&lt;Margins&gt; + dwmExtendFrameIntoClientArea + calloc.free 替换为 using((arena) { ... }) 块。Arena 分配 margins，调用 dwmExtendFrameIntoClientArea，Arena 离开作用域自动释放。

    3. _enterFullscreen() (L158-206):
       - L165-173: 用 using() 包裹 frame 分配 + getWindowRect，但 _savedFrame = calloc&lt;Rect&gt;() 保持手动分配（长生命周期，跨方法保存）。在 using 块内读取 frame 值并拷贝到 _savedFrame。
       - L179-185: 将 calloc&lt;Margins&gt; + dwmExtendFrameIntoClientArea + calloc.free 替换为 using() 块。
       - L189-203: 将 calloc&lt;MonitorInfo&gt; + getMonitorInfo + setWindowPos + calloc.free 替换为 using() 块。

    4. _exitFullscreen() (L208-233):
       - L214: 将 `_savedStyle!` 替换为 null 检查: `if (_savedStyle != null) { win32.setWindowLongPtr(hwnd, gwlStyle, _savedStyle!); }`
       - L217-228: 将整个 _savedFrame 使用和释放逻辑包裹在 try/finally 中: try 块执行 setWindowPos，finally 块执行 calloc.free(_savedFrame!) + _savedFrame = null + _savedStyle = null。

    5. maximize() (L253-287):
       - L258-266: 用 using() 包裹 frame 分配 + getWindowRect，_savedMaximizeFrame = calloc&lt;Rect&gt;() 保持手动分配。在 using 块内读取 frame 值拷贝到 _savedMaximizeFrame。
       - L270-285: 将 calloc&lt;MonitorInfo&gt; + getMonitorInfo + setWindowPos + calloc.free 替换为 using() 块。

    6. restore() (L290-308):
       - 将 L295-306 的 setWindowPos + calloc.free + _savedMaximizeFrame = null 包裹在 try/finally 中，确保异常路径也释放 _savedMaximizeFrame。

    注意: using() 在 `dart:ffi` 中需要 `import 'dart:ffi' show using;` 或通过现有 `import 'dart:ffi' hide Size;` 隐式包含。确认 using 可用。
  </action>
  <verify>
    <automated>dart analyze lib/kernel/bridge/window_service.dart 2>&amp;1 | Select-String -Pattern "error" -CaseSensitive</automated>
  </verify>
  <acceptance_criteria>
    - removeBorderImmediate() 中不包含 `calloc<Margins>()`，改用 `arena<Margins>()`
    - _enterFullscreen() 中 margins 和 mi 使用 `arena<Margins>()` / `arena<MonitorInfo>()`
    - maximize() 中 frame 和 mi 使用 `arena<Rect>()` / `arena<MonitorInfo>()`
    - _exitFullscreen() 中 _savedFrame 的 free 在 finally 块内
    - _exitFullscreen() 中 _savedStyle 使用 null 检查而非 ! 操作符
    - restore() 中 _savedMaximizeFrame 的 free 在 finally 块内
    - _savedFrame 和 _savedMaximizeFrame 的手动分配仍使用 calloc（跨方法保存）
    - `dart analyze` 无 error 级别问题
  </acceptance_criteria>
  <done>所有短生命周期 calloc 改为 Arena，长生命周期指针的 free 在 finally 块中，null 检查替代 ! 操作符</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dispose() 补全 + fullscreen 超时保护</name>
  <files>lib/kernel/bridge/window_service.dart</files>
  <read_first>
    - lib/kernel/bridge/window_service.dart (Task 1 完成后的状态)
  </read_first>
  <behavior>
    - dispose() 同时释放 _savedFrame 和 _savedMaximizeFrame（per D-02, D-04）
    - dispose() 取消 _fullscreenTimeout
    - setFullscreen() 有 Timer 5 秒超时保护（per D-03）
    - 超时后 _fullscreenTransitioning 被强制重置为 false
    - 正常完成时 Timer 被取消
  </behavior>
  <action>
    1. 添加实例字段: `Timer? _fullscreenTimeout;` 在 `_resizeDebounce` 之后。

    2. 修改 setFullscreen() (当前 L144-156):
       - 在 `_fullscreenTransitioning = true;` 之后添加:
         _fullscreenTimeout?.cancel();
         _fullscreenTimeout = Timer(const Duration(seconds: 5), () {
           if (_fullscreenTransitioning) {
             debugPrint('WindowService: fullscreen transition timeout, force reset');
             _fullscreenTransitioning = false;
           }
         });
       - 在 finally 块中添加 `_fullscreenTimeout?.cancel();` 在 `_fullscreenTransitioning = false;` 之前。

    3. 修改 dispose() (当前 L316-327):
       - 在 `_resizeDebounce?.cancel();` 之后添加 `_fullscreenTimeout?.cancel();`
       - 在现有 _savedMaximizeFrame 释放之前，添加 _savedFrame 释放:
         if (_savedFrame != null) {
           calloc.free(_savedFrame!);
           _savedFrame = null;
         }
       - 确保 _savedMaximizeFrame 释放后也设为 null:
         if (_savedMaximizeFrame != null) {
           calloc.free(_savedMaximizeFrame!);
           _savedMaximizeFrame = null;
         }
  </action>
  <verify>
    <automated>dart analyze lib/kernel/bridge/window_service.dart 2>&amp;1 | Select-String -Pattern "error" -CaseSensitive</automated>
  </verify>
  <acceptance_criteria>
    - WindowService 类有 `Timer? _fullscreenTimeout` 字段
    - setFullscreen() 方法体包含 `Timer(const Duration(seconds: 5)` 和 `_fullscreenTimeout?.cancel()`
    - dispose() 方法体包含 `if (_savedFrame != null)` 和 `calloc.free(_savedFrame!)`
    - dispose() 方法体包含 `_fullscreenTimeout?.cancel()`
    - `grep -c "_savedFrame" lib/kernel/bridge/window_service.dart` 在 dispose 方法区域内出现 2 次（null 检查 + free）
    - `dart analyze` 无 error 级别问题
  </acceptance_criteria>
  <done>dispose() 同时释放两个长生命周期指针，fullscreen 转换有 5 秒超时保护</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart → Win32 FFI | Dart 代码通过 FFI 调用 Win32 API，指针管理错误可导致崩溃或内存泄漏 |
| 异步操作 → 同步标志 | fullscreen 转换是异步操作，但使用同步 bool 标志控制并发 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Denial of Service | WindowService._savedFrame | mitigate | finally 块保证异常路径释放 + dispose() 兜底 |
| T-09-02 | Denial of Service | WindowService._savedMaximizeFrame | mitigate | finally 块保证异常路径释放 + dispose() 兜底 |
| T-09-03 | Denial of Service | WindowService._fullscreenTransitioning | mitigate | Timer 5 秒超时强制重置 |
| T-09-04 | Tampering | FFI calloc 指针 | mitigate | Arena 自动管理短生命周期指针，手动指针 null 检查 + finally |
| T-09-05 | Elevation of Privilege | _savedStyle! 空安全 | mitigate | null 检查替代 ! 操作符 |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | 本阶段零新依赖 |
</threat_model>

<verification>
- `dart analyze lib/kernel/bridge/window_service.dart` 无 error
- `flutter test` 全部通过（现有测试不受破坏）
- 手动检查: 搜索 `calloc<` 确认所有短生命周期分配在 using() 块内
- 手动检查: 搜索 `calloc.free` 确认长生命周期 free 在 finally 块内
</verification>

<success_criteria>
- 0 处裸 calloc 短生命周期分配（全部改为 Arena）
- dispose() 释放 _savedFrame 和 _savedMaximizeFrame 两个指针
- fullscreen 转换有 5 秒超时保护
- dart analyze 无 error
- flutter test 通过
</success_criteria>

<output>
创建 `.planning/phases/09-security-hardening/09-01-SUMMARY.md` when done
</output>
