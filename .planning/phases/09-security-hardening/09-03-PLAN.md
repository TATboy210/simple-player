---
phase: 09-security-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified: []
autonomous: false
requirements:
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "FFI 指针在异常路径和正常路径下都能正确释放"
    - "fullscreen 转换超时保护正常工作"
    - "PathValidator 正确拒绝畸形 URL 和控制字符路径"
    - "所有现有测试继续通过"
  artifacts: []
  key_links: []
---

<objective>
Phase 9 安全加固 — 端到端验证

Purpose: 确认 Plan 01 和 Plan 02 的安全加固正确实现，无回归。

Output: 用户确认验证通过，Phase 9 标记完成。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-security-hardening/09-CONTEXT.md
@lib/kernel/bridge/window_service.dart
@lib/kernel/services/path_validator.dart
@test/kernel/services/path_validator_test.dart
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 安全加固验证</name>
  <what-built>
    Plan 01: WindowService FFI 安全加固
    - 短生命周期 calloc 改为 Arena (using)
    - 长生命周期指针 (_savedFrame, _savedMaximizeFrame) finally 块保护
    - dispose() 补全 _savedFrame 释放
    - fullscreen 转换 5 秒超时保护

    Plan 02: PathValidator 输入验证强化
    - HTTP/HTTPS URL 结构化验证 (Uri.tryParse)
    - 控制字符过滤 (0x01-0x1F)
    - RTSP/RTMP 保持跳过验证
  </what-built>
  <how-to-verify>
    1. 运行 `flutter test` — 确认全部通过
    2. 运行 `dart analyze` — 确认无 error
    3. 检查 lib/kernel/bridge/window_service.dart:
       - 搜索 `using((arena)` — 确认存在（Arena 包裹短生命周期分配）
       - 搜索 `finally` — 确认 _savedFrame 和 _savedMaximizeFrame 的 free 在 finally 块中
       - 搜索 `_fullscreenTimeout` — 确认 Timer 超时保护存在
       - 检查 dispose() — 确认同时释放 _savedFrame 和 _savedMaximizeFrame
    4. 检查 lib/kernel/services/path_validator.dart:
       - 搜索 `Uri.tryParse` — 确认 HTTP/HTTPS 结构化验证
       - 搜索 `_hasControlCharacters` — 确认控制字符过滤方法存在
       - 确认 RTSP/RTMP 仍返回 null（不阻断）
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 验证阶段 — 无新信任边界 | 仅确认已有修复正确性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | npm/pip/cargo installs | accept | 本阶段零新依赖 |
</threat_model>

<verification>
- flutter test 全部通过
- dart analyze 无 error
- 用户确认安全加固正确
</verification>

<success_criteria>
- 用户 approved
- 全部测试通过
- 零 error 分析结果
</success_criteria>

<output>
创建 `.planning/phases/09-security-hardening/09-03-SUMMARY.md` when done
</output>
