# Continue Here — Phase 9: Security Hardening

**Paused:** 2026-05-30 14:30 CST
**Branch:** fix/window-startup

## Status

Phase 9 planning is **complete**. 3 plans ready to execute.

| Plan | Wave | Status | Description |
|------|------|--------|-------------|
| 09-01 | 1 | ready | FFI Pointer Lifecycle Safety (window_service.dart) |
| 09-02 | 1 | ready | Input Validation Hardening (path_validator.dart) |
| 09-03 | 2 | ready | E2E Security Verification (depends on 09-01 + 09-02) |

## Key Decisions Made

1. **Arena vs try/finally boundary**: Short-lived pointers (margins, frame reads, MonitorInfo) → `using((arena) {...})`. Long-lived pointers (_savedFrame, _savedMaximizeFrame) → manual calloc + try/finally.
2. **dispose() memory leak**: `_savedFrame` is never freed in dispose() — must add.
3. **Fullscreen timeout**: 5-second Timer to prevent permanent `_fullscreenTransitioning` lock.
4. **URL validation**: HTTP/HTTPS need `Uri.tryParse()`; RTSP/RTMP/SRT/UDP/TCP keep prefix passthrough.

## Resume Command

```
/gsd:execute-phase 9
```

This will run all 3 plans sequentially (Wave 1 parallel, then Wave 2).
