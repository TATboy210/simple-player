---
phase: 21-kernel-engine-bridge-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/engine/d3d11_configurator.dart
  - lib/kernel/engine/subtitle_configurator.dart
  - lib/kernel/engine/volume_controller.dart
  - lib/kernel/engine/track_manager.dart
  - lib/kernel/engine/fvp_callback_handler.dart
  - lib/kernel/engine/video_effect_controller.dart
  - lib/kernel/engine/engine_prewarm.dart
  - lib/kernel/engine/network_configurator.dart
  - lib/kernel/engine/renderer_config.dart
  - lib/kernel/engine/track_control.dart
  - lib/kernel/engine/video_effects.dart
  - lib/kernel/engine/open_result.dart
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-06
  - DOC-07
  - DOC-08
  - DOC-09
  - DOC-10
  - DOC-11
  - DOC-12
must_haves:
  truths:
    - "Every Engine layer file has a class-level /// doc comment explaining purpose and key behavior"
    - "Every public method has a /// doc comment with parameter descriptions"
    - "All magic numbers have inline why-explanations (Chinese) per D-09/D-10"
    - "flutter analyze produces no new warnings or errors"
  artifacts:
    - lib/kernel/engine/d3d11_configurator.dart
    - lib/kernel/engine/subtitle_configurator.dart
    - lib/kernel/engine/volume_controller.dart
    - lib/kernel/engine/track_manager.dart
    - lib/kernel/engine/fvp_callback_handler.dart
    - lib/kernel/engine/video_effect_controller.dart
    - lib/kernel/engine/engine_prewarm.dart
    - lib/kernel/engine/network_configurator.dart
    - lib/kernel/engine/renderer_config.dart
    - lib/kernel/engine/track_control.dart
    - lib/kernel/engine/video_effects.dart
    - lib/kernel/engine/open_result.dart
  key_links:
    - "Engine files are exported via engine_state.dart barrel — doc comments appear in IDE tooltips"
    - "D3D11 parameters inherit from mpv property system — comments must explain mpv concepts"
    - "FFmpeg filter syntax (af parameter) needs filter chain structure explanation"
---

<objective>
Add or improve documentation comments on all 12 Engine layer files.

Purpose: Enable new developers to understand each file's responsibility and key logic without running the code. D3D11/FFmpeg/mpv domain knowledge must be captured in comments per D-01~D-04.

Output: 12 Engine layer .dart files with complete class-level doc comments, method-level docs, and magic number explanations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-kernel-engine-bridge-documentation/21-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document D3D11/Video/Network engine files (6 files)</name>
  <files>
    lib/kernel/engine/d3d11_configurator.dart,
    lib/kernel/engine/subtitle_configurator.dart,
    lib/kernel/engine/volume_controller.dart,
    lib/kernel/engine/video_effect_controller.dart,
    lib/kernel/engine/network_configurator.dart,
    lib/kernel/engine/engine_prewarm.dart
  </files>
  <read_first>
    lib/kernel/engine/d3d11_configurator.dart,
    lib/kernel/engine/subtitle_configurator.dart,
    lib/kernel/engine/volume_controller.dart,
    lib/kernel/engine/video_effect_controller.dart,
    lib/kernel/engine/network_configurator.dart,
    lib/kernel/engine/engine_prewarm.dart,
    .planning/phases/21-kernel-engine-bridge-documentation/21-CONTEXT.md
  </read_first>
  <action>
    Audit each file's existing comments (D-12: A/B/C classification), then supplement per decisions D-01~D-11.

    **d3d11_configurator.dart (DOC-01):**
    - B-class: has basic doc comments, needs deeper D3D11/mpv domain explanations
    - Add why-explanation to `defaultVideoDecoders` constant: explain what `shader_resource=1` does (GPU colorspace YUV→RGB conversion), why D3D11 is first priority (lowest latency on Windows), why NVDEC second (NVIDIA hardware), FFmpeg as fallback
    - Add why-explanation to `_ffmpegDecoderThreads = '2'`: explain tradeoff (2 threads saves 10-30MB RAM vs default CPU core count, sufficient for most 1080p content)
    - Add why-explanation to `_maxBufferFrames = '3'`: explain why 2 is too tight (frame drops on seek), 3 provides safety margin for 1080p
    - Add why-explanation to `reader.starts_with_key = '1'`: explain this forces decoder to start from keyframe, avoiding green frame artifacts
    - Class doc comment: expand to explain the mpv property system origin (these are mpv options exposed through fvp/MDK)

    **subtitle_configurator.dart (DOC-02):**
    - B-class: has basic doc comments, needs subtitle format and FFmpeg filter explanations
    - Add why-explanation to `subtitle.external` property: explain MDK auto-detects format (SRT/ASS/SSA/VTT) from file extension and content
    - Add why-explanation to `subtitle.delay` property: explain positive=delay subtitle, negative=advance subtitle (relative to audio)
    - Expand `setEqualizer` doc comment: explain FFmpeg `af=lavfi=[...]` filter chain syntax — `af` is audio filter property, `lavfi` is the FFmpeg filtergraph bridge, filter parameters are comma-separated key=value pairs inside brackets

    **volume_controller.dart (DOC-03):**
    - B-class: has basic doc comments, needs volume curve explanation
    - Add why-explanation to the auto-mute logic: explain why clamping to 0 triggers mute (prevents audible noise at very low volumes), and why raising from 0 auto-unmutes (UX convenience)
    - Note that volume 0.0-1.0 maps linearly to MDK's internal range (not logarithmic — human perception is logarithmic, but MDK handles the curve internally)

    **video_effect_controller.dart (DOC-06):**
    - B-class: has basic doc comments, needs MDK effect API explanation
    - Add why-explanation to `setVideoEffect` with array parameter: explain MDK expects effect values as single-element array (historical API design from mpv)
    - Add why-explanation to `validRotationDegrees`: explain MDK only supports 0/90/180/270 (hardware rotation steps, no arbitrary angles)
    - Add why-explanation to deinterlace `yadif=mode=send_frame:deint=all`: explain yadif is Yet Another DeInterlacing Filter, `send_frame` outputs one frame per input (vs field), `deint=all` deinterlaces both fields (vs interlaced-only)

    **network_configurator.dart (DOC-08):**
    - B-class: has Chinese doc comments, needs magic number why-explanations per D-09
    - Add inline why to `_networkTimeoutMs = 10000`: // 10秒超时 — 网络波动容忍阈值，太短会误断，太长用户等太久
    - Add inline why to `_networkProbeSize = 1000000`: // 1MB 探测 — 足够识别大多数容器格式和码率，减少首帧延迟
    - Add inline why to `_networkAnalyzeDurationUs = 5000000`: // 5秒分析 — 平衡流识别准确性和启动速度
    - Add inline why to `_rtspProbeSize = 500000`: // 500KB RTSP 快速探测 — 实时流需要更小探测以降低延迟
    - Add why-explanation to `fflags +nobuffer`: explain this disables FFmpeg's internal buffering for lower latency
    - Add why-explanation to `fpsprobesize 0`: explain this skips FPS probing (uses container metadata), saving 1-2s startup
    - Add why-explanation to `avioflags direct`: explain this bypasses FFmpeg's IO layer buffering
    - Add why-explanation to `setBufferRange(min:0, max:0, drop:true)`: explain min/max buffer frames, drop=true allows frame dropping under pressure

    **engine_prewarm.dart (DOC-07):**
    - A-class: already has excellent tier model explanation, minimal changes needed
    - Verify the Tier model explanation is accurate and complete
    - Add why-explanation to `setProperty('log', 'warning')`: // 只输出警告和错误，减少预热期间 I/O 开销

    **Language rules (D-05~D-07):**
    - `///` doc comments: English (internationalization-friendly)
    - `//` inline why-explanations: Chinese (existing codebase convention)
    - Do NOT use `@see` references (D-08) — explain concepts inline

    **Magic number rules (D-09~D-11):**
    - Keep original values + add inline why comment
    - Only extract to constant if same value appears 3+ times in same file
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -c '///' lib/kernel/engine/d3d11_configurator.dart lib/kernel/engine/subtitle_configurator.dart lib/kernel/engine/volume_controller.dart lib/kernel/engine/video_effect_controller.dart lib/kernel/engine/network_configurator.dart lib/kernel/engine/engine_prewarm.dart | grep -v ':0$'</automated>
  </verify>
  <acceptance_criteria>
    - d3d11_configurator.dart: class doc comment mentions "mpv property system" or "MDK properties"; `defaultVideoDecoders` has inline comment explaining shader_resource=1, D3D11 priority, NVDEC, FFmpeg fallback; `_ffmpegDecoderThreads` has why-comment about RAM savings; `_maxBufferFrames` has why-comment about seek safety; `reader.starts_with_key` has why-comment about keyframe seeking
    - subtitle_configurator.dart: `setExternalSubtitle` mentions format auto-detection (SRT/ASS/SSA/VTT); `setEqualizer` doc comment explains `af=lavfi=[...]` filter chain syntax
    - volume_controller.dart: auto-mute logic has inline why-comment about low-volume noise prevention
    - video_effect_controller.dart: `setEffect` mentions MDK array parameter convention; `setDeinterlace` explains yadif parameters
    - network_configurator.dart: all 4 numeric constants have inline why-comments in Chinese; `fflags +nobuffer` has why-comment; `setBufferRange` parameters explained
    - engine_prewarm.dart: log level setting has why-comment about I/O overhead
    - All `///` doc comments are in English (D-05)
    - All `//` why-explanations are in Chinese (D-07)
    - `flutter analyze` shows no new warnings
  </acceptance_criteria>
  <done>6 Engine files have complete English doc comments and Chinese inline why-explanations for all magic numbers and non-obvious logic</done>
</task>

<task type="auto">
  <name>Task 2: Document Track/State/Model engine files (6 files)</name>
  <files>
    lib/kernel/engine/track_manager.dart,
    lib/kernel/engine/fvp_callback_handler.dart,
    lib/kernel/engine/renderer_config.dart,
    lib/kernel/engine/track_control.dart,
    lib/kernel/engine/video_effects.dart,
    lib/kernel/engine/open_result.dart
  </files>
  <read_first>
    lib/kernel/engine/track_manager.dart,
    lib/kernel/engine/fvp_callback_handler.dart,
    lib/kernel/engine/renderer_config.dart,
    lib/kernel/engine/track_control.dart,
    lib/kernel/engine/video_effects.dart,
    lib/kernel/engine/open_result.dart,
    .planning/phases/21-kernel-engine-bridge-documentation/21-CONTEXT.md
  </read_first>
  <action>
    Audit each file's existing comments (D-12: A/B/C classification), then supplement per decisions D-01~D-11.

    **track_manager.dart (DOC-04):**
    - B-class: has Chinese doc comments, needs track management strategy explanation
    - Expand class doc comment (English): explain that MDK uses index-based track selection (not track IDs), tracks are numbered 0..N in order reported by the demuxer
    - Add why-explanation to `switchAudioTrack` bounds check: explain MDK crashes on out-of-range track index, defensive check prevents crash
    - Add why-explanation to `switchSubtitleTrack(-1)`: explain passing empty list `[]` disables subtitle output (MDK convention, not "track -1")
    - Add why-explanation to `toggleSubtitle` logic: explain it cycles between "first subtitle track" and "no subtitle" (not all tracks), because most content has one subtitle track
    - Add English doc comments to `getAudioTracks()`, `getSubtitleTracks()`, `activeAudioTracks` getter

    **fvp_callback_handler.dart (DOC-05):**
    - B-class: has Chinese doc comments, needs callback event type and state transition explanation
    - Expand class doc comment: explain the three callback sources (onStateChanged for play/pause/stop, onMediaStatus for buffering/end), and why main-thread scheduling is required (Flutter UI thread safety — ValueNotifier updates must happen on main isolate)
    - Add why-explanation to `_scheduleOnMain`: explain `SchedulerBinding.addPostFrameCallback` ensures ValueNotifier updates happen during the frame callback phase, preventing mid-frame rebuilds
    - Add why-explanation to buffering state logic: explain the buffering→playing/paased transition — when buffering ends, we check `_player.state` to determine the correct target state (not just unconditionally setting playing)
    - Add why-explanation to `mapMdkState`: explain MDK only has 3 states (stopped/playing/paused), while MediaState has 6 (idle/loading/playing/paused/buffering/completed) — the extra states come from onMediaStatus

    **renderer_config.dart (DOC-09):**
    - C-class (marker mixin): expand to explain the capability pattern
    - Expand class doc comment: explain this is a capability marker for Dart pattern matching — `if (engine case RendererConfig rc)` allows safe runtime capability checks without casting. Used by UI code that needs renderer-specific features (e.g., D3D11 sync toggle)
    - Add usage example in doc comment: `/// Usage: if (engine case RendererConfig rc) { rc.setSyncEnabled(true); }`

    **track_control.dart (DOC-10):**
    - C-class (marker mixin): expand to explain the capability pattern
    - Expand class doc comment: explain this marks engines that support track switching. The actual methods (switchAudioTrack, switchSubtitleTrack, toggleSubtitle) are defined on EngineState base class — this mixin exists purely for type-level pattern matching
    - Note: methods are on EngineState because all FvpEngine instances support track control; the marker enables future MockEngine to omit track support

    **video_effects.dart (DOC-11):**
    - C-class (marker mixin): expand to explain the capability pattern
    - Expand class doc comment: explain this marks engines that support video effects (brightness, contrast, hue, saturation, rotation, aspect ratio, deinterlace). Used for runtime capability checks in settings UI

    **open_result.dart (DOC-12):**
    - B-class: has Chinese doc comments, needs sealed class pattern explanation
    - Expand class doc comment (English): explain sealed class pattern — Dart 3 exhaustive switch ensures all cases handled. `OpenSuccess` carries MediaInfo (codec, resolution, duration), `OpenError` carries error type and human-readable message
    - Add doc comments to `OpenSuccess.mediaInfo` and `OpenError.type`/`OpenError.message` fields
    - Explain `MediaErrorType` categorization in `OpenError` doc comment

    **Language rules (D-05~D-07):**
    - `///` doc comments: English
    - `//` inline why-explanations: Chinese
    - Do NOT use `@see` references (D-08)

    **Magic number rules (D-09~D-11):**
    - Keep original values + add inline why comment
    - Only extract to constant if same value appears 3+ times in same file
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && grep -c '///' lib/kernel/engine/track_manager.dart lib/kernel/engine/fvp_callback_handler.dart lib/kernel/engine/renderer_config.dart lib/kernel/engine/track_control.dart lib/kernel/engine/video_effects.dart lib/kernel/engine/open_result.dart | grep -v ':0$'</automated>
  </verify>
  <acceptance_criteria>
    - track_manager.dart: class doc comment mentions "index-based track selection" or "MDK track numbering"; `switchSubtitleTrack` has why-comment about empty list convention; `toggleSubtitle` explains cycling behavior
    - fvp_callback_handler.dart: class doc comment explains three callback sources and main-thread requirement; `_scheduleOnMain` has why-comment about frame callback phase; buffering state logic has why-comment about player.state check
    - renderer_config.dart: class doc comment explains capability marker pattern with usage example
    - track_control.dart: class doc comment explains type-level pattern matching purpose
    - video_effects.dart: class doc comment explains runtime capability check purpose
    - open_result.dart: class doc comment mentions "sealed class" and "exhaustive switch"; field-level doc comments exist
    - All `///` doc comments are in English (D-05)
    - All `//` why-explanations are in Chinese (D-07)
    - `flutter analyze` shows no new warnings
  </acceptance_criteria>
  <done>6 Engine files have complete English doc comments, capability marker explanations, and Chinese inline why-explanations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Documentation-only changes — no new inputs, outputs, or control flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | N/A | All files | none | accept | Documentation-only changes introduce no new attack surface. No code behavior modification. |
</threat_model>

<verification>
1. `flutter analyze` — no new warnings or errors introduced
2. Grep check: every target file has at least 3 `///` doc comment lines
3. Spot check: D3D11 configurator mentions mpv/MDK property system
4. Spot check: network_configurator magic numbers have Chinese why-comments
5. Spot check: mixin markers (renderer_config, track_control, video_effects) have capability pattern explanation
</verification>

<success_criteria>
- 12 Engine layer files all have class-level `///` doc comments explaining purpose
- All public methods have `///` doc comments with parameter descriptions
- All magic numbers have inline Chinese why-explanations per D-09/D-10
- D3D11/FFmpeg/Win32 domain knowledge captured in comments per D-01~D-04
- `flutter analyze` produces no new warnings or errors
</success_criteria>

<output>
Create `.planning/phases/21-kernel-engine-bridge-documentation/21-01-SUMMARY.md` when done
</output>
