---
phase: 10-window-optimization
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/kernel/bridge/window_bootstrap.dart
  - test/kernel/bridge/window_bootstrap_test.dart
  - test/helpers/fake_screen_retriever.dart
autonomous: true
requirements:
  - WIN-04
must_haves:
  truths:
    - "WindowBootstrap.restoreOrCenter sets position and size when saved geometry exists and is on a visible display"
    - "WindowBootstrap.restoreOrCenter centers window when no saved position (windowX/windowY null)"
    - "WindowBootstrap.restoreOrCenter centers window when saved position is off-screen (no display match with 100px overlap)"
    - "WindowBootstrap.clearFullscreenIfSaved clears isFullscreen flag in SettingsStore"
    - "WindowBootstrap fails open when screen_retriever throws (centers window instead of crashing)"
  artifacts:
    - path: "lib/kernel/bridge/window_bootstrap.dart"
      provides: "Static utility class for startup window geometry restoration"
      exports: ["WindowBootstrap.restoreOrCenter", "WindowBootstrap.clearFullscreenIfSaved"]
    - path: "test/kernel/bridge/window_bootstrap_test.dart"
      provides: "Unit tests for WindowBootstrap"
      min_lines: 80
    - path: "test/helpers/fake_screen_retriever.dart"
      provides: "Test double for screen_retriever with configurable display list"
  key_links:
    - from: "lib/kernel/bridge/window_bootstrap.dart"
      to: "package:screen_retriever"
      via: "screenRetriever.getAllDisplays()"
      pattern: "screenRetriever\\.getAllDisplays"
    - from: "lib/kernel/bridge/window_bootstrap.dart"
      to: "package:window_manager"
      via: "windowManager.setPosition/setSize/center/ensureVisible"
      pattern: "windowManager\\.(setPosition|setSize|center|ensureVisible)"
    - from: "lib/kernel/bridge/window_bootstrap.dart"
      to: "lib/kernel/persistence/settings_store.dart"
      via: "SettingsStore.saveIsFullscreen"
      pattern: "SettingsStore\\.saveIsFullscreen"
---

<objective>
Create WindowBootstrap utility class that reads saved geometry from SettingsStore, validates against current monitor topology via screen_retriever, and applies position/size before windowManager.show().

Purpose: The current startup flow uses hardcoded WindowOptions(960x540) and never restores persisted geometry. WindowBootstrap bridges SettingsStore persistence and window_manager display API.

Output: window_bootstrap.dart (static utility), window_bootstrap_test.dart (TDD), fake_screen_retriever.dart (test helper)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-window-optimization/10-CONTEXT.md
@.planning/phases/10-window-optimization/10-RESEARCH.md
@.planning/phases/10-window-optimization/10-PATTERNS.md

<interfaces>
From lib/kernel/persistence/settings_store.dart:
```dart
class SettingsStore {
  static Future<void> saveIsFullscreen(bool value);
  // RC-3 sanitized: windowWidth, windowHeight, windowX, windowY
}
```

From lib/kernel/models/app_settings.dart:
```dart
class AppSettings {
  final double windowWidth;     // RC-3 sanitized, default 1280
  final double windowHeight;    // RC-3 sanitized, default 720
  final double? windowX;        // null = no saved position
  final double? windowY;        // null = no saved position
  final bool isMaximized;
  final bool isFullscreen;
}
```

From package:window_manager:
```dart
// Singleton accessed via `windowManager`
Future<void> windowManager.setPosition(Offset position);
Future<void> windowManager.setSize(Size size);
Future<void> windowManager.center();
Future<void> windowManager.ensureVisible();
```

From package:screen_retriever:
```dart
// Singleton accessed via `screenRetriever`
Future<List<Display>> screenRetriever.getAllDisplays();
// Display has: visiblePosition (Offset?), visibleSize (Size?)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FakeScreenRetriever test helper</name>
  <files>test/helpers/fake_screen_retriever.dart</files>
  <read_first>test/helpers/fake_window_service.dart</read_first>
  <behavior>
    - FakeScreenRetriever has configurable `displays` list and `shouldThrow` flag
    - getAllDisplays() increments callCount and returns displays or throws
    - Display objects have visiblePosition (Offset) and visibleSize (Size)
  </behavior>
  <action>Create test/helpers/fake_screen_retriever.dart following the FakeWindowService pattern (test/helpers/fake_window_service.dart). The fake provides:
  - `List<Display> displays` configurable list
  - `bool shouldThrow` flag (default false)
  - `int getAllDisplaysCallCount` tracking
  - `Future<List<Display>> getAllDisplays()` method that returns configured displays or throws if shouldThrow is true
  Note: screen_retriever's Display class is from `package:screen_retriever/src/models/display.dart`. Import the package and use the real Display type. If Display constructor is not public, use a minimal wrapper or mock approach.</action>
  <verify>
    <automated>dart analyze test/helpers/fake_screen_retriever.dart</automated>
  </verify>
  <acceptance_criteria>
    - File exists at test/helpers/fake_screen_retriever.dart
    - Class FakeScreenRetriever has `displays` field (List), `shouldThrow` field (bool), `getAllDisplaysCallCount` field (int)
    - `getAllDisplays()` returns Future<List<Display>>
    - `dart analyze` exits 0 on the file
  </acceptance_criteria>
  <done>FakeScreenRetriever compiles and provides configurable display list with call tracking</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create WindowBootstrap with TDD — restoreOrCenter + clearFullscreenIfSaved</name>
  <files>lib/kernel/bridge/window_bootstrap.dart, test/kernel/bridge/window_bootstrap_test.dart</files>
  <read_first>lib/kernel/bridge/window_service.dart, lib/kernel/persistence/settings_store.dart, lib/kernel/models/app_settings.dart, test/kernel/persistence/settings_store_test.dart, test/helpers/fake_screen_retriever.dart</read_first>
  <behavior>
    - restoreOrCenter with valid saved position on visible display: calls setPosition(savedX, savedY) then setSize(savedWidth, savedHeight) then ensureVisible()
    - restoreOrCenter with null windowX/windowY: calls setSize(savedWidth, savedHeight) then center() then ensureVisible()
    - restoreOrCenter with saved position off-screen (no display has 100px overlap): calls setSize then center then ensureVisible
    - restoreOrCenter when screen_retriever throws: fails open, calls setSize then center then ensureVisible
    - clearFullscreenIfSaved when isFullscreen=true: calls SettingsStore.saveIsFullscreen(false)
    - clearFullscreenIfSaved when isFullscreen=false: does nothing
    - _isOnVisibleDisplay returns true when window overlaps display by >= 100px in both dimensions
    - _isOnVisibleDisplay returns false when overlap < 100px in either dimension
  </behavior>
  <action>Create lib/kernel/bridge/window_bootstrap.dart as a static utility class. Key design:
  - `class WindowBootstrap` with private constructor (no instantiation)
  - `static Future<void> restoreOrCenter(AppSettings settings)` — main entry point
  - `static Future<void> clearFullscreenIfSaved(AppSettings settings)` — per D-02, clears isFullscreen on startup
  - `static Future<bool> _isOnVisibleDisplay(AppSettings settings)` — checks if saved position overlaps any display by >= 100px
  - `@visibleForTesting static Future<List<Display>> Function()? _displayProviderOverride` — testability seam for screen_retriever
  - `@visibleForTesting static void setDisplayProvider(...)` and `resetDisplayProvider()`
  - All window_manager calls are async; all screen_retriever calls go through `_getDisplays()` which uses override if set
  - Error handling: try/catch on screen_retriever with fail-open (return true, let ensureVisible handle it)
  - Error handling: try/catch on window_manager calls with debugPrint fallback

  For TDD: Write test/kernel/bridge/window_bootstrap_test.dart FIRST (RED), then implement WindowBootstrap (GREEN). Test file uses SharedPreferences.setMockInitialValues for SettingsStore, and setDisplayProvider for screen_retriever mock. Follow settings_store_test.dart pattern (TestWidgetsFlutterBinding.ensureInitialized, setUp with SharedPreferences mock).</action>
  <verify>
    <automated>flutter test test/kernel/bridge/window_bootstrap_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/kernel/bridge/window_bootstrap_test.dart exists with >= 6 test cases
    - All tests pass
    - lib/kernel/bridge/window_bootstrap.dart exports class WindowBootstrap
    - WindowBootstrap.restoreOrCenter is static, accepts AppSettings, returns Future<void>
    - WindowBootstrap.clearFullscreenIfSaved is static, accepts AppSettings, returns Future<void>
    - _isOnVisibleDisplay checks >= 100px overlap in both width and height
    - Display provider override pattern exists for testability (@visibleForTesting)
    - dart analyze exits 0 on both files
  </acceptance_criteria>
  <done>WindowBootstrap class with restoreOrCenter + clearFullscreenIfSaved, fully tested with 6+ passing test cases covering all geometry restore scenarios</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsStore -> WindowBootstrap | Persisted geometry values cross into window positioning (validated by RC-3 sanitization) |
| screen_retriever -> WindowBootstrap | Monitor topology data from OS platform channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | SettingsStore geometry values | accept | RC-3 sanitization already in SettingsStore (NaN/Infinity/negative protection) |
| T-10-02 | Evasion | Off-screen window positioning | mitigate | _isOnVisibleDisplay with 100px overlap check + ensureVisible() fallback |
| T-10-03 | Denial of Service | Fullscreen crash lock | mitigate | D-02: clearFullscreenIfSaved clears isFullscreen on startup |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `flutter test test/kernel/bridge/window_bootstrap_test.dart` — all tests green
- `dart analyze lib/kernel/bridge/window_bootstrap.dart` — no issues
- `dart analyze test/helpers/fake_screen_retriever.dart` — no issues
</verification>

<success_criteria>
- WindowBootstrap class exists with static restoreOrCenter and clearFullscreenIfSaved methods
- 6+ test cases pass covering: valid position restore, null position center, off-screen fallback, exception fail-open, fullscreen clear, display overlap math
- FakeScreenRetriever test helper exists with configurable behavior and call tracking
- All files pass dart analyze
</success_criteria>

<output>
Create `.planning/phases/10-window-optimization/10-01-SUMMARY.md` when done
</output>
