---
context: phase
phase: 10-window-optimization
task: null
total_tasks: null
status: paused
last_updated: "2026-05-30T17:20:00+08:00"
---

# Phase 10: Window Optimization — Pause Point (Gap Closure Ready)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree cleanup data loss | `git worktree remove` deleted merged commits from history — Wave 1 (10-01 + 10-02) work lost | blocking | Verify commits exist in main branch reflog BEFORE removing worktrees. Use `git log --oneline` to confirm merge persisted. |

<current_state>
Phase 10 gap closure planning COMPLETE. 2 gap plans written and ready for execution.

- Branch: fix/window-startup
- Tests: 608 passing
- dart analyze: 0 errors
- 10-03 commits: 91fde04 (WindowBootstrap wiring), 19d3eca (singleton fix)
- 10-02-GAP-PLAN.md: written (onWindowClose + _saveGeometryImmediate)
- 10-01-GAP-PLAN.md: written (WindowBootstrap tests)
- Gap plans NOT yet executed
</current_state>

<completed_work>

Phase 10 Wave 2 — COMPLETE:
- **10-03** — WindowBootstrap wired into main.dart startup + WindowService singleton injection
  - `lib/kernel/bridge/window_bootstrap.dart` created (restoreOrCenter + clearFullscreenIfSaved)
  - `lib/main.dart` updated (SettingsStore prewarm, WindowBootstrap calls, removed hardcoded WindowOptions size/center)
  - `lib/features/player/player_services.dart` — constructor injection `required this.windowService`
  - `lib/features/player/player_feature.dart` + `deferred_player_feature.dart` — pass windowService through
  - `lib/app.dart` — passes `_windowService` to DeferredPlayerFeature

Phase 10 Gap Closure — PLANNED (not executed):
- **10-02-GAP** — onWindowClose + _saveGeometryImmediate plan written
- **10-01-GAP** — WindowBootstrap test suite plan written

</completed_work>

<remaining_work>

Execute gap closure:
1. Run 10-02-GAP: Add `onWindowClose` override + `_saveGeometryImmediate` in `window_service.dart` (~15 lines)
2. Run 10-01-GAP: Create `test/kernel/bridge/window_bootstrap_test.dart` with 6+ test cases (~30min)
3. Verify: 608+ tests pass, dart analyze clean
4. Phase 10 complete

</remaining_work>

<decisions_made>

- WindowBootstrap uses `PlatformDispatcher.instance.views` (not screen_retriever) — consistent with existing GeometryStore
- WindowService singleton injection via constructor — fixes double instantiation race
- WindowOptions no longer has hardcoded size/center — WindowBootstrap handles geometry
- D-02: Do NOT auto-restore fullscreen on startup (isFullscreen cleared by clearFullscreenIfSaved)
- Gap closure via /gsd:execute-phase 10 --gaps-only

</decisions_made>

<blockers>
None — gap plans are ready for execution.
</blockers>

## Required Reading (in order)
1. `.planning/phases/10-window-optimization/10-02-GAP-PLAN.md` — onWindowClose gap plan (execute first, HIGH priority)
2. `.planning/phases/10-window-optimization/10-01-GAP-PLAN.md` — WindowBootstrap tests gap plan
3. `.planning/phases/10-window-optimization/10-03-SUMMARY.md` — what 10-03 built
4. `.planning/phases/10-window-optimization/10-VERIFICATION.md` — gap analysis
5. `.planning/phases/10-window-optimization/10-CONTEXT.md` — user decisions

## Infrastructure State
- Flutter: D:\flutter\bin\flutter (not in PATH)
- Branch: fix/window-startup
- 608 tests passing, dart analyze clean
- WindowBootstrap at lib/kernel/bridge/window_bootstrap.dart (86 lines, created by 10-03)

<context>
Phase 10 gap closure planning complete. 2 gap plans written to disk. Ready to execute via /gsd:execute-phase 10 --gaps-only. Both plans are small (1 task each), Wave 1 parallel execution. 10-02-GAP is HIGH priority (onWindowClose immediate save). 10-01-GAP is MEDIUM priority (tests).
</context>

<next_action>
Run `/gsd:execute-phase 10 --gaps-only` to execute both gap closure plans. Or manually implement onWindowClose (~15 lines in window_service.dart) and create tests.
</next_action>
