---
context: phase
phase: 10-window-optimization
task: null
total_tasks: null
status: paused
last_updated: "2026-05-30T19:30:00+08:00"
---

# Phase 10: Window Optimization — Pause Point

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Uri.tryParse empty authority | `Uri.tryParse('http://')` returns hasAuthority=true with empty host | advisory | Always check `host.isNotEmpty` alongside `hasAuthority` |
| Arena not available | `dart:ffi` `using()` doesn't support Arena callback pattern in current version | advisory | Use try/finally blocks for FFI pointer cleanup |

<current_state>
Phase 9 complete. Phase 10 planning initiated — research agent spawned but session ended before agent completed.

- Branch: fix/window-startup
- Tests: 608 passing
- dart analyze: 0 errors
- Phase 9 commits: 9131e1f (09-01), 4e009df (09-02), 8c68a3d (09-03 completion)
- Phase 10 directory created: `.planning/phases/10-window-optimization/`
- Research agent: launched, completion unknown
</current_state>

<completed_work>

Phase 9 (Security Hardening) — COMPLETE:
- **09-01: FFI Pointer Lifecycle Safety** — Done (commit 9131e1f)
  - 6 calloc calls wrapped in try/finally
  - _savedFrame / _savedMaximizeFrame exception-path cleanup
  - dispose() now frees _savedFrame (was leaking)
  - setFullscreen() 5-second timeout protection

- **09-02: Input Validation Hardening** — Done (commit 4e009df)
  - HTTP/HTTPS Uri.tryParse + hasAuthority + host.isNotEmpty
  - Control character filter (0x01-0x1F, excludes 0x00/0x09)
  - RTSP/RTMP/SRT/UDP/TCP keep prefix passthrough
  - 12 new test cases

- **09-03: E2E Security Verification** — Done (commit 8c68a3d)
  - User approved all safety changes

</completed_work>

<remaining_work>

Phase 10 (Window Optimization) — PLANNING:
- Research agent may have written 10-RESEARCH.md (check on resume)
- No plans created yet
- Need to: complete research → spawn planner → verify plans → execute

</remaining_work>

<decisions_made>

- Used try/finally instead of Arena for FFI cleanup (dart:ffi using() doesn't support Arena callback)
- Added host.isNotEmpty check alongside hasAuthority for URL validation (Dart's Uri considers empty authority valid)
- Tab character (0x09) excluded from control char filter (Windows filenames allow tab)

</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/10-window-optimization/10-RESEARCH.md` — Research output (if exists)
2. `.planning/ROADMAP.md` — Phase 10 goal and success criteria
3. `.planning/REQUIREMENTS.md` — WIN-04 requirement details
4. `lib/kernel/bridge/window_service.dart` — Current window management implementation

## Infrastructure State
- Flutter: D:\flutter\bin\flutter (not in PATH, use `flutter` from bash)
- Branch: fix/window-startup
- All Phase 9 planning files committed

<context>
Phase 9 Security Hardening completed successfully (3/3 plans). Phase 10 Window Optimization planning was initiated. A gsd-phase-researcher agent was spawned to research window management best practices, multi-monitor handling, and fullscreen animation optimization. The agent was running in the background when the session ended.

On resume, check if `.planning/phases/10-window-optimization/10-RESEARCH.md` exists. If it does, proceed to planning. If not, re-run the research step.
</context>

<next_action>
Start with: `/gsd:plan-phase 10` — check if 10-RESEARCH.md exists from the background agent. If yes, skip to planning. If no, re-run research.
</next_action>
