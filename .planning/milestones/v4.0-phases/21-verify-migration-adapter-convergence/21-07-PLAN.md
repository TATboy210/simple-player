---
phase: 21-verify-migration-adapter-convergence
plan: 07
type: execute
wave: 4
depends_on:
  - "21-04"
files_modified:
  - lib/kernel/player_services.dart
autonomous: true
requirements:
  - VERIFY-04
  - VERIFY-05

must_haves:
  truths:
    - "DelegationPolicy 全部 7 个 per-capability 字段均为 KernelMode.migrated"
    - "migratedMethods 包含全部27个 MediaEngine 方法"
    - "phase21_gates.sh GATE 1 PASS"
    - "flutter analyze 零新增 error"
  artifacts:
    - "lib/kernel/player_services.dart — DelegationPolicy 构造已迁移"
  key_links:
    - "player_services.dart DelegationPolicy → KernelAdapter 路由 → FvpEngine(migrated)"
---

<objective>
翻转 DelegationPolicy 从 all-legacy 到 all-migrated，激活 Phase 20 的引擎迁移工作。

Purpose: 解除 BLOCKER 1（GATE 1 FAIL）— 当前所有7个能力字段仍路由到 legacy 路径，Phase 20 的迁移工作未生效。
Output: player_services.dart 中 DelegationPolicy 使用 KernelMode.migrated + 全部27个方法在 migratedMethods 中。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-verify-migration-adapter-convergence/21-CONTEXT.md
@.planning/phases/21-verify-migration-adapter-convergence/21-RESEARCH.md
@.planning/phases/21-verify-migration-adapter-convergence/21-VERIFICATION.md
@lib/kernel/player_services.dart
@lib/kernel/adapter/kernel_adapter.dart
@tool/audit/phase21_gates.sh
@tool/audit/rollback.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: 翻转 DelegationPolicy 为 all-migrated + 全方法 migratedMethods</name>
  <files>lib/kernel/player_services.dart</files>
  <read_first>
    - lib/kernel/player_services.dart (完整文件，确认当前 DelegationPolicy 构造)
    - lib/kernel/adapter/kernel_adapter.dart (理解 _targetFor 路由逻辑和 DelegationPolicy 构造函数)
    - test/regression/dual_track_regression_test.dart (确认 _allMediaEngineMethods 完整列表：27个方法)
    - tool/audit/phase21_gates.sh (确认 GATE 1 检查逻辑)
    - tool/audit/rollback.sh (确认回退脚本可用)
  </read_first>
  <action>
修改 `lib/kernel/player_services.dart` 第126行，将 DelegationPolicy 从 all-legacy 翻转为 all-migrated。

**当前代码（第126行）：**
```dart
policy: const DelegationPolicy.all(KernelMode.legacy),
```

**关键设计决策：** 不能直接用 `DelegationPolicy.all(KernelMode.migrated)` — 因为 `all()` 构造函数会将 `migratedMethods` 设为空 const Set，导致 `_targetFor(method)` 对所有27个方法调用仍走 legacy 路径（只有7个 state view getter 会走 migrated）。必须同时设置7个 capability 字段为 migrated AND 27个方法在 migratedMethods 中。

**替换为：**
```dart
policy: const DelegationPolicy(
  stateView: KernelMode.migrated,
  playback: KernelMode.migrated,
  track: KernelMode.migrated,
  subtitle: KernelMode.migrated,
  videoEffect: KernelMode.migrated,
  renderer: KernelMode.migrated,
  volume: KernelMode.migrated,
  migratedMethods: {
    // PlaybackControl (10 methods)
    'open', 'play', 'pause', 'stop', 'togglePlayPause',
    'seekTo', 'setPlaybackRate', 'setRange', 'skipForward', 'skipBack',
    // TrackControl (2 methods)
    'getAudioTracks', 'switchAudioTrack',
    // SubtitleConfig (6 methods)
    'getSubtitleTracks', 'switchSubtitleTrack', 'toggleSubtitle',
    'setExternalSubtitle', 'setSubtitleDelay', 'setEqualizer',
    // VideoEffectControl (4 methods)
    'setVideoEffect', 'rotate', 'setAspectRatio', 'setDeinterlace',
    // RendererControl (2 methods)
    'setD3d11SyncEnabled', 'setHardwareDecoding',
    // VolumeControl (2 methods)
    'setVolume', 'setMute',
  },
),
```

**回退步骤（如需）：** 运行 `bash tool/audit/rollback.sh` 或手动改回 `DelegationPolicy.all(KernelMode.legacy)`。rollback.sh 已就位（21-04 SUMMARY 确认）。

**注意事项：**
- 此修改是一行代码改动（D16 决策），但 migratedMethods 集合必须完整
- 27个方法列表来自 `test/regression/dual_track_regression_test.dart` 的 `_allMediaEngineMethods` 常量
- `const` 保留 — DelegationPolicy 的所有字段都是 final，编译期可构造
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/player_services.dart && grep -c 'KernelMode\.migrated' lib/kernel/player_services.dart</automated>
  </verify>
  <done>DelegationPolicy 全部7字段为 KernelMode.migrated，migratedMethods 包含27个方法，flutter analyze 零新增 error</done>
</task>

<task type="auto">
  <name>Task 2: 验证 GATE 1 通过 + 全测试套件</name>
  <files>tool/audit/phase21_gates.sh</files>
  <read_first>
    - tool/audit/phase21_gates.sh (确认 GATE 1 检查逻辑)
    - lib/kernel/player_services.dart (确认修改已生效)
  </read_first>
  <action>
运行 phase21_gates.sh 验证 GATE 1 现在通过。

预期结果：
- GATE 1: PASS — DelegationPolicy 全部 migrated
- GATE 2: PASS — 双轨回归 exit 0（mdk.dll 环境下 skip）
- GATE 3: PASS — OpenGenerationTracker 在 engine_state_machine.dart
- GATE 4: PASS — rollback.sh + ROLLBACK.md 存在

然后运行 `flutter test` 确认无回归。由于 mdk.dll 不可用，约57个 FFI 测试会失败（pre-existing），其余应全部通过。

如果 GATE 1 仍失败，检查 grep 模式是否匹配修改后的代码格式（`KernelMode.migrated` vs `KernelMode. migrated` 等空格问题）。
  </action>
  <verify>
    <automated>bash tool/audit/phase21_gates.sh 2>&1; echo "EXIT_CODE=$?"</automated>
  </verify>
  <done>phase21_gates.sh 4/4 gates PASS，GATE 1 从 FAIL 变为 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DelegationPolicy → KernelAdapter | 路由策略变更，影响所有 MediaEngine 方法调用路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-07-1 | Tampering | DelegationPolicy routing | medium | mitigate | 回退脚本 `tool/audit/rollback.sh` 已就位，一行代码可翻回 all-legacy |
| T-21-07-2 | Denial of Service | FvpEngine(migrated) 路径异常 | medium | mitigate | 双轨回归测试验证行为一致；rollback.sh 提供即时回退 |
</threat_model>

<verification>
- `grep 'KernelMode\.migrated' lib/kernel/player_services.dart` 命中7个字段 + migratedMethods
- `bash tool/audit/phase21_gates.sh` 4/4 PASS
- `flutter analyze lib/kernel/player_services.dart` 零 error
</verification>

<success_criteria>
- DelegationPolicy 全部7字段为 KernelMode.migrated
- migratedMethods 包含27个 MediaEngine 方法
- phase21_gates.sh GATE 1 从 FAIL 变为 PASS
- flutter test 无新增回归（pre-existing mdk.dll 失败除外）
</success_criteria>

<artifacts>
本 plan 产出物：
- lib/kernel/player_services.dart — DelegationPolicy 翻转为 all-migrated
</artifacts>

<output>
Create `.planning/phases/21-verify-migration-adapter-convergence/21-07-SUMMARY.md` when done
</output>
