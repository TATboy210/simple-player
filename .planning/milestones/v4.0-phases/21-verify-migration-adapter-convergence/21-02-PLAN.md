---
phase: 21-verify-migration-adapter-convergence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/regression/dual_track_regression_test.dart
  - test/regression/regression_fixture.dart
  - test/regression/diff_report.dart
autonomous: true
requirements:
  - VERIFY-02

must_haves:
  truths:
    - "同一参数化测试对 all-legacy 和 all-migrated 两组 engine factory 输出一致（VERIFY-02）"
    - "所有 MediaEngine 方法全覆盖（D2）"
    - "全异步场景使用 fakeAsync（D3）"
    - "混合断言：状态值 + 回调触发次数 + 错误状态（D4）"
  artifacts:
    - "test/regression/dual_track_regression_test.dart — 参数化双轨回归测试"
    - "test/regression/regression_fixture.dart — 共享 RegressionFixture 类（D6）"
    - "test/regression/diff_report.dart — DiffEntry + DiffReport 差异收集（D7）"
  key_links:
    - "DelegationPolicy.all(KernelMode.legacy) → all-legacy factory"
    - "DelegationPolicy(migratedMethods: all) → all-migrated factory"
    - "RegressionFixture → engine factory + assertState + assertNoDiffs"
---

<objective>
创建参数化双轨回归套件，验证 all-legacy 和 all-migrated 两组 engine factory 对所有 MediaEngine 方法输出一致。

Purpose: 确认 Phase 20 的 DelegationPolicy per-method 翻转没有引入行为差异。
Output: test/regression/ 下 3 个新文件，双轨回归全绿（0 differences）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-verify-migration-adapter-convergence/21-CONTEXT.md
@.planning/phases/21-verify-migration-adapter-convergence/21-RESEARCH.md
@lib/kernel/adapter/kernel_adapter.dart
@lib/kernel/player_services.dart
@test/engine/fvp_engine_contract_test.dart
@test/contracts/contract_test_runner.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 DiffReport 和 RegressionFixture 基础设施</name>
  <files>test/regression/diff_report.dart, test/regression/regression_fixture.dart</files>
  <read_first>
    - test/engine/fvp_engine_contract_test.dart (texture mock 模式)
    - test/contracts/playback_control_contract.dart (断言模式参考)
    - lib/kernel/adapter/kernel_adapter.dart (DelegationPolicy + KernelMode)
    - lib/kernel/engine/media_engine.dart (MediaEngine 接口全貌)
    - lib/kernel/engine/media_state.dart (MediaState 枚举)
  </read_first>
  <behavior>
    - DiffEntry: method(String) + expected(String) + actual(String) + context(String?) 字段，toString() 格式化
    - DiffReport: List<DiffEntry> diffs + addEntry() + hasDiffs getter + toString() 汇总报告
    - RegressionFixture: engine factory 注入 + engine 实例 + dispose() + assertState(expected, context) 收集差异 + assertNotifierEquals<T>(notifier, expected, context) 收集差异 + assertNoDiffs() 最终校验
    - RegressionFixture.setUp() 调用 factory 创建 engine
    - RegressionFixture.dispose() 调用 engine.dispose()
  </behavior>
  <action>
创建两个基础文件：

1. `test/regression/diff_report.dart`:
   - `DiffEntry` 类：method(String), expected(String), actual(String), context(String?)
   - `toString()` 格式: `[method] expected: {expected}, actual: {actual} ({context})`
   - `DiffReport` 类：内部 `List<DiffEntry>` + `addEntry(DiffEntry)` + `bool get hasDiffs` + `toString()` 汇总

2. `test/regression/regression_fixture.dart`:
   - `RegressionFixture` 类：构造接受 `MediaEngine Function()` factory
   - `setUp()` 调用 factory 创建 `engine`
   - `dispose()` 调用 `engine.dispose()`
   - `assertState(MediaState expected, {String? context})` — 比较 `engine.state.value` 与 expected，不等则 addEntry
   - `assertNotifierEquals<T>(ValueNotifier<T> notifier, T expected, String context)` — 通用 notifier 断言
   - `assertNoDiffs()` — 如果 `report.hasDiffs` 则 `fail(report.toString())`
   - `assertCallbackCount(String callback, int expected, int actual)` — 回调次数断言

先写测试（RED）：创建 `test/regression/diff_report_test.dart` 验证 DiffEntry.toString() 和 DiffReport.hasDiffs。再实现（GREEN）。
  </action>
  <verify>
    <automated>flutter test test/regression/diff_report_test.dart</automated>
  </verify>
  <done>DiffEntry.toString() 格式正确，DiffReport.hasDiffs 在有差异时返回 true，RegressionFixture 可创建和销毁 engine</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建参数化双轨回归测试（全方法覆盖 + fakeAsync + 混合断言）</name>
  <files>test/regression/dual_track_regression_test.dart</files>
  <read_first>
    - test/regression/regression_fixture.dart (Task 1 产出)
    - test/regression/diff_report.dart (Task 1 产出)
    - test/engine/fvp_engine_contract_test.dart (texture mock + factory 模式)
    - test/contracts/playback_control_contract.dart (测试用例参考)
    - test/contracts/engine_state_view_contract.dart (notifier 断言参考)
    - lib/kernel/engine/media_engine.dart (全方法列表)
    - lib/kernel/adapter/kernel_adapter.dart (DelegationPolicy 构造)
  </read_first>
  <behavior>
    - 两个 group（all-legacy / all-migrated）各跑相同测试集
    - all-legacy factory: KernelAdapter(legacy: fvp, migrated: fvp, policy: DelegationPolicy.all(KernelMode.legacy))
    - all-migrated factory: KernelAdapter(legacy: fvp, migrated: fvp, policy: DelegationPolicy(stateView: migrated, playback: migrated, ..., migratedMethods: {all methods}))
    - 测试覆盖: open→idle→playing 流程、seekTo 更新 position、volume set 更新 notifier、mute toggle、playbackRate set、audio track query、subtitle track query、videoEffect set、rotate、aspectRatio、deinterlace、D3D11 sync、hardware decoding
    - 所有涉及 async 的测试使用 fakeAsync（open、seekTo、auto-advance）
    - 每个测试结束调用 fixture.assertNoDiffs()
  </behavior>
  <action>
创建 `test/regression/dual_track_regression_test.dart`：

1. 复制 `fvp_engine_contract_test.dart` 的 texture channel mock（`_installFvpTextureChannelMock()`）
2. 定义两个 engine factory（per D1 参数化）：
   - `allLegacyFactory`: `FvpEngine()` 包裹 `KernelAdapter(legacy: fvp, migrated: fvp, policy: DelegationPolicy.all(KernelMode.legacy))`
   - `allMigratedFactory`: `FvpEngine()` 包裹 `KernelAdapter(legacy: fvp, migrated: fvp, policy: DelegationPolicy(stateView: KernelMode.migrated, playback: KernelMode.migrated, track: KernelMode.migrated, subtitle: KernelMode.migrated, videoEffect: KernelMode.migrated, renderer: KernelMode.migrated, volume: KernelMode.migrated, migratedMethods: {'open', 'play', 'pause', 'stop', 'togglePlayPause', 'seekTo', 'setPlaybackRate', 'setRange', 'skipForward', 'skipBack', 'getAudioTracks', 'switchAudioTrack', 'getSubtitleTracks', 'switchSubtitleTrack', 'toggleSubtitle', 'setExternalSubtitle', 'setSubtitleDelay', 'setEqualizer', 'setVideoEffect', 'rotate', 'setAspectRatio', 'setDeinterlace', 'setD3d11SyncEnabled', 'setHardwareDecoding', 'setVolume', 'setMute'}))`
3. `for (final entry in factories.entries)` 循环创建两组 group（per D1）
4. 每组 group 使用 `RegressionFixture(entry.value)` + setUp/tearDown
5. 测试用例（per D2 全方法覆盖 + D3 fakeAsync + D4 混合断言）：
   - `open → idle → playing` (fakeAsync, assertState)
   - `seekTo updates position` (fakeAsync, assertNotifierEquals)
   - `setVolume updates volume notifier` (assertNotifierEquals)
   - `setMute toggles isMuted` (assertNotifierEquals)
   - `setPlaybackRate updates playbackSpeed` (assertNotifierEquals)
   - `getAudioTracks returns list` (assertNotifierEquals)
   - `getSubtitleTracks returns list` (assertNotifierEquals)
   - `setVideoEffect no throw` (assertNoDiffs)
   - `rotate no throw` (assertNoDiffs)
   - `setAspectRatio updates aspectRatio` (assertNotifierEquals)
   - `setDeinterlace no throw` (assertNoDiffs)
   - `setD3d11SyncEnabled no throw` (assertNoDiffs)
   - `setHardwareDecoding no throw` (assertNoDiffs)
6. 测试结束时两个 group 的 DiffReport 差异为零

注意：由于 all-legacy 和 all-migrated 指向同一个 FvpEngine 实例，行为应该完全一致。DelegationPolicy 只影响 KernelAdapter 的路由，底层执行的是同一个引擎。
  </action>
  <verify>
    <automated>flutter test test/regression/dual_track_regression_test.dart</automated>
  </verify>
  <done>两个 group（all-legacy / all-migrated）全部测试通过，DiffReport 差异为零，覆盖所有 MediaEngine 方法</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → KernelAdapter → FvpEngine | 双轨回归验证路由一致性，不涉及用户输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-02 | Tampering | npm/pip/cargo installs | low | accept | Phase 21 不安装新外部包 |
</threat_model>

<verification>
- `flutter test test/regression/dual_track_regression_test.dart` exit 0
- all-legacy group 和 all-migrated group 各测试全部 PASS
- DiffReport 差异为零
</verification>

<success_criteria>
- 参数化双轨回归测试存在且全绿
- RegressionFixture + DiffReport 基础设施可复用
- 全 MediaEngine 方法覆盖（D2），fakeAsync 全异步（D3），混合断言（D4）
</success_criteria>

<artifacts>
本 phase 产出物：
- test/regression/dual_track_regression_test.dart — 参数化双轨回归测试
- test/regression/regression_fixture.dart — 共享 RegressionFixture 类
- test/regression/diff_report.dart — DiffEntry + DiffReport 差异收集
</artifacts>

<output>
Create `.planning/phases/21-verify-migration-adapter-convergence/21-02-SUMMARY.md` when done
</output>
