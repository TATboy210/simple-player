---
phase: 21-verify-migration-adapter-convergence
plan: 09
type: execute
wave: 5
depends_on:
  - "21-08"
files_modified:
  - test/kernel/services/breakpoint_saver_test.dart
  - test/kernel/player_services_test.dart
  - test/kernel/services/theme_service_test.dart
  - test/kernel/utils/debug_exporter_test.dart
  - test/kernel/services/playback_navigator_test.dart
  - test/kernel/services/auto_advance_policy_test.dart
  - test/kernel/engine/engine_prewarm_test.dart
  - test/kernel/bridge/window_persistence_test.dart
  - test/kernel/services/playback_controller_test.dart
  - test/kernel/services/playback_state_manager_test.dart
  - test/kernel/adapter/kernel_adapter_routing_test.dart
autonomous: true
requirements:
  - VERIFY-05

must_haves:
  truths:
    - "kernel/ 覆盖率从57.6%提升至≥70%（纯 Dart 测试路径）"
    - "所有新增/扩展测试不依赖 mdk.dll（FakeEngine + fakeAsync）"
    - "至少4个之前无测试的模块获得测试覆盖"
    - "mdk.dll 覆盖率瓶颈已量化并记录在 SUMMARY 中"
  artifacts:
    - "test/kernel/services/breakpoint_saver_test.dart — 断点保存测试"
    - "test/kernel/player_services_test.dart — 服务容器测试"
    - "test/kernel/services/theme_service_test.dart — 主题服务测试"
    - "test/kernel/utils/debug_exporter_test.dart — 调试导出测试"
  key_links:
    - "FakeEngine → PlayerServices/BreakpointSaver → 纯 Dart 测试"
    - "fakeAsync → PositionPoller/AutoAdvancePolicy → 定时器测试"
---

<objective>
通过纯 Dart 单元测试最大化 kernel/ 覆盖率，同时精确量化 mdk.dll 依赖造成的覆盖率天花板。

Purpose: 继续解除 BLOCKER 2（kernel/ 覆盖率57.6% vs 目标80%）。Wave 4 (21-08) 仅提升2.6pp，因测试覆盖的代码已有部分覆盖。本轮采取"广度优先"策略：先覆盖所有零覆盖纯 Dart 模块，再扩展中等覆盖模块的未覆盖路径。
Output: 4个新测试文件 + 7个扩展测试文件，目标覆盖率 ≥70%。若 mdk.dll 瓶颈导致80%不可达，在 SUMMARY 中提出调整目标建议。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-verify-migration-adapter-convergence/21-CONTEXT.md
@.planning/phases/21-verify-migration-adapter-convergence/21-RESEARCH.md
@.planning/phases/21-verify-migration-adapter-convergence/21-VERIFICATION.md
@lib/kernel/services/breakpoint_saver.dart
@lib/kernel/player_services.dart
@lib/kernel/services/theme_service.dart
@lib/kernel/utils/debug_exporter.dart
@lib/kernel/services/auto_advance_policy.dart
@lib/kernel/engine/engine_prewarm.dart
@test/helpers/fake_engine.dart
@test/kernel/services/playback_controller_test.dart
@test/kernel/engine/engine_prewarm_test.dart
</context>

<coverage_analysis>

## 精确覆盖率分析（基于 lcov.info 实测数据）

**当前状态：**
- instrumented lines: 3,013
- covered lines: 1,734 (57.6%)
- target: 2,410 (80%)
- gap: 676 lines

**mdk.dll 依赖瓶颈（headless CI 无法覆盖）：**

| File | Instrumented | Hit | Uncovered |
|------|---:|---:|---:|
| fvp_engine.dart | 303 | 2 | 301 |
| media_opener.dart | 85 | 0 | 85 |
| position_poller.dart | 60 | 0 | 60 |
| win32_display_enumerator.dart | 62 | 0 | 62 |
| network_configurator.dart | 40 | 0 | 40 |
| track_manager.dart | 31 | 0 | 31 |
| mdk_player_proxy.dart | 9 | 0 | 9 |
| **Total mdk-blocked** | **590** | **2** | **588** |

**覆盖率天花板计算：**
- 非 mdk 可测试行: 3,013 - 588 = 2,425
- 80% 目标需覆盖: 2,410 行
- 需要非 mdk 代码达到: 2,410/2,425 = **99.4%** — 几乎不可能

**现实目标推导：**
- 纯 Dart 快速胜利（Task 1-2）: +155 lines → 62.8%
- 现有测试扩展（Task 3-6）: +107 lines → 66.3%
- 中等难度模块（Task 7-8）: +110 lines → 69.9%
- **无 mdk mock 的天花板: ~70%**
- 若实现 mdk.Player 依赖注入 + mock（Task 9）: +200 lines → **76.6%**
- 若深度 mock 所有 mdk 路径: +400 lines → **~80%**（但工作量极大）

</coverage_analysis>

<tasks>

<task type="auto">
  <name>Task 1: 新测试 — breakpoint_saver + player_services（零覆盖纯 Dart 模块）</name>
  <files>
    test/kernel/services/breakpoint_saver_test.dart
    test/kernel/player_services_test.dart
  </files>
  <read_first>
    - lib/kernel/services/breakpoint_saver.dart (68行：init/dispose 监听器、暂停时保存位置、dispose 兜底保存)
    - lib/kernel/player_services.dart (182行：DI 容器、init/dispose 生命周期、KernelAdapter 组合)
    - test/helpers/fake_engine.dart (FakeEngine 接口：state/position/duration ValueNotifier)
    - lib/kernel/engine/engine_state.dart (MediaState 枚举)
    - lib/kernel/persistence/playlist_store.dart (PlaylistStore.save 静态方法)
    - lib/kernel/playlist/playlist.dart (Playlist.updatePosition 方法)
    - lib/kernel/bridge/window_bridge.dart (WindowBridge 接口 — PlayerServices 依赖)
  </read_first>
  <action>
**文件 1：`test/kernel/services/breakpoint_saver_test.dart`**

BreakpointSaver 是纯 Dart 模块（68行），当前 0% 覆盖（不在 lcov.info 中）。使用 FakeEngine + mock Playlist 即可测试。

```
group('BreakpointSaver', () {
  late FakeEngine engine;
  late Playlist playlist;
  late BreakpointSaver saver;

  setUp(() {
    engine = FakeEngine();
    playlist = Playlist();
    saver = BreakpointSaver(engine: engine, playlist: playlist);
  });

  group('init', () {
    test('registers state change listener')
    test('does not save on non-paused state transitions')
  })

  group('onStateChanged (paused trigger)', () {
    test('saves position when state becomes paused and currentIndex >= 0')
    test('does not save when state is not paused')
    test('does not save when currentIndex < 0 (no current item)')
    test('saves duration along with position')
  })

  group('dispose', () {
    test('removes state listener')
    test('saves position if position > 0 and currentIndex >= 0')
    test('does not save if position is 0 (never played)')
    test('does not save if currentIndex < 0')
    test('is idempotent (safe to call twice)')
  })
})
```

**关键点：**
- 需要设置 playlist 有至少一个 item 且 currentIndex >= 0
- 通过 engine.state.value = MediaState.paused 触发保存
- 验证 PlaylistStore.save 被调用（可能需要 mock 或验证 playlist 内部状态）
- BreakpointSaver 使用 `PlaylistStore.save(playlist)` 静态方法 — 测试中验证 playlist 的 position 已更新

**测试数量：** ~10 个用例
**预计覆盖：** breakpoint_saver.dart 全部 55 行可执行代码

---

**文件 2：`test/kernel/player_services_test.dart`**

PlayerServices 是 DI 容器（182行），当前 0% 覆盖。其 init() 依赖 FvpEngine（mdk.dll），但可以测试：
- 构造函数行为（windowService 赋值、playlistGeneration 初始化）
- dispose() 顺序（验证各服务 dispose 被调用）
- 使用 mock/子类绕过 FvpEngine 创建

```
group('PlayerServices', () {
  group('construction', () {
    test('windowService is assigned from constructor')
    test('playlistGeneration starts at 0')
  })

  group('dispose', () {
    test('dispose calls playlistGeneration.dispose')
    test('dispose calls windowService.dispose')
    test('dispose calls videoProcessing.dispose')
    test('dispose calls controller.dispose')
    test('dispose calls engine.dispose')
    test('dispose is idempotent')
  })
})
```

**注意：** init() 创建 FvpEngine 需要 mdk.dll，无法在 headless CI 测试。仅测试构造和 dispose 行为。如需测试 init()，需要 Task 9 的 mdk mock 前置。

**测试数量：** ~8 个用例
**预计覆盖：** player_services.dart 构造+dispose 路径 ~31 行
  </action>
  <verify>
    <automated>
flutter test test/kernel/services/breakpoint_saver_test.dart
flutter test test/kernel/player_services_test.dart
flutter test --coverage && grep -c "DA:" coverage/lcov.info
    </automated>
  </verify>
  <done>breakpoint_saver + player_services 测试全部通过，两个零覆盖模块获得测试覆盖</done>
</task>

<task type="auto">
  <name>Task 2: 新测试 — theme_service + debug_exporter（零覆盖纯 Dart 模块）</name>
  <files>
    test/kernel/services/theme_service_test.dart
    test/kernel/utils/debug_exporter_test.dart
  </files>
  <read_first>
    - lib/kernel/services/theme_service.dart (49行：单例、accents 列表、themeIndex ValueNotifier、init/setTheme/dispose)
    - lib/kernel/utils/debug_exporter.dart (67行：exportAll JSON、saveToFile 文件写入、MemoryMonitor/DebugProbeRegistry 依赖)
    - lib/kernel/persistence/settings_store.dart (SettingsStore.loadThemeIndex/saveThemeIndex)
    - lib/kernel/diagnostics/memory_monitor.dart (MemoryMonitor.I.snapshot)
    - lib/kernel/utils/debug_probe.dart (DebugProbeRegistry.summary)
  </read_first>
  <action>
**文件 1：`test/kernel/services/theme_service_test.dart`**

ThemeService 是纯 Dart 单例（49行，16 instrumented，0% 覆盖）。

```
group('ThemeService', () {
  setUp(() {
    // ThemeService 是单例，测试间需要重置
  });

  group('accents', () {
    test('has 3 accent colors')
    test('accents are valid Color values')
  })

  group('themeIndex', () {
    test('defaults to 0')
    test('notifies listeners on change')
  })

  group('currentAccent', () {
    test('returns accent at current index')
    test('clamps to valid range when index out of bounds')
  })

  group('currentTheme', () {
    test('returns dark theme with current accent')
    test('theme colorScheme primary matches currentAccent')
  })

  group('setTheme', () {
    test('updates themeIndex value')
    test('clamps index to valid range')
    test('persists via SettingsStore')
  })

  group('dispose', () {
    test('disposes themeIndex notifier')
  })
})
```

**注意：** ThemeService 使用 SettingsStore 静态方法 — 测试中可能需要 mock 或验证 ValueNotifier 变化而非持久化。

**测试数量：** ~12 个用例
**预计覆盖：** theme_service.dart 16 行全部

---

**文件 2：`test/kernel/utils/debug_exporter_test.dart`**

DebugExporter（67行，27 instrumented，0% 覆盖）。exportAll() 是纯 JSON 序列化，可直接测试。saveToFile() 依赖 path_provider — 需要 mock 或跳过。

```
group('DebugExporter', () {
  group('exportAll', () {
    test('returns valid JSON string')
    test('JSON contains memory key')
    test('JSON contains probes key')
    test('JSON contains exportedAt ISO8601 timestamp')
    test('JSON contains mode key (debug or release)')
    test('probe summary reflects registered probes')
  })

  group('saveToFile', () {
    // saveToFile 依赖 path_provider — headless CI 中可能需要 mock
    // 如无法 mock，标记为 skip 并记录原因
    test('returns file path on success', skip: 'requires path_provider mock')
    test('returns null on exception', skip: 'requires path_provider mock')
  })
})
```

**关键点：**
- exportAll() 依赖 MemoryMonitor.I.snapshot() — 需要先 init MemoryMonitor
- exportAll() 依赖 DebugProbeRegistry.summary() — 已有测试覆盖
- 用 FakeRssProvider + SystemClock 创建 MemoryMonitor 实例

**测试数量：** ~8 个用例
**预计覆盖：** debug_exporter.dart exportAll 路径 ~22 行
  </action>
  <verify>
    <automated>
flutter test test/kernel/services/theme_service_test.dart
flutter test test/kernel/utils/debug_exporter_test.dart
flutter test --coverage
    </automated>
  </verify>
  <done>theme_service + debug_exporter 测试全部通过，覆盖率提升</done>
</task>

<task type="auto">
  <name>Task 3: 扩展 playback_navigator 测试（43.2% → ≥80%）</name>
  <files>test/kernel/services/playback_navigator_test.dart</files>
  <read_first>
    - lib/kernel/services/playback_navigator.dart (44 instrumented, 19 hit：playIndex/playNext/playPrevious/resume)
    - test/kernel/services/playback_navigator_test.dart (现有测试)
    - test/helpers/fake_engine.dart (FakeEngine 方法)
    - lib/kernel/services/path_validator.dart (PathValidator)
    - lib/kernel/playlist/playlist.dart (Playlist 操作)
  </read_first>
  <action>
扩展 playback_navigator_test.dart，覆盖当前未测试的路径：

**需要覆盖的路径（+25 lines）：**
- playNext 在 playlist 末尾时的行为（wrap around for loopAll）
- playPrevious 在 playlist 开头时的行为
- resume 路径（从保存位置恢复播放）
- path validation 失败时的跳过逻辑
- 空 playlist 时的边界处理
- shuffle 模式下的 next/previous 行为

```
group('PlaybackNavigator expanded', () {
  group('playNext edge cases', () {
    test('wraps to first when at end with loopAll mode')
    test('stops when at end with loopNone mode')
    test('skips invalid paths and tries next')
    test('handles empty playlist gracefully')
  })

  group('playPrevious edge cases', () {
    test('wraps to last when at start with loopAll mode')
    test('stops when at start with loopNone mode')
    test('skips invalid paths and tries previous')
  })

  group('resume', () {
    test('seeks to saved position after opening file')
    test('does not seek if saved position is 0')
    test('handles resume with invalid saved position')
  })

  group('path validation', () {
    test('skips items with non-existent paths')
    test('advances to next valid item')
  })
})
```

**测试数量：** ~12 个新用例
**预计覆盖提升：** 19→39 hit (+20 lines) → 覆盖率 ~88%
  </action>
  <verify>
    <automated>
flutter test test/kernel/services/playback_navigator_test.dart
    </automated>
  </verify>
  <done>playback_navigator 覆盖率从43.2%提升至≥80%</done>
</task>

<task type="auto">
  <name>Task 4: 扩展 auto_advance_policy 测试（34.6% → ≥80%）</name>
  <files>test/kernel/services/auto_advance_policy_test.dart</files>
  <read_first>
    - lib/kernel/services/auto_advance_policy.dart (26 instrumented, 9 hit：init/onStateChanged/replayIndex/autoAdvance/dispose)
    - test/kernel/services/auto_advance_policy_test.dart (现有测试)
    - lib/kernel/models/play_mode.dart (PlayMode 枚举)
    - lib/kernel/engine/engine_state.dart (MediaState 枚举)
  </read_first>
  <action>
扩展 auto_advance_policy_test.dart，覆盖所有状态分支：

**需要覆盖的路径（+17 lines）：**
- init 幂等性（多次调用只注册一次监听器）
- completed + loopSingle → 重播当前索引
- completed + loopAll → playNext
- completed + shuffle → playNext
- 非 completed 状态 → 不触发任何操作
- _replayIndex 异常处理路径
- _autoAdvance 异常处理路径
- dispose 注销监听器

```
group('AutoAdvancePolicy expanded', () {
  group('init', () {
    test('is idempotent — second call does not re-register listener')
    test('registers engine state listener')
  })

  group('state transitions', () {
    test('completed + loopSingle replays current index')
    test('completed + loopAll calls playNext')
    test('completed + shuffle calls playNext')
    test('non-completed state triggers nothing')
    test('completed with invalid currentIndex does not crash')
  })

  group('error handling', () {
    test('replayIndex error calls onError callback')
    test('autoAdvance error calls onError callback')
    test('no crash when onError is null')
  })

  group('dispose', () {
    test('removes engine state listener')
    test('no state changes after dispose')
  })
})
```

**测试数量：** ~12 个新用例
**预计覆盖提升：** 9→23 hit (+14 lines) → 覆盖率 ~88%
  </action>
  <verify>
    <automated>
flutter test test/kernel/services/auto_advance_policy_test.dart
    </automated>
  </verify>
  <done>auto_advance_policy 覆盖率从34.6%提升至≥80%</done>
</task>

<task type="auto">
  <name>Task 5: 扩展 engine_prewarm 测试（37.9% → ≥80%）</name>
  <files>test/kernel/engine/engine_prewarm_test.dart</files>
  <read_first>
    - lib/kernel/engine/engine_prewarm.dart (29 instrumented, 11 hit：prewarm 逻辑含 mdk.Player 创建)
    - test/kernel/engine/engine_prewarm_test.dart (现有4个测试：仅测试初始状态和 reset)
  </read_first>
  <action>
扩展 engine_prewarm_test.dart。当前测试仅验证初始状态标志，未覆盖 prewarm() 实际逻辑。

**分析：** prewarm() 内部调用 `mdk.Player()` — headless CI 会失败。但可以覆盖：
- prewarm 幂等性（第二次调用直接返回）
- prewarm 失败时的异常处理路径（需要 mock mdk.Player 或接受 FFI 失败）
- onProgress 回调触发验证
- reset 后可重新 prewarm

```
group('EnginePrewarm expanded', () {
  group('prewarm idempotency', () {
    test('second prewarm call returns immediately (already prewarmed)')
    // 需要先让 prewarm 成功一次，然后验证第二次不执行 FFI
  })

  group('prewarm failure handling', () {
    test('sets _prewarmed to false on exception')
    test('logs error on failure')
    // mdk.Player() 在 headless CI 会抛异常 — 这正好测试 catch 路径
  })

  group('prewarm onProgress', () {
    test('calls onProgress with 0.0 at start')
    test('calls onProgress with 1.0 on success')
    // headless CI 中 mdk.Player() 会失败，onProgress 可能不会到 1.0
  })

  group('state flags', () {
    test('isPlayerCreated is false after failed prewarm')
    test('isCodecsReady is false after failed prewarm')
    test('isGpuReady is false after failed prewarm')
  })
})
```

**关键洞察：** mdk.Player() 在 headless CI 抛异常 → 这恰好测试了 catch 路径（第 67-69 行）。prewarmed 保持 false，flags 保持 false。这实际上是免费的覆盖率提升！

**测试数量：** ~8 个新用例
**预计覆盖提升：** 11→23 hit (+12 lines) → 覆盖率 ~79%
  </action>
  <verify>
    <automated>
flutter test test/kernel/engine/engine_prewarm_test.dart
    </automated>
  </verify>
  <done>engine_prewarm 覆盖率从37.9%提升至接近80%</done>
</task>

<task type="auto">
  <name>Task 6: 扩展 playback_controller + playback_state_manager 测试</name>
  <files>
    test/kernel/services/playback_controller_test.dart
    test/kernel/services/playback_state_manager_test.dart
  </files>
  <read_first>
    - lib/kernel/services/playback_controller.dart (54 instrumented, 42 hit)
    - lib/kernel/services/playback_state_manager.dart (48 instrumented, 36 hit)
    - test/kernel/services/playback_controller_test.dart (343行现有测试)
    - test/kernel/services/playback_state_manager_test.dart (现有测试)
    - test/helpers/fake_engine.dart
  </read_first>
  <action>
扩展两个已有测试文件，覆盖剩余未测试路径。

**playback_controller (+12 lines: 42→54)：**
- addFiles 空列表边界
- removeAt 最后一个 item 后的状态
- clearPlaylist 后的 currentIndex
- reorder 边界（移到同一位置）
- togglePlayMode 循环所有模式
- openAndPlay 失败后状态恢复

**playback_state_manager (+12 lines: 36→48)：**
- 状态同步边界（engine state 变化但 manager 未初始化）
- 多次快速状态切换
- dispose 后状态变化不崩溃
- error 状态的恢复路径

```
// playback_controller 扩展
group('PlaybackController edge cases', () {
  test('addFiles with empty list does nothing')
  test('removeAt last item resets currentIndex to -1')
  test('clearPlaylist resets all state')
  test('reorder to same position is no-op')
  test('togglePlayMode cycles through all modes')
  test('openAndPlay failure restores previous state')
})

// playback_state_manager 扩展
group('PlaybackStateManager edge cases', () {
  test('handles rapid state transitions without crash')
  test('dispose prevents further state updates')
  test('error state can be recovered via recover()')
  test('syncs position during seeking state')
})
```

**测试数量：** ~12 个新用例
**预计覆盖提升：** +24 lines → playback_controller 100%, playback_state_manager 100%
  </action>
  <verify>
    <automated>
flutter test test/kernel/services/playback_controller_test.dart
flutter test test/kernel/services/playback_state_manager_test.dart
    </automated>
  </verify>
  <done>playback_controller + playback_state_manager 覆盖率提升至≥80%</done>
</task>

<task type="auto">
  <name>Task 7: 扩展 kernel_adapter + window_persistence + track_preference 测试</name>
  <files>
    test/kernel/adapter/kernel_adapter_routing_test.dart
    test/kernel/bridge/window_persistence_test.dart
    test/kernel/services/track_preference_service_test.dart
  </files>
  <read_first>
    - lib/kernel/adapter/kernel_adapter.dart (126 instrumented, 72 hit)
    - lib/kernel/bridge/window_persistence.dart (22 instrumented, 10 hit)
    - lib/kernel/services/track_preference_service.dart (31 instrumented, 21 hit)
    - test/kernel/adapter/kernel_adapter_routing_test.dart (Plan 08 创建)
    - test/kernel/bridge/window_persistence_test.dart (现有)
    - test/kernel/services/track_preference_service_test.dart (现有)
  </read_first>
  <action>
扩展三个已有测试文件，覆盖剩余路径。

**kernel_adapter (+30 lines: 72→102)：**
- 每个 capability 字段的边界路由（7 个字段各有 legacy/migrated 路径）
- open 方法的 generation 计数
- dispose 时 bundle 资源释放
- 错误传播路径（legacy engine 抛异常 → adapter 透传）

**window_persistence (+12 lines: 10→22)：**
- save/load 完整窗口状态
- 缺失字段的默认值处理
- 损坏数据的容错

**track_preference_service (+10 lines: 21→31)：**
- 切换不存在的轨道
- 空轨道列表处理
- 持久化失败时的降级
  </action>
  <verify>
    <automated>
flutter test test/kernel/adapter/kernel_adapter_routing_test.dart
flutter test test/kernel/bridge/window_persistence_test.dart
flutter test test/kernel/services/track_preference_service_test.dart
    </automated>
  </verify>
  <done>三个模块覆盖率均提升至≥80%</done>
</task>

<task type="auto">
  <name>Task 8: 扩展 startup_coordinator + subtitle_configurator + diagnostics 测试</name>
  <files>
    test/kernel/startup/startup_coordinator_test.dart
    test/kernel/engine/subtitle_configurator_test.dart
    test/kernel/diagnostics/clock_test.dart
  </files>
  <read_first>
    - lib/kernel/startup/startup_coordinator.dart (36 instrumented, 24 hit)
    - lib/kernel/engine/subtitle_configurator.dart (22 instrumented, 12 hit)
    - lib/kernel/diagnostics/clock.dart (7 instrumented, 4 hit)
    - lib/kernel/diagnostics/rss_provider.dart (7 instrumented, 4 hit)
    - lib/kernel/diagnostics/metrics_slot.dart (9 instrumented, 4 hit)
    - lib/kernel/diagnostics/event_log_slot.dart (6 instrumented, 4 hit)
    - 各自对应的测试文件
  </read_first>
  <action>
扩展多个小模块测试，每个提升几行覆盖。

**startup_coordinator (+12 lines: 24→36)：**
- 启动阶段超时处理
- 阶段依赖顺序验证
- 失败阶段的重试/跳过逻辑

**subtitle_configurator (+10 lines: 12→22)：**
- setExternalSubtitle 空路径
- setSubtitleDelay 边界值
- setEqualizer 参数验证

**diagnostics 小文件 (+9 lines: clock+rss+metrics+eventLog)：**
- SystemClock.now 返回合理值
- ProcessInfoRssProvider 返回正数
- NullMetricsSlot 方法不抛异常
- NullEventLogSlot 方法不抛异常
  </action>
  <verify>
    <automated>
flutter test test/kernel/startup/startup_coordinator_test.dart
flutter test test/kernel/engine/subtitle_configurator_test.dart
flutter test test/kernel/diagnostics/
    </automated>
  </verify>
  <done>小模块覆盖率均提升至≥80%</done>
</task>

<task type="auto">
  <name>Task 9: 覆盖率验证 + mdk.dll 瓶颈量化 + 目标评估</name>
  <files>
    .planning/phases/21-verify-migration-adapter-convergence/21-09-SUMMARY.md
  </files>
  <read_first>
    - coverage/lcov.info (最新覆盖率数据)
    - 所有新增/扩展的测试文件
  </read_first>
  <action>
**步骤 1：运行完整覆盖率**
```bash
flutter test --coverage
```

**步骤 2：计算最终覆盖率**
从 lcov.info 提取 kernel/ 总 instrumented 和 hit 行数，计算百分比。

**步骤 3：量化 mdk.dll 瓶颈**
列出所有 mdk 依赖文件的覆盖率，计算：
- 无 mdk mock 的理论天花板
- 达到 80% 需要额外覆盖的 mdk 文件行数
- 实现 mdk mock 的工作量估算

**步骤 4：生成 SUMMARY.md**

```markdown
## Coverage Results

| Metric | Before | After | Change |
|--------|--------|-------|--------|
| kernel/ covered lines | 1,734 | ??? | +??? |
| kernel/ coverage % | 57.6% | ???% | +???pp |
| Target | 80% | 80% | - |

## mdk.dll Bottleneck Analysis

| File | Lines | Status |
|------|---:|--------|
| fvp_engine.dart | 303 | 0.7% — requires mdk.Player mock |
| media_opener.dart | 85 | 0% — requires mdk.Player mock |
| position_poller.dart | 60 | 0% — requires mdk.Player mock |
| ... | ... | ... |
| **Total blocked** | **588** | **Headless CI impossible** |

## Recommendation

[If ≥80%]: Target achieved. Phase 21 VERIFY-05 resolved.
[If 70-79%]: Recommend (a) mdk.Player DI refactor for FvpEngine factory
  constructor, enabling ~200 additional lines of testable paths, OR
  (b) adjust target to 75% excluding mdk-dependent files from denominator.
[If <70%]: Additional waves needed. Prioritize [specific files].
```

**步骤 5：运行完整测试套件确认无回归**
```bash
flutter test
```
  </action>
  <verify>
    <automated>
flutter test --coverage
flutter analyze
    </automated>
  </verify>
  <done>覆盖率验证完成，mdk.dll 瓶颈量化，SUMMARY.md 生成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → kernel services | 测试验证纯 Dart 逻辑，不涉及用户输入或外部服务 |
| test → FakeEngine | 模拟引擎行为，不加载 mdk.dll |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-09-1 | Tampering | test dependencies | low | accept | 仅使用项目已有依赖（flutter_test, FakeEngine），无新外部包 |
| T-21-09-2 | Information Disclosure | coverage data | low | accept | lcov.info 仅包含行号映射，无敏感数据 |
</threat_model>

<verification>
- `flutter test` 全部通过（0 新增失败）
- `flutter test --coverage` kernel/ 覆盖率 ≥70%
- `flutter analyze` 零新增 error
- 4个新测试文件全部通过
- 7个扩展测试文件新增用例全部通过
- mdk.dll 瓶颈量化数据记录在 SUMMARY 中
</verification>

<success_criteria>
- kernel/ 覆盖率从57.6%提升至≥70%（纯 Dart 路径，无 mdk mock）
- 4个零覆盖纯 Dart 模块获得测试（breakpoint_saver, player_services, theme_service, debug_exporter）
- 7个中等覆盖模块测试扩展（playback_navigator, auto_advance_policy, engine_prewarm, playback_controller, playback_state_manager, kernel_adapter, window_persistence 等）
- 所有测试不依赖 mdk.dll
- mdk.dll 覆盖率瓶颈精确量化，包含达到80%所需工作量估算
- flutter test 全套件无新增失败
</success_criteria>

<artifacts>
本 plan 产出物：
- test/kernel/services/breakpoint_saver_test.dart — 断点保存模块测试
- test/kernel/player_services_test.dart — 服务容器测试
- test/kernel/services/theme_service_test.dart — 主题服务测试
- test/kernel/utils/debug_exporter_test.dart — 调试导出测试
- .planning/phases/21-verify-migration-adapter-convergence/21-09-SUMMARY.md — 覆盖率报告 + mdk 瓶颈分析
</artifacts>

<output>
Create `.planning/phases/21-verify-migration-adapter-convergence/21-09-SUMMARY.md` when done
</output>
