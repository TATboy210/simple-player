---
phase: 21-verify-migration-adapter-convergence
plan: 12
type: execute
wave: 7
depends_on:
  - "21-10"
  - "21-11"
files_modified:
  - test/diagnostics/kernel_logger_impl_test.dart
  - test/diagnostics/memory_monitor_test.dart
  - test/unit/kernel/bridge/window_service_test.dart
  - test/kernel/utils/log_test.dart
  - test/kernel/utils/perf_monitor_test.dart
  - test/kernel/engine/position_poller_test.dart
  - test/kernel/bridge/win32_display_enumerator_test.dart
  - test/kernel/player_services_test.dart
  - test/kernel/diagnostics/clock_test.dart
  - test/kernel/engine/engine_metrics_test.dart
  - test/kernel/engine/engine_event_log_test.dart
  - .planning/phases/21-verify-migration-adapter-convergence/21-12-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - VERIFY-05

must_haves:
  truths:
    - "kernel/ 覆盖率从 ~67%（Plan 10 DI 后估算）提升至 ≥80%"
    - "所有新增/扩展测试不依赖 mdk.dll（纯 Dart 测试替身）"
    - "Tier 1 大文件（kernel_logger + memory_monitor + window_service）获得深度测试覆盖"
    - "Tier 2 薄测试文件（log + perf_monitor + position_poller + win32_display_enumerator + player_services）测试扩展"
  artifacts:
    - "test/diagnostics/kernel_logger_impl_test.dart — KernelLoggerImpl 深度测试"
    - "test/diagnostics/memory_monitor_test.dart — MemoryMonitor 扩展测试"
    - "test/unit/kernel/bridge/window_service_test.dart — WindowService 扩展测试"
    - "test/kernel/utils/log_test.dart — Log 工具扩展测试"
    - "test/kernel/utils/perf_monitor_test.dart — PerfMonitor 扩展测试"
    - "test/kernel/engine/position_poller_test.dart — PositionPoller 扩展测试"
    - "test/kernel/bridge/win32_display_enumerator_test.dart — Win32DisplayEnumerator 扩展测试"
    - "test/kernel/player_services_test.dart — PlayerServices 扩展测试"
    - ".planning/phases/21-verify-migration-adapter-convergence/21-12-SUMMARY.md"
  key_links:
    - "KernelLoggerImpl → SpySink → trace/debug/info/warn/error/fatal 全路径覆盖"
    - "MemoryMonitor → FakeRssProvider + FakeClock → start/stop/snapshot/dispose 生命周期"
    - "WindowService → FakeWindowBridge → toggleFullscreen/setBounds/dispose 路径"
    - "fresh coverage → lcov.info → VERIFY-05 闭合判断"
---

<objective>
通过深度扩展 Tier 1 大文件和 Tier 2 薄测试文件的测试覆盖，将 kernel/ 覆盖率从 ~67% 提升至 ≥80%，闭合 VERIFY-05 覆盖率 gap。

Purpose: Plan 10 完成了 mdk.Player DI 注入（MdkPlayerLike + FakeMdkPlayer），Plan 11 解决了 SDK 编译阻塞。本 plan 专注于纯 Dart 测试路径的最大化覆盖：kernel_logger（406行）、memory_monitor（252行）、window_service（241行）三个大文件 + log/perf_monitor/position_poller/win32_display_enumerator/player_services 五个薄测试扩展。目标新增 ~400 covered 行，推动覆盖率过 80% 门槛。
Output: 3 个新测试文件 + 8 个扩展测试文件 + fresh coverage 测量 + SUMMARY。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-verify-migration-adapter-convergence/21-CONTEXT.md
@.planning/phases/21-verify-migration-adapter-convergence/21-VERIFICATION.md
@.planning/phases/21-verify-migration-adapter-convergence/21-10-PLAN.md
@.planning/phases/21-verify-migration-adapter-convergence/21-11-SUMMARY.md
@test/helpers/fake_engine.dart
@test/helpers/fake_mdk_player.dart
@test/diagnostics/kernel_logger_test.dart
@test/diagnostics/memory_monitor_test.dart
@test/unit/kernel/bridge/window_service_test.dart
@test/kernel/utils/log_test.dart
@test/kernel/utils/perf_monitor_test.dart
@test/kernel/engine/position_poller_test.dart
@test/kernel/bridge/win32_display_enumerator_test.dart
@test/kernel/player_services_test.dart
@test/kernel/diagnostics/clock_test.dart
@test/kernel/engine/engine_metrics_test.dart
@test/kernel/engine/engine_event_log_test.dart
</context>

<coverage_strategy>

## Coverage Gap Math

| Metric | Value |
|--------|-------|
| Total instrumented lines | ~3,059 |
| Current covered (est. post-Plan 10) | ~2,062 (67.4%) |
| Target for 80% | 2,447 lines |
| Gap to close | ~385 lines |

## Priority Targets by Impact

### Task 1 — Tier 1 Large Files (highest impact, ~400 lines)

| File | Lines | Strategy | Expected New Covered |
|------|-------|----------|---------------------|
| kernel_logger.dart | 406 | Expand kernel_logger_impl_test: all 6 log levels, context map, path redaction, CompositeSink multi-sink, init/reset lifecycle, LogLevel comparison, sink filtering | ~320 |
| memory_monitor.dart | 252 | Expand memory_monitor_test: static init/I/dispose lifecycle, interval changes, RSS read errors, snapshot history, dispose idempotency, logger integration | ~200 |
| window_service.dart | 241 | Expand window_service_test: toggleFullscreen/setBounds/setMode/dispose, WindowState fields, bounds clamping, mode transitions | ~190 |

**Task 1 subtotal: ~710 new covered lines** (conservative: ~400 accounting for existing partial coverage)

### Task 2 — Tier 2 Thin Tests (medium impact, ~150 lines)

| File | Current Ratio | Strategy | Expected New Covered |
|------|--------------|----------|---------------------|
| utils/log.dart | 0.03 | Expand log_test: Log.d/i/w/e/f, tag filtering, redaction, performance timing | ~50 |
| utils/perf_monitor.dart | 0.19 | Expand perf_monitor_test: start/end frame, jank detection, histogram, dispose | ~30 |
| engine/position_poller.dart | 0.20 | Expand position_poller_test: start/stop, interval changes, callback firing, dispose | ~30 |
| bridge/win32_display_enumerator.dart | 0.22 | Expand win32_display_enumerator_test: enumerate, filter, bounds calculation | ~30 |
| player_services.dart | 0.36 | Expand player_services_test: init order, error handling, generation tracking | ~20 |

**Task 2 subtotal: ~160 new covered lines**

### Task 3 — Tier 3 Medium Files + Coverage Verification (~60 lines)

| File | Lines | Strategy | Expected New Covered |
|------|-------|----------|---------------------|
| diagnostics/clock.dart | 7 | Expand clock_test: SystemClock.now returns DateTime | ~3 |
| engine/engine_metrics.dart | ~64 | Expand engine_metrics_test: record/reset/histogram | ~20 |
| engine/engine_event_log.dart | ~59 | Expand engine_event_log_test: add/clear/export | ~20 |
| diagnostics bundle small files | ~50 | Additional edge cases | ~15 |

**Task 3 subtotal: ~60 new covered lines**

## Total Expected New Coverage

Conservative estimate: 400 + 160 + 60 = **~620 new covered lines**
This would push from ~2,062 to ~2,682 covered out of ~3,059 = **~87.7%** — well above 80%.

Even at 60% efficiency: 370 + 100 + 40 = ~510 → ~2,572 / 3,059 = **~84.1%** — still above target.

</coverage_strategy>

<tasks>

<task type="auto">
  <name>Task 1: Tier 1 Deep Coverage — kernel_logger + memory_monitor + window_service</name>
  <files>
    test/diagnostics/kernel_logger_impl_test.dart
    test/diagnostics/memory_monitor_test.dart
    test/unit/kernel/bridge/window_service_test.dart
  </files>
  <read_first>
    - lib/kernel/diagnostics/kernel_logger.dart (406 lines: LogLevel, LogSink, DevToolsSink, DebugPrintSink, NullSink, CompositeSink, KernelLoggerImpl, redactPath)
    - test/diagnostics/kernel_logger_test.dart (existing tests: NullKernelLogger signatures + SpySink)
    - lib/kernel/diagnostics/memory_monitor.dart (252 lines: static init/I, Timer-based sampling, snapshot, dispose)
    - test/diagnostics/memory_monitor_test.dart (existing tests: constructor auto-start, snapshot, FakeRssProvider/FakeClock/FakeLogger)
    - lib/kernel/bridge/window_service.dart (241 lines: WindowService composition, toggleFullscreen, setBounds, WindowState)
    - test/unit/kernel/bridge/window_service_test.dart (existing tests: composition defaults, mode delegation)
    - test/helpers/fake_engine.dart (FakeEngine pattern reference)
    - lib/kernel/diagnostics/clock.dart (SystemClock interface)
    - lib/kernel/diagnostics/rss_provider.dart (RssProvider interface)
  </read_first>
  <action>
**文件 1：`test/diagnostics/kernel_logger_impl_test.dart` — KernelLoggerImpl 深度测试**

KernelLoggerImpl 是 kernel_logger.dart 的核心实现（~200 行可执行代码），当前测试仅覆盖 NullKernelLogger 签名和基本 SpySink。需要深度覆盖：

```
group('KernelLoggerImpl', () {
  late SpySink spy;
  late KernelLoggerImpl logger;

  setUp(() {
    spy = SpySink();
    KernelLoggerImpl.resetForTesting();
    logger = KernelLoggerImpl.init(sinks: [spy]);
  });

  tearDown(() {
    KernelLoggerImpl.resetForTesting();
  });

  // --- Lifecycle ---
  group('lifecycle', () {
    test('init() sets static I accessor')
    test('resetForTesting() clears static instance')
    test('init() is idempotent — second call returns same instance')
    test('I throws StateError before init()')
  })

  // --- All 6 log levels ---
  group('trace', () {
    test('records trace level with message')
    test('passes context map to sink')
    test('does not record when level > trace (if filtering)')
  })

  group('debug', () {
    test('records debug level with message')
    test('passes context map to sink')
  })

  group('info', () {
    test('records info level with message')
    test('passes context map to sink')
  })

  group('warn', () {
    test('records warn level with message')
    test('passes context map to sink')
  })

  group('error', () {
    test('records error level with message')
    test('includes error object in context')
    test('includes stackTrace in context')
    test('works with only message (no error/stackTrace)')
    test('works with error only (no stackTrace)')
    test('works with stackTrace only (no error)')
  })

  group('fatal', () {
    test('records fatal level with message')
    test('includes error and stackTrace')
  })

  // --- CompositeSink ---
  group('CompositeSink', () {
    test('forwards to all registered sinks')
    test('handles empty sink list')
    test('one sink throwing does not prevent others from receiving')
  })

  // --- Path redaction (D17) ---
  group('redactPath', () {
    test('strips absolute directory prefix from file path')
    test('preserves filename only')
    test('handles path with no directory separators')
    test('handles empty string')
  })

  // --- LogLevel ordering ---
  group('LogLevel', () {
    test('trace < debug < info < warn < error < fatal')
    test('LogLevel.values has 6 entries')
  })

  // --- NullKernelLogger ---
  group('NullKernelLogger', () {
    test('all methods are no-ops (return normally)')
    test('accepts context parameter')
    test('accepts error and stackTrace parameters')
  })
})
```

**测试数量：** ~35 个用例
**预计覆盖：** kernel_logger.dart ~320 行（LogLevel 全值、LogSink 接口、所有 Sink 实现、KernelLoggerImpl 全生命周期、redactPath 全路径、CompositeSink 多路分发）

---

**文件 2：`test/diagnostics/memory_monitor_test.dart` — 扩展 MemoryMonitor 测试**

当前测试仅覆盖构造函数自动启动和基本 snapshot。需要扩展覆盖 static lifecycle、error handling、dispose 等路径：

```
group('MemoryMonitor expanded', () {
  // --- Static init/I lifecycle ---
  group('static lifecycle', () {
    test('init() sets _instance')
    test('I returns instance after init()')
    test('I throws StateError before init()')
    test('init() with custom interval')
    test('resetForTesting() clears _instance')
  })

  // --- Sampling behavior ---
  group('sampling', () {
    test('takes snapshot at each interval tick')
    test('snapshot includes rssBytes from RssProvider')
    test('snapshot includes timestamp from Clock')
    test('notifies snapshotNotifier listeners on each tick')
    test('handles RssProvider returning 0 bytes')
  })

  // --- Error handling ---
  group('error handling', () {
    test('RssProvider throwing does not crash monitor')
    test('logs warning on RssProvider error')
    test('continues sampling after RssProvider error')
  })

  // --- Dispose ---
  group('dispose', () {
    test('stops timer')
    test('is idempotent — second call is no-op')
    test('snapshot returns null after dispose')
    test('no further notifications after dispose')
  })

  // --- MemorySnapshot ---
  group('MemorySnapshot', () {
    test('rssBytes reflects provider value')
    test('timestamp reflects clock value')
    test('toString includes rssBytes and timestamp')
  })
})
```

**测试数量：** ~20 个新用例
**预计覆盖：** memory_monitor.dart ~200 行（全生命周期、错误处理、dispose 幂等性、snapshot 结构）

---

**文件 3：`test/unit/kernel/bridge/window_service_test.dart` — 扩展 WindowService 测试**

当前测试仅覆盖 composition defaults 和 mode delegation。需要扩展覆盖 toggleFullscreen、setBounds、dispose 等路径：

```
group('WindowService expanded', () {
  // --- Fullscreen ---
  group('toggleFullscreen', () {
    test('transitions from windowed to fullscreen')
    test('transitions from fullscreen back to windowed')
    test('mode.value reflects fullscreen state')
  })

  // --- Bounds ---
  group('setBounds', () {
    test('updates windowSize')
    test('updates windowPosition')
    test('clamps bounds to screen dimensions')
  })

  // --- Mode transitions ---
  group('mode transitions', () {
    test('setMode updates state.mode')
    test('windowed → maximized → windowed cycle')
    test('fullscreen → windowed restores previous size')
  })

  // --- Dispose ---
  group('dispose', () {
    test('disposes all state notifiers')
    test('is idempotent')
  })

  // --- WindowState ---
  group('WindowState', () {
    test('windowSize defaults to 1280x752')
    test('windowPosition defaults to Offset.zero')
    test('mode defaults to windowed')
  })
})
```

**测试数量：** ~15 个新用例
**预计覆盖：** window_service.dart ~190 行（全路径：toggle/set/get/dispose）
  </action>
  <verify>
    <automated>
flutter test test/diagnostics/kernel_logger_impl_test.dart
flutter test test/diagnostics/memory_monitor_test.dart
flutter test test/unit/kernel/bridge/window_service_test.dart
flutter test --coverage
    </automated>
  </verify>
  <done>kernel_logger + memory_monitor + window_service 三个大文件获得深度测试覆盖，覆盖率显著提升</done>
</task>

<task type="auto">
  <name>Task 2: Tier 2 Thin Test Expansion — log + perf_monitor + position_poller + win32_display_enumerator + player_services</name>
  <files>
    test/kernel/utils/log_test.dart
    test/kernel/utils/perf_monitor_test.dart
    test/kernel/engine/position_poller_test.dart
    test/kernel/bridge/win32_display_enumerator_test.dart
    test/kernel/player_services_test.dart
  </files>
  <read_first>
    - lib/kernel/utils/log.dart (294 lines: Log.d/i/w/e/f, tag filtering, redaction)
    - test/kernel/utils/log_test.dart (existing: 8 lines of coverage)
    - lib/kernel/utils/perf_monitor.dart (162 lines: PerfMonitor start/end frame, jank detection)
    - test/kernel/utils/perf_monitor_test.dart (existing: 31 lines of coverage)
    - lib/kernel/engine/position_poller.dart (163 lines: PositionPoller start/stop/callback)
    - test/kernel/engine/position_poller_test.dart (existing: 33 lines of coverage)
    - lib/kernel/bridge/win32_display_enumerator.dart (260 lines: enumerate, filter, bounds)
    - test/kernel/bridge/win32_display_enumerator_test.dart (existing: 57 lines of coverage)
    - lib/kernel/player_services.dart (182 lines: DI container, init, dispose)
    - test/kernel/player_services_test.dart (existing: 66 lines of coverage)
    - test/helpers/fake_engine.dart (FakeEngine pattern)
  </read_first>
  <action>
**文件 1：`test/kernel/utils/log_test.dart` — 扩展 Log 工具测试**

Log 工具（294行）当前仅 8 行测试覆盖（ratio 0.03）。扩展覆盖所有日志方法和过滤行为：

```
group('Log expanded', () {
  group('Log.d', () {
    test('calls KernelLogger.debug with message')
    test('passes context map when provided')
    test('works without context parameter')
  })

  group('Log.i', () {
    test('calls KernelLogger.info with message')
    test('passes context map when provided')
  })

  group('Log.w', () {
    test('calls KernelLogger.warn with message')
    test('passes context map when provided')
  })

  group('Log.e', () {
    test('calls KernelLogger.error with message')
    test('includes error object when provided')
    test('includes stackTrace when provided')
    test('works with only message')
  })

  group('Log.f', () {
    test('calls KernelLogger.fatal with message')
    test('includes error and stackTrace when provided')
  })

  group('tag filtering', () {
    test('logs with tag prefix when tag provided')
    test('omits tag when not provided')
  })
})
```

**测试数量：** ~15 个用例
**预计覆盖：** log.dart ~50 行

---

**文件 2：`test/kernel/utils/perf_monitor_test.dart` — 扩展 PerfMonitor 测试**

PerfMonitor（162行）当前 31 行测试覆盖（ratio 0.19）。扩展覆盖帧计时、jank 检测：

```
group('PerfMonitor expanded', () {
  group('frame timing', () {
    test('startFrame records timestamp')
    test('endFrame calculates duration')
    test('endFrame without startFrame is no-op')
  })

  group('jank detection', () {
    test('flags frame as jank when duration > threshold')
    test('does not flag frame as jank when duration <= threshold')
    test('jank count increments on janky frames')
    test('jank count does not increment on smooth frames')
  })

  group('histogram', () {
    test('records frame durations in histogram')
    test('histogram has expected bucket count')
    test('p95 calculation returns correct percentile')
  })

  group('dispose', () {
    test('clears all state')
    test('is idempotent')
  })
})
```

**测试数量：** ~12 个用例
**预计覆盖：** perf_monitor.dart ~30 行

---

**文件 3：`test/kernel/engine/position_poller_test.dart` — 扩展 PositionPoller 测试**

PositionPoller（163行）当前 33 行测试覆盖（ratio 0.20）。扩展覆盖 start/stop、interval、callback：

```
group('PositionPoller expanded', () {
  group('start/stop', () {
    test('start begins polling at specified interval')
    test('stop halts polling')
    test('start is idempotent — second call is no-op')
    test('stop is idempotent — second call is no-op')
    test('start after stop restarts polling')
  })

  group('callback', () {
    test('fires callback with current position at each tick')
    test('callback receives position from engine.position.value')
    test('does not fire callback after stop')
  })

  group('interval changes', () {
    test('respects custom interval parameter')
    test('default interval is 250ms')
  })

  group('dispose', () {
    test('stops polling')
    test('is idempotent')
  })
})
```

**测试数量：** ~12 个用例
**预计覆盖：** position_poller.dart ~30 行

---

**文件 4：`test/kernel/bridge/win32_display_enumerator_test.dart` — 扩展 Win32DisplayEnumerator 测试**

Win32DisplayEnumerator（260行）当前 57 行测试覆盖（ratio 0.22）。扩展覆盖枚举、过滤、bounds 计算：

```
group('Win32DisplayEnumerator expanded', () {
  group('enumerate', () {
    test('returns list of DisplayInfo')
    test('each DisplayInfo has bounds Rect')
    test('each DisplayInfo has workArea Rect')
    test('workArea is subset of bounds')
  })

  group('filter', () {
    test('findDisplayForPoint returns correct display')
    test('findDisplayForPoint returns null for off-screen point')
    test('primaryDisplay returns the primary monitor')
  })

  group('bounds calculation', () {
    test('clampToBounds keeps rect within display')
    test('clampToBounds handles rect already within bounds')
    test('clampToBounds handles rect larger than display')
  })
})
```

**测试数量：** ~10 个用例
**预计覆盖：** win32_display_enumerator.dart ~30 行

---

**文件 5：`test/kernel/player_services_test.dart` — 扩展 PlayerServices 测试**

PlayerServices（182行）当前 66 行测试覆盖（ratio 0.36）。扩展覆盖 init order、error handling：

```
group('PlayerServices expanded', () {
  group('init order', () {
    test('engine is created before controller')
    test('diagnostics are initialized before engine')
    test('playlist store is loaded during init')
  })

  group('error handling', () {
    test('init failure sets lastError')
    test('dispose after failed init is safe')
  })

  group('generation tracking', () {
    test('playlistGeneration starts at 0')
    test('dispose disposes generation notifier')
  })
})
```

**测试数量：** ~8 个用例
**预计覆盖：** player_services.dart ~20 行
  </action>
  <verify>
    <automated>
flutter test test/kernel/utils/log_test.dart
flutter test test/kernel/utils/perf_monitor_test.dart
flutter test test/kernel/engine/position_poller_test.dart
flutter test test/kernel/bridge/win32_display_enumerator_test.dart
flutter test test/kernel/player_services_test.dart
    </automated>
  </verify>
  <done>五个 Tier 2 薄测试文件全部扩展，覆盖率达到预期水平</done>
</task>

<task type="auto">
  <name>Task 3: Tier 3 Coverage + Final Verification + SUMMARY</name>
  <files>
    test/kernel/diagnostics/clock_test.dart
    test/kernel/engine/engine_metrics_test.dart
    test/kernel/engine/engine_event_log_test.dart
    .planning/phases/21-verify-migration-adapter-convergence/21-12-SUMMARY.md
  </files>
  <read_first>
    - lib/kernel/diagnostics/clock.dart (7 lines: SystemClock.now, FakeClock)
    - test/kernel/diagnostics/clock_test.dart (existing tests)
    - lib/kernel/engine/engine_metrics.dart (~64 lines: EngineMetrics record/reset/histogram)
    - test/kernel/engine/engine_metrics_test.dart (existing tests)
    - lib/kernel/engine/engine_event_log.dart (~59 lines: EngineEventLog add/clear/export)
    - test/kernel/engine/engine_event_log_test.dart (existing tests)
    - coverage/lcov.info (current coverage data)
  </read_first>
  <action>
**文件 1：`test/kernel/diagnostics/clock_test.dart` — 扩展 Clock 测试**

```
group('Clock expanded', () {
  test('SystemClock.now returns DateTime close to DateTime.now()')
  test('FakeClock returns configured time')
  test('FakeClock time does not advance automatically')
})
```

**文件 2：`test/kernel/engine/engine_metrics_test.dart` — 扩展 EngineMetrics 测试**

```
group('EngineMetrics expanded', () {
  group('record', () {
    test('records metric value')
    test('multiple records accumulate')
  })

  group('reset', () {
    test('clears all recorded metrics')
    test('is idempotent')
  })

  group('histogram', () {
    test('returns histogram of recorded values')
    test('empty metrics returns empty histogram')
  })
})
```

**文件 3：`test/kernel/engine/engine_event_log_test.dart` — 扩展 EngineEventLog 测试**

```
group('EngineEventLog expanded', () {
  group('add', () {
    test('adds event to log')
    test('events are ordered by time')
    test('respects max capacity')
  })

  group('clear', () {
    test('removes all events')
    test('is idempotent')
  })

  group('export', () {
    test('returns list of events')
    test('empty log returns empty list')
  })
})
```

**文件 4：`21-12-SUMMARY.md` — 覆盖率验证报告**

运行 `flutter test --coverage`，计算最终 kernel/ 覆盖率。生成 SUMMARY：

```markdown
## Coverage Results

| Metric | Before (Plan 10 est.) | After (Plan 12) | Change |
|--------|----------------------|-----------------|--------|
| kernel/ covered lines | ~2,062 | ??? | +??? |
| kernel/ coverage % | ~67.4% | ???% | +???pp |
| Target | 80% | 80% | - |

## Test Files Added/Modified

| File | Type | New Test Cases |
|------|------|---------------|
| test/diagnostics/kernel_logger_impl_test.dart | New | ~35 |
| test/diagnostics/memory_monitor_test.dart | Expanded | ~20 |
| test/unit/kernel/bridge/window_service_test.dart | Expanded | ~15 |
| test/kernel/utils/log_test.dart | Expanded | ~15 |
| test/kernel/utils/perf_monitor_test.dart | Expanded | ~12 |
| test/kernel/engine/position_poller_test.dart | Expanded | ~12 |
| test/kernel/bridge/win32_display_enumerator_test.dart | Expanded | ~10 |
| test/kernel/player_services_test.dart | Expanded | ~8 |
| test/kernel/diagnostics/clock_test.dart | Expanded | ~3 |
| test/kernel/engine/engine_metrics_test.dart | Expanded | ~6 |
| test/kernel/engine/engine_event_log_test.dart | Expanded | ~6 |

## VERIFY-05 Status

[If ≥80%]: TARGET ACHIEVED. Phase 21 VERIFY-05 resolved.
[If <80%]: Additional coverage needed. List remaining files with line counts.
```

**Final verification steps:**
1. `flutter test --coverage` — measure final coverage
2. `flutter analyze` — zero new errors
3. `flutter test` — full suite passes (no regressions)
  </action>
  <verify>
    <automated>
flutter test --coverage
flutter analyze
flutter test
    </automated>
  </verify>
  <done>kernel/ 覆盖率 ≥80%，SUMMARY.md 生成，VERIFY-05 闭合</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → kernel diagnostics | 测试验证纯 Dart 诊断逻辑，不涉及用户输入或外部服务 |
| test → FakeEngine/FakeMdkPlayer | 模拟引擎/播放器行为，不加载 mdk.dll |
| test → WindowService | 模拟窗口操作，不调用 Win32 API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-12-1 | Tampering | test dependencies | low | accept | 仅使用项目已有依赖（flutter_test, FakeEngine, FakeMdkPlayer），无新外部包 |
| T-21-12-2 | Information Disclosure | coverage data | low | accept | lcov.info 仅包含行号映射，无敏感数据 |
| T-21-12-3 | Elevation of Privilege | KernelLoggerImpl.resetForTesting | low | accept | 仅在测试中使用，不影响生产代码路径 |
</threat_model>

<verification>
- `flutter test --coverage` kernel/ 覆盖率 ≥80%
- `flutter analyze lib/kernel/` 零新增 error
- `flutter test` 全套件无新增失败
- 所有新增测试不依赖 mdk.dll（纯 Dart 测试替身）
- 3 个新测试文件全部通过
- 8 个扩展测试文件新增用例全部通过
</verification>

<success_criteria>
- kernel/ 覆盖率从 ~67% 提升至 ≥80%（VERIFY-05 闭合）
- Tier 1 大文件（kernel_logger + memory_monitor + window_service）获得深度测试覆盖
- Tier 2 薄测试文件（log + perf_monitor + position_poller + win32_display_enumerator + player_services）全部扩展
- Tier 3 小文件（clock + engine_metrics + engine_event_log）补充覆盖
- 所有测试不依赖 mdk.dll
- flutter test 全套件无新增失败
- 覆盖率数据记录在 SUMMARY 中
</success_criteria>

<artifacts>
本 plan 产出物：
- test/diagnostics/kernel_logger_impl_test.dart — KernelLoggerImpl 深度测试（~35 用例）
- test/diagnostics/memory_monitor_test.dart — MemoryMonitor 扩展测试（~20 用例）
- test/unit/kernel/bridge/window_service_test.dart — WindowService 扩展测试（~15 用例）
- test/kernel/utils/log_test.dart — Log 工具扩展测试（~15 用例）
- test/kernel/utils/perf_monitor_test.dart — PerfMonitor 扩展测试（~12 用例）
- test/kernel/engine/position_poller_test.dart — PositionPoller 扩展测试（~12 用例）
- test/kernel/bridge/win32_display_enumerator_test.dart — Win32DisplayEnumerator 扩展测试（~10 用例）
- test/kernel/player_services_test.dart — PlayerServices 扩展测试（~8 用例）
- test/kernel/diagnostics/clock_test.dart — Clock 扩展测试（~3 用例）
- test/kernel/engine/engine_metrics_test.dart — EngineMetrics 扩展测试（~6 用例）
- test/kernel/engine/engine_event_log_test.dart — EngineEventLog 扩展测试（~6 用例）
- .planning/phases/21-verify-migration-adapter-convergence/21-12-SUMMARY.md — 覆盖率验证报告
</artifacts>

<output>
Create `.planning/phases/21-verify-migration-adapter-convergence/21-12-SUMMARY.md` when done
</output>
