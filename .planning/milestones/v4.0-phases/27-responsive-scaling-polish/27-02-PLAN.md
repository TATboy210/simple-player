---
phase: 27-responsive-scaling-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - lib/ui/dialogs/settings/settings_overlay_shell.dart
  - test/ui/dialogs/settings_responsive_scaling_test.dart
  - test/ui/dialogs/settings_responsive_integration_test.dart
autonomous: true
requirements:
  - SCALE-01
  - SCALE-02
  - SCALE-03
must_haves:
  truths:
    - All 7 tab labels are visible at 400px panel width (compact mode)
    - Tab bar height reduces to 48px in compact mode
    - Animation duration stays 200ms (per D-06, unchanged)
    - BackdropFilter remains active during open/close animation (per D-08)
    - Breakpoint crossing causes immediate visual switch, no interpolated transition (per D-09)
  artifacts:
    - Final responsive settings panel with all SCALE-01/02/03 tests passing
  key_links:
    - MediaQuery.width drives both panel continuous scaling and tab bar compact mode
    - Tokens responsive constants are single source of truth for all dimensions
---

<objective>
Polish the responsive settings panel: verify all 7 tabs render at compact width, finalize animation timing compliance, and complete the integration test suite covering all SCALE-01/02/03 success criteria.

Purpose: Ensure the panel works correctly at extreme sizes (400px compact and 600px fullscreen), all 7 tabs remain visible without scrolling, and the complete test suite validates every success criterion from the roadmap.

Output: Polished settings_overlay_shell.dart with verified compact mode, complete test coverage for all 5 roadmap success criteria.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-responsive-scaling-polish/27-CONTEXT.md
@.planning/phases/27-responsive-scaling-polish/27-01-SUMMARY.md
@lib/ui/dialogs/settings/settings_overlay_shell.dart
@lib/ui/theme/tokens.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify compact mode tab rendering and finalize animation timing</name>
  <files>lib/ui/dialogs/settings/settings_overlay_shell.dart, test/ui/dialogs/settings_responsive_scaling_test.dart</files>
  <read_first>
    - lib/ui/dialogs/settings/settings_overlay_shell.dart — _buildTabBar method, current tab labels at lines 109-117, SettingsNavItem usage
    - lib/ui/dialogs/settings/_settings_nav_item.dart — nav item widget signature, accepts fontSize/label
    - test/ui/dialogs/settings_responsive_scaling_test.dart — existing tests from plan 01
    - lib/ui/shared/apple_curves.dart — verify AppleCurves constants unchanged
  </read_first>
  <behavior>
    - Test: at 400px panel width, all 7 tab labels ("通用"/"均衡器"/"音频"/"视频"/"快捷键"/"关于"/"性能") are visible
    - Test: at 400px panel width, tab bar height is 48px
    - Test: at 600px panel width, tab bar height is 64px
    - Test: open/close animation duration is 200ms (assert Duration(milliseconds: 200))
    - Test: BackdropFilter present during open state
  </behavior>
  <action>
Step 1 — Verify all 7 tabs render at compact width:
  In the existing test file, add a test that pumps at 500x400 (compact, panelWidth=400).
  Assert all 7 tab label texts are visible (findsWidgets for each label string).
  This validates D-03: "400px panel width 7 tabs still all visible, no scroll, no hide."

Step 2 — Verify tab bar height adapts:
  Add test: pump at 1000x800, find the tab bar Container, assert height is 64.
  Add test: pump at 500x400, find the tab bar Container, assert height is 48.

Step 3 — Verify animation constants unchanged (per D-06):
  Assert `SettingsOverlayShell.animationDuration == Duration(milliseconds: 200)`.
  Assert `AppleCurves.fullscreenEnter` and `AppleCurves.fullscreenExit` are still used.
  This is a read-only assertion — no code changes for D-06.

Step 4 — Verify D-08 (BackdropFilter during animation):
  Open panel at 800x600, pump, assert BackdropFilter widget exists within GlassContainer.
  This confirms blur stays active during open state.

Step 5 — Verify D-09 (no transition on breakpoint crossing):
  Pump at 1000x800 (normal), assert tab fontSize 14.0.
  Pump at 500x400 (compact), assert tab fontSize 12.0.
  No AnimatedContainer or Tween in tab bar — verify by checking the build method uses direct conditional.
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter test test/ui/dialogs/settings_responsive_scaling_test.dart -x mdk-dll 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 7 tab labels visible at 400px panel width (compact mode)
    - Tab bar height is 48px in compact, 64px in normal
    - Animation duration constant is 200ms (unchanged per D-06)
    - BackdropFilter present when panel is open (per D-08)
    - Tab bar uses conditional sizing, not AnimatedContainer (per D-09)
    - All tests pass
  </acceptance_criteria>
  <done>Compact mode renders all 7 tabs, tab bar height adapts, animation timing verified unchanged, BackdropFilter confirmed active.</done>
</task>

<task type="auto">
  <name>Task 2: Complete SCALE success criteria verification and full regression</name>
  <files>test/ui/dialogs/settings_responsive_integration_test.dart</files>
  <read_first>
    - test/ui/dialogs/settings_responsive_integration_test.dart — existing tests from plan 01
    - .planning/ROADMAP.md lines 387-395 — Phase 27 Success Criteria (5 items)
    - test/ui/dialogs/settings_overlay_shell_test.dart — existing regression tests
    - lib/ui/dialogs/settings/settings_overlay_shell.dart — final state after plan 01
  </read_first>
  <behavior>
    - Test: success criterion 1 — MediaQuery.size drives panel sizing (pump at multiple sizes, assert panel dimensions change)
    - Test: success criterion 2 — fullscreen mode panel is 600x480 (pump at 1920x1080, assert exact size)
    - Test: success criterion 3 — small window <800px panel is 400x320 (pump at 600x400, assert exact size)
    - Test: success criterion 4 — open/close animation completes without error (pump animation frames)
    - Test: success criterion 5 — key paths: open, close, switch tab, drag, keyboard navigation all work
  </behavior>
  <action>
Create or extend the integration test file to map each of the 5 ROADMAP success criteria to explicit test assertions:

1. **SC-1: MediaQuery.size drives panel sizing** — Pump at 3 different window sizes (600, 1000, 1920 width), assert panel width changes proportionally and clamps correctly.

2. **SC-2: Fullscreen 600x480** — Pump at 1920x1080 (fullscreen). Assert panel SizedBox width == 600.0, height == 480.0. Tab bar is normal mode (fontSize 14.0).

3. **SC-3: Small window 400x320** — Pump at 600x400 (<800px). Assert panel SizedBox width == 400.0, height == 320.0. Tab bar is compact mode (fontSize 12.0, height 48).

4. **SC-4: 60fps animation** — Open panel, pump 12 frames (200ms at 60fps), assert no exceptions. Verify AnimatedOpacity and AnimatedScale are present with duration 200ms.

5. **SC-5: Key paths** — Run through: open (controller.open + pump), close (mask tap + pump), switch tab (tap tab 2 + pump), drag (drag title bar + pump), keyboard navigation (send ESC/LB/RB events). Assert each produces expected state change.

Additionally, run the FULL existing test suite regression:
  `flutter test test/ui/dialogs/ -x mdk-dll`
  Assert zero failures — ensures plan 01 changes don't break Phase 23-26 tests.

Final check: `flutter analyze` — zero errors.
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter test test/ui/dialogs/settings_responsive_integration_test.dart -x mdk-dll 2>&1 | tail -5 && flutter test test/ui/dialogs/ -x mdk-dll 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SC-1: Panel width changes with MediaQuery.size at 3 different window sizes
    - SC-2: At 1920x1080, panel is exactly 600x480
    - SC-3: At 600x400, panel is exactly 400x320
    - SC-4: Animation completes in 200ms without exceptions
    - SC-5: All key paths (open/close/tab/drag/keyboard) work correctly
    - Full regression: `flutter test test/ui/dialogs/ -x mdk-dll` — zero failures
    - `flutter analyze` — zero new errors
  </acceptance_criteria>
  <done>All 5 ROADMAP success criteria verified by automated tests. Full regression of settings tests passes. flutter analyze clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Panel size at extreme windows | Must not overflow or clip at 400px or 600px |
| Animation frame completion | Must not leave panel in partial state after 200ms |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03 | Denial of Service | Tab bar overflow at extreme compact size | low | mitigate | Test asserts all 7 tabs visible at 400px |
| T-27-04 | Information Disclosure | N/A | N/A | N/A | No sensitive data in this phase |
| T-27-SC | Tampering | npm/pip/cargo installs | N/A | N/A | No package installs in this phase |
</threat_model>

<verification>
1. `flutter analyze` — zero errors
2. `flutter test test/ui/dialogs/settings_responsive_scaling_test.dart` — all pass
3. `flutter test test/ui/dialogs/settings_responsive_integration_test.dart` — all pass
4. `flutter test test/ui/dialogs/ -x mdk-dll` — full regression zero failures
5. SC-1: Panel width varies with window size (3 sizes tested)
6. SC-2: Fullscreen panel = 600x480
7. SC-3: Compact panel = 400x320
8. SC-4: Animation completes without error
9. SC-5: All key paths functional
</verification>

<success_criteria>
- All 5 ROADMAP success criteria for Phase 27 are verified by automated tests
- Full settings test suite (Phase 23-26) regression passes
- flutter analyze clean
- Zero new warnings or errors
</success_criteria>

<output>
Create `.planning/phases/27-responsive-scaling-polish/27-02-SUMMARY.md` when done
</output>
