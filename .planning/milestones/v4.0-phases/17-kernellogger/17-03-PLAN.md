---
phase: 17-kernellogger
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - test/diagnostics/kernel_logger_test.dart
autonomous: true
requirements:
  - LOG-02
  - LOG-05

must_haves:
  truths:
    - "All sink types (DevToolsSink, DebugPrintSink, NullSink, CompositeSink) have behavioral tests"
    - "KernelLoggerImpl.init() and .I accessor have lifecycle tests"
    - "LogLevel enum has 6 values matching KernelLogger's 6 methods"
    - "Path redaction (D17) is tested: full paths stripped to filename-only in log output"
    - "CI grep gate passes (confirms LOG-01 structural property)"
  artifacts:
    - "test/diagnostics/kernel_logger_test.dart (~150 lines)"
  key_links:
    - "Tests validate Plan 01 implementation + Plan 02 migration correctness"
    - "CI gate script from Plan 02 used as automated verify"
---

<objective>
Extend the kernel_logger_test.dart with comprehensive behavioral tests for all sink types, KernelLoggerImpl lifecycle, and LogLevel enum. Run full verification (tests + gate + analyze).

Purpose: Verify the entire Phase 17 implementation is correct and structurally sound.
Output: Full test coverage for KernelLogger + passing CI gate + green test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-kernellogger/17-CONTEXT.md
@.planning/phases/17-kernellogger/17-RESEARCH.md
@.planning/phases/17-kernellogger/17-01-SUMMARY.md
@.planning/phases/17-kernellogger/17-02-SUMMARY.md

# Implementation to test
@lib/kernel/diagnostics/kernel_logger.dart

# Existing tests (to extend)
@test/diagnostics/kernel_logger_test.dart

# CI gate (from Plan 02)
@tool/audit/kernel_logger_gate.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend kernel_logger_test.dart with behavioral tests for all sinks + KernelLoggerImpl</name>
  <files>test/diagnostics/kernel_logger_test.dart</files>
  <read_first>
    - test/diagnostics/kernel_logger_test.dart (existing signature tests for NullKernelLogger)
    - lib/kernel/diagnostics/kernel_logger.dart (full implementation from Plan 01)
    - test/diagnostics/diagnostics_bundle_test.dart (test style reference)
  </read_first>
  <action>
Extend `test/diagnostics/kernel_logger_test.dart` with the following test groups (keep existing NullKernelLogger tests intact):

**Group 1: LogLevel enum**
- Test: `LogLevel.values` has exactly 6 elements
- Test: values are in order trace, debug, info, warn, error, fatal

**Group 2: NullSink**
- Test: `NullSink().log()` returns normally for all LogLevel values
- Test: NullSink is const-constructible

**Group 3: DebugPrintSink**
- Test: `DebugPrintSink().log(LogLevel.warn, 'hello')` returns normally
- Test: DebugPrintSink is const-constructible
- Note: Cannot easily assert debugPrint output in tests without overriding debugPrint; just verify no-throw

**Group 4: DevToolsSink**
- Test: `DevToolsSink().log(LogLevel.error, 'test')` returns normally
- Test: DevToolsSink is const-constructible

**Group 5: CompositeSink**
- Test: `CompositeSink([NullSink(), NullSink()]).log()` returns normally
- Test: CompositeSink with empty list returns normally

**Group 6: KernelLoggerImpl lifecycle**
- Test: `KernelLoggerImpl.I` throws `StateError` when `init()` has not been called. Use a setup that resets the static `_instance` to null (call `KernelLoggerImpl.init()` then check `.I` is non-null; for the "before init" test, you may need to test the StateError path carefully since previous tests may have already called init).
  - Approach: Test the "throws StateError" case first in the group, or use a test that directly constructs `KernelLoggerImpl(NullSink())` and checks `.I` separately.
  - Alternative: Since `init()` sets the static instance, test that after `init()`, `KernelLoggerImpl.I` is non-null and is a `KernelLogger`.
- Test: After `KernelLoggerImpl.init()`, calling `.I` twice returns the same instance (identity check)

**Group 7: KernelLoggerImpl method delegation**
- Test: Create a `KernelLoggerImpl(NullSink())` directly (not via static init), call all 6 methods + 6 shortcuts, verify no exceptions
- Test: `error('msg', error: Exception(), stackTrace: StackTrace.current)` returns normally
- Test: `fatal('msg', error: Exception(), stackTrace: StackTrace.current)` returns normally

**Group 8: LogSink interface**
- Test: Verify `LogSink` is an abstract interface class (compile-time check: a class implementing it must provide `log()`)

**Group 9: Path redaction (D17)**
- Test: Verify that log messages containing full file paths are redacted — create a `KernelLoggerImpl(NullSink())` to test shortcut delegation, but the actual redaction test should verify the `_redactPath` helper behavior indirectly. Since `_redactPath` is file-private, test it through the sinks:
  - Create a test that constructs a `DebugPrintSink` and calls `log(LogLevel.warn, 'lib/kernel/engine/fvp_engine.dart:259 error')`. Override `debugPrint` in the test to capture output, and assert the captured string contains `fvp_engine.dart:259` but does NOT contain `lib/kernel/engine/`.
  - Alternatively, if `debugPrint` override is too fragile, add a visible `redactPath(String msg)` top-level function (not underscore-prefixed) alongside the sink classes in `kernel_logger.dart`, and test it directly: `expect(redactPath('lib/kernel/engine/fvp_engine.dart:259'), equals('fvp_engine.dart:259'))`.
  - Key assertions: `'lib/kernel/engine/fvp_engine.dart:259'` → `'fvp_engine.dart:259'`; path with backslashes also works; messages without paths pass through unchanged.

**Import note:** Import `package:flutter/foundation.dart` for `kDebugMode` if needed in tests. Import `kernel_logger.dart` which now exports all types.

Keep tests concise — each test 3-8 lines. Use `const` constructors where possible. Do NOT test debugPrint output (requires overriding the debugPrint callback, which is fragile).
  </action>
  <behavior>
    - LogLevel.values.length == 6
    - NullSink.log() is no-op for all levels
    - CompositeSink delegates to all contained sinks
    - KernelLoggerImpl.I throws StateError before init()
    - KernelLoggerImpl.I returns valid KernelLogger after init()
    - KernelLoggerImpl all 6 methods + 6 shortcuts work without exceptions
    - error()/fatal() with error+stackTrace params work
    - Path redaction strips directory prefixes (D17): 'lib/kernel/engine/fvp_engine.dart:259' → 'fvp_engine.dart:259'
  </behavior>
  <verify>
    <automated>flutter test test/diagnostics/kernel_logger_test.dart</automated>
  </verify>
  <done>
    - kernel_logger_test.dart has 8 test groups covering all sink types + KernelLoggerImpl
    - All tests pass
    - Existing NullKernelLogger signature tests preserved
    - Path redaction test (Group 9) verifies D17: full paths stripped to filename only
    - Test file is < 220 lines (concise)
  </done>
</task>

<task type="auto">
  <name>Task 2: Full verification — CI gate + flutter analyze + full test suite</name>
  <files>tool/audit/kernel_logger_gate.sh</files>
  <read_first>
    - tool/audit/kernel_logger_gate.sh (created in Plan 02)
    - lib/kernel/diagnostics/kernel_logger.dart (verify no package:logger import)
  </read_first>
  <action>
Run the full Phase 17 verification suite:

1. Run CI grep gate: `bash tool/audit/kernel_logger_gate.sh` — must PASS
2. Run flutter analyze: `flutter analyze` — must have zero errors
3. Run full test suite: `flutter test` — must be all green
4. Verify non-kernel files still import log.dart:
   - `grep -r 'import.*utils/log\.dart' lib/app.dart lib/main.dart` — should show 2 hits (app.dart + main.dart)
   - `grep -r 'import.*kernel/utils/log\.dart' lib/features/` — should show 2 hits (player_feature.dart + deferred_player_feature.dart)
5. Verify kernel files do NOT import log.dart:
   - `grep -r 'import.*log\.dart' lib/kernel/ --include='*.dart'` — should show ONLY lib/kernel/utils/log.dart itself (it imports dart:io, flutter/foundation, package:logger, package:path_provider — none of which are 'log.dart')

If any check fails, fix the issue and re-verify.
  </action>
  <verify>
    <automated>bash tool/audit/kernel_logger_gate.sh && flutter analyze && flutter test</automated>
  </verify>
  <done>
    - CI grep gate passes (lib/kernel/** zero package:logger + zero utils/log.dart imports)
    - flutter analyze passes with zero errors
    - flutter test full suite passes
    - Non-kernel files still import log.dart (4 files: app.dart, main.dart, player_feature.dart, deferred_player_feature.dart)
    - Kernel files do not import log.dart (only log.dart itself remains in utils/)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel → debugPrint | Log messages cross from kernel internal state to console output |
| kernel → DevTools | Log messages cross from kernel to dart:developer DevTools integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Information Disclosure | DebugPrintSink | medium | mitigate | kDebugMode compile-time gate (Plan 01); verified by test suite + CI gate |
| T-17-05 | Information Disclosure | Test coverage gaps | low | accept | Tests cover all sink types + lifecycle; debugPrint output not asserted (requires callback override, fragile) |
| T-17-SC | Tampering | npm/pip/cargo installs | none | N/A | Zero new packages installed in this phase |
</threat_model>

<verification>
- `flutter test test/diagnostics/kernel_logger_test.dart` — all tests pass
- `bash tool/audit/kernel_logger_gate.sh` — PASS
- `flutter analyze` — zero errors
- `flutter test` — full suite green
</verification>

<success_criteria>
- kernel_logger_test.dart has comprehensive behavioral tests for all 7 types
- CI grep gate confirms structural property (LOG-01)
- Full test suite green (no regressions from migration)
- Phase 17 fully verified and ready for Phase 18
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Description |
|--------|------|-------------|
| (tests) | `test/diagnostics/kernel_logger_test.dart` | Extended with ~15 new tests covering all sink types + KernelLoggerImpl lifecycle |

<output>
Create `.planning/phases/17-kernellogger/17-03-SUMMARY.md` when done
</output>
