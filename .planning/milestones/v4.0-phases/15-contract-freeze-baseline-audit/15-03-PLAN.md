---
phase: 15-contract-freeze-baseline-audit
plan: 03
type: execute
wave: 2
depends_on: ["15-02"]
files_modified:
  - test/contracts/engine_state_view_contract.dart
  - test/contracts/playback_control_contract.dart
  - test/contracts/track_control_contract.dart
  - test/contracts/subtitle_config_contract.dart
  - test/contracts/video_effect_control_contract.dart
  - test/contracts/renderer_control_contract.dart
  - test/contracts/volume_control_contract.dart
  - test/contracts/contract_test_runner.dart
  - test/engine/fvp_engine_contract_test.dart
  - test/fixtures/README.md
  - test/fixtures/corrupted_header.mp4
  - test/fixtures/empty_file.mp4
  - test/fixtures/not_a_video.txt
  - test/fixtures/unsupported_codec.avi
  - test/fixtures/tiny_valid.mp4
autonomous: true
requirements: [BASE-04]

must_haves:
  truths:
    - "test/contracts/ contains exactly 7 *_contract.dart files, one per interface in media_engine.dart's implements clause (EngineStateView, PlaybackControl, TrackControl, SubtitleConfig, VideoEffectControl, RendererControl, VolumeControl — including VolumeControl which D14's text omitted, added as the 7th group per RESEARCH Pitfall 4/OpenQ2)"
    - "Each contract file exposes a top-level parameterized function taking a `MediaEngine Function() createEngine` factory (D13) — NO contract file instantiates FvpEngine() or FakeEngine() directly, so Phase 21 can mount the same functions against NewFvpEngine (VERIFY-01 reuse)"
    - "test/engine/fvp_engine_contract_test.dart mounts all 7 contract functions against real FvpEngine (D13 — real implementation is the gate subject, NOT FakeEngine) and flutter test test/contracts/ (via the mount point) exits 0 against the frozen contracts (sc4 '通过旧引擎')"
    - "A structural regression gate ACTUALLY EXECUTES in the default CI path (not merely present as skippable source): the happy-path test opens the REAL playable fixture test/fixtures/tiny_valid.mp4, asserts open()-success→state==idle, then play()→state==MediaState.playing. This test carries NO default-skip tag (no @Tags(['requires-media']) on it) so `flutter test test/engine/fvp_engine_contract_test.dart --plain-name \"open to play handoff\"` RUNS the assertion against a genuine open() success and exits 0 — the current 'loads video but won't play' regression cannot recur silently after the contract freeze (see debug stubs T-15-04/T-15-07 — Phase 15 gates it, does NOT fix it)"
    - "throws: tags are asserted as BEHAVIORAL assertions (engine.lastError.value is <ErrorType> AND engine.state.value == MediaState.error), NOT expect(fn, throwsA(...)) — because FvpEngine error paths set lastError+state=error and never throw to the caller (PATTERNS behavioral-assertion pattern, D19)"
    - "Contract tests cover D15 first-version priority: states: entry/exit assertions + throws: error scenarios via real bad-file fixtures (D17); ensures/modifies postconditions are second-round (documented as deferred within the plan, not silently skipped)"
    - "Phase 15 contract tests ONLY assert static behavior contracts (D20) — no timing/race tests (open→seek→open, mdk callback marshaling); those are Phase 20 STATE-05/07. Lifecycle new semantics (LifecyclePhase) are the P20 known-gap list, NOT tested as baseline here (D16)"
    - "Every contract test disposes its engine (tearDown or per-test engine.dispose()) to avoid ValueNotifier leak warnings across 7 groups (PATTERNS shared pattern)"
  artifacts:
    - test/contracts/engine_state_view_contract.dart
    - test/contracts/playback_control_contract.dart
    - test/contracts/track_control_contract.dart
    - test/contracts/subtitle_config_contract.dart
    - test/contracts/video_effect_control_contract.dart
    - test/contracts/renderer_control_contract.dart
    - test/contracts/volume_control_contract.dart
    - test/contracts/contract_test_runner.dart
    - test/engine/fvp_engine_contract_test.dart
    - test/fixtures/README.md
    - test/fixtures/corrupted_header.mp4
    - test/fixtures/empty_file.mp4
    - test/fixtures/not_a_video.txt
    - test/fixtures/unsupported_codec.avi
    - test/fixtures/tiny_valid.mp4
  key_links:
    - "Each contract test's states:/throws: assertions mirror the frozen tags authored in Plan 02 (D7/D19 three-view cross-check: contract tag → transition table → test — divergence = caught bug)"
    - "The parameterized createEngine factory is the seam that lets Phase 21 mount identical tests against NewFvpEngine (VERIFY-01) — the migration gate for every DelegationPolicy capability flip (STATE-06)"
    - "test/fixtures/ real bad files drive FvpEngine's genuine error paths (D17) instead of scripted FakeEngine injection (weak gate)"
    - "test/fixtures/tiny_valid.mp4 is a REAL minimal playable clip that drives the happy-path open()→idle→play()→playing regression gate against a genuine open() success (RESEARCH §'真实坏文件 fixture 管理' extended with one good fixture) — this is the seam that makes T-15-07 an executing gate, not a skippable stub"
  prohibitions:
    - "Contract tests must NOT be run against FakeEngine as the gate subject (D13) — FakeEngine stays for widget tests; the gate mounts real FvpEngine"
    - "Contract tests must NOT assert lifecycle-new-semantics (LifecyclePhase disposed-safe-defaults, recover(), disposing) as passing baseline against old FvpEngine (D16) — those would fail the old engine and break sc4; they are the P20 known-gap list"

flagged_assumptions:
  - "R4/BASE-04 edge was probe-unclassified (specless fallback): acceptance authored from BASE-04 text + D13-D20. Machine-checkable: 7 files exist, each has a group( block per interface method, flutter test test/contracts/ exits 0 against real FvpEngine."
---

<objective>
针对接口（非实现）编写契约测试，作为后续每次 capability 翻转的迁移闸门（BASE-04）。测试参数化 accepts `MediaEngine`，对真实 `FvpEngine` 跑（D13），按 7 个 ISP 接口分组（D14 + 补 VolumeControl），首版覆盖 states:+throws:（D15），错误注入用真实坏文件 fixture（D17），只测静态行为契约（D20）。

Purpose: 使 Phase 20 能按 DelegationPolicy 逐能力翻转并每次跑同一套契约测试；Phase 21 只换 factory 即对 NewFvpEngine 复用（VERIFY-01）。同时把 open()-成功→play() 交接做成结构性回归闸门，使当前"加载但不播放"回归冻结后不能静默复发。
Output: test/contracts/ 下 7 个契约测试组 + runner；test/engine/ 下真实 FvpEngine 挂载点；test/fixtures/ 坏文件集。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md
@.planning/phases/15-contract-freeze-baseline-audit/15-RESEARCH.md
@.planning/phases/15-contract-freeze-baseline-audit/15-PATTERNS.md
@.planning/phases/15-contract-freeze-baseline-audit/15-02-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan CREATES these NEW files/symbols (exclude from drift verification):
- `test/contracts/` directory (did not exist) with 7 `*_contract.dart` files + `contract_test_runner.dart`
- 7 parameterized contract functions: `runEngineStateViewContractTests`, `runPlaybackControlContractTests`, `runTrackControlContractTests`, `runSubtitleConfigContractTests`, `runVideoEffectControlContractTests`, `runRendererControlContractTests`, `runVolumeControlContractTests`
- `test/engine/fvp_engine_contract_test.dart` — mount point instantiating real FvpEngine
- `test/fixtures/` directory (did not exist) with README + 4 bad-file fixtures + 1 real playable fixture (`tiny_valid.mp4`) for the happy-path regression gate
</artifacts_this_phase_produces>

<do_not_touch>
In-flight debug work owns these — DO NOT modify:
- `lib/kernel/engine/media_opener.dart`, `lib/main.dart` (uncommitted debug work)
- `.planning/debug/*.md` (read-only diagnostic stubs — reference their hypotheses in rationale only)
- `test/helpers/fake_engine.dart` — READ for shape only; D13 forbids using it as the contract-test gate subject. Do NOT extend it into a contract-driven fake.
Phase 15 GATES the regression via contract tests; it does NOT fix the regression (Phase 20+).
</do_not_touch>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 基础设施 — test/fixtures/ 坏文件集 + 一个真实可播放 fixture + test/contracts/ runner 骨架（D17/D14）</name>
  <files>test/fixtures/README.md, test/fixtures/corrupted_header.mp4, test/fixtures/empty_file.mp4, test/fixtures/not_a_video.txt, test/fixtures/unsupported_codec.avi, test/fixtures/tiny_valid.mp4, test/contracts/contract_test_runner.dart</files>
  <read_first>
    - `.planning/phases/15-contract-freeze-baseline-audit/15-PATTERNS.md` §"test/fixtures/* (D17 real bad-file fixtures) — new, no analog" + §"test/contracts/*_contract.dart" (7 files, 与 implements 子句精确匹配) + "No Analog Found" 表
    - `.planning/phases/15-contract-freeze-baseline-audit/15-RESEARCH.md` §"真实坏文件 fixture 管理（D17）" (fixture 集结构 + 用法)
    - `lib/kernel/engine/media_engine.dart` lines 24-31 — implements 7 接口的精确名单（含 VolumeControl）
    - `lib/kernel/models/player_error.dart` — fixture 触发的错误类型对应
    - `.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md` D13/D14/D17
  </read_first>
  <action>
    创建 `test/fixtures/` 目录及 4 个坏文件（fixture 生成方式属执行裁量）：`corrupted_header.mp4`（有效扩展名、文件头被截断/破坏的少量字节）、`empty_file.mp4`（0 字节）、`not_a_video.txt`（纯文本内容伪装非视频）、`unsupported_codec.avi`（FvpEngine/MDK 明确不支持编码；若难以生成真实不支持流，可用一个明显非法的 AVI 容器字节序列）。
    **另创建 1 个真实可播放 fixture `test/fixtures/tiny_valid.mp4`**（BLOCKER 修复 — happy-path 回归闸门的 open() 成功输入，见 RESEARCH §"真实坏文件 fixture 管理（D17）"，此处在坏文件集之外扩展一个 good fixture）：一个极小（几 KB）、H.264/AAC 或 MDK 默认支持编码的合法 mp4 片段（如 1 帧纯色/黑场、极短时长）。执行者获取方式（裁量选一，README 须记录所选命令）：
      (a) 用系统 ffmpeg 生成：`ffmpeg -f lavfi -i color=c=black:s=64x64:d=1 -c:v libx264 -pix_fmt yuv420p -movflags +faststart test/fixtures/tiny_valid.mp4`（若开发机/CI 有 ffmpeg）；
      (b) 或提交一个已知的小型公开样例合法 mp4（记录来源 + 校验和于 README）。
    作为二进制 fixture 提交进 git（几 KB，可接受）。**目的：让 happy-path open()→idle→play()→playing 断言默认跑真实 open() 成功路径，无需任何 media-gating 的条件跳过。** 另写 `test/fixtures/README.md` 说明每个 fixture 用途 + 如何再生成/复现（含 tiny_valid.mp4 的确切生成命令）。
    创建 `test/contracts/contract_test_runner.dart`：`library;` + 汇总 `export` 7 个 contract 文件的 run* 函数（供挂载点单一 import）。此文件本身不含 test()，只做聚合导出。
    此任务是 Wave 0 建设——只建目录与坏文件与聚合骨架，7 个 contract 文件的实测在 Task 2/3。fixture 是二进制/文本文件，用 Write 创建（文本）或执行者用 shell 生成截断字节（记录命令于 README）。
    conventional-commit `test:`。
  </action>
  <verify>
    <automated>flutter analyze test/contracts/contract_test_runner.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/fixtures/` contains exactly 5 fixture files: corrupted_header.mp4, empty_file.mp4, not_a_video.txt, unsupported_codec.avi, tiny_valid.mp4 (`ls test/fixtures/` shows all 5 + README.md)
    - `test/fixtures/empty_file.mp4` is 0 bytes (`wc -c < test/fixtures/empty_file.mp4` == 0)
    - `test/fixtures/tiny_valid.mp4` is a NON-EMPTY real playable clip (`test -s test/fixtures/tiny_valid.mp4` — >0 bytes; it is NOT one of the bad fixtures)
    - `test/fixtures/README.md` documents each fixture's purpose and regeneration method (including tiny_valid.mp4's exact generation command / source + checksum)
    - `test/contracts/contract_test_runner.dart` exports all 7 run* functions (`grep -c "export" test/contracts/contract_test_runner.dart` >= 7) and contains no `test(` call itself
    - `flutter analyze test/contracts/contract_test_runner.dart` reports no error (exports resolve once Task 2/3 create the files — this task may create empty stub files first so exports resolve)
  </acceptance_criteria>
  <done>test/fixtures/ has the 4 real bad-file fixtures (D17) PLUS 1 real playable fixture (tiny_valid.mp4) for the happy-path regression gate, all documented in the README; test/contracts/ has the aggregating runner exporting all 7 contract functions. Wave 0 infrastructure is in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EngineStateView + PlaybackControl 契约测试组（states:+throws: 首版 + open→idle→play 回归闸门, D13/D15/D19）</name>
  <files>test/contracts/engine_state_view_contract.dart, test/contracts/playback_control_contract.dart</files>
  <read_first>
    - `test/kernel/engine/engine_state_machine_test.dart` (全文) — group/setUp/tearDown/AAA 嵌套结构 + legal/illegal transition 断言模式，是要复刻并 retarget 的 analog
    - `test/kernel/engine/fvp_engine_open_test.dart` (全文) — 现有真实/Fake engine open() 测试模式；注意 line 30 断言 open 成功后 state==idle
    - `test/helpers/fake_engine.dart` (lines 1-70) — 读 ISP 组合 engine 的 shape（勿作为 gate subject, D13）
    - `lib/kernel/engine/engine_state_view.dart` + `lib/kernel/engine/playback_control.dart` (含 Plan 02 写入的 D2 契约标签 — 测试镜像这些 states:/throws:)
    - `lib/kernel/engine/fvp_engine.dart` open() 236-311 / play() 313-334 / dispose 574-618 — 真实行为（happy-path 闸门须对照 open() 成功后 state 落点 + play() 后 state==playing 的真实实现）
    - `test/fixtures/tiny_valid.mp4` + `test/fixtures/README.md`（Task 1 产出的真实可播放 fixture — happy-path 闸门的 open() 成功输入）
    - `lib/kernel/engine/media_state.dart` + `lib/kernel/models/player_error.dart`
    - `.planning/phases/15-contract-freeze-baseline-audit/15-RESEARCH.md` §"参数化测试签名（D13）" + §"Behavioral assertions over thrown-exception" + §"真实坏文件 fixture 管理（D17）"（good fixture 用法在此扩展）
    - `.planning/phases/15-contract-freeze-baseline-audit/15-PATTERNS.md` §"test/contracts/*_contract.dart" + Shared Patterns
    - `.planning/debug/*.md` — open→play 回归假设（rationale 引用，不改）
  </read_first>
  <behavior>
    - EngineStateView contract: state getter never throws before open(); position/duration default 0; all 14 getters callable without open() → returnsNormally
    - PlaybackControl happy-path REGRESSION GATE (runs by DEFAULT, no skip tag): open('test/fixtures/tiny_valid.mp4') on the REAL playable fixture → open() succeeds, state==MediaState.idle (open-success posture); then play() → state==MediaState.playing. This asserts the exact 'loads but won't play' handoff against a genuine open() success and MUST execute in CI, not be tagged out.
    - PlaybackControl throws: open() with test/fixtures/corrupted_header.mp4 → engine.lastError.value is a PlayerError subtype AND state==error (behavioral, not throwsA); open('') empty path → lastError is FileError + state==error
    - states: entry/exit assertions mirror the frozen states: tags from Plan 02, cross-checked against _canTransitionTo
  </behavior>
  <action>
    在 `test/contracts/engine_state_view_contract.dart` 写 `void runEngineStateViewContractTests(MediaEngine Function() createEngine)`（顶层参数化函数，D13）：`group('EngineStateView contract')` + `late MediaEngine engine; setUp(() => engine = createEngine()); tearDown(() => engine.dispose());`。断言：14 个 getter 在 open() 前调用 returnsNormally；state 初始/position/duration 默认值。**勿**直接 new FvpEngine/FakeEngine（只经 createEngine 工厂）。
    在 `test/contracts/playback_control_contract.dart` 写 `void runPlaybackControlContractTests(MediaEngine Function() createEngine)`：嵌套 `group(方法名) > test(具体行为)`。首版覆盖（D15）：
      - **happy-path 结构性回归闸门（BLOCKER 修复 — 必须默认执行，绝不加任何跳过标签）：** 写一个 `test('open to play handoff …', () async {…})`（test 描述含短语 "open to play handoff" 供 `--plain-name` 定位），断言：`await engine.open('test/fixtures/tiny_valid.mp4')` → open 成功、`expect(engine.state.value, MediaState.idle)`（open-success 落点）；随后 `engine.play()` → `expect(engine.state.value, MediaState.playing)`。此 test **不得**加任何 media-gating 的跳过标签、不得包裹 `if (mediaUnavailable) return;` 之类的静默跳过（不要在测试文件里写出任何跳过标签字符串——闸门验收会负向 grep 该标签串，其在源码中出现即判失败）——它对真实 `tiny_valid.mp4` 的 open() 成功路径断言，是本阶段唯一一个针对 "加载但不播放" 回归（T-15-04/T-15-07）的执行型闸门，必须在默认 `flutter test` 路径下真正跑到 `MediaState.playing` 断言。（Task 1 已提供真实可播放 fixture，使该断言不再需要真实媒体的条件跳过。）
      - throws: open('test/fixtures/corrupted_header.mp4') → `expect(engine.lastError.value, isA<PlayerError>())` + `expect(engine.state.value, MediaState.error)`（行为断言，非 throwsA — PATTERNS/D19）；open('') → lastError isA<FileError> + state==error。
    注：若目标 CI 环境确实无法解码（headless 无编解码器），处理方式**不是**给该 test 打跳过标签，而是让闸门命令用 `flutter test --run-skipped`（或去掉跳过标签）强制执行，并在该情形下让 test **失败**（暴露 "环境不能解码" 而非静默通过）——绝不允许 "源码里有断言但被跳过" 满足回归闸门这一 truth。Preferred 路径（本 plan 采用）是提交真实 fixture 使其默认可跑，无需 `--run-skipped`。
    所有断言镜像 Plan 02 冻结的 states:/throws: 标签，逐条与 `_canTransitionTo` 交叉校验（D7/D19）。每个 test 或 tearDown 调 engine.dispose()（避免 ValueNotifier 泄漏警告）。
    遵守 CLAUDE.md：debugPrint 非 print、避免 !/late/as（用 late final 仅限 analog 已用模式）、Fakes 非 mocks、unique controller names 不适用（无 controller）。conventional-commit `test:`。
  </action>
  <verify>
    <automated>flutter test test/engine/fvp_engine_contract_test.dart</automated>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: MediaState.playing -->
    - `test/contracts/engine_state_view_contract.dart` defines top-level `runEngineStateViewContractTests(MediaEngine Function() createEngine)` and contains no `FvpEngine(` / `FakeEngine(` literal (`grep -cE "FvpEngine\(|FakeEngine\(" test/contracts/engine_state_view_contract.dart` == 0)
    - `test/contracts/playback_control_contract.dart` defines `runPlaybackControlContractTests(MediaEngine Function() createEngine)` and asserts the open→error behavioral contract via `lastError.value` + `state.value == MediaState.error` (grep for `lastError` and `MediaState.error` both present); it does NOT use `throwsA` for FvpEngine error paths
    - The open()-success→play()→state==playing handoff assertion is PRESENT AND EXECUTES (not merely text in source): `grep -c "MediaState.playing" test/contracts/playback_control_contract.dart` >= 1 AND the test carries no default-skip tag (`grep -c "requires-media" test/contracts/playback_control_contract.dart` == 0, i.e. the happy-path test is not tagged out)
    - The regression gate RUNS and PASSES against the real fixture: `flutter test test/engine/fvp_engine_contract_test.dart --plain-name "open to play handoff"` reports at least 1 test run (not "0 tests" / not "All tests skipped") and exits 0 — proving the `MediaState.playing` assertion executed against a genuine open('test/fixtures/tiny_valid.mp4') success. (If the environment cannot decode, this command FAILS — it must never silently pass via a skip.)
    - `flutter test test/engine/fvp_engine_contract_test.dart` exits 0 (once Task 3 wires the mount point; if run standalone before Task 3, this task's files at least `flutter analyze` clean)
    - Every `states:` assertion set is a subset of `_canTransitionTo`'s allowed set for that source state (documented cross-check in SUMMARY)
  </acceptance_criteria>
  <done>EngineStateView + PlaybackControl contract functions are parameterized over a MediaEngine factory (D13), assert states:/throws: behaviorally (D19), use real bad-file fixtures for error paths (D17), and the open→idle→play regression gate EXECUTES by default against the real tiny_valid.mp4 fixture (no skip tag; `--plain-name "open to play handoff"` runs and passes) — without instantiating a concrete engine directly.</done>
</task>

<task type="auto">
  <name>Task 3: 剩余 5 接口契约组 + 真实 FvpEngine 挂载点（D14 含 VolumeControl 第 7 组, D13）</name>
  <files>test/contracts/track_control_contract.dart, test/contracts/subtitle_config_contract.dart, test/contracts/video_effect_control_contract.dart, test/contracts/renderer_control_contract.dart, test/contracts/volume_control_contract.dart, test/engine/fvp_engine_contract_test.dart</files>
  <read_first>
    - `lib/kernel/engine/{track_control,subtitle_config,video_effect_control,renderer_control,volume_control}.dart` (含 Plan 02 写入的契约标签)
    - `lib/kernel/engine/fvp_engine.dart` — 对应实现（getAudioTracks/switchAudioTrack/setVideoEffect/setVolume/setMute/setD3d11SyncEnabled 等的真实行为）
    - `test/kernel/engine/fvp_engine_open_test.dart` — 真实 engine 挂载/dispose 现有模式
    - `test/engine/mixin_capability_test.dart` — test/engine/ 现有测试组织（挂载点落此目录）
    - `.planning/phases/15-contract-freeze-baseline-audit/15-RESEARCH.md` §"按 ISP 接口分组的目录结构（D14）" + OpenQ2（VolumeControl 补第 7 组）+ §"参数化测试签名" 挂载点示例
    - `.planning/phases/15-contract-freeze-baseline-audit/15-PATTERNS.md` §"test/engine/fvp_engine_contract_test.dart — mount point" + "Group contract → test group correspondence (D14): 7 interfaces not 6"
    - `.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md` D13/D14/D20
  </read_first>
  <action>
    为剩余 5 接口各写一个 `test/contracts/<iface>_contract.dart`，每个含一个顶层 `run<Iface>ContractTests(MediaEngine Function() createEngine)`：
      - `track_control_contract.dart` → getAudioTracks 返回 List、switchAudioTrack 不抛（disposed 后 no-op / 无媒体时安全）、activeAudioTracks getter。
      - `subtitle_config_contract.dart` → getSubtitleTracks/switchSubtitleTrack/toggleSubtitle/setExternalSubtitle/setSubtitleDelay/setEqualizer/subtitleDelay getter/activeSubtitleTracks getter。
      - `video_effect_control_contract.dart` → setVideoEffect/rotate/setAspectRatio/setDeinterlace（modifies: 断言 aspectRatio 等，或 returnsNormally）。
      - `renderer_control_contract.dart` → setD3d11SyncEnabled/setHardwareDecoding returnsNormally。
      - `volume_control_contract.dart` → **第 7 组（D14 遗漏，RESEARCH OpenQ2 补）**：setVolume(0.5)→volume.value≈0.5、setMute(true)→isMuted.value==true、volume/isMuted getter。
    每组按 D20 只测静态行为契约（无时序/竞态）；每 test 或 tearDown 调 engine.dispose()。均经 createEngine 工厂，不直接 new 具体类。
    写 `test/engine/fvp_engine_contract_test.dart` 挂载点：`void main() { TestWidgetsFlutterBinding.ensureInitialized();` 然后 7 行 `run*ContractTests(() => FvpEngine());`（D13 真实 FvpEngine 是 gate subject）。import 经 `test/contracts/contract_test_runner.dart` 聚合导出（Task 1）。
    conventional-commit `test:`。
  </action>
  <verify>
    <automated>flutter test test/engine/fvp_engine_contract_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/contracts/` contains exactly 7 `*_contract.dart` files (`ls test/contracts/*_contract.dart | wc -l` == 7) plus contract_test_runner.dart
    - `volume_control_contract.dart` exists and defines `runVolumeControlContractTests` (the 7th group D14 omitted, RESEARCH OpenQ2)
    - `test/engine/fvp_engine_contract_test.dart` mounts all 7 run* functions against `FvpEngine()` (`grep -c "() => FvpEngine()" test/engine/fvp_engine_contract_test.dart` == 7)
    - No `*_contract.dart` file instantiates a concrete engine directly (`grep -rcE "FvpEngine\(|FakeEngine\(" test/contracts/` returns 0 matches across all contract files)
    - `flutter test test/engine/fvp_engine_contract_test.dart` exits 0 against real FvpEngine (sc4 — contract tests pass the old engine)
    - No timing/race assertions present (no `fakeAsync`, no rapid open→seek→open sequence) — D20 boundary honored
  </acceptance_criteria>
  <done>All 7 ISP contract groups exist (including the previously-omitted VolumeControl 7th group); the mount point runs them against real FvpEngine and passes (sc4); Phase 21 can mount the identical functions against NewFvpEngine by changing only the factory (VERIFY-01 seam).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract test gate ↔ real FvpEngine | The contract tests are the migration gate. If they mount a Fake instead of the real engine, they test a mock and the gate is meaningless (D13). If they assert lifecycle-new-semantics against the old engine, they fail baseline and break sc4. |

## STRIDE Threat Register (ASVS L1, block_on: standard)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Tampering | contract test gate subject | high | mitigate | If contract tests run against FakeEngine, they validate the test's own fake behavior (weak gate) and contract drift passes silently. Mitigated by D13 + the acceptance criterion that no contract file instantiates a concrete engine and the mount point uses real FvpEngine, plus D17 real bad-file fixtures for genuine error paths. |
| T-15-07 | Tampering | open→play regression gate | high | mitigate | The live "loads but won't play" regression must not recur silently after freeze. Mitigated by the open()-success→play()→state==playing assertion that (a) cannot be deleted and (b) EXECUTES by default against a REAL playable fixture (test/fixtures/tiny_valid.mp4) with NO skip tag — the acceptance criterion requires the `--plain-name "open to play handoff"` run to report ≥1 test run and exit 0 (a skipped/tagged-out test can no longer satisfy the gate). This closes the earlier gap where "assertion text present but skippable" could mark the gate green while leaving the regression unguarded. |
| T-15-08 | Repudiation | baseline over-testing | medium | accept | If contract tests assert P20 lifecycle semantics against the old engine, they turn permanently red and the gate is abandoned. Mitigated by the D16/D20 boundary (baseline-capture only) + prohibition; residual risk accepted as it is a plan-authoring discipline, not a runtime threat. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs (flutter_test is an existing dev_dependency). No legitimacy gate needed. |

Threat model is proportional: the dominant threat is a weak or over-broad gate (T-15-06/07/08), mitigated by D13 real-engine mounting + D16/D20 baseline boundary + the un-deletable regression assertion.
</threat_model>

<verification>
- `flutter test test/engine/fvp_engine_contract_test.dart` exits 0 against real FvpEngine (sc4)
- `test/contracts/` has exactly 7 `*_contract.dart` files (all 7 ISP interfaces, including VolumeControl)
- No contract file instantiates a concrete engine (parameterized factory only, D13)
- The open→idle→play regression gate assertion is present, cannot be deleted, AND executes by default against test/fixtures/tiny_valid.mp4 (no skip tag; `flutter test … --plain-name "open to play handoff"` runs ≥1 test and exits 0)
- No timing/race tests (D20 boundary); lifecycle-new-semantics NOT asserted as baseline (D16)
- `flutter test test/contracts/` and the mount point are green; ValueNotifier disposal in every group (no leak warnings)
</verification>

<success_criteria>
BASE-04 satisfied: interface-level (not implementation) contract tests exist and pass against the old FvpEngine (sc4), organized by the 7 ISP interfaces (D14 + VolumeControl), parameterized over a MediaEngine factory for Phase 21 reuse (D13/VERIFY-01), first-version covering states:+throws: via real bad-file fixtures (D15/D17), static-behavior-only (D20), with an un-deletable open→idle→play regression gate that EXECUTES by default against a real playable fixture (tiny_valid.mp4) — not merely present as skippable source.
</success_criteria>

<output>
Create `.planning/phases/15-contract-freeze-baseline-audit/15-03-SUMMARY.md` when done
</output>