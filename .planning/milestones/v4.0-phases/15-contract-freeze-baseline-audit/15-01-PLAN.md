---
phase: 15-contract-freeze-baseline-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tool/audit/inventory.sh
  - tool/audit/contract_completeness.sh
  - tool/audit/README.md
  - .planning/phases/15-contract-freeze-baseline-audit/15-BASELINE-AUDIT.json
  - .planning/phases/15-contract-freeze-baseline-audit/15-BASELINE-AUDIT.md
  - .planning/PROJECT.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/INTEGRATIONS.md
autonomous: true
requirements: [BASE-02]

must_haves:
  truths:
    - "tool/audit/inventory.sh exits 0 and writes 15-BASELINE-AUDIT.json whose package_logger_usage.total_call_sites and total_files come from the script's live rg run (NOT hardcoded 121/30 — the historical number is stale, see RESEARCH Pitfall 1)"
    - "The script scans all of lib/ with a recursive/glob rg pattern for openGeneration (never hardcodes a file list) so both fvp_engine.dart AND playback_navigator.dart are captured (RESEARCH Pitfall 2)"
    - "The MemoryMonitor grep target excludes lib/kernel/utils/memory_monitor.dart's own doc-comment mentions (lines 61,63) so the count is exactly the real production call sites, not doc false positives (RESEARCH Pitfall / PATTERNS filter requirement)"
    - "Running tool/audit/inventory.sh twice on unchanged code produces BYTE-IDENTICAL JSON output — not merely equal counts — because every file-list / locations array is sorted (deterministic order) before serialization, so `diff <(script-stdout) <(script-stdout)` is empty (reproducibility — sc2 '可复现')"
    - "The counting logic and the print/exit logic are separate functions/sections so Phase 17 can add --enforce without rewriting the counting code (D23 evolution path)"
    - "15-BASELINE-AUDIT.md renders the counts as a table sourced from the JSON, with zero counts typed in prose (RESEARCH Pitfall 1)"
  artifacts:
    - tool/audit/inventory.sh
    - tool/audit/contract_completeness.sh
    - tool/audit/README.md
    - .planning/phases/15-contract-freeze-baseline-audit/15-BASELINE-AUDIT.json
    - .planning/phases/15-contract-freeze-baseline-audit/15-BASELINE-AUDIT.md
  key_links:
    - "inventory.sh reads LIVE lib/ code (single source of truth) — not any static historical document (D21)"
    - "contract_completeness.sh dynamically extracts member signatures from the interface files so BASE-01 acceptance (Plan 02) is script-checked, not hand-counted (RESEARCH Pitfall 3)"

flagged_assumptions:
  - "R2/BASE-02 edge was probe-unclassified (specless fallback): acceptance criteria authored manually from BASE-02 text + D21/D22/D23. Machine-checkable via the two-run reproducibility + count-from-script rules above."
---

<objective>
盘点 v3.0 迁移必需的静态调用点基线（BASE-02），以**可重跑脚本 + 提交快照**形态产出，并标注陈旧 codebase maps、修正 PROJECT.md 陈旧状态描述。

Purpose: 让下游阶段（尤其 Phase 17 logger 迁移、Phase 20 openGeneration 统一）拥有一个可复现、不随重构漂移的调用点真相源。历史数字（121/30）已过时（LIVE 实测 84/28），本阶段用脚本产出真实数字并使其可复现，杜绝第二真相源（D21/D22/D23）。
Output: tool/audit/ 下 2 个可重跑脚本 + README；phase 目录下 JSON + Markdown 双格式快照；7 份陈旧 maps 顶部水印；PROJECT.md 状态描述修正。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md
@.planning/phases/15-contract-freeze-baseline-audit/15-RESEARCH.md
@.planning/phases/15-contract-freeze-baseline-audit/15-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan CREATES these NEW symbols/files (exclude from drift/source-grounding verification):
- `tool/audit/inventory.sh` — new audit script (no analog; built from RESEARCH rg commands + D23 structure)
- `tool/audit/contract_completeness.sh` — new script that extracts interface member signatures from lib/kernel/engine/*.dart and checks for contract tags (supports Plan 02 acceptance)
- `tool/audit/README.md` — usage + --enforce evolution note (Phase 17 fills semantics)
- `.planning/phases/15-contract-freeze-baseline-audit/15-BASELINE-AUDIT.json` — machine-readable snapshot (new schema, no analog)
- `.planning/phases/15-contract-freeze-baseline-audit/15-BASELINE-AUDIT.md` — human-readable snapshot
- New "v2.1 前快照" watermark header lines on the 7 `.planning/codebase/*.md` files (D22)
</artifacts_this_phase_produces>

<do_not_touch>
In-flight debug work owns these — DO NOT modify in this plan:
- `lib/kernel/engine/media_opener.dart` (uncommitted, owned by playback-regression debug)
- `lib/main.dart` (uncommitted; note MemoryMonitor.start moved to line 22 — the script must read the LIVE line, never hardcode :16)
- `.planning/debug/*.md` (read-only diagnostic stubs)
</do_not_touch>

<tasks>

<task type="auto">
  <name>Task 1: 构建可重跑审计脚本 inventory.sh + README（BASE-02, D21/D23）</name>
  <files>tool/audit/inventory.sh, tool/audit/README.md, .planning/phases/15-contract-freeze-baseline-audit/15-BASELINE-AUDIT.json, .planning/phases/15-contract-freeze-baseline-audit/15-BASELINE-AUDIT.md</files>
  <read_first>
    - `.planning/phases/15-contract-freeze-baseline-audit/15-RESEARCH.md` §"复现本次审计所用的确切命令" + §"快照输出格式建议" + Pitfalls 1/2（数字勿硬编码、openGeneration 全扫）
    - `.planning/phases/15-contract-freeze-baseline-audit/15-PATTERNS.md` §"tool/audit/inventory.sh" — filter requirement（排除 memory_monitor.dart 自身 doc-comment）+ 结构要求（统计逻辑与 print/exit 分离）
    - `lib/kernel/utils/memory_monitor.dart` (lines 55-65) — 确认自引用 doc-comment 会被朴素正则误报，脚本须过滤
    - `.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md` D21/D23
  </read_first>
  <action>
    在 `tool/audit/inventory.sh` 写一个 POSIX sh + ripgrep 审计脚本。三个审计目标各自一个纯"计数函数"（bash function），再一个"格式化函数"输出 JSON、一个输出 Markdown，最后 main 调度——统计逻辑与 print/exit 分离（D23：Phase 17 加 `--enforce` 只需在现有计数函数上加阈值比较分支，不重写计数）。
    目标 1 `package:logger` 风格调用：用 `rg -o "\b(log|logEngine|logBridge|logServices|logUi)\.(t|d|i|w|e|f|v)\(" --type dart lib/ | wc -l` 得 total_call_sites；`rg -l ... | wc -l` 得 total_files；breakdown 按 5 个前缀分别计数。
    目标 2 `MemoryMonitor.start|snapshot` 生产调用点：`rg -n "MemoryMonitor\.(start|snapshot)\(" --type dart lib/`，但**必须用 `--glob '!lib/kernel/utils/memory_monitor.dart'`（或等价 `-g`）排除定义文件自身**，因其 doc-comment 含 2 处自引用会被误报（见 read_first）；输出 locations 为 `file:line` 列表（行号取脚本 LIVE 输出，绝不硬编码 :16）。
    目标 3 `openGeneration`：`rg -ln "openGeneration|_openGeneration" --type dart lib/` 递归全扫（绝不硬编码文件路径 — Pitfall 2），得 total_files + locations 文件列表（须同时出现 fvp_engine.dart 与 playback_navigator.dart）。
    **确定性排序（可复现要求）：** 所有进入 JSON 的 `locations` / 文件列表数组，在序列化前必须先 `| sort`（memory_monitor 的 `file:line` 列表、open_generation 的文件列表都要过 `sort`），使数组元素顺序在两次运行间稳定；同时把 `generated_at` 从"每次运行取实时钟"改为可复现输入（对比时把该字段视为可变——见验收；或让脚本接受 `--frozen-timestamp` / 读 git commit 时间，使快照本体除时间戳外字节一致）。目标：除 `generated_at` 外，两次运行的 JSON 字节完全相同（不只是 count 相同，而是数组顺序、缩进、键序全都稳定）。
    JSON schema 用 RESEARCH §"快照输出格式建议" 的形状（`generated_at`/`script_version`/`targets.{package_logger_usage,memory_monitor_calls,open_generation_references}`）；数字全部来自脚本运行，不预填。
    脚本须支持一个 `--stdout` flag：把要写入 JSON 快照的同一内容改为打印到 stdout（不写文件），供可复现性验收用 `diff <(...) <(...)` 对比两次运行；无该 flag 时行为不变（写入 15-BASELINE-AUDIT.json + .md）。
    在 `tool/audit/README.md` 写使用说明 + 占位一节说明 D23 `--enforce` 演进路径（语义留 Phase 17 填），用概念描述不写死阈值。
    脚本用 `debugPrint` 无关（是 shell）；遵循 conventional-commit `feat:` 提交。
  </action>
  <verify>
    <automated>bash tool/audit/inventory.sh && test -f .planning/phases/15-contract-freeze-baseline-audit/15-BASELINE-AUDIT.json</automated>
  </verify>
  <acceptance_criteria>
    - `bash tool/audit/inventory.sh` exits 0
    - The written `15-BASELINE-AUDIT.json` parses as valid JSON (`node -e "JSON.parse(require('fs').readFileSync(process.argv[1]))" .planning/phases/15-contract-freeze-baseline-audit/15-BASELINE-AUDIT.json` exits 0)
    - `open_generation_references.locations` JSON array contains BOTH a path matching `fvp_engine.dart` AND one matching `playback_navigator.dart`
    - `memory_monitor_calls.total_call_sites` equals the count of real production call sites (the two doc-comment lines in memory_monitor.dart are absent from `.locations`)
    - BYTE-IDENTICAL reproducibility (not just count-identical): two runs on unchanged code produce identical JSON except the `generated_at` timestamp. Verify with `diff <(bash tool/audit/inventory.sh --stdout | grep -v '"generated_at"') <(bash tool/audit/inventory.sh --stdout | grep -v '"generated_at"')` exits 0 (empty diff) — proving every `locations` / file-list array is sorted to a stable order, not merely equal in length. (Equivalent: sha256 of the two timestamp-stripped outputs match.)
    - Every `locations` array and file-list in the JSON is sorted (`grep -q 'sort' tool/audit/inventory.sh` — the script pipes rg location/file output through `sort` before serialization)
    - `grep -n 'MemoryMonitor.start' tool/audit/inventory.sh` does NOT contain a hardcoded `:16` or `:22` line number as an expected value
  </acceptance_criteria>
  <done>inventory.sh reproducibly produces the BASE-02 baseline JSON from LIVE code with BYTE-IDENTICAL output across runs (sorted locations/file-list arrays, timestamp-stripped diff empty), filtering doc-comment false positives, scanning all of lib/ for openGeneration, and separating count logic from output for Phase 17 --enforce evolution.</done>
</task>

<task type="auto">
  <name>Task 2: 契约完整性检查脚本 contract_completeness.sh（支撑 Plan 02 验收, RESEARCH Pitfall 3 / Rec 2/7）</name>
  <files>tool/audit/contract_completeness.sh</files>
  <read_first>
    - `lib/kernel/engine/engine_state_view.dart` — 14 个 getter（13 ValueNotifier + mediaInfo）+ dispose，脚本须动态提取签名而非依赖 CONTEXT 写死的"12 个"
    - `lib/kernel/engine/media_engine.dart` — implements 子句列 7 个接口（含 VolumeControl）
    - `lib/kernel/engine/{playback_control,track_control,subtitle_config,video_effect_control,renderer_control,volume_control}.dart` — 各接口成员
    - `.planning/phases/15-contract-freeze-baseline-audit/15-RESEARCH.md` Pitfall 3/5 + Recommendations 2/7；§"Validation Architecture" Wave 0 Gap 提到 contract_completeness.sh
    - `.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md` D2（标签集 requires/ensures/states/modifies/throws）、D3（组契约）
  </read_first>
  <action>
    在 `tool/audit/contract_completeness.sh` 写脚本：对 `lib/kernel/engine/` 下 7 个接口文件（`engine_state_view.dart` + `playback_control.dart` + `track_control.dart` + `subtitle_config.dart` + `video_effect_control.dart` + `renderer_control.dart` + `volume_control.dart`）用 rg **动态提取**每个公开成员签名（getter / 方法声明），再检查每个成员上方 `///` 块是否含至少一个 D2 契约标签行（`requires:`/`ensures:`/`states:`/`modifies:`/`throws:`），或（对 EngineStateView 只读 getter，D3）该接口顶部存在类级组契约块。
    脚本输出：每接口文件的成员总数 + 已有契约标签的成员数 + 缺失清单（member name + line）。以纯计数逻辑函数返回结果、格式化函数打印，与 inventory.sh 同构（复用文件遍历/成员提取辅助逻辑）。
    脚本设计为**读 LIVE code、可重跑**——Plan 02 撰写契约后跑此脚本作为"每个成员都有契约"的自动化验收（替代人工逐个数 14+N 个成员，避免 Pitfall 3 的计数偏差）。
    注意：`states:` 标签的值须与 `engine_state_machine.dart:66-92` 的 `_canTransitionTo` switch 交叉校验属 Plan 03 契约测试职责，本脚本只校验"标签存在"不校验"标签值正确"。
    此任务只建脚本；Plan 02 未写契约前，脚本对 6 个控制接口会报"缺失"——这是预期的（脚本先于契约存在，Wave 0 基础设施）。
  </action>
  <verify>
    <automated>bash tool/audit/contract_completeness.sh; test $? -le 1</automated>
  </verify>
  <acceptance_criteria>
    - `bash tool/audit/contract_completeness.sh` runs without a shell error (exit 0 or a documented non-zero "incomplete" status, ≤ 1)
    - The script's member count for `engine_state_view.dart` is extracted dynamically and equals 14 (13 ValueNotifier getters + mediaInfo) + dispose = 15 members, NOT the literal 12 from CONTEXT (proves dynamic extraction, RESEARCH Pitfall 3)
    - The script iterates over exactly the 7 interface files listed in `media_engine.dart`'s implements clause (grep confirms all 7 filenames appear in the script's file list, including `volume_control.dart`)
    - Output lists per-file missing-contract members (before Plan 02, the 6 control interfaces show as missing — expected)
  </acceptance_criteria>
  <done>contract_completeness.sh dynamically extracts and checks contract-tag presence across all 7 ISP interface files, giving Plan 02 a script-based acceptance gate instead of a hand-count.</done>
</task>

<task type="auto">
  <name>Task 3: 陈旧 maps 水印 + PROJECT.md 状态描述修正（D22 + Deferred 文档收尾）</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/CONCERNS.md, .planning/codebase/TESTING.md, .planning/codebase/STACK.md, .planning/codebase/STRUCTURE.md, .planning/codebase/CONVENTIONS.md, .planning/codebase/INTEGRATIONS.md, .planning/PROJECT.md</files>
  <read_first>
    - `.planning/codebase/ARCHITECTURE.md`, `CONCERNS.md`, `TESTING.md`, `STACK.md`, `STRUCTURE.md`, `CONVENTIONS.md`, `INTEGRATIONS.md` — 7 份 maps 顶部（确认现有首行，水印插在最顶）
    - `.planning/PROJECT.md` — 定位含"9 态"或"~40 边"的陈旧状态描述段
    - `.planning/phases/15-contract-freeze-baseline-audit/15-CONTEXT.md` D22 + Deferred（PROJECT.md 9态~40边修正为"6 态正交 + LifecyclePhase"是 Phase 15 派生任务）
    - `lib/kernel/engine/media_state.dart` — 权威 6 态定义（idle/opening/playing/paused/completed/error）
  </read_first>
  <action>
    在 `.planning/codebase/` 的 7 份 map 文件各自最顶部插入一行水印，内容为："> ⚠️ **v2.1 前快照（2026-07-12）** — 此文档描述 v2.1 重构前结构，Phase 15+ 一律对 LIVE code + codegraph 核对，勿信本快照具体路径/类名。保留作演进历史。" 只加水印，**不刷新内容**（D22：map 刷新是独立 /gsd-map-codebase 任务，不在 Phase 15 范围）。
    在 `.planning/PROJECT.md` 定位"9 态"/"~40 边"陈旧状态描述，用 Edit 精确替换为准确描述："6 态正交 MediaState（idle/opening/playing/paused/completed/error）+ isSeeking/isBuffering 独立标志；v3.0 补充正交 LifecyclePhase{alive,disposing,disposed}（见 Phase 15 契约冻结 D5-D12）"。仅改这一处陈旧描述，不重写 PROJECT.md 其余内容。
    全部为文档修正，用 Edit（scoped）不用 Write（PROJECT.md 是既有文件）。conventional-commit `docs:`。
  </action>
  <verify>
    <automated>grep -l "v2.1 前快照" .planning/codebase/*.md | wc -l | grep -qx 7 && ! grep -qE "9 态|~40 边|9 个状态" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files in `.planning/codebase/*.md` contain the watermark string "v2.1 前快照" at the top (`grep -l "v2.1 前快照" .planning/codebase/*.md` returns 7 files)
    - No `.planning/codebase/*.md` content section was otherwise rewritten (git diff shows only the added watermark line per file)
    - `.planning/PROJECT.md` no longer contains "9 态" / "~40 边" / "9 个状态" (`grep -cE "9 态|~40 边|9 个状态" .planning/PROJECT.md` == 0)
    - `.planning/PROJECT.md` now references "6 态正交" and "LifecyclePhase"
  </acceptance_criteria>
  <done>7 stale maps carry the v2.1-snapshot watermark (not refreshed, per D22) and PROJECT.md's stale 9-state description is corrected to the frozen 6-state + orthogonal LifecyclePhase model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit script → repository source (read-only) | The audit scripts only READ lib/ and WRITE snapshot artifacts; they never modify source. Boundary risk is a false count silently accepted as truth. |

## STRIDE Threat Register (ASVS L1, block_on: standard)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | inventory.sh count logic | medium | mitigate | Baseline drift risk: a naive regex double-counts memory_monitor.dart doc-comments or hardcodes a stale number, producing a wrong "truth"; or non-deterministic array ordering makes two runs superficially differ, masking whether the count is stable. Mitigated by the exclude-self-doc filter + sorted-array BYTE-IDENTICAL two-run reproducibility acceptance criterion (timestamp-stripped diff empty) + never-hardcode-count rule (RESEARCH Pitfall 1/2). |
| T-15-02 | Information Disclosure | 15-BASELINE-AUDIT.{json,md} | low | accept | Snapshot exposes only symbol counts and file paths already in the repo — no secrets. Accepted. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this plan (shell + rg only, both pre-installed). No legitimacy gate needed. |

Threat model is intentionally light: this is a read-only audit/doc plan. The only real threat is "a wrong baseline count is accepted as truth" (T-15-01), mitigated by the reproducibility + filter acceptance criteria.
</threat_model>

<verification>
- `bash tool/audit/inventory.sh` exits 0 and produces valid `15-BASELINE-AUDIT.json` + `.md`
- Two consecutive runs produce BYTE-IDENTICAL JSON (sorted arrays, timestamp-stripped) — not just equal counts (reproducibility gate, sc2)
- `bash tool/audit/contract_completeness.sh` runs (≤ 1 exit) and dynamically enumerates all 7 interface files
- 7 codebase maps watermarked; PROJECT.md 9-state description corrected
- `flutter analyze` unaffected (no lib/ source changed in this plan)
</verification>

<success_criteria>
BASE-02 satisfied: static call-site inventory (logger / MemoryMonitor / openGeneration) is reproducible via a committed script reading LIVE code, with a committed JSON+MD snapshot; stale maps flagged (D22); PROJECT.md stale state description corrected. Numbers are the script's output, not any hardcoded historical figure.
</success_criteria>

<output>
Create `.planning/phases/15-contract-freeze-baseline-audit/15-01-SUMMARY.md` when done
</output>
