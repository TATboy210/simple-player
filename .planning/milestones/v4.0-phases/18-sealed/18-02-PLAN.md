---
phase: 18-sealed
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - lib/kernel/engine/fvp_engine.dart
  - lib/kernel/engine/media_opener.dart
  - lib/kernel/services/playback_controller.dart
  - lib/kernel/engine/fvp_callback_handler.dart
  - lib/kernel/services/auto_advance_policy.dart
  - lib/kernel/services/playback_navigator.dart
  - test/kernel/engine/fvp_engine_error_test.dart
  - test/kernel/engine/fvp_callback_handler_test.dart
autonomous: true
requirements:
  - ERR-03
  - ERR-05

must_haves:
  truths:
    - Every FvpEngine catch point constructs PlayerError with ErrorContext, assigns lastError, logs via log.e()
    - MediaOpener error construction includes ErrorContext with action/path/module
    - PlaybackController._onError signature is PlayerError (not Object)
    - mdk callback errors are caught, wrapped in PlayerError with callbackStackTrace, marshalled to main thread via scheduleMicrotask
    - No bare catch (e) — only typed on Exception catch
    - No Error subtypes are ever caught
  artifacts:
    - lib/kernel/engine/fvp_engine.dart (8 catch points → three-step pattern)
    - lib/kernel/engine/media_opener.dart (ErrorContext on error construction)
    - lib/kernel/services/playback_controller.dart (_onError signature narrowed)
    - lib/kernel/engine/fvp_callback_handler.dart (callback error marshalling)
    - lib/kernel/services/auto_advance_policy.dart (Exception → PlaybackError wrapping at 2 call sites)
    - lib/kernel/services/playback_navigator.dart (Exception → PlaybackError wrapping at 2 call sites)
    - test/kernel/engine/fvp_engine_error_test.dart (new — three-step pattern tests)
    - test/kernel/engine/fvp_callback_handler_test.dart (extended — marshalling tests)
  key_links:
    - FvpEngine uses existing `final log = KernelLogger.I` top-level reference (no constructor change, per research Pitfall 1)
    - PlaybackController._onError is null in production — P18 only narrows signature (per research Pitfall 2)
    - FvpCallbackHandler._scheduleOnMain already exists — reuse for error marshalling
    - KernelLogger.error() accepts {Map? context, Object? error, StackTrace? stackTrace} (P17 delivered)
---

<objective>
Transform all engine catch points to the three-step error pattern (construct PlayerError with ErrorContext → assign lastError → log via kernel logger), narrow PlaybackController._onError signature, and add mdk callback error marshalling with callbackStackTrace.

Purpose: End-to-end error propagation chain from engine to service layer, with structured context at every catch point.
Output: Modified engine/service/callback files + new/extended error handling tests
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-sealed/18-CONTEXT.md
@.planning/phases/18-sealed/18-RESEARCH.md
@.planning/phases/18-sealed/18-01-PLAN.md
@lib/kernel/engine/fvp_engine.dart
@lib/kernel/engine/media_opener.dart
@lib/kernel/services/playback_controller.dart
@lib/kernel/engine/fvp_callback_handler.dart
@lib/kernel/diagnostics/kernel_logger.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FvpEngine catch points → three-step pattern + MediaOpener ErrorContext (ERR-03/ERR-05)</name>
  <files>lib/kernel/engine/fvp_engine.dart, lib/kernel/engine/media_opener.dart, test/kernel/engine/fvp_engine_error_test.dart</files>
  <read_first>
    - lib/kernel/engine/fvp_engine.dart
    - lib/kernel/engine/media_opener.dart
    - lib/kernel/models/player_error.dart (from Plan 01 — ErrorContext API)
    - lib/kernel/diagnostics/kernel_logger.dart (error() signature)
    - .planning/phases/18-sealed/18-CONTEXT.md (D2/D10 — three-step pattern)
    - .planning/phases/18-sealed/18-RESEARCH.md (Pattern 1: Three-Step Error Handling)
  </read_first>
  <action>
    **FvpEngine — transform all catch points to three-step pattern** (per D2/D10):

    The three-step pattern is: (1) construct PlayerError with ErrorContext → (2) assign lastError.value → (3) log.e() with structured context.

    Add `import '../models/player_error.dart';` if not already imported (for ErrorContext).

    **Catch point 1: _guardedAction (line ~234)** — `on Exception catch (e)`:
    - Change to `on Exception catch (e, st)` to capture stack trace.
    - Construct PlaybackError with ErrorContext(action: name, module: 'FvpEngine').
    - Assign to lastError.value.
    - Log via `log.e('FvpEngine.$name error', context: error.context?.toMap(), error: e, stackTrace: st)`.

    **Catch point 2: open() main catch (line ~313)** — `on Exception catch (e)`:
    - Change to `on Exception catch (e, st)`.
    - Construct NetworkError or PlaybackError (keep existing PathValidator.isUrl logic).
    - Add ErrorContext(action: 'open', generation: gen, path: trimmed, module: 'FvpEngine').
    - Assign to lastError.value.
    - Log via `log.e('open() error — ${PathUtils.basename(trimmed)}', context: error.context?.toMap(), error: e, stackTrace: st)`.

    **Catch point 3: open() empty path (line ~255)** — `lastError.value = const FileError(...)`:
    - Remove `const` (constructors no longer const per Plan 01).
    - Add ErrorContext(action: 'open', path: trimmed, module: 'FvpEngine').
    - Log via `log.e('open() empty path', context: error.context?.toMap())`.

    **Catch point 4: play() catch (line ~344)** — `on Exception catch (e)`:
    - Change to `on Exception catch (e, st)`.
    - Construct PlaybackError with ErrorContext(action: 'play', module: 'FvpEngine').
    - Assign to lastError.value.
    - Log via `log.e('play() error', context: error.context?.toMap(), error: e, stackTrace: st)`.

    **Catch point 5: pause() catch (line ~362)** — currently only logs, no lastError:
    - Change to `on Exception catch (e, st)`.
    - Construct PlaybackError(PlaybackErrorCode.playFailed, ...) with ErrorContext(action: 'pause', module: 'FvpEngine').
    - Assign to lastError.value (pause errors should surface to UI).
    - Log via `log.e('pause() error', context: error.context?.toMap(), error: e, stackTrace: st)`.

    **Catch point 6: stop() catch (line ~377)** — currently only logs, no lastError:
    - Change to `on Exception catch (e, st)`.
    - Construct PlaybackError(PlaybackErrorCode.playFailed, ...) with ErrorContext(action: 'stop', module: 'FvpEngine').
    - Assign to lastError.value.
    - Log via `log.e('stop() error', context: error.context?.toMap(), error: e, stackTrace: st)`.

    **Catch point 7: seekTo() catch (line ~398)** — `on Exception catch (e)`:
    - Change to `on Exception catch (e, st)`.
    - Construct PlaybackError with ErrorContext(action: 'seek', module: 'FvpEngine').
    - Assign to lastError.value.
    - Log via `log.e('seekTo() error', context: error.context?.toMap(), error: e, stackTrace: st)`.

    **Catch point 8: open() OpenError branch (line ~307)** — `lastError.value = error`:
    - This receives a PlayerError from MediaOpener. Add ErrorContext enrichment if error.context is null:
      `error.context ??= ErrorContext(action: 'open', generation: gen, path: trimmed, module: 'MediaOpener');`
    - Log via `log.e('open() error — ${PathUtils.basename(trimmed)}', context: error.context?.toMap())`.

    **MediaOpener — add ErrorContext to all error construction points** (per D2):

    All error returns in MediaOpener.open() should include ErrorContext. Since MediaOpener returns OpenError(PlayerError), the ErrorContext is attached to the PlayerError before wrapping in OpenError.

    - Line ~51 (empty path): `FileError(...)` → add `.context = ErrorContext(action: 'open', path: trimmed, module: 'MediaOpener')`
    - Line ~59 (file not found): same pattern
    - Line ~67 (path invalid exception): add ErrorContext with module: 'MediaOpener'
    - Line ~87-102 (prepare timeout/decode failed): add ErrorContext with action: 'prepare', path: trimmed, module: 'MediaOpener'
    - Line ~149 (texture timeout): add ErrorContext with action: 'texture', path: trimmed, module: 'MediaOpener'
    - Line ~157 (null textureId): add ErrorContext with action: 'texture', path: trimmed, module: 'MediaOpener'

    Note: ErrorContext uses the cascade assignment pattern `..context = ErrorContext(...)` since context is a mutable field.

    **Create test/kernel/engine/fvp_engine_error_test.dart** — new test file for ERR-03:
    - Test that _guardedAction constructs PlaybackError with ErrorContext (action matches name, module is 'FvpEngine').
    - Test that open() empty path constructs FileError with ErrorContext.
    - Test that play() exception constructs PlaybackError with ErrorContext.
    - Test that seekTo() exception constructs PlaybackError with ErrorContext(action: 'seek').
    - Use FakeEngine or a minimal mock setup to trigger catch paths.

    Note: Integration tests for the full three-step pattern (construct → lastError → log) require a real or fake engine. Use the existing FakeEngine from test/helpers/ where possible. For catch-point-specific tests, consider creating a test that forces an exception (e.g., open with invalid path after validation passes).
  </action>
  <verify>
    <automated>flutter test test/kernel/engine/fvp_engine_error_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Every FvpEngine catch point follows three-step pattern: construct PlayerError with ErrorContext → assign lastError.value → log.e() with structured context
    - Every MediaOpener error return includes ErrorContext with action/path/module
    - No bare `catch (e)` without stack trace capture — all changed to `catch (e, st)`
    - No `Error` subtypes caught — only `on Exception catch`
    - fvp_engine_error_test.dart passes: ErrorContext fields populated correctly at each catch point
    - Existing FvpEngine tests still pass (backward compatible — ErrorContext is optional)
  </acceptance_criteria>
  <done>
    All FvpEngine and MediaOpener catch points construct PlayerError with ErrorContext, assign lastError, and log via kernel logger. Error propagation chain from engine is structurally complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: PlaybackController signature narrowing + FvpCallbackHandler error marshalling (ERR-03/ERR-05)</name>
  <files>lib/kernel/services/playback_controller.dart, lib/kernel/engine/fvp_callback_handler.dart, test/kernel/engine/fvp_callback_handler_test.dart</files>
  <read_first>
    - lib/kernel/services/playback_controller.dart
    - lib/kernel/engine/fvp_callback_handler.dart
    - test/kernel/engine/fvp_callback_handler_test.dart
    - lib/kernel/models/player_error.dart (from Plan 01 — PlayerError type)
    - .planning/phases/18-sealed/18-CONTEXT.md (D8/D9/D11)
    - .planning/phases/18-sealed/18-RESEARCH.md (Pitfall 2, Pattern: Thread Marshalling)
  </read_first>
  <action>
    **PlaybackController — narrow _onError signature** (per D8):

    - Change `void Function(Object error)? _onError` to `void Function(PlayerError error)? _onError` on line ~73.
    - Change the constructor parameter `void Function(Object error)? onError` to `void Function(PlayerError error)? onError` on line ~50.
    - Change the getter `void Function(Object error)? get onError => _onError;` to `void Function(PlayerError error)? get onError => _onError;` on line ~103.
    - Add `import '../models/player_error.dart';` if not already imported.
    - Note: _onError is null in production (research Pitfall 2). P18 only narrows the type — do NOT attempt to wire it in PlayerServices. **CRITICAL:** The 4 call sites in auto_advance_policy.dart and playback_navigator.dart that pass Exception objects MUST be updated to wrap in PlaybackError (Task 3) before this signature change compiles. Task 3 is a prerequisite for compilation.

    **FvpCallbackHandler — add error marshalling in mdk callbacks** (per D9/D11):

    In `_player.onStateChanged.listen` callback (line ~37):
    - Wrap the existing callback body in a try-catch.
    - `on Exception catch (e, st)`: Construct PlaybackError(PlaybackErrorCode.playFailed, 'mdk callback error: $e', e) with ErrorContext(action: 'mdk.onStateChanged', module: 'FvpCallbackHandler', callbackStackTrace: st).
    - Marshal to main thread: `_scheduleOnMain(() { if (_disposed) return; _lastErrorNotifier.value = error; log.e('mdk callback error', context: error.context?.toMap(), error: e, stackTrace: st); });`
    - This requires FvpCallbackHandler to have access to a `ValueNotifier<PlayerError?>` for lastError. Add a `ValueNotifier<PlayerError?> lastErrorNotifier` parameter to the constructor.

    In `_player.onMediaStatus.listen` callback (line ~48):
    - Same pattern: wrap body in try-catch, construct ErrorContext(action: 'mdk.onMediaStatus', module: 'FvpCallbackHandler', callbackStackTrace: st), marshal to main thread.

    In FvpEngine factory constructor (line ~73-77), pass `lastError` to FvpCallbackHandler:
    - `engine._callbackHandler = FvpCallbackHandler(player, stateMachine: stateMachine, onStopPositionPolling: () => engine._positionPoller.stop(), lastErrorNotifier: engine.lastError);`

    **Extend test/kernel/engine/fvp_callback_handler_test.dart** for ERR-05:
    - Test that mdk callback exception constructs PlaybackError with ErrorContext containing callbackStackTrace.
    - Test that error is marshalled to main thread (lastErrorNotifier.value is set).
    - Test that disposed handler does not process errors.
    - Use existing test patterns — the file already tests state mapping and disposal.
  </action>
  <verify>
    <automated>flutter test test/kernel/engine/fvp_callback_handler_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - PlaybackController._onError type is `void Function(PlayerError error)?`
    - FvpCallbackHandler catches exceptions in both mdk callbacks (onStateChanged, onMediaStatus)
    - Caught exceptions construct PlaybackError with ErrorContext containing callbackStackTrace
    - Errors marshalled to main thread via _scheduleOnMain before assigning lastErrorNotifier.value
    - FvpCallbackHandler receives lastErrorNotifier via constructor injection
    - FvpEngine factory passes engine.lastError to FvpCallbackHandler
    - All existing callback handler tests pass
    - New tests verify error construction and marshalling behavior
  </acceptance_criteria>
  <done>
    PlaybackController._onError narrowed to PlayerError. FvpCallbackHandler catches mdk callback exceptions, constructs PlayerError with ErrorContext (including callbackStackTrace), and marshals to main thread. Error propagation chain is complete from mdk callbacks through to UI.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wrap Exception call sites in auto_advance_policy.dart and playback_navigator.dart (D8 downstream fix)</name>
  <files>lib/kernel/services/auto_advance_policy.dart, lib/kernel/services/playback_navigator.dart</files>
  <read_first>
    - lib/kernel/services/auto_advance_policy.dart
    - lib/kernel/services/playback_navigator.dart
    - lib/kernel/models/player_error.dart (PlaybackError constructor)
    - lib/kernel/services/playback_controller.dart (Task 2 — _onError signature after narrowing)
  </read_first>
  <action>
    **Problem (D8 downstream):** Task 2 narrows PlaybackController._onError from `Object` to `PlayerError`. Four call sites in auto_advance_policy.dart and playback_navigator.dart currently pass raw `Exception` objects, which will not compile after the signature change.

    **auto_advance_policy.dart — 2 call sites:**

    Add import: `import '../models/player_error.dart';`

    Line 62 (`_replayIndex` catch): `_controller.onError?.call(e);`
    - Replace with: `_controller.onError?.call(PlaybackError(PlaybackErrorCode.playFailed, 'AutoAdvancePolicy loopSingle replay failed: $e', e));`
    - This wraps the caught Exception in a PlaybackError with code playFailed, preserving the original exception as cause.

    Line 72 (`_autoAdvance` catch): `_controller.onError?.call(e);`
    - Replace with: `_controller.onError?.call(PlaybackError(PlaybackErrorCode.playFailed, 'AutoAdvancePolicy auto-advance failed: $e', e));`
    - Same pattern.

    **playback_navigator.dart — 2 call sites:**

    Add import: `import '../models/player_error.dart';`

    Line 59 (path validation): `_controller.onError?.call(Exception(validationError));`
    - Replace with: `_controller.onError?.call(FileError(FileErrorCode.pathTraversal, validationError));`
    - Path validation errors are FileError with pathTraversal code — more specific than generic PlaybackError.

    Line 99 (`playIndex` catch): `_controller.onError?.call(e);`
    - Replace with: `_controller.onError?.call(PlaybackError(PlaybackErrorCode.playFailed, 'PlaybackNavigator.playIndex($index) failed: $e', e));`
    - Wraps the caught Exception in a PlaybackError with playFailed code.

    **Verification:** After all 4 replacements, `_controller.onError?.call(...)` always receives a `PlayerError` subtype, matching the narrowed signature from Task 2.
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/services/auto_advance_policy.dart lib/kernel/services/playback_navigator.dart</automated>
  </verify>
  <acceptance_criteria>
    - auto_advance_policy.dart: both _controller.onError calls pass PlaybackError (not raw Exception)
    - playback_navigator.dart: line 59 passes FileError(pathTraversal), line 99 passes PlaybackError(playFailed)
    - Both files import player_error.dart
    - flutter analyze passes with zero errors on both files
    - Existing tests for AutoAdvancePolicy and PlaybackNavigator still pass
  </acceptance_criteria>
  <done>
    All 4 Exception call sites wrapped in PlayerError subtypes. PlaybackController._onError(PlayerError) signature compiles cleanly across the entire call graph.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mdk callback thread → main thread | Error objects cross thread boundary via scheduleMicrotask |
| ErrorContext.path → logger | File paths in error context logged to debug output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-02 | Information Disclosure | ErrorContext.path in logger output | low | accept | KernelLogger redactPath() already strips directory prefixes. ErrorContext.path is diagnostic-only, same data already present in error.message strings. |
| T-18-03 | Denial of Service | scheduleMicrotask in callback error path | low | accept | scheduleMicrotask is standard Flutter mechanism. Error construction is lightweight (no I/O). Disposed guard prevents accumulation. |
| T-18-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages — Phase 18 is zero-dependency. |
</threat_model>

## Artifacts this phase produces

| Symbol / File | Type | Description |
|---|---|---|
| FvpEngine catch points (8) | modified | Three-step pattern: construct PlayerError with ErrorContext → lastError → log.e() |
| MediaOpener error construction | modified | ErrorContext added to all 6 error return sites |
| PlaybackController._onError | signature change | `Object` → `PlayerError` per D8 |
| FvpCallbackHandler error marshalling | new behavior | mdk callback catch → ErrorContext(callbackStackTrace) → scheduleMicrotask → main thread |
| FvpCallbackHandler.lastErrorNotifier | constructor param | ValueNotifier<PlayerError?> injection for error propagation |
| auto_advance_policy.dart (2 call sites) | modified | Exception → PlaybackError wrapping per D8 downstream |
| playback_navigator.dart (2 call sites) | modified | Exception → FileError/PlaybackError wrapping per D8 downstream |
| test/kernel/engine/fvp_engine_error_test.dart | new test file | ERR-03 three-step pattern verification |
| test/kernel/engine/fvp_callback_handler_test.dart | extended | ERR-05 marshalling behavior tests |

<verification>
- `flutter analyze` passes on all modified files (including auto_advance_policy.dart and playback_navigator.dart)
- `flutter test test/kernel/engine/fvp_engine_error_test.dart` — all green
- `flutter test test/kernel/engine/fvp_callback_handler_test.dart` — all green
- `flutter test test/kernel/services/` — all green (PlaybackController signature compatible)
- Grep verification: no bare `catch (e)` in fvp_engine.dart (all have `, st`)
- Grep verification: no `_controller.onError?.call(e)` with raw Exception in auto_advance_policy.dart or playback_navigator.dart
</verification>

<success_criteria>
- All 8 FvpEngine catch points use three-step pattern (ERR-03)
- MediaOpener errors carry ErrorContext (ERR-03)
- PlaybackController._onError takes PlayerError (ERR-03)
- All 4 downstream call sites (auto_advance_policy + playback_navigator) wrap Exception → PlayerError before calling _onError
- mdk callback errors marshalled with callbackStackTrace (ERR-05)
- All tests pass
- Zero bare catch(e) in engine files
</success_criteria>

<output>
Create `.planning/phases/18-sealed/18-02-SUMMARY.md` when done
</output>
