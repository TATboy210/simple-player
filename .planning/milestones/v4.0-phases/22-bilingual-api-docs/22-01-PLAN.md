---
phase: 22
plan: 01
title: Bilingual API Documentation — Tier 1 Core + Tier 2 Interfaces + Tier 3 Overrides
status: executing
created: 2026-07-21
---

# Plan 22-01: Bilingual API Documentation Sweep

**Goal:** Add bilingual comments (Chinese intent + English contract) to all v3.0 public symbols in `lib/kernel/**`.

**Scope:** 14 v3.0 files, ~165 symbols needing work (excluding ~80 forwarding/null-object overrides).

## Audit Summary

| Tier | Description | Symbols | Strategy |
|------|-------------|---------|----------|
| 1 | Core API (sealed classes, state machine, logger methods, bundle) | ~45 | Full bilingual per-symbol |
| 2 | Interface methods (slot interfaces, error codes) | ~40 | Full bilingual per-symbol |
| 3 | @override forwarding (null-objects, adapter stubs, impl) | ~80 | Section headers + inherit from interface |

## Execution Waves

### Wave 1: Tier 1 — Core API contracts
1. `models/player_error.dart` — PlayerError sealed + ErrorContext + 5 subclasses + 4 error code enums
2. `engine/engine_state_machine.dart` — constructor + 6 fields + togglePlayPause
3. `diagnostics/kernel_logger.dart` — KernelLogger abstract class + 12 methods + 6 shortcuts + 4 sink classes
4. `diagnostics/diagnostics_bundle.dart` — constructor + 4 fields
5. `diagnostics/memory_monitor.dart` — 2 statics + 6 fields

### Wave 2: Tier 2 — Interface method contracts
1. `diagnostics/event_log_slot.dart` — 5 interface methods + 6 null-object overrides
2. `diagnostics/memory_monitor_slot.dart` — 4 interface methods + 5 null-object overrides
3. `diagnostics/metrics_slot.dart` — 8 interface methods + 9 null-object overrides
4. `diagnostics/memory_snapshot.dart` — 2 classes, 11 symbols
5. `diagnostics/rss_provider.dart` — interface + 2 implementations
6. `diagnostics/clock.dart` — interface + 2 implementations

### Wave 3: Tier 3 — Adapter forwarding stubs
1. `adapter/kernel_adapter.dart` — DelegationPolicy fields + class-level section header for overrides

## Verification
- `flutter analyze` passes
- `flutter test` passes
- Grep: no Chinese-only `///` on public symbols in v3.0 files
