---
phase: 24-sidebar-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/dialogs/settings/_settings_nav_item.dart
  - lib/ui/dialogs/settings/settings_overlay_shell.dart
  - lib/ui/dialogs/settings/settings_panel_controller.dart
  - test/ui/dialogs/settings_overlay_shell_test.dart
  - test/ui/dialogs/settings_nav_item_test.dart
autonomous: true
requirements:
  - SIDEBAR-01
  - SIDEBAR-02
  - SIDEBAR-03

must_haves:
  truths:
    - "SettingsNavItem renders horizontally with icon + label side by side (not stacked vertically)"
    - "Shell renders a 40px horizontal tab bar with 7 equal-width tabs between title bar and content area"
    - "Tab bar background is Tokens.bgPanel (not bgSurface which does not exist)"
    - "Clicking a tab switches the IndexedStack to show that tab's content"
    - "Tab content fades in/out via TweenAnimationBuilder 200ms opacity transition (D-02)"
    - "All 7 tabs stay alive in IndexedStack — switching preserves pending state (D-01)"
    - "Opening the panel resets selectedTab to index 0 (General) every time (D-03)"
    - "Content area has 16dp padding (Tokens.spMd) around all edges (D-10)"
  artifacts:
    - lib/ui/dialogs/settings/_settings_nav_item.dart (refactored horizontal layout)
    - lib/ui/dialogs/settings/settings_overlay_shell.dart (tab bar + IndexedStack added)
    - lib/ui/dialogs/settings/settings_panel_controller.dart (nextTab/prevTab/reset)
    - test/ui/dialogs/settings_nav_item_test.dart (new)
    - test/ui/dialogs/settings_overlay_shell_test.dart (extended)
  key_links:
    - SettingsNavItem renders inside shell's tab bar Row via Expanded
    - SettingsPanelController.nextTab()/prevTab() wraps around首尾循环
    - selectedTab ValueNotifier drives both tab bar highlight and IndexedStack index
    - TweenAnimationBuilder reads selectedTab to animate opacity per tab
---

<objective>
Build the horizontal tab bar navigation system inside the Phase 23 overlay shell: refactor SettingsNavItem from vertical 80px to horizontal equal-width, add 7-tab Row bar with bgPanel background, replace empty Expanded content area with IndexedStack + TweenAnimationBuilder fade animation, and wire click switching with state reset on open.

Purpose: Proves the complete tab navigation architecture end-to-end (nav item renders horizontally, tab bar displays 7 tabs, click switches IndexedStack, animation plays, state resets) before adding keyboard/gamepad input in Plan 2.
Output: Working horizontal tab bar navigation with click switching and fade animation inside the settings overlay shell.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-sidebar-navigation/24-CONTEXT.md
@.planning/phases/24-sidebar-navigation/24-RESEARCH.md
@.planning/phases/24-sidebar-navigation/24-VALIDATION.md
@lib/ui/dialogs/settings/settings_overlay_shell.dart
@lib/ui/dialogs/settings/_settings_nav_item.dart
@lib/ui/dialogs/settings/settings_panel_state.dart
@lib/ui/dialogs/settings/settings_panel_controller.dart
@lib/ui/theme/tokens.dart
@lib/ui/shared/glass_container.dart
@test/ui/dialogs/settings_overlay_shell_test.dart
@test/ui/dialogs/settings_panel_controller_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor SettingsNavItem to horizontal + tab bar + IndexedStack + controller methods (tracer)</name>
  <files>
    lib/ui/dialogs/settings/_settings_nav_item.dart,
    lib/ui/dialogs/settings/settings_overlay_shell.dart,
    lib/ui/dialogs/settings/settings_panel_controller.dart
  </files>
  <read_first>
    lib/ui/dialogs/settings/_settings_nav_item.dart,
    lib/ui/dialogs/settings/settings_overlay_shell.dart,
    lib/ui/dialogs/settings/settings_panel_controller.dart,
    lib/ui/dialogs/settings/settings_panel_state.dart,
    lib/ui/theme/tokens.dart,
    lib/ui/shared/glass_container.dart,
    lib/ui/dialogs/settings_panel.dart (lines 880-945 for _Sidebar tab icons/labels)
  </read_first>
  <action>
This is the tracer task — builds the complete tab navigation architecture end-to-end.

**Step 1: Refactor SettingsNavItem (D-11)**
File: `lib/ui/dialogs/settings/_settings_nav_item.dart`
Rewrite from vertical 80px Column layout to horizontal Row layout:
- Remove `width: 80` (parent Row uses Expanded equal-width per D-09)
- Change internal Column to Row: icon + label side by side
- Change left border indicator to bottom border indicator (horizontal tab convention)
- Keep MouseRegion hover + GestureDetector tap + AnimatedContainer interaction logic unchanged
- Keep all Tokens.* usage unchanged (bgHover, accent, textSecondary, durationFast, radiusSm)
- Change padding from `symmetric(vertical: spSm)` to `symmetric(horizontal: spSm, vertical: spXs)` for horizontal compactness
- Icon size stays Tokens.iconLg, text stays Tokens.fontOverline

**Step 2: Add nextTab()/prevTab() + reset to controller**
File: `lib/ui/dialogs/settings/settings_panel_controller.dart`
- Add `nextTab()` method: increments selectedTab.value with wrapping (6→0). Per D-05: `state.selectedTab.value = (state.selectedTab.value + 1) % 7`
- Add `prevTab()` method: decrements selectedTab.value with wrapping (0→6). Per D-05: `state.selectedTab.value = (state.selectedTab.value - 1 + 7) % 7`
- In `open()` method: add `state.selectedTab.value = 0` BEFORE `state.isOpen.value = true` per D-03 (always reset to General on open)
- Add `static const int tabCount = 7` constant for the wrapping modulo

**Step 3: Add tab bar + IndexedStack to shell**
File: `lib/ui/dialogs/settings/settings_overlay_shell.dart`
In `_buildPanel()`, replace the Column's children:
- Keep `_buildTitleBar(mediaSize, panelSize)` as first child
- Add new `_buildTabBar()` as second child (40px height, bgPanel background, per D-07/D-08)
- Replace the empty `Expanded(child: GestureDetector(...))` with `Expanded(child: _buildContent())` per D-01/D-02

`_buildTabBar()` method:
- Returns Container with height 40, color Tokens.bgPanel (NOT bgSurface — does not exist per research Pitfall 1)
- Child is Row with 7 Expanded children, each containing a SettingsNavItem
- Tab definitions as static const lists in the shell class:
  - Icons: `Icons.tune, Icons.equalizer, Icons.headphones, Icons.videocam, Icons.keyboard, Icons.info_outline, Icons.speed` (from old _Sidebar lines 899-939)
  - Labels: Use hardcoded strings 'General', 'EQ', 'Audio', 'Video', 'Shortcuts', 'About', 'Performance' (the l10n keys like l10n.generalTab require AppLocalizations which adds complexity; Phase 25 can upgrade to l10n)
- Each tab: `Expanded(child: SettingsNavItem(icon: _tabIcons[i], label: _tabLabels[i], selected: selectedIndex == i, onTap: () => _controller.state.selectedTab.value = i))`
- Wrap in ValueListenableBuilder<int> reading `_controller.state.selectedTab`

`_buildContent()` method:
- Returns ValueListenableBuilder<int> reading `_controller.state.selectedTab`
- Child is IndexedStack with index = selectedIndex
- 7 children, each wrapped in TweenAnimationBuilder<double> per D-02:
  - `tween: Tween<double>(end: i == selectedIndex ? 1.0 : 0.0)`
  - `duration: Duration(milliseconds: 200)` (D-07 unified duration)
  - builder returns `Opacity(opacity: opacity, child: child)`
  - child is a placeholder: `Center(child: Text(_tabLabels[i], style: TextStyle(color: Tokens.textSecondary, fontSize: Tokens.fontBody)))`
- Content area wrapped in Padding(padding: EdgeInsets.all(Tokens.spMd)) per D-10

**Important constraints:**
- Tokens.bgPanel (0xFF111520), NOT Tokens.bgSurface (does not exist — research Pitfall 1)
- All visual values via Tokens.* — no hardcoded colors/sizes
- Keep existing title bar, mask, animation, drag logic unchanged
- GlassButton.iconOnly import stays (used by close button)
- Do NOT add keyboard/gamepad handling in this plan — that is Plan 2
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter test test/ui/dialogs/settings_overlay_shell_test.dart test/ui/dialogs/settings_nav_item_test.dart 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` passes with zero errors on modified files
    - SettingsNavItem renders as horizontal Row (icon left, label right) — NOT vertical Column
    - Tab bar renders 7 equal-width tabs in a Row with Tokens.bgPanel background, height 40
    - IndexedStack contains 7 children, one visible at a time
    - TweenAnimationBuilder wraps each IndexedStack child with 200ms opacity animation
    - Clicking a tab sets selectedTab.value to the correct index
    - selectedTab resets to 0 when controller.open() is called (D-03)
    - Controller.nextTab() wraps 6→0, prevTab() wraps 0→6
    - Content area has Padding(spMd) around all edges
    - All existing shell tests still pass (ESC, B, drag, mask tap, sizing)
  </acceptance_criteria>
  <done>
    SettingsNavItem renders horizontally, shell displays 7-tab horizontal bar with bgPanel background, IndexedStack with TweenAnimationBuilder 200ms fade handles tab content, click switching works, selectedTab resets to 0 on open, all existing tests pass.
  </done>
  <reversibility rating="reversible">All changes are to UI widgets and controller logic; no data model or persistence changes.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Widget tests for SIDEBAR-01/02/03 — tab bar rendering, click switching, fade animation, state preservation</name>
  <files>
    test/ui/dialogs/settings_nav_item_test.dart,
    test/ui/dialogs/settings_overlay_shell_test.dart
  </files>
  <read_first>
    test/ui/dialogs/settings_overlay_shell_test.dart,
    test/ui/dialogs/settings_panel_controller_test.dart,
    lib/ui/dialogs/settings/settings_overlay_shell.dart,
    lib/ui/dialogs/settings/_settings_nav_item.dart
  </read_first>
  <action>
**Step 1: Create SettingsNavItem tests**
File: `test/ui/dialogs/settings_nav_item_test.dart`
New test file covering SIDEBAR-02:
- Test: SettingsNavItem renders icon + label text
- Test: selected=false shows transparent background, selected=true shows accent-colored indicator
- Test: hover shows bgHover background (MouseRegion enter/exit)
- Test: onTap callback fires when tapped
- Test: layout is horizontal Row (not vertical Column) — verify by checking RenderFlex direction

**Step 2: Extend shell tests for SIDEBAR-01/03**
File: `test/ui/dialogs/settings_overlay_shell_test.dart`
Add new test group `'Tab Navigation'`:
- Test: open shell renders 7 tab items in a Row (find 7 SettingsNavItem widgets)
- Test: tab bar has height 40 and bgPanel background color
- Test: default selected tab is index 0 (General) on open (D-03)
- Test: clicking tab index 3 switches IndexedStack to show index 3 content
- Test: clicking tab changes selectedTab.value (verify via controller.state.selectedTab)
- Test: opening panel resets selectedTab to 0 even if previously on another tab (D-03 reset)
- Test: all 7 tab labels are visible in the tab bar
- Test: TweenAnimationBuilder wraps IndexedStack children (verify by finding TweenAnimationBuilder widgets)
- Test: IndexedStack index matches selectedTab value
- Test: after switching tabs, the previous tab's widget is still in tree (IndexedStack keeps alive, D-01)

All tests use the existing `pumpShell()` helper pattern from the current test file.
Use `FakePlaybackController` from `settings_panel_controller_test.dart` (already imported).
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter test test/ui/dialogs/settings_nav_item_test.dart test/ui/dialogs/settings_overlay_shell_test.dart 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - New file `test/ui/dialogs/settings_nav_item_test.dart` exists with at least 4 test cases
    - Extended `test/ui/dialogs/settings_overlay_shell_test.dart` has new 'Tab Navigation' group with at least 8 test cases
    - All tests pass: `flutter test test/ui/dialogs/` exits 0
    - SIDEBAR-01 covered: tab bar renders 7 equal-width tabs
    - SIDEBAR-02 covered: tabs render icon + label, selected/unselected states
    - SIDEBAR-03 covered: click switches content, FadeTransition present, IndexedStack preserves state
    - D-03 covered: reset to tab 0 on open
  </acceptance_criteria>
  <done>
    Comprehensive widget tests exist for tab bar rendering (SIDEBAR-01), nav item states (SIDEBAR-02), click switching with fade animation (SIDEBAR-03), and reset-on-open (D-03). All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | UI-only tab navigation. No authentication, data handling, or cryptographic operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed in this phase. All components are Flutter SDK built-in or project-local. |
| T-24-02 | Information Disclosure | N/A | low | accept | No sensitive data handled in tab navigation UI. |
</threat_model>

<verification>
1. `flutter analyze` — zero errors on all modified files
2. `flutter test test/ui/dialogs/` — all tests pass (existing + new)
3. `flutter test test/ui/dialogs/settings_nav_item_test.dart` — new nav item tests pass
4. `flutter test test/ui/dialogs/settings_overlay_shell_test.dart` — extended shell tests pass
5. Visual: tab bar renders 7 equal-width tabs with bgPanel background between title bar and content area
6. Interaction: clicking a tab switches content with 200ms fade animation
7. State: opening panel resets to General tab (index 0)
</verification>

<success_criteria>
- SettingsNavItem renders horizontally (Row, not Column)
- Shell displays 7-tab horizontal bar (40px, bgPanel, equal-width)
- IndexedStack + TweenAnimationBuilder handles tab content with 200ms fade
- Click switching works end-to-end
- selectedTab resets to 0 on every open()
- All existing + new tests pass
- SIDEBAR-01, SIDEBAR-02, SIDEBAR-03 fully covered
</success_criteria>

<output>
Create `.planning/phases/24-sidebar-navigation/24-01-SUMMARY.md` when done
</output>
