---
phase: 23-overlay-shell-state-model
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/ui/shared/apple_curves.dart
  - lib/kernel/services/playback_controller.dart
  - lib/ui/dialogs/settings/settings_panel_state.dart
  - lib/ui/dialogs/settings/settings_panel_controller.dart
  - test/ui/dialogs/settings_panel_state_test.dart
  - test/ui/dialogs/settings_panel_controller_test.dart
autonomous: false
requirements:
  - PANEL-01
  - PANEL-02
user_setup: []
must_haves:
  truths:
    - "PANEL-01: SettingsPanelState initializes exactly isOpen=false, selectedTab=0, and dragOffset=Offset.zero, and disposes all three notifiers."
    - "PANEL-02: open() snapshots playing state once, pauses only a playing controller, makes the shell open, and close() resumes only that captured playing session while resetting dragOffset."
    - "PANEL-02 idempotency: a second open() while open and a second close() while closed leave playback call counts and state unchanged."
    - "PANEL-02 concurrency assumption resolved: wasPlaying is read before pause() in the same synchronous open() call; tests cover its snapshot semantics without asynchronous interleaving."
    - "FLAGGED ASSUMPTION (PANEL-01 unclassified): dragOffset is session-only presentation state; it starts at zero and is reset on close rather than persisted."
  artifacts:
    - "lib/ui/shared/apple_curves.dart is tracked in git before any shell source imports AppleCurves."
    - "lib/kernel/services/playback_controller.dart exposes SettingsPanelPlayback, pause(), play(), and isPlaying."
    - "lib/ui/dialogs/settings/settings_panel_state.dart defines SettingsPanelState with only its three required ValueNotifiers."
    - "lib/ui/dialogs/settings/settings_panel_controller.dart defines SettingsPanelController."
    - "test/ui/dialogs/settings_panel_state_test.dart and test/ui/dialogs/settings_panel_controller_test.dart execute without a real MediaEngine."
  key_links:
    - "SettingsPanelController invokes the PlaybackController service boundary through SettingsPanelPlayback rather than accessing MediaEngine."
    - "The tracked AppleCurves API is available before the Wave 1 shell imports fullscreenEnter and fullscreenExit."
  prohibitions:
    - statement: "FLAGGED-UNVERIFIED: SettingsPanelController must not bypass PlaybackController to invoke MediaEngine playback methods."
      status: flagged-unverified
    - statement: "FLAGGED-UNVERIFIED: the state model must not introduce Provider, Riverpod, Bloc, or another state-management framework."
      status: flagged-unverified
---

<objective>
Lock the animation-curve prerequisite and create the testable state/controller lifecycle that coordinates playback through the existing service facade.

Purpose: Establish the shell's real state and playback contract before any in-tree overlay mounts it.
Output: Tracked AppleCurves utility, PlaybackController forwarders and narrow test seam, SettingsPanelState/SettingsPanelController, and focused unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-overlay-shell-state-model/23-CONTEXT.md
@.planning/phases/23-overlay-shell-state-model/23-RESEARCH.md
@.planning/phases/23-overlay-shell-state-model/23-PATTERNS.md
@.planning/phases/23-overlay-shell-state-model/23-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- Classes: SettingsPanelState, SettingsPanelController, SettingsOverlayShell, FakePlaybackController.
- Service contract: SettingsPanelPlayback.
- Methods: SettingsPanelController.open(), close(), toggle(); PlaybackController.pause(), play(); PlaybackController.isPlaying getter.
- Notifier fields: SettingsPanelState.isOpen, selectedTab, dragOffset.
- Source paths: lib/ui/shared/apple_curves.dart; lib/kernel/services/playback_controller.dart; lib/ui/dialogs/settings/settings_panel_state.dart; lib/ui/dialogs/settings/settings_panel_controller.dart; lib/ui/dialogs/settings/settings_overlay_shell.dart.
- Test paths: test/ui/dialogs/settings_panel_state_test.dart; test/ui/dialogs/settings_panel_controller_test.dart; test/ui/dialogs/settings_overlay_shell_test.dart.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="advisory">
  <name>Task 1: Record the already-locked AppleCurves dependency decision</name>
  <read_first>
    - lib/ui/shared/apple_curves.dart
    - .planning/phases/23-overlay-shell-state-model/23-CONTEXT.md
    - .planning/phases/23-overlay-shell-state-model/23-RESEARCH.md
    - .planning/phases/23-overlay-shell-state-model/23-PATTERNS.md
    - .planning/phases/23-overlay-shell-state-model/23-VALIDATION.md
    - CLAUDE.md
  </read_first>
  <reversibility rating="costly">D-08 makes the in-tree AppleCurves API a shared animation dependency; changing it after shell adoption changes coordinated animation references.</reversibility>
  <decision>Confirm the D-08 curve API lock before it becomes a tracked dependency.</decision>
  <context>The user already chose apple_curves.dart. This advisory checkpoint records that fullscreenEnter is the open curve and fullscreenExit is the close curve; it does not request a new design choice or halt execution.</context>
  <options>
    <option id="locked-d08">
      <name>Keep the user-locked AppleCurves API</name>
      <pros>Matches Apple HIG comments already present in the utility and the Phase 23 design contract.</pros>
      <cons>Later curve-family replacement requires coordinated changes in the overlay shell.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The execution record identifies AppleCurves.fullscreenEnter for opening and AppleCurves.fullscreenExit for closing per D-08.
    - The checkpoint remains advisory and does not require user input to continue.
  </acceptance_criteria>
  <verify><automated>git ls-files --error-unmatch lib/ui/shared/apple_curves.dart || test -f lib/ui/shared/apple_curves.dart</automated></verify>
  <resume-signal>Continue with locked-d08.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Track curve API and add playback-facade controls</name>
  <read_first>
    - lib/ui/shared/apple_curves.dart
    - lib/kernel/services/playback_controller.dart
    - lib/kernel/engine/engine_state.dart
    - lib/kernel/engine/media_state.dart
    - .planning/phases/23-overlay-shell-state-model/23-CONTEXT.md
    - .planning/phases/23-overlay-shell-state-model/23-PATTERNS.md
    - .planning/phases/23-overlay-shell-state-model/23-VALIDATION.md
    - CLAUDE.md
  </read_first>
  <reversibility rating="costly">D-08 tracks the shared AppleCurves utility before imports depend on it; curve API changes later affect the shell's coordinated enter and exit animation references.</reversibility>
  <files>lib/ui/shared/apple_curves.dart, lib/kernel/services/playback_controller.dart</files>
  <action>First run `git add lib/ui/shared/apple_curves.dart` and commit only that already-complete utility with `chore: track apple curves for settings shell`; do not edit its seven Cubic constants. In `lib/kernel/services/playback_controller.dart`, add the narrow `SettingsPanelPlayback` service contract exposing `bool get isPlaying`, `void pause()`, and `void play()` so the unit-test FakePlaybackController can be hand-written without MediaEngine or mdk.dll. Make PlaybackController implement that contract. In its existing UI forwarding section, add documented `pause()` and `play()` direct forwarders to `engine.pause()` and `engine.play()`, and derive `isPlaying` from `engine.state.value == MediaState.playing`. This implements D-03 through the PlaybackController boundary; no UI code may access the engine through this new controller path.</action>
  <acceptance_criteria>
    - `git ls-files --error-unmatch lib/ui/shared/apple_curves.dart` exits 0 before any SettingsOverlayShell implementation begins.
    - `lib/kernel/services/playback_controller.dart` contains `SettingsPanelPlayback`, `void pause() => engine.pause();`, `void play() => engine.play();`, and an `isPlaying` getter derived from `MediaState.playing`.
    - The AppleCurves tracking commit contains only `lib/ui/shared/apple_curves.dart`.
  </acceptance_criteria>
  <verify><automated>git ls-files --error-unmatch lib/ui/shared/apple_curves.dart && flutter analyze lib/kernel/services/playback_controller.dart</automated></verify>
  <done>AppleCurves is committed as a standalone prerequisite and PlaybackController provides the D-03 pause/play/isPlaying service API plus a narrow fakeable contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build and test the three-notifier state and playback lifecycle</name>
  <read_first>
    - lib/kernel/services/playback_controller.dart
    - lib/ui/dialogs/settings_panel.dart
    - lib/ui/playlist/playlist_panel.dart
    - test/engine/fvp_engine_open_test.dart
    - .planning/phases/23-overlay-shell-state-model/23-CONTEXT.md
    - .planning/phases/23-overlay-shell-state-model/23-PATTERNS.md
    - .planning/phases/23-overlay-shell-state-model/23-VALIDATION.md
    - CLAUDE.md
  </read_first>
  <files>lib/ui/dialogs/settings/settings_panel_state.dart, lib/ui/dialogs/settings/settings_panel_controller.dart, test/ui/dialogs/settings_panel_state_test.dart, test/ui/dialogs/settings_panel_controller_test.dart</files>
  <behavior>
    - Test 1: SettingsPanelState begins with false, 0, and Offset.zero; disposing it disposes exactly its three notifiers.
    - Test 2: opening while fake playback is playing snapshots true, pauses once, and exposes isOpen=true.
    - Test 3: closing a session opened while playing resumes once and resets dragOffset to Offset.zero; a session opened while paused never invokes play().
    - Test 4: repeated open(), repeated close(), and toggle() retain idempotent lifecycle semantics and expected pause/play call counts.
  </behavior>
  <action>Write the two unit-test files first using Arrange-Act-Assert and a hand-written `FakePlaybackController implements SettingsPanelPlayback` in the controller test. Then create `SettingsPanelState` in `lib/ui/dialogs/settings/` with exactly `ValueNotifier<bool> isOpen`, `ValueNotifier<int> selectedTab`, and `ValueNotifier<Offset> dragOffset`; give it a documented dispose method that releases all three. Create `SettingsPanelController` with constructor injection of `SettingsPanelPlayback`, an owned `SettingsPanelState`, a private `_wasPlaying` snapshot, and documented synchronous open(), close(), toggle(), and dispose() methods. Per D-01/D-02/D-03/D-04, open snapshots before calling pause, close resumes only a true snapshot and resets dragOffset, and the controller owns no locale, theme, shortcut, tab-content, or MediaEngine state.</action>
  <acceptance_criteria>
    - `SettingsPanelState` declares exactly three `ValueNotifier` fields named isOpen, selectedTab, and dragOffset with the required initial values.
    - `SettingsPanelController` uses SettingsPanelPlayback and has open(), close(), toggle(), and dispose() methods with the tested pause/resume behavior.
    - `test/ui/dialogs/settings_panel_controller_test.dart` declares FakePlaybackController and contains tests for already-open, already-closed, toggle, playing-open, and paused-open cases.
    - `flutter test test/ui/dialogs/settings_panel_state_test.dart test/ui/dialogs/settings_panel_controller_test.dart -x` exits 0.
  </acceptance_criteria>
  <verify><automated>flutter test test/ui/dialogs/settings_panel_state_test.dart test/ui/dialogs/settings_panel_controller_test.dart -x</automated></verify>
  <done>PANEL-01 and PANEL-02 are covered by passing isolated tests that use no real MediaEngine and preserve the D-03 service boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings shell controller to playback service | UI intent crosses into playback state changes through the constrained facade. |
| test fake to controller lifecycle | Test-only input simulates playing and paused source states without native engine execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering | future SettingsOverlayShell hit testing | low | mitigate | Wave 1 will use IgnorePointer plus conditional rendering when isOpen is false and cover it in widget tests for PANEL-05. |
| T-23-02 | Tampering | future SettingsOverlayShell Focus handler | low | mitigate | Wave 1 will return KeyEventResult.handled for ESC and B, with a no-fullscreen-bubble widget test for PANEL-06. |
| T-23-03 | Tampering | future title-bar drag handler | low | mitigate | Wave 1 will clamp dragOffset against MediaQuery-derived panel bounds and assert boundary cases for PANEL-04/PANEL-07. |
| T-23-SC | Tampering | package installation | low | accept | No package-manager installation occurs; Phase 23 uses Flutter SDK and tracked in-tree code only. |
</threat_model>

<verification>
Run the two new isolated test files after Task 3, then run `flutter analyze lib/kernel/services/playback_controller.dart lib/ui/dialogs/settings/settings_panel_state.dart lib/ui/dialogs/settings/settings_panel_controller.dart`.
</verification>

<success_criteria>
- AppleCurves is a separately committed, tracked prerequisite.
- The settings lifecycle uses a narrow service facade and never requires a real MediaEngine in unit tests.
- State contains only the three D-04 notifier fields and all PANEL-02 lifecycle edge cases pass.
</success_criteria>

<output>
Create `.planning/phases/23-overlay-shell-state-model/23-01-SUMMARY.md` when done.
</output>
