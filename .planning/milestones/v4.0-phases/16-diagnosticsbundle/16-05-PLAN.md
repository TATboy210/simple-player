---
phase: 16-diagnosticsbundle
plan: 05
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - tool/audit/phase16_gates.sh
autonomous: true
requirements: [ADAPT-04, ADAPT-05]
must_haves:
  truths:
    - "D22 grep gate: `grep -r '_openGeneration' lib/kernel/adapter/` returns 0 hits — the P16 adapter has NO generation-counter field (D20); `grep -r 'openGeneration' lib/kernel/adapter/` returns only the class-level D21 doc-comment checklist lines"
    - "D27 wc gate: the 6 production files (1 adapter + 5 diagnostics) total < 636 lines (FvpEngine exact baseline)"
    - "Both gates are re-runnable shell checks that exit non-zero on violation (CI-automatable, LOG-01 precedent)"
  artifacts:
    - "tool/audit/phase16_gates.sh (executable, self-locating, no hardcoded counts)"
  key_links:
    - "Gate script mirrors the Phase 15 tool/audit/inventory.sh convention (set -euo pipefail, SCRIPT_DIR->REPO_ROOT)"
    - "Baseline 636 read live from wc -l fvp_engine.dart, not hardcoded (drift-proof)"
---

<objective>
Add the two static structural gates as a single re-runnable shell script under `tool/audit/`
(the Phase 15 precedent directory): the D22 grep gate (no double openGeneration data source in
the adapter) and the D27 size-budget gate (6 files < FvpEngine's 636-line baseline). Both are
CI-automatable shell checks following the LOG-01 grep-gate project convention.

Purpose: ADAPT-04 sc4 (无双数据源) and ADAPT-05 (尺寸预算受控) are hard requirements needing
repeatable, objective verification手段 (D22/D27) — not review. These are structural properties
verifiable statically, separate from the Dart unit tests.

Output: `tool/audit/phase16_gates.sh` — exits 0 when both gates pass, non-zero with a clear
message on violation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-diagnosticsbundle/16-CONTEXT.md
@.planning/phases/16-diagnosticsbundle/16-PATTERNS.md

# Convention to mirror (Phase 15 static audit script — self-locating, no hardcoded counts):
@tool/audit/inventory.sh

# Files the gates measure (produced by Plans 16-01 and 16-02):
@lib/kernel/adapter/kernel_adapter.dart
@lib/kernel/diagnostics/diagnostics_bundle.dart

# The size baseline source (read live, do NOT hardcode 636 as the ceiling):
@lib/kernel/engine/fvp_engine.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: D22 grep gate + D27 wc gate in tool/audit/phase16_gates.sh</name>
  <files>tool/audit/phase16_gates.sh</files>
  <action>
Create `tool/audit/phase16_gates.sh`, executable, following the `tool/audit/inventory.sh`
convention: `#!/usr/bin/env bash`, `set -euo pipefail`, self-locating via
`SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` then
`REPO_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"`. Do NOT hardcode any count or the 636 baseline —
read the baseline live (drift-proof, per inventory.sh design principle).

GATE 1 — D22 (no double openGeneration data source in the adapter). Per CONTEXT D22 (literal
text: `grep -r '_openGeneration' lib/kernel/adapter/` must return 0 hits; `grep -r
'openGeneration' lib/kernel/adapter/` returns only class-level D21 doc-comment checklist
matches). This enforces D20's ruling that the P16 adapter is transparent: NO counter field,
NO generation code, open() 100% forwards to legacy (the openGeneration guard lives entirely in
the old engine at fvp_engine.dart:259/267/311/320; counter migration into the adapter is a
P20 task per STATE-02, listed only as a forward-looking placeholder in the D21 class-level
checklist). Concretely, run TWO checks and assert both:
  (a) `grep -rn '_openGeneration' "$REPO_ROOT/lib/kernel/adapter/"` MUST return 0 hits. If any
      hit is found, the adapter has smuggled in a counter field/reference — a D20/D22
      violation. Print the offending lines and exit non-zero.
  (b) `grep -rn 'openGeneration' "$REPO_ROOT/lib/kernel/adapter/"` MUST return ONLY lines that
      are inside `///` class-level doc comments (the D21 migration-checklist lines). Assert
      every matched line lives in a `///` comment context and mentions 'openGeneration' (no
      leading underscore) as part of the P20 migration checklist. Any match outside a doc
      comment (e.g. a field, a method body, an import) is a violation — exit non-zero.
The `git diff --stat fvp_engine.dart unchanged` check is out of scope for this script (that is
a Plan 16-04/manual review); this gate is purely the adapter-directory grep. Print all matched
lines for auditability. This gate is the structural enforcement of ADAPT-04 sc4 ('无双数据源')
for P16: the P16 adapter has zero openGeneration data sources (counter migration is P20).

GATE 2 — D27 (size budget). Per PATTERNS.md §Static Grep/Size Gates and CONTEXT D27:
  - Read the baseline live: `baseline=$(wc -l < "$REPO_ROOT/lib/kernel/engine/fvp_engine.dart")`
    (currently 636 — do NOT hardcode; read it so it stays correct if fvp_engine changes).
  - Sum the 6 production files:
    `total=$(cat lib/kernel/adapter/*.dart lib/kernel/diagnostics/*.dart | wc -l)` — or sum
    per-file `wc -l` (含注释空行, D27). The 6 files = 1 adapter (D19 single file) + 5 diagnostics
    (D11). Print the per-file breakdown for the ADAPT-05 audit明细 (D26/D27 — planner budget: adapter
    ~200-250, diagnostics ~230, total ~400-480; ceiling 636).
  - Assert `total < baseline`. Exit non-zero with the total, baseline, and per-file breakdown
    printed on violation. If total exceeds the D26 20%-deviation threshold vs the ~480 estimate
    (i.e. approaches the 636 ceiling), print a WARNING recommending a senior-architect/red-team
    re-challenge (D26/D27 escalation) even if still under the ceiling.

Add a `--stdout` flag consistent with inventory.sh (print results without side effects). On
success print a one-line PASS summary for each gate. Add a bilingual header comment (DOC-01)
explaining both gates, referencing D20/D22/D27/ADAPT-04/ADAPT-05, and noting the LOG-01
grep-gate precedent this follows.
  </action>
  <verify>
    <automated>bash tool/audit/phase16_gates.sh</automated>
  </verify>
  <done>tool/audit/phase16_gates.sh is executable, self-locating, hardcodes nothing. GATE 1 (D22) passes — adapter source has 0 hits for the underscore-prefixed counter identifier; 'openGeneration' (no underscore) appears only in class-level D21 doc-comment checklist lines. GATE 2 (D27) passes — 6-file total < live-read 636 baseline, with per-file breakdown printed. Script exits 0 on the current tree; exits non-zero on either violation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Audit-tooling-only plan. The script reads source files and prints results; no production trust boundary, no runtime surface, no input/network/persistence. Actors unchanged (developer/CI running the script locally). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-06 | Tampering | Static gate reliability (drift/false-pass) | low | mitigate | Baseline (636) and counts are read LIVE from the source tree, never hardcoded (inventory.sh design principle) — prevents a stale-constant false pass that masks drift. GATE 1 reads adapter source for the underscore-prefixed counter identifier and asserts 0 hits — a doc-comment mention of 'openGeneration' (no underscore) in the D21 class-level checklist is allowed by D22 and does not trip GATE 1's underscore-prefixed check; GATE 1's secondary check confirms every 'openGeneration' match lives in a `///` doc comment. |
| T-16-05 | Denial of Service | Over-engineering scope creep (Constraint #8) | low | mitigate | GATE 2 is itself the #8 size-budget enforcement: if the 6 files approach the 636 ceiling, the script prints a WARNING recommending senior-architect/red-team re-challenge (D26) rather than silently allowing scope expansion. |

**threat_level: low** — no high-severity threats. Static audit tooling with no auth/input/
crypto/file-system-write/external-call surface (read-only + stdout). Gate emitted
unconditionally per Step 5.55.
</threat_model>

<verification>
Wave 3 checks:
- `bash tool/audit/phase16_gates.sh` exits 0 on the current tree (both gates pass).
- GATE 1 (D22): `grep -r '_openGeneration' lib/kernel/adapter/` returns 0 hits (D20 — P16 adapter has no counter); `grep -r 'openGeneration' lib/kernel/adapter/` returns only class-level D21 doc-comment checklist lines.
- GATE 2 (D27): sum of the 6 production files < live-read 636 baseline; per-file breakdown printed.
- Deliberately break each gate once (e.g. temporarily add a counter field to the adapter for GATE 1, or add lines to push the total over baseline for GATE 2) to confirm non-zero exit, then revert — do NOT commit the break.
</verification>

<success_criteria>
- tool/audit/phase16_gates.sh exists, executable, mirrors the Phase 15 inventory.sh convention (self-locating, no hardcoded counts) (ADAPT-04/ADAPT-05).
- D22 grep gate enforces ADAPT-04 sc4 ('无双数据源') at 0 hits for the underscore-prefixed counter identifier in the adapter source (D20: P16 adapter is transparent, no counter; counter migration is P20 per STATE-02, listed as a D21 class-level placeholder).
- D27 wc gate enforces 6-file total < live-read FvpEngine baseline (ADAPT-05, Success Criterion 5).
- Both gates CI-automatable, exit non-zero on violation (LOG-01 precedent).
- Size WARNING escalation to red-team on 20%-deviation approach (D26).
</success_criteria>

<red_team_challenge>
## Red-Team / Senior-Architect Scope Challenge (D26 — PLAN-phase, recorded here)

Per D26, the size-budget prediction is challenged at PLAN time (prevention, cheapest place to
catch scope creep). VERIFY phase does NOT re-run a full red-team — it compares implemented size
vs this budget and only re-escalates if deviation exceeds the D26 20% threshold.

**Predicted size budget (D27 breakdown, per-file LOC estimate — the明细 D26 requires):**

| File | Plan | Estimate (LOC, incl. comments/blanks) |
|------|------|----------------------------------------|
| lib/kernel/adapter/kernel_adapter.dart (KernelAdapter + DelegationPolicy + KernelMode, D19 single file) | 16-01 | ~200-250 |
| lib/kernel/diagnostics/kernel_logger.dart | 16-02 | ~55 |
| lib/kernel/diagnostics/memory_monitor_slot.dart | 16-02 | ~45 |
| lib/kernel/diagnostics/metrics_slot.dart | 16-02 | ~55 |
| lib/kernel/diagnostics/event_log_slot.dart | 16-02 | ~50 |
| lib/kernel/diagnostics/diagnostics_bundle.dart | 16-02 | ~55 |
| **Total (6 files)** | | **~460-510** |
| **Ceiling (FvpEngine baseline, D27)** | | **636** |
| **Headroom** | | **~126-176 LOC (~20-28%)** |

**Challenges raised and dispositions:**

1. **"Why 44 member overrides in one file — will the adapter blow the budget?"** — Each override
   is a one-line ternary (getter or `=>` method); ~44 lines of dispatch + struct/enum boilerplate
   + doc comments lands at ~200-250. The ternary shape (PATTERNS.md) is the minimum expressible
   forwarding. Disposition: within budget, no split needed. RESISTED the temptation to add per-
   capability `_routeXxx` helper methods (Claude's discretion allows either, but helpers would add
   ~14 LOC and indirection for zero benefit at all-legacy — KISS wins, #8).

2. **"Could the diagnostics slots be over-specified?"** — Minimal method sets are traced from
   existing concrete public APIs and capped at D11 (no speculative methods). RESEARCH Pitfall 3
   (dead-code bundle temptation) explicitly flags this; the plan (16-02) forbids methods beyond
   the minimal set. Disposition: minimal, no expansion.

3. **"Does the openGeneration counter justify its own field, given legacy already has one?"** —
   No (D20): the P16 adapter has NO counter field — open() is pure forward, the openGeneration guard
   lives in the old engine at fvp_engine.dart:259/267/311/320. Counter migration into the adapter is a
   P20 task (STATE-02) listed as a D21 class-level placeholder. ADAPT-04 "single arbiter + no dual
   source" is satisfied in P16 by KernelMode + the D22 0-hit grep gate, not by an adapter field.
   Disposition: minimal — adapter counter-free per D20.

4. **"Any risk the seam becomes a permanent god-adapter (Out of Scope violation)?"** — Structural
   guards: separate `lib/kernel/adapter/` directory marks transience (D19, collapsed in Phase 21);
   D17 forbids extra mutable state; this gate (GATE 2) enforces the ceiling every commit.
   Disposition: guarded, low risk.

**Conclusion:** Predicted total ~460-510 LOC is comfortably under the 636 ceiling (~20-28%
headroom). No scope-reduction or phase-split needed; no high-severity over-engineering risk. If
implementation exceeds ~575 LOC (20% over the ~480 midpoint estimate), GATE 2 prints the WARNING
and VERIFY re-escalates to a fresh red-team per D26.
</red_team_challenge>

<output>
Create `.planning/phases/16-diagnosticsbundle/16-05-SUMMARY.md` when done.
</output>
