---
phase: 16-diagnosticsbundle
plan: 03
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - lib/kernel/player_services.dart
autonomous: true
requirements: [ADAPT-01, ADAPT-02]
must_haves:
  truths:
    - "PlayerServices.init() constructs KernelAdapter in place of the raw FvpEngine (composition-root swap)"
    - "The SAME FvpEngine instance is passed to both legacy and migrated (D13/D19 — NewFvpEngine doesn't exist yet)"
    - "policy = const DelegationPolicy.all(KernelMode.legacy); bundle defaults to noop (D14/D10)"
    - "The `late final MediaEngine engine` field type is unchanged (drop-in transparency — 0 casts)"
  artifacts:
    - "lib/kernel/player_services.dart (minimal-diff edit at init())"
  key_links:
    - "engine field consumed unchanged by PlaybackController (:89) and VideoProcessingService (:96)"
    - "bundle.dispose() cascades via adapter.dispose() via existing engine.dispose() at :108"
---

<objective>
Wire the seam at the composition root: a minimal-diff edit to `PlayerServices.init()` that
replaces `engine = FvpEngine();` with a `KernelAdapter` constructed around the same FvpEngine
instance, `DelegationPolicy.all(KernelMode.legacy)`, and the default noop DiagnosticsBundle.

Purpose: This single edit is the ENTIRE application-code integration surface for Phase 16
(RESEARCH State of the Art — no other file constructs an engine). Because `KernelAdapter
implements MediaEngine`, the `late final MediaEngine engine` field type is unchanged and every
downstream consumer (PlaybackController, VideoProcessingService, UI ValueListenableBuilders)
is untouched — confirmed by the cast audit (0 `as FvpEngine`, 0 convenience-getter usages).

Output: `lib/kernel/player_services.dart` with the adapter wired at init(), same-instance
legacy/migrated, all-legacy policy, noop bundle default.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-diagnosticsbundle/16-CONTEXT.md
@.planning/phases/16-diagnosticsbundle/16-PATTERNS.md

# The file to edit (init() at line 87, `late final MediaEngine engine` at line 55):
@lib/kernel/player_services.dart

# Types being wired (produced by Plans 16-01 and 16-02):
@lib/kernel/adapter/kernel_adapter.dart
@lib/kernel/diagnostics/diagnostics_bundle.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap FvpEngine construction for KernelAdapter at init() (minimal-diff, same instance)</name>
  <files>lib/kernel/player_services.dart</files>
  <action>
Minimal-diff edit to `init()` (currently `player_services.dart:86-97`). Apply the target shape
from PATTERNS.md lines 336-355:

Replace `engine = FvpEngine();` with:
  final fvp = FvpEngine();
  engine = KernelAdapter(
    legacy: fvp,
    migrated: fvp,   // Phase 16: same instance — NewFvpEngine doesn't exist yet (D13/D19)
    policy: const DelegationPolicy.all(KernelMode.legacy),
    // bundle defaults to DiagnosticsBundle.noop() per the constructor default (D10/D12)
  );

Add the two imports at the top: `import 'adapter/kernel_adapter.dart';` (package-relative
consistent with the existing relative imports in this file) — KernelAdapter, DelegationPolicy,
and KernelMode all come from the single adapter file per D19. The bundle default is supplied
by KernelAdapter's constructor, so no diagnostics import is needed here unless the analyzer
requires the DiagnosticsBundle type to be visible for the const default; if so, add
`import 'diagnostics/diagnostics_bundle.dart';`.

Pass the SAME `fvp` instance to both `legacy` and `migrated` (D13/D19: zero extra resources —
no double mdk.Player/texture/ValueNotifier source; robustness bonus — if policy ever
misroutes to migrated it still hits the same old engine, safer than null; D19 permits either,
same-instance is RECOMMENDED per RESEARCH Open Question 2).

Do NOT change the `late final MediaEngine engine` field declaration at line 55 — its interface
type is satisfied by `KernelAdapter implements MediaEngine` with zero downstream diff (0
`as FvpEngine` casts, 0 convenience-getter usages in live code per the cast audit). Do NOT
change any other line of init() (playlist/controller/settings/videoProcessing wiring stays
identical — they consume `engine` by the MediaEngine interface). Do NOT change dispose() — the
existing `engine.dispose()` at line 108 now routes through the adapter, which cascades to
`bundle.dispose()` (D10, implemented in Plan 16-01).

Add a `//` comment at the swap explaining the Strangler Fig seam wiring (Phase 16: 100%
legacy; Phase 20 flips migrated to NewFvpEngine here). Match surrounding comment density
(DOC-01).
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/player_services.dart && grep -q 'DelegationPolicy.all(KernelMode.legacy)' lib/kernel/player_services.dart && grep -q 'late final MediaEngine engine' lib/kernel/player_services.dart</automated>
  </verify>
  <done>init() constructs KernelAdapter(legacy: fvp, migrated: fvp, policy: const DelegationPolicy.all(KernelMode.legacy)) with the bundle defaulting to noop; the same fvp instance is passed to both slots; `late final MediaEngine engine` field type unchanged; no other init()/dispose() line altered. `flutter analyze lib/kernel/player_services.dart` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The composition-root swap changes only which concrete `MediaEngine` is constructed. No new input surface, no new external call. The wrapped `FvpEngine` (already audited) still performs all real work; `KernelAdapter` routes 100% to it in Phase 16. Actors unchanged (in-process, single-user desktop app). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-04 | Denial of Service | PlayerServices.init() adapter construction | low | accept | Same-instance legacy/migrated (D13/D19) avoids duplicate native resource allocation (no second mdk.Player/texture). Construction is a thin wrapper around the existing FvpEngine construction — no new failure mode. Defaults suffice. |
| T-16-01 | Tampering | engine field consumer transparency | low | accept | `KernelAdapter implements MediaEngine` is a clean drop-in (0 `as FvpEngine` casts, 0 convenience-getter usages per cast audit). No consumer can observe or tamper with routing. Full regression suite (Plan 16-04) proves zero behavior change. |

**threat_level: low** — no high-severity threats, no blocking mitigations. A single-line
composition-root swap to a pure delegation seam. No new auth/input/crypto/file-system/
external-call surface. Gate emitted unconditionally per Step 5.55.
</threat_model>

<verification>
Wave 3 checks (this plan + Plan 16-04 + Plan 16-05 run in parallel — disjoint files):
- `flutter analyze lib/kernel/player_services.dart` clean.
- `git diff lib/kernel/player_services.dart` shows ONLY the init() swap + import lines (minimal-diff — dispose() and field decls unchanged).
- Full suite regression (`flutter test`) green after the swap — proves zero behavior change (ADAPT-01, Success Criterion 1). Run as part of Plan 16-04's full-suite gate.
- `git diff --stat lib/kernel/engine/fvp_engine.dart` EMPTY (D20).
</verification>

<success_criteria>
- PlayerServices.init() constructs KernelAdapter in place of raw FvpEngine at the composition root (ADAPT-01, Success Criterion 2).
- Same FvpEngine instance to legacy + migrated (D13/D19); policy = all-legacy (D14); bundle = noop default (D10, ADAPT-02).
- `late final MediaEngine engine` field type unchanged — drop-in transparency (0 casts).
- No other init()/dispose() line changed; downstream consumers untouched.
- Existing full test suite stays green (ADAPT-01 regression gate).
</success_criteria>

<output>
Create `.planning/phases/16-diagnosticsbundle/16-03-SUMMARY.md` when done.
</output>
