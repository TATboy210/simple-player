---
phase: 16-diagnosticsbundle
plan: 01
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - lib/kernel/adapter/kernel_adapter.dart
autonomous: true
requirements: [ADAPT-01, ADAPT-03, ADAPT-04, ADAPT-05]
must_haves:
  truths:
    - "KernelAdapter implements MediaEngine and routes 100% to legacy (D14 all-legacy policy)"
    - "Every EngineStateView/VolumeControl ValueNotifier getter returns the wrapped engine's own instance (identity forwarding, never rewrapped)"
    - "Adapter has NO generation-counter field and NO generation code; open() 100% forwards to legacy (D20); counter migration into adapter is a P20 task (STATE-02) listed only as a forward-looking placeholder in the D21 class-level checklist"
    - "Class-level doc comment lists exactly 3 P20 migration items (D21/D23): openGeneration migration, bundle activation, policy flip"
  artifacts:
    - "lib/kernel/adapter/kernel_adapter.dart (single file: KernelAdapter + DelegationPolicy + KernelMode per D19)"
  key_links:
    - "KernelAdapter constructor accepts legacy/migrated/policy/bundle=const DiagnosticsBundle.noop() (D12)"
    - "VolumeControl 4 shared members routed once via _policy.volume (RESEARCH Pitfall 2)"
    - "DelegationPolicy 7 final fields + .all() factory (D14/D15)"
---

<objective>
Build the Strangler Fig seam: a single-file `KernelAdapter implements MediaEngine` that
forwards every one of the ~44 members across the 7 ISP sub-interfaces to a wrapped `legacy`
engine, plus the `DelegationPolicy` struct (7 final `KernelMode` fields) and the `KernelMode`
enum — all three types in ONE file per D19 (authoritative over RESEARCH's 3-file split).

Purpose: Phase 16 delivers the seam + carrier skeleton only. The adapter is deliberate dead
routing (100% to legacy, `DelegationPolicy.all(KernelMode.legacy)`) until Phase 20 flips
capabilities. This plan owns the routing layer; the adapter carries NO openGeneration counter
in Phase 16 (D20 — guards live in the old engine; counter migration is a P20 task).

Output: `lib/kernel/adapter/kernel_adapter.dart` — implements MediaEngine, zero behavior
change, identity-preserving ValueNotifier forwarding (Blocking Constraint #6), pure-forward
open() with no counter (D20), class-level P20 migration checklist (D21/D23).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-diagnosticsbundle/16-CONTEXT.md
@.planning/phases/16-diagnosticsbundle/16-PATTERNS.md

# Interface surface to implement (7 ISP sub-interfaces — read all before coding):
@lib/kernel/engine/media_engine.dart
@lib/kernel/engine/engine_state_view.dart
@lib/kernel/engine/playback_control.dart
@lib/kernel/engine/track_control.dart
@lib/kernel/engine/subtitle_config.dart
@lib/kernel/engine/video_effect_control.dart
@lib/kernel/engine/renderer_control.dart
@lib/kernel/engine/volume_control.dart

# Class-level doc-comment shape + openGeneration guard reference (DO NOT edit this file — D20):
@lib/kernel/engine/fvp_engine.dart

# Bundle type consumed by the adapter constructor (produced by Plan 16-02, Wave 1):
@lib/kernel/diagnostics/diagnostics_bundle.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: DelegationPolicy struct + KernelMode enum (same file, top of kernel_adapter.dart)</name>
  <files>lib/kernel/adapter/kernel_adapter.dart</files>
  <action>
Create the new directory `lib/kernel/adapter/` and start `kernel_adapter.dart` with the
`KernelMode` enum and `DelegationPolicy` struct (D19: all three types live in this ONE file;
do NOT create kernel_mode.dart or delegation_policy.dart — CONTEXT D19 is authoritative over
RESEARCH's 3-file suggestion).

`enum KernelMode { legacy, migrated }` — the single arbiter enum (D14/ADAPT-04).

`final class DelegationPolicy` with 7 `final KernelMode` fields named exactly:
stateView, playback, track, subtitle, videoEffect, renderer, volume — one per MediaEngine
sub-interface (D14). All fields `final` (D15: immutability forces adapter recreation on
cutover, not unsafe in-place flips — this is the structural protection for Blocking
Constraint #6). The `stateView` field is a PLAIN `KernelMode` field with NO extra structural
constraint / factory pin (D18: D15's final+recreate already guarantees stateView can only be
flipped by rebuilding the adapter, so an additional pin would be redundant over-engineering
that also blocks Phase 20 partial cutover — do NOT add one). Provide the primary
`const DelegationPolicy({required this.stateView, ...})`
constructor AND the `const DelegationPolicy.all(KernelMode mode)` factory that sets all 7
fields to the same mode (D14). Copy the struct verbatim from PATTERNS.md lines 106-137.

Add `///` doc comments per project convention (DOC-01 bilingual): Chinese intent line +
English contract note. Note in the `.all()` doc that Phase 16 uses
`DelegationPolicy.all(KernelMode.legacy)` exclusively (D14).
  </action>
  <verify>
    <automated>test -f lib/kernel/adapter/kernel_adapter.dart && grep -q 'enum KernelMode { legacy, migrated }' lib/kernel/adapter/kernel_adapter.dart && grep -q 'const DelegationPolicy.all(KernelMode mode)' lib/kernel/adapter/kernel_adapter.dart</automated>
  </verify>
  <done>File exists with KernelMode enum (legacy, migrated), DelegationPolicy with 7 final KernelMode fields matching the 7 sub-interface names, and a const .all() factory. `flutter analyze lib/kernel/adapter/kernel_adapter.dart` reports no errors for this partial file (adapter class added in Task 2/3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KernelAdapter class shell — constructor, fields, class-level P20 migration checklist (D21/D23)</name>
  <files>lib/kernel/adapter/kernel_adapter.dart</files>
  <behavior>
    - KernelAdapter(legacy: fvp, migrated: fvp, policy: all-legacy) constructs without error and its type satisfies `MediaEngine` (assignable to `late final MediaEngine engine`).
    - bundle parameter defaults to const DiagnosticsBundle.noop() when omitted.
    - adapter.dispose() forwards to bundle.dispose() (cascading dispose, D10) — verified by the diagnostics/adapter dispose test in Plan 16-04.
  </behavior>
  <action>
Add `class KernelAdapter implements MediaEngine` to the same file. Constructor signature
EXACTLY per D12 (copy from PATTERNS.md lines 90-96):
`KernelAdapter({required MediaEngine legacy, required MediaEngine migrated, required DelegationPolicy policy, DiagnosticsBundle bundle = const DiagnosticsBundle.noop()})`.
Store as `final MediaEngine _legacy`, `final MediaEngine _migrated`, `final DelegationPolicy _policy`,
`final DiagnosticsBundle _bundle`. Import `package:simple_player_flutter/kernel/diagnostics/diagnostics_bundle.dart`
and the MediaEngine interface files as needed.

Per D17 the adapter has NO mutable state: no generation-counter field (D20 — the openGeneration
guard lives in the old engine at fvp_engine.dart:259/267/311/320; the P16 adapter is
transparent, and counter migration into the adapter is a P20 task listed in the D21
class-level checklist below), no `_isMigrating` bool, no caches, no intermediate flags.
engine/policy/bundle refs are injected dependencies (not mutable state).

Class-level `///` doc comment — mirror `fvp_engine.dart:25-41` structure (architecture
summary + composition list). Describe the Strangler Fig seam role (transient seam, not a
permanent layer; collapsed/deleted in Phase 21 per Out of Scope). Then a CLASS-LEVEL (NOT
per-method) P20 migration checklist listing EXACTLY these 3 items, one line each with the
REQ-ID it satisfies (D21/D23):
  (i)   openGeneration unified counter migrates from legacy into adapter/tracker (STATE-02, D23a)
  (ii)  DiagnosticsBundle activation: swap noop slots to real slots (D3 dead-code -> live, D23b)
  (iii) DelegationPolicy field flip: all-legacy -> per-capability migrated (D14/STATE-06, D23c)
Do NOT list D16 (kill-switch retention before adapter deletion — explicitly deferred to Phase 21
per ROADMAP Phase 21 SC4; P16 does not touch it) or D15 engine-rebuild here — those are
PlayerServices/assembly-point concerns, not adapter class-level (D23).

Do NOT place fenced code blocks or the literal migration-checklist token strings that a
negative grep might match — write the checklist as ordinary prose bullet lines in the doc
comment.

Add `@override void dispose()` that routes per policy to the active engine's dispose AND
cascades to `_bundle.dispose()` (D10: bundle.dispose() triggered by adapter.dispose()).
Follow Phase 15 D8: dispose forwards unconditionally (no internal condition branches).
  </action>
  <verify>
    <automated>grep -q 'class KernelAdapter implements MediaEngine' lib/kernel/adapter/kernel_adapter.dart && test "$(grep -c '_openGeneration' lib/kernel/adapter/kernel_adapter.dart)" -eq 0</automated>
  </verify>
  <done>KernelAdapter class present with D12 constructor (bundle defaults to noop), NO generation-counter field and NO generation code (D20 — open() 100% forwards, guards live in the old engine fvp_engine.dart:259/267/311/320), class-level doc comment containing exactly the 3-item P20 migration checklist with REQ-IDs, and a dispose() that cascades to bundle.dispose(). No mutable fields exist (D17). The adapter source has zero occurrences of the underscore-prefixed counter identifier (D22).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement all ~44 MediaEngine members — identity-preserving ternary dispatch (ADAPT-01/03), VolumeControl single-route (Pitfall 2)</name>
  <files>lib/kernel/adapter/kernel_adapter.dart</files>
  <behavior>
    - Every EngineStateView ValueNotifier getter returns `same()` object as the wrapped legacy notifier (verified by Plan 16-04 identity test — 13 notifiers).
    - Contract tests (Plan 16-04, factory swap) pass for all 7 ISP groups mounted against KernelAdapter.
    - setVolume/setMute/volume/isMuted implemented exactly once; a second policy branch would be unreachable dead code.
  </behavior>
  <action>
Implement every abstract member across the 7 sub-interfaces using the ternary-dispatch shape
from PATTERNS.md lines 59-69. For each member: read the relevant `DelegationPolicy` field,
then `_policy.X == KernelMode.legacy ? _legacy.member : _migrated.member`.

ValueNotifier getters (13 EngineStateView notifiers) — identity-preserving, return the
notifier OBJECT itself, NEVER `ValueNotifier(x.value)` (Blocking Constraint #6; this is the
exact failure D25's same() test catches). Route via `_policy.stateView` EXCEPT the 4 shared
volume members below.

Field->policy mapping:
  - EngineStateView (via _policy.stateView): textureId, state, position, duration,
    isBuffering, isSeeking, subtitleText, buffered, aspectRatio, lastError, playbackSpeed
    (11 notifiers), plus `mediaInfo` getter and `dispose()` (dispose already added in Task 2).
  - PlaybackControl (via _policy.playback): open, play, pause, stop, togglePlayPause, seekTo,
    setPlaybackRate, setRange, skipForward, skipBack (10 methods — NOT setVolume/setMute, see
    VolumeControl below).
  - TrackControl (via _policy.track): getAudioTracks, switchAudioTrack, activeAudioTracks.
  - SubtitleConfig (via _policy.subtitle): getSubtitleTracks, switchSubtitleTrack,
    toggleSubtitle, setExternalSubtitle, setSubtitleDelay, setEqualizer, subtitleDelay,
    activeSubtitleTracks.
  - VideoEffectControl (via _policy.videoEffect): setVideoEffect, rotate, setAspectRatio,
    setDeinterlace.
  - RendererControl (via _policy.renderer): setD3d11SyncEnabled, setHardwareDecoding.
  - VolumeControl (via _policy.volume): setVolume, setMute, volume, isMuted — implement these
    4 EXACTLY ONCE, dispatched via `_policy.volume`. Add an inline `//` comment at these
    members explaining WHY there is no second branch: PlaybackControl.setVolume/setMute and
    EngineStateView.volume/isMuted have identical signatures, so Dart interface composition
    means one override satisfies all three parent interfaces simultaneously; a branch keyed on
    _policy.playback or _policy.stateView would be unreachable dead code (RESEARCH Pitfall 2 /
    Assumption A2 — VolumeControl is the more specific/dedicated interface, so it wins).

`open()` (ADAPT-04 seam, D20 transparency): PURE forwarding — no increment, no guard, no
counter. The openGeneration guard lives entirely in the old engine at
fvp_engine.dart:259/267/311/320; the P16 adapter is transparent. The counter's migration
into the adapter is a P20 task (STATE-02) listed in the D21 class-level checklist. Implement
open() as a single ternary line forwarding via _policy.playback to _legacy.open(path) or
_migrated.open(path) — same shape as every other PlaybackControl method. Add a `///` doc
comment noting that openGeneration guards live in the old engine, the P16 adapter is
transparent with no counter (D20), and counter migration is a P20 task (per the class-level
D21 checklist). Use the term 'openGeneration' (no leading underscore) in doc comments; the
adapter source must have ZERO occurrences of the underscore-prefixed counter identifier
(D22). Do NOT mention legacy's internal generation mechanism beyond this transparent contract
note (D21 — migration detail lives in the class-level checklist only).

Add `///` bilingual doc comments per DOC-01 on non-trivial overrides; match surrounding
comment density. Keep total file size well under budget (D27: 6-file total < 636; this file
targets ~200-250 lines).
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/adapter/kernel_adapter.dart</automated>
  </verify>
  <done>`flutter analyze lib/kernel/adapter/kernel_adapter.dart` is clean (no missing-override errors — all 7 ISP interfaces fully implemented). setVolume/setMute/volume/isMuted appear exactly once each, routed via _policy.volume, with an inline comment explaining the single-route rationale. open() is a pure ternary forward with no counter/guard (D20). All ValueNotifier getters return the wrapped instance directly (no ValueNotifier( ) rewrap anywhere). The file has zero occurrences of the underscore-prefixed counter identifier (D22).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 16 introduces no new trust boundary. `KernelAdapter` forwards already-validated types (e.g. `String path` to `open()`) to the existing, already-audited `FvpEngine`. Input validation, if any, remains `FvpEngine`/`PathValidator`'s responsibility (unchanged). All code is in-process, single-user desktop app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | KernelAdapter routing (in-process delegation) | low | accept | No new external input surface; adapter is a pure pass-through to the already-audited FvpEngine. Existing FvpEngine input handling (PathValidator) unchanged. Defaults suffice. |
| T-16-02 | Information Disclosure | DiagnosticsBundle noop slots | low | accept | All 4 slots are no-ops in Phase 16 (D2/D3) — they emit/store nothing. Real logging (with path redaction) is Phase 17's job behind the KernelLogger interface (D7). No leakage possible from noop bodies. |

**threat_level: low** — no high-severity threats, no blocking mitigations. This is a pure
delegation seam with noop diagnostics: no new auth/input/crypto/file-system/external-call
surface. Gate emitted unconditionally per Step 5.55 even though risk is negligible.
</threat_model>

<verification>
Wave 2 checks (after this plan, before dependents run):
- `flutter analyze lib/kernel/adapter/` clean (all 7 ISP interfaces implemented, no missing overrides).
- `grep -r '_openGeneration' lib/kernel/adapter/` returns 0 hits — adapter has no counter field (D20/D22; full gate enforced in Plan 16-05).
- `grep -r 'openGeneration' lib/kernel/adapter/` returns only the class-level D21 doc-comment checklist lines (D22 allows the concept name in class-level doc comments).
- No `ValueNotifier(` re-wrap constructor call appears in any getter body (identity forwarding, #6 — full same() gate in Plan 16-04).
- `git diff --stat lib/kernel/engine/fvp_engine.dart` is EMPTY (D20 — fvp_engine untouched).
</verification>

<success_criteria>
- KernelAdapter implements MediaEngine, routes 100% to legacy under DelegationPolicy.all(KernelMode.legacy) (ADAPT-01).
- All 13 EngineStateView + VolumeControl notifiers forwarded by identity, never rewrapped (ADAPT-03, #6).
- Single KernelMode arbiter via DelegationPolicy.all(KernelMode.legacy) (ADAPT-04 'single arbiter'); adapter has NO generation-counter field, open() 100% forwards (D20); 'no dual source' verified by D22 grep gate at 0 hits (16-05 Gate 1); counter migration into adapter is a P20 forward-looking placeholder listed in the D21 class-level checklist.
- DelegationPolicy 7 final fields + .all() factory (D14/D15); adapter has no mutable state (D17/ADAPT-05).
- Class-level 3-item P20 migration checklist present (D21/D23); D16 kill-switch deferred to Phase 21 SC4.
- Single file per D19; targets ~200-250 lines (ADAPT-05 budget headroom, D27).
</success_criteria>

<output>
Create `.planning/phases/16-diagnosticsbundle/16-01-SUMMARY.md` when done.
</output>
