---
phase: 19-memorymonitor
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - lib/main.dart
  - lib/kernel/utils/debug_exporter.dart
  - lib/kernel/player_services.dart
  - lib/kernel/diagnostics/memory_monitor.dart
  - lib/kernel/diagnostics/diagnostics_bundle.dart
  - lib/kernel/utils/memory_monitor.dart
  - test/diagnostics/diagnostics_bundle_test.dart
  - test/kernel/utils/memory_monitor_test.dart
autonomous: true
requirements:
  - MEM-04
  - MEM-05
must_haves:
  truths:
    - Static singleton MemoryMonitor in lib/kernel/utils/ is deleted
    - All call sites use the new instance-based MemoryMonitor from lib/kernel/diagnostics/
    - Migration completes in ONE atomic commit (no cross-commit split, R2-5 lesson honored)
    - DiagnosticsBundle.memoryMonitor is a real MemoryMonitor instance (not NullMemoryMonitorSlot)
    - MemoryMonitor uses KernelLogger for all logging (no debugPrint in the new implementation)
    - DebugExporter accesses MemoryMonitor via static MemoryMonitor.I accessor (initialized in PlayerServices.init())
    - Old test file deleted or migrated, new diagnostics_bundle_test passes with real MemoryMonitor
  artifacts:
    - lib/kernel/diagnostics/memory_monitor.dart (with static I accessor added)
    - lib/kernel/player_services.dart (wires MemoryMonitor into DiagnosticsBundle)
    - lib/main.dart (removes static MemoryMonitor.start() call)
    - lib/kernel/utils/debug_exporter.dart (uses MemoryMonitor.I.snapshot())
  key_links:
    - PlayerServices.init() creates MemoryMonitor instance and sets static I accessor
    - DiagnosticsBundle receives real MemoryMonitor (replaces NullMemoryMonitorSlot)
    - DebugExporter uses MemoryMonitor.I.snapshot() (no import of old utils/memory_monitor.dart)
    - Old lib/kernel/utils/memory_monitor.dart is deleted (data classes now in diagnostics/memory_snapshot.dart)
---

<objective>
Atomic singleton-to-instance migration: remove the static MemoryMonitor from lib/kernel/utils/, wire the new instance-based MemoryMonitor into DiagnosticsBundle via PlayerServices, update all 2 call sites (main.dart, debug_exporter.dart), and delete the old file. This is ONE atomic commit per MEM-04 and R2-5 lesson.

Purpose: Complete the MemoryMonitor migration so all code uses the injectable, testable, bundle-integrated instance.
Output: Clean migration — old file deleted, all tests pass, DiagnosticsBundle has real MemoryMonitor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-memorymonitor/19-CONTEXT.md
@.planning/phases/19-memorymonitor/19-RESEARCH.md
@.planning/phases/19-memorymonitor/19-01-SUMMARY.md
@lib/kernel/diagnostics/memory_monitor.dart
@lib/kernel/diagnostics/memory_monitor_slot.dart
@lib/kernel/diagnostics/diagnostics_bundle.dart
@lib/kernel/player_services.dart
@lib/main.dart
@lib/kernel/utils/debug_exporter.dart
@lib/kernel/utils/memory_monitor.dart
@lib/ui/player/keyboard_handler.dart
@test/diagnostics/diagnostics_bundle_test.dart
@test/kernel/utils/memory_monitor_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add static I accessor to MemoryMonitor + wire into PlayerServices</name>
  <files>
    lib/kernel/diagnostics/memory_monitor.dart,
    lib/kernel/player_services.dart
  </files>
  <read_first>
    lib/kernel/diagnostics/memory_monitor.dart,
    lib/kernel/diagnostics/kernel_logger.dart,
    lib/kernel/player_services.dart,
    lib/kernel/diagnostics/diagnostics_bundle.dart
  </read_first>
  <action>
    ⚠️ ATOMICITY CONSTRAINT: This task and Task 2 MUST be staged into a single git commit. Do NOT commit Task 1 independently — doing so would create a transient state where both the old static singleton Timer and the new instance Timer run simultaneously.

    Add a static `I` accessor to MemoryMonitor (following KernelLoggerImpl pattern from Phase 17):

    In `lib/kernel/diagnostics/memory_monitor.dart`:
    - Add static field: `static MemoryMonitor? _instance;`
    - Add static getter: `static MemoryMonitor get I { final inst = _instance; if (inst == null) throw StateError('MemoryMonitor.I accessed before PlayerServices.init().'); return inst; }`
    - Add static init method: `static void init(MemoryMonitor monitor) { _instance = monitor; }`
    - Add static resetForTesting: `@visibleForTesting static void resetForTesting() { _instance = null; }`
    - These additions go at the top of the class body, before the constructor
    - Import `package:flutter/foundation.dart` if not already imported (for @visibleForTesting)

    In `lib/kernel/player_services.dart`:
    - Add imports: `import 'diagnostics/memory_monitor.dart';` and `import 'diagnostics/rss_provider.dart';` and `import 'diagnostics/clock.dart';`
    - In `init()`, after `KernelLoggerImpl.init();` and before the DiagnosticsBundle construction:
      - Create: `final memoryMonitor = MemoryMonitor(rssProvider: const ProcessInfoRssProvider(), clock: const SystemClock(), logger: KernelLoggerImpl.I);`
      - Initialize static accessor: `MemoryMonitor.init(memoryMonitor);`
      - Change bundle line: `memoryMonitor: const NullMemoryMonitorSlot()` → `memoryMonitor: memoryMonitor`
    - Remove the import of `diagnostics/memory_monitor_slot.dart` if NullMemoryMonitorSlot is no longer used (check: metrics and eventLog still use their Null* slots, so keep those imports)
    - Keep the `NullMetricsSlot` and `NullEventLogSlot` imports as-is (they're still used)
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter analyze lib/kernel/diagnostics/memory_monitor.dart lib/kernel/player_services.dart</automated>
  </verify>
  <done>
    - MemoryMonitor has static I accessor + init() + resetForTesting()
    - PlayerServices.init() creates MemoryMonitor instance and calls MemoryMonitor.init()
    - DiagnosticsBundle receives real MemoryMonitor (not NullMemoryMonitorSlot)
    - flutter analyze passes on both files
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate call sites + delete old singleton (atomic commit)</name>
  <files>
    lib/main.dart,
    lib/kernel/utils/debug_exporter.dart,
    lib/kernel/utils/memory_monitor.dart,
    test/diagnostics/diagnostics_bundle_test.dart,
    test/kernel/utils/memory_monitor_test.dart
  </files>
  <read_first>
    lib/main.dart,
    lib/kernel/utils/debug_exporter.dart,
    lib/kernel/utils/memory_monitor.dart,
    lib/kernel/diagnostics/memory_monitor.dart,
    lib/kernel/diagnostics/memory_snapshot.dart,
    test/diagnostics/diagnostics_bundle_test.dart,
    test/kernel/utils/memory_monitor_test.dart,
    lib/ui/player/keyboard_handler.dart
  </read_first>
  <action>
    Perform all changes in a SINGLE atomic commit per MEM-04 (R2-5 lesson: never split singleton removal across commits):

    **main.dart (call site 1):**
    - Remove import: `import 'kernel/utils/memory_monitor.dart';`
    - Remove line: `MemoryMonitor.start();` (line 22 — PlayerServices.init() now handles this via DiagnosticsBundle)
    - No replacement needed — the bundle auto-starts MemoryMonitor in its constructor (D5)

    **debug_exporter.dart (call site 2):**
    - Change import from `import 'memory_monitor.dart';` to `import '../diagnostics/memory_monitor.dart';`
    - Change `_memorySnapshot()` method: `final snap = MemoryMonitor.snapshot();` → `final snap = MemoryMonitor.I.snapshot();` (uses static I accessor set in PlayerServices.init())
    - Design rationale: Uses static `I` accessor (not parameter-passing) for consistency with Phase 17 `KernelLoggerImpl.I` pattern. Threading an instance through keyboard_handler → DebugExporter callback chain adds unnecessary coupling; the static accessor is set once in PlayerServices.init() and is safe for desktop single-window use.

    **Delete old file:**
    - Delete `lib/kernel/utils/memory_monitor.dart` entirely (data classes now in `lib/kernel/diagnostics/memory_snapshot.dart`, implementation in `lib/kernel/diagnostics/memory_monitor.dart`)

    **Update test files:**
    - `test/diagnostics/diagnostics_bundle_test.dart`: The bundle now has a real MemoryMonitor. The test `bundle.memoryMonitor.snapshot()` may return non-null after timer fires. Update the test to handle this — either use DiagnosticsBundle.noop() for the null-snapshot test, or accept that the real MemoryMonitor auto-starts. The key assertion is that `bundle.memoryMonitor` is non-null and `start()`/`dispose()` work.
    - `test/kernel/utils/memory_monitor_test.dart`: Delete this file entirely — the data class tests have been ported to `test/diagnostics/memory_snapshot_test.dart` in Plan 01.

    **Verify no remaining references to old file:**
    - Run `grep -r 'utils/memory_monitor' lib/ test/` — must return 0 results
    - Run `grep -r 'MemoryMonitor\._' lib/` — must return 0 results (no private constructor access)

    **Single commit message:** `refactor(19): atomic singleton→instance MemoryMonitor migration — MEM-04`
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && flutter test test/diagnostics/ && flutter analyze</automated>
  </verify>
  <done>
    - lib/kernel/utils/memory_monitor.dart is deleted
    - main.dart has no MemoryMonitor import or static call
    - debug_exporter.dart uses MemoryMonitor.I.snapshot()
    - Zero references to utils/memory_monitor in lib/ and test/
    - Zero MemoryMonitor._ private access in lib/
    - diagnostics_bundle_test passes with real MemoryMonitor
    - All diagnostics tests pass
    - flutter analyze clean
    - All changes in ONE atomic commit
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel→dart:io | RssProvider wraps ProcessInfo.currentRss (desktop-only, trusted SDK) |
| static→instance | MemoryMonitor.I static accessor bridges old call sites during migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-02 | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed — all SDK dependencies only |
| T-19-03 | Repudiation | Atomic commit integrity | medium | mitigate | Single commit contains all migration changes; grep gates verify no partial state |
</threat_model>

<verification>
After Plan 02 completes (full Phase 19 verification):
1. `flutter test` — full suite green
2. `flutter analyze` — zero issues
3. `grep -r 'utils/memory_monitor' lib/ test/` — 0 results
4. `grep -r 'MemoryMonitor\._' lib/` — 0 results (no static singleton remnants)
5. `grep -r 'debugPrint.*MemoryMonitor' lib/kernel/` — 0 results (KernelLogger replaces debugPrint)
6. `flutter test test/diagnostics/diagnostics_bundle_test.dart` — passes with real MemoryMonitor in bundle
</verification>

<success_criteria>
- Old static singleton deleted, all call sites migrated in one atomic commit
- DiagnosticsBundle.memoryMonitor is a real MemoryMonitor instance
- MemoryMonitor uses KernelLogger (not debugPrint) for all logging
- Full test suite passes, flutter analyze clean
- Zero references to old utils/memory_monitor.dart in codebase
</success_criteria>

<output>
Create `.planning/phases/19-memorymonitor/19-02-SUMMARY.md` when done
</output>
