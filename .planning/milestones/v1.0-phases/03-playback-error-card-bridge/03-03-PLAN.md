---
phase: 03-playback-error-card-bridge
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - lib/ui/player/error_card.dart
  - lib/ui/player/error_card_host.dart
  - test/widget/player/error_card_test.dart
  - test/widget/player/error_card_host_test.dart
autonomous: true
requirements:
  - CARD-04
  - CARD-03
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CARD-04/D-06：一键复制产出的文本与 formatDiagnosticPack（即日志文件格式）一致；复制成功/失败各出一条 OsdOverlay pill 反馈；复制异常不抛出、不影响卡片与其余反馈。"
    - "D-02：warning 头部报告走 OsdOverlay 短暂提示并恰好推进一次队列，不上卡片、不形成重建循环；error/fatal 上常驻卡片。"
    - "D-01/D-11：折叠区计数徽标可点击，在宿主本地有界错误快照内轮览历史错误；轮览绝不调用 dismissCurrent；kernel 零新增 API。"
    - "新错误到达时替换当前卡片内容（替换 + 徽标，不堆叠多条卡片）。"
  artifacts:
    - path: lib/ui/player/error_card.dart
      provides: "复制按钮 + _copyDiagnosticPack（typed catch + OSD 反馈）；徽标轮览入口。"
    - path: lib/ui/player/error_card_host.dart
      provides: "severity 路由（warning→OSD+单次 dismiss）、有界本地快照、轮览状态机。"
    - path: test/widget/player/error_card_test.dart
      provides: "copy 成功/失败注入测试（mock flutter/platform channel）。"
    - path: test/widget/player/error_card_host_test.dart
      provides: "warning 路由与徽标轮览测试。"
  key_links:
    - from: ErrorCard 复制按钮
      to: formatDiagnosticPack(report, logPath)
      via: "LOG-05 单一来源：卡内复制 == 日志文件格式"
      pattern: "formatDiagnosticPack("
    - from: ErrorCardHost._apply (severity==warning)
      to: OsdService.I.show + 单次 dismissCurrent
      via: "D-02 呈现层过滤，无重建循环"
      pattern: "OsdService.I.show"
    - from: 计数徽标点击
      to: 宿主本地快照轮览索引
      via: "D-11 本地有界快照（≤20），不触碰 kernel API"
      pattern: "_history.add(report)"
---

<objective>
**用户故事：作为开发者用户，我希望在卡片上一键复制与日志文件完全一致的诊断包发给别的会话排查，并在多个错误排队时通过计数徽标逐个回看，以便不打开日志文件也能完整回溯每一次出错。**

在 03-02 的完整卡片上补齐两个反馈切片：其一，CARD-04/D-06 的一键复制诊断包与失败隔离；其二，D-01/D-02/D-11 的严重级路由与徽标轮览。

Purpose: 卡片补齐「复制即证据」闭环与多错误回看能力，并让 warning 分流不再依赖未实现的分支；至此除 MIG-01 迁移外的全部卡片行为到位。
Output: 复制通路、warning OSD 分流、徽标轮览及其测试。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playback-error-card-bridge/03-CONTEXT.md
@.planning/phases/03-playback-error-card-bridge/03-RESEARCH.md
@.planning/phases/03-playback-error-card-bridge/03-PATTERNS.md
@.planning/phases/03-playback-error-card-bridge/03-VALIDATION.md
@lib/ui/player/error_card.dart
@lib/ui/player/error_card_host.dart
@lib/ui/shared/osd_overlay.dart
@lib/kernel/diagnostics/diagnostic_pack_formatter.dart
@lib/kernel/diagnostics/error_reporter.dart
@test/diagnostics/player_error_report_bridge_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 一键复制诊断包与失败隔离（CARD-04/D-06）</name>
  <files>lib/ui/player/error_card.dart, test/widget/player/error_card_test.dart</files>
  <read_first>
    - lib/ui/player/error_card.dart（03-02 卡片本体；复制按钮挂接位置）
    - lib/kernel/diagnostics/diagnostic_pack_formatter.dart:13（formatDiagnosticPack(ErrorReport, {String? logPath}) 签名与段格式——LOG-05 单一来源，卡片内禁止自拼字符串）
    - lib/kernel/diagnostics/error_reporter.dart:133-135（diagnosticLogPath ValueListenable，复制时取 .value）
    - lib/ui/shared/osd_overlay.dart:38-49（OsdService.I.show(text, icon:) 契约与自动隐藏；D-06 两态文案 key）
    - lib/ui/dialogs/media_info_dialog.dart:190-200（反面教材：Clipboard.setData 未 await 未 catch——卡片必须优于它）
    - test/widget/player/error_banner_test.dart（harness 形态）
    - .planning/phases/03-playback-error-card-bridge/03-RESEARCH.md（Pattern 4 完整复制处理、headless 注意：flutter/platform channel mock）
    - .planning/phases/03-playback-error-card-bridge/03-PATTERNS.md（Copy with failure isolation 模式）
    - .planning/phases/03-playback-error-card-bridge/03-VALIDATION.md（03-03-01 验证行与 mock channel 缝）
  </read_first>
  <behavior>
    - RED：mock `flutter/platform` 的 Clipboard.setData handler 后点击复制按钮，handler 收到的文本与 `formatDiagnosticPack(同一 report, logPath: 同一值)` 输出逐字符相等；OSD 显示「已复制」。
    - RED：handler 注入 PlatformException 后，OSD 显示「复制失败」，卡片仍可见且内容不变，测试无未捕获异步异常。
    - RED：handler 未注册（MissingPluginException 天然路径）时同样「复制失败」且卡片无恙。
    - RED：diagnosticLogPath 为 null 时复制包 logPath 段与 formatter 对 null 的既有降级输出一致。
    - GREEN：实现后全部转绿。
  </behavior>
  <action>先写失败测试：用 `tester.binding.defaultBinaryMessenger.setMockMethodCallHandler` 在 `SystemChannels.platform`（Clipboard channel 名）上注册 mock——这同时是成功捕获与失败注入的缝；测试结束 removeHandler。实现侧：error_card.dart 增加 `Future<void> _copyDiagnosticPack(ErrorReport report)`——格式化一律调用 `formatDiagnosticPack(report, logPath: ErrorReporterImpl.I.diagnosticLogPath?.value)`（复制时刻取值，不缓存）；`await Clipboard.setData(ClipboardData(text: pack))`；成功路径 `OsdService.I.show(l10n.errorCardCopied, icon: Icons.check)`（l10n key 已于 03-01 Task 1 预置——见 03-VALIDATION.md Wave 0 第 4 项——直接引用 `AppLocalizations` 既有 key，禁止新增重复 key；errorCardCopyFailed、errorCardCopyTooltip 同理）；typed catch 两个：`on PlatformException catch (e, st)` 与 `on MissingPluginException catch (e)`，两态都 `OsdService.I.show(l10n.errorCardCopyFailed, icon: Icons.error_outline)`，PlatformException 分支加 `KernelLogger.I.w(...)`（UI 层可 debugPrint，但结构化日志优先），不捕获任何 `Error` 子类型；async 异常绝不外溢到调用方。复制按钮为 GestureDetector（canRequestFocus:false 惯例延续），带 errorCardCopyTooltip 语义；整卡 GestureDetector 与复制按钮的命中用行为层级区分（按钮在内层优先命中，GestureDetector 命中测试天然内层优先，无需 IgnorePointer）。复制期间不改变折叠/展开状态，不禁用卡片其余交互。D-09 边界：不加 reopen/retry 任何动作按钮。</action>
  <verify>
    <automated>flutter test test/widget/player/error_card_test.dart --plain-name copy</automated>
    <fails_when>命令非零退出，或复制文本与 formatter 输出不等、成功/失败两态 OSD 反馈缺失、注入异常后卡片内容改变、出现未捕获异步异常任一情形出现。</fails_when>
  </verify>
  <acceptance_criteria>
    - 复制文本逐字符等于 formatDiagnosticPack 输出（无卡内自拼格式，LOG-05 单一来源保持）。
    - PlatformException 与 MissingPluginException 两路径都有 OSD 反馈且无异常外溢；卡片可见性与内容不受复制结果影响。
    - logPath 取自 diagnosticLogPath.value（复制时刻），null 降级与 formatter 既有行为一致。
    - 无新增动作按钮（D-09）；测试通过 mock channel 驱动而非真实剪贴板。
    - focused 命令退出 0。
  </acceptance_criteria>
  <done>卡片一键复制与日志文件同源同格式，成功与异常两态各有 OSD 反馈且互不牵连；CARD-04 全部可自动化断言落地。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 严重级路由与计数徽标轮览（D-01/D-02/D-11）</name>
  <files>lib/ui/player/error_card.dart, lib/ui/player/error_card_host.dart, test/widget/player/error_card_host_test.dart</files>
  <read_first>
    - lib/ui/player/error_card_host.dart（03-01/02 宿主；_apply 与适配 notifier 是本任务改造点）
    - lib/ui/shared/osd_overlay.dart:25-56（OsdService 契约；warning 提示与复制反馈共用此组件）
    - lib/kernel/diagnostics/error_report.dart（ErrorSeverity 三值、occurrenceCount、eventId——快照身份来源）
    - lib/kernel/diagnostics/error_reporter.dart:47-56（_maxQueueLength=5/_dedupeWindow=10s——快照上界取值参考）、:229-245（flush/dismiss 语义）、:169-170（queuedReports 为 @visibleForTesting——D-11 明确不用它）
    - .planning/phases/03-playback-error-card-bridge/03-CONTEXT.md（D-01 替换+徽标、D-02 分层、D-11 本地快照）
    - .planning/phases/03-playback-error-card-bridge/03-RESEARCH.md（Severity routing 代码示例、Anti-Patterns 的 warning 循环与 dismiss 误用两条、Open Question 3 快照语义）
    - .planning/phases/03-playback-error-card-bridge/03-PATTERNS.md（Badge local-snapshot cycling 无先例条目与 fallback 顺序）
    - .planning/phases/03-playback-error-card-bridge/03-VALIDATION.md（03-03-02 验证行：--plain-name warning）
  </read_first>
  <behavior>
    - RED：合成 warning 报告成为队首并 flush 后，OsdService 收到提示、卡片不显示该 warning、presentation 队列恰好推进一次（下一 error 可见）；重复触发不产生二次 dismiss/提示（无重建循环）。
    - RED：连续接纳 3 份不同 error 报告，卡片显示最新（替换语义），徽标显示「3 错误」；新报告到达重置轮览索引到队首。
    - RED：点击徽标沿快照向旧轮览（循环），卡片渲染对应历史报告（eventId 可辨认）；轮览过程中 dismissCurrent 零调用；轮览到队首后再继续回到最旧（循环语义，discretion）。
    - RED：快照达上界（命名常量，参考队列 5/去重 10s 取 20）后最旧被挤出，徽标计数封顶于上界。
    - RED：轮览状态下手动关闭 → dismissCurrent 推进真实队首并重置轮览到新队首。
    - GREEN：实现后全部转绿。
  </behavior>
  <action>先写失败测试（warning 合成报告经 ErrorReporterImpl.forTesting 直接纳纳，不构造引擎）。实现侧：第一，宿主 `_apply` 补齐 severity 路由——`report == null` 时适配值置空（卡片隐藏）；`severity == ErrorSeverity.warning` 时 `OsdService.I.show(report.message, icon: Icons.warning_amber_outlined)` 并恰好一次 `dismissCurrent()`（用本地状态防同帧重复提示，杜绝 rebuild 循环——research Anti-Pattern 明令）；error/fatal 时把 report 追加进宿主私有有界快照列表（`List<ErrorReport>`，上界命名常量 `_maxSnapshotLength = 20`，超出挤掉最旧；以 eventId 为身份，occurrenceCount 合并不改 eventId）再更新适配 notifier——零 kernel 改动（D-11；research Open Question 3 的 fallback 顺序：仅当快照语义被测试证伪才考虑 reporter 只读 API，且需先回 planner）。第二，卡片折叠区徽标改为可点击（GestureDetector）：显示 errorCardBadgeLabel（快照长度），点击循环移动宿主轮览索引（新到达重置到队首；渲染快照[i] 而非 presentation.current——适配值保持 reporter 语义，轮览是纯视图偏移，轮览时渲染函数优先取快照索引）；徽标与轮览 l10n key（errorCardBadgeLabel、errorCardCycleTooltip）已于 03-01 预置，直接引用 `AppLocalizations` 既有 key，禁止新增重复 key；轮览态在 doc comment 注明「dismissCurrent 仅手动关闭调用」（research Anti-Pattern：误用会永久丢队首）。第三，轮览状态全在宿主 State 内，无新 notifier、无新状态库；徽标 tooltip 用 errorCardCycleTooltip。warning 提示时长沿用 OsdService 默认（discretion，不做节流参数）。<!-- planner-discipline-allow: dismissCurrent --></action>
  <verify>
    <automated>flutter test test/widget/player/error_card_host_test.dart --plain-name warning</automated>
    <fails_when>命令非零退出，或 warning 提示后队列推进次数不为 1、卡片显示 warning、徽标计数与快照不符、轮览触发 dismissCurrent、快照超界、关闭后轮览未重置任一断言未通过。</fails_when>
  </verify>
  <acceptance_criteria>
    - warning 分流恰好一次 OSD + 一次 dismissCurrent，重复触发不放大（无重建循环）。
    - 快照以 eventId 为身份、上界 20 命名常量、最旧挤出；kernel 文件 diff 为零（D-11）。
    - 徽标轮览是纯视图偏移：轮览期间 dismissCurrent 调用数为 0；手动关闭推进真实队列并重置轮览。
    - 新错误替换卡片内容（不堆叠多卡，D-01）且重置轮览索引。
    - focused 命令退出 0；flutter analyze 0 error。
  </acceptance_criteria>
  <done>warning/error/fatal 三级各得其所（OSD 短暂提示 vs 常驻卡片），多错误经徽标在本地有界快照内回看，D-01/D-02/D-11 全部落地且 kernel 保持零改动。</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | Symbols produced or modified | Purpose |
|---|---|---|
| `lib/ui/player/error_card.dart` | `_copyDiagnosticPack`、复制按钮、可点击计数徽标 | CARD-04 复制通路 + D-01 轮览入口。 |
| `lib/ui/player/error_card_host.dart` | severity 路由完成版、`_maxSnapshotLength` 快照、轮览索引 | D-02/D-11 呈现层逻辑（零 kernel 改动）。 |
| `test/widget/player/error_card_test.dart` | copy 成功/失败注入组 | CARD-04 自动化验证。 |
| `test/widget/player/error_card_host_test.dart` | warning 路由 + 徽标轮览组 | D-01/D-02/D-11 自动化验证。 |

## Multi-Source Coverage Audit

| Source | Item | Disposition |
|---|---|---|
| GOAL | 一键复制、多错误反馈不阻碍操作 | COVERED：Task 1 复制 + Task 2 轮览。 |
| REQ | CARD-04 | COVERED：Task 1 全部断言（含失败注入）。 |
| REQ | CARD-03 | COVERED（共享）：Task 2 徽标属折叠区信息呈现的扩展。 |
| CONTEXT | D-01 | COVERED：Task 2 替换语义 + 徽标轮览。 |
| CONTEXT | D-02 | COVERED：Task 2 warning→OSD 恰好一次推进。 |
| CONTEXT | D-06 | COVERED：Task 1 两态 OSD 反馈。 |
| CONTEXT | D-09 | COVERED：Task 1 明确不加动作按钮。 |
| CONTEXT | D-11 | COVERED：Task 2 本地有界快照，零 kernel 改动。 |
| CONTEXT | D-07 | COVERED BY 03-04：等效覆盖与删除。 |
| CONTEXT | D-12 | COVERED BY 03-01。 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| 诊断包 → 系统剪贴板 | 完整路径字段按 D-07 允许进入复制包；剪贴板被本机其他进程读取属开发机接受的暴露面。 |
| warning 分流 ↔ 队列推进 | 分流逻辑若重复 dismiss 会静默丢弃待呈现错误。 |
| 本地快照 ↔ reporter 真队列 | 视图快照与真实 FIFO 是两套状态，混淆会造成「关错条目」。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-08 | Information Disclosure | 剪贴板诊断包 | medium | accept | D-07 批准开发机本地证据含完整路径；包内容已在 intake 脱敏普通字段，本机单用户场景。 |
| T-03-09 | Denial of Service | warning 分流 | medium | mitigate | 恰好一次 dismiss + 同帧防重提示状态；无重建循环测试锁死。 |
| T-03-10 | Tampering | 轮览/队列状态混淆 | medium | mitigate | 轮览为纯视图偏移不写 reporter；dismissCurrent 仅手动关闭路径；测试断言调用计数。 |
| T-03-11 | Denial of Service | 复制异常外溢 | medium | mitigate | typed catch（PlatformException/MissingPluginException），异常不达调用方，卡片状态不变。 |
| T-03-SC | Tampering | 依赖供应链 | low | accept | 零新增包。 |
</threat_model>

<verification>
  <automated>flutter test test/widget/player/error_card_test.dart test/widget/player/error_card_host_test.dart &amp;&amp; flutter analyze</automated>
  <fails_when>测试或 analyze 非零退出，或 copy/warning/轮览断言组任一回归，或出现新 analyzer error。</fails_when>
</verification>

<success_criteria>
- 复制文本与日志文件格式逐字符同源（LOG-05 在 UI 侧闭环），失败两态隔离验证齐备。
- warning 恰好一次 OSD + 单次推进，无循环；error/fatal 常驻卡片。
- 徽标轮览在本地快照内完成，dismissCurrent 零误用，kernel 零改动。
- flutter analyze 0 error；headless 预存失败按基线鉴别。
</success_criteria>

<output>
Create `.planning/phases/03-playback-error-card-bridge/03-03-SUMMARY.md` when done.
</output>
