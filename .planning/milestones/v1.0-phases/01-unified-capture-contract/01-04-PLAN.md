---
phase: 01-unified-capture-contract
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - lib/kernel/diagnostics/diagnostic_redactor.dart
  - lib/kernel/diagnostics/error_report.dart
  - lib/kernel/diagnostics/error_reporter.dart
  - lib/kernel/diagnostics/player_error_report_bridge.dart
  - test/diagnostics/error_report_test.dart
  - test/diagnostics/error_reporter_test.dart
  - test/diagnostics/player_error_report_bridge_test.dart
autonomous: true
gap_closure: true
gaps:
  - CAP-01-WHITESPACE-PATH-REDACTION
  - CAP-01-BRIDGE-METADATA-CONTAINMENT
  - CAP-04-SEMANTIC-DEDUPE-IDENTITY
requirements:
  - CAP-01
  - CAP-02
  - CAP-03
  - CAP-04
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Valid quoted and unquoted Windows and POSIX local paths containing whitespace, parentheses, and brackets expose only the basename in ErrorReport.mediaPath, message, and rawStackTrace; effects and presentation receive that same sanitized snapshot."
    - "Reports with the same source, runtime type, message, and top frame remain distinct when severity, structured PlayerError code/discriminator, or sanitized media path differs."
    - "Reports merge only when every semantic fingerprint field matches and elapsed time remains within D-04's inclusive zero-through-ten-second window; capacity remains exactly five."
    - "The immutable ErrorReport snapshots structured PlayerError identity at intake, so later mutable ErrorContext changes cannot alter accepted evidence."
    - "If PlayerErrorReportBridge media-context lookup throws, the same PlayerError still reaches ErrorReporter exactly once with a safe null media-path fallback."
    - "D-01 same-zone bootstrap, D-02 singleton ownership, D-03 readiness-gated flush, and CAP-03 failure containment remain unchanged."
  artifacts:
    - "lib/kernel/diagnostics/diagnostic_redactor.dart"
    - "lib/kernel/diagnostics/error_report.dart"
    - "lib/kernel/diagnostics/error_reporter.dart"
    - "lib/kernel/diagnostics/player_error_report_bridge.dart"
    - "test/diagnostics/error_report_test.dart"
    - "test/diagnostics/error_reporter_test.dart"
    - "test/diagnostics/player_error_report_bridge_test.dart"
  key_links:
    - "ErrorReporterImpl._createReport → DiagnosticRedactor → bounded ErrorReport → _accept → queue/effects/presentation"
    - "PlayerErrorReportBridge media-path snapshot boundary → contained provider lookup → exactly one ErrorReporter.reportPlayerError call with path or null"
    - "ErrorReporterImpl.reportPlayerError → exhaustive PlayerError subtype/code projection → ErrorReport.playerErrorCode"
    - "ErrorReporterImpl._fingerprint → source + severity + errorType + playerErrorCode + message + sanitized mediaPath + top application frame"
    - "ErrorReport.copyWith → immutable semantic identity preservation during D-04 occurrence merging"
  assumptions:
    - requirement: CAP-01
      status: unresolved
      classification: unclassified
      statement: "Spec-less edge coverage for CAP-01 remains a flagged assumption; this closure covers the verifier-named quoted/unquoted Windows and POSIX whitespace/parenthesis/bracket paths without claiming exhaustive filesystem grammar coverage."
    - requirement: CAP-02
      status: unresolved
      classification: unclassified
      statement: "Spec-less edge coverage for CAP-02 remains a flagged assumption; the injected hook tests remain the automated evidence and the separately recorded real Windows global-hook smoke remains human verification."
    - requirement: CAP-03
      status: unresolved
      classification: unclassified
      statement: "Spec-less edge coverage for CAP-03 remains a flagged assumption; the plan preserves the existing reentrancy and per-effect containment contract while changing only normalized evidence and fingerprint identity."
    - requirement: CAP-04
      status: unresolved
      classification: unclassified
      statement: "Spec-less edge coverage for CAP-04 remains a flagged assumption; semantic equality is defined by the verifier-required fields plus the existing source/type/message/top-frame identity, with no inferred cross-process or permanent dedupe semantics."
  prohibitions:
    - statement: "MUST NOT send captured errors, stacks, structured codes, or media-path snapshots to remote telemetry; this diagnostic path remains process-local."
      status: flagged-unverified
      flagged: true
    - statement: "MUST NOT make capture, redaction, deduplication, or queue retention depend on a mounted UI, modal route, or user acknowledgement."
      status: flagged-unverified
      flagged: true
    - statement: "MUST NOT modify media_kit or subscribe below the project-owned MediaEngine wrapper surface."
      status: flagged-unverified
      flagged: true
    - statement: "MUST NOT merge reports whose severity, structured PlayerError code/discriminator, or sanitized media path differs."
      status: flagged-unverified
      flagged: true
---

<objective>
Close the verifier-confirmed CAP-01 and CAP-04 residual gaps by making local-path redaction delimiter-aware and deduplication semantically lossless, while preserving D-01, D-02, D-03, and D-04.

Purpose: Ensure diagnostic capture never leaks valid whitespace-bearing local directory prefixes and never hides a fatal, differently coded, or different-media event behind an earlier report.
Output: A tested local-path tokenizer/redactor, immutable `ErrorReport.playerErrorCode` snapshot, expanded fingerprint identity, and regression coverage through queue, effects, and presentation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-unified-capture-contract/01-CONTEXT.md
@.planning/phases/01-unified-capture-contract/01-RESEARCH.md
@.planning/phases/01-unified-capture-contract/01-PATTERNS.md
@.planning/phases/01-unified-capture-contract/01-VERIFICATION.md
@.planning/phases/01-unified-capture-contract/01-03-SUMMARY.md
@lib/kernel/diagnostics/diagnostic_redactor.dart
@lib/kernel/diagnostics/error_report.dart
@lib/kernel/diagnostics/error_reporter.dart
@test/diagnostics/error_reporter_test.dart
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace whitespace-bearing local paths safely through every reporter observation</name>
  <reversibility rating="reversible">The tokenizer remains an internal normalization policy with no persisted-format migration or external dependency.</reversibility>
  <files>lib/kernel/diagnostics/diagnostic_redactor.dart, lib/kernel/diagnostics/player_error_report_bridge.dart, test/diagnostics/error_reporter_test.dart, test/diagnostics/player_error_report_bridge_test.dart</files>
  <read_first>
    - .planning/phases/01-unified-capture-contract/01-VERIFICATION.md `gaps` entry for CAP-01, especially the required Windows/POSIX downstream observations
    - .planning/phases/01-unified-capture-contract/01-CONTEXT.md D-01 through D-04; preserve D-03 queue/flush behavior
    - lib/kernel/diagnostics/diagnostic_redactor.dart `redactPathValue`, `redactDiagnosticText`, `_isLocalPath`, and `_basename`
    - lib/kernel/diagnostics/error_reporter.dart `_createReport`, `_snapshotStack`, `_sanitizeMediaPath`, `_publishSafely`, and `_notifyEffects`
    - lib/kernel/diagnostics/player_error_report_bridge.dart media-path provider lookup, identity-correlation forwarding, and `on Object` containment boundary
    - test/diagnostics/error_reporter_test.dart `ErrorReporterImpl diagnostic redaction` group and `_reporter` helper
    - test/diagnostics/player_error_report_bridge_test.dart hand-written recording reporter/provider fakes and existing exactly-once bridge regressions
  </read_first>
  <behavior>
    - RED: a Windows path such as `C:\Users\alice\Private Videos (Archive)\[2026]\incident.mp4` loses every directory component while retaining `incident.mp4` in mediaPath, message, rawStackTrace, an effect observation, and presentation.current.
    - RED: a POSIX path such as `/home/alice/Private Videos (Archive)/[2026]/incident.mp4` has the same end-to-end result.
    - RED: quoted and diagnostic-delimited unquoted forms both preserve a trailing source location such as `:7:8`, surrounding prose, and balanced diagnostic punctuation.
    - RED: a second redaction pass is identical to the first, while `package:simple_player_flutter/...`, `http://`, `https://`, and `rtsp://` text remains unchanged.
    - RED: when the bridge's injected media-path provider throws, the PlayerError is forwarded exactly once to the recording reporter with `mediaPath: null`, and the bridge intake returns normally.
    - GREEN: one delimiter-aware scanner replaces whitespace-terminated path matching and feeds the existing pre-fan-out normalization path; bridge metadata failure degrades only the optional path snapshot, not PlayerError delivery.
  </behavior>
  <action>Write the failing table-driven tracer tests first. Replace the four whitespace-terminated regular-expression passes in `DiagnosticRedactor.redactDiagnosticText` with small documented scanning helpers that recognize `file:` URIs, UNC paths, drive-rooted Windows paths, and rooted POSIX paths; consume quoted paths through their matching quote and consume unquoted paths through explicit diagnostic delimiters rather than stopping at spaces. Treat spaces, `(`, `)`, `[`, and `]` as legal path content when they occur inside the recognized token, retain only the final component through `_basename`, and preserve a source suffix matching `:<line>:<column>` plus surrounding message/stack punctuation. Keep URI scheme detection ahead of local-path detection so package frames and ordinary `http`, `https`, and `rtsp` URLs are not rewritten. Keep `redactPathValue` and `redactDiagnosticText` deterministic and idempotent, and document why delimiter parsing is required for valid local filenames. Do not move sanitization into an effect or UI consumer: the existing `ErrorReporterImpl._createReport` path must continue to redact before bounds, queue acceptance, effects, or presentation per D-03 and CAP-03. In `PlayerErrorReportBridge`, narrow the optional metadata snapshot containment so a throwing `CurrentMediaPathProvider` yields a local null path snapshot and execution still performs exactly one `reportPlayerError` call for that PlayerError. Keep the broader bridge intake non-throwing for CAP-03, but do not let provider failure skip forwarding; document why `on Object` is intentional at this external/callback boundary. Add the regression first with a hand-written throwing provider and recording reporter, asserting one accepted call, original PlayerError identity, null media path, and normal return. Preserve same-instance OpenError correlation so this fallback cannot create a callback/notifier duplicate.</action>
  <verify>
    <automated>flutter test test/diagnostics/error_reporter_test.dart test/diagnostics/player_error_report_bridge_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Active Windows and POSIX tests include whitespace, parentheses, and brackets and cover both quoted and diagnostic-delimited unquoted embedded paths.
    - Each case asserts that directory prefixes are absent and only `incident.mp4` remains as path evidence in `mediaPath`, `message`, `rawStackTrace`, the effect argument, and `presentation.current`.
    - Stack line/column evidence and surrounding diagnostic text survive without leaking a directory fragment.
    - Repeated redaction is stable; project package frames and http/https/rtsp URLs are byte-for-byte preserved.
    - A deterministic throwing-provider bridge test records exactly one reporter call for the original PlayerError with a null media-path fallback, and bridge intake returns normally.
    - Existing same-instance OpenError duplicate suppression remains green, proving metadata fallback does not add a second report.
    - The focused automated command exits 0.
  </acceptance_criteria>
  <done>One real player report carrying a valid whitespace-bearing local path crosses normalization, FIFO, effect, and presentation boundaries with basename-only evidence, and optional metadata lookup failure cannot prevent exactly-once PlayerError delivery.</done>
</task>

<task type="expansion" tdd="true">
  <name>Task 2: Preserve severity, structured player code, and media target in dedupe identity</name>
  <reversibility rating="costly">Adding immutable `ErrorReport.playerErrorCode` extends the shared diagnostic contract consumed by later file and card phases, so removal would require coordinated downstream migration.</reversibility>
  <files>lib/kernel/diagnostics/error_report.dart, lib/kernel/diagnostics/error_reporter.dart, test/diagnostics/error_report_test.dart, test/diagnostics/error_reporter_test.dart, test/diagnostics/player_error_report_bridge_test.dart</files>
  <read_first>
    - .planning/phases/01-unified-capture-contract/01-VERIFICATION.md CAP-04 gap reason, affected artifacts, and all three required separation cases
    - .planning/phases/01-unified-capture-contract/01-CONTEXT.md D-03 and D-04 queue/dedupe decisions plus D-01/D-02 ownership constraints
    - lib/kernel/diagnostics/error_report.dart `ErrorReport` constructor, fields, and `copyWith`
    - lib/kernel/diagnostics/error_reporter.dart `reportPlayerError`, `_createReport`, `_accept`, `_findMatchingIndex`, and `_fingerprint`
    - lib/kernel/models/player_error.dart sealed subtypes, typed `code` enums, bilingual immutable-contract documentation, and replacement-object conventions mapped by 01-PATTERNS.md
    - lib/kernel/diagnostics/kernel_logger.dart `KernelLoggerImpl.I` nullable-static lifecycle and per-sink `on Object` service-boundary containment mapped by 01-PATTERNS.md
    - test/unit/kernel/diagnostics/startup_timeline_test.dart direct dependency injection, focused immutable-contract assertions, and behavior-driven test naming mapped to error_report_test.dart
    - test/diagnostics/kernel_logger_test.dart hand-written recording sink, singleton reset/isolation, and contained-failure testing mapped to reporter tests
    - test/diagnostics/error_report_test.dart immutable replacement test
    - test/diagnostics/error_reporter_test.dart adapter, queue-policy, redaction, and fault-isolation groups
    - test/diagnostics/player_error_report_bridge_test.dart same-message distinct-instance expectation
  </read_first>
  <behavior>
    - RED: two same-message/top-frame player reports with recoverable versus fatal severity remain two FIFO entries and retain their own severity.
    - RED: two same-message/top-frame reports using different recoverable `PlaybackErrorCode` values remain two FIFO entries even when severity and media path match.
    - RED: two otherwise identical player reports for `first.mp4` and `second.mp4` remain two FIFO entries and retain their own sanitized media path.
    - RED: two reports whose source, severity, runtime type, structured code, sanitized message, sanitized media path, and top frame all match still merge inside the inclusive ten-second D-04 window.
    - GREEN: `ErrorReport.copyWith` preserves the new structured identity snapshot along with all existing identity fields.
  </behavior>
  <action>Write the failing semantic-separation tests before production changes. Follow the `player_error.dart` analog from 01-PATTERNS.md: keep the report contract fully immutable, use bilingual public API documentation, and make `copyWith` return a replacement object rather than mutate accepted evidence. Add a nullable immutable `String? playerErrorCode` field to `ErrorReport`, require it at every constructor call, preserve it unchanged in `copyWith`, and update the contract test to prove replacement identity preservation using the direct-injection and focused assertion style from `startup_timeline_test.dart`. Preserve the `KernelLoggerImpl` analog's nullable-static singleton/service lifecycle; do not create a second reporter instance or move containment out of the service boundary. Keep broad `on Object` handling only at the documented diagnostic composition boundary, with each effect isolated so a sibling still runs. Build tests with small hand-written recording fakes/spies and explicit singleton reset where applicable, following `kernel_logger_test.dart`, rather than mocks or global time. In `ErrorReporterImpl.reportPlayerError`, snapshot a stable discriminator before entering the generic report path using an exhaustive Dart 3 switch over the sealed hierarchy: `file:<FileErrorCode.name>`, `codec:<CodecErrorCode.name>`, `playback:<PlaybackErrorCode.name>`, `network:<NetworkErrorCode.name>`, and `unknown`; pass null for Flutter, platform, and guarded-zone reports. Thread that primitive through `_reportSafely` and `_createReport` without retaining the mutable `PlayerError`. Expand `_fingerprint` in a fixed order to include `report.source.name`, `report.severity.name`, `report.errorType`, `report.playerErrorCode`, `report.message`, `report.mediaPath`, and `_topFrame(report.rawStackTrace)`. Fingerprint only the already-sanitized `mediaPath` stored on the report, never the original local path. Update the bridge regression whose same-message distinct `UnknownError` objects currently expect a merge: make its evidence explicitly prove identity-scoped bridge forwarding while accepting reporter merging only when every semantic field matches, and add distinct-code/path cases in the reporter suite. Preserve D-04 capacity five, FIFO position, event ID, occurrence count, inclusive zero-to-ten-second timing, and rollback behavior; preserve D-03 flush identity and CAP-03 effect/reentrancy containment. Do not change global hooks, `PlayerError` enums, `PlayerErrorReportBridge` production logic, media_kit, file logging, or card UI.</action>
  <verify>
    <automated>flutter test test/diagnostics/error_report_test.dart test/diagnostics/error_reporter_test.dart test/diagnostics/player_error_report_bridge_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `ErrorReport` contains immutable `playerErrorCode`; all four non-player adapters set it to null and every PlayerError subtype maps to the documented stable discriminator format.
    - Active tests produce separate FIFO entries for different severity, different structured code, and different sanitized media path while holding the remaining practical identity inputs constant.
    - An all-fields-equal duplicate still merges within D-04's inclusive ten-second window and preserves the first event ID and queue slot.
    - `copyWith` changes only occurrence metadata and preserves severity, structured code, media path, and every prior identity field.
    - Existing capacity-five, rollback, redaction, flush, bridge identity-correlation, and fault-isolation behaviors remain green.
    - The focused automated command exits 0.
  </acceptance_criteria>
  <done>Deduplication merges only semantically equivalent reports and cannot overwrite later severity, typed player-code, or media-target evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | Symbols produced or modified | Purpose |
|---|---|---|
| `lib/kernel/diagnostics/diagnostic_redactor.dart` | `DiagnosticRedactor.redactDiagnosticText`, local-token scanner helpers, `_basename` suffix handling | Consume quoted/unquoted local paths with spaces and punctuation while preserving safe basename/location evidence. |
| `lib/kernel/diagnostics/error_report.dart` | `ErrorReport.playerErrorCode`, constructor contract, `copyWith` | Retain immutable structured player identity for later dedupe, logging, and presentation consumers. |
| `lib/kernel/diagnostics/error_reporter.dart` | `reportPlayerError`, `_reportSafely`, `_createReport`, player-code projection helper, `_fingerprint` | Snapshot typed player identity and compare all verifier-required semantic fields after sanitization. |
| `lib/kernel/diagnostics/player_error_report_bridge.dart` | media-path snapshot helper and exactly-once forwarding boundary | Degrade a throwing optional metadata provider to null without losing or duplicating the PlayerError report. |
| `test/diagnostics/error_report_test.dart` | `ErrorReport` identity-preservation assertions | Prove immutable replacement retains the structured code snapshot. |
| `test/diagnostics/error_reporter_test.dart` | Whitespace-path fan-out tracer and semantic-fingerprint regression groups | Prove CAP-01 safe redaction and CAP-04 distinct-evidence retention. |
| `test/diagnostics/player_error_report_bridge_test.dart` | Throwing-provider exactly-once regression plus same-message bridge/reporter expectation adjustment | Prove optional metadata failure cannot drop a report while keeping object-identity ingress correlation distinct from semantic queue dedupe. |

<source_audit>
SOURCE | ID | Feature/requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Four sources safely normalize into traceable, deduplicated, ordered reports without reporter-chain failure | 01-01 through 01-04 | COVERED | Task 1 closes unsafe local-path flow; Task 2 prevents semantically distinct evidence collision.
REQ | CAP-01 | Four source types share one immutable report with event, time, severity, error, stack, and media snapshot | 01-04 | COVERED | Task 1 keeps the player source unconditional when optional metadata lookup throws; Tasks 1–2 sanitize valid local paths and retain structured player identity.
REQ | CAP-02 | Global hooks share the guarded zone, preserve framework presentation, and handle dispatcher errors | 01-02, 01-04 | COVERED | Existing implementation remains; Plan 04 explicitly preserves D-01/D-02 and carries the required ID without claiming the pending human smoke is automated.
REQ | CAP-03 | Sole non-throwing, reentrancy-safe fan-in with isolated effects | 01-01, 01-02, 01-04 | COVERED | Both tasks retain pre-fan-out normalization and run existing fault-isolation regressions.
REQ | CAP-04 | Five-entry FIFO plus correct short-window fingerprint merging | 01-04 | COVERED | Task 2 adds severity, player code, and sanitized path while retaining D-04 timing/capacity.
RESEARCH | — | Immutable primitive snapshots and `copyWith` replacement | 01-04 | COVERED | Task 2 snapshots a primitive discriminator and preserves it through immutable replacement.
RESEARCH | — | Ten-second queue-local dedupe using source/type/message/top application frame | 01-04 | COVERED | Task 2 retains those fields and adds verifier-required semantic dimensions.
RESEARCH | — | Diagnostic content is untrusted, bounded, and sanitized before fan-out | 01-04 | COVERED | Task 1 replaces unsafe regex termination while keeping existing post-redaction bounds.
RESEARCH | — | Five-entry `ListQueue`, readiness-gated immutable presentation, and isolated synchronous effects | 01-01, 01-04 | COVERED | Plan 04 changes neither ownership nor fan-out order and reruns focused regressions.
RESEARCH | — | No package install or external API required | 01-04 | COVERED | Uses Dart scanning and existing fakes; `COVERAGE.md` records the internal-API detector false positive.
CONTEXT | D-01 | One `runZonedGuarded` encloses complete bootstrap | 01-02, 01-04 | COVERED | Explicitly preserved; no startup files change.
CONTEXT | D-02 | Main owns early reporter/logger singleton initialization | 01-02, 01-04 | COVERED | Explicitly preserved; no initializer or service ownership changes.
CONTEXT | D-03 | Pre-runApp evidence uses the same FIFO/dedupe and flush path | 01-04 | COVERED | Redaction and semantic identity happen before the unchanged readiness-gated publication path.
CONTEXT | D-04 | Capacity exactly five; duplicates merge in a researched time window | 01-04 | COVERED | Task 2 keeps capacity five and inclusive zero-through-ten-second policy while correcting equality.
CONTEXT | Deferred Ideas | None in Phase 1 context | — | EXCLUDED | No deferred feature is introduced; remote telemetry, file output, and card UI remain out of scope.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Error source → `ErrorReporterImpl._createReport` | Untrusted exception text, stack text, typed player errors, and local/media paths enter normalization. |
| `PlayerErrorReportBridge` metadata provider → reporter intake | Optional media context can throw, but the required PlayerError event must still cross the bridge exactly once. |
| `DiagnosticRedactor` → bounded immutable `ErrorReport` | Potentially sensitive path tokens become retained process-local evidence. |
| `ErrorReport` → `_fingerprint` / FIFO | Semantic evidence is either retained distinctly or merged into an existing queue slot. |
| FIFO → effects / presentation | The same accepted immutable report fans out to future file and UI consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14 | Information Disclosure | `DiagnosticRedactor.redactDiagnosticText` | high | mitigate | Task 1 tokenizes quoted/unquoted Windows and POSIX paths with whitespace/parentheses/brackets and proves prefixes are absent before every fan-out. |
| T-01-20 | Denial of Service | `PlayerErrorReportBridge` metadata snapshot | high | mitigate | Task 1 contains provider failure separately, forwards the original PlayerError once with null media path, and regression-tests normal return plus unchanged identity correlation. |
| T-01-15 | Tampering | `ErrorReporterImpl._fingerprint` | high | mitigate | Task 2 includes severity, stable player code, and sanitized media target so a later fatal or different-media event cannot inherit earlier semantics. |
| T-01-16 | Repudiation | `ErrorReport.playerErrorCode` and `copyWith` | medium | mitigate | Task 2 freezes the sealed subtype/code discriminator at intake and tests immutable preservation during occurrence merging. |
| T-01-17 | Denial of Service | Local-path scanner and diagnostic input | medium | mitigate | Task 1 uses a linear scanner over the existing bounded input flow and preserves post-redaction `_bounded` limits; focused burst/fault tests remain required. |
| T-01-18 | Spoofing | Network URL text resembling a drive/root path | medium | mitigate | Task 1 gives recognized non-file URI schemes precedence and tests package/http/https/rtsp evidence remains unchanged. |
| T-01-19 | Elevation of Privilege | Retained diagnostic strings | low | accept | Phase 1 performs no command execution, HTML interpretation, remote transport, or path-based file operation. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation occurs; implementation uses the Dart/Flutter SDK and current project contracts only. |
</threat_model>

<verification>
1. Run `flutter test test/diagnostics/error_report_test.dart test/diagnostics/error_reporter_test.dart test/diagnostics/player_error_report_bridge_test.dart test/diagnostics/global_error_hooks_test.dart`.
2. Run `flutter analyze`; it must exit 0.
3. Run `flutter test`; it must exit 0. If a known headless native-library baseline failure appears, perform the documented identical-baseline comparison and record evidence; never silently waive a nonzero result.
4. Confirm the bridge regression uses a throwing media-path provider and observes exactly one reporter call carrying the original PlayerError with a null media path.
5. Confirm `ErrorReport.playerErrorCode` is assigned at every constructor call and preserved by `copyWith`.
6. Confirm the diff contains no changes to media_kit dependencies/source, global-hook startup, PlayerError enum registries, file logging, or card UI.
7. Confirm each CAP-01/CAP-02/CAP-03/CAP-04 assumption remains flagged and every prohibition remains descriptor-less and flagged rather than silently dismissed.
</verification>

<success_criteria>
- Windows and POSIX paths containing whitespace, parentheses, and brackets expose basename-only evidence through queue, effect, and presentation observations.
- A throwing bridge media-path provider degrades to null metadata while the original PlayerError still reaches the reporter exactly once.
- Severity, typed player code, and sanitized media target each prevent an otherwise matching report from merging.
- Fully equivalent reports retain D-04's five-entry FIFO and inclusive ten-second merging behavior.
- D-01 through D-03 startup/flush ownership and CAP-03 containment regressions remain green.
- Focused tests, `flutter analyze`, and the full `flutter test` gate exit 0 under the documented baseline-attribution policy.
</success_criteria>

<output>
Create `.planning/phases/01-unified-capture-contract/01-04-SUMMARY.md` when done.
</output>
