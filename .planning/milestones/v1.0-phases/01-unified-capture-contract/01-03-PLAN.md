---
phase: 01-unified-capture-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - lib/kernel/diagnostics/diagnostic_redactor.dart
  - lib/kernel/diagnostics/error_reporter.dart
  - lib/kernel/diagnostics/player_error_report_bridge.dart
  - lib/kernel/player_services.dart
  - test/diagnostics/error_reporter_test.dart
  - test/diagnostics/player_error_report_bridge_test.dart
  - test/kernel/player_services_test.dart
autonomous: true
gap_closure: true
gaps:
  - GAP-1
  - GAP-2
requirements:
  - CAP-01
  - CAP-04
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A synchronous PlaybackController validation failure reaches ErrorReporterImpl once through the single production PlayerErrorReportBridge."
    - "An OpenError published through MediaKitEngine.lastError and then returned to PlaybackController produces one report, while a later asynchronous lastError value also produces one report."
    - "Disposing PlayerServices detaches the bridge listener before the controller and engine are disposed, so later wrapper notifications cannot enter diagnostics."
    - "Windows drive, UNC, POSIX, and file-URI paths are redacted from ErrorReport.mediaPath, message, and rawStackTrace before queue insertion; effects and presentation observe only the same sanitized immutable report."
    - "A matching fingerprint merges only when elapsed time is in the closed interval from Duration.zero through ten seconds; a wall-clock rollback appends a distinct report."
  artifacts:
    - "lib/kernel/diagnostics/diagnostic_redactor.dart"
    - "lib/kernel/diagnostics/player_error_report_bridge.dart"
    - "lib/kernel/diagnostics/error_reporter.dart"
    - "lib/kernel/player_services.dart"
    - "test/diagnostics/player_error_report_bridge_test.dart"
    - "test/diagnostics/error_reporter_test.dart"
    - "test/kernel/player_services_test.dart"
  key_links:
    - "PlayerServices-owned PlayerErrorReportBridge → PlaybackController.onError plus MediaEngine.lastError → ErrorReporter.reportPlayerError"
    - "PlayerErrorReportBridge identity correlation → one OpenError report despite notifier and callback exposure"
    - "ErrorReporterImpl._createReport → DiagnosticRedactor → bounded immutable ErrorReport → queue/presentation/effects"
    - "ErrorReporterImpl._accept → nonnegative elapsed guard plus D-04 ten-second upper bound"
  assumptions:
    - requirement: CAP-01
      status: unresolved
      classification: unclassified
      statement: "Spec-less edge coverage for CAP-01 remains unclassified; this gap plan resolves only the verifier-named production bridge and four path families without inventing additional product semantics."
    - requirement: CAP-02
      status: unresolved
      classification: unclassified
      statement: "Spec-less edge coverage for CAP-02 remains unclassified; this plan preserves the verified global-hook implementation and does not reinterpret its runtime semantics."
    - requirement: CAP-03
      status: unresolved
      classification: unclassified
      statement: "Spec-less edge coverage for CAP-03 remains unclassified; this plan preserves reporter containment and requires the new bridge/redaction boundary to return normally."
    - requirement: CAP-04
      status: unresolved
      classification: unclassified
      statement: "Spec-less edge coverage for CAP-04 remains unclassified; the explicit rollback regression closes the verifier finding without inferring an unspecified clock model."
  prohibitions:
    - statement: "MUST NOT send captured errors, stacks, or media-path snapshots to remote telemetry; this Phase 1 diagnostic path remains process-local."
      status: flagged-unverified
      flagged: true
    - statement: "MUST NOT make player-error capture depend on a mounted UI, modal route, or user acknowledgement."
      status: flagged-unverified
      flagged: true
    - statement: "MUST NOT modify media_kit or subscribe below the project-owned MediaEngine.lastError wrapper surface."
      status: flagged-unverified
      flagged: true
    - statement: "MUST NOT emit duplicate diagnostic evidence when one engine OpenError is exposed by both lastError and PlaybackController.onError."
      status: flagged-unverified
      flagged: true
---

<objective>
Close the two verifier-confirmed Phase 1 gaps: make the fourth error source production-reachable and safe before fan-out, then correct rollback-sensitive deduplication.

Purpose: Fulfill CAP-01 and CAP-04 without re-planning the already verified immutable contract, global hooks, bounded FIFO, or failure-containment work.
Output: One lifecycle-owned player-error bridge, one diagnostic path-redaction policy, corrected elapsed-window logic, and deterministic regression coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-unified-capture-contract/01-CONTEXT.md
@.planning/phases/01-unified-capture-contract/01-RESEARCH.md
@.planning/phases/01-unified-capture-contract/01-PATTERNS.md
@.planning/phases/01-unified-capture-contract/01-VERIFICATION.md
@.planning/phases/01-unified-capture-contract/01-01-SUMMARY.md
@.planning/phases/01-unified-capture-contract/01-02-SUMMARY.md
@lib/kernel/diagnostics/error_reporter.dart
@lib/kernel/player_services.dart
@lib/kernel/services/playback_controller.dart
@lib/kernel/engine/engine_state_view.dart
@test/helpers/fake_engine.dart
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one controller validation failure through the production bridge into a sanitized report</name>
  <reversibility rating="costly">The bridge becomes the shared production ownership contract between PlayerServices, PlaybackController, MediaEngine.lastError, and ErrorReporter, so replacing it later requires coordinated lifecycle changes across those call sites.</reversibility>
  <files>lib/kernel/diagnostics/diagnostic_redactor.dart, lib/kernel/diagnostics/error_reporter.dart, lib/kernel/diagnostics/player_error_report_bridge.dart, lib/kernel/player_services.dart, test/diagnostics/player_error_report_bridge_test.dart</files>
  <read_first>
    - .planning/phases/01-unified-capture-contract/01-VERIFICATION.md GAP-1 reason, artifacts, and missing items
    - .planning/phases/01-unified-capture-contract/01-CONTEXT.md D-01 through D-04; preserve D-01/D-02 startup ownership and D-03 pre-presentation queue semantics
    - lib/kernel/diagnostics/error_reporter.dart, error_report.dart, and error_reporting_dependencies.dart
    - lib/kernel/player_services.dart and lib/kernel/services/playback_controller.dart
    - lib/kernel/engine/engine_state_view.dart and lib/kernel/engine/media_kit_engine.dart for the project-owned lastError contract only
    - lib/kernel/models/player_error.dart and test/helpers/fake_engine.dart
    - test/diagnostics/error_reporter_test.dart and test/kernel/services/playback_controller_test.dart for existing fake and AAA conventions
  </read_first>
  <behavior>
    - RED: a PlaybackController validation failure sent to PlayerErrorReportBridge produces exactly one ErrorSource.playerEngine report without constructing native MediaKitEngine.
    - RED: a validation message/path containing a Windows user directory is already sanitized in queuedReports, an effect observation, and presentation.current after flushPresentation.
    - GREEN: PlayerServices constructs one bridge, injects its controller callback, and owns bridge disposal before controller/engine teardown.
  </behavior>
  <action>Write the failing tracer first, then add a documented `DiagnosticRedactor` with separate path-value and diagnostic-text entry points sharing one deterministic policy: retain useful basename/source-line information while removing absolute directory, drive, authority, account, and share components. Apply that policy inside `ErrorReporterImpl._createReport` before bounding and before `_accept`, `_publishSafely`, or `_notifyEffects`, so no downstream branch can receive the original payload. Add one `PlayerErrorReportBridge` that depends only on `MediaEngine`, `ErrorReporter`, and a `CurrentMediaPathProvider`; expose the one callback injected into `PlaybackController.onError`, subscribe only to `MediaEngine.lastError`, and contain untrusted intake without using `debugPrint` in kernel code. In `PlayerServices`, create exactly one bridge around the existing project-owned engine surface, inject its callback into the controller, and add bridge ownership flags/rollback cleanup so the listener is removed before controller and engine disposal. Preserve the same-zone singleton initialization ownership from D-01/D-02, the pre-presentation queue behavior from D-03, all existing MemoryMonitor and borrowed WindowBridge behavior, and media_kit immutability. Do not alter the media_kit package, `MediaKitEngine` error publication, global hooks, queue capacity, card UI, or file output.</action>
  <verify>
    <automated>flutter test test/diagnostics/player_error_report_bridge_test.dart test/diagnostics/error_reporter_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - A focused test starts at `PlaybackController.openAndPlay` with an invalid path and observes one sanitized player-engine report through `PlayerErrorReportBridge`.
    - The same accepted immutable report is observed sanitized in `queuedReports`, an injected effect, and `presentation.current` after flush.
    - Repository production code contains one PlayerServices-owned `PlayerErrorReportBridge` and its callback is supplied to `PlaybackController.onError`.
    - Bridge construction and cleanup use only `MediaEngine.lastError`; no media_kit source or dependency file changes.
    - Focused tests exit 0.
  </acceptance_criteria>
  <done>One real controller failure crosses every Phase 1 diagnostic layer and reaches all downstream observations with sensitive path prefixes removed.</done>
</task>

<task type="expansion" tdd="true">
  <name>Task 2: Cover OpenError, asynchronous lastError, duplicate suppression, and listener cleanup</name>
  <reversibility rating="reversible">Identity correlation and listener cleanup are local bridge policies behind the existing ErrorReporter and MediaEngine contracts.</reversibility>
  <files>lib/kernel/diagnostics/player_error_report_bridge.dart, lib/kernel/player_services.dart, test/diagnostics/player_error_report_bridge_test.dart, test/kernel/player_services_test.dart</files>
  <read_first>
    - lib/kernel/diagnostics/player_error_report_bridge.dart and test/diagnostics/player_error_report_bridge_test.dart from Task 1
    - lib/kernel/player_services.dart including `_initOnce`, `_disposeCreatedResources`, init rollback flags, and borrowed WindowBridge contract
    - lib/kernel/services/playback_controller.dart OpenError and validation callback branches
    - lib/kernel/engine/media_kit_engine.dart open/stop/seek/stream writes to lastError; read only to understand wrapper behavior
    - test/helpers/fake_engine.dart `failNextOpenWith`, `simulateError`, and lastError notifier
    - test/kernel/player_services_test.dart and test/kernel/services/playback_controller_test.dart
  </read_first>
  <behavior>
    - RED: FakeEngine OpenError first notifies lastError and then reaches the controller callback with the same PlayerError object, yet the bridge accepts one report rather than two.
    - RED: a distinct asynchronous FakeEngine.lastError update after successful media load creates one report carrying the controller's current media-path snapshot.
    - RED: a controller validation error that never enters lastError still creates one report.
    - RED: after bridge/PlayerServices disposal, a retained test notifier or spy cannot deliver another report, and repeated disposal returns normally.
    - GREEN: bridge correlation suppresses only the same already-observed engine error identity and does not suppress later distinct error instances or callback-only failures.
  </behavior>
  <action>Write all four failing behaviors before expanding the bridge. Correlate the synchronous `OpenError` dual exposure by object identity at the single bridge, not by changing D-04 fingerprint dedupe and not by permanently suppressing equal messages: record which non-null `lastError` instance the listener actually forwarded, and suppress the controller callback only when it is that same instance. Forward distinct notifier values, callback-only validation errors, and later same-message objects independently to `ErrorReporter.reportPlayerError`; let D-04 reporter policy decide temporal fingerprint merging after ingress. Snapshot media context at intake using the error context path when present and otherwise the injected `PlaybackController.currentPath` provider. Make `dispose()` idempotently detach the exact listener before its engine notifier is disposed. Add a narrow PlayerServices test seam only if necessary to behaviorally prove production ownership with `FakeEngine`; keep default construction unchanged and avoid native libmpv in tests. Update reverse-order rollback/disposal tests to prove bridge cleanup occurs before controller/engine cleanup. Preserve CAP-03 containment and all existing service ownership contracts; do not add a second callback adapter, second notifier listener, UI listener, or direct MediaKitEngine dependency to the bridge.</action>
  <verify>
    <automated>flutter test test/diagnostics/player_error_report_bridge_test.dart test/kernel/player_services_test.dart test/kernel/services/playback_controller_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The OpenError dual-exposure test observes exactly one bridge-to-reporter call/report.
    - Validation-only and distinct asynchronous lastError tests each observe one report and do not depend on native media libraries.
    - A same-message but distinct PlayerError instance remains eligible for reporting, proving suppression is identity-scoped rather than message-scoped.
    - Disposal removes the listener, is idempotent, and is ordered before controller/engine teardown in normal disposal and initialization rollback.
    - Production search shows one bridge owner and no direct `reportPlayerError` calls from PlayerServices, PlaybackController, UI, or MediaKitEngine outside that bridge.
    - Focused tests exit 0.
  </acceptance_criteria>
  <done>Both synchronous controller paths and asynchronous engine-wrapper errors reach one lifecycle-owned bridge with no duplicate OpenError evidence or listener leak.</done>
</task>

<task type="expansion" tdd="true">
  <name>Task 3: Lock all redaction families and reject negative elapsed dedupe windows</name>
  <reversibility rating="reversible">The redaction and elapsed-window policies are isolated internal functions with no persisted-format migration in Phase 1.</reversibility>
  <files>lib/kernel/diagnostics/diagnostic_redactor.dart, lib/kernel/diagnostics/error_reporter.dart, test/diagnostics/error_reporter_test.dart</files>
  <read_first>
    - lib/kernel/diagnostics/diagnostic_redactor.dart and error_reporter.dart after Tasks 1–2
    - test/diagnostics/error_reporter_test.dart adapter, queue-policy, effect, and presentation groups
    - lib/kernel/diagnostics/kernel_logger.dart `redactPath` only as an existing narrow Dart-path precedent; do not reuse it for broader media-path policy
    - .planning/phases/01-unified-capture-contract/01-VERIFICATION.md GAP-1 redaction families and GAP-2 rollback finding
    - .planning/phases/01-unified-capture-contract/01-CONTEXT.md D-03 and D-04
  </read_first>
  <behavior>
    - RED: table-driven Windows drive, UNC, POSIX, and file-URI examples lose directory/authority/account/share components from mediaPath, message, and rawStackTrace while retaining a useful basename and source line when present.
    - RED: each path family is absent from queuedReports, effect arguments, and presentation.current, proving redaction precedes all fan-out rather than occurring in a consumer.
    - RED: when FakeClock moves backward by minutes after a matching report, the second report appends with a new event ID and occurrenceCount 1 instead of replacing the stale slot.
    - RED: elapsed durations at zero and exactly ten seconds still merge, while durations above ten seconds append, preserving D-04's inclusive short-window semantics.
    - GREEN: `_accept` checks both elapsed lower and upper bounds without changing capacity, fingerprint fields, immutable replacement, or FIFO order.
  </behavior>
  <action>Add table-driven RED tests for representative raw values such as a Windows user directory, UNC server/share path, POSIX home path, and percent-encoded absolute file URI. Assert against the sensitive components themselves across `mediaPath`, `message`, `rawStackTrace`, effect arguments, and flushed presentation; also assert the useful basename and stack line/column survive. Complete `DiagnosticRedactor` so replacements are deterministic, idempotent, bounded after sanitization, and leave project package stack frames plus non-file network URLs intact. Keep the dedicated policy inside reporter normalization rather than sinks or future UI. Add a deterministic FakeClock rollback test and boundary cases, then change `_accept` to merge only when `elapsed >= Duration.zero` and `elapsed <= _dedupeWindow` per D-04. Do not switch queue timestamps to a new external clock source, alter first/last occurrence semantics, or rework already verified FIFO/effect containment.</action>
  <verify>
    <automated>flutter test test/diagnostics/error_reporter_test.dart test/diagnostics/player_error_report_bridge_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All four path-family cases prove sensitive directory/authority components are absent from every pre-queue and downstream observable named in the behavior while useful basename/source-line data remains.
    - Redaction is idempotent and does not redact `package:simple_player_flutter/...` frames or ordinary http/https/rtsp URLs as local filesystem paths.
    - The rollback test moves FakeClock backward deterministically and observes two FIFO entries with distinct event IDs and count 1 each.
    - Zero-second and exact-ten-second duplicates merge; an over-ten-second duplicate appends.
    - Existing capacity-five, burst, dismissal, fault-isolation, and global-hook tests remain unchanged in behavior.
    - Focused tests exit 0.
  </acceptance_criteria>
  <done>Unsafe diagnostic path text cannot cross the reporter fan-out boundary, and clock rollback can no longer merge stale evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | Symbols produced or modified | Purpose |
|---|---|---|
| `lib/kernel/diagnostics/diagnostic_redactor.dart` | `DiagnosticRedactor`, path-value and diagnostic-text redaction methods | One tested policy for Windows, UNC, POSIX, and file-URI path removal before diagnostic fan-out. |
| `lib/kernel/diagnostics/player_error_report_bridge.dart` | `PlayerErrorReportBridge`, controller callback intake, lastError listener, identity correlation, `dispose` | Sole project-owned production bridge from controller/engine wrapper errors to `ErrorReporter`. |
| `lib/kernel/diagnostics/error_reporter.dart` | `ErrorReporterImpl._createReport`, `ErrorReporterImpl._accept` | Sanitizes snapshots before acceptance and requires nonnegative elapsed time for dedupe. |
| `lib/kernel/player_services.dart` | Bridge field/ownership flag, `_initOnce` wiring, reverse cleanup | Creates exactly one bridge and owns its lifecycle alongside controller and engine. |
| `test/diagnostics/player_error_report_bridge_test.dart` | Controller validation, OpenError, async notifier, duplicate, and disposal tests | Proves all player ingress forms and bridge lifecycle without native libmpv. |
| `test/diagnostics/error_reporter_test.dart` | Redaction-family/fan-out tests and clock-rollback/boundary tests | Proves safe normalization and corrected CAP-04 time-window behavior. |
| `test/kernel/player_services_test.dart` | PlayerServices bridge ownership and teardown tests | Proves composition-root wiring and cleanup order with project fakes. |

<source_audit>
SOURCE | ID | Feature/requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Four sources safely normalize into traceable, deduplicated, ordered reports without reporter-chain failure | 01-01, 01-02, 01-03 | COVERED | 01-03 connects and sanitizes the missing player source and closes rollback handling.
REQ | CAP-01 | Framework, async, startup, and player sources share immutable ErrorReport | 01-03 | COVERED | Tasks 1–2 establish the production player bridge; Tasks 1/3 sanitize before fan-out.
REQ | CAP-02 | Global hooks and guarded-zone behavior | 01-02 | COVERED | Already verified except the separately recorded Windows human smoke; untouched by this gap plan.
REQ | CAP-03 | Sole safe fan-in/fan-out and failure containment | 01-01, 01-02 | COVERED | Existing implementation remains; Tasks 1–2 preserve containment at new ingress.
REQ | CAP-04 | Capacity-five FIFO and ten-second fingerprint dedupe | 01-03 | COVERED | Task 3 fixes the negative elapsed case while preserving verified queue behavior.
RESEARCH | — | Immutable primitive snapshots, injected clock, bounded FIFO, isolated effects | 01-01 | COVERED | Existing verified kernel is reused, not re-planned.
RESEARCH | — | Project-owned PlayerError adapter and lifecycle composition | 01-03 | COVERED | Verifier supersedes the earlier Phase-3 deferral for CAP-01; Tasks 1–2 wire it now without media_kit changes.
RESEARCH | — | Diagnostic paths/messages/stacks are untrusted local data | 01-03 | COVERED | Tasks 1/3 sanitize all named fields before queue/effects/presentation; CWE-532 is handled in the threat register rather than minted as a bespoke prohibition.
CONTEXT | D-01 | One runZonedGuarded encloses bootstrap | 01-02 | COVERED | Existing verified startup shape is preserved.
CONTEXT | D-02 | Main owns early reporter/logger singleton initialization | 01-02, 01-03 | COVERED | Task 1 consumes the initialized reporter from PlayerServices without adding an initializer.
CONTEXT | D-03 | Pre-runApp reports retain FIFO/dedupe semantics for later presentation | 01-01, 01-03 | COVERED | Redaction occurs before the same queue and flush path; no UI dependency is added.
CONTEXT | D-04 | Capacity five and short-window fingerprint merging | 01-03 | COVERED | Task 3 retains the ten-second upper bound and adds its missing nonnegative lower bound.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| MediaEngine.lastError / PlaybackController.onError → PlayerErrorReportBridge | Mutable, wrapper-originated PlayerError values and paths enter the unified diagnostics service. |
| PlayerErrorReportBridge → ErrorReporterImpl normalization | The sole bridge forwards untrusted message, stack, and media context for immutable snapshotting. |
| ErrorReporterImpl normalization → queue / effects / presentation | Sanitized data fans out to retained memory, future disk effects, and future UI. |
| Injected wall clock → dedupe policy | Non-monotonic timestamps influence whether evidence is merged or retained distinctly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-09 | Information Disclosure | `ErrorReporterImpl._createReport` | high | mitigate | Tasks 1/3 apply one tested redactor to mediaPath, message, and raw stack before queue insertion, effect dispatch, or presentation (CWE-532). |
| T-01-10 | Tampering | Dual OpenError callback/notifier ingress | medium | mitigate | Task 2 correlates only the identical already-forwarded PlayerError instance at the sole bridge and tests distinct errors remain reportable. |
| T-01-11 | Denial of Service | lastError listener and reporter callback | high | mitigate | Tasks 1–2 contain intake, keep one listener, prevent duplicate loops, and detach it before engine disposal. |
| T-01-12 | Repudiation | Clock rollback in dedupe | medium | mitigate | Task 3 requires a nonnegative elapsed duration and retains rollback-separated events with distinct IDs. |
| T-01-13 | Elevation of Privilege | Untrusted diagnostic text interpreted downstream | low | accept | Reports remain immutable local text with no command execution, HTML rendering, remote transport, or filesystem action in this phase. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installation occurs; implementation uses Dart/Flutter SDK and existing project abstractions only. |
</threat_model>

<verification>
1. Run `flutter test test/diagnostics/error_reporter_test.dart test/diagnostics/player_error_report_bridge_test.dart test/kernel/player_services_test.dart test/kernel/services/playback_controller_test.dart`.
2. Run the existing Phase 1 diagnostics tests, including `test/diagnostics/error_report_test.dart` and `test/diagnostics/global_error_hooks_test.dart`, to prove no regression in the three verified sources.
3. Run `flutter analyze`; it must exit 0.
4. Run `flutter test`; it must exit 0. If known headless native media-DLL failures occur, use the project-memory stash/re-run comparison for every failure, record explicit attribution in the summary, and escalate for a developer-approved exception or prerequisite baseline repair if the suite remains nonzero; never silently accept a failing gate.
5. Search production Dart sources for `reportPlayerError` and lastError listener registration: the only player ingress implementation is `PlayerErrorReportBridge`, and the only production owner is `PlayerServices`.
6. Confirm git diff contains no media_kit package source, dependency-version, global-hook, UI card, or file-output changes.
</verification>

<success_criteria>
- PlaybackController validation and OpenError failures plus asynchronous MediaEngine.lastError updates all reach the single production bridge.
- One OpenError exposed through both notifier and callback creates one ingress report; distinct later errors remain reportable.
- Windows, UNC, POSIX, and file-URI sensitive prefixes are absent before queue, effects, and presentation while useful basename/location evidence survives.
- Bridge cleanup is idempotent and precedes controller/engine disposal.
- Clock rollback produces a distinct FIFO report, while zero-through-ten-second duplicates retain D-04 merging behavior.
- CAP-02/CAP-03 and all already verified Phase 1 behavior remain unchanged.
- Focused tests, `flutter analyze`, and the full `flutter test` gate exit 0 under the documented baseline-attribution policy.
</success_criteria>

<output>
Create `.planning/phases/01-unified-capture-contract/01-03-SUMMARY.md` when done.
</output>
