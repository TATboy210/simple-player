---
phase: 05-e2e-resilience-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/diagnostics/end_to_end_injection_test.dart
  - test/diagnostics/burst_resilience_test.dart
  - .planning/phases/05-e2e-resilience-verification/headless-baseline.txt
  - .planning/phases/05-e2e-resilience-verification/05-EVIDENCE-MAP.md
  - docs/error-diagnostics-limitations.md
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04, VER-05]
estimate:
  tokens: 60000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - 四源各注入一次 → 各产一份报告 + 一条文件证据 + 卡片呈现断言通过(D-01 整合用例锁死端到端)
    - 100–1000 合成事件爆发下 FIFO ≤5、快照 ≤20、合并计数可见、无 unhandled error、单写者链不断、pump 响应不卡(D-03/D-04 设计值口径)
    - zone 一致性、reentrancy、复制/关闭失败隔离均有归档证据指向(引用既有用例,缺口处补一用例)
    - VER-04 Windows 实机冒烟以 03/04 UAT 记录正式归档,不重跑(D-02)
    - VER-05 开发者文档存在,覆盖 release 降级/原生崩溃边界/isolate 写盘语义/WER LocalDumps 四主题(D-05)
    - 产品实现零 diff —— phase 窗口内 lib/、windows/、pubspec.yaml 无任何变更
  artifacts:
    - path: test/diagnostics/end_to_end_injection_test.dart
      provides: VER-01 四源端到端整合注入用例(tracer)
    - path: test/diagnostics/burst_resilience_test.dart
      provides: VER-02 爆发压测补差用例 + VER-03 关闭失败隔离补差(如盘点确认缺口)
    - path: .planning/phases/05-e2e-resilience-verification/05-EVIDENCE-MAP.md
      provides: VER-01~05 证据映射表(VER 项 → 测试文件#用例名/UAT 记录)
    - path: docs/error-diagnostics-limitations.md
      provides: VER-05 开发者文档(四主题,中文双语)
  key_links:
    - ErrorReporterImpl 公开 intake → FIFO/effects → ErrorCardHost → ErrorCard(整合用例一次调用走真实链)
    - 05-EVIDENCE-MAP.md 每条引用可 grep 定位(文件#用例名 / UAT 文档#Test N)
    - docs 文档读数方法引用真实实现文件(isolated_error_log_sink.dart / source_line_reader.dart)
---

<objective>
端到端韧性验证收官:把 Phase 1–4 已积累的自动化测试与实机 UAT 证据正式归档为 VER-01~05 的验收证据,补齐唯一实质缺口(四源端到端整合注入用例 + 爆发压测补差),产出 VER-05 开发者文档。**不是重验阶段**——已有证据按采信边界归档,避免与既有测试套件重复劳动。

**User Story(MVP_MODE,源 ROADMAP Phase 5 Goal):**
**As a** developer and user of the player, **I want to** verify that the diagnostics system reliably captures, persists, and surfaces errors under real, burst, and Windows-interaction conditions, **so that** I can trust it without a debugger.
(中文原意:用户和开发者可确信该诊断系统在真实错误、高频错误及 Windows 播放器交互期间完整捕获、可回溯且不妨碍使用。)

Purpose: 里程碑最后一阶段——证明四源捕获、洪流处理和 Windows 交互在交付环境中可靠可用,形成可审计的证据闭环。
Output: 2 个新测试文件 + 1 份证据映射表(.planning/)+ 1 份 VER-05 开发者文档(docs/)。**零产品代码**——kernel/UI 全零 diff 是本 phase 验收底线。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e2e-resilience-verification/05-CONTEXT.md

# 证据盘点事实源(planner 已盘点,executor 按 read_first 深读)
test/diagnostics/error_reporter_test.dart
test/diagnostics/global_error_hooks_test.dart
test/diagnostics/player_error_report_bridge_test.dart
test/widget/player/error_card_host_test.dart
test/widget/player/error_card_test.dart
test/diagnostics/error_log_file_sink_test.dart
test/diagnostics/isolated_error_log_sink_test.dart

# VER-04 归档证据源
.planning/phases/03-playback-error-card-bridge/03-UAT.md
.planning/phases/04-error-feedback-settings/04-UAT.md

# VER-05 文档引用的实现事实源
lib/kernel/diagnostics/isolated_error_log_sink.dart
lib/kernel/diagnostics/isolated_error_log_sink_worker.dart
lib/kernel/diagnostics/source_line_reader.dart
lib/kernel/diagnostics/error_log_location.dart
</context>

<tasks>

<task type="tracer">
  <name>Task 1: tracer——四源端到端整合注入用例(VER-01, per D-01)</name>
  <files>test/diagnostics/end_to_end_injection_test.dart, .planning/phases/05-e2e-resilience-verification/headless-baseline.txt</files>
  <read_first>
    - test/diagnostics/global_error_hooks_test.dart(四钩子注入先例:'forwards exact dispatcher error and stack then returns true'、'delegates an initialized bootstrap error once'、'contains reporter failures from both installed callbacks')
    - test/diagnostics/player_error_report_bridge_test.dart(PlayerError 经桥注入先例)
    - test/diagnostics/error_reporter_test.dart:16 用例 'normalizes framework, bootstrap, platform, and player inputs'(reporter 层四源归一化先例;整合用例要把同样的四源打到端到端)
    - test/widget/player/error_card_host_test.dart(ErrorCardHost + ErrorReporter 注入 seam 组装先例,'accepts a report and shows the card at the top-left')
    - test/diagnostics/error_log_file_sink_test.dart(temp 目录 FileSink 文件证据断言先例)
    - lib/kernel/diagnostics/global_error_hooks.dart / player_error_report_bridge.dart / error_reporter.dart(注入入口与公开 intake 签名)
  </read_first>
  <action>
    先行步骤(step 0,机械化基线鉴别):跑全量 `flutter test --machine`,把失败用例的完整名称(suite 路径#完整用例名,每行一条、排序)提取写入 `.planning/phases/05-e2e-resilience-verification/headless-baseline.txt`——此文件随 plan 目录入库,作为本 phase 的基线快照(预存失败如 ~57 项 mdk.dll FFI 加载失败属预存,见 MEMORY)。提取命令(可直接复用):
    `flutter test --machine | node -e "const fs=require('fs');let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{const S=new Map(),T=new Map(),out=new Set();for(const l of s.split(/\r?\n/)){if(!l.trim())continue;let e;try{e=JSON.parse(l)}catch(err){continue}if(e.type==='suite'&&e.suite&&e.suite.path)S.set(e.suite.id,e.suite.path);if(e.type==='testStart'&&e.test)T.set(e.test.id,e);if(e.type==='testDone'&&e.result!=='success'&&!e.hidden){const t=T.get(e.testID);if(t)out.add((S.get(t.test.suiteID)||'')+'#'+t.test.name);}}fs.writeFileSync(process.argv[1],[...out].sort().join('\n')+(out.size?'\n':''));console.log('headless baseline written: '+out.size+' failed test(s)');});" .planning/phases/05-e2e-resilience-verification/headless-baseline.txt`
    过滤口径:type 为 testDone 且 result 非 success 且 hidden 非真的事件视为失败;名称由 testStart 事件的 test.name 与 suite 事件的 suite.path 拼接为 `路径#用例名`。若本次 headless 基线未复现(如 Phase 4 已观察到 mdk.dll 不再触发),写出空基线文件并在 done 注明「当前环境零基线失败」——机制对空/非空两种情况都成立。基线文件一经写出不得手工增删行,Task 3 门禁以它为准。

    创建 test/diagnostics/end_to_end_injection_test.dart(文件名循 D-01 建议):一个 testWidgets 组,经真实链路逐源注入——每源一次注入调用 → 断言三件套(reporter 恰增一份新报告、诊断文件新增对应记录、ErrorCardHost 呈现 ErrorCard 且摘要可见)。四个注入入口按既有先例:
    源1 FlutterError.onError(框架异常):经 GlobalErrorHooks 安装的框架回调注入 FlutterError;
    源2 PlatformDispatcher.onError(异步未捕获):dispatcher delegate 回调注入(先例 'forwards exact dispatcher error and stack then returns true');
    源3 runZonedGuarded 启动兜底:bootstrap zone handler 注入(先例 'delegates an initialized bootstrap error once');
    源4 PlayerError:经 PlayerErrorReportBridge 注入一个 FileError(先例 player_error_report_bridge_test)。
    断言口径:四源消息互异以避开 10s 去重窗(每源恰一份报告);文件证据用 temp 目录 FileSink/诊断 seam(每源一行新记录,内容含该源标识);卡片断言 toggle 开启态(SET-01 默认开,门控语义已由 host 回归锁锁定,不在本用例重复)。
    遵守既有测试惯例:setUp 走 KernelLoggerImpl.resetForTesting + 注入 seam(test hooks);teardown 遵守 04-04 真实 I/O 协议(禁在 teardown await plain async fn,dispose 用 fire-and-forget;真实 I/O 断言锚定文件落点)。中文双语 doc comment,AAA 结构。
    若盘点中发现既有用例已完整覆盖"四源各产报告+文件证据+卡片"端到端(预盘点结论:不存在——error_reporter_test.dart:16 仅 reporter 层归一化,无文件+卡片断言),则按 D-01 Discretion 降级为引用既有并在 Task 3 映射表记录,不重复造轮子。
  </action>
  <verify>
    <automated>flutter test test/diagnostics/end_to_end_injection_test.dart && flutter analyze</automated>
    <fails_when>整合用例红:任一源注入后 reporter 未增报告/诊断文件无新记录/卡片未呈现;或 analyze 出现 error</fails_when>
  </verify>
  <acceptance_criteria>
    - `flutter test test/diagnostics/end_to_end_injection_test.dart` 全绿
    - grep 断言:文件内含四个源的注入用例(每源 ≥1 test/testWidgets),且用例体含报告、文件、卡片三类断言(`grep -c "expect(" test/diagnostics/end_to_end_injection_test.dart` ≥ 8)
    - `flutter analyze` 0 error
    - `git diff --name-only -- lib/ windows/ pubspec.yaml` 输出为空(产品零 diff)
  </acceptance_criteria>
  <done>四源各一次注入经真实链各产一份报告+一条文件证据+一张卡片,全部断言绿;headless-baseline.txt 已写出(空文件时注明「当前环境零基线失败」)</done>
</task>

<task type="auto">
  <name>Task 2: 爆发压测补差与关闭失败隔离补差(VER-02/VER-03, per D-03/D-04)</name>
  <files>test/diagnostics/burst_resilience_test.dart</files>
  <read_first>
    - test/diagnostics/error_reporter_test.dart:291 用例 'keeps 100 and 1000 duplicate bursts bounded with accumulated counts'(既有 duplicate burst 主证据——归档引用,不重写);:156 'evicts the sixth distinct head...'(FIFO ≤5 单点先例);:743 'isolates listener and effect failures while suppressing reentrant intake'(reentrancy 先例)
    - test/diagnostics/isolated_error_log_sink_test.dart + error_log_file_sink_test.dart(写盘受控/顺序/失败隔离先例)
    - test/widget/player/error_card_test.dart:746 'unmocked clipboard channel leaves card intact with no crash'(复制失败隔离既有证据);:251 'close button dismisses current and advances the FIFO'(关闭推进先例)
    - lib/kernel/diagnostics/error_reporter.dart(occurrenceCount、队列边界、effect seam 可观察面)
    - lib/kernel/diagnostics/isolated_error_log_sink.dart(单写者链)
  </read_first>
  <action>
    创建 test/diagnostics/burst_resilience_test.dart,补既有覆盖与 D-03/D-04 口径之间的差距(fake_async/WidgetTester 驱动 100–1000 合成事件;pubspec 已有 fake_async 1.3.3)。三组缺口用例:
    用例 A 混合爆发:注入 100–1000 条互异合成事件,断言 FIFO 有界 ≤5(设计值)、被逐出报告不再在队列、合并报告的 occurrenceCount 可见、无 unhandled error(FlutterError.onError/zone 未捕获计数为零口径,循既有 fault isolation 断言式)。
    用例 B 爆发下写盘受控:temp 目录真实 seam 激活后驱动爆发,断言单写者链不断——error.log 最终内容完整有序、无交错损坏行、无并发写冲突错误(循 isolated_error_log_sink_test 先例)。
    用例 C pump 响应不卡:爆发后预排定的 timer/microtask 回调在测试预算内准时完成("播放控制仍响应"的自动化代理;实机面由 03-UAT Test 1 归档佐证)。
    条件补差(VER-03 letter):盘点"关闭推进过程中 effect 失败不产生第二错误"是否有用例直接覆盖——reporter 层 reentrancy 用例(:743)已覆盖任意 effect 失败,但若卡片关闭→队列推进路径无用例显式锁死,循 :743 模式补一条"close-advance 触发 failing effect 不抛第二错误"用例;若确认已覆盖则在用例 A 文件头注释中引用既有用例名即可,不重复造轮子(D-01 同款口径)。
    不另设 profile/内存曲线阈值(D-04 明确排除,那属后端优化轮——Deferred Ideas)。
  </action>
  <verify>
    <automated>flutter test test/diagnostics/burst_resilience_test.dart && flutter analyze</automated>
    <fails_when>爆发用例红:FIFO 超 5/合并计数不可见/出现 unhandled error/写盘行交错损坏/pump 超时卡死;或 analyze 出现 error</fails_when>
  </verify>
  <acceptance_criteria>
    - `flutter test test/diagnostics/burst_resilience_test.dart` 全绿
    - 用例 A/B/C 三组断言齐备(D-03 口径清单逐项有对应 expect)
    - `flutter analyze` 0 error;`git diff --name-only -- lib/ windows/ pubspec.yaml` 输出为空
    - VER-03 关闭失败隔离:或补差用例存在,或文件头注释引用既有覆盖用例名(二选一,不允缺失)
  </acceptance_criteria>
  <done>100–1000 混合爆发在设计值口径下有界、合并可见、写盘受控、pump 不卡;关闭失败隔离或有补差用例或有既有引用</done>
</task>

<task type="auto">
  <name>Task 3: 证据归档映射表 + VER-05 开发者文档 + Phase 收口门禁(VER-01~05 全归档, per D-02/D-05)</name>
  <files>.planning/phases/05-e2e-resilience-verification/05-EVIDENCE-MAP.md, docs/error-diagnostics-limitations.md</files>
  <read_first>
    - .planning/phases/03-playback-error-card-bridge/03-UAT.md(VER-04 归档源:Test 1 卡片显示期间宿主交互 6 项全过/Test 2 位置/Test 3 F1/Test 4 注入入口移除)
    - .planning/phases/04-error-feedback-settings/04-UAT.md(VER-04 归档源:Test 1 开关/Test 2 持久化/Test 3、5 路径配置移除/Test 4 MSIX ACL/Test 6 快速并发)
    - .planning/REQUIREMENTS.md(VER-01~05 验收原文口径)
    - test/diagnostics/ 与 test/widget/player/ 目录清单 + Task 1/2 产出文件(Task 1 盘点记录)
    - lib/kernel/diagnostics/isolated_error_log_sink.dart + isolated_error_log_sink_worker.dart(isolate 写盘语义事实源)、source_line_reader.dart + error_log_location.dart(release 降级事实源)、error_reporter.dart(单写者链事实源)
  </read_first>
  <action>
    一、创建 .planning/phases/05-e2e-resilience-verification/05-EVIDENCE-MAP.md(git-tracked 归档映射表):表格 VER-01~05 逐项列出 证据类型(自动化测试/UAT 实机/文档)→ 引用(格式 `测试文件#用例名` 或 `UAT 文档#Test N`,可 grep 定位)→ 归档状态。内容口径:
    VER-01 = Task 1 整合用例(主证据)+ error_reporter_test.dart:16 归一化先例 + host/card 呈现用例;
    VER-02 = error_reporter_test.dart:291 既有 100/1000 duplicate burst(主证据)+ Task 2 用例 A/B/C(补差);
    VER-03 = global_error_hooks_test.dart zone 用例组 + error_reporter_test.dart:721/:743 reentrancy + error_card_test.dart:746 复制失败隔离 + 关闭失败隔离(引用 Task 2 结论);
    VER-04 = 03-UAT Test 1–4 + 04-UAT Test 1–6 正式归档(D-02:不重跑);盘点移除后未覆盖点(注入入口 G-03-4 已用后即撤、路径配置 SET-02 已修订移除——预 count:无已知缺口,若盘点有新发现列入实机核对清单附于表中);
    VER-05 = docs/error-diagnostics-limitations.md 文件引用 + 四章节清单。
    表尾附 Phase 收口记录:全门禁结果 + 产品零 diff 断言结果(START commit hash 记录在案)。
    二、创建 docs/error-diagnostics-limitations.md(VER-05, D-05;中文双语,开发者自助排障向),四章:
    1) release 源码/符号降级策略——release 读不到源码,source_line_reader 优雅降级为仅定位文本不报错不闪退(LOC-02),debug/profile 才有源码行;
    2) Dart 钩子边界——FlutterError.onError/PlatformDispatcher.onError/runZonedGuarded 三钩子不覆盖 libmpv/FFI 原生进程崩溃(access violation 等),此类崩溃属 WER LocalDumps 工具域(与 REQUIREMENTS Out of Scope 口径一致);
    3) isolate 写盘语义与卡死时间窗读数方法——isolated_error_log_sink_worker 的写盘语义、诊断日志"日志不可用"状态含义、如何从日志时间戳读出卡死时间窗;
    4) Windows WER LocalDumps 注册表配置建议——零代码原生崩溃兜底:注册表键位、DumpFolder/DumpType/DumpCount 建议值、与本项目 error.log 的关系(互不替代)。
    文档不含密钥/敏感本机信息,仅公开机制与注册表路径。
    三、Phase 收口门禁:机械化基线鉴别——重跑全量 `flutter test --machine`,经 node 一行 diff 脚本对照 Task 1 step 0 写出的 headless-baseline.txt:diff 出基线外新增失败用例即非零退出(门红);零新增失败即门绿(基线内预存失败不阻塞)。完整命令见 <verify><automated>。其余门禁:`flutter analyze` 0 error、`bash tool/audit/kernel_logger_gate.sh` 过、产品零 diff 复核。
  </action>
  <verify>
    <automated>flutter analyze && flutter test --machine | node -e "const fs=require('fs');let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{const B=new Set(fs.readFileSync(process.argv[1],'utf8').split(/\r?\n/).filter(Boolean));const S=new Map(),T=new Map(),bad=[];for(const l of s.split(/\r?\n/)){if(!l.trim())continue;let e;try{e=JSON.parse(l)}catch(err){continue}if(e.type==='suite'&&e.suite&&e.suite.path)S.set(e.suite.id,e.suite.path);if(e.type==='testStart'&&e.test)T.set(e.test.id,e);if(e.type==='testDone'&&e.result!=='success'&&!e.hidden){const t=T.get(e.testID);if(!t)continue;const f=(S.get(t.test.suiteID)||'')+'#'+t.test.name;if(!B.has(f))bad.push(f);}}if(bad.length){console.error('FAIL: new failures not in headless baseline:');for(const n of bad)console.error('  '+n);process.exit(1);}console.log('PASS: no new failures beyond headless baseline ('+B.size+' baseline entries)');});" .planning/phases/05-e2e-resilience-verification/headless-baseline.txt && bash tool/audit/kernel_logger_gate.sh && test -f .planning/phases/05-e2e-resilience-verification/05-EVIDENCE-MAP.md && test -f docs/error-diagnostics-limitations.md</automated>
    <fails_when>任一门禁失败:analyze error/全量 machine 跑 diff 出基线外新增失败用例(node 脚本非零退出即门红)/kernel_logger_gate 红/两个归档文件任一缺失</fails_when>
  </verify>
  <acceptance_criteria>
    - 05-EVIDENCE-MAP.md 存在且 VER-01~05 各 ≥1 条可 grep 定位的证据引用;VER-04 行含 03/04 UAT 的 Test 编号引用
    - docs/error-diagnostics-limitations.md 存在且含四章节(release 降级/Dart 钩子边界/isolate 写盘/WER LocalDumps),中文双语
    - `flutter analyze` 0 error;machine 跑 diff 脚本退出码 0(基线外新增失败为零,headless-baseline.txt 内预存失败不阻塞);`bash tool/audit/kernel_logger_gate.sh` 退出码 0
    - `git diff --name-only <START>..HEAD -- lib/ windows/ pubspec.yaml` 输出为空(<START> = Task 1 开始前记录的 commit hash)
  </acceptance_criteria>
  <done>五个 VER 项全部有归档证据指向,VER-05 文档四章节齐备,全门禁绿,产品实现零 diff 被断言证明</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

本 phase 无新产品代码、无新信任边界——唯一触碰面为测试文件(.planning/、test/)与文档(docs/)。

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | 新测试文件断言放宽掩盖回归 | low | mitigate | 断言锁 D-03/D-04 设计值口径 + Task 3 全门禁复跑(analyze/test/gate) |
| T-05-02 | Information Disclosure | docs 文档泄露敏感信息 | low | accept | 文档仅含公开机制与本机注册表路径建议,无密钥无个人路径 |
| T-05-SC | Tampering | npm/pip/cargo 安装 | n/a | accept | 本 phase 零包安装;pubspec.yaml 零 diff 断言同时锁死依赖面 |
</threat_model>

<verification>
- VER-01~05 在 05-EVIDENCE-MAP.md 各有可 grep 定位的归档证据(映射完整性 = must_haves 核心断言)
- Task 1 整合用例 + Task 2 爆发补差用例全绿;全量 test 无基线外新增失败
- `flutter analyze` 0 error;`bash tool/audit/kernel_logger_gate.sh` 过
- 产品实现零 diff:`git diff --name-only <START>..HEAD -- lib/ windows/ pubspec.yaml` 为空
</verification>

<success_criteria>
1. 四源端到端整合注入用例存在且全绿——每源一次注入产一份报告+一条文件证据+一张卡片(VER-01/D-01)
2. 100–1000 爆发在设计值口径下有界、合并可见、写盘受控、pump 不卡;既有 duplicate burst 主证据归档引用(VER-02/D-03/D-04)
3. zone 一致性/reentrancy/复制与关闭失败隔离全部有归档证据指向,缺口处已补差(VER-03/D-01)
4. VER-04 以 03/04 UAT 实机记录正式归档,不重跑;移除后未覆盖点盘点完成(VER-04/D-02)
5. VER-05 开发者文档落 docs/,四章节覆盖 release 降级/原生崩溃边界/isolate 写盘/WER LocalDumps(VER-05/D-05)
6. 产品实现零 diff(lib/ windows/ pubspec.yaml),质量三门禁全绿
</success_criteria>

<output>
Create `.planning/phases/05-e2e-resilience-verification/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Location | Tracked | Serves |
|----------|----------|---------|--------|
| 四源端到端整合注入用例(tracer) | `test/diagnostics/end_to_end_injection_test.dart` | test/ (git) | VER-01 (D-01) |
| 爆发压测补差 + 关闭失败隔离补差用例 | `test/diagnostics/burst_resilience_test.dart` | test/ (git) | VER-02/VER-03 (D-03/D-04) |
| VER-01~05 证据映射表 | `.planning/phases/05-e2e-resilience-verification/05-EVIDENCE-MAP.md` | .planning/ (git) | 全部 VER 项归档 |
| VER-05 开发者文档 | `docs/error-diagnostics-limitations.md` | docs/ (git) | VER-05 (D-05) |
| Plan 执行摘要 | `.planning/phases/05-e2e-resilience-verification/05-01-SUMMARY.md` | .planning/ (git) | 执行留痕 |
