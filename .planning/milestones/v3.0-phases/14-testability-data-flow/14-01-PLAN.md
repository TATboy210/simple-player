---
phase: 14-testability-data-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/widget/settings/shortcuts_tab_test.dart
  - test/kernel/persistence/settings_import_export_test.dart
  - test/widget/player/player_screen_test.dart
  - test/widget/settings/general_equalizer_tab_test.dart
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-04

must_haves:
  truths:
    - "flutter analyze reports 0 issues (currently 17)"
    - "All 1135 tests pass (currently 1131 pass, 4 fail)"
    - "shortcuts_tab_test.dart 4 failures are fixed"
  artifacts:
    - "test/widget/settings/shortcuts_tab_test.dart — fixed and passing"
    - "Clean flutter analyze output"
  key_links:
    - "shortcuts_tab_test.dart → ShortcutsTab widget → must match current API"
---

<objective>
Fix all pre-existing test failures and analysis warnings to establish a clean baseline for Phase 14 testability work.

Purpose: Before adding new tests, the existing test suite must be 100% green and flutter analyze must report 0 issues. Currently 4 tests fail in shortcuts_tab_test.dart and 17 analysis warnings exist.
Output: All tests passing, flutter analyze clean (0 issues).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-testability-data-flow/RESEARCH.md

test/widget/settings/shortcuts_tab_test.dart
test/kernel/persistence/settings_import_export_test.dart
test/widget/player/player_screen_test.dart
test/widget/settings/general_equalizer_tab_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 4 pre-existing test failures in shortcuts_tab_test.dart</name>
  <files>test/widget/settings/shortcuts_tab_test.dart</files>
  <action>
Investigate and fix the 4 failing tests in shortcuts_tab_test.dart:
- "ShortcutsTab preserves keyboard listener structure"
- "ShortcutsTab renders SectionHeader"
- "ShortcutsTab renders shortcut rows"
- "ShortcutsTab renders with GlassContainer"

Steps:
1. Read the current ShortcutsTab widget source to understand its actual API
2. Read the failing test file to understand what it expects
3. Fix the tests to match the current widget implementation (not the other way around — the widget is the source of truth)
4. If the widget API changed (e.g., different constructor params, different child widgets), update tests accordingly
5. Run `flutter test test/widget/settings/shortcuts_tab_test.dart` to verify all pass
  </action>
  <verify>
    <automated>flutter test test/widget/settings/shortcuts_tab_test.dart</automated>
  </verify>
  <done>All 4 previously-failing tests in shortcuts_tab_test.dart pass. No regressions in other tests.</done>
</task>

<task type="auto">
  <name>Task 2: Fix 17 flutter analyze warnings/infos to 0 issues</name>
  <files>
    test/kernel/persistence/settings_import_export_test.dart
    test/widget/player/player_screen_test.dart
    test/widget/settings/general_equalizer_tab_test.dart
  </files>
  <action>
Fix all 17 analysis issues reported by `flutter analyze`:

**settings_import_export_test.dart (2 issues):**
- Line 88: `strict_raw_type` — Add explicit type arguments to `Map<dynamic, dynamic>`
- Line 344: `prefer_const_constructors` — Add `const` keyword to constructor invocation

**player_screen_test.dart (3 issues):**
- Lines 84, 102, 254: `unawaited_futures` — Add `await` or wrap with `unawaited()` for Future expressions

**general_equalizer_tab_test.dart (3 issues):**
- Lines 25, 37, 55: `prefer_const_constructors` — Add `const` keyword to constructor invocations

The remaining 9 issues (from the 17 total) may be in other files — run `flutter analyze` after fixing the above to identify any remaining issues and fix them too.

Goal: `flutter analyze` reports 0 issues total.
  </action>
  <verify>
    <automated>flutter analyze 2>&1 | grep -E "^[0-9]+ issues" || echo "No issues found"</automated>
  </verify>
  <done>flutter analyze reports 0 issues. All existing tests still pass.</done>
</task>

</tasks>

<verification>
```bash
# All tests pass (0 failures)
flutter test 2>&1 | tail -3

# Zero analysis issues
flutter analyze 2>&1 | tail -3
```
</verification>

<success_criteria>
1. `flutter test` reports 0 failures (currently 4)
2. `flutter analyze` reports 0 issues (currently 17)
3. No regressions — all 1131 previously-passing tests still pass
</success_criteria>

<output>
Create `.planning/phases/14-testability-data-flow/14-01-SUMMARY.md` when done
</output>
