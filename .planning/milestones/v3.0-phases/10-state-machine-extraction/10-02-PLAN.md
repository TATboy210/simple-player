---
phase: 10-state-machine-extraction
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - lib/kernel/engine/fvp_engine.dart
  - lib/kernel/engine/fvp_callback_handler.dart
  - lib/kernel/engine/track_manager.dart
  - lib/kernel/engine/subtitle_configurator.dart
  - lib/kernel/engine/video_effect_controller.dart
  - lib/kernel/engine/d3d11_configurator.dart
  - lib/kernel/engine/volume_controller.dart
  - lib/kernel/engine/media_engine.dart
  - lib/kernel/engine/volume_control.dart
  - test/helpers/fake_engine.dart
  - test/kernel/engine/fvp_callback_handler_test.dart
autonomous: true
requirements: [ENG-02, SVC-02]
must_haves:
  truths:
    - FvpEngine 从 632 行减至 <350 行 (wc -l verified 2026-07-14, ROADMAP stale 641)
    - 所有 helper 实现对应 ISP 接口
    - FvpEngine 暴露接口 getter 替代 delegation 方法
    - FvpEngine 使用 EngineStateMachine 管理状态转换
    - FvpCallbackHandler 通过 stateMachine.transitionTo 更新状态
    - togglePlayPause/skipForward/skipBack/setRange 从 FvpEngine 瘦身
  artifacts:
    - lib/kernel/engine/volume_control.dart
    - lib/kernel/engine/fvp_engine.dart (<350 行)
  key_links:
    - FvpEngine.factory 创建 EngineStateMachine 并注入 FvpCallbackHandler
    - 所有 state.value 赋值通过 stateMachine.transitionTo
    - interface getter 返回 helper 实例（相同类型）
---

<objective>
将 FvpEngine 从 632 行精简至 <350 行，helper 实现对应 ISP 接口，FvpEngine 暴露接口 getter
替代 delegation 方法。集成 EngineStateMachine 管理所有状态转换。

Purpose: 完成 ENG-02 需求 — FvpEngine 只保留 open/play/pause/stop/seekTo + 状态调用 +
dispose + ValueNotifier 字段 + 工厂构造函数。helper 通过接口 getter 暴露。
Output: fvp_engine.dart <350 行, volume_control.dart 新接口, 所有 helper implements 对应接口。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-state-machine-extraction/10-CONTEXT.md
@.planning/phases/10-state-machine-extraction/10-RESEARCH.md
@.planning/phases/10-state-machine-extraction/10-01-SUMMARY.md
@lib/kernel/engine/fvp_engine.dart
@lib/kernel/engine/engine_state_machine.dart
@lib/kernel/engine/engine_state.dart
@lib/kernel/engine/playback_control.dart
@lib/kernel/engine/engine_state_view.dart
@lib/kernel/engine/track_control.dart
@lib/kernel/engine/subtitle_config.dart
@lib/kernel/engine/video_effect_control.dart
@lib/kernel/engine/renderer_control.dart
@lib/kernel/engine/media_engine.dart
@lib/kernel/engine/track_manager.dart
@lib/kernel/engine/subtitle_configurator.dart
@lib/kernel/engine/video_effect_controller.dart
@lib/kernel/engine/d3d11_configurator.dart
@lib/kernel/engine/volume_controller.dart
@lib/kernel/engine/fvp_callback_handler.dart
@lib/kernel/engine/player_proxy.dart
@lib/kernel/engine/media_state.dart
@test/helpers/fake_engine.dart
@test/kernel/engine/fvp_callback_handler_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper implements 接口 + VolumeControl 新接口</name>
  <files>
    lib/kernel/engine/volume_control.dart,
    lib/kernel/engine/track_manager.dart,
    lib/kernel/engine/subtitle_configurator.dart,
    lib/kernel/engine/video_effect_controller.dart,
    lib/kernel/engine/d3d11_configurator.dart,
    lib/kernel/engine/volume_controller.dart,
    lib/kernel/engine/media_engine.dart,
    lib/kernel/engine/engine_state.dart
  </files>
  <read_first>
    lib/kernel/engine/track_control.dart
    lib/kernel/engine/subtitle_config.dart
    lib/kernel/engine/video_effect_control.dart
    lib/kernel/engine/renderer_control.dart
    lib/kernel/engine/media_engine.dart
    lib/kernel/engine/track_manager.dart
    lib/kernel/engine/subtitle_configurator.dart
    lib/kernel/engine/video_effect_controller.dart
    lib/kernel/engine/d3d11_configurator.dart
    lib/kernel/engine/volume_controller.dart
    lib/kernel/engine/engine_state.dart
  </read_first>
  <action>
创建 `lib/kernel/engine/volume_control.dart` (per D-11):
- abstract class VolumeControl
- 方法: `void setVolume(double value)`, `void setMute(bool mute)`
- getter: `ValueNotifier<double> get volume`, `ValueNotifier<bool> get isMuted`

修改 `lib/kernel/engine/track_manager.dart` (per D-06):
- 添加 `import 'track_control.dart';`
- 类声明改为 `class TrackManager implements TrackControl`
- 现有方法签名已匹配 TrackControl 接口，只需添加 `@override` 注解:
  - `@override List<AudioTrackInfo> getAudioTracks()`
  - `@override void switchAudioTrack(int trackIndex)`
  - `@override List<int> get activeAudioTracks`

修改 `lib/kernel/engine/subtitle_configurator.dart` (per D-06):
- 添加 `import 'subtitle_config.dart';` 和 `import 'package:flutter/foundation.dart';`
- 类声明改为 `class SubtitleConfigurator implements SubtitleConfig`
- 现有方法签名已匹配，只需添加 `@override` 注解:
  - `@override void setExternalSubtitle(String path)`
  - `@override void setSubtitleDelay(int milliseconds)`
  - `@override int getSubtitleDelay()`
  - `@override void setEqualizer(String afFilter)`
  - `@override List<SubtitleTrackInfo> getSubtitleTracks()` — 需要添加此方法,
    委托给注入的 TrackManager 或返回空列表。实际上 SubtitleConfigurator 没有
    getSubtitleTracks/switchSubtitleTrack/toggleSubtitle — 这些在 TrackManager 中。
    所以 SubtitleConfigurator 只 implements SubtitleConfig 的部分方法。
    方案: SubtitleConfigurator 只实现它已有的方法，不添加不存在的方法。
    调用者通过 SubtitleConfig 接口访问时，getSubtitleTracks 等方法由 FvpEngine 的
    interface getter 返回的 SubtitleConfigurator 实例提供 — 但 SubtitleConfigurator
    没有这些方法。

    **重新审视:** SubtitleConfig 接口包含 getSubtitleTracks/switchSubtitleTrack/toggleSubtitle，
    但这些方法在 TrackManager 中，不在 SubtitleConfigurator 中。
    这是 Phase 9 接口设计的一部分 — SubtitleConfig 包含了字幕轨道管理。

    方案: SubtitleConfigurator 不 implements SubtitleConfig（它只实现字幕配置部分）。
    FvpEngine 的 subtitleConfig getter 返回一个组合对象或 FvpEngine 自身。

    **最终方案:** SubtitleConfigurator 只 implements SubtitleConfig 的配置方法
    （setExternalSubtitle, setSubtitleDelay, getSubtitleDelay, setEqualizer）。
    getSubtitleTracks/switchSubtitleTrack/toggleSubtitle 由 FvpEngine 直接实现
    （委托给 TrackManager）。FvpEngine 的 subtitleConfig getter 返回 FvpEngine 自身
    （因为 FvpEngine implements SubtitleConfig）。

    简化: SubtitleConfigurator 不改 implements，保持原样。FvpEngine 实现 SubtitleConfig
    的全部方法（部分委托给 SubtitleConfigurator，部分委托给 TrackManager）。

修改 `lib/kernel/engine/video_effect_controller.dart` (per D-06):
- 添加 `import 'video_effect_control.dart';` 和 `import 'package:flutter/foundation.dart';`
- 类声明改为 `class VideoEffectController implements VideoEffectControl`
- 添加 `@override` 注解到现有方法
- 不添加 aspectRatio getter（VideoEffectController 不拥有 aspectRatio ValueNotifier）

修改 `lib/kernel/engine/d3d11_configurator.dart` (per D-06):
- 添加 `import 'renderer_control.dart';`
- 类声明改为 `class D3D11Configurator implements RendererControl`
- 添加 `@override` 注解到现有方法

修改 `lib/kernel/engine/volume_controller.dart` (per D-11):
- 添加 `import 'volume_control.dart';`
- 类声明改为 `VolumeController implements VolumeControl`
- 添加 `@override` 注解到 setVolume/setMute
- volume 和 isMuted getter 已存在（字段），添加 `@override` 注解

修改 `lib/kernel/engine/media_engine.dart`:
- 添加 `import 'volume_control.dart';`
- MediaEngine implements 列表添加 `VolumeControl`

修改 `lib/kernel/engine/engine_state.dart`:
- 添加 `export 'volume_control.dart';`

关键约束:
- SubtitleConfigurator 不改 implements SubtitleConfig（它没有 getSubtitleTracks 等方法）
- VideoEffectController 不添加 aspectRatio getter（不拥有该 ValueNotifier）
- 所有 `@override` 注解确保编译期检查接口实现完整性
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/engine/</automated>
  </verify>
  <acceptance_criteria>
    - volume_control.dart 存在且包含 VolumeControl abstract class
    - TrackManager implements TrackControl (编译通过)
    - VideoEffectController implements VideoEffectControl (编译通过)
    - D3D11Configurator implements RendererControl (编译通过)
    - VolumeController implements VolumeControl (编译通过)
    - MediaEngine implements 列表包含 VolumeControl
    - engine_state.dart barrel export 包含 volume_control.dart
    - flutter analyze lib/kernel/engine/ 无错误
  </acceptance_criteria>
  <done>
    所有 helper 实现对应 ISP 接口（SubtitleConfigurator 除外，因为它不拥有字幕轨道方法）。
    VolumeControl 接口已创建。MediaEngine 组合接口包含 VolumeControl。
  </done>
</task>

<task type="auto">
  <name>Task 2: FvpEngine 瘦身 — 集成状态机 + 接口 getter + 删除 delegation</name>
  <files>
    lib/kernel/engine/fvp_engine.dart,
    lib/kernel/engine/fvp_callback_handler.dart,
    test/helpers/fake_engine.dart,
    test/kernel/engine/fvp_callback_handler_test.dart
  </files>
  <read_first>
    lib/kernel/engine/fvp_engine.dart
    lib/kernel/engine/engine_state_machine.dart
    lib/kernel/engine/fvp_callback_handler.dart
    lib/kernel/engine/media_state.dart
    lib/kernel/engine/playback_control.dart
    lib/kernel/engine/engine_state_view.dart
    lib/kernel/engine/volume_control.dart
    test/helpers/fake_engine.dart
    test/kernel/engine/fvp_callback_handler_test.dart
  </read_first>
  <action>
**A. FvpEngine 集成 EngineStateMachine (per D-02/D-07/D-08/D-09):**

修改 `lib/kernel/engine/fvp_engine.dart`:

1. 添加 import: `import 'engine_state_machine.dart';`
2. 添加字段: `late final EngineStateMachine stateMachine;`
3. 修改工厂构造函数:
   - 创建 stateMachine: `final sm = EngineStateMachine(
     onPlay: () {}, // 占位，稍后替换为 engine.play
     onPause: () {}, // 占位，稍后替换为 engine.pause
   );`
   - 修改 FvpCallbackHandler 构造: `state: sm.state, isBuffering: sm.isBuffering`
   - 在 engine 创建后设置回调: `sm.onPlay = engine.play; sm.onPause = engine.pause;`
   - 注释说明为什么需要延迟设置（循环依赖）

4. 修改 ValueNotifier 字段:
   - 删除 `state` 字段, 改为 getter: `@override ValueNotifier<MediaState> get state => stateMachine.state;`
   - 删除 `isSeeking` 字段, 改为 getter: `@override ValueNotifier<bool> get isSeeking => stateMachine.isSeeking;`
   - 删除 `isBuffering` 字段, 改为 getter: `@override ValueNotifier<bool> get isBuffering => stateMachine.isBuffering;`

5. 删除 `_safeSetState` 方法，替换所有调用为 `stateMachine.transitionTo(next, 'caller')`:
   - open() 中的 _safeSetState 调用 -> stateMachine.transitionTo
   - play() 中的 _safeSetState 调用 -> stateMachine.transitionTo
   - pause() 中的 _safeSetState 调用 -> stateMachine.transitionTo
   - stop() 中的 _safeSetState 调用 -> stateMachine.transitionTo
   - seekTo() 中的 _safeSetState 调用 -> stateMachine.transitionTo

6. 添加接口 getter (per D-07):
   - `@override TrackControl get trackControl => _trackManager;`
   - `@override VideoEffectControl get videoEffectControl => _videoEffectController;`
   - `@override RendererControl get rendererControl => _d3d11Configurator;`
   - `@override VolumeControl get volumeControl => _volumeController;`
   - `@override SubtitleConfig get subtitleConfig => this;` (FvpEngine 自身实现 SubtitleConfig)
   注意: subtitleConfig 返回 this 因为 FvpEngine 实现了 SubtitleConfig 的全部方法

7. 删除 delegation 方法 (~200 行):
   - 删除 getAudioTracks/switchAudioTrack/activeAudioTracks (委托给 _trackManager)
   - 删除 getSubtitleTracks/switchSubtitleTrack/toggleSubtitle (委托给 _trackManager)
   - 删除 setExternalSubtitle/setSubtitleDelay/subtitleDelay/setEqualizer (委托给 _subtitleConfigurator)
   - 删除 setVideoEffect/rotate/setAspectRatio/setDeinterlace (委托给 _videoEffectController)
   - 删除 setD3d11SyncEnabled/setHardwareDecoding (委托给 _d3d11Configurator)
   - 删除 setVolume/setMute (委托给 _volumeController)
   注意: 这些方法的调用者改为通过 interface getter 访问:
   `engine.getAudioTracks()` -> `engine.trackControl.getAudioTracks()`

8. 修改 togglePlayPause (per D-09):
   - 删除 FvpEngine.togglePlayPause 方法
   - 改为: `@override void togglePlayPause() => stateMachine.togglePlayPause();`
   注意: FvpEngine 仍实现 PlaybackControl 接口，togglePlayPause 是接口方法

9. 修改 skipForward/skipBack/setRange (per D-10):
   - skipForward: 保留在 FvpEngine，使用 _guardedAction 包装
   - skipBack: 保留在 FvpEngine，使用 _guardedAction 包装
   - setRange: 保留在 FvpEngine（需要 _player.setRange + _guardedAction）
   注意: D-10 说移至 mixin，但 FvpEngine 仍需实现 PlaybackControl 接口。
   最终: FvpEngine 保留这些方法的实现（它们是 PlaybackControl 接口的一部分）。

10. 修改 dispose:
    - 添加 stateMachine.dispose() 调用
    - 删除 state.dispose()（state 现在是 getter，由 stateMachine 管理生命周期）
    - 删除 isSeeking.dispose() 和 isBuffering.dispose()（同理）

11. 修改 _guardedAction 中的错误处理:
    - 保持不变（_guardedAction 使用 lastError.value 而非 state）

12. 添加 FvpEngine implements SubtitleConfig:
    - `class FvpEngine implements MediaEngine, SubtitleConfig`
    - 或者 SubtitleConfig 已在 MediaEngine 的 implements 列表中？
    检查: MediaEngine implements EngineStateView, PlaybackControl, TrackControl,
    SubtitleConfig, VideoEffectControl, RendererControl — SubtitleConfig 已在其中。
    所以 FvpEngine 只需 `implements MediaEngine` 即可。
    但 FvpEngine 的 subtitleConfig getter 返回 this，这意味着 FvpEngine 需要
    实现 SubtitleConfig 的所有方法。检查现有方法:
    - getSubtitleTracks -> 委托给 _trackManager (需保留或删除后通过 getter)
    - switchSubtitleTrack -> 同上
    - toggleSubtitle -> 同上
    - setExternalSubtitle -> 委托给 _subtitleConfigurator
    - setSubtitleDelay -> 同上
    - getSubtitleDelay -> 同上
    - setEqualizer -> 同上
    - subtitleText getter -> 已有
    - subtitleDelay getter -> 已有

    如果删除 getSubtitleTracks/switchSubtitleTrack/toggleSubtitle 的 delegation，
    这些方法在 FvpEngine 中不存在，subtitleConfig 返回 this 就不满足 SubtitleConfig 接口。

    **解决方案:** 保留这3个方法在 FvpEngine 中（它们是 SubtitleConfig 接口的一部分），
    委托给 _trackManager。这不是 delegation 方法的"删除"，而是接口实现。
    真正删除的是 subtitleConfig 相关的配置方法 delegation（setExternalSubtitle 等），
    因为调用者改为通过 `engine.subtitleConfig.setExternalSubtitle()` 访问。

    **重新定义 delegation 方法删除范围:**
    删除的是"调用者可以通过 interface getter 访问的方法"。
    - getAudioTracks/switchAudioTrack/activeAudioTracks -> 通过 engine.trackControl 访问
    - setExternalSubtitle/setSubtitleDelay/subtitleDelay/setEqualizer -> 通过 engine.subtitleConfig 访问
    - setVideoEffect/rotate/setAspectRatio/setDeinterlace -> 通过 engine.videoEffectControl 访问
    - setD3d11SyncEnabled/setHardwareDecoding -> 通过 engine.rendererControl 访问
    - setVolume/setMute -> 通过 engine.volumeControl 访问
    保留的是:
    - getSubtitleTracks/switchSubtitleTrack/toggleSubtitle -> FvpEngine 直接实现（委托 _trackManager）
    - subtitleText/subtitleDelay getter -> FvpEngine 直接实现
    - aspectRatio getter -> FvpEngine 直接实现

**B. FvpCallbackHandler 使用 stateMachine (per Pitfall 3):**

修改 `lib/kernel/engine/fvp_callback_handler.dart`:
- 添加字段: `final EngineStateMachine _stateMachine;`
- 修改构造函数: 添加 `required EngineStateMachine stateMachine` 参数
- 修改 init() 中的回调:
  - `state.value = mapped` -> `_stateMachine.transitionTo(mapped, 'mdk.onStateChanged')`
  - `state.value = MediaState.completed` -> `_stateMachine.transitionTo(MediaState.completed, 'mdk.onMediaStatus.end')`
  - `isBuffering.value = true/false` -> `_stateMachine.isBuffering.value = true/false`
    注意: isBuffering 不通过 transitionTo，直接设置 ValueNotifier
    （isBuffering 是独立 bool 标志，不需要状态转换守卫）
- 删除旧的 `state` 和 `isBuffering` 构造函数参数
- 添加 import: `import 'engine_state_machine.dart';`

**C. FakeEngine 更新:**

修改 `test/helpers/fake_engine.dart`:
- 添加 import: `import 'package:simple_player_flutter/kernel/engine/engine_state_machine.dart';`
- 添加字段: `final EngineStateMachine stateMachine = EngineStateMachine();`
- 修改 state getter: `@override ValueNotifier<MediaState> get state => stateMachine.state;`
- 修改 isSeeking getter: `@override ValueNotifier<bool> get isSeeking => stateMachine.isSeeking;`
- 修改 isBuffering getter: `@override ValueNotifier<bool> get isBuffering => stateMachine.isBuffering;`
- 修改 play(): `stateMachine.transitionTo(MediaState.playing, 'fake.play');`
- 修改 pause(): `stateMachine.transitionTo(MediaState.paused, 'fake.pause');`
- 修改 stop(): `stateMachine.transitionTo(MediaState.idle, 'fake.stop');`
- 修改 open(): `stateMachine.transitionTo(MediaState.opening, 'fake.open');`
  以及错误/完成状态
- 修改 dispose(): 添加 `stateMachine.dispose();`，删除旧 state/isSeeking/isBuffering dispose
- 修改 togglePlayPause(): 改为 `stateMachine.togglePlayPause();`（与 FvpEngine 一致）
- 删除 getSubtitleTracks/switchSubtitleTrack/toggleSubtitle delegation（如果它们存在）
  — 检查 FakeEngine 当前有这些方法，保留不变（FakeEngine 仍实现 PlaybackControl 接口）

**D. FvpCallbackHandler 测试更新:**

修改 `test/kernel/engine/fvp_callback_handler_test.dart`:
- 构造 FvpCallbackHandler 时传入 stateMachine 参数
- 添加 stateMachine 实例
- 验证 transitionTo 被调用（通过检查 stateMachine.state.value）

**E. 验证 FvpEngine 行数:**
- 运行 `wc -l lib/kernel/engine/fvp_engine.dart` 确认 <350 行
- 如果超过350行，进一步提取方法或常量
  </action>
  <verify>
    <automated>flutter test test/kernel/engine/</automated>
  </verify>
  <acceptance_criteria>
    - FvpEngine 使用 EngineStateMachine 管理状态转换（无直接 state.value 赋值）
    - FvpEngine 暴露 interface getter: trackControl, subtitleConfig, videoEffectControl, rendererControl, volumeControl
    - FvpEngine 删除 ~200 行 delegation 方法
    - FvpEngine 行数 < 350 (wc -l 确认)
    - FvpCallbackHandler 使用 stateMachine.transitionTo 更新状态
    - FakeEngine 使用 stateMachine 管理状态
    - 所有测试通过: flutter test test/kernel/engine/
    - flutter analyze 无错误
  </acceptance_criteria>
  <done>
    FvpEngine 从 632 行精简至 <350 行 (per ENG-02)。
    所有状态转换通过 EngineStateMachine 守卫 (per SVC-02)。
    Helper 通过接口 getter 暴露，调用者 API 改为 engine.trackControl.xxx。
    FvpCallbackHandler 集成状态机，不再直接设置 state.value。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FvpEngine -> EngineStateMachine | 所有状态转换通过 transitionTo 守卫 |
| FvpCallbackHandler -> EngineStateMachine | mdk 回调通过 transitionTo 更新状态 |
| UI -> Engine | 通过 interface getter 访问，不直接操作 ValueNotifier |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering | FvpEngine state.value 直接赋值 | medium | mitigate | 删除所有直接 state.value 赋值，统一通过 stateMachine.transitionTo |
| T-10-04 | Elevation | FvpCallbackHandler 绕过守卫 | medium | mitigate | 所有回调通过 stateMachine.transitionTo，不再直接设置 state.value |
| T-10-05 | Info Disclosure | 接口 getter 返回 helper 实例 | low | accept | helper 本身不暴露敏感信息，getter 是设计模式 |
| T-10-SC | Tampering | npm/pip/cargo installs | n/a | n/a | 本阶段无新包安装 |
</threat_model>

<verification>
1. `flutter test test/kernel/engine/` — 所有引擎测试通过
2. `flutter test` — 全量测试通过
3. `flutter analyze` — 无错误
4. `wc -l lib/kernel/engine/fvp_engine.dart` — 确认 <350 行
5. grep 确认 FvpEngine 中无直接 `state.value =` 赋值（除 getter 定义外）
6. grep 确认 FvpCallbackHandler 中无直接 `state.value =` 赋值
</verification>

<success_criteria>
- FvpEngine < 350 行 (ENG-02)
- 所有状态转换通过 EngineStateMachine (SVC-02)
- 所有 helper implements 对应 ISP 接口
- FvpEngine 暴露 interface getter 替代 delegation 方法
- FvpCallbackHandler 集成状态机
- flutter analyze 无错误，所有测试通过
</success_criteria>

<output>
Create `.planning/phases/10-state-machine-extraction/10-02-SUMMARY.md` when done
</output>
