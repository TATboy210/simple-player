---
phase: 09-interface-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/engine/engine_state_view.dart
  - lib/kernel/engine/playback_control.dart
  - lib/kernel/engine/track_control.dart
  - lib/kernel/engine/subtitle_config.dart
  - lib/kernel/engine/video_effect_control.dart
  - lib/kernel/engine/renderer_control.dart
  - lib/kernel/models/player_error.dart
  - lib/kernel/engine/media_state.dart
  - lib/kernel/engine/open_result.dart
  - lib/kernel/engine/engine_state.dart
  - lib/kernel/engine/media_error_type.dart
  - lib/kernel/models/media_state.dart
  - lib/kernel/engine/track_control.dart
  - lib/kernel/engine/video_effects.dart
  - lib/kernel/engine/renderer_config.dart
  - test/helpers/fake_engine.dart
autonomous: true
requirements:
  - ENG-01
  - ENG-03
must_haves:
  truths:
    - EngineStateView abstract class exists with 12 read-only ValueNotifier getters
    - PlaybackControl abstract class exists with 12 control methods
    - TrackControl/SubtitleConfig/VideoEffectControl/RendererControl abstract classes exist with correct method signatures
    - PlayerError sealed class supports exhaustive pattern matching with 5 subtypes
    - MediaState enum has exactly 6 values (idle/opening/playing/paused/completed/error)
    - OpenResult.OpenError wraps PlayerError instead of MediaErrorType
    - FakeEngine compiles with implements (not with) all 6 new interfaces
  artifacts:
    - lib/kernel/engine/engine_state_view.dart
    - lib/kernel/engine/playback_control.dart
    - lib/kernel/engine/track_control.dart
    - lib/kernel/engine/subtitle_config.dart
    - lib/kernel/engine/video_effect_control.dart
    - lib/kernel/engine/renderer_control.dart
    - lib/kernel/models/player_error.dart
  key_links:
    - EngineStateView.get lastError returns ValueNotifier<PlayerError?>
    - PlayerError sealed class subtypes carry sub-enum codes
    - MediaState no longer has seeking/buffering values
---

<objective>
Create 6 ISP interface abstract classes, rewrite PlayerError as nested sealed class, rewrite MediaState as orthogonal 6-value enum, update OpenResult to use PlayerError, update FakeEngine to implement new interfaces. Delete old EngineState mixin and empty marker mixins.

Purpose: Establish the new interface contracts and error model that all consumers (Plan 02) will migrate to. This is purely additive — new files plus FakeEngine update for test compilation.
Output: 7 new interface/model files, 3 deleted files, 3 rewritten files (FakeEngine, OpenResult, MediaState)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-interface-decomposition/09-CONTEXT.md
@.planning/phases/09-interface-decomposition/09-RESEARCH.md
@lib/kernel/engine/engine_state.dart
@lib/kernel/models/player_error.dart
@lib/kernel/engine/media_state.dart
@lib/kernel/engine/open_result.dart
@lib/kernel/engine/media_error_type.dart
@test/helpers/fake_engine.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 ISP interface abstract classes + rewrite PlayerError + MediaState + OpenResult</name>
  <files>
    lib/kernel/engine/engine_state_view.dart,
    lib/kernel/engine/playback_control.dart,
    lib/kernel/engine/track_control.dart,
    lib/kernel/engine/subtitle_config.dart,
    lib/kernel/engine/video_effect_control.dart,
    lib/kernel/engine/renderer_control.dart,
    lib/kernel/models/player_error.dart,
    lib/kernel/engine/media_state.dart,
    lib/kernel/engine/open_result.dart
  </files>
  <read_first>
    lib/kernel/engine/engine_state.dart — source of truth for all method signatures and ValueNotifier fields to extract
    lib/kernel/models/player_error.dart — current PlayerErrorCode enum values to merge into new sealed class
    lib/kernel/engine/media_error_type.dart — current MediaErrorType 5 values to map to sealed class subtypes
    lib/kernel/engine/media_state.dart — current 9-value enum + transition guard extension to replace
    lib/kernel/engine/open_result.dart — current OpenResult using MediaErrorType, must update to PlayerError
    lib/kernel/engine/track_control.dart — current empty mixin signature to promote
    lib/kernel/engine/video_effects.dart — current empty mixin signature to promote
    lib/kernel/engine/renderer_config.dart — current empty mixin signature to promote
    lib/kernel/engine/video_effect_type.dart — VideoEffectType enum used in VideoEffectControl
    lib/kernel/models/audio_track_info.dart — AudioTrackInfo type used in TrackControl
    lib/kernel/models/subtitle_track_info.dart — SubtitleTrackInfo type used in SubtitleConfig
    lib/kernel/models/media_info.dart — MediaInfo type used in EngineStateView
  </read_first>
  <action>
Create 7 new files and rewrite 3 existing files per D-01 through D-10, D-16, D-17, D-19 from CONTEXT.md.

**New file 1: `lib/kernel/engine/engine_state_view.dart`** (per D-01, D-05)
- Define `abstract class EngineStateView` with 12 read-only getters:
  - `ValueNotifier<int?> get textureId`
  - `ValueNotifier<MediaState> get state` (new 6-value enum)
  - `ValueNotifier<int> get position`
  - `ValueNotifier<int> get duration`
  - `ValueNotifier<double> get volume`
  - `ValueNotifier<bool> get isMuted`
  - `ValueNotifier<bool> get isBuffering` (transient flag, separate from state enum)
  - `ValueNotifier<String> get subtitleText`
  - `ValueNotifier<int> get buffered`
  - `ValueNotifier<double> get aspectRatio`
  - `ValueNotifier<PlayerError?> get lastError` (replaces errorMessage + errorType)
  - `ValueNotifier<double> get playbackSpeed`
- Also include `MediaInfo get mediaInfo` and `void dispose()`
- Import `package:flutter/foundation.dart`, `media_state.dart`, `player_error.dart`, `models/media_info.dart`

**New file 2: `lib/kernel/engine/playback_control.dart`** (per D-02)
- Define `abstract class PlaybackControl` with 12 methods:
  - `Future<void> open(String path)`, `void play()`, `void pause()`, `void stop()`, `void togglePlayPause()`
  - `Future<void> seekTo(int ms)`, `void setVolume(double volume)`, `void setMute(bool mute)`
  - `void setPlaybackRate(double rate)`, `void setRange({required int from, int to = -1})`
  - `void skipForward([int ms = 10000])`, `void skipBack([int ms = 10000])`

**New file 3: `lib/kernel/engine/track_control.dart`** (per D-03, D-06 — replaces old empty mixin)
- Define `abstract class TrackControl` with:
  - `List<AudioTrackInfo> getAudioTracks()`, `void switchAudioTrack(int trackId)`, `List<int> get activeAudioTracks`
- Import `models/audio_track_info.dart`

**New file 4: `lib/kernel/engine/subtitle_config.dart`** (per D-03)
- Define `abstract class SubtitleConfig` with:
  - `List<SubtitleTrackInfo> getSubtitleTracks()`, `void switchSubtitleTrack(int trackId)`, `void toggleSubtitle()`
  - `void setExternalSubtitle(String path)`, `void setSubtitleDelay(int delay)`, `void setEqualizer(String preset)`
  - `ValueNotifier<String> get subtitleText` (returns same instance as EngineStateView, per research Q1)
  - `int get subtitleDelay`
- Import `package:flutter/foundation.dart`, `models/subtitle_track_info.dart`

**New file 5: `lib/kernel/engine/video_effect_control.dart`** (per D-03, D-06 — replaces old empty mixin)
- Define `abstract class VideoEffectControl` with:
  - `void setVideoEffect(VideoEffectType effectType, double value)`, `void rotate(int degrees)`
  - `void setAspectRatio(double ratio)`, `void setDeinterlace(bool enable)`
  - `ValueNotifier<double> get aspectRatio` (returns same instance as EngineStateView, per research Q2)
- Import `package:flutter/foundation.dart`, `video_effect_type.dart`

**New file 6: `lib/kernel/engine/renderer_control.dart`** (per D-03, D-06 — replaces old empty mixin)
- Define `abstract class RendererControl` with:
  - `void setD3d11SyncEnabled(bool enabled)`, `void setHardwareDecoding(bool enabled)`

**Rewrite file 1: `lib/kernel/models/player_error.dart`** (per D-07)
- Replace current `PlayerErrorCode` enum + `PlayerError` class with nested sealed class:
  - `sealed class PlayerError` with `String get message` and `Object? get cause`
  - `final class FileError extends PlayerError` with `FileErrorCode code` + `message` + optional `cause`
  - `enum FileErrorCode { pathEmpty, fileNotFound, pathTraversal }`
  - `final class CodecError extends PlayerError` with `CodecErrorCode code`
  - `enum CodecErrorCode { unsupportedFormat, decodeFailed, codecUnsupported }`
  - `final class PlaybackError extends PlayerError` with `PlaybackErrorCode code`
  - `enum PlaybackErrorCode { playFailed, seekFailed, textureFailed, openTimeout }`
  - `final class NetworkError extends PlayerError` with `NetworkErrorCode code`
  - `enum NetworkErrorCode { timeout, connectionLost }`
  - `final class UnknownError extends PlayerError` with `message` + optional `cause`
- All subtypes are `final class` with `const` constructors
- Include `toString()` override on each subtype

**Rewrite file 2: `lib/kernel/engine/media_state.dart`** (per D-16, D-19)
- Replace current 9-value enum with 6-value orthogonal enum:
  - Remove: `loading` (rename to `opening` per D-16), `stopped`, `seeking`, `buffering`
  - Add: `opening` (replaces `loading`)
  - Result: `enum MediaState { idle, opening, playing, paused, completed, error }`
- DELETE the `MediaStateTransition` extension entirely (per D-19) — Phase 10 adds new state machine
- Update the doc comment to reflect the new orthogonal model: primary state + transient flags (isSeeking/isBuffering are separate ValueNotifier<bool> on EngineStateView)

**Rewrite file 3: `lib/kernel/engine/open_result.dart`** (per D-08)
- Replace `import 'media_error_type.dart'` with `import '../../models/player_error.dart'`
- Change `OpenError` constructor: remove `MediaErrorType type` + `String message`, add `PlayerError error`
- Remove the `message` getter from OpenError — callers use `error.message` instead
- Update the doc comment and code example to use PlayerError pattern matching

**Delete files** (per D-06, D-10):
- `lib/kernel/engine/media_error_type.dart` — replaced by PlayerError sealed class subtypes
- `lib/kernel/engine/track_control.dart` — old empty mixin, replaced by new abstract class (already created above)
- `lib/kernel/engine/video_effects.dart` — old empty mixin, replaced by video_effect_control.dart
- `lib/kernel/engine/renderer_config.dart` — old empty mixin, replaced by renderer_control.dart
- `lib/kernel/models/media_state.dart` — delete if exists (duplicate, real one is in engine/)

**Update `lib/kernel/engine/engine_state.dart`**:
- REMOVE the entire `mixin EngineState` class body (per D-04)
- Keep the file as a barrel export that re-exports all new interfaces:
  ```dart
  export 'engine_state_view.dart';
  export 'playback_control.dart';
  export 'track_control.dart';
  export 'subtitle_config.dart';
  export 'video_effect_control.dart';
  export 'renderer_control.dart';
  export 'media_state.dart';
  export 'models/media_info.dart';
  export 'models/video_codec_info.dart';
  export 'video_effect_type.dart';
  export 'models/audio_track_info.dart';
  export 'models/subtitle_track_info.dart';
  ```
- This preserves backward compatibility for files that `import 'engine_state.dart'` — they get all new types through the barrel export

**Verification after this task:**
- `flutter analyze` on the new files alone (they are standalone, no consumers yet)
- Verify each new abstract class compiles independently
- Verify PlayerError sealed class compiles and supports exhaustive matching
- Verify MediaState has exactly 6 values
- Verify OpenResult.OpenError wraps PlayerError
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && dart analyze lib/kernel/engine/engine_state_view.dart lib/kernel/engine/playback_control.dart lib/kernel/engine/track_control.dart lib/kernel/engine/subtitle_config.dart lib/kernel/engine/video_effect_control.dart lib/kernel/engine/renderer_control.dart lib/kernel/models/player_error.dart lib/kernel/engine/media_state.dart lib/kernel/engine/open_result.dart 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/kernel/engine/engine_state_view.dart` exists and defines `abstract class EngineStateView` with exactly 12 ValueNotifier getters + mediaInfo + dispose
    - File `lib/kernel/engine/playback_control.dart` exists and defines `abstract class PlaybackControl` with 12 methods
    - File `lib/kernel/engine/track_control.dart` exists and defines `abstract class TrackControl` with getAudioTracks/switchAudioTrack/activeAudioTracks
    - File `lib/kernel/engine/subtitle_config.dart` exists and defines `abstract class SubtitleConfig` with 6 methods + subtitleText getter + subtitleDelay getter
    - File `lib/kernel/engine/video_effect_control.dart` exists and defines `abstract class VideoEffectControl` with 4 methods + aspectRatio getter
    - File `lib/kernel/engine/renderer_control.dart` exists and defines `abstract class RendererControl` with 2 methods
    - `lib/kernel/models/player_error.dart` defines `sealed class PlayerError` with 5 final subtypes (FileError/CodecError/PlaybackError/NetworkError/UnknownError), each carrying a sub-enum code
    - `lib/kernel/engine/media_state.dart` defines `enum MediaState` with exactly 6 values: idle, opening, playing, paused, completed, error — no seeking/buffering values, no transition guard extension
    - `lib/kernel/engine/open_result.dart` OpenError constructor takes `PlayerError error` instead of `MediaErrorType type, String message`
    - `lib/kernel/engine/engine_state.dart` is a barrel export file re-exporting all new interfaces (no mixin EngineState)
    - Files `lib/kernel/engine/media_error_type.dart`, `lib/kernel/engine/video_effects.dart`, `lib/kernel/engine/renderer_config.dart` are deleted
    - `dart analyze` on all new/rewritten files reports 0 errors
  </acceptance_criteria>
  <done>
    7 new interface/model files created with correct abstract class signatures
    PlayerError sealed class with 5 subtypes and sub-enum codes
    MediaState reduced to 6 values, transition guard deleted
    OpenResult adapted to use PlayerError
    engine_state.dart converted to barrel export
    4 old files deleted (media_error_type.dart, old track_control mixin, video_effects mixin, renderer_config mixin)
    dart analyze passes on all new/modified kernel files
  </done>
</task>

<task type="auto">
  <name>Task 2: Update FakeEngine to implement 6 new interfaces + adapt to new error/state model</name>
  <files>
    test/helpers/fake_engine.dart
  </files>
  <read_first>
    test/helpers/fake_engine.dart — current FakeEngine using `with EngineState, TrackControl, VideoEffects, RendererConfig`
    lib/kernel/engine/engine_state_view.dart — new interface (from Task 1)
    lib/kernel/engine/playback_control.dart — new interface (from Task 1)
    lib/kernel/engine/track_control.dart — new interface (from Task 1)
    lib/kernel/engine/subtitle_config.dart — new interface (from Task 1)
    lib/kernel/engine/video_effect_control.dart — new interface (from Task 1)
    lib/kernel/engine/renderer_control.dart — new interface (from Task 1)
    lib/kernel/models/player_error.dart — new error model (from Task 1)
    lib/kernel/engine/media_state.dart — new 6-value MediaState (from Task 1)
  </read_first>
  <action>
Update `test/helpers/fake_engine.dart` per D-04, D-09, D-10, D-16, D-17:

1. **Change class declaration** (per D-04):
   - FROM: `class FakeEngine with EngineState, TrackControl, VideoEffects, RendererConfig`
   - TO: `class FakeEngine implements EngineStateView, PlaybackControl, TrackControl, SubtitleConfig, VideoEffectControl, RendererConfig`

2. **Replace errorMessage with lastError** (per D-09):
   - DELETE: `final ValueNotifier<String?> errorMessage = ValueNotifier<String?>(null);`
   - ADD: `final ValueNotifier<PlayerError?> lastError = ValueNotifier<PlayerError?>(null);`
   - Update `simulateError(String message)` method to set `lastError.value` instead of `errorMessage.value`:
     - Use `UnknownError(message)` as the default error type (matching current behavior of string-only errors)
   - Update `failNextOpenWith` handler in `open()`: set `lastError.value = UnknownError(msg)` instead of `errorMessage.value = msg`

3. **Delete errorType field** (per D-10):
   - DELETE: `MediaErrorType errorType = MediaErrorType.unknown;`
   - This field was used by ErrorBanner tests — those tests will be updated in Plan 02

4. **Add isSeeking ValueNotifier** (per D-17):
   - ADD: `final ValueNotifier<bool> isSeeking = ValueNotifier<bool>(false);`
   - Add `isSeeking.dispose()` in `dispose()` method

5. **Update MediaState references** (per D-16):
   - `MediaState.loading` → `MediaState.opening` in `open()` method
   - `MediaState.buffering` → remove from `simulateBuffering()` method (set `isBuffering.value` only, do NOT set state to buffering since that enum value no longer exists)
   - `MediaState.stopped` → remove from `stop()` method (set `state.value = MediaState.idle` instead, since `stopped` is no longer a valid state — or leave state unchanged, per D-18 state transition guards are Phase 10)
   - Actually, for `stop()`: set `state.value = MediaState.idle` since `stopped` no longer exists and idle is the natural reset state
   - Remove `MediaState.stopped` from `togglePlayPause()` condition — replace with `MediaState.completed` check (idle/playing/paused/completed are the valid states for toggle)

6. **Update open() method**:
   - Change `state.value = MediaState.loading` to `state.value = MediaState.opening`
   - Change `errorMessage.value = null` to `lastError.value = null`

7. **Update dispose()**:
   - Remove `errorMessage.dispose()`
   - Add `lastError.dispose()`
   - Add `isSeeking.dispose()`

8. **Update imports**:
   - Remove `import 'package:simple_player_flutter/kernel/engine/engine_state.dart'` (barrel export will still work, but keep it for backward compat — the barrel export re-exports all new interfaces)
   - Actually, keep the import since engine_state.dart is now a barrel export that re-exports all interfaces

9. **Remove import of deleted files**:
   - The import of `engine_state.dart` now re-exports the new types, so no import changes needed for the FakeEngine file itself
   - But remove any direct imports of `media_error_type.dart` if present

10. **Update test helper methods**:
    - `simulateError(String message)` → `simulateError(String message)` body changes: `lastError.value = UnknownError(message)` instead of `errorMessage.value = message`
    - `simulateBuffering(bool buffering)` → only set `isBuffering.value = buffering`, do NOT set `state.value = MediaState.buffering`
  </action>
  <verify>
    <automated>cd D:/simple_player_flutter && dart analyze test/helpers/fake_engine.dart 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `test/helpers/fake_engine.dart` class declaration is `class FakeEngine implements EngineStateView, PlaybackControl, TrackControl, SubtitleConfig, VideoEffectControl, RendererConfig`
    - `errorMessage` field is removed, `lastError` field of type `ValueNotifier<PlayerError?>` exists
    - `errorType` field is removed
    - `isSeeking` field of type `ValueNotifier<bool>` exists
    - `dispose()` disposes all 13 ValueNotifiers (12 from EngineStateView including lastError + isSeeking)
    - `simulateError()` sets `lastError.value` to an `UnknownError` instance
    - `open()` uses `MediaState.opening` (not `loading`)
    - `open()` sets `lastError.value = null` (not `errorMessage.value`)
    - `stop()` sets `state.value = MediaState.idle` (not `stopped`)
    - `simulateBuffering()` sets `isBuffering.value` only (no `state.value = MediaState.buffering`)
    - `dart analyze test/helpers/fake_engine.dart` reports 0 errors
  </acceptance_criteria>
  <done>
    FakeEngine compiles with `implements` (not `with`) all 6 new interfaces
    errorMessage replaced by lastError (ValueNotifier<PlayerError?>)
    isSeeking ValueNotifier added
    MediaState.loading/opening, stopped, buffering references updated
    dart analyze passes on FakeEngine
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | This phase creates abstract interfaces and data models only — no new I/O, no new external calls, no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-SC | Tampering | npm/pip/cargo installs | N/A | N/A | No external packages installed in this phase |
| T-9-01 | Information Disclosure | PlayerError.message | low | accept | Error messages are user-facing strings, not sensitive data. Existing pattern preserved. |
</threat_model>

<verification>
- `dart analyze` on all new/modified files in this plan: 0 errors
- `flutter test test/engine/mixin_capability_test.dart` — will FAIL until FakeEngine is updated (Task 2) and consumers are migrated (Plan 02). Task 2 ensures FakeEngine compiles, but consumers still reference old types.
- After Task 2: `dart analyze test/helpers/fake_engine.dart` — 0 errors
- Cross-check: engine_state.dart barrel export re-exports all 6 new interfaces + MediaState + model types
</verification>

<success_criteria>
- 7 new interface/model files exist with correct abstract class signatures
- PlayerError sealed class compiles and supports exhaustive pattern matching
- MediaState has exactly 6 values, transition guard extension deleted
- OpenResult.OpenError wraps PlayerError
- FakeEngine compiles implementing all 6 new interfaces
- engine_state.dart is a barrel export (no mixin body)
- 4 old files deleted (media_error_type.dart, old empty mixins)
- dart analyze passes on all files in this plan
</success_criteria>

<output>
Create `.planning/phases/09-interface-decomposition/09-01-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

### New Files (7)
| File | Symbol | Description |
|------|--------|-------------|
| `lib/kernel/engine/engine_state_view.dart` | `abstract class EngineStateView` | 12 read-only ValueNotifier getters + mediaInfo + dispose |
| `lib/kernel/engine/playback_control.dart` | `abstract class PlaybackControl` | 12 control methods (open/play/pause/seek/volume/etc.) |
| `lib/kernel/engine/track_control.dart` | `abstract class TrackControl` | Audio track methods (getAudioTracks/switchAudioTrack/activeAudioTracks) |
| `lib/kernel/engine/subtitle_config.dart` | `abstract class SubtitleConfig` | Subtitle methods + subtitleText/subtitleDelay getters |
| `lib/kernel/engine/video_effect_control.dart` | `abstract class VideoEffectControl` | Video effect methods + aspectRatio getter |
| `lib/kernel/engine/renderer_control.dart` | `abstract class RendererControl` | D3D11 config methods (setD3d11SyncEnabled/setHardwareDecoding) |
| `lib/kernel/models/player_error.dart` | `sealed class PlayerError` + 5 subtypes | Nested sealed hierarchy: FileError/CodecError/PlaybackError/NetworkError/UnknownError |

### Modified Files (4)
| File | Change |
|------|--------|
| `lib/kernel/engine/media_state.dart` | 9 values -> 6 values, transition guard deleted |
| `lib/kernel/engine/open_result.dart` | OpenError wraps PlayerError instead of MediaErrorType |
| `lib/kernel/engine/engine_state.dart` | mixin body deleted, converted to barrel export |
| `test/helpers/fake_engine.dart` | `with` -> `implements`, errorMessage -> lastError, isSeeking added |

### Deleted Files (4)
| File | Reason |
|------|--------|
| `lib/kernel/engine/media_error_type.dart` | Replaced by PlayerError sealed class subtypes |
| `lib/kernel/engine/video_effects.dart` | Old empty mixin, replaced by video_effect_control.dart |
| `lib/kernel/engine/renderer_config.dart` | Old empty mixin, replaced by renderer_control.dart |
| `lib/kernel/models/media_state.dart` | Duplicate, real one is in engine/ |
