---
phase: 39-progress-bar-three-symptoms-root-cause
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-DIAGNOSIS.md
  - test/widget/player/progress_bar_data_chain_diagnosis_test.dart
  - integration_test/progress_bar_real_runtime_diagnosis_test.dart
autonomous: true
requirements: [PROG-01, PROG-02, PROG-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - statement: "PROG-01 flagged assumption（spec-less probe unresolved）：真实 duration/position 事件必须能沿 PlayerPort → PlayerControlsState → ControlBarViewModel → ProgressBar 到达；本计划须用逐段证据定位第一个不满足该契约的符号。"
      verification: explicit
    - statement: "PROG-02 flagged assumption（spec-less probe unresolved）：seek 失效或回跳与已定位的数据链断点相关，但 C 事件驱动 v2 的到达目标后释放及超时兜底本身必须作为受保护基线。"
      verification: explicit
    - statement: "PROG-03 flagged assumption（spec-less probe unresolved）：悬停气泡依赖非零 duration 与 ProgressBar 的真实 pointer 命中；诊断必须区分数据禁用态和上层命中遮挡。"
      verification: explicit
    - "39-DIAGNOSIS.md 明确给出唯一首个断点的 file:symbol、事件顺序、复现命令、实际输出和被排除的候选，不以猜测代替证据。"
  artifacts:
    - path: ".planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-DIAGNOSIS.md"
      provides: "duration/position 全链路逐段取证、首个断点和 Plan 02 修复契约"
    - path: "test/widget/player/progress_bar_data_chain_diagnosis_test.dart"
      provides: "无 FFI 的端到端 controls 数据链、延迟 duration、source replacement、seek 与 hover 命中证据"
    - path: "integration_test/progress_bar_real_runtime_diagnosis_test.dart"
      provides: "Windows 上以 tiny_valid.mp4 驱动真实 media_kit Player，独立记录原始 Player.stream，并以相邻层 observed/missing 证据定位 port、PlayerControlsState、ViewModel/Timeline 或 widget 首断点"
  key_links:
    - from: "lib/ui/player/media_kit_player_port.dart"
      to: "lib/ui/player/player_video_controls.dart"
      via: "PlayerPort duration/position stream 与 state snapshot 进入 PlayerControlsState.init/updateSources"
      pattern: "durationNow|positionNow|duration\.listen|position\.listen|updateSources"
    - from: "lib/ui/player/player_video_controls.dart"
      to: "lib/ui/player/control_bar_view_model.dart"
      via: "_controlsState.durationMs/positionMs 保持 notifier identity 并装入 ControlBarViewModel"
      pattern: "duration: _controlsState\.durationMs|position: _controlsState\.positionMs"
    - from: "lib/ui/player/control_bar_timeline.dart"
      to: "lib/ui/player/progress_bar.dart"
      via: "ControlBarTimeline 将 vm.duration/vm.position 和 vm.onSeek 原样交给 ProgressBar"
      pattern: "duration: vm\.duration|position: vm\.position|onSeek: vm\.onSeek"
  prohibitions:
    - statement: "不得修改 media_kit package、Pub cache 或任何 vendored media_kit/libmpv 文件；诊断只读第三方源码并只在项目文件中取证。"
      status: resolved
      verification: test
    - statement: "不得恢复 ControlsOverlay 或建立第二棵控制树来绕过当前 Video.controls → PlayerVideoControls 路径。"
      status: resolved
      verification: judgment
    - statement: "不得把 C 事件驱动 v2 seek-hold 退化为固定延迟清除拖拽状态。"
      status: resolved
      verification: test
---

<objective>
在写任何修复之前，为 PROG-01、PROG-02、PROG-03 建立一条可重复的端到端证据链，并把 duration/position 首个断点精确定位到项目侧具体符号。

Purpose: 三个症状都可能由 `durationMs == 0` 禁用态引起；先排除 port 订阅、`updateSources` 时序、ViewModel/Timeline 装配和 pointer 命中中的错误候选，避免在错误层补丁。
Output: `39-DIAGNOSIS.md`、一个无原生依赖的隔离 widget test，以及一个使用仓库 fixture 的 Windows 真实运行 integration test。
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.8-phases/36-medium-grain-rebuild-boundary-refactor/36-02-SUMMARY.md
@.planning/milestones/v1.8-phases/36-medium-grain-rebuild-boundary-refactor/36-03-SUMMARY.md
@lib/ui/player/media_kit_player_port.dart
@lib/ui/player/player_video_controls.dart
@lib/ui/player/control_bar_view_model.dart
@lib/ui/player/control_bar_timeline.dart
@lib/ui/player/progress_bar.dart
</context>

<interfaces>
`PlayerPort` 暴露 `position`/`duration` stream 与 `positionNow`/`durationNow` 快照；`PlayerControlsState.init()` 将二者转为 `positionMs`/`durationMs`，`updateSources()` 必须保留下游 notifier identity；`_createControlBarViewModel()` 把这两个 notifier 交给 `ControlBarViewModel`；`ControlBarTimeline` 原样装配 `ProgressBar`；`ProgressBar` 在 duration 大于 0 时启用 pointer、seek 和自绘时间气泡。任务 1 的隔离 tracer 使用 `FakePlayerControls`、`FakeVideoControlsPort`、`FakeEngine`；任务 2 必须在 Windows integration_test 中构造真实 media_kit `Player` 并打开仓库内 `test/fixtures/tiny_valid.mp4`，fake 证据不得替代真实 Player.stream 时序证据。
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>任务 1：建立 duration/position 到 ProgressBar 的逐段证据 tracer</name>
  <files>test/widget/player/progress_bar_data_chain_diagnosis_test.dart</files>
  <read_first>
    - lib/ui/player/media_kit_player_port.dart
    - lib/ui/player/player_video_controls.dart
    - lib/ui/player/control_bar_view_model.dart
    - lib/ui/player/control_bar.dart
    - lib/ui/player/control_bar_timeline.dart
    - lib/ui/player/progress_bar.dart
    - lib/ui/player/player_screen.dart
    - lib/kernel/engine/media_kit_engine.dart
    - lib/kernel/services/playback_controller.dart
    - test/widget/player/player_video_controls_test.dart
    - test/widget/player/progress_bar_test.dart
    - test/widget/player/progress_bar_source_replacement_test.dart
    - test/helpers/fake_player_controls.dart
    - test/helpers/fake_video_controls.dart
  </read_first>
  <behavior>
    - PROG-01：controls 首次以 duration=0 挂载后，FakePlayerControls 延迟发出 60 秒 duration 和递增 position；逐段断言 `PlayerControlsState.durationMs/positionMs`、ProgressBar slider semantics 百分比和时间文本更新。
    - PROG-01：同一 GlobalKey 下替换 VideoControlsPort，旧 port 的 duration/position 不能再驱动，新的 port 快照与后续事件必须驱动且 `positionMs`/`durationMs` notifier identity 不变。
    - PROG-02：duration 延迟到达后，不依赖父 ControlBar 重建即可点击中点并使 active port 收到约 30 秒 seek；拖拽时至少产生 update seek 与 end seek。
    - PROG-03：MouseRegion 收到两个不同 x 坐标的 hover 后，自绘气泡文本与 `Positioned.left` 均发生变化；同时记录位于 ProgressBar 之上的 sibling hit-test 层是否截获 pointer。
  </behavior>
  <action>新建一个使用真实 `PlayerVideoControls → ControlBarViewModel → ControlBarTimeline → ProgressBar` widget 树的诊断测试，数据源只用现有 fake。每个测试都从 duration=0 开始，随后通过 `emitDuration`/`emitPosition` 模拟视频加载后的异步事件；通过 slider Semantics、格式化时间文本、seek 记录以及 tooltip 的 `Positioned.left` 证明每一段是否收到数据和 pointer。加入同 key source replacement 场景，显式保存并比较 `PlayerControlsState` 下游可观察状态、旧新 port 监听与事件结果。测试名称中分别标明 `port-to-state`、`state-to-progress`、`pointer-hit`，使失败输出直接对应候选断点。不得为使诊断变绿而修改生产代码，也不得初始化 media_kit/MDK。</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/widget/player/progress_bar_data_chain_diagnosis_test.dart --reporter expanded</automated>
  </verify>
  <acceptance_criteria>
    - `test/widget/player/progress_bar_data_chain_diagnosis_test.dart` 存在，并覆盖延迟 duration、递增 position、source replacement、tap、drag、两个 hover x 坐标。
    - 输出能把失败归属到 port-to-state、state-to-progress 或 pointer-hit 中的具体一段，而不是只断言 `ProgressBar` 存在。
    - 测试仅使用 `FakePlayerControls`、`FakeVideoControlsPort`、`FakeEngine`，运行时不出现 mdk.dll 加载。
    - 本任务结束时 `git diff -- lib/` 不包含本任务新增的生产代码变化。
  </acceptance_criteria>
  <done>一条可运行的无 FFI tracer 已把三个症状映射为逐段可观察结果，并保留首个失败位置的测试输出。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：采集真实 Windows PlayerPort 时序并锁定 Plan 02 修复入口</name>
  <files>integration_test/progress_bar_real_runtime_diagnosis_test.dart, .planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-DIAGNOSIS.md</files>
  <read_first>
    - test/widget/player/progress_bar_data_chain_diagnosis_test.dart
    - test/fixtures/tiny_valid.mp4
    - integration_test/simple_test.dart
    - lib/ui/player/media_kit_player_port.dart
    - lib/ui/player/player_video_controls.dart
    - lib/ui/player/control_bar_view_model.dart
    - lib/ui/player/control_bar_timeline.dart
    - lib/ui/player/progress_bar.dart
    - lib/ui/player/player_screen.dart
    - lib/kernel/engine/media_kit_engine.dart
    - lib/kernel/services/playback_controller.dart
    - .planning/milestones/v1.8-phases/36-medium-grain-rebuild-boundary-refactor/36-03-SUMMARY.md
  </read_first>
  <behavior>
    - PROG-01：Windows runner 用仓库 fixture `test/fixtures/tiny_valid.mp4` 打开真实 media_kit `Player`；在创建/订阅 `MediaKitPlayerPort` 之前独立订阅原始 `Player.stream.duration` 与 `Player.stream.position`，并以单调递增序号记录 `raw.player.stream.duration/position`，再记录 port snapshot/stream、`PlayerControlsState`、ViewModel/Timeline 装配和 ProgressBar semantics/widget value。
    - harness 有效性与项目链诊断分离：fixture 必须成功打开且原始 Player 必须产生大于 0 的 duration；其后任何项目侧层可以缺值，但诊断仍须把每层记为 `observed` 或 `missing-after-deadline`，保存完整有序 trace，并由相邻层证据算出唯一首断点。
    - PROG-01：duration 的相邻链为 raw Player stream → port stream → state notifier → ViewModel/Timeline identity → widget/semantics；position 使用同一顺序，并至少记录一个原始 emission，以判断值从哪一条项目边界开始消失。
    - PROG-02/PROG-03：任务 1 的 fake tracer 继续隔离 state-to-progress、seek 与 pointer-hit 后半链；真实运行证据只补齐 fake 无法证明的原始 Player/MediaKitPlayerPort 边界，不替代该 tracer。
    - 只有 fixture/原始 Player 信号无效，或相邻证据无法推出唯一项目侧 `file:symbol` 时命令非零退出并阻塞 Plan 02；port、state 或 widget 缺值本身是可诊断结果，不能让 trace 在写出 `First broken link` 前提前失败。
  </behavior>
  <action>先创建 `integration_test/progress_bar_real_runtime_diagnosis_test.dart`：初始化 integration binding 与 MediaKit，在 Windows 上构造真实 `Player`，先直接订阅 `Player.stream.duration`/`Player.stream.position`，再构造 `MediaKitPlayerPort`，以 `Directory.current` 下的仓库相对路径定位并打开 `test/fixtures/tiny_valid.mp4`，最后把 port 接入包含 `PlayerControlsState → ControlBarViewModel → ControlBarTimeline → ProgressBar` 的诊断 widget 树。诊断 harness 使用统一 `[P39]`、单调序号、层名、状态和毫秒值记录 `fixture.open`、`raw.player.stream.duration/position`、`port.subscribe`、`port.snapshot.duration/position`、`port.stream.duration/position`、`state.durationMs/positionMs`、ViewModel/Timeline notifier identity、`widget.durationMs/positionMs/semantics`；日志只能写 fixture basename，不得写绝对路径，也不得为采集日志修改生产文件。等待窗口结束时，无论下游是否收到值，都为每个预期层写出 `observed` 或 `missing-after-deadline`，并在 teardown 中取消原始订阅、取消诊断监听且 dispose 真实 Player。

把 evaluator 写成确定性的相邻层规则，而不是要求全链先健康：先单独断言 fixture 已打开且 `raw.player.stream.duration` 有大于 0 的值，否则判定 harness 无效；然后对同一有效 duration 按顺序比较 raw→port→state→ViewModel/Timeline→widget。若 raw 有效而 port 在 deadline 内无对应非零值，结论为 `lib/ui/player/media_kit_player_port.dart:MediaKitPlayerPort.duration`；若 port 有效而 state 缺失，依据 init 或 replacement 场景结论为 `lib/ui/player/player_video_controls.dart:PlayerControlsState.init` 或 `PlayerControlsState.updateSources`；若 state 有效但 ViewModel identity/value 不匹配，结论为 `lib/ui/player/player_video_controls.dart:_createControlBarViewModel`；若 ViewModel 有效但 Timeline 传入值不匹配，结论为 `lib/ui/player/control_bar_timeline.dart:ControlBarTimeline.build`；若 Timeline 输入有效但 widget/semantics 缺失，结论为 `lib/ui/player/progress_bar.dart:_ProgressBarState.build`。position 按相同边界作为交叉证据；snapshot 只用于说明订阅时状态，不得替代 raw stream 上游证据。若多个相邻边界同时缺少可比较证据，选择从 raw 起第一个“上游 observed、紧邻下游 missing/mismatch”的边界；若不存在这样的唯一边界则 evaluator 失败并禁止写断点结论。

执行且仅以这条 Windows 命令采集真实证据：`mkdir -p .planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/.tmp &amp;&amp; set -o pipefail &amp;&amp; D:/flutter/bin/flutter test integration_test/progress_bar_real_runtime_diagnosis_test.dart -d windows --reporter expanded 2&gt;&amp;1 | tee .planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/.tmp/39-real-runtime.log`。命令成功后，从日志复制完整连续的 `[P39]` 记录到 `39-DIAGNOSIS.md` 的 `Real Windows evidence` fenced 区，并在相邻位置原样记录命令；不得丢弃 downstream missing 记录。随后创建 `Observed symptoms`、`Data-chain map`、`Evidence matrix`、`First broken link`、`Ruled-out candidates`、`Protected behavior`、`Plan 02 repair contract`。矩阵逐行关联 raw、port、state、ViewModel/Timeline、widget 的 observed/missing 证据与任务 1 fake tracer；`First broken link` 必须采用 evaluator 的唯一结果并引用相邻层序号。若真实命令失败或证据不支持唯一断点，立即停止并报告阻塞。`Protected behavior` 明列 C 事件驱动 v2 的 position 到达容差释放与 `Tokens.progressSeekHoldTimeoutMs` 兜底，以及 AppTooltip 静态提示和自绘实时预览边界。最后删除临时日志；保留 integration test 作为可重跑诊断，并预留 `P39_POST_REPAIR=true` 严格模式供 Plan 02 验证全链恢复。</action>
  <verify>
    <automated>set -o pipefail &amp;&amp; D:/flutter/bin/flutter test integration_test/progress_bar_real_runtime_diagnosis_test.dart -d windows --reporter expanded 2&gt;&amp;1 | tee .planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-runtime-verify.log &amp;&amp; python -c "from pathlib import Path; import re; d=Path(r'.planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-DIAGNOSIS.md').read_text(encoding='utf-8'); log=Path(r'.planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-runtime-verify.log').read_text(encoding='utf-8'); required=['Observed symptoms','Data-chain map','Evidence matrix','Real Windows evidence','First broken link','Ruled-out candidates','Protected behavior','Plan 02 repair contract','raw.player.stream.duration','raw.player.stream.position','port.stream.duration','port.stream.position','state.durationMs','state.positionMs','widget.durationMs','widget.positionMs','observed','missing-after-deadline','PROG-01','PROG-02','PROG-03']; assert all(x in d for x in required), [x for x in required if x not in d]; assert re.search(r'raw\.player\.stream\.duration[^\n]*(?:value=)?[1-9]\d*', log); assert 'diagnosis.firstBrokenBoundary=' in log; assert re.search(r'First broken link[\s\S]{0,800}`lib/[^`]+:[^`]+`', d); assert re.search(r'raw\.player\.stream\.duration[^\n]*(?:value=)?[1-9]\d*', d); assert not re.search(r'[A-Za-z]:[/\\](?!flutter[/\\]bin[/\\]flutter)', d); assert not Path(r'.planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/.tmp/39-real-runtime.log').exists()" &amp;&amp; rm .planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-runtime-verify.log &amp;&amp; D:/flutter/bin/flutter test test/widget/player/progress_bar_data_chain_diagnosis_test.dart --reporter expanded &amp;&amp; git diff --exit-code -- lib/ui/player/media_kit_player_port.dart lib/ui/player/player_video_controls.dart lib/ui/player/progress_bar.dart</automated>
  </verify>
  <acceptance_criteria>
    - Windows 命令真实打开 `tiny_valid.mp4`；可执行断言要求 `raw.player.stream.duration` 出现大于 0 的毫秒值，并要求原始 position、port、state、ViewModel/Timeline、widget 各层都有 `observed` 或 `missing-after-deadline` 记录。
    - port/state/widget 缺值时测试仍先保存完整连续 trace，并输出唯一 `diagnosis.firstBrokenBoundary=lib/...:symbol`；只有原始 Player 无有效 duration 或相邻层无法形成唯一断点时测试失败。
    - `39-DIAGNOSIS.md` 保存精确命令和经过路径清理的实际 `[P39]` 输出；Python 断言拒绝用户媒体绝对路径、无原始非零 duration、无 missing/observed 状态或缺少单一 `file:symbol` 的文档。
    - `First broken link` 只命名 raw→port→state→ViewModel/Timeline→widget 顺序中第一个上游 observed、紧邻下游 missing/mismatch 的项目符号，并引用两侧有序日志；snapshot 不得作为 raw stream 的替代证据。
    - 三个候选类别（port 订阅、updateSources 时序、widget 装配/pointer 命中）各有真实或 fake 隔离证据；`Plan 02 repair contract` 列出精确修改符号与严格 post-repair 行为，临时日志已移除，生产文件与任务开始时一致。
  </acceptance_criteria>
  <done>真实 Windows 原始 Player 到 port/state/widget 的完整有序证据已落入诊断工件；下游缺值能够被保留并映射到唯一项目侧首断点，只有无有效 raw 信号或结论不唯一才阻塞 Plan 02。</done>
</task>

</tasks>

## Artifacts this phase produces

- 新文件 `test/widget/player/progress_bar_data_chain_diagnosis_test.dart`；测试可创建文件私有 harness/helper，但不得新增生产 class、function 或 ValueNotifier。
- 新文件 `integration_test/progress_bar_real_runtime_diagnosis_test.dart`；仅用于 Windows 上以仓库 fixture 采集真实 PlayerPort → state → widget 时序，不成为生产入口。
- 新文件 `.planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-DIAGNOSIS.md`；其 `Real Windows evidence`、`First broken link` 与 `Plan 02 repair contract` 是 Plan 02 的阻塞输入契约。
- 本计划不新增或修改生产符号；任何临时诊断符号必须在任务完成前撤销。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| media_kit Player stream/state → project PlayerPort | 第三方异步事件与快照进入项目控制状态；事件可能迟到、丢首值或来自已替换 source。 |
| pointer events → seek command | 用户坐标转换为媒体时间并触发原生 seek；禁用态或遮挡层不得产生错误命令。 |
| diagnostic output → repository artifact | 日志可证明时序，但不得固化本地媒体绝对路径或第三方包改动。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-39-01 | Tampering | `PlayerControlsState.updateSources` | high | mitigate | 旧新 source 发送相反事件，断言只有 active source 改变 UI。 |
| T-39-02 | Denial of Service | stream subscription lifecycle | medium | mitigate | 记录每轮 stream 监听数并验证 replacement 后旧 port 无监听。 |
| T-39-03 | Information Disclosure | diagnosis logs | medium | mitigate | 文档只记录 basename、毫秒值与符号，不记录用户路径。 |
| T-39-04 | Repudiation | root-cause conclusion | medium | mitigate | 每个结论绑定测试命令、实际输出和 file:symbol。 |
| T-39-SC | Tampering | package supply chain | low | accept | 不安装依赖；media_kit 仅只读。 |
</threat_model>

<verification>
先运行 fake 单文件诊断测试隔离后半链，再用 `D:/flutter/bin/flutter test integration_test/progress_bar_real_runtime_diagnosis_test.dart -d windows --reporter expanded` 打开仓库 fixture，独立采集 raw Player.stream，并采集 port → state → ViewModel/Timeline → widget 的 observed/missing 有序日志。只有 raw duration 大于 0、完整 trace 已保存且 evaluator 依据第一对“上游 observed、紧邻下游 missing/mismatch”证明唯一项目侧 `file:symbol` 时才通过；下游缺值是诊断输入，不得提前中断落盘。raw 信号无效或首断点不唯一才阻塞 Plan 02。最后确认临时 instrumentation 与日志已移除，且 `git diff --name-only` 不出现 Pub cache、media_kit package 或 vendored libmpv 路径。
</verification>

<success_criteria>
- duration/position 全链路的首个断点由真实 Windows 原始 Player.stream、PlayerPort 及后续层的相邻 observed/missing 输出与 fake 后半链隔离共同定位，而非仅由文档文字断言。
- 真实证据包含 raw Player stream、subscription、snapshot、port stream、PlayerControlsState notifier、ViewModel/Timeline identity 和 widget value/semantics；下游缺层必须落盘并参与首断点推导，只有 raw duration 无效或断点不唯一时计划阻塞。
- PROG-01、PROG-02、PROG-03 各自对应的禁用态、seek、hover 证据已记录。
- C 事件驱动 v2 与 AppTooltip/自绘气泡职责边界被列为后续修复不可破坏的契约。
- 没有第三方 package 文件或生产代码被诊断性改写。
</success_criteria>

<output>
完成后创建 `.planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-01-SUMMARY.md`
</output>
