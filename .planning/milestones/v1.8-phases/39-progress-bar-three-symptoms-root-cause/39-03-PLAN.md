---
phase: 39-progress-bar-three-symptoms-root-cause
plan: 03
type: execute
wave: 3
depends_on: ["39-02"]
gap_closure: true
gap_ids: [G-39-2]
files_modified:
  - lib/ui/player/player_video_controls.dart
  - lib/ui/player/progress_bar.dart
  - test/widget/player/player_video_controls_test.dart
  - test/widget/player/progress_bar_data_chain_diagnosis_test.dart
  - test/widget/player/progress_bar_test.dart
autonomous: true
requirements: [PROG-02]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - statement: "G-39-2 truth：拖拽松手后时间气泡保持拖拽目标时间，不随旧 position 事件回跳；pump 一帧（无指针移动）后气泡文本仍为松手目标时间。"
      verification: explicit
    - statement: "PlayerControlsState.seek 不再乐观写 positionMs；positionMs 只由 port 的 duration/position stream 与快照更新。seek-hold 只由 active source 的真实 position 到达（progressSeekArriveToleranceMs 容差内）或 progressSeekHoldTimeoutMs 超时兜底释放 — C 事件驱动 v2 语义被恢复而非被替换。"
      verification: explicit
    - statement: "dragEnd 将 hover.x 同步为松手分数；即使 seek-hold 因 FakeEngine 同步 position 写入而瞬时完成，Tooltip 也从拖拽目标时间连续过渡，不闪回拖拽前的 hover 坐标。"
      verification: explicit
    - statement: "现有 player_video_controls_test.dart 的 'seek 乐观更新 positionMs 再调 port.seek' 断言的正是本 gap 的 bug 行为；该测试必须改写为 stream-确认契约，这不是'改测试迁就实现'，而是纠正编码了缺陷的旧断言。"
      verification: judgment
  artifacts:
    - path: "lib/ui/player/player_video_controls.dart"
      provides: "seek() 仅转发 port.seek 并保留 clamp；positionMs 更新权完全归属 stream/快照路径"
    - path: "lib/ui/player/progress_bar.dart"
      provides: "dragEnd 捕获松手分数并同步 hover.x，Tooltip 在 hold 释放前后连续"
    - path: "test/widget/player/player_video_controls_test.dart"
      provides: "state 层 stream-确认 seek 契约（port.seek 被调、positionMs 不被乐观写入、emitPosition 后更新）"
    - path: "test/widget/player/progress_bar_data_chain_diagnosis_test.dart"
      provides: "state→timeline 集成层 RED→GREEN：拖拽松手后 hold 保持目标直到 FakePlayerControls.emitPosition 确认；超时兜底路径可测"
    - path: "test/widget/player/progress_bar_test.dart"
      provides: "鼠标 hover + 拖拽 + 松手组合回归：pump 一帧无指针移动，气泡仍显示目标时间"
  key_links:
    - from: "lib/ui/player/progress_bar.dart"
      to: "lib/ui/player/player_video_controls.dart"
      via: "widget.onSeek(ms) → PlayerControlsState.seek → port.seek，全程不写 positionMs"
      pattern: "onSeek\(|_port\.seek"
    - from: "lib/ui/player/player_video_controls.dart"
      to: "lib/ui/player/progress_bar.dart"
      via: "positionMs 只经 _positionSub/_position 快照更新，成为 seek-hold 唯一确认源"
      pattern: "positionMs\.value = ms\(|_positionSub"
    - from: "lib/ui/player/progress_bar.dart"
      to: "ProgressBar Tooltip"
      via: "dragEnd 捕获 releaseFraction 并写入 _hoverNotifier，hold 释放后 Tooltip 分数连续"
      pattern: "_beginSeekHold|_hoverNotifier\.value = _HoverState"
  prohibitions:
    - statement: "不得修改 media_kit package、Pub cache 或 vendored libmpv 文件；修复仅在项目 wrapper、UI 和 tests 中。"
      status: resolved
      verification: test
    - statement: "不得新增第二组 mirrored position notifier（拆分乐观/确认 listenable 方案被否决——39-02 已禁止重复数据源，且最小 diff 优先）；也不得在 ProgressBar 内做'跳过 onSeek 后首次通知'的时序 hack（脆弱、与 throttle 多次 seek 冲突）。"
      status: resolved
      verification: judgment
    - statement: "不得用固定毫秒延迟替换 C 事件驱动 v2 的 position 到达判定；progressSeekArriveToleranceMs 与 progressSeekHoldTimeoutMs 语义不变。"
      status: resolved
      verification: test
    - statement: "不得修改 FakeEngine.seekTo 的同步 position 写入语义——它模拟的是'引擎已确认新位置'的 stream 事件，是合法确认源，不是本 gap 的根因。"
      status: resolved
      verification: test
---

<objective>
关闭 UAT Gap G-39-2（拖拽松手 Tooltip 回跳）：移除 PlayerControlsState.seek 的乐观 positionMs 写入使 seek-hold 恢复事件驱动确认，并在 dragEnd 把 hover.x 同步为松手分数，让时间气泡从拖拽目标连续过渡而不闪回陈旧坐标。

Purpose: 39-02 修复后唯一残留症状是松手瞬间气泡短暂回跳到拖拽开始时的鼠标 x 坐标。根因已由 UAT 诊断锁定：seek() 乐观写入同步满足 _beginSeekHold 到达容差（保持 0ms），_dragNotifier 立即清除，Tooltip 回退到拖拽期间从未更新的陈旧 _hoverX。
Output: 两处最小定点修复 + 三层（state/集成/组件）TDD 回归，全部 headless 可验证。
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-UAT.md
@.planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-02-SUMMARY.md
@.planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-VERIFICATION.md
@lib/ui/player/player_video_controls.dart
@lib/ui/player/progress_bar.dart
@lib/ui/player/control_bar_view_model.dart
@lib/ui/player/control_bar_timeline.dart
@lib/ui/theme/tokens.dart
</context>

<interfaces>
稳定接口零变更：`PlayerPort.seek(Duration)` 不变；`PlayerControlsState.seek(int ms)` 签名不变（仅去掉体内 `positionMs.value = clamped` 一行及其注释）；`ControlBarViewModel.onSeek`、`ControlBarTimeline`、`ProgressBar` 的公开参数全部不变。唯一语义变化是契约层的：`positionMs` 的写入者收敛为 `_positionSub` stream 回调与 `init()/updateSources` 快照读取；`seek()` 只做 clamp + `_port.seek` 转发。禁止新增任何 listenable、stream 或 ViewModel 字段。
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：移除 seek 乐观写入，恢复 seek-hold 事件驱动确认</name>
  <reversibility rating="easy">单行生产行为回撤 + 相邻测试改写；若真实播放下出现 hold 永远超时的反证，可用一行 `positionMs.value = clamped` 回滚。</reversibility>
  <files>lib/ui/player/player_video_controls.dart, test/widget/player/player_video_controls_test.dart, test/widget/player/progress_bar_data_chain_diagnosis_test.dart</files>
  <read_first>
    - .planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-UAT.md
    - lib/ui/player/player_video_controls.dart
    - lib/ui/player/progress_bar.dart
    - lib/ui/theme/tokens.dart
    - test/widget/player/player_video_controls_test.dart
    - test/widget/player/progress_bar_data_chain_diagnosis_test.dart
    - test/helpers/fake_player_controls.dart
    - test/helpers/fake_video_controls.dart
  </read_first>
  <behavior>
    - G-39-2/PROG-02：`state.seek(5000)` 立即调用 `port.seek(5000)`（含 clamp），但 `positionMs.value` 保持原值；随后 `port.emitPosition(5000)` 才使 `positionMs == 5000`。
    - 集成层（_timelineTree）：duration=60000、position=15000 挂载后，鼠标拖拽到 75% 松手并 pump 一帧（无指针移动、无 position 事件）——slider semantics 仍停在 75% 目标、拖拽气泡文本仍为目标时间（hold 生效中）；随后 `player.emitPosition(45000)`（目标容差内）→ hold 释放，semantics/气泡跟随确认位置。
    - 超时兜底：松手后不 emit 任何 position，用 `tester.pump(const Duration(milliseconds: Tokens.progressSeekHoldTimeoutMs))` 推进——hold 被兜底释放，无 pending timer 残留。
    - 既有 source replacement、延迟 duration、tap seek 测试全部保持通过（tap 只断言 port 级 seek 记录，不依赖乐观写入）。
  </behavior>
  <action>先写 RED：把 `player_video_controls_test.dart` 中断言乐观行为的测试（'seek 乐观更新 positionMs 再调 port.seek'）改写为新契约——`state.durationMs.value = 60000; state.positionMs.value = 1000; state.seek(5000);` 断言 `port.lastSeekPosition == 5000ms` 且 `state.positionMs.value == 1000`（未被乐观写入）；再 `port.emitPosition(5000)` 断言 `state.positionMs.value == 5000`。然后在 `progress_bar_data_chain_diagnosis_test.dart` 增加集成用例（命名含 `G-39-2`）：用 `_timelineTree` + FakePlayerControls，鼠标 gesture 先 enter bar 建立真实 hover，再 down→moveTo 75%→up 完成拖拽；pump 一帧断言 semantics 停在目标、气泡文本为目标时间；emitPosition 确认后断言跟随；另断言超时路径。两处 RED 后实施生产修复：删除 `player_video_controls.dart` `seek()` 内 `positionMs.value = clamped;` 一行，并把 doc comment 从"乐观更新让 seek-hold 立即到达容差"改为解释新契约——"positionMs 由 stream 确认；乐观写入会同步满足 ProgressBar seek-hold 容差使拖拽保持 0ms、气泡回退陈旧 hover 坐标（UAT G-39-2）"。为 clamp 与"为何不写本地状态"补 why 注释。不得触碰 init/updateSources 的快照与订阅路径，不得改 `MediaKitPlayerPort`。</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/widget/player/player_video_controls_test.dart test/widget/player/progress_bar_data_chain_diagnosis_test.dart --reporter expanded</automated>
  </verify>
  <acceptance_criteria>
    - `seek()` 生产 diff 仅删除乐观写入并更新注释；`positionMs` 的全部写入点是 `_positionSub` 回调与 init/updateSources 快照。
    - 改写后的 state 测试证明 port.seek 被调且 positionMs 不被 seek 直接改写，emitPosition 后才更新。
    - 集成测试证明松手后 hold 保持目标（semantics + 气泡文本），stream 确认到达容差后释放，超时兜底独立释放且无 pending timer。
    - 两个文件中既有测试（快照初始化、replacement、tap、hover）零回归；不新增任何生产符号。
  </acceptance_criteria>
  <done>seek-hold 不再被组件自身写入满足，G-39-2 的根因链第一环（乐观写入旁路 v2 保持逻辑）已消除并有 state+集成两层测试锁定。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：dragEnd 同步 hover.x 为松手分数并锁定 Tooltip 连续性回归</name>
  <reversibility rating="easy">单点 UI 状态赋值改动，随时可回退；组合回归测试独立于任务 1 可单独 RED。</reversibility>
  <files>lib/ui/player/progress_bar.dart, test/widget/player/progress_bar_test.dart</files>
  <read_first>
    - .planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-UAT.md
    - lib/ui/player/progress_bar.dart
    - lib/ui/theme/tokens.dart
    - test/widget/player/progress_bar_test.dart
    - test/widget/player/progress_bar_source_replacement_test.dart
  </read_first>
  <behavior>
    - G-39-2：duration=60000 时，鼠标 hover 到 25% 处建立 hover 态，随后同 gesture down 于 25%、拖到 75%、up 松手；pump 一帧（无任何指针移动）——气泡文本仍为 75% 目标时间（'00:45'），不是 25% 的 '00:15'。
    - hold 释放后（FakeEngine.seekTo 同步写 position 使 hold 瞬时完成，或手动推进 position）气泡文本仍为松手目标时间——Tooltip 从拖拽目标连续过渡，不闪回。
    - 拖拽中 MouseRegion.onExit 不清 hover 的既有行为、duration<=0 边界、二次拖拽与 dispose 无 pending timer/listener 全部保持。
  </behavior>
  <action>先写 RED：在 `progress_bar_test.dart` 增加 G-39-2 组合用例——参照既有 'hover exit during drag does not crash' 的 gesture 模式：`createGesture(kind: PointerDeviceKind.mouse)` → moveTo 25% 处（建立 hover=true, x=0.25）→ down → moveTo 75%（拖拽中 onHover 不触发，hover.x 仍 0.25）→ up → `tester.pump()` 一帧断言时间文本为 '00:45'（当前实现为 '00:15'，RED）。RED 后修 `progress_bar.dart` `onHorizontalDragEnd`：在调用 `widget.onSeek` 之前捕获 `final releaseFraction = _dragNotifier.value;`（hold 可能被同步确认写瞬时清空，必须在 onSeek/_beginSeekHold 之前取值），并把 L343 `_hoverNotifier.value = _HoverState(true, _hoverX)` 改为 `_HoverState(true, releaseFraction ?? _hoverX)`，注释说明"拖拽中 onHover 不触发，hover.x 停在拖拽前坐标；松手必须同步为松手分数，否则 hold 释放后气泡回跳（UAT G-39-2）"。再补一条瞬时完成路径用例：FakeEngine（seekTo 同步写 position）下同样拖拽松手，pump 后断言文本仍为 '00:45'——覆盖任务 1 修复后 `seek()` 不再写 position、FakeEngine 同步确认使 hold 在 `_beginSeekHold` 的立即 `listener.call()` 中完成的时序。跑全套 focused 回归确认无破坏。</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/widget/player/progress_bar_test.dart test/widget/player/progress_bar_source_replacement_test.dart test/widget/player/progress_bar_data_chain_diagnosis_test.dart test/widget/player/player_video_controls_test.dart test/widget/player/player_video_controls_lifecycle_test.dart --reporter expanded &amp;&amp; D:/flutter/bin/flutter analyze &amp;&amp; git diff --check &amp;&amp; python -c "import subprocess; files=subprocess.check_output(['git','diff','--name-only'], text=True).splitlines(); bad=[p for p in files if '/media_kit/' in p.replace(chr(92),'/') or p.replace(chr(92),'/').startswith('.pub-cache/')]; assert not bad, bad"</automated>
  </verify>
  <acceptance_criteria>
    - 组合回归用例证明：松手后 pump 一帧（无指针移动），气泡显示拖拽目标时间；hold 瞬时完成与流确认两条路径下文本均连续不闪回。
    - `progress_bar.dart` diff 仅涉及 dragEnd 的分数捕获与 hover 同步赋值及注释；`_beginSeekHold`、到达容差、超时常量、onExit 拖拽保护逻辑零改动。
    - 五个 focused 测试文件全部通过；`flutter analyze` 零 issue；`git diff --check` 干净；diff 不含 media_kit/Pub cache 路径。
  </acceptance_criteria>
  <done>Tooltip 在松手瞬间与 hold 释放后均保持拖拽目标时间，G-39-2 的陈旧 hover.x 回退链已切断，组合回归已入 `progress_bar_test.dart`。</done>
</task>

</tasks>

## Artifacts this phase produces

- 生产改动：仅 `player_video_controls.dart` `seek()` 移除乐观写入 + 注释契约更新，`progress_bar.dart` `onHorizontalDragEnd` 的分数捕获与 hover 同步。不新增任何 public class、function、ValueNotifier、stream 或依赖。
- 测试符号：`player_video_controls_test.dart` 改写 1 个既有断言（编码了 bug 行为）；`progress_bar_data_chain_diagnosis_test.dart` 新增 G-39-2 集成用例（hold 保持/确认释放/超时兜底）；`progress_bar_test.dart` 新增鼠标 hover+拖拽组合回归 2 条（陈旧 hover 路径 + 瞬时确认路径）。
- 本计划不创建新的生产文件或诊断工件；根因证据已在 39-UAT.md，修复证据落在 39-03-SUMMARY.md。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| seek 命令 → position 状态 | 用户 seek 意图经 clamp 后直达 Player；positionMs 只信 stream 确认，自身命令不得伪造确认。 |
| pointer 坐标 → Tooltip 分数 | 拖拽期间 onHover 静默，hover.x 可能陈旧；松手时必须以确定性的松手分数覆盖，不得信任陈旧指针状态。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-39-09 | Tampering | optimistic position write | high | mitigate | state 测试证明 seek 不写 positionMs，唯 stream/快照可写。 |
| T-39-10 | Tampering | stale hover.x after dragEnd | medium | mitigate | 组合回归断言 pump 一帧后文本仍为目标时间。 |
| T-39-11 | Denial of Service | seek-hold 永久卡住 | medium | mitigate | 超时兜底用例独立验证 progressSeekHoldTimeoutMs 释放且无 pending timer。 |
| T-39-12 | Repudiation | 旧断言编码 bug 行为 | medium | mitigate | must_haves 显式授权改写 'seek 乐观更新' 测试，SUMMARY 记录该 deviation。 |
| T-39-SC | Tampering | package supply chain | low | accept | 无依赖安装；diff gate 排除第三方路径。 |
</threat_model>

<verification>
先跑任务 1 的两个 focused 文件确认 RED→GREEN，再跑任务 2 的五文件组合 gate + `flutter analyze` + `git diff --check` + 第三方路径扫描。核心判据：拖拽松手 pump 一帧（无指针移动）气泡仍显示拖拽目标时间；seek-hold 仅由 stream 到达或超时释放；`positionMs` 写入者收敛为 stream/快照。真实 Windows 上"松手瞬间气泡不回跳"的视觉确认属于 UAT 复测/Phase 41 smoke 范畴，headless 绿灯不替代人工复测，但组合回归已把该时序锁定为确定性断言。
</verification>

<success_criteria>
- 拖拽松手后时间气泡保持拖拽目标时间，不出现回到拖拽前位置的短暂闪跳（G-39-2 truth 关闭）。
- `PlayerControlsState.seek` 不再乐观写 `positionMs`；C 事件驱动 v2 的到达判定与超时兜底语义原样保持并有测试覆盖。
- dragEnd 后 hover.x 等于松手分数，Tooltip 从拖拽目标连续过渡。
- 五个 focused 测试文件、`flutter analyze`、`git diff --check` 全部通过；无 media_kit/Pub cache 改动，无新增生产符号或第二数据源。
- 39-VERIFICATION.md 曾标记的 behavior_unverified 项（same-source rollback/timeout）由任务 1 的集成用例补齐命名测试证据。
</success_criteria>

<output>
完成后创建 `.planning/milestones/v1.8-phases/39-progress-bar-three-symptoms-root-cause/39-03-SUMMARY.md`（frontmatter 含 `gap_closure: true` 与 `gap_ids: [G-39-2]`，供 /gsd-verify-work 将 G-39-2 标记为 resolved）
</output>
