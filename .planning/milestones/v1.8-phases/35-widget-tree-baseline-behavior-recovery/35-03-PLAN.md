---
phase: 35-widget-tree-baseline-behavior-recovery
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - test/widget/player/player_video_controls_test.dart
  - test/helpers/fake_player_controls.dart
  - test/helpers/fake_video_controls.dart
  - lib/ui/player/player_video_controls.dart
autonomous: true
requirements:
  - BASE-02
  - BASE-03
  - BASE-05
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PlayerVideoControls source replacement 后只有新 player、engine、resize、currentFileName 与 video port 能驱动当前 State。"
    - "GlobalKey reparent 的 deactivate/activate 保持同一 State，stream 订阅不重复，生命周期 listener 对称恢复。"
    - "字幕 padding 始终等于每个 source 自身 base 加一次控制栏 inset；隐藏恢复 base，旧 source replacement/dispose 后不再被写。"
    - "dispose 后旧新 source、engine、resize 与文件名变化都不排帧或触发 route/padding 调用。"
  artifacts:
    - path: "test/widget/player/player_video_controls_test.dart"
      provides: "source replacement、reparent、activate/deactivate、dispose、字幕 padding 与旧 source 隔离回归"
    - path: "test/helpers/fake_player_controls.dart"
      provides: "8 条 stream 的订阅访问与解绑证据"
    - path: "test/helpers/fake_video_controls.dart"
      provides: "route、mounted 与 subtitle padding 调用历史证据"
  key_links:
    - from: "lib/ui/player/player_video_controls.dart"
      to: "test/helpers/fake_player_controls.dart"
      via: "PlayerControlsState.updateSources 取消旧订阅并订阅新 source"
      pattern: "updateSources|_cancelSubscriptions"
    - from: "lib/ui/player/player_video_controls.dart"
      to: "test/helpers/fake_video_controls.dart"
      via: "deactivate/activate guard 与每-source subtitle base cache"
      pattern: "_isDeactivating|_subtitleBasePadding"
---

<objective>
强化 PlayerVideoControls 的 source replacement、reparent、activate/deactivate、dispose 与 subtitle padding 生命周期回归，并仅在失败测试证明时做必要修复，覆盖 BASE-02、BASE-03、BASE-05。

Purpose: 在 Phase 36 拆 rebuild 边界前，先保证所有可替换 source 和 route-local 状态不会串扰、重复订阅或污染字幕安全区。
Output: 强化的 fake-port widget tests、必要的 fake 观测字段，以及按红灯证据限定的 PlayerVideoControls 局部修复。
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-widget-tree-baseline-behavior-recovery/35-RESEARCH.md
@lib/ui/player/player_video_controls.dart
@test/helpers/fake_player_controls.dart
@test/helpers/fake_video_controls.dart
@test/widget/player/player_video_controls_test.dart
</context>

<interfaces>
`PlayerControlsState.updateSources(PlayerPort port, {required MediaEngine engine})` 必须复用下游 ValueNotifier identity，同时取消旧 port 的 8 条 stream subscription、读取新快照并建立新订阅。

`VideoControlsPort` 提供 route-local `player`、`isFullscreen`、`isMounted`、`subtitlePadding`、`toggleFullscreen()`、`exitFullscreen()` 与 `setSubtitleViewPadding()`。

`PlayerVideoControls.didUpdateWidget` 负责 video/engine/currentFileName/resizing replacement；`deactivate` 先设置 inactive guard 并解绑 lifecycle listeners，`activate` 对称恢复，`dispose` 释放 timer、focus、animation、auto-hide 与 controls state。

`FakePlayerControls` 已暴露 `hasListeners` 和 `streamListenAccessCount`；`FakeVideoControlsPort` 已暴露 fullscreen read、route command 和 subtitle padding history，可按需要只增加测试观测字段，不改变生产接口。
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>任务 1：锁定 replacement + reparent 后的新 source 独占驱动链</name>
  <files>test/widget/player/player_video_controls_test.dart, test/helpers/fake_player_controls.dart, test/helpers/fake_video_controls.dart, lib/ui/player/player_video_controls.dart</files>
  <read_first>
    - .planning/phases/35-widget-tree-baseline-behavior-recovery/35-RESEARCH.md
    - lib/ui/player/player_video_controls.dart
    - test/helpers/fake_player_controls.dart
    - test/helpers/fake_video_controls.dart
    - test/widget/player/player_video_controls_test.dart
  </read_first>
  <behavior>
    - replacement 与 GlobalKey reparent 同帧发生后，State identity 不变，old player 无 listener，new player 恰好建立一轮 8 条 stream 订阅。
    - old player、old engine、old resize notifier、old currentFileName 和 old video port 的后续变化均不安排帧、不改变标题/播放状态，也不新增 subtitle padding/fullscreen 读取。
    - new player、new engine、new resize notifier、new currentFileName 与 new video port 均继续驱动当前控件。
    - 多次 reparent 不增加 streamListenAccessCount；deactivate 期间 route/padding 访问被拦截，activate 后恢复一次有效监听。
  </behavior>
  <action>
    在现有 `reparent 同时替换 source` 与 `替换 currentFileName` 用例基础上扩展一个完整 replacement 场景：同时替换 video、engine、resizing、currentFileName，再在同一帧移动 GlobalKey。为每个旧 source 记录 replacement 后基线计数；先完成必要的固定时长 pump 以排空本控件已知的 auto-hide/animation 帧，再触发单个旧事件并只比较该事件可归因的标题、订阅、fullscreen read、padding history 与 fake 计数，避免把全局 `hasScheduledFrame` 当作唯一证据。随后逐个触发新 source，证明其仍有效。显式断言 new player `streamListenAccessCount == 8`，重复 reparent 后仍为 8。

    如现有 fake 缺少精确观测能力，只添加只读计数/历史字段和测试 helper，保持 fake 简单且不加载 media_kit。先运行新增测试；只有红灯显示旧 listener、合并 Listenable、resize source 或 lifecycle attach/detach 泄漏时，才局部修复 `didUpdateWidget`、`_attachLifecycleListeners`、`_detachLifecycleListeners`、`deactivate`/`activate`。保留 ValueNotifier identity 与现有直接 ControlBar 架构，不恢复旧 overlay，不改 media_kit。
  </action>
  <verify>
    <automated>flutter test test/widget/player/player_video_controls_test.dart --plain-name "reparent 同时替换全部 source 后只响应新依赖"</automated>
  </verify>
  <acceptance_criteria>
    - 一个端到端 widget 用例同时覆盖 video/player/engine/resizing/currentFileName replacement 与 reparent 交错。
    - 旧 source 的每类副作用均有显式负向断言，新 source 的每类驱动均有正向断言。
    - 8 条 stream 只建立一轮，重复 activate/reparent 不重复初始化 PlayerControlsState。
    - 生产修复仅限失败测试定位的 listener/source 生命周期方法。
  </acceptance_criteria>
  <done>replacement + reparent 的最窄端到端路径证明当前 State 只连接新 source，旧依赖完全隔离。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：覆盖 subtitle base/inset、隐藏恢复与 dispose 后零写入</name>
  <files>test/widget/player/player_video_controls_test.dart, test/helpers/fake_video_controls.dart, lib/ui/player/player_video_controls.dart</files>
  <read_first>
    - .planning/phases/35-widget-tree-baseline-behavior-recovery/35-RESEARCH.md
    - lib/ui/player/player_video_controls.dart
    - test/helpers/fake_video_controls.dart
    - test/widget/player/player_video_controls_test.dart
  </read_first>
  <behavior>
    - 非零 base padding 的 source 在 controls visible 时得到 `base + inset`，auto-hide 后精确恢复 base，再显示时仍只加一次 inset。
    - 同一 source 多次 reparent/activate 不会把已写入 padding 当作新 base。
    - replacement 到另一个非零 base source 后，新 source 立即按当前 visibility 同步；旧 source 的 padding history 从 replacement 起不再增长，即使 old player/engine/resize 再变化。
    - `isMounted=false`、deactivate 窗口与 dispose 后均不调用 setSubtitleViewPadding；恢复 activate 后只对当前 source 恢复同步。
  </behavior>
  <action>
    扩展 subtitle 生命周期测试，使用两个不同的非零 base padding，并通过 playing stream/auto-hide 的实际 visible 变化验证 visible、hidden、再次 visible 三态。记录每个 fake port 的 padding history，明确断言 hidden 恢复各自 base、reparent 两次不重复 inset、replacement 后旧 port history 冻结、新 port 从自己的 base 计算。最后卸载控件，先 pump 至无本控件遗留回调，再逐个驱动旧新 player、engine、resize 与文件名，断言 padding history、fake route/read 计数与由这些事件引发的可观察状态均不再变化；不要以可能受测试框架定时器影响的全局 scheduled-frame 标志作为唯一断言。

    先运行测试。若失败，只修复 `_subtitleBasePadding` 的 per-source reset、`_syncSubtitlePadding` guard 或 deactivate/activate/dispose 的监听顺序；不得把 padding 移回 PlayerScreen `_videoKey`，不得引入跨 route 共享 sink，也不得修改 media_kit。完成后运行整个 player_video_controls 测试、Phase 35 targeted suite、analyzer 与 diff check。
  </action>
  <verify>
    <automated>flutter test test/widget/player/player_video_controls_test.dart &amp;&amp; flutter test test/widget/shared/glass_button_test.dart test/widget/player/player_screen_accessibility_resize_test.dart test/widget/player/player_screen_stop_empty_state_test.dart test/widget/player/player_keyboard_actions_test.dart test/widget/player/drop_handler_test.dart test/integration/controls_flow_test.dart test/integration/error_propagation_test.dart &amp;&amp; flutter analyze &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - 两个 source 的 base padding 分别保存，visible/hidden/re-visible 结果精确且不累加。
    - replacement 后旧 port 的 padding history 冻结，新 port 立即按当前可见性同步。
    - deactivate、未挂载与 dispose 三种生命周期边界均阻止不安全写入。
    - PlayerVideoControls 全文件测试、Phase 35 targeted suite、analyzer 与 diff check 通过。
  </acceptance_criteria>
  <done>BASE-05 的 source、reparent、activate/deactivate、dispose、subtitle padding 与旧 source 隔离全部有明确自动化断言。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|---|---|
| 外部 stream/notifier → PlayerVideoControls State | 已替换或已释放 source 不得继续排帧或写 UI/route 状态。 |
| route-local VideoControlsPort → media_kit VideoState | inactive/unmounted route 不得接收 fullscreen 或 subtitle padding 调用。 |

## STRIDE 威胁登记

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-35-07 | Tampering | source replacement | high | mitigate | old/new 双向断言、8-stream 订阅计数与 scheduled-frame 检查。 |
| T-35-08 | Denial of Service | deactivate/dispose listener 泄漏 | high | mitigate | reparent 多轮、inactive guard、dispose 后驱动全部外部 source。 |
| T-35-09 | Tampering | subtitle padding cache | medium | mitigate | 每 source 非零 base + visible/hidden/re-visible/replacement history 精确断言。 |
| T-35-SC | Tampering | package 供应链 | low | accept | 无 package 安装。 |
</threat_model>

<verification>
1. `player_video_controls_test.dart` 全部通过，且新增测试不依赖真实 native media runtime。
2. Phase 35 targeted suite、`flutter analyze`、`git diff --check` 通过。
3. `git diff` 仅包含测试/fake 与由失败测试授权的局部生命周期修复。
4. 未恢复 ControlsOverlay、未修改 media_kit、未处理未确认截图。
</verification>

<success_criteria>
- BASE-05 的五类生命周期行为均由可执行断言覆盖。
- BASE-02 的 route-local controls 架构保持不变。
- BASE-03 的播放/控制回归门在强化后仍通过。
</success_criteria>

<output>
完成后创建 `.planning/phases/35-widget-tree-baseline-behavior-recovery/35-03-SUMMARY.md`
</output>
