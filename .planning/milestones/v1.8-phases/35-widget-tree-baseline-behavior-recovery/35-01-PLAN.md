---
phase: 35-widget-tree-baseline-behavior-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/35-widget-tree-baseline-behavior-recovery/35-WIDGET-TREE-BASELINE.md
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - BASE-03
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Git 历史只作为只读行为基线，不覆盖当前工作树，也不处理未确认截图。"
    - "当前 PlayerScreen → Video.controls → PlayerVideoControls → ControlBar 主路径被源码与测试证据共同锁定。"
    - "ControlsOverlay、旧 fullscreen plugin 和 media_kit 修改均明确排除在恢复候选之外。"
    - "关键播放、窗口与控制交互拥有可重复执行的 Phase 35 自动化基线。"
  artifacts:
    - path: ".planning/phases/35-widget-tree-baseline-behavior-recovery/35-WIDGET-TREE-BASELINE.md"
      provides: "按提交、父提交、文件与行为分类的 widget tree 基线及回归命令结果"
  key_links:
    - from: "lib/ui/player/player_screen.dart"
      to: "lib/ui/player/player_video_controls.dart"
      via: "Video.controls 的 _buildControls/playerVideoControls 装配"
      pattern: "controls: _buildControls|playerVideoControls"
    - from: "lib/ui/player/player_video_controls.dart"
      to: "lib/ui/player/control_bar.dart"
      via: "PlayerVideoControls._buildControlBar 构造 ControlBar"
      pattern: "ControlBar\\("
---

<objective>
建立 Phase 35 的只读 Git widget tree 与行为回归基线，覆盖 BASE-01、BASE-02、BASE-03。

Purpose: 在任何定点修复前先证明哪些当前结构必须保留、哪些旧结构禁止恢复，并为后续两个计划提供可执行的绿色基线。
Output: `35-WIDGET-TREE-BASELINE.md`，包含历史边界、按文件行为矩阵、当前脏工作树隔离记录和关键测试结果。
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-widget-tree-baseline-behavior-recovery/35-RESEARCH.md
@lib/ui/player/player_screen.dart
@lib/ui/player/player_video_controls.dart
@lib/ui/player/control_bar.dart
@lib/ui/window/custom_title_bar.dart
@lib/ui/shared/glass_container.dart
</context>

<tasks>

<task type="tracer">
  <name>任务 1：沿当前 controls 主路径建立只读 Git 行为基线</name>
  <files>.planning/phases/35-widget-tree-baseline-behavior-recovery/35-WIDGET-TREE-BASELINE.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/35-widget-tree-baseline-behavior-recovery/35-RESEARCH.md
    - lib/ui/player/player_screen.dart
    - lib/ui/player/player_video_controls.dart
    - lib/ui/player/control_bar.dart
    - lib/ui/window/custom_title_bar.dart
    - lib/ui/shared/glass_container.dart
  </read_first>
  <action>
    为 BASE-01 与 BASE-02 创建 `35-WIDGET-TREE-BASELINE.md`。先记录执行前 `git status --short`，明确现有已修改文件和四个未追踪 PNG 仅作保护清单，不删除、不暂存、不提交。只使用 `git show`、`git diff`、`git log` 等只读命令，分别比较 `e0083842^..e0083842`、`e0083842..f590cce2`、`f590cce2..6e0edbb8`、`6e0edbb8..HEAD`，并用 `git diff -- <相关路径>` 单列当前未提交增量；禁止任何 reset、checkout、restore 或整树覆盖操作。

    基线表按文件列出 `player_screen.dart`、`player_video_controls.dart`、`control_bar.dart`、`glass_container.dart`、`custom_title_bar.dart` 及相关测试在每个边界的结构变化，并将差异归入三类：保留的当前稳定化、已删除且禁止恢复的旧架构、只有失败测试才能授权的定点恢复候选。用源码位置和只读 Git 证据锁定 `PlayerScreen → Video.controls → PlayerVideoControls → ControlBar`，明确 `ControlsOverlay`、旧 fullscreen plugin 与 media_kit/libmpv 修改不是候选。不要把未确认截图内容或视觉判断写成恢复依据。
  </action>
  <verify>
    <automated>git status --short &amp;&amp; git diff --check &amp;&amp; git diff --name-status e0083842^..e0083842 -- lib/ui/player lib/ui/shared/glass_container.dart lib/ui/window test/widget/player test/widget/shared &amp;&amp; git diff --name-status e0083842..f590cce2 -- lib/ui/player lib/ui/shared/glass_container.dart lib/ui/window test/widget/player test/widget/shared &amp;&amp; git diff --name-status f590cce2..6e0edbb8 -- lib/ui/player lib/ui/shared/glass_container.dart lib/ui/window test/widget/player test/widget/shared &amp;&amp; git diff --name-status 6e0edbb8..HEAD -- lib/ui/player lib/ui/shared/glass_container.dart lib/ui/window test/widget/player test/widget/shared</automated>
  </verify>
  <acceptance_criteria>
    - 基线文档同时覆盖三个指定提交、各自父边界、HEAD 和当前未提交增量。
    - 每个关键文件都有行为分类与证据，不把历史文件整体视为恢复目标。
    - 文档明确锁定当前 controls 主路径及三条禁止边界。
    - 比较执行前后时排除本任务新建的基线文档；其余受保护源码、测试与未追踪截图清单一致，截图未被读取、删除、暂存或提交。
  </acceptance_criteria>
  <done>BASE-01 的按文件历史基线可复核，BASE-02 的当前主路径和禁止恢复边界有源码与 Git 双重证据。</done>
</task>

<task type="auto">
  <name>任务 2：冻结关键播放、窗口与控制交互的自动化基线</name>
  <files>.planning/phases/35-widget-tree-baseline-behavior-recovery/35-WIDGET-TREE-BASELINE.md</files>
  <read_first>
    - .planning/phases/35-widget-tree-baseline-behavior-recovery/35-WIDGET-TREE-BASELINE.md
    - test/widget/shared/glass_button_test.dart
    - test/widget/player/player_video_controls_test.dart
    - test/widget/player/player_screen_accessibility_resize_test.dart
    - test/widget/player/player_screen_stop_empty_state_test.dart
    - test/widget/player/player_keyboard_actions_test.dart
    - test/widget/player/drop_handler_test.dart
    - test/integration/controls_flow_test.dart
    - test/integration/error_propagation_test.dart
  </read_first>
  <action>
    为 BASE-03 按 `35-VALIDATION.md` 的八文件 quick gate 执行 targeted suite，并在基线文档中按行为矩阵记录播放/暂停、seek、音量、倍速、字幕、全屏、ESC、空状态、错误传播、拖放、键盘快捷键和窗口模式各自由哪个测试文件与用例覆盖。每条命令独立执行并记录退出码与真实通过/失败结果，不复制研究中的旧计数；即使 targeted suite 失败，也必须继续独立执行 `flutter analyze` 和 `git diff --check`，再以三个命令各自的结果判定任务。

    采用 `35-VALIDATION.md` 的 headless FFI 隔离协议：quick gate 使用 injected `videoSurfaceBuilder`、`FakeVideoControlsPort` 与 `FakeWindowService`，不得初始化真实 libmpv/MDK；若它仍出现 `mdk.dll`/FFI 失败，保留完整失败列表，并在临时、detached 的 `HEAD` worktree 复跑**同一命令**后删除该临时 worktree。只有失败在临时 HEAD 同样出现且 Phase 35 未触及其模块时，才能记录为既有环境失败；否则视为本阶段阻塞。不得 stash、reset、checkout、restore、删除断言、skip 或放宽 matcher 制造绿色结果。完整 `flutter test` 不属于本 Phase 的无头通过门，留给 Phase 38；不要把其历史 FFI 故障写成 targeted suite 的结果。把命令、结果和隔离证据写入同一文档。
  </action>
  <verify>
    <automated>flutter test test/widget/shared/glass_button_test.dart test/widget/player/player_video_controls_test.dart test/widget/player/player_screen_accessibility_resize_test.dart test/widget/player/player_screen_stop_empty_state_test.dart test/widget/player/player_keyboard_actions_test.dart test/widget/player/drop_handler_test.dart test/integration/controls_flow_test.dart test/integration/error_propagation_test.dart &amp;&amp; flutter analyze &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - BASE-03 列出的每类用户行为均映射到具体自动化测试，不以笼统的“全套件通过”替代。
    - targeted suite、analyzer 和 diff check 的本次真实结果写入基线文档。
    - 若有既有失败，按 `35-VALIDATION.md` 的临时 detached-HEAD 对照留存可复核证据；不修改测试来掩盖失败。
    - 本任务不修改 `lib/`、`test/`、media_kit 或未追踪截图。
  </acceptance_criteria>
  <done>后续修复可以用一条可重复命令验证主要用户契约，且基线结果与当前脏工作树状态可区分。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|---|---|
| Git 历史 → 当前工作树 | 历史内容只允许读取和比较，不能覆盖用户未提交增量。 |
| 测试替身 → UI 行为结论 | fake port/bridge 只证明项目层契约，不能冒充真实 Windows native profile 证据。 |

## STRIDE 威胁登记

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-35-01 | Tampering | Git 历史基线命令 | high | mitigate | 仅使用只读 show/diff/log，前后记录 `git status --short`，禁止 reset/checkout/restore。 |
| T-35-02 | Repudiation | 行为测试结果 | medium | mitigate | 文档记录实际命令、实际结果和测试文件映射，不沿用研究中的旧计数。 |
| T-35-03 | Information Disclosure | 未追踪截图 | low | accept | 不读取内容、不删除、不暂存、不提交，只记录文件名为受保护工作树项。 |
| T-35-SC | Tampering | package 供应链 | low | accept | 本计划无 package 安装或依赖变更。 |
</threat_model>

<verification>
1. `35-WIDGET-TREE-BASELINE.md` 能从提交边界追溯到当前主路径与行为测试。
2. targeted suite、`flutter analyze`、`git diff --check` 均按实际结果记录。
3. `git status --short` 证明既有源码增量与未追踪截图未被覆盖或处理。
</verification>

<success_criteria>
- BASE-01、BASE-02、BASE-03 均有可执行证据。
- 后续计划只需针对明确测试缺口工作，无需重新解释历史 tree。
- 没有恢复 ControlsOverlay、旧 fullscreen plugin，也没有修改 media_kit。
</success_criteria>

<output>
完成后创建 `.planning/phases/35-widget-tree-baseline-behavior-recovery/35-01-SUMMARY.md`
</output>
