---
phase: 31-visual-design-alignment
plan: 03
type: execute
wave: 3
depends_on:
  - 31-01
  - 31-02
files_modified: []
autonomous: false
requirements:
  - VISUAL-01
  - VISUAL-02
  - VISUAL-03
  - VISUAL-04
  - VISUAL-05
must_haves:
  truths:
    - "Panel title bar, tab strip, and button bar render the shared four-shadow ControlBarDecoration playing appearance with controlBarBg, a one-pixel controlBarBorderWhite border, and glowOuterRing."
    - "The content section remains thin glass through panelSectionBg to bgGlass while the panel subtree contains one BackdropFilter."
    - "Option rows render transparent and borderless by default, use bgHover on hover, and show a one-pixel controlBarBorderWhite focused border with an accent active value."
    - "Option rows provide InkWell highlight and ripple feedback without custom scale animation."
    - "Option rows render at 40 logical pixels high with Tokens.spXs horizontal padding."
    - "ControlBarDecoration is the single shared route consumed by both the control bar and all three panel chrome sections."
    - "Decoration/color assertions re-baseline to the chrome/content split without changing the Phase 30 panel geometry contract."
    - statement: "Accent #2C58F4 active-value text remains readable on glass for color-weak users."
      verification: backstop
    - statement: "The accepted desktop row height of 40 logical pixels is usable despite being below the 44-pixel touch-target guideline."
      verification: backstop
    - statement: "Option-row labels and values do not overflow at Tokens.spXs horizontal padding."
      verification: backstop
  artifacts:
    - path: lib/ui/shared/control_bar_decoration.dart
      provides: Shared four-shadow playing and tween-compatible idle decoration factories
    - path: test/ui/shared/control_bar_decoration_test.dart
      provides: Decoration extraction field-equivalence coverage
    - path: test/ui/shared/settings_card_test.dart
      provides: Row three-state and density coverage
    - path: test/widgets/panel_color_test.dart
      provides: Chrome/content route and single-BackdropFilter coverage
  key_links:
    - from: lib/ui/player/control_bar.dart
      to: lib/ui/shared/control_bar_decoration.dart
      via: Local DecorationTween evaluates shared cached playing and idle values
      pattern: ControlBarDecoration\.(playing|idle)
    - from: lib/ui/dialogs/settings/settings_overlay_shell.dart
      to: lib/ui/shared/control_bar_decoration.dart
      via: Title and button bars use corner-only playing decorations within existing GlassContainer
      pattern: ControlBarDecoration\.playing
    - from: lib/ui/shared/settings_card.dart
      to: lib/ui/shared/focusable_setting_row.dart
      via: focusedBuilder supplies focus state while InkWell remains non-focusable
      pattern: focusedBuilder
---

<objective>
Validate the complete Phase 31 visual alignment, including automated integration coverage and the Windows profile-mode evidence that cannot be truthfully asserted in headless tests.

Purpose: Confirm that visual routing, interaction states, focus ownership, and blur performance work together after both implementation plans.
Output: Recorded automated verification and a developer-confirmed profile/visual validation checkpoint.
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-visual-design-alignment/31-CONTEXT.md
@.planning/phases/31-visual-design-alignment/31-RESEARCH.md
@.planning/phases/31-visual-design-alignment/31-PATTERNS.md
@.planning/phases/31-visual-design-alignment/31-UI-SPEC.md
@.planning/phases/31-visual-design-alignment/31-VALIDATION.md
@.planning/phases/31-visual-design-alignment/31-01-SUMMARY.md
@.planning/phases/31-visual-design-alignment/31-02-SUMMARY.md
</context>

## Artifacts this phase produces

- New file: `lib/ui/shared/control_bar_decoration.dart`
- New class: `ControlBarDecoration` with private constructor `ControlBarDecoration._()`
- New methods: `playing({BorderRadius? borderRadius})` and `idle({BorderRadius? borderRadius})`
- New test files: `test/ui/shared/control_bar_decoration_test.dart`, `test/ui/shared/settings_card_test.dart`
- Modified files: `lib/ui/player/control_bar.dart`, `lib/ui/shared/settings_card.dart`, `lib/ui/dialogs/settings/settings_overlay_shell.dart`, `lib/ui/dialogs/settings/tab_strip.dart`, `test/widgets/panel_color_test.dart`, `test/ui/shared/focusable_setting_row_test.dart`

GlassButton selected-state work is deferred to the Phase 32 NAV-06 batch per Plan 02's scoped deferral; it is not a Phase 31 produced artifact.

<tasks>

<task type="auto">
  <name>Task 1: Run the complete visual-alignment automated regression gate</name>
  <read_first>
    - .planning/phases/31-visual-design-alignment/31-VALIDATION.md (commands, baseline discrimination, and Nyquist checks)
    - .planning/phases/31-visual-design-alignment/31-UI-SPEC.md (all covered and backstop considerations)
    - .planning/phases/31-visual-design-alignment/31-RESEARCH.md (§2 profile threshold and §6 contrast evidence)
    - test/ui/shared/control_bar_decoration_test.dart
    - test/ui/shared/settings_card_test.dart
    - test/widgets/panel_color_test.dart
    - test/ui/shared/focusable_setting_row_test.dart
  </read_first>
  <files>test/ui/shared/control_bar_decoration_test.dart, test/ui/shared/settings_card_test.dart, test/widgets/panel_color_test.dart, test/ui/shared/focusable_setting_row_test.dart</files>
  <action>Run the Phase 31 focused suite covering extracted decoration fields, panel chrome/content routing, one BackdropFilter, three-state rows, and focus geometry. Run `D:/flutter/bin/flutter analyze` on the Phase 31 production files. Then run the full Flutter suite and compare failures to the documented baseline: engine/kernel `mdk.dll` headless failures and the four dialog failures are pre-existing; no changed UI test may introduce a new failure. If an assertion is insufficient to establish a locked UI-SPEC covered behavior, strengthen the existing Phase 31 test rather than removing or weakening coverage. Do not introduce packages, tokens, APIs, schemas, focus roots, or navigation cover widgets during validation.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/shared/control_bar_decoration_test.dart test/ui/shared/settings_card_test.dart test/widgets/panel_color_test.dart test/ui/shared/focusable_setting_row_test.dart && D:/flutter/bin/flutter analyze lib/ui/shared/control_bar_decoration.dart lib/ui/player/control_bar.dart lib/ui/shared/settings_card.dart lib/ui/shared/focusable_setting_row.dart lib/ui/dialogs/settings/settings_overlay_shell.dart lib/ui/dialogs/settings/tab_strip.dart</automated>
  </verify>
  <acceptance_criteria>
    - The focused Phase 31 Flutter suite exits 0.
    - Flutter analysis of every Phase 31 production file reports no issues.
    - Full-suite failures, if any, are documented as matching the existing non-UI baseline; no new failure is attributed to changed UI files.
    - Automated evidence covers each eight covered UI Considerations and leaves only the three declared backstop items for human confirmation.
  </acceptance_criteria>
  <done>All testable visual-alignment assertions have passing evidence, and no regression is concealed by removing an assertion or broadening a baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm Windows visual seams, row readability, and profile-mode performance</name>
  <read_first>
    - .planning/phases/31-visual-design-alignment/31-RESEARCH.md (§2 six-step profile protocol, §5 seam risk, and §6 contrast calculation)
    - .planning/phases/31-visual-design-alignment/31-UI-SPEC.md (UI Considerations backstop rows)
    - .planning/phases/31-visual-design-alignment/31-VALIDATION.md (manual-only instructions and pass criteria)
    - .planning/phases/31-visual-design-alignment/perf-baseline-pre.json (Plan 01 pre-change PerfMonitor exportStats A/B baseline)
    - lib/ui/dialogs/settings/settings_overlay_shell.dart
    - lib/ui/shared/settings_card.dart
  </read_first>
  <files>lib/ui/dialogs/settings/settings_overlay_shell.dart, lib/ui/shared/settings_card.dart</files>
  <what-built>Shared control-bar chrome on three panel sections, thin-glass content, compact InkWell rows, and test guards for one blur layer and one focus owner.</what-built>
  <action>Use the pre-change PerfMonitor export captured by Plan 01 as the A/B baseline, then execute the five Windows profile-mode checks below. This task changes no source files unless the developer reports a specific failure; return the affected section, measured raster evidence, or row state to the executor for a corrective plan.</action>
  <how-to-verify>
    1. Run `D:/flutter/bin/flutter run --profile -d windows`, enable `PerfMonitor.instance.enable()`, open Settings, drag the panel across the window, scroll the General content, and operate representative setting controls.
    2. Export PerfMonitor statistics and compare with the Task 1 pre-change JSON. Pass if raster average increases by no more than 1 ms and there are no sustained raster-dominant slow frames; report a failing comparison instead of approving it.
    3. Compare the title/tab/button chrome with the control bar. There must be no unacceptable bright or dark seam between chrome sections; corner-only top/zero/bottom radii must be visible in the code path.
    4. Verify a default row is fused/transparent, hover has the hover surface, focus has the blue glow border and accented active value, and press produces InkWell feedback without scale motion.
    5. Check normal lighting readability of accented active values, keyboard/mouse usability of 40-pixel rows, and no clipping/overflow of labels or values at 4-pixel horizontal padding.
  </how-to-verify>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/shared/control_bar_decoration_test.dart test/ui/shared/settings_card_test.dart test/widgets/panel_color_test.dart test/ui/shared/focusable_setting_row_test.dart</automated>
    <human-check>Complete the profile-mode and visual checks in how-to-verify.</human-check>
  </verify>
  <acceptance_criteria>
    - Profile evidence records no sustained raster-dominant slow frames and raster average change is at most 1 ms from the recorded baseline.
    - The three chrome sections have no unacceptable inter-section shadow seam in the Windows profile build.
    - Accent readability, desktop 40-pixel row usability, and text overflow at spXs padding are explicitly approved or returned with actionable issues.
  </acceptance_criteria>
  <resume-signal>Type `approved` after all five checks pass, or describe the visual/performance issue with the affected section or row.</resume-signal>
  <done>Windows profile and visual evidence explicitly approves the three backstop considerations and reports no seam or raster regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local profile build to developer observation | Automated UI evidence ends at source/widget behavior; perceptual seams, contrast, and raster timing require a Windows profile build review. |
| Existing UI interaction paths | Pointer and keyboard interaction stay within existing settings widgets; no new external boundary is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-07 | Denial of service | BackdropFilter and panel paint path | medium | mitigate | Automated one-filter gate plus profile-mode raster sampling and pre/post exportStats comparison. |
| T-31-08 | Tampering | Visual state regression | low | mitigate | Run focused and full-suite checks; preserve exact token/state assertions. |
| T-31-09 | Spoofing | Visual focus/readability cues | low | accept | The phase changes no identity or authorization signals; human review validates presentation quality. |
| T-31-SC | Tampering | Package installs | high | mitigate | No package-manager install occurs; the Phase 31 package legitimacy audit records zero new dependencies. |

Minimal surface assessment: validation only for a pure UI visual phase; no new input surface, auth, I/O, FFI, external API/SDK/service, schema, or package. API Coverage checkpoint does not fire because this phase introduces no external API/SDK/service. Assumption-Delta checkpoint does not fire because no singular-to-plural, required-to-optional, or derived-to-chosen identity-model transition occurs. Schema Push gate does not fire because the phase contains Dart/Flutter UI and tests only.
</threat_model>

<verification>
Focused tests and analysis run before the blocking human profile/visual check. The backstop considerations are intentionally human-verified; absent explicit evidence they must remain human-needed rather than being treated as passing.
</verification>

<success_criteria>
All five VISUAL requirements have automated evidence where possible and Windows profile evidence where rendering/perception makes automated confirmation incomplete.
</success_criteria>

<output>
Create `.planning/phases/31-visual-design-alignment/31-03-SUMMARY.md` when done.
</output>
