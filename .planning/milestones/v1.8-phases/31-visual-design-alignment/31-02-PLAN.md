---
phase: 31-visual-design-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - lib/ui/shared/settings_card.dart
  - lib/ui/shared/focusable_setting_row.dart
  - test/ui/shared/settings_card_test.dart
  - test/ui/shared/focusable_setting_row_test.dart
autonomous: true
requirements:
  - VISUAL-02
  - VISUAL-03
must_haves:
  truths:
    - "Option rows render transparent and borderless by default, use bgHover on hover, and show a one-pixel controlBarBorderWhite focused border with an accent active value."
    - "Option rows provide InkWell highlight and ripple feedback without custom scale animation."
    - "Option rows render at 40 logical pixels high with Tokens.spXs horizontal padding."
  artifacts:
    - path: lib/ui/shared/settings_card.dart
      provides: Three-state SettingRow with Material and non-focusable InkWell interaction
      contains: InkWell
    - path: lib/ui/shared/focusable_setting_row.dart
      provides: One-pixel focused border and focusedBuilder state delivery
      contains: focusedBuilder
    - path: test/ui/shared/settings_card_test.dart
      provides: SettingRow state, density, focus, and interaction contract coverage
  key_links:
    - from: lib/ui/shared/settings_card.dart
      to: lib/ui/shared/focusable_setting_row.dart
      via: focusedBuilder delivers focused state to the active control/value subtree
      pattern: focusedBuilder
    - from: lib/ui/shared/settings_card.dart
      to: Flutter Material ink system
      via: transparent Material hosts InkWell while canRequestFocus false preserves one focus stop
      pattern: canRequestFocus: false
---

<objective>
Expand the proven chrome direction to settings option rows: implement the locked default/hover/focused/pressed behavior and compact density without compromising focus traversal.

Purpose: Make interactive settings rows use the control-bar visual language and retain a single focus owner for Phase 32.
Output: Refactored SettingRow and FocusableSettingRow plus dedicated state and geometry tests.
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-visual-design-alignment/31-CONTEXT.md
@.planning/phases/31-visual-design-alignment/31-RESEARCH.md
@.planning/phases/31-visual-design-alignment/31-PATTERNS.md
@.planning/phases/31-visual-design-alignment/31-UI-SPEC.md
@.planning/phases/31-visual-design-alignment/31-VALIDATION.md
@.planning/phases/31-visual-design-alignment/31-01-SUMMARY.md
</context>

## Artifacts this phase produces

- New file: `lib/ui/shared/control_bar_decoration.dart`
- New class: `ControlBarDecoration` with private constructor `ControlBarDecoration._()`
- New methods: `playing({BorderRadius? borderRadius})` and `idle({BorderRadius? borderRadius})`
- New test files: `test/ui/shared/control_bar_decoration_test.dart`, `test/ui/shared/settings_card_test.dart`
- Modified files: `lib/ui/player/control_bar.dart`, `lib/ui/shared/settings_card.dart`, `lib/ui/dialogs/settings/settings_overlay_shell.dart`, `lib/ui/dialogs/settings/tab_strip.dart`, `test/widgets/panel_color_test.dart`, `test/ui/shared/focusable_setting_row_test.dart`

## Deferred to Phase 32

GlassButton's selected-state visual route and ScaleTransition removal are deferred to the Phase 32 NAV-06 batch. Per RESEARCH §A5/Open Question 3 and PATTERNS L154-156, SettingRow is the Phase 31 hard success-criterion surface; deferral preserves D-05's focused-selection direction without coupling this row refactor to a separate button migration.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test and implement the SettingRow three-state interaction contract</name>
  <read_first>
    - .planning/phases/31-visual-design-alignment/31-PATTERNS.md (InkWell/Material and focusedBuilder patterns)
    - .planning/phases/31-visual-design-alignment/31-RESEARCH.md (§3 InkWell constraints, §4 focus invariant, and Pitfalls 2, 3, 6, 7)
    - .planning/phases/31-visual-design-alignment/31-CONTEXT.md (locked decisions: D-05: focused-selection semantics; D-06: 1px controlBarBorderWhite focused border + bgHover hover; D-07: accent active-value text; D-08: InkWell pressed no-scale; D-09: height 40; D-10: padding spXs)
    - lib/ui/shared/settings_card.dart
    - lib/ui/shared/focusable_setting_row.dart
    - test/ui/shared/focusable_setting_row_test.dart
  </read_first>
  <files>lib/ui/shared/settings_card.dart, lib/ui/shared/focusable_setting_row.dart, test/ui/shared/settings_card_test.dart, test/ui/shared/focusable_setting_row_test.dart</files>
  <behavior>
    - Test 1: A SettingRow is transparent and has no visible border before hover or focus.
    - Test 2: Hover applies Tokens.bgHover; focus applies a one-pixel Tokens.controlBarBorderWhite border without changing the 40-pixel outer height.
    - Test 3: The active value builder receives focus state and renders its active text with Tokens.accent when focused.
    - Test 4: The row contains InkWell hosted by a transparent Material, the InkWell cannot request focus, and tapping calls onTap without a Transform.scale press path.
    - Test 5: SettingRow height is 40 and horizontal padding resolves to Tokens.spXs.
  </behavior>
  <action>Start RED by adding the dedicated widget assertions described in the behavior block, then implement D-05 through D-10 on the SettingRow surface. Extend FocusableSettingRow with a backward-compatible optional `focusedBuilder(BuildContext, bool focused)` so its existing child API remains valid and focused state rebuilds the active-value subtree; make this wrapper the only focus owner. Change its focus border to width 1 and `Tokens.controlBarBorderWhite`, remove its hover background ownership so hover has exactly one source, and retain immediate Container-based border changes. Refactor SettingRow from MouseRegion/GestureDetector/AnimatedContainer/Transform.scale to a transparent Material containing InkWell with `canRequestFocus: false`, `autofocus: false`, `hoverColor: Tokens.bgHover`, `splashColor: Tokens.accentLight`, and a highlight color based on Tokens so pressed feedback is visible on glass. Retain enabled behavior through onTap, use fixed height 40 and horizontal `Tokens.spXs`, preserve icon/title/description/control layout, and render the supplied active control/value through focusedBuilder with Tokens.accent only while focused. GlassButton selected-state work remains deferred to Phase 32 as documented above; do not modify `glass_container.dart` in this plan. Keep all visual values in Tokens and add public/non-trivial API documentation plus why-comments as code is written.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/shared/settings_card_test.dart test/ui/shared/focusable_setting_row_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/ui/shared/settings_card_test.dart` exists and verifies default, hover, focused, pressed, no-layout-shift, 40-pixel, and 4-pixel-padding behavior.
    - `FocusableSettingRow` exposes an optional `focusedBuilder`, uses a one-pixel `Tokens.controlBarBorderWhite` focused border, and does not render bgHover itself.
    - `SettingRow` contains `Material` and `InkWell`, with `canRequestFocus: false`; it no longer applies press feedback by scaling its child.
    - Active value text uses `Tokens.accent` while the row is focused, and the same row keeps its render height before and after focus.
    - `D:/flutter/bin/flutter test test/ui/shared/settings_card_test.dart test/ui/shared/focusable_setting_row_test.dart` exits 0.
  </acceptance_criteria>
  <done>Interactive rows provide the locked three-state behavior, ripple feedback, compact geometry, and a single focus-stop architecture covered by widget tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove setting-tab row consumers preserve focus traversal and active-value styling</name>
  <read_first>
    - .planning/phases/31-visual-design-alignment/31-PATTERNS.md (single-focus-owner invariant and existing row consumers)
    - .planning/phases/31-visual-design-alignment/31-RESEARCH.md (§4 focus traversal requirements and §6 contrast backstop)
    - lib/ui/shared/settings_card.dart
    - lib/ui/shared/focusable_setting_row.dart
    - lib/ui/dialogs/settings/tabs/general_tab.dart
    - test/ui/shared/settings_card_test.dart
    - test/widget/settings/general_equalizer_tab_test.dart
  </read_first>
  <files>test/ui/shared/settings_card_test.dart, test/widget/settings/general_equalizer_tab_test.dart</files>
  <action>Extend the test harness to pump representative existing SettingRow consumers from General and Equalizer settings content, request row focus through the existing FocusableSettingRow path, and verify each interactive row has one focusable route rather than a second InkWell stop. Assert focus changes leave row geometry unchanged and that active value text receives the locked `Tokens.accent` route. Keep the coverage focused on current consumers: do not alter tab contents, keyboard root handling, navigation arrows, or control-bar button behavior. Record the D-07 contrast calculation as a backstop in the final human profile check; the locked active-value token remains `Tokens.accent` unless verification produces explicit evidence requiring the existing brighter token path.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/shared/settings_card_test.dart test/ui/shared/focusable_setting_row_test.dart test/widget/settings/general_equalizer_tab_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Existing General and Equalizer representative row tests continue to exercise SettingRow consumers after the refactor.
    - Tests establish one focus owner per enabled row and no outer-height change after focus transitions.
    - Focused active-value styling is asserted as `Tokens.accent` and non-focused styling remains the existing secondary route.
    - `D:/flutter/bin/flutter test test/ui/shared/settings_card_test.dart test/ui/shared/focusable_setting_row_test.dart test/widget/settings/general_equalizer_tab_test.dart` exits 0.
  </acceptance_criteria>
  <done>The row refactor is validated against live settings-tab consumers and preserves the Phase 32-ready focus traversal contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing keyboard, pointer, and focus events | Existing UI event signals cross into the row state machine; no new input parsing or persistence is introduced. |
| FocusableSettingRow to embedded controls | Focus state is passed in-process to value rendering while the wrapper remains the sole focus owner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-04 | Denial of service | Focus traversal in SettingRow | medium | mitigate | Make InkWell non-focusable, preserve FocusableSettingRow ownership, and test one focus route per row. |
| T-31-05 | Tampering | Row visual-state tokens | low | mitigate | Widget tests assert transparent default, bgHover hover, controlBarBorderWhite focused border, and Tokens-only geometry. |
| T-31-06 | Spoofing | Focused active-value indication | low | accept | No identity or authorization semantics exist; focus indication is local presentation only. |
| T-31-SC | Tampering | Package installs | high | mitigate | No package-manager install occurs; the Phase 31 package legitimacy audit records zero new dependencies. |

Minimal surface assessment: pure UI event and paint refactor with no new input surface, auth, I/O, FFI, external API/SDK/service, schema, or package.
</threat_model>

<verification>
Run the focused row tests after each task. The successor plan runs all Phase 31 UI tests and profile-mode validation.
</verification>

<success_criteria>
Rows visibly implement default, hover, focused, and pressed behavior at the locked density while keyboard traversal still reaches each row exactly once.
</success_criteria>

<output>
Create `.planning/phases/31-visual-design-alignment/31-02-SUMMARY.md` when done.
</output>
