---
phase: 31-visual-design-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/shared/control_bar_decoration.dart
  - lib/ui/player/control_bar.dart
  - lib/ui/dialogs/settings/settings_overlay_shell.dart
  - lib/ui/dialogs/settings/tab_strip.dart
  - test/ui/shared/control_bar_decoration_test.dart
  - test/widgets/panel_color_test.dart
autonomous: true
requirements:
  - VISUAL-01
  - VISUAL-04
  - VISUAL-05
must_haves:
  truths:
    - "Panel title bar, tab strip, and button bar render the shared four-shadow ControlBarDecoration playing appearance with controlBarBg, a one-pixel controlBarBorderWhite border, and glowOuterRing."
    - "The content section remains thin glass through panelSectionBg to bgGlass while the panel subtree contains one BackdropFilter."
    - "ControlBarDecoration is the single shared route consumed by both the control bar and all three panel chrome sections."
    - "Decoration/color assertions re-baseline to the chrome/content split without changing the Phase 30 panel geometry contract."
  artifacts:
    - path: lib/ui/shared/control_bar_decoration.dart
      provides: ControlBarDecoration static playing and idle BoxDecoration factories
      contains: class ControlBarDecoration
    - path: test/ui/shared/control_bar_decoration_test.dart
      provides: field-equivalence and tween-compatibility tests for the extracted decoration
    - path: test/widgets/panel_color_test.dart
      provides: panel chrome decoration and single-BackdropFilter route assertions
  key_links:
    - from: lib/ui/player/control_bar.dart
      to: lib/ui/shared/control_bar_decoration.dart
      via: cached ControlBarDecoration.playing and idle instances feed the existing local DecorationTween
      pattern: ControlBarDecoration\.(playing|idle)
    - from: lib/ui/dialogs/settings/settings_overlay_shell.dart
      to: lib/ui/shared/control_bar_decoration.dart
      via: title and button containers use playing with corner-only radius overrides
      pattern: ControlBarDecoration\.playing
    - from: lib/ui/dialogs/settings/tab_strip.dart
      to: lib/ui/shared/control_bar_decoration.dart
      via: tab-strip container uses playing with BorderRadius.zero
      pattern: ControlBarDecoration\.playing
---

<objective>
Extract the control-bar decoration into its locked shared API and prove one end-to-end panel-chrome path before expanding it to all three chrome sections.

Purpose: Establish a production-quality, single-route glass decoration without changing the content-layer blur architecture.
Output: Shared ControlBarDecoration, migrated control-bar consumers, all three chrome consumers, and re-baselined color/blur tests.
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-visual-design-alignment/31-CONTEXT.md
@.planning/phases/31-visual-design-alignment/31-RESEARCH.md
@.planning/phases/31-visual-design-alignment/31-PATTERNS.md
@.planning/phases/31-visual-design-alignment/31-UI-SPEC.md
@.planning/phases/31-visual-design-alignment/31-VALIDATION.md
@.planning/phases/30-panel-layout-redesign/30-03-SUMMARY.md
</context>

## Artifacts this phase produces

- New file: `lib/ui/shared/control_bar_decoration.dart`
- New class: `ControlBarDecoration` with private constructor `ControlBarDecoration._()`
- New methods: `playing({BorderRadius? borderRadius})` and `idle({BorderRadius? borderRadius})`
- New test files: `test/ui/shared/control_bar_decoration_test.dart`, `test/ui/shared/settings_card_test.dart`
- Modified files: `lib/ui/player/control_bar.dart`, `lib/ui/shared/settings_card.dart`, `lib/ui/shared/glass_container.dart`, `lib/ui/dialogs/settings/settings_overlay_shell.dart`, `lib/ui/dialogs/settings/tab_strip.dart`, `test/widgets/panel_color_test.dart`, `test/ui/shared/focusable_setting_row_test.dart`

<tasks>

<task type="tracer">
  <name>Task 1: Extract and prove one title-bar chrome route end to end</name>
  <reversibility rating="costly">D-01 creates a shared file used by two consumers, so undoing it requires coordinated re-inlining; D-02 directional naming would require cross-consumer renaming.</reversibility>
  <read_first>
    - .planning/phases/31-visual-design-alignment/31-PATTERNS.md (the verbatim four-shadow source and consumer seams)
    - .planning/phases/31-visual-design-alignment/31-RESEARCH.md (§5 extraction source and §2 single-blur invariant)
    - .planning/phases/31-visual-design-alignment/31-CONTEXT.md (D-01 through D-04 and D-11 locked decisions)
    - lib/ui/player/control_bar.dart
    - lib/ui/dialogs/settings/settings_overlay_shell.dart
    - lib/ui/theme/tokens.dart
    - test/widgets/panel_color_test.dart
  </read_first>
  <files>lib/ui/shared/control_bar_decoration.dart, lib/ui/player/control_bar.dart, lib/ui/dialogs/settings/settings_overlay_shell.dart, test/ui/shared/control_bar_decoration_test.dart</files>
  <action>Before modifying production code, run the profile-mode baseline protocol from 31-RESEARCH.md §2 on Windows: launch `D:/flutter/bin/flutter run --profile -d windows`, enable PerfMonitor sampling, open the panel, drag its title bar and scroll its content, then save the pre-change `PerfMonitor.exportStats()` JSON to `.planning/phases/31-visual-design-alignment/perf-baseline-pre.json` for the final comparison. Create `ControlBarDecoration` per D-01/D-02 with a private constructor and static `playing({BorderRadius? borderRadius})` and `idle({BorderRadius? borderRadius})` methods. Copy the playing four BoxShadow values and idle four-entry shadow list exactly from the current ControlBar decorations; default the optional radius to `BorderRadius.circular(Tokens.controlBarRadius)` per D-03/D-04. Keep `DecorationTween` local to ControlBar, replace its cached source decorations with cached shared factory calls, and preserve EdgeGlow, blur, and top-gradient ownership locally. Apply `ControlBarDecoration.playing(borderRadius: BorderRadius.vertical(top: Radius.circular(Tokens.radiusLg)))` only to the title-bar container for the tracer path; retain the existing GlassContainer, RepaintBoundary, FocusTraversalGroup, root Focus, content route, and panel geometry. Add field-equivalence tests asserting playing color, one-pixel border, four shadows with glowOuterRing at index 3, default/overridden radii, and the four-entry idle list. Write doc comments and non-obvious tween-compatibility comments while editing, per project policy.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/shared/control_bar_decoration_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/ui/shared/control_bar_decoration.dart` contains `class ControlBarDecoration`, `ControlBarDecoration._()`, `playing({BorderRadius? borderRadius})`, and `idle({BorderRadius? borderRadius})`.
    - `ControlBarDecoration.playing().boxShadow.length` is 4; its color is `Tokens.controlBarBg`; its border is width 1 and `Tokens.controlBarBorderWhite`; shadow index 3 uses `Tokens.glowOuterRing`.
    - `ControlBarDecoration.idle().boxShadow.length` is 4 and ControlBar still owns `_decorationTween`.
    - The title-bar Container decoration is `ControlBarDecoration.playing` with a top-only `Tokens.radiusLg` radius, and `D:/flutter/bin/flutter test test/ui/shared/control_bar_decoration_test.dart` exits 0.
    - The pre-change PerfMonitor baseline JSON exists at `.planning/phases/31-visual-design-alignment/perf-baseline-pre.json`; record this exact path in `31-01-SUMMARY.md` for Plan 03's A/B comparison.
  </acceptance_criteria>
  <done>The tracer proves the shared decoration factory reaches a live panel chrome section and leaves the control-bar transition supplied by its local tween.</done>
</task>

<task type="auto">
  <name>Task 2: Expand shared chrome routing and re-baseline the panel contract</name>
  <read_first>
    - .planning/phases/31-visual-design-alignment/31-PATTERNS.md (corner-only radius and panel test patterns)
    - .planning/phases/31-visual-design-alignment/31-RESEARCH.md (§2, §5, and Pitfalls 1, 4, and 5)
    - .planning/phases/31-visual-design-alignment/31-UI-SPEC.md (chrome/content color table and UI Considerations)
    - lib/ui/shared/control_bar_decoration.dart
    - lib/ui/dialogs/settings/settings_overlay_shell.dart
    - lib/ui/dialogs/settings/tab_strip.dart
    - lib/ui/dialogs/settings/tab_content.dart
    - test/widgets/panel_color_test.dart
  </read_first>
  <files>lib/ui/dialogs/settings/settings_overlay_shell.dart, lib/ui/dialogs/settings/tab_strip.dart, test/widgets/panel_color_test.dart</files>
  <action>Complete D-11 chrome routing: change the button bar to `ControlBarDecoration.playing(borderRadius: BorderRadius.vertical(bottom: Radius.circular(Tokens.radiusLg)))` and the tab-strip root to `ControlBarDecoration.playing(borderRadius: BorderRadius.zero)`. This corner-only segmentation is required to mitigate the documented shadow-seam risk. Keep the content `ColoredBox(color: Tokens.panelSectionBg)` so it continues to resolve to `Tokens.bgGlass`; do not add a Stack wrapper or navigation covers per D-12. Re-baseline `panel_color_test.dart` so title, tab strip, and button bar inspect `BoxDecoration` fields for `Tokens.controlBarBg`, `Tokens.controlBarBorderWhite`, and a four-shadow list ending in `Tokens.glowOuterRing`; retain the content assertion against `Tokens.panelSectionBg` and the `panelSectionBg == bgGlass` alias contract. Add a panel-subtree `BackdropFilter` count assertion of exactly one. Do not modify Tokens, sizing, tab sequence, root focus ownership, or the GlassContainer/RepaintBoundary composition.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/widgets/panel_color_test.dart test/ui/shared/control_bar_decoration_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All three chrome sections use `ControlBarDecoration.playing`; title uses top-only radius, tab strip uses `BorderRadius.zero`, and button bar uses bottom-only radius.
    - `SettingsTabContent` still renders `Tokens.panelSectionBg`, and the panel widget-test subtree has exactly one `BackdropFilter`.
    - `test/widgets/panel_color_test.dart` asserts decoration fields for three chrome sections and the unchanged content color route.
    - `D:/flutter/bin/flutter test test/widgets/panel_color_test.dart test/ui/shared/control_bar_decoration_test.dart` exits 0.
  </acceptance_criteria>
  <done>The complete chrome adopts the shared control-bar glass while thin content glass, one-blur composition, and Phase 30 layout behavior remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing pointer/focus events to panel widgets | Existing UI events remain local; this plan adds no input parser, I/O, FFI, service, secret, or dependency. |
| Shared decoration factories to UI consumers | Existing Tokens-only visuals cross from a shared in-process utility into two widget consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | ControlBarDecoration token route | low | mitigate | Field-equivalence tests lock the existing four-shadow values and both consumers call the same factory. |
| T-31-02 | Denial of service | Settings panel blur composition | medium | mitigate | Preserve one GlassContainer BackdropFilter and assert a count of one in panel_color_test; profile check occurs in Plan 03. |
| T-31-03 | Spoofing | Panel chrome focus appearance | low | accept | No identity, authentication, or authorization surface exists; appearance-only changes cannot grant capability. |
| T-31-SC | Tampering | Package installs | high | mitigate | No package-manager install occurs; the Phase 31 package legitimacy audit records zero new dependencies. |

Minimal surface assessment: pure in-process UI decoration extraction and token routing; no new input surface, auth, I/O, FFI, external API/SDK/service, schema, or package.
</threat_model>

<verification>
Run the two focused test files after each task. The final plan records the profile comparison and full-suite discrimination against the documented headless baseline.
</verification>

<success_criteria>
The same reusable four-shadow decoration drives ControlBar and the panel's three chrome sections; content stays thin glass and the panel retains exactly one blur layer.
</success_criteria>

<output>
Create `.planning/phases/31-visual-design-alignment/31-01-SUMMARY.md` when done.
</output>
