---
phase: 30-panel-layout-redesign
plan: 04
type: execute
wave: 3
depends_on: [30-02, 30-03]
files_modified:
  - test/ui/dialogs/settings_responsive_integration_test.dart
  - test/ui/dialogs/settings_responsive_scaling_test.dart
  - test/ui/dialogs/settings_overlay_shell_test.dart
  - test/ui/dialogs/settings_tab_content_test.dart
  - test/widgets/panel_size_test.dart
  - test/widgets/multi_monitor_clamp_test.dart
  - test/widgets/panel_color_test.dart
  - test/widgets/tab_strip_order_test.dart
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04, LAYOUT-05]
must_haves:
  truths:
    - "The widget-test baseline proves the strict panel formula: width is min(0.5 × screen width, screen height × 16/9), clamped from 400 through 960, and height is width × 9/16 (D-04)."
    - "Opening the panel selects General at index 3 and each tab test identifies the reordered [EQ, Audio, Video, General, Shortcuts, About, Performance] sequence (D-01)."
    - "Widget tests prove injected work-area clamping during drag, symmetric fallback when display information is unavailable, and re-clamping after a window-size change (D-03, D-05)."
    - "Widget tests prove title bar, tab strip, content, and button bar consume the unified panelSectionBg route while its Phase 30 value remains bgGlass (D-02)."
    - "Compact tab-strip behavior remains independently covered by Tokens.breakpointResponsive; panel sizing has no breakpoint branch (D-04)."
  artifacts:
    - path: test/ui/dialogs/settings_responsive_integration_test.dart
      provides: Re-baselined end-to-end responsive size and drag assertions.
    - path: test/ui/dialogs/settings_responsive_scaling_test.dart
      provides: Re-baselined clamp, strict-aspect, and compact-tab regression assertions.
    - path: test/ui/dialogs/settings_overlay_shell_test.dart
      provides: Re-baselined fallback drag geometry and General-default assertions.
    - path: test/ui/dialogs/settings_tab_content_test.dart
      provides: Semantic tab-order and explicit IndexedStack behavior assertions.
    - path: test/widgets/multi_monitor_clamp_test.dart
      provides: Fake-DisplayEnumerator coverage for work-area, fallback, and resize clamp paths.
    - path: test/widgets/panel_color_test.dart
      provides: Four-section panel token-route assertion.
  key_links:
    - from: test/widgets/multi_monitor_clamp_test.dart
      to: lib/ui/dialogs/settings/settings_overlay_shell.dart
      via: An injected hand-written DisplayEnumerator fake drives the shell's work-area, null-fallback, and resize paths.
      pattern: DisplayEnumerator
    - from: test/ui/dialogs/settings_tab_content_test.dart
      to: lib/ui/dialogs/settings/settings_panel_controller.dart
      via: Opening the controller selects its defaultTabIndex and the seven explicit IndexedStack children render in the matching order.
      pattern: defaultTabIndex
    - from: test/widgets/panel_color_test.dart
      to: lib/ui/theme/tokens.dart
      via: Each panel section resolves through Tokens.panelSectionBg, whose Phase 30 alias value is Tokens.bgGlass.
      pattern: panelSectionBg
---

## Goal

Re-baseline the complete Phase 30 widget-test contract after the finalized Wave 1–2 production changes: strict 16:9 geometry, General as the middle/default tab, work-area clamp plus fallback plus resize behavior, and the four-section structural token route.

Purpose: Preserve Phase 30's observable layout guarantees while distinguishing regressions from the known headless MDK FFI failures.

Output: Updated legacy dialog widget tests and completed Wave-0 contract tests under `test/widgets/`; no production code changes.

<objective>
Deliver the test-only expansion layer for all LAYOUT-01 through LAYOUT-05 outcomes after Plans 30-02 and 30-03 have finalized their production seams.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-panel-layout-redesign/30-CONTEXT.md
@.planning/phases/30-panel-layout-redesign/30-RESEARCH.md
@.planning/phases/30-panel-layout-redesign/30-UI-SPEC.md
@.planning/phases/30-panel-layout-redesign/30-VALIDATION.md
@.planning/phases/30-panel-layout-redesign/30-01-SUMMARY.md
@.planning/phases/30-panel-layout-redesign/30-02-SUMMARY.md
@.planning/phases/30-panel-layout-redesign/30-03-SUMMARY.md
@lib/ui/dialogs/settings/settings_overlay_shell.dart
@lib/ui/dialogs/settings/settings_panel_controller.dart
@lib/ui/dialogs/settings/tab_content.dart
@lib/ui/dialogs/settings/tab_strip.dart
@lib/ui/theme/tokens.dart
</context>

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Re-baseline legacy responsive geometry and drag assertions</name>
  <reversibility rating="costly">D-04 changes a coordinated positional test contract across documented size and drag suites; undoing it requires restoring the prior formula and every affected assertion.</reversibility>
  <files>test/ui/dialogs/settings_responsive_integration_test.dart, test/ui/dialogs/settings_responsive_scaling_test.dart, test/ui/dialogs/settings_overlay_shell_test.dart, test/widgets/panel_size_test.dart</files>
  <read_first>
    - test/ui/dialogs/settings_responsive_integration_test.dart — update its SCALE success-criteria geometry and 1200×800 drag expectations.
    - test/ui/dialogs/settings_responsive_scaling_test.dart — preserve compact-strip tests while replacing only formula-dependent measurements.
    - test/ui/dialogs/settings_overlay_shell_test.dart — update fallback drag bounds and small-window geometry assertions.
    - test/widgets/panel_size_test.dart — consume the tracer's formula test scaffold and retain it as the canonical focused geometry suite.
    - lib/ui/dialogs/settings/settings_overlay_shell.dart and lib/ui/theme/tokens.dart — derive expected values from the finalized production formula and Tokens values; do not duplicate a divergent calculation.
  </read_first>
  <action>After Plans 30-01 and 30-02 are present, update every documented 5:4-dependent assertion in the three legacy dialog suites and complete the focused panel-size suite. Per D-04, calculate expected dimensions from the strict formula rather than stale literals: cover at least 1920×1080 → 960×540, 1366×768 → approximately 683×384, 800×600 → 400×225, and a sub-minimum-width input such as 500×400 → 400×225. Update 1200×800 fallback drag expectations for the new panel height, including maxY 231.25 and an in-range dy assertion that remains below it; retain symmetric MediaQuery fallback coverage because the display resolver can be unavailable in headless tests. Preserve the existing compact tab-strip font, spacing, and height tests driven by Tokens.breakpointResponsive, because D-04 removes a sizing branch only. Keep RepaintBoundary coverage per D-06. Use existing fake playback controllers and no mocks; edit test names/comments at the same time so they state the 16:9 contract. Do not alter production files in this test-baselining plan.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/dialogs/settings_responsive_integration_test.dart test/ui/dialogs/settings_responsive_scaling_test.dart test/ui/dialogs/settings_overlay_shell_test.dart test/widgets/panel_size_test.dart</automated>
  </verify>
  <done>All formula-dependent legacy size and fallback-drag assertions use the strict 16:9 min/clamp result, focused size tests cover max, proportional, and minimum-clamp cases, and compact tab-strip plus RepaintBoundary tests remain meaningful and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Complete tab, work-area, resize, and structural-token contract tests</name>
  <files>test/ui/dialogs/settings_tab_content_test.dart, test/ui/dialogs/settings_overlay_shell_test.dart, test/widgets/multi_monitor_clamp_test.dart, test/widgets/panel_color_test.dart, test/widgets/tab_strip_order_test.dart</files>
  <read_first>
    - test/ui/dialogs/settings_tab_content_test.dart and test/ui/dialogs/settings_overlay_shell_test.dart — re-baseline index-sensitive General-default and tab-content assertions.
    - test/widgets/multi_monitor_clamp_test.dart — use the Wave 2 fixture and assertions as the dedicated clamp contract.
    - test/widgets/panel_color_test.dart and test/widgets/tab_strip_order_test.dart — use the Wave 1 scaffolds as focused structural contracts.
    - lib/ui/dialogs/settings/settings_panel_controller.dart, tab_strip.dart, and tab_content.dart — verify the final tab sequence, defaultTabIndex owner, and explicit seven-child structure.
    - lib/ui/dialogs/settings/settings_overlay_shell.dart and lib/ui/theme/tokens.dart — verify injected display resolution, resize re-clamping, and panelSectionBg routing.
  </read_first>
  <action>Re-baseline tab tests per D-01 so the exact visual and content sequence is EQ, Audio, Video, General, Shortcuts, About, Performance; opening and reopening must select General through SettingsPanelController.defaultTabIndex at index 3. Retarget semantic tab taps and keep-alive checks to their new indices while retaining the explicit seven-child IndexedStack check. Complete the dedicated work-area test suite with a hand-written configurable DisplayEnumerator fake: assert a drag beyond a supplied DisplayInfo.workArea is constrained on that work area, a null result follows the symmetric fallback path, and resizing to a smaller MediaQuery size re-clamps an illegal existing offset, per D-03 and D-05. Complete panel-color assertions that identify title bar, strip, content, and button bar and prove they all resolve through Tokens.panelSectionBg, with the alias value remaining Tokens.bgGlass per D-02; do not assert or introduce Phase 31 chrome tokens. Keep test fakes deterministic and local, update Chinese test descriptions/comments alongside changed semantics, and avoid changing any production implementation.</action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/dialogs/settings_tab_content_test.dart test/ui/dialogs/settings_overlay_shell_test.dart test/widgets/multi_monitor_clamp_test.dart test/widgets/panel_color_test.dart test/widgets/tab_strip_order_test.dart</automated>
  </verify>
  <done>The tab contract proves General is the fourth/default tab, the clamp contract proves work-area, null-fallback, and resize behavior through injected fakes, and the color contract proves all four sections share the Phase 30 alias route without extending Phase 31 scope.</done>
</task>

</tasks>

## Threat Model

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter gesture system → settings shell | Drag deltas enter the clamp calculation, but are framework-originated and bounded by widget tests. |
| Existing Win32 display FFI → settings shell | Work-area data can be absent in headless execution; the tested null-result fallback retains usable panel behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04-01 | Tampering | Drag-offset / work-area conversion | low | mitigate | Fake-driven widget tests cover work-area bounds, null resolver fallback, and resize re-clamping; no user-controlled persistence or external input is added. |
| T-30-04-02 | Denial of service | Headless test runner | low | accept | Existing MDK DLL failures are outside touched UI modules; stash and rerun the same test commands to compare the failure baseline before classifying a regression. |
</threat_model>

Threat refs: —. Secure behavior: N/A; this is test-only layout validation with no auth, data, filesystem, network, or package-install surface.

## Verification

1. Run both task-level commands after their respective edits.
2. Run `D:/flutter/bin/flutter test test/ui/dialogs/` after the plan to exercise the full settings dialog boundary.
3. Run `D:/flutter/bin/flutter test` after stashing the Phase 30 changes, then rerun after restoring them; classify only newly introduced failures as regressions. Treat the known headless `mdk.dll` failures as baseline evidence rather than a product-code failure.
4. Run `D:/flutter/bin/flutter analyze` to retain strict-casts, strict-inference, and strict-raw-types compatibility for modified Dart tests.

## Goal-Backward Check

- **Geometry truth:** The test suite proves the panel formula and strict 16:9 height at representative large, proportional, and minimum-clamp window sizes (LAYOUT-01, LAYOUT-02, D-04).
- **Tab truth:** The test suite proves General opens at the middle index and all content tests follow the seven-tab sequence (LAYOUT-03, D-01).
- **Clamp truth:** The test suite proves real-time work-area bounds, null-result fallback, and resize correction through an injected fake rather than physical monitor hardware alone (LAYOUT-04, D-03, D-05).
- **Color truth:** The test suite proves the four structural sections share the `panelSectionBg` alias and retain `bgGlass` as its Phase 30 value (LAYOUT-05, D-02).
- **Reachability:** Each contract pumps `SettingsOverlayShell`, opens its controller, performs the user-equivalent action (drag, resize, or tab select), and observes the resulting widget or controller state.

<verification>
D:/flutter/bin/flutter test test/ui/dialogs/settings_responsive_integration_test.dart test/ui/dialogs/settings_responsive_scaling_test.dart test/ui/dialogs/settings_overlay_shell_test.dart test/ui/dialogs/settings_tab_content_test.dart test/widgets/panel_size_test.dart test/widgets/multi_monitor_clamp_test.dart test/widgets/panel_color_test.dart test/widgets/tab_strip_order_test.dart

D:/flutter/bin/flutter analyze
</verification>

<success_criteria>
- All five LAYOUT requirements have explicit passing widget-test evidence in this plan.
- The test contract reflects D-01 through D-06 without production duplication or Phase 31 scope leakage.
- Full-suite comparison distinguishes new regressions from the documented headless MDK baseline.
</success_criteria>

<output>
Create `.planning/phases/30-panel-layout-redesign/30-04-SUMMARY.md` when done.
</output>
