---
phase: 36-medium-grain-rebuild-boundary-refactor
plan: 03
type: execute
wave: 3
depends_on: ["36-01", "36-02"]
files_modified:
  - test/widget/player/progress_bar_source_replacement_test.dart
  - test/widget/player/player_video_controls_lifecycle_test.dart
  - lib/ui/player/progress_bar.dart
  - lib/ui/player/player_video_controls.dart
  - lib/ui/player/control_bar.dart
autonomous: true
requirements: [REBUILD-01, REBUILD-04, REBUILD-05]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "ProgressBar seek-hold source replacement 先由 RED 测试验证；旧 position listener/timer 不得清理新 source 的 drag state。"
    - "ControlBar blur merge、PlayerVideoControls media identity、resize/engine/visible listeners 与 timers 在 replacement/deactivate/dispose 时对称解绑。"
    - "dispose 后旧新 notifier、timer 和 player stream 均不能触发 UI、padding 或 route 副作用。"
  artifacts:
    - path: "test/widget/player/progress_bar_source_replacement_test.dart"
      provides: "seek-hold old/new position source 隔离"
    - path: "test/widget/player/player_video_controls_lifecycle_test.dart"
      provides: "merged listener/timer 生命周期回归"
  key_links:
    - from: "lib/ui/player/progress_bar.dart"
      to: "ValueListenable<int> position"
      via: "seek-hold add/removeListener 对称迁移"
      pattern: "_seekHoldListener|didUpdateWidget"
    - from: "lib/ui/player/player_video_controls.dart"
      to: "engine/resizing/autoHide"
      via: "attach/detach/deactivate/activate/dispose"
      pattern: "_attachLifecycleListeners|_detachLifecycleListeners"
---

<objective>
测试先行关闭合并监听器、source replacement、timer、deactivate/dispose 的生命周期缺口，覆盖 REBUILD-01、REBUILD-04、REBUILD-05。

Purpose: 防止局部 rebuild 优化以旧 notifier 串扰、重复 listener 或 pending timer 为代价。
Output: ProgressBar seek-hold replacement 测试、controls 生命周期测试，以及仅由 RED 授权的最小修复。
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-widget-tree-baseline-behavior-recovery/35-03-SUMMARY.md
@lib/ui/player/progress_bar.dart
@lib/ui/player/player_video_controls.dart
@lib/ui/player/control_bar.dart
@test/widget/player/player_video_controls_test.dart
</context>

<interfaces>
`ProgressBar` 的 `position`/`duration`/`resizing` 可 replacement；seek-hold 临时监听当前 position 并有 timeout。`PlayerVideoControls` 管理 8 条 PlayerPort stream、engine/resize/visible listeners、click timer、AutoHide timer 与 animation controllers。所有测试使用 fake/injected source 和 `fakeAsync`/tester pump，不加载 MDK。
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>任务 1：以 RED 验证 ProgressBar seek-hold source replacement</name>
  <files>test/widget/player/progress_bar_source_replacement_test.dart, lib/ui/player/progress_bar.dart</files>
  <read_first>
    - lib/ui/player/progress_bar.dart
    - lib/ui/player/control_bar_timeline.dart
    - test/widget/player/control_bar_test.dart
    - test/perf/control_bar_perf_test.dart
  </read_first>
  <behavior>
    - drag end 建立 seek-hold 后替换 position source，旧 notifier 到达 target 不得结束当前 hold 或改变新进度。
    - 新 notifier 到达 target 才结束 hold；timeout 只触发一次且不留下 pending timer。
    - replacement、第二次 drag、dispose 均从实际注册 source 移除 listener。
    - duration/resizing replacement 继续驱动当前 source，不重建非进度区域。
  </behavior>
  <action>先创建精确 widget test：开始 drag、触发 hold、同 key rebuild 替换 position/duration/resizing，再分别驱动旧新 notifier，并在卸载后检查无 pending timer/异常。先运行 RED。只有失败证明 `_seekHoldListener` 仍注册在旧 `widget.position` 时，才让 ProgressBar 记录实际监听 source，并在 didUpdateWidget replacement 前对旧 source 对称解绑、按当前 hold 语义迁移或安全结束；不得未经测试改变 seek-hold 用户行为。若测试已绿，不改生产。</action>
  <verify><automated>flutter test test/widget/player/progress_bar_source_replacement_test.dart</automated></verify>
  <acceptance_criteria>
    - old/new position source 有明确双向断言，测试不只检查 `hasListeners`。
    - timeout、第二次 drag、dispose 均无 pending timer。
    - 生产修复仅在首次 RED 后实施，SUMMARY 记录结果。
    - 非平凡生命周期函数有 why 注释，函数少于 50 行，文件若将超过 500 行则提取聚焦 helper 文件而非继续膨胀。
  </acceptance_criteria>
  <done>ProgressBar source replacement 的 seek-hold 所有权被测试判定并在需要时最小修复。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：验证 merged listeners、timer 与 controls 生命周期对称性</name>
  <files>test/widget/player/player_video_controls_lifecycle_test.dart, lib/ui/player/player_video_controls.dart, lib/ui/player/control_bar.dart</files>
  <read_first>
    - lib/ui/player/player_video_controls.dart
    - lib/ui/player/control_bar.dart
    - lib/ui/player/auto_hide_controller.dart
    - test/widget/player/player_video_controls_test.dart
    - test/helpers/fake_player_controls.dart
    - test/helpers/fake_video_controls.dart
  </read_first>
  <behavior>
    - engine/currentFileName/resizing replacement 后旧 source 零响应，新 source 单次响应。
    - deactivate/activate 多轮不重复 8-stream 订阅或 lifecycle listener；inactive 时 timer/visible 不写 route padding。
    - ControlBar opacity/resizing source replacement 后 blur 只响应新 source。
    - dispose 后 click/auto-hide/seek timers、merged listeners、animations 全部静默且 tester 无 pending timer。
  </behavior>
  <action>先写组合生命周期测试，利用现有 fake 计数、padding history、streamListenAccessCount 和可观察 blur/visibility，不为方便接入真实 media_kit。逐一覆盖 replacement、两轮 deactivate/activate、卸载后驱动旧新 source 与推进所有已知 timer 窗口。仅在 RED 时修复 `_mediaIdentityListenable`、`_ControlBarBlurState` merge source、attach/detach 顺序或 timer cancel；注意 `Listenable.merge` 本身无 dispose API，生命周期责任是停止由 widget builder 持有并确保手工 addListener 对称移除。保持 notifier identity 和 route-local VideoControlsPort。</action>
  <verify><automated>flutter test test/widget/player/progress_bar_source_replacement_test.dart test/widget/player/player_video_controls_lifecycle_test.dart test/widget/player/player_video_controls_test.dart test/widget/player/control_bar_rebuild_boundary_test.dart &amp;&amp; flutter analyze &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - 每个手工 addListener 都有 replacement/deactivate/dispose 对称证据。
    - 多轮 activate 不增加 stream 订阅；dispose 后无 padding/route/build 副作用或 pending timer。
    - ControlBar merged signal replacement 有旧源隔离断言。
    - 不修改 media_kit、不恢复 ControlsOverlay、不引入状态管理；函数/文件尺寸和同步注释规则通过。
  </acceptance_criteria>
  <done>REBUILD-04 的 listener、merge、timer、stream 生命周期均由可执行测试覆盖。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| replaceable notifier → retained State | retained State 不得继续监听旧 source。 |
| timer/animation → inactive/disposed widget | 延迟回调不得访问 route 或排帧。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-06 | Denial of Service | listener/timer leak | high | mitigate | replacement、reparent、dispose 后推进事件与时间并断言零副作用。 |
| T-36-07 | Tampering | seek-hold source ownership | high | mitigate | old/new position 到达 target 的相反结果断言。 |
| T-36-08 | Repudiation | speculative lifecycle rewrite | medium | mitigate | RED 结果记录，最小方法级修复。 |
| T-36-SC | Tampering | package supply chain | low | accept | 不安装 package。 |
</threat_model>

<verification>
运行本计划两文件、Phase 35 PlayerVideoControls 回归、Plan 01 rebuild gate、`flutter analyze`、`git diff --check`；确认 diff 未触及受保护标题栏、截图、media_kit 或旧 overlay。
</verification>

<success_criteria>
- REBUILD-04 listener/timer 对称性完整。
- REBUILD-01 progress/resize 高频边界不污染非目标子树。
- REBUILD-05 保持中等颗粒度和项目代码质量约束。
</success_criteria>

<output>
完成后创建 `.planning/phases/36-medium-grain-rebuild-boundary-refactor/36-03-SUMMARY.md`
</output>
