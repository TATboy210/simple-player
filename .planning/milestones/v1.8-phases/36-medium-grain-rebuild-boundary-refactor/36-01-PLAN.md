---
phase: 36-medium-grain-rebuild-boundary-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/widget/player/control_bar_rebuild_boundary_test.dart
  - test/widget/player/control_bar_test.dart
  - lib/ui/player/control_bar.dart
  - lib/ui/player/control_bar_layout.dart
  - lib/ui/player/control_bar_title.dart
  - lib/ui/player/control_bar_actions.dart
autonomous: true
requirements: [REBUILD-01, REBUILD-02, REBUILD-05]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "标题变化只重建 ControlBarTitle 的动态标题区域，不重建 timeline、actions 或 ControlBar 外壳。"
    - "idle/播放状态变化只更新中央动作组及既有装饰动画，不重建标题、进度或左右静态动作组。"
    - "音量与播放状态由各自最小 ValueListenableBuilder 消费；ControlBar 静态构造参数仍可独立使用。"
    - "实现保持直接 ControlBar 架构和中等颗粒度，不恢复 ControlsOverlay。"
  artifacts:
    - path: "test/widget/player/control_bar_rebuild_boundary_test.dart"
      provides: "局部 rebuild 端到端计数与 identity 回归"
    - path: "lib/ui/player/control_bar_layout.dart"
      provides: "title/timeline/actions 三个稳定分区"
  key_links:
    - from: "lib/ui/player/player_video_controls.dart"
      to: "lib/ui/player/control_bar.dart"
      via: "titleListenable、isIdleListenable 与 ControlBarViewModel"
      pattern: "titleListenable|isIdleListenable"
    - from: "lib/ui/player/control_bar_layout.dart"
      to: "lib/ui/player/control_bar_title.dart"
      via: "titleListenable 局部消费"
      pattern: "ControlBarTitle"
---

<objective>
先以一条可执行的端到端 tracer 证明“文件名变化只触达标题局部”，再扩展 idle、播放状态与音量的中等颗粒度 rebuild 边界，覆盖 REBUILD-01、REBUILD-02、REBUILD-05。

Purpose: 用测试计数而非结构猜测证明单一状态变化不会重建整条 ControlBar。
Output: rebuild-boundary widget tests，以及仅由 RED 结果授权的 ControlBar 局部修正。
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-widget-tree-baseline-behavior-recovery/35-03-SUMMARY.md
@lib/ui/player/player_video_controls.dart
@lib/ui/player/control_bar.dart
@lib/ui/player/control_bar_layout.dart
@test/widget/player/control_bar_test.dart
</context>

<interfaces>
`ControlBar` 必须继续同时支持静态 `title`/`isIdle` 与播放器路径的 `titleListenable`/`isIdleListenable`。`ControlBarViewModel` 的播放、进度、音量、静音、倍速和全屏 listenable 由对应叶节点消费。测试使用可计数 builder/probe 与 fake ValueNotifier，不依赖 media_kit。
</interfaces>

<tasks>
<task type="tracer" tdd="true">
  <name>任务 1：端到端证明标题变化只重建标题 slice</name>
  <files>test/widget/player/control_bar_rebuild_boundary_test.dart, lib/ui/player/control_bar.dart, lib/ui/player/control_bar_layout.dart, lib/ui/player/control_bar_title.dart</files>
  <read_first>
    - lib/ui/player/control_bar.dart
    - lib/ui/player/control_bar_layout.dart
    - lib/ui/player/control_bar_title.dart
    - lib/ui/player/control_bar_timeline.dart
    - lib/ui/player/control_bar_actions.dart
    - lib/ui/player/control_bar_view_model.dart
    - test/widget/player/control_bar_test.dart
  </read_first>
  <behavior>
    - 使用真实 ControlBar 装配与 `titleListenable`，更新标题后新文本可见。
    - 标题 probe/build 计数增加，而 timeline、actions、ControlBar 外壳 probe 计数不增加。
    - timeline/actions 的 Element identity 在标题更新前后保持相同。
    - 静态 `title` 构造路径仍显示传入值。
  </behavior>
  <action>先创建失败测试，使用测试专用计数 probe 或现有可识别 key 包裹真实 ControlBar 分区；不得为了测试向生产暴露 debug-only 全局计数。运行 RED 后，仅修正标题 listenable 的消费位置，使动态值在 `ControlBarTitle` 内被监听并保留稳定 child；如果当前实现已经满足全部断言，只保留测试。不得重写整条 ControlBar、恢复 ControlsOverlay、修改 media_kit 或引入状态管理依赖。</action>
  <verify><automated>flutter test test/widget/player/control_bar_rebuild_boundary_test.dart --plain-name "title change rebuilds only title slice"</automated></verify>
  <acceptance_criteria>
    - 测试从真实 ControlBar 输入走到可见标题，且包含非目标区域不重建的负向断言。
    - 静态参数兼容性有明确断言。
    - 生产修改仅由 RED 证明授权。
    - 新增/修改非平凡函数同步写 `///` 或 why 注释；函数少于 50 行，生产文件少于 500 行。
  </acceptance_criteria>
  <done>REBUILD-02 的 titleListenable 链有端到端、可重复的最小局部 rebuild 证据。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：扩展 idle、播放状态与音量局部边界</name>
  <files>test/widget/player/control_bar_rebuild_boundary_test.dart, test/widget/player/control_bar_test.dart, lib/ui/player/control_bar_layout.dart, lib/ui/player/control_bar_actions.dart</files>
  <read_first>
    - test/widget/player/control_bar_rebuild_boundary_test.dart
    - test/widget/player/control_bar_test.dart
    - lib/ui/player/control_bar_layout.dart
    - lib/ui/player/control_bar_actions.dart
    - lib/ui/player/center_controls.dart
    - lib/ui/player/volume_controls.dart
    - lib/ui/player/control_bar_timeline.dart
  </read_first>
  <behavior>
    - idle notifier 变化只更新 CenterGroup 与装饰信号消费者，标题、timeline、左右动作区不重建。
    - isPlaying 变化只更新播放按钮；volume/isMuted 变化只更新音量区域。
    - position/duration 变化不重建标题或 actions；既有语义与 callbacks 保持。
  </behavior>
  <action>逐个先写 RED 测试并独立触发 idle、isPlaying、volume/isMuted、position/duration notifier。对每种输入同时断言目标叶节点变化及非目标 subtree build count/identity 稳定。仅对失败场景将 ValueListenableBuilder 下沉到直接消费者，保留 `ControlBarTitle.titleListenable`、`CenterGroup.isIdleListenable` 和静态参数 API；不创建一个监听全部媒体身份的父 builder。保持 Tokens、焦点、语义和交互回调不变。</action>
  <verify><automated>flutter test test/widget/player/control_bar_rebuild_boundary_test.dart test/widget/player/control_bar_test.dart &amp;&amp; flutter analyze &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - 每类状态有目标更新和至少两个非目标区域不重建断言。
    - 现有 ControlBar 行为/语义测试全部通过。
    - 无新单体文件超过 500 行、无函数超过 50 行；必要拆分按现有 player 组件组织。
    - 所有视觉值继续使用 Tokens；重要生命周期/非显然逻辑同步注释。
  </acceptance_criteria>
  <done>标题、idle、播放状态、音量和进度各自被最小必要子树消费，静态 API 保持兼容。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| notifier → widget subtree | 高频外部状态只能触发声明依赖它的局部区域。 |
| test instrumentation → production | 计数设施不得污染发布行为或形成新的全局状态。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-01 | Denial of Service | ControlBar rebuild fan-out | medium | mitigate | 每种 notifier 变化均以目标/非目标 build 计数锁定。 |
| T-36-02 | Tampering | static/dynamic parameter compatibility | medium | mitigate | 同时测试静态参数和 listenable 路径。 |
| T-36-SC | Tampering | package supply chain | low | accept | 不安装 package。 |
</threat_model>

<verification>
运行两个 ControlBar 测试文件、`flutter analyze`、`git diff --check`；检查 diff 不包含 `custom_title_bar.dart`、ControlsOverlay、media_kit、截图或新状态管理。
</verification>

<success_criteria>
- REBUILD-01/02 的 title、idle、playing、volume、progress 局部消费有自动化证据。
- REBUILD-05 的中等颗粒度、函数/文件尺寸和注释约束得到执行。
- 未经 RED 证明不修改生产实现。
</success_criteria>

<output>
完成后创建 `.planning/phases/36-medium-grain-rebuild-boundary-refactor/36-01-SUMMARY.md`
</output>
