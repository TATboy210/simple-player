---
phase: 36-medium-grain-rebuild-boundary-refactor
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - test/widget/player/player_screen_identity_source_replacement_test.dart
  - test/widget/player/player_screen_accessibility_resize_test.dart
  - lib/ui/player/player_screen.dart
  - lib/ui/player/player_video_controls.dart
autonomous: true
requirements: [REBUILD-03, REBUILD-04, REBUILD-05]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "普通 rebuild、窗口 mode 变化和 resize session 不替换标题栏、视频 surface 或 PlayerVideoControls Element。"
    - "WindowBridge replacement 绑定新 service；旧 mode/resize notifier 不再驱动当前树。"
    - "PlayerScreen controller/engine replacement 先由 RED 测试确定真实契约，再做最小 source 迁移；无失败则不改生产。"
  artifacts:
    - path: "test/widget/player/player_screen_identity_source_replacement_test.dart"
      provides: "screen shell identity 与 controller/engine replacement 证据"
  key_links:
    - from: "lib/ui/player/player_screen.dart"
      to: "lib/ui/player/player_video_controls.dart"
      via: "injected surface + FakeVideoControlsPort"
      pattern: "_buildTestControls"
    - from: "lib/ui/player/player_screen.dart"
      to: "lib/ui/window/custom_title_bar.dart"
      via: "cached title widget and WindowBridge replacement"
      pattern: "oldWidget.windowService"
---

<objective>
验证并收紧 PlayerScreen 标题栏、视频 surface、controls 的 identity/source 边界，覆盖 REBUILD-03、REBUILD-04、REBUILD-05。

Purpose: 确保外壳 rebuild 和 resize 不重挂载核心视频树，同时 replacement 不继续使用旧依赖。
Output: injected-surface 集成式测试，以及仅由 RED 授权的 PlayerScreen/PlayerVideoControls 最小迁移修复。
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-widget-tree-baseline-behavior-recovery/35-02-SUMMARY.md
@.planning/phases/35-widget-tree-baseline-behavior-recovery/35-03-SUMMARY.md
@lib/ui/player/player_screen.dart
@test/widget/player/player_screen_accessibility_resize_test.dart
@test/widget/player/player_screen_window_bridge_replacement_test.dart
</context>

<interfaces>
`PlayerScreen` 提供 `videoSurfaceBuilder` 与 `testVideoControls` 的 headless seam。`FakeWindowService` 暴露 mode/isResizing 与命令计数。测试必须保持同一 PlayerScreen key，并用 GlobalKey/context 记录 surface、title 和 PlayerVideoControls identity；不得构造真实 Video/MDK。
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>任务 1：锁定普通 build、mode、resize 与 bridge replacement 的三重 identity</name>
  <files>test/widget/player/player_screen_identity_source_replacement_test.dart, test/widget/player/player_screen_accessibility_resize_test.dart, lib/ui/player/player_screen.dart</files>
  <read_first>
    - lib/ui/player/player_screen.dart
    - lib/ui/window/custom_title_bar.dart
    - test/widget/player/player_screen_accessibility_resize_test.dart
    - test/widget/player/player_screen_window_bridge_replacement_test.dart
    - test/helpers/fake_window_service.dart
    - test/helpers/fake_video_controls.dart
  </read_first>
  <behavior>
    - 父级无关 setState、window mode、三轮 resize 中 title/surface/controls Element identity 不变。
    - bridge replacement 后 surface/controls identity 不变，title cache 可按依赖更新并只响应新 bridge。
    - old bridge mode/isResizing 在 replacement 后不改变当前 UI；new bridge 继续有效。
  </behavior>
  <action>先在同一 shell 记录受保护工作树基线：`TITLE_BAR_BASELINE=$(git diff --no-ext-diff -- lib/ui/window/custom_title_bar.dart | git hash-object --stdin)`，并将四个既有 PNG 的 `git status --porcelain -- 114514-upscaled.png devtools-current.png devtools-live-performance.png devtools-performance.png` 输出保存为基线。再用 injected surface 和 fake controls 编写同一 key 的集成测试，记录三类 Element context；将普通父 rebuild、mode、resize、bridge replacement 串为一条可归因流程。保护当前未提交 `lib/ui/window/custom_title_bar.dart`：只读，不编辑、不格式化、不暂存。结束前在同一 shell 比较 title-bar diff hash 和四个 PNG 的 porcelain 输出与基线完全相同。若 RED 显示 PlayerScreen 每次 build 新建导致 identity 变化，只在 `player_screen.dart` 调整稳定 child/cache 与 replacement 条件；若已通过则不改生产。不得缓存依赖 BuildContext 的过期结果。</action>
  <verify><automated>flutter test test/widget/player/player_screen_identity_source_replacement_test.dart test/widget/player/player_screen_accessibility_resize_test.dart test/widget/player/player_screen_window_bridge_replacement_test.dart</automated></verify>
  <acceptance_criteria>
    - 三类 identity 都有普通 rebuild、mode、resize 的断言。
    - bridge replacement 同时证明新 service 有效与旧 service 隔离。
    - `custom_title_bar.dart` 的执行前后 diff 完全保持用户现有增量。
    - 函数少于 50 行、生产文件少于 500 行；非显然 cache/replacement 逻辑有 why 注释。
  </acceptance_criteria>
  <done>REBUILD-03 的 shell、title、surface、controls identity 与 bridge replacement 均有 headless 证据。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：以 RED 验证 PlayerScreen controller/engine replacement</name>
  <files>test/widget/player/player_screen_identity_source_replacement_test.dart, lib/ui/player/player_screen.dart, lib/ui/player/player_video_controls.dart</files>
  <read_first>
    - test/widget/player/player_screen_identity_source_replacement_test.dart
    - lib/ui/player/player_screen.dart
    - lib/ui/player/player_video_controls.dart
    - test/helpers/fake_engine.dart
    - test/helpers/fake_video_controls.dart
  </read_first>
  <behavior>
    - 同一 PlayerScreen State 替换 engine/controller 后，旧 engine state/currentFileName 不再驱动空状态、标题或 controls。
    - 新 engine/controller 可驱动对应状态，surface identity 保持不变。
    - listener 数量与 teardown 表明旧 source 已解绑，dispose 后两组 source 均无 UI 副作用。
  </behavior>
  <action>本任务是验证门：先编写 replacement 测试并运行 RED，分别替换 engine 与 PlaybackController，记录旧新 title/state/listener 可观察结果。只有测试确实失败时，才在 `PlayerScreen.didUpdateWidget` 中对 `_syncOpenFileAvailability` listener、open-file timer、actions/source wiring 做最小迁移；如果稳定 `_actions` 的 callback 需要读取当前 widget，采用 invoke-time 读取而非重建整棵 Video。若测试已绿，保留测试且不修改生产。不得假定 replacement 必然有 bug，不得改变 media_kit builder 契约。</action>
  <verify><automated>flutter test test/widget/player/player_screen_identity_source_replacement_test.dart --plain-name "controller and engine replacement use only new sources" &amp;&amp; flutter analyze &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - SUMMARY 记录首次 RED/GREEN 结果；只有真实 RED 才有生产 diff。
    - 旧/新 source 均有正负断言，surface identity 不变。
    - 不恢复 ControlsOverlay、不修改 media_kit、不引入新状态管理。
    - 新增/修改公开或非平凡函数同步写文档/why 注释并满足尺寸限制。
  </acceptance_criteria>
  <done>PlayerScreen replacement 风险被测试判定；存在缺陷时以最小迁移修复，不存在时保持生产代码原样。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| cached widget → replaceable bridge/source | 稳定 identity 不能导致旧依赖继续生效。 |
| fake surface → production conclusion | headless test 证明项目层 identity，不冒充 native texture profile。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-03 | Tampering | cached title/actions | high | mitigate | 同 key replacement 的 old/new 双向断言。 |
| T-36-04 | Denial of Service | video subtree remount | high | mitigate | build/mode/resize 全程 Element identity 断言。 |
| T-36-05 | Repudiation | speculative production fix | medium | mitigate | 首次测试结果写 SUMMARY，RED 才修。 |
| T-36-SC | Tampering | package supply chain | low | accept | 无安装任务。 |
</threat_model>

<verification>
运行三个 PlayerScreen identity/replacement 测试、`flutter analyze`、`git diff --check`；用 `git diff -- lib/ui/window/custom_title_bar.dart` 确认受保护增量未被本计划改变。
</verification>

<success_criteria>
- REBUILD-03 identity 与 replacement 全覆盖。
- REBUILD-04 的旧 source 隔离在 PlayerScreen 层得到证据。
- controller/engine production 修复严格受 RED 约束。
</success_criteria>

<output>
完成后创建 `.planning/phases/36-medium-grain-rebuild-boundary-refactor/36-02-SUMMARY.md`
</output>
