---
phase: 32-navigation-interaction-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/32-navigation-interaction-polish/32-baseline-failures.txt
  - lib/kernel/services/input_mode_detector.dart
  - lib/ui/dialogs/settings/panel_key_bindings.dart
  - lib/ui/theme/tokens.dart
  - test/kernel/services/input_mode_detector_test.dart
  - test/ui/dialogs/settings_overlay_shell_test.dart
  - test/ui/dialogs/settings_focus_navigation_test.dart
autonomous: true
requirements:
  - NAV-02
  - NAV-04
  - NAV-07
must_haves:
  truths:
    - "InputModeDetector singleton exposes InputMode { keyboard, gamepad, auto } as user preference and a derived effective mode (keyboard or gamepad); mouse hover and the idle-arrow timer mutate effectiveMode ONLY while preference is auto — explicit keyboard/gamepad selections persist until the user selects auto (D-03); all four directional arrows (Up, Down, Left, Right) record arrow activity; manual toggle overrides the heuristic, selecting auto re-enables the heuristic (D-01, D-03)."
    - "panel_key_bindings.handle() returns KeyEventResult.handled for ALL four directional arrows (left, right, up, down); up/down call setArrowGlow (which sets the glow AND schedules a cancellable null-reset) before returning handled; the glow-reset lifecycle is owned by the detector and disposes cleanly (NAV-06, NAV-07, D-07)."
    - "No production reference to gameButtonLeft1 or gameButtonRight1 exists in lib/ — grep gate returns zero (NAV-04)."
    - "gameButton12 and gameButton13 direct gamepad routing is preserved — only Left1/Right1 cross-platform constants are deleted (D-05)."
    - "Outer KeyboardHandler seek and volume callbacks are NOT invoked when the settings panel is open and any directional arrow is pressed — containment proven by a spy test with zero-call assertions (NAV-07)."
    - "settings_focus_navigation_test.dart compiles after the stale FakePlaybackController(initiallyPlaying:) parameter is repaired to initialState: MediaState.playing."
    - "InputModeDetector.onPanelClosed() cancels the pending 5s gamepad-detection timer and the glow-reset timer and resets arrowGlow to null without disposing the singleton — so an arrow pressed just before the settings panel closes does not flip effectiveMode to gamepad 5s later while the panel is closed (checker warning 1, wired by Plan 02 settings_overlay_shell._onIsOpenChanged); the detector survives across open/close."
  artifacts:
    - path: lib/kernel/services/input_mode_detector.dart
      provides: Singleton ValueNotifier<InputMode> with mouse-idle heuristic (auto-gated per D-03), arrow-key timer (auto-gated), toggle fallback, setArrowGlow with cancellable null-reset lifecycle, arrowGlow notifier for NAV-06, onPanelClosed() panel-session hook, and @visibleForTesting forTest/resetInstance seam with injectable clock
    - path: test/kernel/services/input_mode_detector_test.dart
      provides: fakeAsync coverage (13 behaviors) of hover, idle, arrow, toggle, auto-restart, dispose-cancels-timer, panel-close-cancels-timer, and glow-reset lifecycle — all on isolated forTest instances with an injected fakeAsync-backed clock
    - path: lib/ui/dialogs/settings/panel_key_bindings.dart
      provides: Root key router with all four arrows handled, gameButtonLeft1/Right1 deleted, recordArrowKey wired, glow notifier on up/down
    - path: test/ui/dialogs/settings_overlay_shell_test.dart
      provides: gameButtonLeft1/Right1 tests deleted, grep assertion added, outer KeyboardHandler spy proving arrow containment
    - path: test/ui/dialogs/settings_focus_navigation_test.dart
      provides: Stale fake repaired — compiles and FocusTraversalGroup >= 4 assertion intact
  key_links:
    - from: lib/ui/dialogs/settings/panel_key_bindings.dart
      to: lib/kernel/services/input_mode_detector.dart
      via: recordArrowKey() called on every recognized directional arrow (all four); setArrowGlow() called on up/down (sets glow + schedules null-reset)
      pattern: InputModeDetector\.(instance|recordArrowKey|setArrowGlow|arrowGlow)
    - from: lib/ui/dialogs/settings/settings_overlay_shell.dart
      to: lib/ui/dialogs/settings/panel_key_bindings.dart
      via: Existing root Focus onKeyEvent delegates to keyBindings.handle — mount unchanged (D-08)
      pattern: keyBindings\.handle
---

<objective>
Prove the two most unknown Phase 32 risks end-to-end: (a) the InputModeDetector heuristic state machine is reliable under mouse-idle, arrow-key, and toggle scenarios, and (b) every directional arrow is contained by the single root Focus and cannot leak to the outer KeyboardHandler seek/volume callbacks. Land NAV-04 (delete obsolete gameButtonLeft1/Right1 bindings) atomically with NAV-07 (root containment) in the same plan and commit — deleting bindings without containment lets arrows escape to seek ±5s, and containment without deletion leaves duplicate navigation routes (D-07).

Purpose: Establish the sole new v4.5 infrastructure (InputModeDetector) and the arrow containment boundary that all subsequent plans build upon.
Output: A tested InputModeDetector singleton, an expanded root key router with all arrows handled and obsolete bindings deleted, repaired test baselines, and a grep gate proving zero production references to the deleted constants.
</objective>

<execution_context>
@C:/Users/35490/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/35490/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-navigation-interaction-polish/32-RESEARCH.md
@.planning/phases/32-navigation-interaction-polish/32-PLAN-OUTLINE.md
@lib/ui/dialogs/settings/panel_key_bindings.dart
@lib/ui/dialogs/settings/settings_overlay_shell.dart
@lib/ui/player/keyboard_handler.dart
@lib/ui/dialogs/settings/settings_panel_controller.dart
</context>

<tasks>

<task type="auto">
  <name>Task 0: Generate pre-Phase-32 baseline failure snapshot (clean trust root)</name>
  <precondition>The working tree is clean of Phase 32 lib/ code — Phase 32 PLAN.md files are committed but lib/kernel/services/input_mode_detector.dart does not exist yet (it is created in Task 1). This is the natural clean-baseline moment at the execute startpoint.</precondition>
  <files>.planning/phases/32-navigation-interaction-polish/32-baseline-failures.txt</files>
  <action>
    This task runs at the execute startpoint — the natural clean-baseline moment: the working tree is at Phase 31 green (Phase 32 PLAN.md files are committed, but NO Phase 32 lib/ code has been introduced yet). This moment is the ONLY valid trust root for the Phase 32 regression gate baseline, because any Phase 32 regression introduced after this point would be caught as a NEW failure by the gate (32-03 Task 1), not baked into the baseline. If Task 0 ran after Phase 32 lib/ code existed, a Phase 32 regression present at snapshot time would be baked into the baseline and silently allowed by the gate — invalidating the regression evidence.

    Run the full test suite once: D:/flutter/bin/flutter test. Capture stdout+stderr to a temp file. Extract every [E] failing test identity (path: test name) from lines matching the pattern "00:01 +1 -1: test/path.dart: test name [E]" using the same extraction as the 32-03 gate: grep -E '\[E\]' "$tmp" | sed -E 's/^[0-9]{2}:[0-9]{2} [+-][0-9]+( -[0-9]+)?: //; s/ \[E\].*$//' | sort -u. Write the deduplicated result to .planning/phases/32-navigation-interaction-polish/32-baseline-failures.txt.

    The snapshot documents the pre-existing environment failures (per memory reference_mdk_dll_headless_test_failures.md: ~57 mdk.dll FFI headless failures + 4 Phase 25 settings-dialog headless failures). The settings_focus_navigation_test.dart compile error is repaired by Task 2 in this plan — if the repair has NOT yet landed at Task 0 time, the compile error appears as an expected baseline entry; if it has landed, it does NOT appear. The exact count is determined empirically by running the suite — do not hardcode a count.

    The baseline artifact is IMMUTABLE once committed: re-generation is only valid by deleting the file and re-running this Task 0 on a clean Phase 31 tree (Phase 32 lib/ code not yet introduced). Do not use git stash or git worktree to obtain a pristine tree — the clean-baseline property is guaranteed by execution ORDER (Task 0 runs before Task 1 touches lib/), not by tree manipulation. Commit the artifact: git add the baseline file and commit with message "docs(32): generate pre-Phase-32 baseline failure snapshot".
  </action>
  <verify>
    <automated>test -s .planning/phases/32-navigation-interaction-polish/32-baseline-failures.txt && echo "BASELINE OK: $(wc -l < .planning/phases/32-navigation-interaction-polish/32-baseline-failures.txt | tr -d ' ') entries" || (echo "BASELINE FAIL: missing or empty snapshot — re-run Task 0 on a clean Phase 31 tree (Phase 32 lib/ code not yet introduced)"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/32-navigation-interaction-polish/32-baseline-failures.txt exists and is non-empty.
    - Every line is a deduplicated failing test identity (path: test name) extracted from a single D:/flutter/bin/flutter test run via the grep+sed extraction.
    - The snapshot was generated BEFORE any Phase 32 lib/ code was introduced (clean-baseline precondition — execution order guarantees this; the working tree had zero Phase 32 lib/ changes when the suite ran).
    - The artifact is committed to git (immutable once committed; re-generate only by deleting the file and re-running Task 0 on a clean Phase 31 tree).
  </acceptance_criteria>
  <done>The pre-Phase-32 baseline failure snapshot is generated on a clean Phase 31 tree (no Phase 32 lib/ code yet), committed as an immutable artifact at .planning/phases/32-navigation-interaction-polish/32-baseline-failures.txt, and ready for consumption by 32-03 Task 1's regression gate.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: InputModeDetector core + root key router expansion (tracer)</name>
  <read_first>
    - .planning/phases/32-navigation-interaction-polish/32-RESEARCH.md (Pattern 1 treat Steam arrows as ordinary; Pattern 2 mouse-idle heuristic; Pitfall 3 preference vs effective split)
    - lib/kernel/services/input_mode_detector.dart (does not exist yet — create)
    - lib/ui/dialogs/settings/panel_key_bindings.dart (L34-81 current handle + _isLeftShoulder/_isRightShoulder)
    - lib/ui/dialogs/settings/settings_panel_controller.dart (L37-74 prevTab/nextTab — sole tab-state owner)
    - lib/ui/player/keyboard_handler.dart (L114-132 outer seek/volume boundary)
  </read_first>
  <files>lib/kernel/services/input_mode_detector.dart, test/kernel/services/input_mode_detector_test.dart, lib/ui/dialogs/settings/panel_key_bindings.dart</files>
  <behavior>
    - Test 1: After mouse hover (PointerHoverEvent kind=mouse) while preference is auto, effective mode is keyboard.
    - Test 2: After 5s idle (no mouse) plus a recordArrowKey call while preference is auto, effective mode is gamepad.
    - Test 3: After manual toggle to keyboard, effective mode is keyboard regardless of heuristic signals.
    - Test 4: After manual toggle to auto, heuristic re-engages (mouse hover → keyboard).
    - Test 5: After dispose(), the pending 5s timer does not fire its callback.
    - Test 6: recordPointerActivity with kind=touch does NOT change mode (mouse filter only).
    - Test 7 (D-03 manual override persistence): After manual toggle to gamepad, subsequent mouse hover does NOT reset effectiveMode to keyboard — explicit gamepad selection persists until the user selects auto.
    - Test 8 (D-03 manual override persistence): After manual toggle to keyboard, the 5s idle+arrow timer firing does NOT reset effectiveMode to gamepad — explicit keyboard selection persists until the user selects auto.
    - Test 9 (vertical arrow recording): recordArrowKey() called from arrowUp and arrowDown branches schedules/refreshes the same 5s gamepad-detection timer as Left/Right — all four recognized directional branches record arrow activity per the NAV-02 heuristic contract.
    - Test 10 (glow successive-press replacement): Two rapid setArrowGlow calls (up then down) do not stack — the second cancels the first's reset timer and starts a fresh one; only one glow is active at a time.
    - Test 11 (glow auto-expiry): After setArrowGlow, the glow-reset timer fires and arrowGlow.value returns to null.
    - Test 12 (glow disposal): After dispose(), the in-flight glow-reset timer does not fire (no use-after-dispose).
    - Test 13 (panel-close cancellation): After onPanelClosed() is called, the pending 5s gamepad-detection timer is canceled and does NOT fire even after the idle timeout elapses; effectiveMode is not flipped to gamepad; arrowGlow resets to null. All tests instantiate an isolated InputModeDetector.forTest(...) instance (never the process-wide InputModeDetector.instance) for every behavior including dispose and timer tests — the production singleton is never disposed by tests.
  </behavior>
  <action>
    Create lib/kernel/services/input_mode_detector.dart defining two enums: InputMode { keyboard, gamepad, auto } (public user preference, D-03) and ArrowDirection { up, down } (for the glow notifier consumed by Plan 03). Implement InputModeDetector as a singleton with a private constructor InputModeDetector._({required Duration idleTimeout, required Duration glowResetDuration, required DateTime Function() clock}) and a static instance getter — the production singleton, lazily constructed with defaults idleTimeout=Duration(seconds: Tokens.inputModeIdleTimeoutSec), glowResetDuration=Duration(milliseconds: Tokens.arrowGlowDuration), clock=DateTime.now. It owns three ValueNotifiers: preference (InputMode, default auto), effectiveMode (InputMode, default keyboard — the derived mode that hints read, never auto), and arrowGlow (ArrowDirection?, default null — set by up/down arrows via setArrowGlow, consumed by Plan 03 overlay). The idle timeout Duration, the glow-reset Duration, AND an injectable monotonic clock (DateTime Function(), default DateTime.now) are ALL injectable via the private constructor — fakeAsync advances Timer instances but NOT DateTime.now, so tests pass a clock backed by fakeAsync.getClock(DateTime(2020)) (or an equivalent advancing clock) that moves in lockstep with the fake timer scheduler; recordPointerActivity and the timer callback read _clock() (NOT DateTime.now()) for the lastMouseActivity timestamp and the idle comparison. Add two tokens to lib/ui/theme/tokens.dart in a new Phase 32 section before the closing brace of the Tokens class: Tokens.inputModeIdleTimeoutSec (int, 5 — the heuristic idle threshold in seconds; D-06 no hardcoded values — the InputModeDetector production default reads this token so the idle timeout is token-driven, not a magic number) and Tokens.arrowGlowDuration (int, milliseconds for the transient glow display window before auto-reset to null). Both tokens are const. Creating inputModeIdleTimeoutSec in this Wave 1 task (which already owns tokens.dart) restores the Wave 1 to Wave 2 dependency order: the 32-01 tracer references Tokens.inputModeIdleTimeoutSec in its production default and can compile before 32-02 executes (blocker 1 dependency fix).

    Test seam (CLAUDE.md @visibleForTesting idiom): expose @visibleForTesting InputModeDetector.forTest({required Duration idleTimeout, required Duration glowResetDuration, required DateTime Function() clock}) — a factory constructing an isolated, NON-singleton instance for tests — and @visibleForTesting static void resetInstance() that re-creates the production singleton in a clean state. Singleton lifecycle/reset rules: the thirteen behavior tests MUST instantiate isolated InputModeDetector.forTest(...) instances for every behavior including dispose (Test 5), glow disposal (Test 12), and the panel-close test (Test 13) — the process-wide InputModeDetector.instance is NEVER disposed by tests (disposing the production singleton would leave subsequent tests without a working detector); tests that touch instance call resetInstance() in setUp. recordPointerActivity, recordArrowKey, setArrowGlow, toggle, onPanelClosed, and dispose operate on the receiver (this), so an isolated forTest instance behaves identically to the singleton.

    Implement recordPointerActivity(PointerEvent event): if event.kind != PointerDeviceKind.mouse, return without change (D-02 mouse filter); otherwise update a lastMouseActivity timestamp via the injected _clock() (default DateTime.now — tests pass a fakeAsync-backed clock so the timestamp advances in lockstep with the fake scheduler; DateTime.now itself is NOT advanced by fakeAsync). ONLY if preference.value == InputMode.auto (D-03 — preserve explicit keyboard/gamepad selections), set effectiveMode to keyboard and cancel any pending gamepad-detection timer; an explicit keyboard or gamepad preference stays stable until the user selects auto, so mouse movement never overrides a manual gamepad choice.

    Implement recordArrowKey(): refresh/ start a cancellable 5s timer; when it fires, compare _clock() against lastMouseActivity to verify the mouse has been idle longer than the timeout (D-01 source-indistinguishable heuristic — do NOT inspect physicalKey or deviceType; use the injected clock, not DateTime.now), and ONLY if preference.value == InputMode.auto (D-03 — preserve explicit keyboard/gamepad selections) set effectiveMode to gamepad; an explicit keyboard preference stays stable, so the idle-arrow timer never overrides a manual keyboard choice. Call this before panel actions on every recognized directional arrow in panel_key_bindings.handle() — all four branches (Up, Down, Left, Right) must record arrow activity per the NAV-02 heuristic contract.

    Implement setArrowGlow(ArrowDirection direction): cancel any in-flight _glowResetTimer, set arrowGlow.value = direction, then start a fresh _glowResetTimer (default Duration(milliseconds: Tokens.arrowGlowDuration)) that resets arrowGlow.value = null when it fires — replacement-on-new-press semantics so successive Up/Down presses replace (not stack) the glow. The timer is cancellable and tokenized via cancel-and-restart: a new directional press cancels the in-flight timer and starts a fresh one, so only one glow is active at a time.

    Implement toggle(InputMode mode): set preference to mode; if mode is keyboard or gamepad, set effectiveMode directly; if mode is auto, re-engage the heuristic (effectiveMode follows the last mouse/arrow state).

    Implement dispose(): cancel both the gamepad-detection timer and the glow-reset timer, and dispose all three ValueNotifiers (no use-after-dispose — ties to the timer-after-dispose DoS threat). This is the end-of-life teardown for isolated test instances; the production singleton is never disposed by tests.

    Implement onPanelClosed(): cancel the pending gamepad-detection timer and the glow-reset timer, and set arrowGlow.value = null — but do NOT dispose the ValueNotifiers or the singleton (the detector survives across panel open/close). This is the panel-session close hook called by settings_overlay_shell._onIsOpenChanged when isOpen flips to false (Plan 02, checker warning 1), ensuring an arrow pressed just before close does NOT flip effectiveMode to gamepad 5s later while the panel is closed. The singleton remains alive; subsequent panel re-opening re-engages the heuristic from the current mouse/arrow state.

    Expand panel_key_bindings.dart handle(): call InputModeDetector.instance.recordArrowKey() before EVERY recognized directional panel action — in the existing arrowLeft and arrowRight branches (before controller.prevTab()/nextTab()) AND in the two new arrowUp and arrowDown branches (before the glow call). Add two new branches for arrowUp and arrowDown: call InputModeDetector.instance.setArrowGlow(ArrowDirection.up or .down) (which sets the glow AND schedules its own cancellable null-reset — do NOT set arrowGlow.value directly from the router), then return KeyEventResult.handled (NAV-06 route, NAV-07 containment). Delete the gameButtonLeft1 comparison from _isLeftShoulder (keep gameButton13 only, D-05) and the gameButtonRight1 comparison from _isRightShoulder (keep gameButton12 only). Update the class doc comment to remove cross-platform Left1/Right1 wording. Do NOT introduce a new FocusTraversalGroup (D-08).

    Write test/kernel/services/input_mode_detector_test.dart using fakeAsync from dart:async. Construct each case with InputModeDetector.forTest(idleTimeout: short Duration, glowResetDuration: short Duration, clock: fakeAsync.getClock(DateTime(2020))) so the injected clock advances in lockstep with the fake timer scheduler (fakeAsync does NOT advance DateTime.now). Cover all thirteen behaviors listed in the behavior block: hover, idle+arrow, manual toggle keyboard, toggle auto re-engage, dispose cancels gamepad timer, touch filter, manual gamepad persists across mouse (D-03), manual keyboard persists across timer (D-03), vertical arrow recording, glow successive-press replacement, glow auto-expiry to null, glow disposal cancellation, and panel-close cancellation (onPanelClosed cancels the pending timer so it does not fire after the idle timeout). Every test calls dispose() on its isolated forTest instance — never on InputModeDetector.instance. Prefer fakes over mocks (CLAUDE.md convention).
  </action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/kernel/services/input_mode_detector_test.dart && D:/flutter/bin/flutter analyze lib/kernel/services/input_mode_detector.dart lib/ui/dialogs/settings/panel_key_bindings.dart lib/ui/theme/tokens.dart</automated>
  </verify>
  <acceptance_criteria>
    - input_mode_detector_test.dart passes all thirteen behaviors with fakeAsync (including D-03 manual-override persistence, glow-reset lifecycle, and panel-close cancellation).
    - InputModeDetector.instance exposes preference, effectiveMode, and arrowGlow as ValueNotifiers.
    - effectiveMode is never auto (D-03 split — auto is preference-only).
    - recordPointerActivity and the idle-arrow timer mutate effectiveMode ONLY when preference is auto (D-03 — manual keyboard/gamepad selections persist across mouse/timer signals).
    - recordArrowKey() is called before all four directional actions (Up, Down, Left, Right) — vertical arrows record and schedule the gamepad-detection timer.
    - setArrowGlow sets the glow and schedules a cancellable null-reset; successive presses replace (not stack); dispose cancels the glow-reset timer.
    - InputModeDetector exposes @visibleForTesting forTest({...}) and @visibleForTesting resetInstance(); tests construct isolated forTest instances so the process-wide InputModeDetector.instance is never disposed (singleton lifecycle/reset rules).
    - An injectable DateTime Function() clock (default DateTime.now) is read via _clock() in recordPointerActivity and the timer callback; tests pass a fakeAsync-backed clock so DateTime advances in lockstep with fakeAsync timers (fakeAsync does not advance DateTime.now).
    - onPanelClosed() cancels the pending gamepad-detection and glow-reset timers and resets arrowGlow to null without disposing the singleton; Test 13 proves the pending timer does not fire after onPanelClosed().
    - panel_key_bindings.handle() returns handled for arrowUp, arrowDown, arrowLeft, arrowRight, escape, keyB, gameButton12, gameButton13.
    - panel_key_bindings._isLeftShoulder checks only gameButton13; _isRightShoulder checks only gameButton12.
    - Tokens.arrowGlowDuration and Tokens.inputModeIdleTimeoutSec are const in the Tokens class (inputModeIdleTimeoutSec is created in this Wave 1 task — same task that references it in the InputModeDetector default, so the tracer compiles before Wave 2 executes; blocker 1 dependency fix).
    - Per CLAUDE.md Comment Policy (MANDATORY — write comments WHILE coding): InputModeDetector, InputMode, and ArrowDirection public types carry `///` doc comments explaining purpose, parameters, and behavior; non-trivial public APIs (recordPointerActivity, recordArrowKey, setArrowGlow, toggle, onPanelClosed, forTest, resetInstance) carry `///` doc comments. Inline why-comments explain: (a) the injected clock — fakeAsync advances Timer instances but NOT DateTime.now(), that is WHY the clock is injected and read via _clock() in recordPointerActivity and the timer callback; (b) timer cancellation in dispose() and onPanelClosed() — why both timers are canceled (no use-after-dispose DoS); (c) panel-close lifecycle — why onPanelClosed resets arrowGlow and cancels timers without disposing the singleton.
    - flutter analyze reports zero warnings on the three new/modified files.
  </acceptance_criteria>
  <done>InputModeDetector singleton is tested with D-03 manual-override protection (auto-gated mutations), all four arrows record activity, up/down glow has a cancellable null-reset lifecycle, and the root key router handles all four arrows — obsolete gameButtonLeft1/Right1 comparisons deleted from the shoulder helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair stale fake + delete gameButton tests + add containment spy + grep gate (NAV-04 + NAV-07)</name>
  <read_first>
    - test/ui/dialogs/settings_focus_navigation_test.dart (L1-39 pumpShell with stale fake at L20; L173-186 FocusTraversalGroup >= 4 assertion)
    - test/ui/dialogs/settings_panel_controller_test.dart (L16-39 FakePlaybackController constructor — uses initialState not initiallyPlaying)
    - test/ui/dialogs/settings_overlay_shell_test.dart (L574-651 gameButton12/13 tests to KEEP; L614-651 gameButtonRight1/Left1 tests to DELETE; L653+ arrow key tests)
    - lib/ui/player/keyboard_handler.dart (L40-58 VoidCallback spy surface — onSeekBackward/onSeekForward/onVolumeUp/onVolumeDown)
  </read_first>
  <files>test/ui/dialogs/settings_focus_navigation_test.dart, test/ui/dialogs/settings_overlay_shell_test.dart</files>
  <behavior>
    - Test 1: settings_focus_navigation_test.dart compiles and the FocusTraversalGroup >= 4 assertion still passes after the stale fake is repaired.
    - Test 2: settings_overlay_shell_test.dart no longer contains tests for gameButtonRight1 or gameButtonLeft1.
    - Test 3: A grep assertion confirms zero gameButtonLeft1/gameButtonRight1 references in lib/ (production code).
    - Test 4: An outer KeyboardHandler spy records zero calls to onSeekBackward, onSeekForward, onVolumeUp, onVolumeDown when the panel is open and arrowLeft/arrowRight/arrowUp/arrowDown are pressed.
  </behavior>
  <action>
    Repair test/ui/dialogs/settings_focus_navigation_test.dart: at line 20 change FakePlaybackController(initiallyPlaying: true) to FakePlaybackController(initialState: MediaState.playing). Add the import package:simple_player_flutter/kernel/engine/media_state.dart if not already present. Do NOT change any assertion logic — only the stale constructor parameter.

    Modify test/ui/dialogs/settings_overlay_shell_test.dart: delete the two widget tests at L614-651 (gamepad gameButtonRight1 also works and gamepad gameButtonLeft1 also works). Keep the gameButton12 and gameButton13 tests at L574-612 intact (D-05 — direct gamepad routing is preserved). Replace the deleted tests with two new tests: (a) a source-grep assertion via Process.run('git', ['grep','-nE','gameButtonLeft1|gameButtonRight1','--',':(exclude)test/**','lib/**']) that treats result.exitCode == 1 as PASS (zero matches — git grep exit 1 means no matches), result.exitCode == 0 as FAIL (prohibited references remain), and any other exitCode as a command error — this captures git grep's exit status directly (no shell pipe, so exit status is never lost); (b) an outer KeyboardHandler spy test that wraps the SettingsOverlayShell in a KeyboardHandler with counting VoidCallbacks for onSeekBackward, onSeekForward, onVolumeUp, onVolumeDown, opens the panel, sends arrowLeft/arrowRight/arrowUp/arrowDown KeyDownEvents, pumps, and asserts each spy callback count is zero. This spy test proves NAV-07 containment — arrows cannot escape the root Focus to the outer player shortcuts (Pitfall 1 mitigation).

    Do NOT add a new FocusTraversalGroup anywhere (D-08). Do NOT weaken or remove any existing assertion (CLAUDE.md testing rule).
  </action>
  <verify>
    <automated>D:/flutter/bin/flutter test test/ui/dialogs/settings_focus_navigation_test.dart test/ui/dialogs/settings_overlay_shell_test.dart && (
      # Direct conditional — preserves git grep's own exit status (a pipe would lose it on the RHS).
      # git grep exit 0 = matches found (FAIL); exit 1 = zero matches (PASS); other = command error.
      if git grep -nE 'gameButtonLeft1|gameButtonRight1' -- ':(exclude)test/**' 'lib/**'; then
        echo "GREP GATE FAIL: prohibited production references remain"
        exit 1
      else
        status=$?
        if [ "$status" -eq 1 ]; then
          echo "GREP GATE PASS: zero matches in lib/"
        else
          echo "GREP GATE FAIL: git grep could not complete (exit $status)"
          exit "$status"
        fi
      fi
    )</automated>
  </verify>
  <acceptance_criteria>
    - settings_focus_navigation_test.dart compiles and passes (stale fake repaired).
    - The FocusTraversalGroup >= 4 assertion in settings_focus_navigation_test.dart still passes (D-08 — no new group added).
    - settings_overlay_shell_test.dart contains no gameButtonLeft1/gameButtonRight1 widget-test names or synthetic key-event dispatches for Left1/Right1, EXCEPT the dedicated production-source grep assertion whose string literal ('gameButtonLeft1|gameButtonRight1') is the assertion's subject (not a behavioral reference) — the authoritative production-reference check remains the lib/** grep gate below.
    - The gameButton12 and gameButton13 tests at L574-612 are preserved and pass.
    - The outer KeyboardHandler spy test asserts zero calls to seek/volume callbacks when panel is open.
    - git grep -nE 'gameButtonLeft1|gameButtonRight1' -- ':(exclude)test/**' 'lib/**' returns zero matches (exit code 1 from grep = no matches).
  </acceptance_criteria>
  <done>The stale fake is repaired, obsolete gameButton tests are replaced with a grep gate and a containment spy, and the grep gate confirms zero production references to the deleted constants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings panel root Focus → outer KeyboardHandler | Untrusted directional key events must be consumed by the panel root before they bubble to player seek/volume shortcuts. |
| InputModeDetector timer → dispose | A pending 5s timer must not fire after the detector is disposed. |
| Steam Input → Flutter KeyEvent | External mapping injects keyboard arrows with no retained provenance; the heuristic must not branch on key source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | Arrow key leak to KeyboardHandler seek/volume | high | mitigate | Root Focus returns handled for ALL four directional arrows (left/right/up/down); outer KeyboardHandler spy test asserts zero callback invocations when panel is open. |
| T-32-02 | Denial of service | Timer callback after InputModeDetector disposal | medium | mitigate | Cancel both the gamepad-detection timer and the glow-reset timer in dispose(); fakeAsync tests verify no callback fires after disposal. |
| T-32-03 | Tampering | Stale gameButtonLeft1/Right1 bindings cause double-fire with Steam LB/RB | high | mitigate | Delete both comparisons from _isLeftShoulder/_isRightShoulder; grep gate enforces zero production references (NAV-04). |
| T-32-04 | Spoofing | Key-source guessing from physicalKey/deviceType | medium | mitigate | Heuristic uses mouse-idle + arrow-presence only; never inspects physicalKey, HardwareKeyboard, or deviceType (D-01). |
| T-32-01X | Denial of service | Nested blur/readback from option-list overlay (cross-plan, nested-blur) | low | mitigate | Owned by 32-03 Task 1 — overlay uses Container(color: Tokens.bgGlass), structural test asserts 0 BackdropFilter (NAV-05, D-04); not directly applicable in 32-01's detector/router scope. |
| T-32-05 | Tampering | Baseline snapshot staleness — Task 0 runs after Phase 32 lib/ code introduced, baking a Phase 32 regression into the trust root | high | mitigate | Execution order guarantees Task 0 runs before Task 1 touches lib/ (documented precondition + executor asserts it); baseline artifact is committed immutable; re-generation only by deleting the file and re-running Task 0 on a clean Phase 31 tree. No git stash/worktree used. |
| T-32-06 | Denial of service | Stale auto-detection fires after settings panel close, flipping effectiveMode to gamepad while panel is closed | medium | mitigate | onPanelClosed() cancels the pending gamepad-detection + glow-reset timers and resets arrowGlow to null (does NOT dispose the singleton); Plan 02 calls it from settings_overlay_shell._onIsOpenChanged on isOpen→false; Test 13 verifies the pending timer does not fire after onPanelClosed(). |
| T-32-SC | Tampering | Package installs | high | mitigate | No package-manager install occurs; Phase 32 adds zero new dependencies (D-06). The package legitimacy audit records zero new packages. |
</threat_model>

<verification>
Wave 1 tracer proves both unknowns end-to-end: (a) InputModeDetector state machine passes six fakeAsync behavior tests, and (b) the outer KeyboardHandler spy test confirms zero arrow-leak to seek/volume when the panel is open. The grep gate enforces NAV-04 zero-production-references as an executable shell command. Flutter analyze must report zero warnings on all Phase 32 modified files.
</verification>

<success_criteria>
- InputModeDetector singleton is tested with thirteen fakeAsync behaviors covering hover, idle, toggle, auto-restart, dispose, touch filter, D-03 manual-override persistence (gamepad across mouse, keyboard across timer), vertical arrow recording, glow-reset lifecycle (successive-press replacement, auto-expiry, disposal cancellation), and panel-close cancellation (onPanelClosed cancels pending auto-detection). Tests use isolated forTest instances with an injected fakeAsync-backed clock; the production singleton is never disposed by tests.
- All four directional arrows are handled by the root key router; all four record arrow activity; up/down call setArrowGlow with cancellable null-reset.
- gameButtonLeft1 and gameButtonRight1 are deleted from lib/ (grep gate zero).
- gameButton12 and gameButton13 direct gamepad routing is preserved and tested.
- settings_focus_navigation_test.dart compiles and its FocusTraversalGroup >= 4 assertion passes.
- The outer KeyboardHandler spy proves zero arrow leakage to seek/volume callbacks.
- The pre-Phase-32 baseline failure snapshot is generated on a clean Phase 31 tree and committed as an immutable artifact (32-baseline-failures.txt) for consumption by 32-03 Task 1's regression gate.
- Tokens.arrowGlowDuration and Tokens.inputModeIdleTimeoutSec are const in the Tokens class (inputModeIdleTimeoutSec created in Wave 1, referenced by the InputModeDetector default in the same task — dependency fix for blocker 1).
- Per CLAUDE.md Comment Policy: all new public types (InputModeDetector, InputMode, ArrowDirection) and non-trivial public APIs carry `///` doc comments; inline why-comments explain the injected-clock rationale, timer cancellation, and panel-close lifecycle.
- flutter analyze reports zero warnings on input_mode_detector.dart, panel_key_bindings.dart, and tokens.dart.
</success_criteria>

<output>
Create .planning/phases/32-navigation-interaction-polish/32-01-SUMMARY.md when done.
</output>
