---
phase: 33-audio-settings-tab
plan: 02
type: tdd
wave: 2
depends_on: [33-01]
files_modified:
  - lib/ui/dialogs/settings/audio_filter_compositor.dart
  - lib/ui/dialogs/settings/tabs/equalizer_tab.dart
  - test/kernel/audio/audio_filter_compositor_test.dart
  - test/ui/dialogs/settings/audio_tab_test.dart
autonomous: true
requirements: [AUDIO-02, AUDIO-03, AUDIO-05, AUDIO-06]
must_haves:
  truths:
    - "A user can stage balance from full left through center to full right and audio delay from 0ms through +10000ms without live engine updates."
    - "An Apply or OK produces one af chain where balance is represented by pan and a positive sync value produces the matching per-channel adelay value."
  artifacts:
    - path: "lib/ui/dialogs/settings/audio_filter_compositor.dart"
      provides: "Bounded balance and sync filter composition"
      exports: ["AudioSettings", "AudioFilterCompositor"]
    - path: "lib/ui/dialogs/settings/tabs/equalizer_tab.dart"
      provides: "Deferred balance and audio-sync slider controls"
    - path: "test/kernel/audio/audio_filter_compositor_test.dart"
      provides: "Pan and adelay boundary tests"
  key_links:
    - from: "lib/ui/dialogs/settings/tabs/equalizer_tab.dart"
      to: "lib/ui/dialogs/settings/pending_settings.dart"
      via: "slider callbacks write balance and syncMs pending keys"
      pattern: "pending\.update\('(balance|syncMs)'"
    - from: "lib/ui/dialogs/settings/audio_filter_compositor.dart"
      to: "lib/features/player/player_feature.dart"
      via: "the existing tracer commit callback consumes the single composed chain"
      pattern: "AudioFilterCompositor"
---

<objective>
Expand the proven audio tracer with deferred balance and sync controls, retaining one deterministic af chain and the existing single commit route.

Purpose: Add spatial and timing control without violating pure deferred-apply behavior.
Output: Pan and adelay composition, two bounded settings controls, and boundary/commit regression coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-audio-settings-tab/33-RESEARCH.md
@.planning/phases/33-audio-settings-tab/33-01-SUMMARY.md
</context>

## Artifacts this phase produces

- Balance-to-pan composition with canonical full-left, center omission, and full-right behavior.
- Audio-delay-to-adelay composition for the persisted 0ms through +10000ms range.
- Deferred `Balance` and `Audio Delay` slider controls in the combined first audio settings tab.

<tasks>

<task type="executor" agent_type="gsd-executor" tdd="true">
  <name>Task 1: Add deferred balance and audio-delay sliders plus their pan and adelay filter composition</name>
  <files>lib/ui/dialogs/settings/audio_filter_compositor.dart, lib/ui/dialogs/settings/tabs/equalizer_tab.dart, test/kernel/audio/audio_filter_compositor_test.dart, test/ui/dialogs/settings/audio_tab_test.dart</files>
  <read_first>
    - lib/ui/dialogs/settings/audio_filter_compositor.dart
    - lib/ui/dialogs/settings/tabs/equalizer_tab.dart
    - lib/ui/dialogs/settings/pending_settings.dart
    - lib/ui/dialogs/settings/settings_panel_controller.dart
    - lib/ui/shared/settings_card.dart
    - lib/ui/theme/tokens.dart
    - test/kernel/audio/audio_filter_compositor_test.dart
    - test/ui/dialogs/settings/audio_tab_test.dart
    - .planning/phases/33-audio-settings-tab/33-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: balance `-1.0` produces `pan=stereo|c0=1.00*c0|c1=0.00*c1`, balance `1.0` produces `pan=stereo|c0=0.00*c0|c1=1.00*c1`, and balance `0.0` adds no pan segment.
    - Test 2: nonzero balance is clamped before two-decimal coefficient formatting; preset EQ precedes pan in a complete chain.
    - Test 3: audio delay `10000` yields `adelay=10000|10000`, audio delay `0` adds no delay segment, and persisted out-of-range values are bounded to `0..10000` before composition.
    - Test 4: a complete chain orders EQ before pan and pan before adelay.
    - Test 5: changing either slider updates its pending key while the commit fake has zero calls until Apply/OK; one Apply/OK call sends the complete chain only once.
  </behavior>
  <action>Start RED by extending the tracer tests. In `AudioFilterCompositor`, derive balance coefficients using `leftGain = clamp(1.0 - balance, 0.0, 1.0)` and `rightGain = clamp(1.0 + balance, 0.0, 1.0)`, format both with `toStringAsFixed(2)`, and omit the pan segment for exact center. For a nonzero audio-delay setting, append `adelay=<milliseconds>|<milliseconds>` after pan using the validated value directly; do not transform it with `abs()`. Document in the composer why Phase 33 deliberately exposes delay-only `0..10000ms`: FFmpeg `adelay` cannot advance audio, so the range guarantees that a positive UI value consistently means audio plays later. Keep the Phase 33 canonical order EQ → balance → delay and honor the Plan 01 availability result by excluding an unsupported segment with its logged warning. Add `Balance` and `Audio Delay` rows to the combined first tab using SettingRow, Slider, Tokens.accent, and Tokens.bgHover. Set balance range to `-1.0..1.0`; set audio-delay range to `0.0..10000.0`, round slider values to int, and give both controls stable `ValueKey` identifiers for widget tests. Each slider callback must call only `pending.update` and `setState` to redraw values; it must not reach the commit callback, PlayerFeature, or an engine object. Preserve the second `tabs/audio_tab.dart` and all non-audio tab placeholder files unchanged. Keep functions below the local 50-line limit by extracting formatting or row builders where needed.</action>
  <acceptance_criteria>
    - The first tab exposes a balance slider with bounds `-1.0` and `1.0` and an Audio Delay slider with bounds `0.0` and `10000.0`.
    - The composer produces exact full-left/full-right pan strings and no pan segment at center.
    - The composer appends per-channel adelay after pan for each nonzero bounded audio-delay value, passes the direct nonnegative value without `abs()`, and omits unavailable runtime filter segments per the Plan 01 smoke result.
    - Focused widget tests prove slider interactions do not invoke the audio commit fake before Apply/OK and the existing commit seam remains the only application route.
    - No change is made to any MediaEngine, SubtitleConfig, FvpEngine, or SubtitleConfigurator declaration.
  </acceptance_criteria>
  <verify>
    <automated>D:/flutter/bin/flutter test test/kernel/audio/audio_filter_compositor_test.dart test/ui/dialogs/settings/audio_tab_test.dart --concurrency=1 &amp;&amp; D:/flutter/bin/flutter analyze</automated>
    <human-check>On Windows with a stereo media file, apply full-left and full-right balance separately, then apply 500ms Audio Delay and confirm the controls affect audio only after Apply or OK.</human-check>
  </verify>
  <done>Balance and sync are bounded deferred controls, their exact pan/adelay segments join the existing single af chain in canonical order, and their focused tests demonstrate one commit-side application path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slider values → filter composition | Continuous UI input is transformed into finite MDK pan and delay arguments. |
| Persisted audio state → slider values | Stored doubles and integers are rendered and committed as bounded user controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-04 | Tampering | Balance/sync filter values | low | mitigate | Use SettingsValidator bounds and composer clamps before formatting the FFmpeg chain. |
| T-33-05 | Denial of service | MDK af reinitialization | medium | mitigate | Slider callbacks only update pending state; existing commit callback applies one complete chain. |
| T-33-06 | Integrity | Stereo pan mapping | medium | mitigate | Assert exact edge mappings and perform the targeted Windows stereo smoke test. |
| T-33-SC | Tampering | package installs | high | accept | No package-manager operation occurs in this phase; pubspec remains unchanged. |
</threat_model>

<verification>
Run the compositor and audio widget test files, then static analysis. Perform the focused Windows stereo playback check using the Plan 01 smoke result because the fvp-bundled pan and adelay runtime support must be established before combined application.
</verification>

<success_criteria>
- AUDIO-02 produces correct pan mappings for full-left, center, and full-right balance.
- AUDIO-03 composes bounded `0..10000ms` per-channel adelay values and documents that positive Audio Delay values play later.
- AUDIO-05 and AUDIO-06 preserve the single deferred commit route for the expanded chain.
</success_criteria>

<output>
Create `.planning/phases/33-audio-settings-tab/33-02-SUMMARY.md` when done.
</output>
