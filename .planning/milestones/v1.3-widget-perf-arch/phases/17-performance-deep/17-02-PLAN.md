---
phase: 17-performance-deep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - PERF-09
  - PERF-10
must_haves:
  truths:
    - "VLB audit complete — no merge candidates found"
    - "Zero OverlayEntry instances — all popups use showMenu/showDialog"
    - "controls_overlay.dart audit comment is accurate"
  artifacts:
    - path: "lib/ui/player/controls_overlay.dart"
      contains: "已优化"
---

<objective>
PERF-09 + PERF-10: VLB 审计验证 + OverlayEntry 确认

Purpose: 验证现有 VLB 审计结论正确，确认无 OverlayEntry 优化需求。
Output: 验证报告 + REQUIREMENTS.md 更新。
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: VLB 审计验证</name>
  <files></files>
  <behavior>
    - controls_overlay.dart 审计注释准确
    - player_screen.dart 4 层 VLB 嵌套合理（各自独立变更频率）
    - VLB2 helper 正确用于 2-notifier 组合
    - child: 参数在 controls_overlay 和 settings_panel 中正确使用
  </behavior>
  <action>
    验证 3 个关键 VLB 站点：
    1. controls_overlay.dart — 确认 child: 参数缓存 Stack 子树
    2. player_screen.dart — 确认 4 层嵌套各自独立
    3. settings_panel.dart — 确认 child: 参数缓存面板内容

    无需代码修改 — 仅验证。
  </action>
  <verify>
    <automated>grep -n "child:" lib/ui/player/controls_overlay.dart lib/ui/dialogs/settings_panel.dart</automated>
  </verify>
  <done>VLB 审计验证完成，现有优化正确</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: OverlayEntry 确认</name>
  <files></files>
  <behavior>
    - 零 OverlayEntry 实例
    - 所有 popup 使用 showMenu/showDialog
    - 无 setState 在 popup 构建代码中
  </behavior>
  <action>
    搜索确认：
    1. grep OverlayEntry in lib/ui/ → 零结果
    2. grep showMenu/showDialog → 确认使用内置 API
    3. 确认 thumbnail_tile 和 speed_button 的 popup 代码无多余 setState
  </action>
  <verify>
    <automated>grep -rn "OverlayEntry" lib/ui/</automated>
  </verify>
  <done>OverlayEntry 确认完成，无优化需求</done>
</task>

</tasks>

<success_criteria>
- VLB 审计结论验证正确
- OverlayEntry 优化确认为 N/A
- REQUIREMENTS.md 更新 PERF-09 和 PERF-10 状态
</success_criteria>

<output>
Create `.planning/milestones/v1.3-widget-perf-arch/phases/17-performance-deep/17-02-SUMMARY.md` when done
</output>
