---
phase: 16-performance-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/player/control_bar.dart
  - lib/ui/player/video_surface.dart
  - lib/ui/shared/aurora_background.dart
  - lib/ui/player/progress_bar.dart
autonomous: true
requirements:
  - PERF-05
must_haves:
  truths:
    - "ControlBar has RepaintBoundary wrapping its content"
    - "VideoSurface has RepaintBoundary wrapping its texture"
    - "AuroraBackground has RepaintBoundary wrapping its CustomPaint"
    - "ProgressBar._buildBarLayers has RepaintBoundary wrapping its bar layers"
  artifacts:
    - path: "lib/ui/player/control_bar.dart"
      contains: "RepaintBoundary"
    - path: "lib/ui/player/video_surface.dart"
      contains: "RepaintBoundary"
    - path: "lib/ui/shared/aurora_background.dart"
      contains: "RepaintBoundary"
    - path: "lib/ui/player/progress_bar.dart"
      contains: "RepaintBoundary"
  key_links:
    - from: "lib/ui/player/player_screen.dart"
      to: "lib/ui/player/control_bar.dart"
      via: "ControlBar rendered inside ControlsOverlay with RepaintBoundary"
---

<objective>
PERF-05: RepaintBoundary 全面审计

Purpose: 确认 4 个关键渲染路径 widget 已有 RepaintBoundary 包裹。研究发现所有目标 widget 均已完成，此计划为验证 + 文档确认。
Output: 验证通过，无需代码变更。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3-widget-perf-arch/ROADMAP.md
@.planning/milestones/v1.3-widget-perf-arch/phases/16-performance-foundation/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证 RepaintBoundary 完整性</name>
  <files>lib/ui/player/control_bar.dart, lib/ui/player/video_surface.dart, lib/ui/shared/aurora_background.dart, lib/ui/player/progress_bar.dart</files>
  <action>
    验证以下 4 个 widget 已有 RepaintBoundary 包裹（研究已确认，此步为代码级验证）：

    1. **ControlBar** (`lib/ui/player/control_bar.dart`): 在 build 方法中，`RepaintBoundary(child: content)` 包裹 GlassContainer 内容。确认存在。
    2. **VideoSurface** (`lib/ui/player/video_surface.dart`): Texture 渲染区域有 RepaintBoundary 包裹。确认存在。
    3. **AuroraBackground** (`lib/ui/shared/aurora_background.dart`): 在 build 中 `RepaintBoundary(child: LayoutBuilder(...))` 包裹。确认存在（line 229）。
    4. **ProgressBar** (`lib/ui/player/progress_bar.dart`): `_buildBarLayers` 方法中 `RepaintBoundary(child: ...)` 包裹。确认存在（line 180）。

    如果任何 widget 缺少 RepaintBoundary，添加之。如果全部存在，无需修改。

    同时验证次要 widget 的 RepaintBoundary 状态（信息性，不需要修改）：
    - OsdOverlay, ErrorBanner, PlaylistPanel, ThumbnailTile, EmptyState, GlassContainer — 均应已有。
  </action>
  <verify>
    <automated>grep -n "RepaintBoundary" lib/ui/player/control_bar.dart lib/ui/player/video_surface.dart lib/ui/shared/aurora_background.dart lib/ui/player/progress_bar.dart</automated>
  </verify>
  <done>4 个目标 widget 均有 RepaintBoundary 包裹，grep 输出显示每个文件至少一处 RepaintBoundary</done>
</task>

<task type="auto">
  <name>Task 2: 验证无多余 RepaintBoundary（避免过度使用）</name>
  <files>lib/ui/</files>
  <action>
    统计 lib/ui/ 下 RepaintBoundary 总数量。研究显示当前有 14 处。确认数量合理（12-16 范围）。

    如果发现 RepaintBoundary 包裹了不包含 CustomPainter / Texture / BackdropFilter 的简单 Container（过度使用），移除该 RepaintBoundary 以减少不必要的 compositing layer 开销。

    审计标准：
    - RepaintBoundary 包裹 CustomPainter → 保留
    - RepaintBoundary 包裹 Texture → 保留
    - RepaintBoundary 包裹 BackdropFilter → 保留
    - RepaintBoundary 包裹纯 Container（无上述三种） → 评估是否需要
  </action>
  <verify>
    <automated>grep -rn "RepaintBoundary" lib/ui/ | wc -l</automated>
  </verify>
  <done>RepaintBoundary 总数在 12-16 范围内，无过度使用（每个实例都有正当理由）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

此计划无信任边界变更 — 仅验证现有代码结构。

## STRIDE Threat Register

无安全相关变更。此计划为纯验证。
</threat_model>

<verification>
1. `grep -n "RepaintBoundary" lib/ui/player/control_bar.dart lib/ui/player/video_surface.dart lib/ui/shared/aurora_background.dart lib/ui/player/progress_bar.dart` — 每个文件至少一处
2. `grep -rn "RepaintBoundary" lib/ui/ | wc -l` — 总数 12-16
3. `flutter test` — 所有现有测试通过
</verification>

<success_criteria>
- 4 个目标 widget 均确认有 RepaintBoundary
- 无过度使用的 RepaintBoundary
- 所有测试通过
</success_criteria>

<output>
Create `.planning/milestones/v1.3-widget-perf-arch/phases/16-performance-foundation/16-01-SUMMARY.md` when done
</output>
