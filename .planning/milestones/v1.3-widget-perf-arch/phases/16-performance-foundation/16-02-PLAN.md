---
phase: 16-performance-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/bridge/window_bridge.dart
  - lib/kernel/bridge/window_service.dart
  - lib/ui/shared/glass_container.dart
  - lib/ui/player/control_bar.dart
  - lib/ui/playlist/playlist_panel.dart
  - lib/ui/player/player_screen.dart
  - lib/ui/player/controls_overlay.dart
  - lib/ui/shared/aurora_background.dart
  - test/helpers/fake_window_service.dart
autonomous: true
requirements:
  - PERF-06
  - PERF-07
must_haves:
  truths:
    - "Blur is disabled during window resize in main widget tree (GlassContainer, ControlBar, PlaylistPanel)"
    - "Resize signal fires on first frame and clears 200ms after resize ends"
    - "Aurora animation uses zero per-frame Paint allocations"
    - "Dialog BackdropFilter excluded — modal overlays can't be resized while open"
  artifacts:
    - path: "lib/kernel/bridge/window_bridge.dart"
      contains: "isResizing"
    - path: "lib/kernel/bridge/window_service.dart"
      contains: "isResizing"
    - path: "lib/ui/shared/glass_container.dart"
      contains: "resizing"
    - path: "lib/ui/shared/aurora_background.dart"
      contains: "static final"
    - path: "test/helpers/fake_window_service.dart"
      contains: "isResizing"
  key_links:
    - from: "lib/kernel/bridge/window_service.dart"
      to: "lib/kernel/bridge/window_bridge.dart"
      via: "WindowService implements WindowBridge.isResizing"
      pattern: "ValueNotifier<bool> isResizing"
    - from: "lib/ui/shared/glass_container.dart"
      to: "lib/kernel/bridge/window_bridge.dart"
      via: "GlassContainer receives resizing via constructor param"
    - from: "lib/ui/player/player_screen.dart"
      to: "lib/kernel/bridge/window_service.dart"
      via: "PlayerScreen passes windowService.isResizing to ControlsOverlay"
---

<objective>
PERF-06 + PERF-07: BackdropFilter resize 降级 + Paint cache

Purpose: 消除 resize 期间 BackdropFilter GPU readback 瓶颈，同时缓存 CustomPainter Paint 对象减少每帧分配。
Output: isResizing 信号链路完成，3 个 BackdropFilter 站点 resize 时跳过，_AuroraPainter Paint 对象改为 static final。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3-widget-perf-arch/ROADMAP.md
@.planning/milestones/v1.3-widget-perf-arch/phases/16-performance-foundation/RESEARCH.md
@lib/kernel/bridge/window_bridge.dart
@lib/kernel/bridge/window_service.dart
@lib/ui/shared/glass_container.dart
@lib/ui/player/control_bar.dart
@lib/ui/playlist/playlist_panel.dart
@lib/ui/shared/aurora_background.dart
@test/helpers/fake_window_service.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: isResizing 信号链路 — WindowBridge + WindowService + FakeWindowService</name>
  <files>lib/kernel/bridge/window_bridge.dart, lib/kernel/bridge/window_service.dart, test/helpers/fake_window_service.dart</files>
  <behavior>
    - FakeWindowService.isResizing 默认值为 false
    - FakeWindowService.isResizing.value = true 后，监听者收到通知
    - WindowService.onWindowResize() 同步设置 isResizing.value = true
    - WindowService.onWindowResize() 200ms 后自动设置 isResizing.value = false
    - WindowService.dispose() 清理 _resizeEndTimer
  </behavior>
  <action>
    **1. WindowBridge 接口** (`lib/kernel/bridge/window_bridge.dart`):
    在 `ValueNotifier<Size> get windowSize;` 之后添加：
    ```
    ValueNotifier<bool> get isResizing;
    ```

    **2. WindowService 实现** (`lib/kernel/bridge/window_service.dart`):
    - 在 `_resizeDebounce` 声明附近添加：`Timer? _resizeEndTimer;`
    - 在 `windowSize` 声明附近添加：`final ValueNotifier<bool> isResizing = ValueNotifier(false);`
    - 修改 `onWindowResize()` 方法，在现有 debounce 逻辑之前添加：
      ```
      isResizing.value = true;  // 立即设置，捕获第一帧
      _resizeEndTimer?.cancel();
      _resizeEndTimer = Timer(const Duration(milliseconds: 200), () {
        if (!_disposed) isResizing.value = false;
      });
      ```
    - 在 `dispose()` 方法中，`_resizeDebounce?.cancel()` 之后添加：`_resizeEndTimer?.cancel();`
    - 在 `dispose()` 方法中，`windowSize.dispose()` 附近添加：`isResizing.dispose();`

    **3. FakeWindowService** (`test/helpers/fake_window_service.dart`):
    - 添加：`@override final ValueNotifier<bool> isResizing = ValueNotifier(false);`
    - 在 `dispose()` 方法中添加：`isResizing.dispose();`
  </action>
  <verify>
    <automated>flutter analyze lib/kernel/bridge/window_bridge.dart lib/kernel/bridge/window_service.dart test/helpers/fake_window_service.dart</automated>
  </verify>
  <done>WindowBridge 定义 isResizing，WindowService 实现带 200ms debounce 的 resize 信号，FakeWindowService 提供测试替身</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BackdropFilter resize 降级 — GlassContainer + ControlBar + PlaylistPanel</name>
  <files>lib/ui/shared/glass_container.dart, lib/ui/player/control_bar.dart, lib/ui/playlist/playlist_panel.dart, lib/ui/player/player_screen.dart, lib/ui/player/controls_overlay.dart</files>
  <behavior>
    - GlassContainer resizing 参数非空且 value=true 时，跳过 BackdropFilter，仅渲染 ClipRRect + content
    - GlassContainer resizing 和 blurEnabled 是独立的 OR 条件 — 任一为 true 即跳过 BackdropFilter
    - GlassContainer resizing 和 opacity 交互：resizing=true 时直接跳过，不检查 opacity
    - ControlBar resizing 信号为 true 时跳过 BackdropFilter
    - PlaylistPanel resizing 信号为 true 时跳过 BackdropFilter
  </behavior>
  <action>
    **1. GlassContainer** (`lib/ui/shared/glass_container.dart`):
    - 添加构造参数：`final ValueListenable<bool>? resizing;`
    - 在构造函数中添加：`this.resizing;`
    - 在 `build()` 方法中，`!blurEnabled` 检查之后、`blurContent` 创建之前，添加 resize 降级检查：
      ```dart
      // resize 期间跳过 BackdropFilter（PERF-06）
      if (resizing != null && resizing!.value) {
        return ClipRRect(
          borderRadius: rRect,
          child: RepaintBoundary(child: content),
        );
      }
      ```
    - 如果 GlassContainer 使用 AnimatedBuilder 监听 opacity，resize 检查应在 AnimatedBuilder 内部（因为 resize 状态可能在动画过程中变化）。

    **2. ControlBar** (`lib/ui/player/control_bar.dart`):
    - 添加构造参数：`final ValueListenable<bool>? resizing;`
    - 在构造函数中添加：`this.resizing;`
    - 在 build 方法中 BackdropFilter 使用处，添加 resize 检查：`if (!enableBlur || (resizing != null && resizing!.value)) return RepaintBoundary(child: content);`
    - 在 opacity AnimatedBuilder 内部，也添加 resize 检查。

    **3. PlaylistPanel** (`lib/ui/playlist/playlist_panel.dart`):
    - 添加构造参数：`final ValueListenable<bool>? resizing;`
    - 在构造函数中添加：`this.resizing;`
    - 在 BackdropFilter 使用处（line 170 附近），添加 resize 检查。当 resizing?.value == true 时，跳过 BackdropFilter，仅渲染 ClipRRect + FadeTransition + SizedBox.expand()。
    - 具体实现：用 AnimatedBuilder 监听 resizing，在 builder 中判断。

    **4. PlayerScreen** (`lib/ui/player/player_screen.dart`):
    - 将 `windowService.isResizing` 传递给 ControlsOverlay。

    **5. ControlsOverlay** (`lib/ui/player/controls_overlay.dart`):
    - 添加构造参数接收 `ValueListenable<bool>? resizing`
    - 将 resizing 传递给 ControlBar。
  </action>
  <verify>
    <automated>flutter analyze lib/ui/shared/glass_container.dart lib/ui/player/control_bar.dart lib/ui/playlist/playlist_panel.dart lib/ui/player/player_screen.dart lib/ui/player/controls_overlay.dart</automated>
  </verify>
  <done>3 个 BackdropFilter 站点在 isResizing=true 时跳过 BackdropFilter，信号从 WindowService 传递到各 widget</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Paint cache — _AuroraPainter static final</name>
  <files>lib/ui/shared/aurora_background.dart</files>
  <behavior>
    - _AuroraPainter 使用 static final _bgPaint 和 _compositePaint
    - drawRect 使用 _bgPaint 而非内联 Paint()..color = Tokens.bgBase
    - drawImage 使用 _compositePaint 而非内联 Paint()
    - flutter analyze 无 warning
    - 现有测试通过
  </behavior>
  <action>
    **_AuroraPainter** (`lib/ui/shared/aurora_background.dart`):
    - 在 `_AuroraPainter` 类中，`_lissajous` 声明之前添加两个 static final Paint：
      ```dart
      static final _bgPaint = Paint()..color = Tokens.bgBase;
      static final _compositePaint = Paint();
      ```
    - 在 `paint()` 方法中：
      - Line 287: `canvas.drawRect(Offset.zero & size, Paint()..color = Tokens.bgBase);` → `canvas.drawRect(Offset.zero & size, _bgPaint);`
      - Line 316: `canvas.drawImage(img, Offset(-img.width / 2, -img.height / 2), Paint());` → `canvas.drawImage(img, Offset(-img.width / 2, -img.height / 2), _compositePaint);`
    - 注意：循环内有 3 次 drawImage 调用，全部替换为 _compositePaint。Paint 对象属性固定（默认 compositing），跨帧共享安全。
  </action>
  <verify>
    <automated>grep -n "Paint()" lib/ui/shared/aurora_background.dart</automated>
  </verify>
  <done>_AuroraPainter 的 4 个 per-frame Paint 分配替换为 static final，grep 确认无残留 Paint() 内联分配（除 _generateBlobImages 中的初始化代码）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 测试 — GlassContainer resize 行为 + FakeWindowService</name>
  <files>test/helpers/fake_window_service.dart, test/ui/shared/glass_container_test.dart</files>
  <behavior>
    - FakeWindowService.isResizing 默认 false
    - GlassContainer 接收 resizing=ValueNotifier(true) 时，不渲染 BackdropFilter
    - GlassContainer resizing=false 时，正常渲染 BackdropFilter
    - GlassContainer resizing + blurEnabled=false 双条件互不影响
  </behavior>
  <action>
    **1. FakeWindowService** — Task 1 已添加 isResizing，此处确认 dispose 正确。

    **2. GlassContainer 测试** (新建 `test/ui/shared/glass_container_test.dart`):
    - 测试 1: `resizing=null` 且 `blurEnabled=true` → 渲染 BackdropFilter
    - 测试 2: `resizing=ValueNotifier(true)` → 不渲染 BackdropFilter，仅 ClipRRect
    - 测试 3: `resizing=ValueNotifier(false)` → 渲染 BackdropFilter
    - 测试 4: `blurEnabled=false` → 不渲染 BackdropFilter（已有行为，回归保护）
  </action>
  <verify>
    <automated>flutter test test/ui/shared/glass_container_test.dart</automated>
  </verify>
  <done>GlassContainer resize 行为测试通过，覆盖 blurEnabled × resizing 矩阵</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WindowService → Widget tree | isResizing 信号从 Bridge 层传播到 UI 层 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | npm/pip/cargo installs | accept | 此阶段无新包安装 |

无安全相关变更 — isResizing 是只读信号，不接受外部输入。
</threat_model>

<verification>
1. `flutter analyze` — 零 warning
2. `flutter test` — 所有现有测试通过
3. `grep -n "Paint()" lib/ui/shared/aurora_background.dart` — 无 per-frame Paint 分配（除初始化代码）
4. `grep -n "isResizing" lib/kernel/bridge/window_bridge.dart lib/kernel/bridge/window_service.dart test/helpers/fake_window_service.dart` — 信号链路完整
5. `grep -n "resizing" lib/ui/shared/glass_container.dart lib/ui/player/control_bar.dart lib/ui/playlist/playlist_panel.dart` — 3 个站点均有 resize 检查
</verification>

<success_criteria>
- isResizing ValueNotifier 在 WindowBridge 接口定义，WindowService 实现带 200ms debounce
- GlassContainer、ControlBar、PlaylistPanel 在 isResizing=true 时跳过 BackdropFilter
- _AuroraPainter 使用 static final Paint 对象，零 per-frame 分配
- 所有测试通过，flutter analyze 零 warning
</success_criteria>

<output>
Create `.planning/milestones/v1.3-widget-perf-arch/phases/16-performance-foundation/16-02-SUMMARY.md` when done
</output>
