---
phase: quick-260901-e97
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/l10n/app_en.arb
  - lib/l10n/app_zh.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_en.dart
  - lib/l10n/app_localizations_zh.dart
  - lib/ui/player/error_card.dart
  - lib/ui/player/error_card_host.dart
  - test/widget/player/error_card_test.dart
  - test/widget/player/error_card_host_test.dart
autonomous: true
requirements: [] # quick task (260901-e97) — sourced from /gsd-quick brief, no ROADMAP requirement IDs
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 折叠态错误卡片同时暴露「复制详情」与「打开日志」两个动作，无需展开
    - 「复制详情」= 既有 error-card-copy 按钮（formatDiagnosticPack 单一来源），剪贴板内容与 error.log 逐字符一致，全路径允许进剪贴板（D-07 边界与展开区复制一致）
    - 「打开日志」以 explorer.exe /select, 语义定位当前生效 error.log（diagnosticLogPath.value 取值），完整路径只进进程参数与剪贴板、绝不渲染进可见树（T-03-05 不变）
    - 两动作均失败隔离：typed catch（copy=PlatformException/MissingPluginException 既有；open-log=ProcessException 新增），OSD 反馈，异常绝不外溢
  artifacts:
    - lib/ui/player/error_card.dart（onOpenLog 参数 + error-card-open-log 按钮）
    - lib/ui/player/error_card_host.dart（logExplorer 测试缝 + _openLog 接线）
    - lib/l10n/app_en.arb、app_zh.arb（3 个新 key）+ gen-l10n 产物
    - test/widget/player/error_card_test.dart、error_card_host_test.dart 新用例
  key_links:
    - error-card-open-log → widget.onOpenLog → 宿主 _onOpenLog → logExplorer 缝 → Process.start('/select,', path)
    - open-log 目标路径 ← ErrorReporterImpl.I.diagnosticLogPath.value（复制时刻同源读取路径）
    - OSD 反馈 ← OsdService.I.show（成功/失败/路径不可用三态）
---

<objective>
错误卡片加「打开日志」按钮并锁定「复制详情」单一实现：折叠态即可复制完整诊断包（复用既有 error-card-copy，不新建复制路径），新增按钮以 dart:io explorer.exe /select, 定位 error.log（零新增依赖），两动作均带 CARD-04 形态失败隔离。

Purpose: 出错后无需手动翻日志目录即可一键回溯现场——复制诊断包发人、打开日志定位文件，构成错误反馈闭环的最后两个动作。
Output: error_card.dart 折叠态按钮区 +1 按钮；error_card_host.dart 回调接线 + 注入缝；3 个 l10n key；两测试文件新增用例全绿。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@lib/ui/player/error_card.dart
@lib/ui/player/error_card_host.dart

关键事实（已核查，执行时不必重读全文件）：
- 既有折叠态复制按钮已完整实现「复制详情」需求：error_card.dart 内 key `error-card-copy`，`_copyDiagnosticPack()` 走 `formatDiagnosticPack`（LOG-05 单一来源）+ typed catch（PlatformException/MissingPluginException）+ OsdService 反馈 + `KernelLoggerImpl.isInitialized` 探针守卫（WR-02）。本任务**不新增任何复制实现**。
- `ErrorReporterImpl.I.diagnosticLogPath` 为 `ValueListenable<String?>?`（error_reporter.dart:134），其 `.value` 即 ErrorLogFileSink 当前生效路径，展开区日志路径段同一读取路径。
- ErrorCardHost 生产挂载点 `const ErrorCardHost()`（app.dart:185）——新增可选构造参数不破坏挂载点。
- 测试先例：test/widget/player/error_card_test.dart 的 `mockPlatformChannel` + `settleOsdTimer` helper + `OsdService.I.message.value?.text` 断言；`_FakeLogStatus`/`makeReporter`/`acceptHead` helper；error_card_host_test.dart 的 close/cycle 接线用例组。
- l10n：l10n.yaml（arb-dir lib/l10n，template app_en.arb，nullable-getter: false），`flutter gen-l10n` 再生成；zh key 与 en key 相邻放置。
- 本机已知：headless 全量 flutter test 有预存失败（mdk.dll 等历史项）——验证范围锚定本次改动的两个测试文件与 flutter analyze。
</context>

<tasks>

<task type="auto">
  <name>Task 1: l10n 新增 3 个 errorCard open-log key 并再生成</name>
  <files>lib/l10n/app_en.arb, lib/l10n/app_zh.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_en.dart, lib/l10n/app_localizations_zh.dart</files>
  <action>
在 app_en.arb 的 errorCardCopyFailed（约 450 行）之后新增三个 key，各带 @key description 元数据：errorCardOpenLogTooltip = "Open log location"（按钮 tooltip/语义标签）；errorCardLogOpened = "Log opened"（打开成功 OSD）；errorCardOpenLogFailed = "Open log failed"（打开失败 OSD）。在 app_zh.arb 的既有 errorCardCopyTooltip（约 214 行）附近对称位置新增同名字段（zh ARB 不写 @元数据，与现有风格一致）：errorCardOpenLogTooltip = "打开日志位置"；errorCardLogOpened = "已打开日志位置"；errorCardOpenLogFailed = "打开日志失败"。然后运行 flutter gen-l10n 再生成本地化类。不要手改 app_localizations*.dart 生成文件。路径不可用降级复用既有 errorCardLogUnavailable（"日志文件不可用"/"Log file unavailable"），不新增第四个 key。
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter analyze</automated>
  </verify>
  <done>flutter analyze 0 error；AppLocalizations 出现 errorCardOpenLogTooltip/errorCardLogOpened/errorCardOpenLogFailed 三个 getter（en+zh 双语）；生成文件为 gen-l10n 产物而非手工编辑。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ErrorCard 折叠态新增「打开日志」按钮（RED→GREEN）</name>
  <files>lib/ui/player/error_card.dart, test/widget/player/error_card_test.dart</files>
  <behavior>
    - Test 1（折叠态存在性 + 接线）：构造带 onOpenLog 回调的 ErrorCard（复用现有 buildCard/makeReporter helper），折叠态下 find.byKey(ValueKey('error-card-open-log')) findsOneWidget；tap 恰好回调一次；卡片保持折叠（chevron-down 仍在）。
    - Test 2（命中隔离）：tap error-card-open-log 后展开区段（errorCardSectionLocation 文本）不出现——按钮 GestureDetector 内层优先，整卡 _toggle 不被触发（与 copy 按钮同一 CARD-04 命中保证）。
    - Test 3（条件渲染）：不传 onOpenLog 的 ErrorCard（现有用例的默认构造）下 error-card-open-log findsNothing——与 onClose 的 `if (widget.onClose != null)` 同一条件渲染纪律。
  </behavior>
  <action>
先在 test/widget/player/error_card_test.dart 新增一个 group（命名「open-log 打开日志按钮（E97）」）落上述三个用例，运行 flutter test 确认 RED（按钮不存在）。然后在 lib/ui/player/error_card.dart：ErrorCard 构造参数在 onClose 之后新增 `final VoidCallback? onOpenLog;`，附双语 /// doc comment（说明宿主接线打开日志、条件渲染语义）。build 的 Row 中在 copy 按钮 Semantics 块之后、chevron Icon 之前插入条件渲染块 `if (widget.onOpenLog != null)`：外层 Semantics(label: l10n.errorCardOpenLogTooltip, button: true)，内层 GestureDetector(key: const ValueKey('error-card-open-log'), onTap: widget.onOpenLog)，child 为 Padding(all: Tokens.spXs) 包 Icon(Icons.folder_open, size: Tokens.iconSm, color: Tokens.textSecondary)。不用 GlassButton（其 FocusableActionDetector 请求焦点破坏 CARD-01 零焦点抢占——与 copy/close 同一理由，写进注释）。按钮不渲染任何路径文本（T-03-05：完整路径不进可见树）。GREEN 后 dart format 两文件。
  </action>
  <verify>
    <automated>flutter test test/widget/player/error_card_test.dart && flutter analyze</automated>
  </verify>
  <done>新 group 三用例全绿；既有 copy/折叠/展开用例零回归；flutter analyze 0 error；按钮条件渲染、语义标签、Token 全量来源。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 宿主接线 _openLog + explorer.exe 启动缝 + 失败隔离（RED→GREEN）</name>
  <files>lib/ui/player/error_card_host.dart, test/widget/player/error_card_host_test.dart</files>
  <behavior>
    - Test 1（取值 + 成功反馈）：挂载 ErrorCardHost 且注入记录型 logExplorer 假缝，reporter 带 _FakeLogStatus('C:/logs/diag.txt') 使 diagnosticLogPath.value 非空；tap error-card-open-log → 假缝收到的唯一实参逐字符等于 'C:/logs/diag.txt'，OsdService.I.message.value?.text == '已打开日志位置'；settleOsdTimer 收尾。
    - Test 2（路径不可用降级）：reporter 无 diagnosticLogStatus（logPath null）→ tap 后假缝零调用，OSD text == '日志文件不可用'（复用既有降级 key）。
    - Test 3（ProcessException 失败隔离）：注入的假缝抛 ProcessException('explorer.exe', OSError(), '') → OSD text == '打开日志失败'，测试正常完成无未捕获异常外溢（isolation 证明），settleOsdTimer 收尾。
  </behavior>
  <action>
先在 test/widget/player/error_card_host_test.dart 新增 group（「open-log 宿主接线与失败隔离（E97）」）落上述三个用例（挂载方式沿用该文件现有 tracer 组的 host 挂载与 reporter 初始化纪律），确认 RED。然后在 lib/ui/player/error_card_host.dart：ErrorCardHost 构造新增可选参数 `final Future<void> Function(String logPath)? logExplorer;`（/// 注释注明：仅测试注入缝，生产走默认 explorer 启动器）。文件顶部新增 dart:io 与 AppLocalizations（../../l10n/app_localizations.dart）、dart:async（unawaited）导入。新增私有静态/顶层函数 `Future<void> _launchExplorerSelect(String logPath) => Process.start('explorer.exe', ['/select,', logPath]);`——参数列表形态启动（无 shell、无字符串拼接），/select, + 路径为规划钦定 Windows 语义，注释标注。新增 `Future<void> _openLog()`：await 前先解析 l10n（沿用 _copyDiagnosticPack 的 context 提前捕获纪律）；`ErrorReporterImpl.isInitialized` 为假或 `diagnosticLogPath?.value` 为 null/空 → OsdService.I.show(errorCardLogUnavailable, icon: Icons.info_outline) 并 return；否则 `(logExplorer ?? _launchExplorerSelect)(path)` 包在 try 中，成功 → OsdService.I.show(errorCardLogOpened, icon: Icons.folder_open)；typed catch 只捕 ProcessException → OsdService.I.show(errorCardOpenLogFailed, icon: Icons.error_outline) + KernelLoggerImpl.isInitialized 探针守卫的 KernelLogger.I.w（WR-02 同款守卫；日志 context 只记 error.executable 与 error.message，不记完整路径——D-07 日志侧同纪律）；不捕任何 Error 子类型，异常绝不外溢。新增 `void _onOpenLog() { unawaited(_openLog()); }` 适配 VoidCallback，build 中 ErrorCard 传 `onOpenLog: _onOpenLog`（与 onClose 同级接线注释）。测试严禁不注入缝就 tap open-log（真实缝会拉起 explorer.exe；三个新用例全部注入假缝）。GREEN 后 dart format。
  </action>
  <verify>
    <automated>flutter test test/widget/player/error_card_host_test.dart test/widget/player/error_card_test.dart && flutter analyze</automated>
  </verify>
  <done>新 group 三用例全绿且既有 host 用例零回归；open-log 全链路 error-card-open-log → _onOpenLog → _openLog → 注入缝打通；ProcessException 与 null 路径两失败态均 OSD 反馈且零外溢；analyze 0 error。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → explorer.exe | 点击「打开日志」把 error.log 路径作为参数交给外部进程 explorer.exe |
| clipboard | 折叠态复制把含完整路径的诊断包写入系统剪贴板（D-07 明确放行） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-E97-01 | Tampering | _launchExplorerSelect Process.start | low | mitigate | 列表参数形态（argList）启动，无 shell 拼接，路径不参与命令行字符串拼装；路径源为 reporter 自身 diagnosticLogPath，非用户自由输入 |
| T-E97-02 | Information Disclosure | 剪贴板/explorer 参数中的完整 error.log 路径 | low | accept | D-07 决策明确放行：剪贴板完整路径与现有展开区复制行为一致；可见树零渲染（T-03-05 不变），日志 context 不记完整路径 |
| T-E97-03 | Denial of Service | explorer.exe 启动失败/非 Windows 平台 | low | mitigate | typed catch ProcessException + OSD「打开日志失败」，异常不外溢，卡片可见性与内容不受影响（CARD-04 同形态） |
</threat_model>

<verification>
- flutter gen-l10n 幂等（重跑无 diff）
- flutter analyze 0 error（全项目）
- flutter test test/widget/player/error_card_test.dart test/widget/player/error_card_host_test.dart 全绿（新用例 + 既有用例零回归）
- 卡内只有一条复制实现路径（grep error_card.dart：formatDiagnosticPack 仅被 _copyDiagnosticPack 调用）
- 可见树零完整路径渲染（T-03-05 既有守卫用例不回归）
</verification>

<success_criteria>
折叠态错误卡片一键复制与日志逐字符一致的完整诊断包；一键以资源管理器定位当前生效 error.log；两动作失败均有 OSD 反馈且异常零外溢；零新增依赖、kernel 零接触、analyze 0 error。
</success_criteria>

<output>
Create `.planning/quick/260901-e97-error-card-buttons/260901-e97-SUMMARY.md` when done
</output>
