---
phase: 08-delete-abstraction
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - test/regression/smoke_suite_test.dart
  - test/regression/high_risk_suite_test.dart
  - test/integration/fullscreen_e2e_test.dart
autonomous: false
requirements:
  - SIMPLIFY-01
  - SIMPLIFY-02
  - SIMPLIFY-03

must_haves:
  truths:
    - smoke_suite_test.dart 不再引用 FullscreenPhase、FullscreenSnapshot、FullscreenError 类型
    - high_risk_suite_test.dart 不再引用 FullscreenPhase、FullscreenSnapshot 类型
    - fullscreen_e2e_test.dart 不再引用 FullscreenAdapter 接口和 FullscreenPhase/FullscreenMode 类型
    - flutter analyze 零 error
    - flutter test 全通过（包括 regression/ 和 integration/ 目录）
  artifacts:
    - test/regression/smoke_suite_test.dart（更新后）
    - test/regression/high_risk_suite_test.dart（更新后）
    - test/integration/fullscreen_e2e_test.dart（更新后）
  key_links:
    - 所有测试通过 adapter.isFullscreen.value 查询状态
    - 所有测试通过 driver.fullscreenState 控制行为
    - regression 测试覆盖 8+6 项关键场景
---

<objective>
更新 3 个引用已删除 model 类型的测试文件，使它们适配 DesktopFullscreenAdapter 的新 API（ValueNotifier<bool> 替代 FullscreenSnapshot）。

Purpose: 08-01 删除了 model 文件后，这些测试会因 import 错误无法编译。此 plan 完成 Phase 8 的编译修复，确保 flutter analyze + flutter test 全通过。

Output: 3 个测试文件适配新 API，flutter analyze 零 error，flutter test 全通过。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-8/08-RESEARCH.md
@.planning/phases/08-delete-abstraction/08-01-SUMMARY.md

@lib/kernel/bridge/desktop_fullscreen_adapter.dart
@lib/kernel/bridge/fullscreen_driver.dart
@lib/kernel/models/fullscreen_capability.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 更新 smoke_suite_test.dart — 适配新 API</name>
  <read_first>
    - test/regression/smoke_suite_test.dart（当前 291 行 — 理解所有测试用例）
    - lib/kernel/bridge/desktop_fullscreen_adapter.dart（08-01 完成后 — 确认新 API：isFullscreen getter）
    - lib/kernel/bridge/fullscreen_driver.dart（理解 driver 接口）
  </read_first>
  <files>test/regression/smoke_suite_test.dart</files>
  <behavior>
    - 8 个测试场景全部保留（FS-REG-001 ~ FS-REG-008）
    - adapter.snapshot().value.isFullscreen 替换为 adapter.isFullscreen.value
    - adapter.snapshot().value.phase 替换为直接移除（不再有 phase 概念）
    - FullscreenPhase.stable 断言移除
    - PlatformFailure 类型断言替换为检查 adapter.isFullscreen.value == false
    - import fullscreen_error.dart 和 fullscreen_snapshot.dart 移除
  </behavior>
  <action>
更新 `test/regression/smoke_suite_test.dart`：

**删除 import（2 行）：**
- `import 'package:simple_player_flutter/kernel/models/fullscreen_error.dart';`
- `import 'package:simple_player_flutter/kernel/models/fullscreen_snapshot.dart';`

**替换所有状态查询（全文替换）：**
- `adapter.snapshot().value.isFullscreen` → `adapter.isFullscreen.value`
- `adapter.snapshot().value.phase` → 删除该断言行（不再有 phase）
- `adapter.snapshot().value.lastError` → 删除该断言行（不再有 lastError）
- `adapter.snapshot().value.hasError` → 删除该断言行（不再有 hasError）

**FS-REG-001 ~ FS-REG-007 断言调整：**
每个测试中的 `expect(adapter.snapshot().value.isFullscreen, ...)` 改为 `expect(adapter.isFullscreen.value, ...)`
每个测试中的 `expect(adapter.snapshot().value.phase, FullscreenPhase.stable)` 删除该行

**FS-REG-008 特殊处理（错误状态测试）：**
原始测试断言 `FullscreenPhase.error` 和 `PlatformFailure` 类型。
新行为：enter 失败时 `adapter.isFullscreen.value` 应为 false（不设 true 后回滚）。
断言改为：
- `expect(adapter.isFullscreen.value, isFalse);` — enter 失败后不处于全屏
- 删除 `expect(adapter.snapshot().value.phase, FullscreenPhase.error);`
- 删除 `expect(adapter.snapshot().value.lastError, isA<PlatformFailure>());`

**FS-REG-005 特殊处理（连续切换）：**
原始断言 `adapter.snapshot().value.phase, FullscreenPhase.stable` — 删除该行。
  </action>
  <acceptance_criteria>
    - Source assertion: `smoke_suite_test.dart` 不包含 `import.*fullscreen_error` 或 `import.*fullscreen_snapshot`
    - Source assertion: `smoke_suite_test.dart` 不包含 `FullscreenPhase`、`FullscreenSnapshot`、`PlatformFailure` 中的任何一个
    - Source assertion: `smoke_suite_test.dart` 包含 `adapter.isFullscreen.value` 出现至少 8 次
    - Source assertion: `smoke_suite_test.dart` 包含 8 个 test（FS-REG-001 ~ FS-REG-008）
    - Test command: `flutter test test/regression/smoke_suite_test.dart` 全部通过
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/regression/smoke_suite_test.dart 2>&1</automated>
  </verify>
  <done>smoke_suite_test.dart 适配新 API，8 个测试全部通过，不再引用已删除的 model 类型。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 high_risk_suite_test.dart + fullscreen_e2e_test.dart</name>
  <read_first>
    - test/regression/high_risk_suite_test.dart（当前 272 行 — 理解所有测试用例）
    - test/integration/fullscreen_e2e_test.dart（当前 133 行 — 理解 E2E 测试）
    - lib/kernel/bridge/desktop_fullscreen_adapter.dart（08-01 完成后 — 确认新 API）
    - lib/kernel/bridge/fullscreen_driver.dart（driver 接口）
  </read_first>
  <files>
    test/regression/high_risk_suite_test.dart,
    test/integration/fullscreen_e2e_test.dart
  </files>
  <behavior>
    - high_risk_suite 6 个测试全部保留
    - fullscreen_e2e 5 个测试全部保留
    - 所有 adapter.snapshot().value.* 替换为 adapter.isFullscreen.value
    - 所有 FullscreenPhase/FullscreenSnapshot 引用移除
    - fullscreen_e2e 不再引用 FullscreenAdapter 接口类型
  </behavior>
  <action>
**Part A: 更新 test/regression/high_risk_suite_test.dart**

1. 删除 import：
   - `import 'package:simple_player_flutter/kernel/models/fullscreen_snapshot.dart';`

2. 全文替换：
   - `adapter.snapshot().value.isFullscreen` → `adapter.isFullscreen.value`
   - `adapter1.snapshot().value.isFullscreen` → `adapter1.isFullscreen.value`
   - `adapter2.snapshot().value.isFullscreen` → `adapter2.isFullscreen.value`

3. 删除所有 `FullscreenPhase.stable`、`FullscreenPhase.error` 断言行

4. 删除所有 `adapter.snapshot().value.hasError` 断言行

**Part B: 更新 test/integration/fullscreen_e2e_test.dart**

1. 删除 import：
   - `import 'package:simple_player_flutter/kernel/bridge/fullscreen_adapter.dart';`
   - `import 'package:simple_player_flutter/kernel/models/fullscreen_snapshot.dart';`

2. 类型声明变更：
   - `late FullscreenAdapter adapter;` → `late DesktopFullscreenAdapter adapter;`

3. 全文替换：
   - `adapter.snapshot().value.isFullscreen` → `adapter.isFullscreen.value`
   - `adapter.snapshot().value.phase` → 删除该断言行
   - `adapter.snapshot().value.effectiveMode` → 删除该断言行
   - `adapter.snapshot().value.hasError` → 删除该断言行

4. E2E 测试断言调整：
   - 删除所有 `expect(snapshot.phase, FullscreenPhase.stable);`
   - 删除所有 `expect(snapshot.effectiveMode, FullscreenMode.borderless);`
   - 删除所有 `expect(snapshot.effectiveMode, FullscreenMode.windowed);`
   - 保留 `expect(snapshot.isFullscreen, true/false);` → `expect(adapter.isFullscreen.value, true/false);`
  </action>
  <acceptance_criteria>
    - Source assertion: `high_risk_suite_test.dart` 不包含 `FullscreenPhase`、`FullscreenSnapshot`、`import.*fullscreen_snapshot`
    - Source assertion: `high_risk_suite_test.dart` 包含 `adapter.isFullscreen.value`（或 `adapter1.isFullscreen.value`）
    - Source assertion: `fullscreen_e2e_test.dart` 不包含 `FullscreenAdapter`、`FullscreenPhase`、`FullscreenMode`、`FullscreenSnapshot`
    - Source assertion: `fullscreen_e2e_test.dart` 包含 `DesktopFullscreenAdapter adapter`
    - Test command: `flutter test test/regression/high_risk_suite_test.dart` 全部通过
    - Test command: `flutter test test/integration/fullscreen_e2e_test.dart`（e2e 需桌面环境，可跳过）
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/regression/high_risk_suite_test.dart 2>&1</automated>
  </verify>
  <done>high_risk_suite 和 fullscreen_e2e 适配新 API，不再引用已删除的 model 类型，回归测试全通过。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 8 最终验证 — 全量分析 + 测试 + 残留检查</name>
  <files>lib/kernel/bridge/desktop_fullscreen_adapter.dart, lib/kernel/bridge/window_service.dart, lib/main.dart, test/regression/smoke_suite_test.dart, test/regression/high_risk_suite_test.dart, test/integration/fullscreen_e2e_test.dart</files>
  <read_first>
    - lib/kernel/bridge/desktop_fullscreen_adapter.dart
    - lib/kernel/bridge/window_service.dart
    - lib/main.dart
  </read_first>
  <what-built>
    - 9 个文件已删除（6 源码 + 3 测试）
    - 6 个文件已更新（3 消费者 + 3 测试）
    - DesktopFullscreenAdapter 使用 ValueNotifier&lt;bool&gt; 替代 FullscreenSnapshot
  </what-built>
  <how-to-verify>
    1. 运行 `flutter analyze` — 确认零 error
    2. 运行 `flutter test` — 确认全通过
    3. 残留 import 检查：`grep -r "fullscreen_adapter\|fullscreen_command_queue\|fullscreen_snapshot\|fullscreen_error\|fullscreen_event\|fullscreen_request" lib/ test/` 应返回空（除注释和字符串常量外）
    4. 文件删除确认：`ls lib/kernel/models/fullscreen_*.dart lib/kernel/bridge/fullscreen_adapter.dart lib/kernel/bridge/fullscreen_command_queue.dart` 应全部报 "No such file"
    5. 行数确认：`wc -l lib/kernel/bridge/desktop_fullscreen_adapter.dart` 应小于 400（从 520 行缩减）
  </how-to-verify>
  <action>
Phase 8 最终验证步骤：
1. 运行 `flutter analyze` 确认零 error
2. 运行 `flutter test` 确认全通过
3. 检查残留 import
4. 确认文件删除
5. 确认行数缩减
  </action>
  <verify>
    <automated>flutter analyze 2>&1 | grep -c "error" || true</automated>
  </verify>
  <resume-signal>输入 "approved" 或描述发现的问题</resume-signal>
  <done>Phase 8 全部完成：9 个文件删除，6 个文件更新，flutter analyze 零 error，flutter test 全通过。</done>
</task>

</tasks>

<artifacts-this-phase-produces>
- 更新后的 `test/regression/smoke_suite_test.dart`（适配新 API）
- 更新后的 `test/regression/high_risk_suite_test.dart`（适配新 API）
- 更新后的 `test/integration/fullscreen_e2e_test.dart`（适配新 API）
</artifacts-this-phase-produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 → 被测代码 | 测试通过 adapter.isFullscreen.value 查询状态，需确认新 API 行为一致 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Repudiation | regression tests | medium | mitigate | 更新断言确保覆盖率不退化 |
| T-08-05 | Denial of Service | flutter test | low | accept | 删除的测试在 Phase 9 重写 |
| T-08-SC | Tampering | test assertions | medium | mitigate | 更新后逐个验证测试通过 |
</threat_model>

<verification>
1. `flutter analyze` 零 error
2. `flutter test` 全通过
3. `grep -r "FullscreenPhase\|FullscreenSnapshot\|FullscreenError\|FullscreenEvent\|FullscreenRequest\|FullscreenMode" lib/ test/` 返回空
4. 所有测试文件不再 import 已删除的 model 文件
</verification>

<success_criteria>
- 3 个测试文件适配新 API
- flutter analyze 零 error
- flutter test 全通过
- 无残留的已删除类型引用
</success_criteria>

<output>
Create `.planning/phases/08-delete-abstraction/08-02-SUMMARY.md` when done
</output>
