---
phase: 08-delete-abstraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kernel/bridge/desktop_fullscreen_adapter.dart
  - lib/kernel/bridge/window_service.dart
  - lib/main.dart
  - lib/kernel/models/fullscreen_request.dart
  - lib/kernel/models/fullscreen_event.dart
  - lib/kernel/models/fullscreen_error.dart
  - lib/kernel/models/fullscreen_snapshot.dart
  - test/kernel/bridge/fullscreen_adapter_test.dart
  - test/kernel/bridge/fullscreen_command_queue_test.dart
  - test/kernel/bridge/desktop_fullscreen_adapter_test.dart
autonomous: false
requirements:
  - SIMPLIFY-01
  - SIMPLIFY-02
  - SIMPLIFY-03

must_haves:
  truths:
    - DesktopFullscreenAdapter 不再 import 任何被删除的 model 类
    - WindowService 不再 import fullscreen_event 或 fullscreen_snapshot
    - main.dart 不再 import fullscreen_adapter.dart 抽象接口
    - 4 个 model 文件（request/event/error/snapshot）已从 lib/ 删除
    - 3 个测试文件（adapter/command_queue/desktop_adapter）已从 test/ 删除
    - flutter analyze 零 error（至少针对这些文件的 import 错误）
  artifacts:
    - lib/kernel/bridge/desktop_fullscreen_adapter.dart（简化后）
    - lib/kernel/bridge/window_service.dart（简化后）
    - lib/main.dart（简化后）
  key_links:
    - DesktopFullscreenAdapter 不再实现 FullscreenAdapter 接口
    - WindowService._onFullscreenEvent 替换为 ValueNotifier<bool> 监听
---

<objective>
删除 4 个 model 文件（fullscreen_request/event/error/snapshot）和 3 个测试文件，同时更新 3 个消费者（desktop_fullscreen_adapter、window_service、main）使它们不再依赖被删除的类型。

Purpose: 这是 v3 简化的第一步 — 移除过度工程化的状态模型（FullscreenSnapshot/FullscreenPhase/FullscreenMode/FullscreenError/FullscreenEvent/FullscreenRequest），将状态表示从复杂 sealed class 简化为 ValueNotifier<bool>。

Output: 消费者文件编译通过（无 import 错误），4 个 model + 3 个 test 文件已删除。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-8/08-RESEARCH.md

@lib/kernel/bridge/desktop_fullscreen_adapter.dart
@lib/kernel/bridge/window_service.dart
@lib/main.dart
@lib/kernel/bridge/fullscreen_adapter.dart
@lib/kernel/bridge/fullscreen_command_queue.dart
@lib/kernel/models/fullscreen_snapshot.dart
@lib/kernel/models/fullscreen_error.dart
@lib/kernel/models/fullscreen_event.dart
@lib/kernel/models/fullscreen_request.dart
@lib/kernel/bridge/fullscreen_driver.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: 简化 DesktopFullscreenAdapter — 移除 CommandQueue + model 依赖</name>
  <read_first>
    - lib/kernel/bridge/desktop_fullscreen_adapter.dart（当前 521 行完整实现）
    - lib/kernel/bridge/fullscreen_command_queue.dart（删除目标 — 理解队列接口）
    - lib/kernel/models/fullscreen_snapshot.dart（删除目标 — 理解 FullscreenSnapshot/FullscreenPhase/FullscreenMode）
    - lib/kernel/models/fullscreen_error.dart（删除目标 — 理解 FullscreenError）
    - lib/kernel/models/fullscreen_event.dart（删除目标 — 理解 FullscreenEvent）
    - lib/kernel/models/fullscreen_request.dart（删除目标 — 理解 FullscreenRequest）
    - lib/kernel/bridge/fullscreen_driver.dart（保留 — 理解 driver 接口）
  </read_first>
  <files>lib/kernel/bridge/desktop_fullscreen_adapter.dart</files>
  <action>
重写 `desktop_fullscreen_adapter.dart`，移除对 6 个被删除文件的依赖：

**删除的 import（6 行）：**
- `import '../models/fullscreen_error.dart';`
- `import '../models/fullscreen_event.dart';`
- `import '../models/fullscreen_request.dart';`
- `import '../models/fullscreen_snapshot.dart';`
- `import 'fullscreen_adapter.dart';`
- `import 'fullscreen_command_queue.dart';`

**类声明变更：**
- 移除 `implements FullscreenAdapter`，改为 `class DesktopFullscreenAdapter`
- 移除所有 `@override` 注解（不再实现接口）

**状态管理简化（核心变更）：**
- 删除 `final FullscreenCommandQueue _queue = FullscreenCommandQueue();`
- 删除 `final Map<int, ValueNotifier<FullscreenSnapshot>> _snapshots = {};`
- 删除 `final StreamController<FullscreenEvent> _events = StreamController<FullscreenEvent>.broadcast();`
- 新增 `final ValueNotifier<bool> _isFullscreen = ValueNotifier(false);`
- 新增 `ValueNotifier<bool> get isFullscreen => _isFullscreen;`
- 新增 `bool get _currentFullscreen => _isFullscreen.value;`

**setFullscreen/toggle 简化：**
- 删除 FullscreenRequest 构造，直接调用 `_handleEnter`/`_handleLeave`
- 删除 queue.enqueue 调用，改为直接 `await _handleEnter()`/`await _handleLeave()`
- toggle 方法：`if (_currentFullscreen) await _handleLeave() else await _handleEnter()`

**_handleEnter 简化：**
- 删除 FullscreenSnapshot.copyWith / FullscreenPhase / FullscreenMode 引用
- 删除 `_events.add(FullscreenEvent.enterRequested(...))` 等事件广播
- 保留：三级确认链（_waitForConfirmation）、快速路径（supportsFastPath）、minimized 检查、恢复快照捕获
- 错误处理：移除 `FullscreenError.platformFailure`，改为 `debugPrint('[DesktopFullscreenAdapter] enter failed: $e');`
- 成功时：`_isFullscreen.value = true;`
- 失败时：`_isFullscreen.value = false;`（不设 true 后回滚，直接保持 false）

**_handleLeave 简化：**
- 同理移除事件广播和 FullscreenSnapshot
- 成功时：`_isFullscreen.value = false;`
- 保留恢复策略（_restoreFromSnapshot）

**onNativeFullScreenChanged 简化：**
- 保留 Completer 确认逻辑
- 移除 FullscreenEvent.forcedChange 广播
- 移除 FullscreenSnapshot.copyWith
- 直接 `_isFullscreen.value = isFullscreen;`

**dispose 简化：**
- 删除 `_queue.dispose()`
- 删除 `_events.close()`
- 删除 `_snapshots` 遍历 dispose
- 新增 `_isFullscreen.dispose()`

**_snapshotFor / _applyDesync 删除：**
- `_snapshotFor` 不再需要
- `_applyDesync` 简化：不再操作 FullscreenSnapshot，改为 `debugPrint` + `_isFullscreen.value = await _driver.queryFullscreen();`

**保留不变：**
- `_RestoreSnapshot` 私有类（不依赖被删除的 model）
- `_PendingConfirmation` 私有类
- `_captureRestoreSnapshot` / `_restoreFromSnapshot` 方法
- `_waitForConfirmation` / `_registerConfirmation` 方法
- `_driver` 引用和所有 driver 调用
  </action>
  <acceptance_criteria>
    - Source assertion: `desktop_fullscreen_adapter.dart` 不包含 `import.*fullscreen_(adapter|command_queue|snapshot|error|event|request)` 中的任何一个
    - Source assertion: `desktop_fullscreen_adapter.dart` 包含 `class DesktopFullscreenAdapter` 且不包含 `implements FullscreenAdapter`
    - Source assertion: `desktop_fullscreen_adapter.dart` 包含 `ValueNotifier<bool> _isFullscreen` 和 `ValueNotifier<bool> get isFullscreen`
    - Source assertion: `desktop_fullscreen_adapter.dart` 不包含 `FullscreenSnapshot`、`FullscreenPhase`、`FullscreenMode`、`FullscreenError`、`FullscreenEvent`、`FullscreenRequest` 中的任何一个引用
    - Source assertion: `desktop_fullscreen_adapter.dart` 包含 `_driver.enterFullscreen` 和 `_driver.leaveFullscreen` 调用
    - Source assertion: `desktop_fullscreen_adapter.dart` 包含 `_restoreFromSnapshot` 和 `_captureRestoreSnapshot` 方法
    - Source assertion: `desktop_fullscreen_adapter.dart` 包含 `_PendingConfirmation` 和 `_RestoreSnapshot` 私有类
    - Test command: `flutter analyze lib/kernel/bridge/desktop_fullscreen_adapter.dart` 零 error（可能有其他文件的 error，只看此文件）
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/kernel/bridge/desktop_fullscreen_adapter.dart 2>&1 | grep -c "error" || true</automated>
  </verify>
  <done>DesktopFullscreenAdapter 不再依赖任何 model 文件和 CommandQueue，内部状态从 FullscreenSnapshot 简化为 ValueNotifier<bool>，保留三级确认链和恢复策略。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 WindowService + main.dart 并删除 model/test 文件</name>
  <read_first>
    - lib/kernel/bridge/window_service.dart（当前 381 行 — 理解 _onFullscreenEvent 和 _fullscreenAdapter 使用方式）
    - lib/main.dart（当前 83 行 — 理解 FullscreenAdapter 类型声明）
    - lib/kernel/bridge/desktop_fullscreen_adapter.dart（Task 1 完成后 — 确认新 API 有 isFullscreen getter）
    - lib/kernel/bridge/fullscreen_adapter.dart（删除目标 — 理解要移除的接口）
  </read_first>
  <files>
    lib/kernel/bridge/window_service.dart,
    lib/main.dart,
    lib/kernel/bridge/fullscreen_adapter.dart,
    lib/kernel/bridge/fullscreen_command_queue.dart,
    lib/kernel/models/fullscreen_request.dart,
    lib/kernel/models/fullscreen_event.dart,
    lib/kernel/models/fullscreen_error.dart,
    lib/kernel/models/fullscreen_snapshot.dart,
    test/kernel/bridge/fullscreen_adapter_test.dart,
    test/kernel/bridge/fullscreen_command_queue_test.dart,
    test/kernel/bridge/desktop_fullscreen_adapter_test.dart
  </files>
  <behavior>
    - WindowService 构造函数接受 DesktopFullscreenAdapter 而非 FullscreenAdapter
    - WindowService 不再 import fullscreen_event.dart 或 fullscreen_snapshot.dart
    - WindowService._onFullscreenEvent 替换为 _onFullscreenChanged 监听 ValueNotifier<bool>
    - main.dart 不再 import fullscreen_adapter.dart
    - main.dart 使用 DesktopFullscreenAdapter? 而非 FullscreenAdapter?
    - 6 个被删除文件不再存在于 lib/ 和 test/
  </behavior>
  <action>
**Part A: 更新 window_service.dart（per D-01 ~ D-03）**

1. 删除 import：
   - `import '../models/fullscreen_event.dart';`
   - `import '../models/fullscreen_snapshot.dart';`
   - `import 'fullscreen_adapter.dart';`

2. 更新 import：
   - 添加 `import 'desktop_fullscreen_adapter.dart';`

3. 构造函数参数变更：
   - `FullscreenAdapter? fullscreenAdapter` → `DesktopFullscreenAdapter? fullscreenAdapter`
   - 删除 `_fullscreenAdapter?.events.listen(_onFullscreenEvent);` 行

4. 在构造函数中添加 bool 监听：
   ```dart
   _fullscreenAdapter?.isFullscreen.addListener(_onFullscreenChanged);
   ```

5. 替换 `_onFullscreenEvent` 方法为 `_onFullscreenChanged`：
   - 删除整个 `_onFullscreenEvent(FullscreenEvent event)` 方法（约 30 行）
   - 新增：
     ```dart
     void _onFullscreenChanged() {
       if (_disposed) return;
       _updateOnUIThread(() {
         _state.mode.value = _isFullscreen.value
             ? WindowMode.fullscreen
             : WindowMode.windowed;
       });
     }
     ```
   - `_isFullscreen` 通过 `_fullscreenAdapter!.isFullscreen` 访问

6. dispose 中：
   - 将 `_fullscreenAdapter?.dispose();` 之前添加 `_fullscreenAdapter?.isFullscreen.removeListener(_onFullscreenChanged);`

**Part B: 更新 main.dart（per D-01）**

1. 删除 import：
   - `import 'kernel/bridge/fullscreen_adapter.dart';`

2. 类型声明变更：
   - `FullscreenAdapter? fullscreenAdapter;` → `DesktopFullscreenAdapter? fullscreenAdapter;`

**Part C: 删除 4 个 model 文件**
- 删除 `lib/kernel/models/fullscreen_request.dart`
- 删除 `lib/kernel/models/fullscreen_event.dart`
- 删除 `lib/kernel/models/fullscreen_error.dart`
- 删除 `lib/kernel/models/fullscreen_snapshot.dart`

**Part D: 删除 adapter/queue 抽象文件**
- 删除 `lib/kernel/bridge/fullscreen_adapter.dart`
- 删除 `lib/kernel/bridge/fullscreen_command_queue.dart`

**Part E: 删除 3 个测试文件**
- 删除 `test/kernel/bridge/fullscreen_adapter_test.dart`
- 删除 `test/kernel/bridge/fullscreen_command_queue_test.dart`
- 删除 `test/kernel/bridge/desktop_fullscreen_adapter_test.dart`
  </action>
  <acceptance_criteria>
    - Source assertion: `window_service.dart` 不包含 `fullscreen_event`、`fullscreen_snapshot`、`fullscreen_adapter.dart` 的 import
    - Source assertion: `window_service.dart` 包含 `import 'desktop_fullscreen_adapter.dart';`
    - Source assertion: `window_service.dart` 不包含 `_onFullscreenEvent` 方法
    - Source assertion: `window_service.dart` 包含 `_onFullscreenChanged` 方法
    - Source assertion: `main.dart` 不包含 `import.*fullscreen_adapter.dart`（抽象接口）
    - Source assertion: `main.dart` 包含 `DesktopFullscreenAdapter? fullscreenAdapter`
    - Source assertion: `lib/kernel/bridge/fullscreen_adapter.dart` 不存在（`ls` 返回非零）
    - Source assertion: `lib/kernel/bridge/fullscreen_command_queue.dart` 不存在
    - Source assertion: `lib/kernel/models/fullscreen_request.dart` 不存在
    - Source assertion: `lib/kernel/models/fullscreen_event.dart` 不存在
    - Source assertion: `lib/kernel/models/fullscreen_error.dart` 不存在
    - Source assertion: `lib/kernel/models/fullscreen_snapshot.dart` 不存在
    - Source assertion: `test/kernel/bridge/fullscreen_adapter_test.dart` 不存在
    - Source assertion: `test/kernel/bridge/fullscreen_command_queue_test.dart` 不存在
    - Source assertion: `test/kernel/bridge/desktop_fullscreen_adapter_test.dart` 不存在
    - Test command: `flutter analyze` 中不出现上述 6 个已删除文件的 import 错误
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/kernel/bridge/window_service.dart lib/main.dart 2>&1 | grep -c "error" || true</automated>
  </verify>
  <done>WindowService 和 main.dart 更新完毕，6 个源文件和 3 个测试文件已删除，flutter analyze 不报 import 错误。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工验证 — 残留 import 检查 + 编译测试</name>
  <files>lib/kernel/bridge/desktop_fullscreen_adapter.dart, lib/kernel/bridge/window_service.dart, lib/main.dart</files>
  <read_first>
    - lib/kernel/bridge/desktop_fullscreen_adapter.dart
    - lib/kernel/bridge/window_service.dart
    - lib/main.dart
  </read_first>
  <what-built>
    - 3 个消费者文件已更新，移除了所有对已删除 model/adapter/queue 的引用
    - 9 个文件已删除（6 源码 + 3 测试）
  </what-built>
  <how-to-verify>
    1. 运行 `flutter analyze` — 确认零 error（可能有 warning 但无 import 相关 error）
    2. 运行 `flutter test` — 确认剩余测试全通过（被删除的测试不再运行）
    3. 检查残留：`grep -r "fullscreen_adapter\|fullscreen_command_queue\|fullscreen_snapshot\|fullscreen_error\|fullscreen_event\|fullscreen_request" lib/` 应返回空（除 window_mode.dart 等不相关文件）
    4. 确认 `DesktopFullscreenAdapter` 可独立实例化（无接口依赖）
  </how-to-verify>
  <action>
人工验证步骤：
1. 运行 `flutter analyze` 确认零 error
2. 运行 `flutter test` 确认全通过
3. 检查残留 import
4. 确认新 API 可用
  </action>
  <verify>
    <automated>flutter analyze 2>&1 | grep -c "error" || true</automated>
  </verify>
  <resume-signal>输入 "approved" 或描述发现的问题</resume-signal>
  <done>人工确认 flutter analyze 零 error，flutter test 全通过，无残留 import。</done>
</task>

</tasks>

<artifacts-this-phase-produces>
- 简化后的 `desktop_fullscreen_adapter.dart`（~350 行，从 520 行缩减）
- 简化后的 `window_service.dart`（~350 行，从 381 行缩减，移除事件 switch）
- 简化后的 `main.dart`（~80 行，仅类型声明变更）
- 已删除文件清单：6 个源文件 + 3 个测试文件 = 9 个文件
</artifacts-this-phase-produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 删除文件 → 消费者 | 删除 model 文件后，消费者必须更新 import，否则编译失败 |
| DesktopFullscreenAdapter → WindowService | StateNotifier<bool> 新接口替代 Stream<FullscreenEvent> |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | desktop_fullscreen_adapter.dart | high | mitigate | 删除 model 引用后必须同步更新所有消费者，否则 flutter analyze 报错 |
| T-08-02 | Repudiation | window_service.dart | medium | mitigate | _onFullscreenChanged 替代 _onFullscreenEvent，保留调试日志 |
| T-08-03 | Information Disclosure | 删除的测试文件 | low | accept | 测试文件随源文件删除，Phase 9 重写 |
| T-08-SC | Tampering | 删除文件操作 | high | mitigate | 手动验证 grep 无残留 import |
</threat_model>

<verification>
1. `flutter analyze` 零 error
2. `flutter test` 全通过（被删除的测试不再运行）
3. `grep -r "fullscreen_adapter\|fullscreen_command_queue\|fullscreen_snapshot\|fullscreen_error\|fullscreen_event\|fullscreen_request" lib/` 返回空
4. `ls lib/kernel/models/fullscreen_*.dart` 返回 "No such file"
5. `ls lib/kernel/bridge/fullscreen_adapter.dart lib/kernel/bridge/fullscreen_command_queue.dart` 返回 "No such file"
</verification>

<success_criteria>
- 6 个源文件已删除
- 3 个测试文件已删除
- 3 个消费者文件更新完毕，无残留 import
- flutter analyze 零 import error
- flutter test 全通过
</success_criteria>

<output>
Create `.planning/phases/08-delete-abstraction/08-01-SUMMARY.md` when done
</output>
