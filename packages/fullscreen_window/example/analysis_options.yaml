# Example app analysis options — kept minimal since dependencies
# are resolved separately from the root project.
analyzer:
  errors:
    missing_required_param: error
    missing_return: error

linter:
  rules:
    prefer_const_constructors: true
    avoid_print: true
