# 🔄 继续工作 — 设计语言 + 播放器 UI 优化

## 上下文

会话太长需要 compact，但工作还没完成。
**下次会话用 `/gsd-resume-work` 恢复时读这个文件。**

## 当前任务

**设计语言 + 播放器 UI 优化**：
- EdgeGlow/TransmittedLight/HoverGlow 视觉效果实现
- 界面暗化、控制栏优化、交互改进
- 视频显示策略（contain + 窗口自适应比例）

## 已完成的工作

### 1. 设计效果组件
- [x] `edge_glow.dart` — EdgeGlow 3 变体（gradient/omni/pulse）
- [x] `transmitted_light.dart` — TransmittedLight 2 类型（bottom/center）
- [x] `hover_glow.dart` — HoverGlow 鼠标跟随效果（已创建但已从 UI 移除）

### 2. tokens.dart 暗化
```dart
// 所有背景色加深 ~20%
static const bgDeep = Color(0xFF060810);      // was 0xFF0A0C14
static const bgBase = Color(0xFF0C0F18);      // was 0xFF10131E
static const bgPanel = Color(0xFF111520);     // was 0xFF161A2A
static const bgElevated = Color(0xFF161A28);  // was 0xFF1C2038
static const bgHover = Color(0xFF1E2232);     // was 0xFF2A2A3A
static const bgGlass = Color(0x8C0C0F18);    // was 0x8C121628
```

### 3. 控制栏优化
- [x] 三行等高（Expanded 1:1:1）
- [x] 按钮行内缩防边缘重叠（Padding horizontal: 4）
- [x] 进度行居中（padding 2→1）

### 4. 交互修复
- [x] 进度条滑块固定显示播放进度（不跟随鼠标）
- [x] 音量滚轮限定在滑块上（不在全视频范围）

### 5. 视频显示
- [x] VideoSurface BoxFit.contain（不裁剪）
- [x] 视频加载后窗口自适应比例
- [x] **WM_SIZING 窗口拖拽时锁定视频宽高比**（mpv 风格）
  - 实现: `AspectRatioService` 使用 `windowManager.setAspectRatio()`
  - 视频加载时自动锁定比例，拖拽时 Win32 自动约束窗口尺寸

### 6. 背景模糊问题
- [x] BackdropFilter 导致全画面模糊 — 已移除
- [x] TransmittedLight 改用纯 RadialGradient 叠加

## 下次继续

### 立即要做
- 运行 `flutter analyze` 和 `flutter test` 确认无误
- 测试 WM_SIZING 效果（拖拽窗口时比例锁定）
- git commit

### 新增文件
- `lib/kernel/window/aspect_ratio_service.dart` — 窗口宽高比约束服务

## 关键决策

- **不用 BackdropFilter** — 会模糊整个视频画面
- **不用 HoverGlow** — 用户要求去掉
- **contain + 窗口自适应** — 视频不裁剪，窗口匹配视频比例
- **WM_SIZING 比例锁定** — 参考 mpv，使用 windowManager.setAspectRatio()

## 反模式

- ❌ BackdropFilter 放在视频层上方 — 会模糊所有像素
- ❌ 全局滚轮控制音量 — 应限定在滑块上
- ❌ 进度条滑块跟随鼠标 — 应固定在播放位置
- ✅ TransmittedLight 用纯 RadialGradient 叠加
- ✅ AspectRatioService 使用 windowManager.setAspectRatio()

## 研究成果

**mpv 视频显示策略**：
- 3 层方案：WM_SIZING 锁定比例 + keepaspect=contain + autofit=自动调整窗口
- WM_SIZING 在拖拽时实时调整窗口尺寸保持比例
- `windowManager.setAspectRatio()` 等效于 mpv 的 keepaspect-window

**VLC 视频显示策略**：
- `vout_display_PlacePicture()` 计算缩放后尺寸
- 本质上是 contain 模式

**我们的差距**：
- 已实现：contain + 窗口自适应比例
- 已实现：WM_SIZING 比例锁定（通过 AspectRatioService）
