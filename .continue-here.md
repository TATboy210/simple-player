---
context: control-bar-bugfixes-and-theme
status: in_progress
last_updated: 2026-07-01
---

## 当前状态

**Branch:** `feat/v1.8-stability-polish-plan-02-02`
**本次会话主题:** 控制栏 bug 修复 + 主题调整 + 参数导出
**测试:** 869/869 通过

---

## 已完成工作 (本次会话)

### 1. 控制栏 Bug 修复

| 优先级 | 文件 | 修改 | 状态 |
|--------|------|------|------|
| P0 | `lib/ui/player/volume_controls.dart` | 添加 `_onVolumeChanged` listener — 静音拖滑块自动同步 savedVolume + 自动取消静音 | ✅ |
| P1 | `lib/ui/player/auto_hide_controller.dart` | 无需修改（removeStatusListener 已存在于 dispose） | ⏭️ |
| P2 | `lib/ui/player/speed_button.dart` | `lastIndexWhere` → `indexWhere` — 非标准值 snap 到正确挡位 | ✅ |

**计划文档:** `.planning/CONTROL_BAR_FIXES.md`（已提交 aa7a141）

### 2. Golden Test 修复

- 3 个 golden test 因平台渲染差异失败（1.61% 像素超过容差）
- 用 `--update-goldens` 重新生成当前环境基准图
- 文件：`test/golden/goldens/control_bar_*.png`

### 3. 控制栏主题调整

**风格方向:** 用户选择"更通透的毛玻璃"（macOS 风格）

| Token | 旧值 | 新值 | 效果 |
|-------|------|------|------|
| `bgGlass` | `0x8C` (55%) | `0x52` (32%) | 全局毛玻璃更通透 |
| `controlBarBg` | `0x80` (50%) | `0x4D` (30%) | 控制栏背景更透明 |
| `glassBlur` | `12.0` | `18.0` | 更强磨砂模糊 |
| `controlBarBorder` | `0x14` (8%) | `0x1F` (12%) | 边框更亮 |
| `controlBarShadowBlack` | `0x1A` (10%) | `0x20` (12%) | 阴影微增 |

### 4. 设计参数导出

**文件:** `design_tokens.json`（新建，项目根目录）
- 20 个分类、100+ 个参数
- 包含所有 Tokens 常量 + Material Design 3 可扩展参数
- 用途：用户用专业设计软件调整参数后，对照修改 tokens.dart

---

## 未提交的文件

```
M  lib/ui/player/volume_controls.dart     — P0 bug fix
M  lib/ui/player/speed_button.dart        — P2 bug fix
M  lib/ui/theme/tokens.dart               — 主题调整
M  test/golden/goldens/control_bar_*.png  — golden 重新生成
?? design_tokens.json                      — 参数导出文件
```

---

## 下一步

1. **提交当前改动** — 3 个代码修改 + golden 更新 + 参数导出
2. **用户调参** — 用户用设计软件查看 design_tokens.json，调整后告知新值
3. **应用新主题** — 根据用户调整的值修改 tokens.dart

---

## 关键决策

- **D-01:** VolumeControls 用 listener 模式同步 savedVolume，不引入共享状态管理
- **D-02:** SpeedButton 用 indexWhere（向上找）替代 lastIndexWhere（向下找），处理非标准值
- **D-03:** 毛玻璃风格方向：低 alpha (30%) + 高 blur (18px) = macOS 通透效果
- **D-04:** design_tokens.json 作为设计参数的"中间格式"，用户调完后对照修改 tokens.dart

---

## 相关文件

- `.planning/CONTROL_BAR_FIXES.md` — Bug 修复计划文档
- `lib/ui/theme/tokens.dart` — 设计令牌（唯一来源）
- `lib/kernel/services/theme_service.dart` — 主题服务（3 个 accent）
- `design_tokens.json` — 完整参数导出

---

## 恢复命令

```bash
/gsd-resume-work
```
