---
context: research
status: in_progress
last_updated: 2026-06-25
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| BackdropFilter on video | TransmittedLight's BackdropFilter blurred the entire video surface | blocking | Never use BackdropFilter in a layer above the video texture; use gradient-only overlays instead |
| Global scroll wheel on VideoSurface | PointerScrollEvent on VideoSurface captured all scroll events, preventing volume-slider-only control | blocking | Only attach scroll handlers to specific UI elements (volume slider), not the video surface |

<current_state>
设计语言效果 + 播放器 UI 优化进行中。已完成：tokens 更新、EdgeGlow/TransmittedLight/HoverGlow 创建、背景加深、控制栏三行等高、进度条 thumb 固定、音量滚轮限定、视频 contain 显示、窗口自适应比例。
待实现：mpv 风格 WM_SIZING 拖动边框锁定视频宽高比。
</current_state>

<completed_work>

**本次会话完成：**
1. **tokens.dart 更新** — bgDeep/bgBase/bgPanel/bgElevated/bgHover 整体加深约 20%
2. **EdgeGlow widget** — 3 变体（gradient/omni/pulse），已应用到 ControlBar
3. **TransmittedLight widget** — 底部透射光效（去掉 BackdropFilter，纯渐变）
4. **HoverGlow widget** — 鼠标跟踪光效（已移除，用户要求去掉）
5. **control_bar.dart** — 三行 Expanded 等高布局，按钮行 Padding(horizontal: 4) 防重叠
6. **progress_bar.dart** — Thumb 始终显示在 playedFraction，不跟随鼠标 hover
7. **video_surface.dart** — BoxFit.cover → contain，移除 ClipRect，移除全局滚轮音量控制
8. **player_screen.dart** — 监听 engine.aspectRatio，视频加载后自动调整窗口比例并居中
9. **controls_overlay.dart** — 添加 TransmittedLight 底部光效
10. **video_surface_test.dart** — 更新测试：滚轮不再调节音量
</completed_work>

<remaining_work>

- **WM_SIZING 比例锁定** — 拖动窗口边框时强制保持视频宽高比（mpv 风格）
  - 参考 `C:\Users\35490\Downloads\mpv-0.41.0\mpv-0.41.0\video\out\w32_common.c:1499`
  - 需要在 WindowBridge 的 WM_SIZING 处理中添加 aspect ratio 约束
</remaining_work>

<decisions_made>

- 选择方案 C（窗口自适应视频比例 + contain 兜底）而非纯 contain 或模糊背景填充
- 背景色加深约 20%（HSL 明度降低 5-8%），保持蓝色色调
- 移除 HoverGlow（用户明确要求去掉）
- 进度条 thumb 固定在播放位置（用户明确要求不跟随鼠标）
- 音量滚轮只在音量滑块上生效（用户明确要求）
</decisions_made>

<blockers>
无
</blockers>

## Required Reading (in order)
1. `C:\Users\35490\Downloads\mpv-0.41.0\mpv-0.41.0\video\out\w32_common.c:1499-1519` — WM_SIZING 比例锁定实现
2. `C:\Users\35490\Downloads\mpv-0.41.0\mpv-0.41.0\video\out\aspect.c` — aspect_calc_panscan 算法
3. `.planning/DESIGN_LANGUAGE_REPORT.md` — 设计语言完整分析

<context>
mpv/VLC 源码调研已完成。核心发现：mpv 用 WM_SIZING 强制锁定窗口宽高比，VLC 用 vout_display_PlacePicture 做 contain。我们缺的是 mpv 的第 1 层（拖动边框锁定比例）。下一步需要在 WindowBridge 的 Win32 消息处理中添加 aspect ratio 约束。
</context>

<next_action>
实现 WM_SIZING 比例锁定：在 WindowBridge 的 WM_SIZING 处理中，根据 engine.aspectRatio 强制调整窗口尺寸，参考 mpv w32_common.c:1499-1519 的实现。
</next_action>
