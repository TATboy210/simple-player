---
context: v1.6-control-bar-quality
status: planning_complete
last_updated: 2026-07-05
---

# Continue Here: v1.6 Control Bar Quality

**Paused:** 2026-07-05 15:05 UTC+8
**Context:** 76% — context exhaustion
**Milestone:** v1.6 Control Bar Quality & Test Coverage

---

## Position

- **Milestone:** v1.6 — 3 Phases (25-27), 0 started
- **Current Phase:** 25 — Performance Quick Wins (not started)
- **Status:** Planning complete, ready to execute Phase 25
- **Progress:** 0/3 plans, 0%

## What Was Done This Session

1. ✅ v1.5 收尾：STATE.md 更新为 complete，创建 `v1.5` tag，commit `b6bbe35`
2. ✅ 读取控制栏分析报告（~1660 行，覆盖代码/结构/功能/优化/拓展/接口 6 大维度）
3. ✅ 使用 Agent 分析报告并提出 v1.6+ 路线图（3 里程碑 11 Phases）
4. ✅ 用户决定 v1.6 只做 P0-P1 技术债务 + 测试基础
5. ✅ 创建 v1.6 规划文件：PROJECT.md、REQUIREMENTS.md、ROADMAP.md、STATE.md
6. ✅ Commit `a97aaad` — v1.6 planning files

## Key Findings (from analysis report)

### 毛玻璃突兀跳变问题（用户最关心）

**根因：** `control_bar.dart:212-222` — resize 期间 `resizingNotifier.value == true` 时完全跳过 BackdropFilter，只显示半透明背景；resize 结束瞬间突然恢复模糊。

```
正常:  [BackdropFilter(blur=10)] + [背景] + [按钮]
resize: [RepaintBoundary] + [背景(无模糊)] + [按钮]  ← 模糊消失
恢复:  [BackdropFilter(blur=10)] + [背景] + [按钮]  ← 模糊突然出现
```

**解决方案：** 用 Animation<double> 驱动 blur sigma 10→0（150ms 渐出）和 0→10（150ms 渐入），替代二元切换。

### 其他技术债务

| 项 | 位置 | 问题 | 优先级 |
|----|------|------|--------|
| decoration getter | control_bar.dart:29-77 | 每次 build() 创建 2 BoxDecoration + 8 BoxShadow | P0 |
| ImageFilter.blur | glass_container.dart:116 | 每次 _buildBlurContent() 重新创建 | P0 |
| 魔法数字 18 | controls_overlay.dart:98 | 硬编码手指抖动容差 | P0 |
| VolumeSlider debounce | volume_controls.dart:133 | 拖拽时 60+ OSD/秒 | P1 |

## Phase 25 Requirements (PERF-01~04)

1. **PERF-01:** Resize 毛玻璃渐变过渡（150ms 淡出/淡入）
2. **PERF-02:** `_decorationPlaying`/`_decorationIdle` 缓存
3. **PERF-03:** GlassContainer ImageFilter.blur 按 GlassTier 缓存
4. **PERF-04:** 18px 提取为命名常量

## Phase 26 Requirements (PERF-05)

- VolumeSlider 拖拽时 OSD 调用从 60+/秒降至 ≤10/秒

## Phase 27 Requirements (TEST-01~04)

- AutoHideController 单元测试
- ProgressBar Widget 测试
- VolumeButton Widget 测试
- SpeedButton Widget 测试

## Key Files

| 文件 | 行数 | 作用 |
|------|------|------|
| `lib/ui/player/control_bar.dart` | 474 | 控制栏主容器、3 行布局、毛玻璃背景 |
| `lib/ui/player/controls_overlay.dart` | 237 | 最外层遮罩、手势分发、自动隐藏调度 |
| `lib/ui/player/glass_container.dart` | 356 | 毛玻璃基础组件 |
| `lib/ui/player/auto_hide_controller.dart` | 172 | 自动隐藏状态机 |
| `lib/ui/player/progress_bar.dart` | 449 | 三层进度条 |
| `lib/ui/player/volume_controls.dart` | 146 | 音量控制 |
| `lib/ui/player/speed_button.dart` | 148 | 倍速控制 |
| `lib/ui/player/center_controls.dart` | 118 | 中心播放按钮组 |
| `.planning/reports/control_bar_analysis_report.md` | 1660 | 完整分析报告 |

## Resume Instructions

```
# 在新会话中：
1. 运行 /gsd-progress — 确认位置在 v1.6 Phase 25
2. 运行 /gsd-plan-phase 25 — 创建 Phase 25 的执行计划
3. 按计划执行 Phase 25 的 4 个优化项
```

## Decisions Made

- v1.6 范围限定：只做 P0-P1 技术债务 + 测试基础，不做架构重构
- resize 毛玻璃方案：渐变过渡（Animation<double> 驱动 sigma）而非缓存上一帧
- 测试优先级：AutoHideController > ProgressBar > VolumeButton > SpeedButton
