# Changelog

## 0.0.8 · 设置面板停靠化 · XMB 式两级导航 🪟

> 一句话版本：**设置面板不再是弹窗——它成了一条窄面板：打开设置也能
> 拖动窗口，点分区详情从右滑入，键盘十字导航一步到位。**

### 🪟 设置面板 · 停靠化重构
- 修复：打开设置后自绘标题栏无法拖动（原 `showDialog` 全屏遮罩拦截
  手势；面板迁入控制层后无遮罩，标题栏拖动/双击最大化恢复正常）
- 面板停靠为控制栏上方区域的**中列**：与播放列表（右列）同层同槽位，
  宽度恒为槽位的**三分之一**（最小窗 854 下 ≈ 播放列表大小，1280 下
  ≈415px），高度撑满；播放列表开关不影响面板位置
- 面板视觉与开关动画对齐控制栏/播放列表同款：毛玻璃 + 蓝色微光边框
  + 22px 圆角，150ms 缓进缓出渐显

### 🎛 交互 · XMB 式两级覆盖导航
- 打开面板先见**竖排分区入口**（通用/视频/音频/关于）：撑满面板内容
  区、左右对称居中，宽度随窗口拉伸缩放响应联动
- 分区入口条目下有**条目摘要小字**：空间足够时静止完整显示，不够宽
  时从右向左循环滚动展示次要条目——窗口大小变化时滚动空间随之联动、
  能放得下就停止滚动，全程渲染层合成丝滑零卡顿
- 点击分区后**详细选项从右向左滑入覆盖**，分区入口后退（左移+微缩）
  并淡出至完全消失，内容层背后是干净的玻璃面板；退出分区反向回归
- 返回按钮就地出现在标题"设置"右侧；↑↓ 选条目（焦点行高亮）+
  Enter 激活开关；面板聚焦期间 Space 不再误触播放/暂停，**关面板后
  方向键立即恢复 seek/音量**
- 灰显分区（视频/音频）鼠标键盘均不可达，重新启用后自动纳入导航
- 去掉标题与分区的蓝色竖条装饰，分区-内容渐变分割线移至内容层左缘；
  字号/按钮/间距与播放列表面板完全对齐

### ⌨️ 键盘动线
- **→** 在分区入口直接进入内容层；内容层内 **→** 切换下一分区
- 内容层内 **←** 返回分区入口；↑↓ 选条目、Enter 激活开关不变
- **Esc 在任何层级都直接关闭整个设置面板**；面板开着时方向键仍不会
  误触发快进/快退

### 🖥 关于页
- 品牌行拆为两行自适应：窄面板下应用名不再被社交按钮挤压截断

### 🧹 内部 · 冗余清理
- 设置面板多轮演进积累的冗余清理：删除无消费者的对话框关闭回调
  通道、16 个早期设置面板几何时代的死设计令牌（全仓零引用实证）
- 新增循环跑马灯组件（空间足够静止/不足循环滚动/响应式重算），
  与控制栏标题跑马灯同族共享速度令牌
- 修复控制栏超长文件名跑马灯的存量缺陷：滚动时尾部内容此前永远
  显示不出来

### 🔧 修正
- **空置态（未加载媒体）下点击面板矩形外现在也能关闭浮层面板**（设置
  优先于播放列表）——此前空置态手势层为保护中央"打开文件"按钮整体
  禁用，副作用是面板外点击完全无响应；现改为面板开着时手势层只负责
  关面板，全关后恢复中央按钮交互
- 关闭设置面板后键盘焦点显式归还播放器（此前关闭后方向键可能失聪）
- 版本号标识补齐（关于页版本号此前落后于发版）

## 0.0.7 · 批量删除 · 缩略图新生 🚀

> 一句话版本：**列表支持批量删除了，缩略图更快更准还会自己认错。**

这一版两条主线：播放列表终于能批量删除了；缩略图子系统按工程规格
全面重写——更快、更准、出错可恢复，且严格遵守"缩略图为播放让路"。

### 🗑️ 播放列表 · 批量删除
- 条目右键菜单新增**批量删除**：进入多选模式，点击条目自由勾选
  （右上角红框标记），支持全选/反选，一次移除多个视频
- 删除前弹出确认条，明示：**仅从播放列表移除，不影响本地磁盘文件**
- 单条"移除"也走同一确认弹条——手滑多一层保险
- 删除后缩略图缓存同步清理（O(移除数)，绝不误伤未删条目）
- 删除正在播放的条目：引擎自动跳转下一首，断点不残留（孤儿防护）

### 🖼️ 缩略图 · 全面重写（P-Thumb v1.3.2）
- **内容感知缓存身份**（路径+大小+修改时间+规格）：文件被替换后
  缩略图正确刷新，旧版"换了内容还显示旧画面"的问题根除
- **快速滚动更省**：解帧并发限流（≤2）、相同条目请求自动合并为一次
  解帧、命中路径零磁盘访问——500 条列表滚动不再卡
- **失败可恢复**：解码失败的条目出现重试按钮，点击强制重新生成；
  磁盘写失败自动降级为内存展示，绝不让你看不到图
- **磁盘自动清理**：64 MiB / 512 条 / 30 天限额 + 水位删除，
  永不无限膨胀；损坏缓存文件读取时自动删除自愈
- **内存更省**：按显示尺寸解码，200 张同时驻留约省一半

### 🎨 界面 · 玻璃语言统一
- 右键菜单换成**控制栏同款毛玻璃**：悬停微亮、按下变暗，位置自动
  收进屏幕；危险操作（移除）图标红色标记
- 确认弹条为**长条玻璃浮层**，浮现于控制栏上方——不再是突兀的
  居中大对话框；按钮即控制栏同款小方块
- 批量选中的条目以**红色边框**标记，与播放中的蓝色严格区分语义
- 条目边框恒定占位：选中前后**尺寸零跳变**，列表不再抖动

### 🛠️ 内部
- 缩略图子系统 P-Thumb v1.3.2：Flight/Epoch 失效模型、有限并发闸、
  负缓存与重试、独立磁盘缓存层（temp→publish 原子写 + 4 字节 JPEG
  校验读写对称）、identity memo 零 stat 热路径——经独立代码审计修订
  （2 严重 + 5 高危全修）
- 批量移除与断点续播/持久化/锚点语义全面契合测试
- 全仓 dart format 收口、DCM 诊断清理
- 全量 1641 项测试护航，`flutter analyze` 零告警

---

## 0.0.6.2 · 它还记得你 🎯

> 一句话版本：**关掉再开，上次看到哪，一眼就找到。**

0.0.6 让断点"说到做到"，0.0.6.2 让它跨越开关软件——
你上次看到的地方，重启后一眼就能找到。

### 🎯 断点续播 · 跨会话现场恢复
- 重启软件后，播放列表自动**高亮上次播放的那一条**（白色标记）
- 点该条目的"断点续播"分区，一键从上次位置继续
- 退出软件前自动补一次进度落盘——播放中直接关窗，断点也不丢
- "记住播放位置"总开关关闭时，以上行为全部不生效

### 🎨 播放列表 · 三态视觉分级
- **播放中**：蓝色边框 + 蓝色名称
- **上次播放**（未播放时）：白色边框 + 白色名称
- **鼠标悬停**：整卡微微泛白——纯定位反馈，不与状态高亮抢层级

### 🖱️ 光标 · 全界面统一食指
- 修复控制栏全部按钮、进度条、倍速、音量、标题栏窗口按钮
  （置顶/最小化/最大化/关闭）、播放列表条目、设置面板交互件的
  悬停光标——此前这些位置悬停显示的是箭头
- 禁用状态（如未加载媒体的进度条）仍为箭头，语义正确

### 🛠️ 内部
- 播放列表存储升级 v3：新增"上次播放"锚点，旧文件无缝迁移
- 静态分析诊断全清零（lib 逐项修复 + 测试惯例豁免）
- 全量 1575 项测试护航，`flutter analyze` 零告警

### ⚠️ 本期取舍
- 切换视频时分辨率闪变的根除方案已设计定稿，因需要更充分的实机
  验证暂缓发布（下版本见）

---

## 0.0.6 · 各归其位 🗂

> 一句话版本：**随机不再乱动你的列表，断点这次说到做到。**

0.0.5 把播放列表请了回来，0.0.6 专门收拾它留下的几件"说到没做到"的事：
随机播放悄悄打乱列表、停止按钮弄丢断点、切视频标题装作没看见。
顺路把排序、持久化和韩语日语一并办了。

---

### 🔀 播放列表排序 · 新能力
- 面板顶栏新排序按钮（控制栏同款方块造型）：**添加顺序 / 文件名 /
  最后播放 / 时长**，任意升降序，同键再点即反向
- 排序即真实重排队列：**面板顺序 = 播放顺序 = 保存顺序**，切换播放
  永不打断，排序结果持久化，重启后原样奉还
- 文件名排序走自然序——`第2集` 排在 `第10集` 前面，资源管理器惯例

### 🎲 随机播放 · 不再"顺手"打乱列表
- **修复**：此前开启随机会物理打乱整个列表、还把乱序存盘；
  现在随机只是播放顺序，列表纹丝不动（VLC / IINA 同款惯例）
- 随机也终于名副其实：下一曲随机选（避开刚播过的），上一曲沿
  播放历史**精确回退**，播完自动随机接续

### ⏯ 断点续播 · 说到做到
- **修复**：点停止竟然会把断点清零——现在停止 = 记下此刻位置
- 看到片尾 5 秒内自动清除断点，不留"从 23:59 继续"的尴尬
- 播放进度每 5 秒落盘，强杀进程最多丢 5 秒
- 通用设置新增「记住播放位置」总开关

### 🎛 控制栏 · 该在的都在
- **修复**：未播放时控制栏不再自动消失——那是唯一的入口
- **修复**：播放列表切换视频时，标题终于跟着换了
  （此前永远停留在打开文件时的那一个）
- 顶栏三个按钮统一为控制栏同款方块风格

### 🌐 语言 · 4 门
- 新增 **한국어 / 日本語** 全量翻译，语言切换即点即生效

### 🛡 稳定性与内部
- 打开失败连锁熔断：目录整体不可访问时不再狂刷 90 秒错误，
  连续 3 次失败即干净停止并聚合上报；错误报告带上失败路径
  与 mpv 内部日志
- 播放偏好全面持久化：音量 / 静音 / 倍速 / 硬解重启后原样奉还
- 控制栏显隐热路径瘦身（事件时间戳节流、字幕空写守卫、按钮树
  跨显隐复用），枢纽文件 1083 → 838 行
- `playlist.json` 升级 v2（旧文件无缝迁移）

### ⚠️ 本期取舍
- 设置的**视频 / 音频调节分区暂缓开放**（mpv 属性链路实机验证
  未完成，代码已就位），均衡器（EQ）延后
- 全量 1547 项测试护航，`flutter analyze` 零告警

---

## 0.0.5 · 播放列表，全面回归 🎬

> 一句话版本：**你双击的那个文件夹，现在是一部连续剧。**

时隔数个版本，播放列表以全新的架构强势回归——这一次，我们把队列交给
媒体引擎内核（mpv）20 年久经考验的原生_playlist_ 管理，自己只做真正
值得做的事：记住你看过的每一帧，并在你想继续时，一键接上。

---

### ✨ 全新功能

#### 🎯 智能播放队列 · 相册式连播
- **双击任意视频，整部文件夹自动入列** —— 打开 `第01集.mp4`，同目录下
  的所有影片按文件名自动排成队列，从它开始连播，播完自动接下一集
- 拖入多个文件 → 按拖入顺序整体装载；拖入单个 → 同目录智能装载
- 网络流地址（http/rtsp）自动识别，走单元素队列
- 队列里混入坏文件？播放器内核会**自动跳过它继续播**，永不卡死

#### 🪟 右侧沉浸式播放列表面板
- **控制栏同款圆角毛玻璃**：同装饰、同圆角、同边框、同模糊档位——
  视觉上就是控制栏的孪生兄弟
- 280px 窄条贴右而立，**底部精确避开控制栏**，边看边选两不误
- **全屏状态下照常可用**：面板住进了渲染链路，全屏沉浸时呼之即来
- 三种关闭方式随心选：`Esc` / 点面板外任意处 / 关闭按钮
- 进出场是控制栏同款的渐进渐退，透明度过渡期间自动停用玻璃采样——
  **不闪、不抖、不留残影**

#### 🫧 透明"塑料膜"按钮 · 隐形的双手
缩略图上覆盖着一层看不见的膜，上下两片：上片**播放**，下片**断点续播**。
- 静置时**完全隐形**——画面就是画面，没有任何按钮抢戏
- 鼠标悬停 1 秒：图标如呼吸般浮现，居中亮起
- 再过 1 秒：**"播放" / "断点续播"字样渐进舒展**，图标被自然推向左侧
- 悬停分区微亮（白 6%），按下变暗（黑 12%）——反馈即时，却轻得
  像一层膜，绝不喧宾夺主
- 鼠标移开，一切反向渐退，缩略图恢复完整

#### ⏯ 断点续播 · 从上次的地方继续
- 每个视频自动记录播放位置与总时长，切走、关窗都不丢
- 面板里每个条目右侧显示**断点细进度条 + 「断点 xx:xx」**
- 无断点的条目，续播按钮自动禁用——不给误导性交互
- 精确到帧的落点：续播信号与新片源起播信号严格锁存对齐，
  不会"看起来续了、实际从头播"

#### 🖼 缩略图 · Windows 首次全面支持
- 采用 **Media Foundation 系统解码器**直接解帧（与播放器解码能力同源，
  能播的就能出图），WIC 硬件级编码
- 取 1 秒处的画面，巧妙避开常见的黑屏首帧
- **MD5 指纹磁盘缓存**：生成一次，重启秒加载
- 滚动列表零阻塞：缩略图异步加载，失败静默降级为占位图

#### 🎛 控制栏与快捷键升级
- 中央控制组新增 **⏮ 上一个 / ⏭ 下一个**，紧贴播放键两侧——
  叫"上一个/下一个"而非"上一曲"，视频与音乐两相宜
- **L** 键或控制栏按钮开关播放列表；**N / P** 键快速切换队列条目
- 播放模式一键循环：**全部循环 → 单曲循环 → 随机播放**，
  随机由 mpv 原生 `playlist-shuffle` 驱动，乱序循环不打断

### 🔧 体验修复

- 🌑 **对齐媒体引擎原生交互**：暂停状态静置数秒，控制栏同样会优雅
  收起（此前"暂停永显"与引擎原生行为相悖）；进度条拖拽、音量调节
  的保活逻辑原样保留
- 🪟 **模态窗口保活**：设置窗口/右键菜单开启期间，控制栏不再
  "自顾自消失"；关窗后重新开始计时
- 👻 **杜绝幽灵点击**：面板关闭的瞬间即让出全部命中区域——
  渐隐动画再快，也点不到已经看不见的东西
- 🎞 渐入渐出全程无闪烁：玻璃模糊采样随透明度自动启停

### ⚙️ 工程亮点（写给懂行的人）

- **队列权威下沉 mpv C 层**：`open(Playlist)` / `add` / `remove` /
  `jump` / `setPlaylistMode` / `setShuffle` 全部走官方公开 API——
  自研 312 行队列状态机光荣退役，软件杠杆原则的教科书案例
- 引擎接口新增第 8 个 ISP 切面 **QueueControl**：队列镜像
  (`queuePaths` / `queueIndex`) + `queueRevision` 单通知点，
  从根本上杜绝"新列表 × 旧索引"的中间态竞态
- 断点归属以 **path 为键**（索引在整队列替换时会错位，path 恒稳）；
  持久化为人人可读的纯文本 JSON（Unix 原则：flat text files）
- **1448 项自动化测试全绿**，`flutter analyze` 零告警
- 设计系统单一事实源收紧：设置面板圆角/模糊改绑控制栏同源 Token

### 🗺 下一步预告

- 文件夹分组视图 · 播放历史页签
- 面板拖宽 · 条目拖拽排序

---

## 1.0.0

### 功能
- 视频播放：基于 fvp (MDK/FFmpeg) 的桌面媒体播放器，支持主流视频/音频格式
- 播放列表：添加/删除/清空，4 种播放模式（顺序/全部循环/单曲循环/随机）
- AB 循环：3 按钮状态（设A点 → 设B点激活 → 清除），支持键盘快捷键
- 音轨切换：多音轨视频的音轨选择
- 字幕支持：外挂字幕加载、字幕轨道切换
- 均衡器：10 频段 EQ 预设（流行/摇滚/古典/爵士/低音增强/人声增强）
- 播放历史：MRU 播放记录，最多 50 条，持久化存储
- 缩略图预览：进度条悬停显示关键帧缩略图（LRU 缓存 60 帧）
- 文件拖放：拖拽文件到窗口添加到播放列表
- 窗口自适应：视频打开后自动调整窗口至原始分辨率，保持宽高比

### UI/UX
- 毛玻璃控制栏：BackdropFilter 磨砂效果，自动隐藏（播放中 5s/全屏 3s）
- 星河背景动画：空闲状态流星星河粒子效果
- OSD 浮动提示：音量/全屏/静音等操作反馈
- 空状态引导：毛玻璃按钮行（打开文件/播放记录/设置）
- 主题系统：4 套内置主题（午夜蓝/暗夜紫/极光绿/日落橙），300ms 过渡动画
- 设置面板：Tab 式对话框，音量/播放/外观分组

### 快捷键
- 空格：播放/暂停
- 左/右：前进/后退 5 秒
- 上/下：音量 ±5%
- F：全屏切换
- M：静音切换
- N/P：上/下一首
- O：打开文件
- A/B：AB 循环
- S：字幕切换
- ESC：退出全屏

### 工程
- 测试覆盖：89 个单元测试（Playlist/PlaylistItem/time_utils/SettingsStorage/HistoryItem）
- 架构：ValueNotifier 响应式，ThemeConfig 编译时常量 Token
- 设计系统：50 个语义 Token，单一 Midnight 主题
- 持久化：SharedPreferences（设置）+ JSON 文件（播放列表/历史）
