import 'package:flutter_test/flutter_test.dart';
import 'package:simple_player_flutter/kernel/engine/engine_state.dart';

/// VideoEffectControl contract tests (D13 parameterized over a factory).
///
/// Wave 0 stub — Task 3 replaces this body with the full contract suite.
void runVideoEffectControlContractTests(MediaEngine Function() createEngine) {
  group('VideoEffectControl contract (stub — Task 3 fills this in)', () {
    test('placeholder', () {
      expect(createEngine, isNotNull);
    });
  });
}
