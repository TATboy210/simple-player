import 'package:flutter_test/flutter_test.dart';
import 'package:simple_player_flutter/kernel/engine/engine_state.dart';

/// RendererControl contract tests (D13 parameterized over a factory).
///
/// Wave 0 stub — Task 3 replaces this body with the full contract suite.
void runRendererControlContractTests(MediaEngine Function() createEngine) {
  group('RendererControl contract (stub — Task 3 fills this in)', () {
    test('placeholder', () {
      expect(createEngine, isNotNull);
    });
  });
}
