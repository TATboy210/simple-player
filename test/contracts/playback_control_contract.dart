import 'package:flutter_test/flutter_test.dart';
import 'package:simple_player_flutter/kernel/engine/engine_state.dart';

/// PlaybackControl contract tests (D13 parameterized over a factory).
///
/// Wave 0 stub — Task 2 replaces this body with the full contract suite,
/// including the mandatory "open to play handoff" regression gate.
void runPlaybackControlContractTests(MediaEngine Function() createEngine) {
  group('PlaybackControl contract (stub — Task 2 fills this in)', () {
    test('placeholder', () {
      expect(createEngine, isNotNull);
    });
  });
}
