import 'package:flutter_test/flutter_test.dart';
import 'package:simple_player_flutter/kernel/engine/engine_state.dart';

/// TrackControl contract tests (D13 parameterized over a factory).
///
/// Wave 0 stub — Task 3 replaces this body with the full contract suite.
void runTrackControlContractTests(MediaEngine Function() createEngine) {
  group('TrackControl contract (stub — Task 3 fills this in)', () {
    test('placeholder', () {
      expect(createEngine, isNotNull);
    });
  });
}
